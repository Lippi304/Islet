import SwiftUI

struct SettingsView: View {
    var body: some View {
        // Placeholder — Plan 02 adds the Launch-at-Login toggle + version label.
        Text("Islet Settings")
            .padding(20)
            .frame(width: 360)
    }
}
