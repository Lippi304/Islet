---
phase: quick-260731-61m
plan: 01
subsystem: ui
tags: [swiftui, notch, osd, animation, debug-tooling]

requires:
  - phase: 72.1
    provides: OSDLevelBar opacity ramp and DigitRollText digit-roll counter this reverts/retunes
provides:
  - OSDLevelBar fill Capsule always renders at full opacity, no ramp
  - DigitRollText retuned to a slower/smoother base spring (0.30 response)
  - Dead opacity-nudge DEBUG plumbing fully deleted across 4 files
affects: [osd-hud, debug-menu]

tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - Islet/Notch/NotchPillView.swift
    - Islet/ActivitySettings.swift
    - Islet/AppDelegate.swift
    - IsletTests/ActivitySettingsTests.swift

key-decisions:
  - "Kept the pre-existing .animation(.spring(response: 0.15, dampingFraction: 0.86), value: fraction) line on OSDLevelBar's fill Capsule -- it drives the width animation and pre-dates Phase 72.1, confirmed via git show f8a9a21"
  - "Committed as 2 atomic tasks matching the plan: opacity-ramp revert + dead plumbing deletion (Task 1), DigitRollText spring retune (Task 2), by temporarily reverting/reapplying the single-line rollResponse change to isolate the diffs"

patterns-established: []

requirements-completed: []

duration: 12min
completed: 2026-07-31
---

# Quick Task 260731-61m: Revert OSD bar opacity ramp to always-full-opacity Summary

**OSDLevelBar fill Capsule reverted to always-full-opacity (Phase 72.1's D-01/D-02/D-03 ramp deleted along with its DEBUG plumbing); DigitRollText's base spring response raised 0.15 -> 0.30 for a slower/smoother glide, move-only transitions untouched**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-31T13:54:00Z
- **Completed:** 2026-07-31T14:06:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- `OSDLevelBar`'s fill `Capsule` has no `.opacity(...)` modifier at all -- full alpha at every fraction, identical for Volume/Brightness
- All opacity-ramp DEBUG plumbing removed: `opacityMinNudge`/`opacityMaxNudge` struct params, `osdOpacityMinNudge`/`osdOpacityMaxNudge` computed properties, `debugOSDOpacityMinNudge`/`debugOSDOpacityMaxNudge` `@AppStorage` vars, `debugOSDOpacityMinNudgeKey`/`debugOSDOpacityMaxNudgeKey` static keys, their 4 menu items + 4 action functions, and their 2 unit tests
- Roll Speed DEBUG nudge and Reset/Print OSD Tuner controls trimmed to roll-speed-only and remain fully functional
- `DigitRollText`'s base `rollResponse` raised from 0.15 to 0.30 at the `osdWings(for:)` call site -- noticeably slower/smoother glide, Roll Speed nudge still applies on top
- `DigitRollText`'s zero-fade, move-only transition guarantee (D-04a) fully preserved -- no opacity/fade modifier touched

## Task Commits

Each task was committed atomically:

1. **Task 1: Revert OSDLevelBar opacity ramp and delete dead opacity-nudge plumbing** - `07df8c1` (fix)
2. **Task 2: Retune DigitRollText spring for a smoother/slower glide, no fade regression** - `3729334` (feat)

**Plan metadata:** pending (docs: complete plan, added by orchestrator)

## Files Created/Modified
- `Islet/Notch/NotchPillView.swift` - `OSDLevelBar` opacity ramp deleted (struct params + locals + `.opacity(...)` line), reverted to 2-arg call site; `osdOpacityMinNudge`/`osdOpacityMaxNudge` computed properties and their backing `@AppStorage` vars deleted; `DigitRollText`'s call-site `rollResponse` base raised 0.15 -> 0.30
- `Islet/ActivitySettings.swift` - `debugOSDOpacityMinNudgeKey`/`debugOSDOpacityMaxNudgeKey` static keys deleted, `debugOSDRollSpeedNudgeKey` kept
- `Islet/AppDelegate.swift` - 4 Opacity Min/Max menu items and their 4 action functions deleted; `debugOSDTunerReset`/`debugOSDTunerPrint` trimmed to roll-speed-only
- `IsletTests/ActivitySettingsTests.swift` - `testOSDOpacityMinNudgeKeyName`/`testOSDOpacityMaxNudgeKeyName` deleted, `testOSDRollSpeedNudgeKeyName` kept

## Decisions Made
- Kept the fill `Capsule`'s pre-existing `.animation(.spring(response: 0.15, dampingFraction: 0.86), value: fraction)` line untouched -- it drives the width-fill animation and pre-dates Phase 72.1 (confirmed via `git show f8a9a21`), not part of the opacity ramp being reverted
- Split the single-file `NotchPillView.swift` changes into 2 atomic commits matching the plan's 2 tasks by temporarily reverting the `rollResponse: 0.30` line before committing Task 1, then reapplying it for Task 2's isolated commit

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Deleted dangling `@AppStorage` declarations not covered by the plan's interfaces excerpt**
- **Found during:** Task 1 verification (post-edit grep for `opacityMinNudge`/`opacityMaxNudge`)
- **Issue:** `NotchPillView.swift` lines 190-191 held `@AppStorage(ActivitySettings.debugOSDOpacityMinNudgeKey) private var debugOSDOpacityMinNudge: Double = 0` and the matching `Max` line -- these referenced the just-deleted `ActivitySettings` static keys and were not included in the plan's interfaces excerpt (which only showed the read-point computed properties and call sites). Left in place, the build would fail with "cannot find type" / unresolved member errors.
- **Fix:** Deleted both `@AppStorage` lines, trimmed the preceding comment to describe roll-speed-only storage, matching the same pattern applied to the read points and `ActivitySettings` keys elsewhere in Task 1.
- **Files modified:** Islet/Notch/NotchPillView.swift
- **Verification:** `grep -rn 'opacityMinNudge\|opacityMaxNudge\|OpacityMin\|OpacityMax'` across Islet/ and IsletTests/ returns 0 matches; `xcodebuild build` succeeds clean.
- **Committed in:** 07df8c1 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary for the build to compile at all -- the plan's interfaces excerpt didn't enumerate every reference to the deleted keys. No scope creep; purely completing the deletion the plan already specified.

## Issues Encountered
None beyond the deviation above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- OSD bar and digit-roll counter both build clean and pass `ActivitySettingsTests` with zero dangling references
- Ready for on-device manual UAT of the new always-full-opacity bar and the slower digit-roll glide (no automated test covers visual/animation feel, consistent with 72.1's own scoping)

---
*Phase: quick-260731-61m*
*Completed: 2026-07-31*

## Self-Check: PASSED

All claimed commit hashes (`07df8c1`, `3729334`) and all 4 claimed modified files verified present.
