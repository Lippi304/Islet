---
phase: quick-260729-4oi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Notch/NotchPillView.swift
  - Islet/Assets.xcassets/BluetoothGlyph.imageset/Contents.json
  - Islet/Assets.xcassets/BluetoothGlyph.imageset/icons8-bluetooth-48.png
autonomous: false
requirements: []
user_setup: []
---

<objective>
Wing Tuner rounds 7-8 (260728-wg7 -> ... -> 260729-47v -> this): executed directly (no separate
planner/executor spawn) given the small, well-understood, mechanical nature of each piece and the
established pattern from 6 prior rounds in this series. Live feedback arrived in 3 waves within the
same session — see 260729-4oi-SUMMARY.md for the full breakdown (Charging tuned deltas + Bluetooth
real-icon asset + Charging single-icon redesign, then Timer tuned deltas + Countdown margin
mechanism, added mid-turn).
</objective>

<tasks>

<task type="auto">
  <name>Bake in Charging's tuned deltas, replace Device's hand-drawn Bluetooth Path with the
  user-supplied reference icon asset, redesign Charging to a single battery+bolt icon with no
  percentage, bake in Timer's tuned deltas, convert Countdown to the margin+cameraBlockWidth
  formula</name>
  <files>Islet/Notch/NotchPillView.swift, Islet/Assets.xcassets/BluetoothGlyph.imageset/*</files>
  <action>See 260729-4oi-SUMMARY.md for the complete, itemized breakdown of every change.</action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED" && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Release 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <done>Both Debug and Release builds succeed. See SUMMARY.md for full itemized verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>See 260729-4oi-SUMMARY.md.</what-built>
  <how-to-verify>
Open Islet.xcodeproj in Xcode, Cmd-R (Debug). Open the debug menu -> Wing Tuner + Preview Wing.
1. Preview: Charging -> confirm a single battery+bolt icon (no plug, no percentage number).
2. Preview: Device -> confirm the real Bluetooth icon (not the hand-drawn approximation) at a
   reasonable size.
3. Preview: Timer -> confirm it matches the round-8 tuning session.
4. Preview: Countdown (or trigger a real calendar countdown) -> confirm Margin now visibly
   affects it (previously did nothing).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue.</resume-signal>
</task>

</tasks>

<output>
Summary already written to 260729-4oi-SUMMARY.md.
</output>
