---
phase: quick-260729-5pv
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Notch/NotchPillView.swift
autonomous: false
requirements: []
user_setup: []
---

<objective>
Follow-up polish on the OSD (Brightness/Volume) percent number added in quick task
260729-5jc: the number swap between old and new values was a hard cut with no transition,
and the gap between the level bar and the number (4pt) felt too tight. Executed directly
given the small, well-understood, mechanical nature of the change.
</objective>

<tasks>

<task type="auto">
  <name>Widen bar-to-number gap and add a smooth digit transition in osdWings(for:)</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <action>See 260729-5pv-SUMMARY.md for the complete detail.</action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED" && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Release 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <done>Both Debug and Release builds succeed. percentGap is 10pt. The percent Text has a numericText content transition driven by its own local spring animation, mirroring OSDLevelBar's existing per-value animation convention.</done>
</task>

</tasks>

<output>
Summary already written to 260729-5pv-SUMMARY.md.
</output>
