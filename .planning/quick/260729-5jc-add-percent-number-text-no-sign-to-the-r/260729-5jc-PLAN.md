---
phase: quick-260729-5jc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Notch/NotchPillView.swift
autonomous: true
requirements: []
user_setup: []
---

<objective>
Add a plain percent number (0-100, no "%" sign) to the right of the OSD (Volume/Brightness)
wing's existing `OSDLevelBar` fill bar, before the wing's trailing pad. The wing's right-side
footprint (`rightWidth`/`totalWidth`) grows to fit the new text — it is not squeezed into the
existing trailing padding.

Purpose: user-requested numeric readout alongside the existing minimal bar, so the exact level
is visible at a glance instead of only inferred from bar fill.

Output: `osdWings(for:)` in `Islet/Notch/NotchPillView.swift` renders icon — camera gap — bar —
fixed gap — percent number — trailing pad.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Target function: `osdWings(for activity: OSDActivity)` in
`Islet/Notch/NotchPillView.swift` (currently lines 3573-3708).

<interfaces>
The `percent` local variable already exists at the top of this function (from the
`OSDActivity` switch, lines 3574-3588) — reuse it directly, do not recompute.

Existing DEBUG-only live-tuning properties, already used by every wing in this file
(`wingLeadingNudge`, `wingTrailingNudge`, `wingMarginNudge`, `wingGapNudge` — lines 191-219):
`#if DEBUG` each returns a live `@AppStorage`-backed `CGFloat`; `#else` returns `0`. This task
does NOT add a new nudge axis — the new number's spacing needs are covered by a fixed gap
constant plus the wing's existing `trailingPad` (line 3655: `24 * wScale + wingTrailingNudge`).

Current width math (lines 3654-3662) to extend:
```
let barWidth: CGFloat = 90
let trailingPad: CGFloat = 24 * wScale + wingTrailingNudge
...
let totalWidth = iconLeadingPad + iconWidth + cameraBlockWidth + barWidth + trailingPad
let rightWidth = totalWidth - leftWidth
```

Current HStack tail (lines 3695-3703), the exact insertion point:
```
OSDLevelBar(fraction: fraction, tint: tint)
    .frame(width: barWidth, height: 5)
    .modifier(OSDFrameLogger(...))
    .modifier(OSDFrameLogger(...))
Color.clear.frame(width: trailingPad)
```

Nearby reference for a small white monospaced-digit `Text` on this exact wing style
(`UpdateVersionPill`, ~line 4788): `.font(.system(size: 11, weight: .semibold, design: .rounded))`
+ `.monospacedDigit()` + `.foregroundStyle(.white)`. This task uses `design: .monospaced` on the
font itself instead (equivalent digit-width guarantee, one modifier instead of two) — either is
acceptable per the task brief; this plan picks the single-modifier form.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add percent number to OSD wing, widening the wing's right footprint</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <action>
In `osdWings(for:)`, immediately after the `let barWidth: CGFloat = 90` line, add two new fixed
constants: `percentGap` (small fixed gap between the bar and the number, e.g. 4pt — NOT a new
nudge axis) and `percentTextWidth` (fixed content-width budget for up to 3 digits, e.g. 22pt —
a fixed constant added into the width math, mirroring how `barWidth` is already a fixed constant
there). Extend the `totalWidth` computation to add `percentGap + percentTextWidth` between
`barWidth` and `trailingPad`. Leave `leftWidth` and every left-side value untouched — this is a
right-side-only footprint change. `rightWidth` is derived from `totalWidth - leftWidth` and will
grow automatically.

In the HStack body, immediately after the existing `OSDLevelBar(...)` element's two
`OSDFrameLogger` modifiers and before the trailing `Color.clear.frame(width: trailingPad)`
line, insert: a `Color.clear.frame(width: percentGap)` spacer, followed by
`Text("\(percent)")` (reusing the function's existing `percent` variable — never append a "%"
character) styled with a monospaced-digit font (e.g.
`.font(.system(size: 11, weight: .semibold, design: .monospaced))`) and
`.foregroundStyle(.white)` per this wing's D-02 fixed-color rule, given a fixed
`.frame(width: percentTextWidth, alignment: .leading)` so its width doesn't shift the layout as
the digit count changes between 1-3 digits. Do not add `OSDFrameLogger` instrumentation to the
new text — that modifier is specific to the camera-cutout-clearance saga documented in this
function's comments and does not apply here. Do not touch `iconLeadingPad`, `cameraBlockWidth`,
`leftWidth`, `margin`, or any DEBUG print/assert statement's logic — they already read the
updated `totalWidth`/`rightWidth` values automatically.
</action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED" && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Release 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <done>Both Debug and Release builds succeed. `osdWings(for:)` renders a plain percent number
(no "%" sign, reusing the existing `percent` variable) to the right of `OSDLevelBar`, before
`trailingPad`, and the wing's `totalWidth`/`rightWidth` account for the added gap + text width
so the capsule visually grows rather than clipping or overlapping the existing trailing pad.</done>
</task>

</tasks>

<verification>
Build both configurations (Debug and Release) via `xcodebuild build`. Do not run `xcodebuild
test` (documented elsewhere in this project as headless-hang-prone). User will live-tune exact
spacing/widths afterward via the existing on-device DEBUG "Wing Tuner" mechanism
(`AppDelegate.swift`) — this plan only needs a reasonable, non-clipping baseline.
</verification>

<success_criteria>
- `osdWings(for:)` shows icon — camera gap — bar — gap — percent number — trailing pad
- Percent number text is exactly `percent` with no "%" character appended
- Wing's `totalWidth`/`rightWidth` include the new gap + text width (not squeezed into existing
  `trailingPad`)
- Debug and Release builds both succeed
</success_criteria>

<output>
Create `.planning/quick/260729-5jc-add-percent-number-text-no-sign-to-the-r/260729-5jc-SUMMARY.md` when done
</output>
