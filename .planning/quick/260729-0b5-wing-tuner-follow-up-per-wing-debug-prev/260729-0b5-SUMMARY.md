---
phase: quick-260729-0b5
plan: 01
subsystem: ui
tags: [swiftui, appkit, debug-tooling, notch, wing-tuner]

# Dependency graph
requires:
  - phase: quick-260728-wg7
    provides: DEBUG-only Wing Tuner live-nudge mechanism (4 nudge axes, wingLeadingNudge/wingTrailingNudge/wingMarginNudge/wingGapNudge as @AppStorage-backed properties on NotchPillView)
provides:
  - focusWings(for:) (DND wing) real margin constant wired to wingMarginNudge
  - Wing Tuner Margin axis with 6 step buttons (-20/-10/-5/+5/+10/+20)
  - "Preview Wing" debug submenu (12 actions) fake-triggering every collapsed-state HUD wing
  - 8 NotchWindowController handler functions widened private -> internal for cross-file debug calls
  - 2 new NotchWindowController wrapper functions (debugPreviewDevice/debugPreviewDownload)
  - Decluttered 🐞 debug menu (10 "Spike: ..." items + functions removed, orphaned debugClipboardMonitor property removed)
affects: [future debug-tooling quick tasks, any future wing-layout tuning work]

# Tech tracking
tech-stack:
  added: []
  patterns: [debug-only fake-trigger actions mirroring an existing per-activity precedent, one function per activity rather than a generic dispatcher]

key-files:
  created: []
  modified:
    - Islet/Notch/NotchPillView.swift
    - Islet/Notch/NotchWindowController.swift
    - Islet/AppDelegate.swift

key-decisions:
  - "focusWings(for:) keeps leftWidth/rightWidth (118/160 * widthGrowth) byte-for-byte unchanged; margin (175 + wingMarginNudge) is a best-effort estimate of the old Spacer()'s natural fill, not a live-measured value, pending the on-device checkpoint"
  - "Preview Wing built as 12 discrete one-line-body functions (mirroring debugSpikeSimulateUpdateAvailable's existing shape), not a generic parameterized dispatcher, per the plan's explicit instruction"
  - "debugClipboardMonitor property removed as truly orphaned (only referenced by the 2 removed clipboard-monitor spike functions); its one remaining doc-comment cross-reference was also updated instead of left dangling"

requirements-completed: []

# Metrics
duration: ~5min (4 auto tasks)
completed: 2026-07-29
---

# Quick Task 260729-0b5: Wing Tuner Follow-Up (Per-Wing Debug Preview) Summary

**DND wing margin bug fixed with a real nudgeable constant, Wing Tuner Margin axis widened to 6 steps, a 12-action "Preview Wing" debug submenu added to fake-trigger every collapsed HUD wing on demand, and the 🐞 debug menu decluttered of its 10 one-off Spike items.**

## Performance

- **Duration:** ~5 min (4 auto tasks, no checkpoint reached)
- **Started:** 2026-07-29T00:29:31+02:00 (first task commit)
- **Completed:** 2026-07-29T00:34:14+02:00 (last task commit, on-device checkpoint still pending)
- **Tasks:** 4/5 (Tasks 1-4 complete; Task 5, the on-device checkpoint, deliberately not executed — requires the user on real hardware)
- **Files modified:** 3

## Accomplishments
- `focusWings(for:)` (DND wing) no longer uses a bare, un-nudgeable `Spacer()` — it now has an explicit `margin: CGFloat = 175 + wingMarginNudge` constant, closing the one reported bug from 260728-wg7 (Margin nudge did nothing on the DND wing)
- Wing Tuner's Margin axis widened from 2 buttons (±5) to 6 (-20/-10/-5/+5/+10/+20)
- New "Preview Wing" submenu (12 actions) fake-triggers Now Playing, OSD Volume, OSD Brightness, Focus/DND, Caps Lock, Charging, Device, Update, Download, Timer, Countdown, and Meeting wings without needing the real underlying system condition
- 8 `NotchWindowController` handler functions widened `private` -> internal (mirroring the existing `handleUpdateAvailable` precedent) plus 2 new thin wrapper functions (`debugPreviewDevice`, `debugPreviewDownload`) reaching the private `deviceCoordinator`/`downloadCoordinator`
- 🐞 debug menu decluttered: 10 "Spike: ..." items and their backing functions removed, plus the now-orphaned `debugClipboardMonitor` property

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix focusWings — give DND wing a real, nudgeable margin** - `5410e3a` (fix)
2. **Task 2: Wing Tuner — add Margin ±10/±20 step buttons** - `35963a3` (feat)
3. **Task 3: Add per-wing "Preview Wing" debug submenu** - `cb93b56` (feat)
4. **Task 4: Declutter debug menu — remove the 10 Spike items** - `91ebbba` (refactor)

**Plan metadata:** committed separately by the orchestrator (this executor does not commit docs artifacts).

_Note: no TDD tasks in this plan — all 4 were single-commit `auto` tasks._

## Files Created/Modified
- `Islet/Notch/NotchPillView.swift` - `focusWings(for:)` gained an explicit `margin` constant, replacing its bare `Spacer()`
- `Islet/Notch/NotchWindowController.swift` - 8 handler functions widened `private` -> internal; 2 new thin wrapper functions (`debugPreviewDevice`, `debugPreviewDownload`)
- `Islet/AppDelegate.swift` - Wing Tuner Margin block expanded to 6 buttons; new "Preview Wing" submenu (12 items + 12 backing functions); 10 "Spike: ..." items + functions removed; orphaned `debugClipboardMonitor` property removed

## Decisions Made
- Kept `leftWidth`/`rightWidth` in `focusWings(for:)` completely unchanged (118/160 * widthGrowth) rather than migrating to the `osdWings`/`capsLockWings` notch-cutout-derived `cameraBlockWidth` formula, per the plan's explicit reasoning (that formula would visibly widen this specific wing)
- Built all 12 Preview Wing actions as separate one-line-body functions rather than a single generic/parameterized dispatcher, matching the plan's explicit instruction and the existing `debugSpikeSimulateUpdateAvailable`-style precedent
- Removed the `debugClipboardMonitor` property's dangling doc-comment reference (in the production `clipboardStore` comment) rather than leaving a comment pointing at deleted code — a same-file consistency fix, not a scope expansion

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Removed 2 stale comment references to the deleted `debugSpikeSimulateUpdateAvailable` function name**
- **Found during:** Task 4 (post-removal grep check)
- **Issue:** The plan's own done-criteria required zero occurrences of the 10 removed `debugSpike*` function names anywhere in the file. Two doc comments (on the new "Preview Wing" submenu construction and the new Preview action functions) referenced `debugSpikeSimulateUpdateAvailable()` by name as the "precedent" being mirrored, added during Task 3 before Task 4 removed that function.
- **Fix:** Reworded both comments to describe the precedent without naming the now-deleted function.
- **Files modified:** `Islet/AppDelegate.swift`
- **Verification:** `grep -c "Spike:" Islet/AppDelegate.swift` → 0; `grep -n "debugSpike" Islet/AppDelegate.swift` → no matches. Debug and Release builds re-verified BUILD SUCCEEDED after the fix.
- **Committed in:** `91ebbba` (part of Task 4 commit)

**2. [Rule 1 - Bug] Updated the `clipboardStore` doc comment that referenced the removed `debugClipboardMonitor` property**
- **Found during:** Task 4 (removing the orphaned property)
- **Issue:** A comment on `private var clipboardStore` ("Distinct from the #if DEBUG-only `debugClipboardMonitor` below") would have gone stale/dangling once that property was deleted.
- **Fix:** Trimmed the comment to drop the now-invalid cross-reference, keeping the still-accurate part (production clipboard wiring description).
- **Files modified:** `Islet/AppDelegate.swift`
- **Verification:** Build succeeded; `grep -n "debugClipboardMonitor" Islet/AppDelegate.swift` shows zero remaining references after the property itself was removed.
- **Committed in:** `91ebbba` (part of Task 4 commit)

---

**Total deviations:** 2 auto-fixed (both Rule 1, same-file comment-accuracy fixes directly caused by Task 4's own removals)
**Impact on plan:** Both fixes are strictly within Task 4's own scope (the file it was already editing) and needed to satisfy the plan's own literal done-criteria ("zero occurrences ... of the 10 removed debugSpike* function names"). No scope creep.

## Issues Encountered
None - both build gates (Debug per-task, Release on Task 4) passed on first or second (post-deviation-fix) attempt with no other errors.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness

**Task 5 (on-device checkpoint) is pending — NOT executed by this run.** Per the calling instructions, the final `checkpoint:human-verify` task in `260729-0b5-PLAN.md` requires the user to open `Islet.xcodeproj`, run the app (Cmd-R, Debug), and manually verify:
1. The 🐞 menu shows exactly 3 "Debug: ..." items + "Wing Tuner" + "Preview Wing" submenus, no "Spike: ..." items
2. "Preview: Focus/DND" renders the DND wing, and the new Margin -20/-10/-5/+5/+10/+20 buttons visibly move the icon-to-label gap live (the originally reported bug)
3. All 11 other Preview Wing actions render their respective wings without crashing
4. No other wing's layout changed incidentally
5. "Preview: Update" still works after quitting/reopening (replaces the removed Spike action)

Both the Debug and Release builds are clean and all 4 code tasks are committed — the project is in a ready state for the user to run the on-device checkpoint at their convenience. No blockers.

---
*Phase: quick-260729-0b5*
*Completed: 2026-07-29 (code tasks only; on-device checkpoint pending)*

## Self-Check: PASSED

All 3 modified source files and this SUMMARY.md confirmed present on disk. All 4 task commit hashes (`5410e3a`, `35963a3`, `cb93b56`, `91ebbba`) confirmed present in `git log`.
