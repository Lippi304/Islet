---
quick_id: 260708-ol8
status: planned
---

# Quick Task 260708-ol8: Bump MARKETING_VERSION to 1.0 for public launch

**Context:** User is publishing Islet publicly (website + GitHub Release) for the first time. `project.yml` line 60 has `MARKETING_VERSION: "0.1"` with an explicit prior note: "private first release; 1.0 reserved for public launch" (D-14). This is that public launch.

## Task 1: Bump version and regenerate project

**Files:** `project.yml`, `Islet.xcodeproj/project.pbxproj` (regenerated)
**Action:**
1. Edit `project.yml` line 60: `MARKETING_VERSION: "0.1"` → `MARKETING_VERSION: "1.0"`
2. Run `xcodegen generate` to regenerate `Islet.xcodeproj` from the updated `project.yml`
**Verify:** `grep MARKETING_VERSION project.yml` shows `"1.0"`; `xcodegen generate` exits 0
**Done:** CFBundleShortVersionString will read 1.0 in the next release build
