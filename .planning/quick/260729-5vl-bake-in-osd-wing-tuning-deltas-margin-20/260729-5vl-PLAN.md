---
phase: quick-260729-5vl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Notch/NotchPillView.swift
autonomous: false
requirements: []
user_setup: []
---

<objective>
Bake in the user's on-device Wing Tuner deltas for the OSD (Brightness/Volume) wing —
`[WingTuner] leadingNudge=0.0 trailingNudge=-6.0 marginNudge=20.0 gapNudge=0.0` — into
`osdWings(for:)`'s own fixed constants in `Islet/Notch/NotchPillView.swift`, now that the
percent-number addition (260729-5jc/5pv) changed the wing's ideal footprint. Executed
directly given the small, well-understood, mechanical nature of the change.
</objective>

<tasks>

<task type="auto">
  <name>Bake tuned margin/trailingPad deltas into osdWings(for:)</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <action>See 260729-5vl-SUMMARY.md for the complete detail.</action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED" && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Release 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <done>Both Debug and Release builds succeed. margin is 60 (was 40, +20), trailingPad base is 18*wScale (was 24*wScale, -6). leadingPad and gap constants untouched (deltas were 0).</done>
</task>

</tasks>

<output>
Summary already written to 260729-5vl-SUMMARY.md.
</output>
