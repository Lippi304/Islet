---
phase: quick-260729-47v
plan: 01
subsystem: ui
tags: [swiftui, wing-tuner, battery-indicator, bluetooth-glyph]

# Dependency graph
requires:
  - phase: quick-260729-3pc
    provides: Charging/Device wing white-icon redesign (battery accent .white, plug/radiowaves icons, Connected text) that this round reverts/replaces
provides:
  - Charging wing converted to margin+cameraBlockWidth formula (Wing Tuner nudges now work)
  - Charging + Device BatteryIndicator fill reverted to accent .green
  - Device wing hand-drawn bluetoothGlyph(size:opacity:) replacing the dot.radiowaves SF Symbol stand-in
  - "Connected" text removed from Device wing entirely
affects: [wing-tuner, notch-pill-view, device-activity, charging-activity]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Charging wing now mirrors focusWings/deviceWings' margin+cameraBlockWidth+asserts formula (no wing left on the old fixed-half+Spacer() mechanism)"
    - "Custom Bluetooth bind-rune glyph drawn via SwiftUI Path (vertical spine + two triangular flags) instead of an SF Symbol stand-in"

key-files:
  created: []
  modified:
    - Islet/Notch/NotchPillView.swift

key-decisions:
  - "Charging wing's starting margin=24 is an unmeasured estimate (matches the old symmetric 145pt/145pt fixed-half footprint at widthGrowth=1) — flagged for on-device Wing Tuner re-tuning, not treated as final"
  - "bluetoothGlyph is a first-pass hand-drawn approximation, not pixel-verified against Apple's/Bluetooth SIG's exact official proportions — flagged in the pending on-device checkpoint for explicit user confirmation or tweak request"

patterns-established: []

requirements-completed: []

# Metrics
duration: ~15min
completed: 2026-07-29
---

# Quick Task 260729-47v: Wing Tuner Round 6 — Battery Green, Charging Margin Conversion, Bluetooth Glyph Summary

**Reverted Charging/Device BatteryIndicator fill from white back to green, converted Charging wing's layout onto the same margin+cameraBlockWidth formula every other wing uses (fixing dead Wing Tuner nudges), and replaced Device wing's generic radiowaves SF-Symbol stand-in with a hand-drawn Bluetooth bind-rune Path glyph while dropping the adjacent "Connected" text.**

## Performance

- **Duration:** ~15 min
- **Completed:** 2026-07-29T01:13:46Z
- **Tasks:** 2/2 auto tasks completed (both `auto` tasks executed; final `checkpoint:human-verify` task intentionally NOT executed — requires real hardware)
- **Files modified:** 1

## Accomplishments
- Charging wing's `wings(for activity: ChargingActivity)` migrated off its old fixed `Self.wingsSize.width / 2` halves + flexible `Spacer()` onto the notch-cutout-derived `margin`/`cameraBlockWidth` formula (mirrors `focusWings(for:)`), so Wing Tuner's Margin/Leading/Trailing nudges now visibly affect this wing — previously the one wing round 4 never converted.
- Both Charging's and Device's `BatteryIndicator` fill reverted from `accent: .white` (round 5) back to `accent: .green`; outline and `%` text remain hardcoded white in `BatteryIndicator` itself, unchanged either way.
- New `bluetoothGlyph(size:opacity:)` helper renders a hand-drawn Bluetooth bind-rune (vertical spine + two right-pointing triangular flags) via a custom `Path`, replacing the `dot.radiowaves.left.and.right` SF Symbol stand-in on the Device wing.
- Device wing's "Connected" text and its wrapping inner `HStack` removed entirely — left flank is icon-only in every state (connected or disconnected); `labelGap`/`labelWidth` locals removed as no longer referenced.
- Both Debug and Release builds verified clean after Task 2's changes.

## Task Commits

Each task was committed atomically:

1. **Task 1: Charging wing — margin/cameraBlockWidth conversion + green battery fill** - `d372d86` (feat)
2. **Task 2: Device wing — real Bluetooth rune glyph, drop Connected text, green battery fill** - `a66ec4e` (feat)

**Plan metadata:** not yet committed (orchestrator handles final docs commit separately, per this quick task's constraints)

## Files Created/Modified
- `Islet/Notch/NotchPillView.swift` - `wings(for activity: ChargingActivity)` rewritten to the margin+cameraBlockWidth formula with `accent: .green`; new `bluetoothGlyph(size:opacity:)` helper added; `deviceWings(for:)` drops "Connected" text and uses `bluetoothGlyph`; `deviceTrailing(isConnected:battery:)`'s battery branch reverted to `accent: .green`

## Decisions Made
- Followed the plan's exact target function bodies verbatim (both tasks provided complete, ready-to-use replacement code) — no improvisation on the `bluetoothGlyph` `Path` construction or the Charging wing's formula constants.
- Charging wing's `isCharging` local remains assigned-but-unused, exactly as round 5 left it, per the plan's explicit instruction not to "clean up" that out-of-scope detail.

## Deviations from Plan

None — plan executed exactly as written for both auto tasks. Both tasks' automated verify conditions were confirmed by direct inspection of the edited function bodies (the plan's own `sed`-range-based grep checks incidentally also match unrelated pre-existing comment text in neighboring functions due to imprecise range boundaries — not a defect introduced by this task; the actual code changes match the plan's target verbatim with no live `Spacer()` calls, no `Text("Connected")`, and no leftover `labelGap`/`labelWidth` locals remaining, only prose comments referencing them as instructed).

## Known Stubs

None.

## Threat Flags

None — no new network endpoints, auth paths, file access patterns, or schema changes introduced. Matches the plan's own threat register (T-47v-01/02/03, all disposition `accept`, no new surface class versus prior rounds).

## Pending Checkpoint

The plan's final task (`checkpoint:human-verify`, gate=blocking) was intentionally NOT executed by this run — it requires the actual user verifying on real hardware (Wing Tuner Margin/Leading/Trailing nudges, Preview Charging/Device, a real low-battery Mac and/or Bluetooth device, and explicit confirmation on the hand-drawn Bluetooth glyph's proportions). See `260729-47v-PLAN.md`'s final task for the full 7-step verification sequence and resume-signal.

## Self-Check: PASSED
