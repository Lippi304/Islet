---
phase: quick-260729-57h
plan: 01
status: complete
---

# Quick Task 260729-57h: Disable Phase 66's Menu-Bar-Overflow Chevron Summary

Phase 66 (Menü-Bar-Overflow, Ice-style MVP) is PAUSED per project memory
(`phase66_menubar_overflow_second_nogo`) after a third consecutive on-device NO-GO — even the real
reference implementation (Ice.app) stopped working on this machine. The chevron was still being
constructed and started unconditionally at every app launch (`Islet/AppDelegate.swift`
`applicationDidFinishLaunching`, no Settings toggle, no gate), so it kept appearing in the menu bar
despite not doing anything useful. User asked for it to be removed again.

## What changed

Commented out the 2 lines in `applicationDidFinishLaunching` that construct and start
`MenuBarOverflowController` (`menuBarOverflowController = MenuBarOverflowController()` /
`menuBarOverflowController.start()`), with a comment explaining why (paused, not dropped — links
to the CONTEXT.md third revision and project memory). Did NOT delete
`Islet/Notch/MenuBarOverflowController.swift` or `Islet/Notch/MenuBarOverflowBridging.swift` — the
feature is paused, still wanted eventually, matching this project's own PAUSED-not-DROPPED
convention (Phase 49/50 precedent). `menuBarOverflowController`'s declaration
(`private var menuBarOverflowController: MenuBarOverflowController!`) is left in place, unassigned
(implicitly-unwrapped optional properties default to `nil`, no compile error) — it is simply never
constructed or started now, so no chevron/spacer status item is created at launch.

No other file references `menuBarOverflowController` (confirmed via repo-wide grep before editing)
— this is a self-contained, 2-line change.

## Build Verification

- Debug: BUILD SUCCEEDED
- Release: BUILD SUCCEEDED

User explicitly declined an on-device checkpoint for this change ("kein Test").

## Files Modified

- `Islet/AppDelegate.swift`
