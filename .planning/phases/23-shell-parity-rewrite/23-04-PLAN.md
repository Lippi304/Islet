---
phase: 23-shell-parity-rewrite
plan: 04
type: execute
wave: 3
depends_on: ["23-01", "23-03"]
files_modified: []
autonomous: false
requirements: [ARCH-01]

must_haves:
  truths:
    - "The island positions on the built-in notch, morphs collapsed<->expanded on hover/click, and grace-collapses after ~0.4s — identical to today, verified on-device"
    - "The island hides in true fullscreen across all 3 trigger methods (green-button, menu bar, fullscreen video) and click-through works with no dead-zone regressions, including the CR-01 empty-shelf trace"
    - "The island stays visible above all windows across all Spaces and correctly repositions through external-display/clamshell changes"
    - "No NSDraggingDestination conformance or drag-stub overrides remain in NotchPanel.swift"
    - "IslandResolver.swift, DeviceCoordinator.swift, and Islet/Shelf/ show zero diff from before this phase"
  artifacts:
    - path: ".planning/phases/23-shell-parity-rewrite/23-04-SUMMARY.md"
      provides: "The recorded on-device UAT outcome and the git-diff zero-diff confirmation, closing out ARCH-01"
  key_links:
    - from: "git diff (pre-phase baseline vs current)"
      to: "Islet/Notch/IslandResolver.swift, Islet/Notch/DeviceCoordinator.swift, Islet/Shelf/"
      via: "git diff --stat showing zero changed lines in these paths"
      pattern: "zero diff"
---

<objective>
Close out ARCH-01 with the phase-gate verification pass: confirm Success Criterion #5 (zero diff
on the explicitly-locked files) via git, run the full Cmd-U test suite, and run the consolidated
~20-item on-device UAT (Phase 2's 8 scenarios + Phase 9's fullscreen 3-trigger matrix + the CR-01
hover→expand→move-down trace) reconstructed in RESEARCH.md — given ARCH-01's explicit
"zero behavioral regression" framing and that this is the most safety-critical code in the app.

Purpose: this is the only verification gate for Success Criteria #1-3 (live AppKit/Window-Server
interaction behavior is not unit-testable — confirmed by RESEARCH.md's Validation Architecture) and
for the CR-01 regression class specifically, which project memory confirms grep/build gates alone
cannot catch.
Output: `.planning/phases/23-shell-parity-rewrite/23-04-SUMMARY.md` recording the git-diff
confirmation, the Cmd-U result, and the full on-device UAT outcome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-shell-parity-rewrite/23-CONTEXT.md
@.planning/phases/23-shell-parity-rewrite/23-RESEARCH.md
@.planning/phases/23-shell-parity-rewrite/23-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm zero-diff on locked files and run the full Debug build gate</name>
  <files></files>
  <read_first>
    .planning/ROADMAP.md (Phase 23 Success Criterion #5 — exact wording of what must show zero diff)
    .planning/phases/23-shell-parity-rewrite/23-CONTEXT.md (the domain boundary confirming IslandResolver.swift, DeviceCoordinator.swift, and Islet/Shelf/ are explicitly out of this phase's scope)
  </read_first>
  <action>
    Run `git diff` (or `git diff main...HEAD` if this phase is on its own branch — check
    `.planning/config.json`'s `git.branching_strategy`, currently `"none"`, so compare against the
    commit immediately before Plan 23-01's first commit) scoped to exactly these three paths:
    `Islet/Notch/IslandResolver.swift`, `Islet/Notch/DeviceCoordinator.swift`, `Islet/Shelf/`.
    Confirm the diff is empty for all three. Then run the full Debug build
    (`xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build`) to confirm
    the fully reconstructed `NotchPanel.swift`/`NotchWindowController.swift` compile clean
    end-to-end (not just per-task, now that all 4 plans' edits are combined). Record both results
    in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `git diff --stat -- Islet/Notch/IslandResolver.swift Islet/Notch/DeviceCoordinator.swift Islet/Shelf/` since the pre-phase-23 baseline commit outputs nothing (empty diff)
    - `xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build` exits 0
    - `grep -rc "NSDraggingDestination" Islet/Notch/NotchPanel.swift` returns 0
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build</automated>
  </verify>
  <done>Success Criterion #5 confirmed via git diff; Success Criterion #4 confirmed via grep; full Debug build green with all 4 plans' changes combined.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Cmd-U test suite + consolidated on-device UAT</name>
  <read_first>
    .planning/ROADMAP.md "Phase 23: Shell Parity Rewrite" Success Criteria (5 items)
    .planning/phases/23-shell-parity-rewrite/23-VALIDATION.md "## Manual-Only Verifications" table and its "Consolidated On-Device UAT" reference into 23-RESEARCH.md
  </read_first>
  <files>Islet/Notch/NotchPanel.swift, Islet/Notch/NotchWindowController.swift, IsletTests/NotchPanelTests.swift</files>
  <action>
    No file edits in this task — build and run the app on-device (Cmd-R in Xcode) and run the full
    test suite (Cmd-U), then perform the consolidated on-device UAT described in how-to-verify
    below. This task's only output is the recorded PASSED/FAILED verdict covering all 5 ROADMAP
    Success Criteria for Phase 23.
  </action>
  <what-built>
    The notch window shell (`NotchPanel.swift`/`NotchWindowController.swift`) has been fully
    reconstructed in place across Plans 23-01 through 23-03: the Phase-22 drag scaffold is
    deleted, and every other behavior — positioning, hover/click/grace-collapse, fullscreen
    hiding, click-through hit-testing (including the CR-01 fix), and multi-Space visibility — has
    been reconstructed and re-verified line-for-line against the pre-rewrite implementation.
    Task 1 above already confirmed the build compiles and the locked files show zero diff.
  </what-built>
  <how-to-verify>
    **Part A — Cmd-U test suite (in Xcode):**
    1. Open `Islet.xcodeproj` in Xcode (Finder: navigate to the project root folder, double-click
       `Islet.xcodeproj`).
    2. Press Cmd-U to run the full test suite.
    3. Confirm `NotchPanelTests` (7 tests, including the new
       `testPanelHasNoDraggingDestinationResidue`), `InteractionStateTests`,
       `VisibilityDecisionTests`, and `FullscreenDetectorTests` all show green checkmarks with no
       failures.

    **Part B — Consolidated on-device UAT (run the app via Cmd-R in Xcode, on a physical
    notch MacBook):**

    *Hover/click/morph:*
    4. Hover the pointer over the collapsed pill — confirm a haptic + a small bounce fires WITHOUT
       expanding.
    5. Click the pill — confirm it expands with a spring morph (continuous shape morph, no
       cross-fade/flicker/jump).
    6. Move the pointer away — confirm it collapses after roughly 0.4s.
    7. Hover, then quickly move away and back before it collapses — confirm the collapse is
       cancelled by the re-entry.

    *Fullscreen (3 trigger methods, confirm the island hides completely with no ghost bar on
    ALL three, and restores on exit without stealing focus from the app you return to):*
    8. Click a window's green traffic-light button to enter fullscreen.
    9. Use a menu bar "Enter Full Screen" command (e.g. View menu in a browser).
    10. Play a video fullscreen (e.g. a YouTube video in Safari, or QuickTime).
    11. Select a file in Finder, press Space, then fullscreen the QuickLook preview — confirm the
        island hides while QuickLook is fullscreen and restores when QuickLook closes.
    12. Zoom (NOT fullscreen) a window via the green button's alternate zoom — confirm the island
        STAYS visible (a zoom is not a fullscreen Space).

    *Click-through, including the CR-01 regression trace:*
    13. With the shelf pill collapsed, click just outside the pill on the desktop or another
        app's window — confirm the click passes through to that app (Islet is never activated).
    14. With the shelf EMPTY: hover the pill, click to expand, then move the pointer DOWN into the
        reserved-but-invisible empty-shelf band (below the visible blob, inside the expanded
        panel's lower region) — confirm a click there passes through to whatever app is under the
        notch (does NOT get swallowed by Islet). Then move the pointer back UP into the visible
        blob and click — confirm that click IS captured by Islet. This exact sequence is the only
        verification that has ever caught the CR-01 regression.

    *Multi-Space / display / clamshell:*
    15. Switch between 2+ ordinary (non-fullscreen) Spaces — confirm the island stays visible and
        correctly positioned on each.
    16. Connect an external display (or use System Settings > Displays to simulate a
        configuration change) — confirm the island repositions correctly and only ever shows on
        the built-in notched display.
    17. Close the laptop lid with an external display connected (clamshell mode), then reopen it —
        confirm no flicker, stuck-hidden, or stuck-shown state.

    *Lock-screen / sleep-wake:*
    18. Lock the screen (Apple menu > Lock Screen), unlock, and confirm the island behaves
        correctly (visible again, no stuck state).
    19. Put the Mac to sleep and wake it — confirm the same.

    *Accepted, NOT a regression if present (do not report as a bug):*
    20. A single-frame flash of the island at the very end of a fullscreen-ENTER transition is
        pre-existing, accepted, product-deferred technical debt (see
        `.planning/debug/resolved/fullscreen-enter-flash.md`) — only report this if it is now
        WORSE, MORE frequent, or has a NEW trigger method not in items 8-10 above.
  </how-to-verify>
  <acceptance_criteria>
    - Step 3 (Cmd-U): all 4 test classes green, zero failures
    - Steps 4-7 (Success Criterion #1): hover/click/morph/grace-collapse behave identically to today
    - Steps 8-12 (Success Criterion #2, fullscreen half): island hides on all 3 real trigger
      methods plus QuickLook, restores without stealing focus, and a mere zoom does not hide it
    - Steps 13-14 (Success Criterion #2, click-through half): ordinary click-through works, and the
      CR-01 empty-shelf trace specifically passes
    - Steps 15-17 (Success Criterion #3): multi-Space/display/clamshell repositioning correct, no
      flicker/stuck states
    - Steps 18-19: lock/sleep/wake stability, no stuck states
    - Step 20 is informational only — not a pass/fail gate unless it regressed in severity/trigger
  </acceptance_criteria>
  <resume-signal>Type "approved" if Cmd-U is green and all items in steps 4-19 pass (step 20 excluded per its own note), or describe exactly which numbered step failed and what was observed.</resume-signal>
  <verify>
    <automated>MISSING — manual on-device UAT only; xcodebuild test hangs headlessly (project memory xcodebuild-test-headless-hang) and no automated harness exists for real Window Server/pointer/Space-transition behavior (23-VALIDATION.md)</automated>
  </verify>
  <done>All 5 ROADMAP Success Criteria for Phase 23 confirmed on-device; full Cmd-U test suite green; user has explicitly typed "approved".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan performs verification only — no new code, no new trust boundary. It exists to CONFIRM the mitigations recorded in Plans 23-01 through 23-03's threat registers actually hold on-device. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04-01 | Elevation of Privilege | Focus-stealing across the whole reconstructed shell | mitigate | UAT steps 8-12 (fullscreen restore) and step 13 (click-through) are the on-device proof that `.nonactivatingPanel` + `canBecomeKey/Main == false` + `orderFrontRegardless`-only discipline (D-04/D-07) held across the entire reconstruction, not just at the grep level |
| T-23-04-02 | Information Disclosure | Click-through hit-testing regression (CR-01 class) | mitigate | UAT step 14 is the exact hover→expand→move-down→click trace that is the ONLY verification method that has ever caught this regression class (grep/build gates confirmed insufficient by the original incident) |
| T-23-04-03 | Tampering | Supply chain (npm/pip/cargo) | accept | Not applicable — zero new dependencies introduced anywhere in this phase (RESEARCH.md Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
Task 1's `git diff`/`grep`/build checks are the automated gate. Task 2's Cmd-U + on-device UAT is
the manual gate — this phase cannot be marked complete without an explicit "approved" from the
user on Task 2, given ARCH-01's "zero behavioral regression" framing over the app's most
safety-critical code.
</verification>

<success_criteria>
- All 5 ROADMAP Success Criteria for Phase 23 are confirmed true: on-device parity (SC#1-3), zero
  drag-scaffold residue (SC#4, grep-confirmed), zero diff on locked files (SC#5, git-confirmed).
- Cmd-U green across `NotchPanelTests`/`InteractionStateTests`/`VisibilityDecisionTests`/
  `FullscreenDetectorTests`.
- User has explicitly approved the consolidated on-device UAT.
</success_criteria>

<output>
Create `.planning/phases/23-shell-parity-rewrite/23-04-SUMMARY.md` when done
</output>
