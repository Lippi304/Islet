---
phase: 23-shell-parity-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Notch/NotchPanel.swift
  - IsletTests/NotchPanelTests.swift
autonomous: true
requirements: [ARCH-01]

must_haves:
  truths:
    - "NotchPanel no longer conforms to NSDraggingDestination and registers no dragged types"
    - "Every other NotchPanel construction property (styleMask, ignoresMouseEvents start value, level, collectionBehavior, canBecomeKey/canBecomeMain, isReleasedWhenClosed, isOpaque/backgroundColor/hasShadow) is byte-identical to before this plan ran"
  artifacts:
    - path: "Islet/Notch/NotchPanel.swift"
      provides: "The borderless, non-activating overlay window, now with zero Phase-22 drag-destination residue"
      contains: "final class NotchPanel: NSPanel {"
    - path: "IsletTests/NotchPanelTests.swift"
      provides: "Unit-level proof that the drag scaffold is gone (Success Criterion #4)"
      contains: "testPanelHasNoDraggingDestinationResidue"
  key_links:
    - from: "IsletTests/NotchPanelTests.swift"
      to: "Islet/Notch/NotchPanel.swift"
      via: "XCTAssertFalse(panel is NSDraggingDestination)"
      pattern: "panel is NSDraggingDestination"
---

<objective>
Remove the Phase-22 `NSDraggingDestination` drag scaffold from `NotchPanel.swift` per locked
decision D-01, and add a regression-proof unit test confirming it is gone. This is the one
unambiguous code change required by ARCH-01 (Success Criterion #4) — everything else in
`NotchPanel.swift` (styleMask, click-through start state, level, collectionBehavior, focus-safety
overrides) is preserved verbatim.

Purpose: Phase 24 must build its `DragApproachDetector` global-monitor drag-detection mechanism
from scratch against a clean panel with zero inherited Phase-22 residue — per D-01, no named
extension seam or hook is left behind for it to reuse.
Output: `Islet/Notch/NotchPanel.swift` with the drag scaffold deleted; `IsletTests/NotchPanelTests.swift`
with a new assertion covering the deletion at the unit level.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-shell-parity-rewrite/23-CONTEXT.md
@.planning/phases/23-shell-parity-rewrite/23-RESEARCH.md
@.planning/phases/23-shell-parity-rewrite/23-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the D-01 NSDraggingDestination scaffold from NotchPanel.swift</name>
  <files>Islet/Notch/NotchPanel.swift</files>
  <read_first>
    Islet/Notch/NotchPanel.swift (current full 62-line file — read before editing, do not assume line numbers below are still exact)
    .planning/phases/23-shell-parity-rewrite/23-PATTERNS.md (section "Islet/Notch/NotchPanel.swift" — the exact deletion boundary and the full target source)
    .planning/phases/23-shell-parity-rewrite/23-CONTEXT.md (D-01 locked decision text)
  </read_first>
  <action>
    Edit `Islet/Notch/NotchPanel.swift` to delete exactly three things and preserve everything else
    byte-for-byte:
    1. On the `final class NotchPanel: NSPanel, NSDraggingDestination {` line, drop the
       `, NSDraggingDestination` conformance so the line reads `final class NotchPanel: NSPanel {`.
    2. Delete the `registerForDraggedTypes([.fileURL])` call (with its trailing "Phase 22 spike (A1)..."
       comment) from `init`.
    3. Delete the entire "SPIKE — throwaway..." comment block and all 4 `NSDraggingDestination`
       stub method implementations (`draggingEntered(_:)`, `draggingUpdated(_:)`, `draggingExited(_:)`,
       `performDragOperation(_:)`) that follow the `canBecomeKey`/`canBecomeMain` overrides.
    Do not touch `styleMask`, `isOpaque`, `backgroundColor`, `hasShadow`, `isMovable`,
    `isMovableByWindowBackground`, `isReleasedWhenClosed`, `ignoresMouseEvents = true`,
    `level = .statusBar`, `collectionBehavior`, or the `canBecomeKey`/`canBecomeMain` overrides —
    every one of these must read identical to the pre-edit file. Per D-01, do NOT leave a named
    extension seam, TODO, or hook referencing Phase 24's `DragApproachDetector` — the deletion is
    total and clean.
  </action>
  <acceptance_criteria>
    - `grep -c "NSDraggingDestination" Islet/Notch/NotchPanel.swift` returns 0
    - `grep -c "registerForDraggedTypes" Islet/Notch/NotchPanel.swift` returns 0
    - `grep -c "draggingEntered\|draggingUpdated\|draggingExited\|performDragOperation" Islet/Notch/NotchPanel.swift` returns 0
    - `grep -c "canBecomeKey\|canBecomeMain\|collectionBehavior = \[.canJoinAllSpaces" Islet/Notch/NotchPanel.swift` returns 3 (all three preserved lines still present)
    - `xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build</automated>
  </verify>
  <done>NotchPanel.swift compiles with zero NSDraggingDestination residue; every other panel-construction property unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression assertion to NotchPanelTests.swift</name>
  <files>IsletTests/NotchPanelTests.swift</files>
  <read_first>
    IsletTests/NotchPanelTests.swift (current full 58-line file — matches the style every new test must follow)
    Islet/Notch/NotchPanel.swift (as edited by Task 1 — confirm the class no longer conforms to NSDraggingDestination before writing the assertion)
    .planning/phases/23-shell-parity-rewrite/23-PATTERNS.md (section "IsletTests/NotchPanelTests.swift" — exact assertion text to add)
  </read_first>
  <action>
    Add one new test method `testPanelHasNoDraggingDestinationResidue()` to the `NotchPanelTests`
    class, following the exact style of the 6 existing tests (uses `makePanel()`, one
    `XCTAssertFalse` with an explanatory message referencing D-01). The assertion is
    `XCTAssertFalse(panel is NSDraggingDestination, ...)` with a message explaining the Phase-22
    scaffold must be fully removed. Do not modify any of the 6 existing test methods — they must
    keep passing unchanged since Task 1 did not touch any property they assert on.
  </action>
  <acceptance_criteria>
    - `grep -c "func test" IsletTests/NotchPanelTests.swift` returns 7 (6 existing + 1 new)
    - `grep -c "testPanelHasNoDraggingDestinationResidue" IsletTests/NotchPanelTests.swift` returns 1
    - `grep -c "NSDraggingDestination" IsletTests/NotchPanelTests.swift` returns 1 (only inside the new assertion)
    - `xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build` succeeds (test target compiles)
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build</automated>
  </verify>
  <done>NotchPanelTests.swift has 7 test methods, compiles cleanly; actual Cmd-U execution deferred to the phase-gate consolidated UAT plan (23-04) — xcodebuild test hangs headlessly per project memory xcodebuild-test-headless-hang.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| OS drag-pasteboard → app | Removed entirely by this plan — NotchPanel no longer registers as a drag destination, so there is no longer any code path receiving OS drag-pasteboard data in this file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-01 | Tampering / Elevation of Privilege | `NotchPanel` drag-destination surface | mitigate | Deleted entirely (D-01) — a click-through, non-activating panel accepting arbitrary drag-pasteboard data was the largest incidental attack surface in the window shell; removing it removes the threat rather than defending it |
| T-23-01-02 | Denial of Service | Build/test gate | accept | Zero new dependencies, zero new runtime surface — the only risk is a compile break from the deletion, caught immediately by the mandatory `xcodebuild build` gate on both tasks |
</threat_model>

<verification>
`xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build` after each task.
Full Cmd-U test run (including the new `testPanelHasNoDraggingDestinationResidue`) happens once,
consolidated with the phase's on-device UAT, in Plan 23-04.
</verification>

<success_criteria>
- `Islet/Notch/NotchPanel.swift` has zero `NSDraggingDestination` residue (Success Criterion #4).
- `IsletTests/NotchPanelTests.swift` has a unit-level regression guard against reintroducing it.
- Every other panel-construction property is unchanged from before this plan.
</success_criteria>

<output>
Create `.planning/phases/23-shell-parity-rewrite/23-01-SUMMARY.md` when done
</output>
