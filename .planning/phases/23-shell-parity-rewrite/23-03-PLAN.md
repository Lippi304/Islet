---
phase: 23-shell-parity-rewrite
plan: 03
type: execute
wave: 2
depends_on: ["23-02"]
files_modified:
  - Islet/Notch/NotchWindowController.swift
autonomous: true
requirements: [ARCH-01]

must_haves:
  truths:
    - "updateVisibility() remains the SOLE show/hide arbiter — no second panel.orderOut/positionAndShow call site exists anywhere in the file"
    - "syncClickThrough() remains the SOLE writer of panel.ignoresMouseEvents, and its expanded-state branch stays a pure visibleContentZone() check, never OR'd with pointerInZone (CR-01)"
    - "positionAndShow(on:) preserves the exact frame-set -> NSHostingView-assign -> CGSSpace-join -> orderFrontRegardless() sequence"
    - "The hover/click/grace-collapse interaction state machine (handlePointer, handleHoverEnter, handleHoverExit, handleClick) produces the same phase transitions as before this plan"
  artifacts:
    - path: "Islet/Notch/NotchWindowController.swift"
      provides: "The single-arbiter visibility/click-through/interaction core (updateVisibility, positionAndShow, handlePointer, visibleContentZone, handleHoverEnter, syncClickThrough, handleHoverExit, handleClick, handlePower, scheduleActivityDismiss, syncActivityModels) reconstructed and re-verified line-for-line"
  key_links:
    - from: "NotchWindowController.syncClickThrough()"
      to: "panel?.ignoresMouseEvents"
      via: "the ONLY assignment to ignoresMouseEvents in the entire file"
      pattern: "panel\\?\\.ignoresMouseEvents = !interactive"
    - from: "NotchWindowController.updateVisibility()"
      to: "panel?.orderOut(nil) / positionAndShow(on:)"
      via: "the ONLY hide/show call sites in the entire file"
      pattern: "panel\\?\\.orderOut\\(nil\\)"
---

<objective>
Reconstruct the single highest-risk third of `NotchWindowController.swift` — the resolver/render
pipeline, the sole visibility arbiter (`updateVisibility()`), panel creation
(`positionAndShow(on:)`, including the FS-01 `CGSSpace` join sequence), the sole click-through
arbiter (`syncClickThrough()`, CR-01-hardened), and the full hover/click interaction state
machine (`handlePointer`, `handleHoverEnter`, `handleHoverExit`, `handleClick`) — in a dedicated
plan with maximally fresh context, given this is the most safety-critical code in the app
(focus-safety, click-through, fullscreen visibility) and the exact code this project has already
regressed once (CR-01) and is suspected (unconfirmed) to be entangled in Phase 22's unresolved
drag-delivery mystery.

Purpose: this is the section RESEARCH.md flags as needing line-for-line diffing against the
current implementation, "not re-derived from the surrounding comments' intent" — CR-01's own
incident report confirms grep/build gates alone miss this regression class.
Output: `Islet/Notch/NotchWindowController.swift`'s single-arbiter core reconstructed and
re-verified, ready for Plan 23-04's on-device UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-shell-parity-rewrite/23-CONTEXT.md
@.planning/phases/23-shell-parity-rewrite/23-RESEARCH.md
@.planning/phases/23-shell-parity-rewrite/23-PATTERNS.md
@.planning/phases/21-drag-out/21-CONTEXT.md
</context>

<interfaces>
<!-- Pure seams this section calls into, unaffected by this phase (zero-diff, out of scope) -->
```swift
// From NotchGeometry.swift (pure, untouched):
notchFrame(screenFrame:safeAreaTop:auxLeftWidth:auxRightWidth:widthFudge:) -> CGRect?
notchSize(screenWidth:safeAreaTop:auxLeftWidth:auxRightWidth:widthFudge:) -> CGSize?
expandedNotchFrame(collapsed:expandedSize:) -> CGRect
wingsFrame(collapsed:wingsSize:) -> CGRect

// From NotchInteractionState.swift (pure, untouched):
nextState(_ phase: InteractionPhase, _ event: InteractionEvent) -> InteractionPhase
// interaction.isExpanded / interaction.isHovering / interaction.collapsedNotchSize

// From FullscreenSpaceProbe.swift (untouched, fail-safe: nil/parse-failure -> false):
isBuiltinDisplayInFullscreenSpace(builtinUUID: String?) -> Bool

// From IslandResolver.swift (pure, untouched, Success Criterion #5 zero-diff):
shouldShow(hasTarget:hideInFullscreen:isFullscreen:isLicensed:) -> Bool
resolve(activeTransient:nowPlaying:nowPlayingHealthy:hasPlayedSinceLaunch:isExpanded:) -> IslandPresentation
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Reconstruct the resolver/render pipeline, updateVisibility(), and positionAndShow(on:)</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift, the section from `currentPresentation()` through the end of `positionAndShow(on:)` (as edited by Plan 23-02 — read the live current file, do not assume exact line numbers) — this is `// MARK: - Phase 6: the single arbiter (resolver) + its render` through the closing brace of `positionAndShow`
    .planning/phases/23-shell-parity-rewrite/23-RESEARCH.md (Pattern 3 additive CGSSpace, Pattern 5 single show/hide arbiter, the "Full current updateVisibility()" Code Example, and Pitfall 2's specific request: confirm the frame-set -> NSHostingView-assign -> CGSSpace-join -> orderFrontRegardless() ordering is unchanged, since it is one of the few uninvestigated variables in the Phase-22 drag-delivery mystery)
    .planning/phases/23-shell-parity-rewrite/23-PATTERNS.md (the exact quoted current source for updateVisibility() and positionAndShow(on:))
  </read_first>
  <action>
    Reconstruct `currentPresentation()` preserving the exact Now-Playing-disabled-forces-.none
    gate (`npEnabled` applied to BOTH `np` and the health flag via `nowPlayingHealthGate`) and its
    call into the pure `resolve(...)` seam. Reconstruct `renderPresentation()` as the single
    `presentationState.presentation = currentPresentation()` assignment (the caller owns the
    spring wrapper, per D-08 — this function itself applies no animation). Reconstruct
    `presentTransientChange()` preserving its exact triplet: spring-wrapped
    (toast-clear-if-showing +) `renderPresentation()`, then unconditional `updateVisibility()`,
    then `scheduleActivityDismiss()` — this consolidates what `handlePower`/device-coordinator
    callbacks share, and must not be inlined or reordered. Reconstruct `updateVisibility()` as
    the ONE function that ever calls `panel?.orderOut(nil)` or `positionAndShow(on:)` in the
    entire file: preserve the exact D-13 idle-state guard (`midInteraction = pointerInZone ||
    interaction.isExpanded`; if `!licenseState.isEntitled && midInteraction`, set
    `pendingLockoutHide = true` and return WITHOUT touching panel/hotZone/expandedZone/
    pointerInZone), the `descriptors`/`selectTargetScreen`/`isBuiltinDisplayInFullscreenSpace`
    computation, the `shouldShow(...)` AND-gate, the `wasVisible`-gated
    `refreshWeather()`/`refreshCalendar()` resume-on-show, and the hide branch's full reset
    (`hotZone = nil; expandedZone = nil; pointerInZone = false; isCurrentlyVisible = false`).
    Reconstruct `positionAndShow(on:)` preserving the EXACT sequence: compute `collapsedFrame` via
    `notchFrame(...)`, publish `interaction.collapsedNotchSize` via the UNFUDGED
    `notchSize(...widthFudge: 0)` call (D-01, distinct from the fudged hot-zone geometry), compute
    `expandedFrame` (reserving the shelf row height UNCONDITIONALLY — this is permanent, not a
    later-conditioned state, per the CR-01 resolution note), compute `wings` and the unioned
    `panelFrame`, set `hotZone`/`expandedZone` (both padded by `hotZonePadding`), THEN: construct
    `NotchPanel` only if `self.panel == nil`, assign `NSHostingView(rootView: makeRootView(...))`
    as `contentView`, store `self.panel = panel`, call `notchSpace.windows.insert(panel)` — in
    that exact order, exactly once, only at first construction (never re-synced per show/hide
    cycle) — then reposition via `setFrame` if the frame changed, and finally
    `panel.orderFrontRegardless()` as the LAST statement (focus-safe show, D-07 — never
    `makeKeyAndOrderFront`). If this pass surfaces any genuine deviation from this ordering, fix
    it and flag it prominently in the SUMMARY as directly relevant to the Phase-22 mystery
    (RESEARCH.md Pitfall 2); if it already matches, confirm and note "ordering verified unchanged."
  </action>
  <acceptance_criteria>
    - `Islet/Notch/NotchWindowController.swift` compiles (`xcodebuild build` succeeds)
    - `grep -c "panel?.orderOut(nil)" Islet/Notch/NotchWindowController.swift` returns exactly 1
    - `grep -c "positionAndShow(on:" Islet/Notch/NotchWindowController.swift` shows exactly 1 definition site (`func positionAndShow`) and exactly 1 call site (inside `updateVisibility()`)
    - `grep -n "orderFrontRegardless()" Islet/Notch/NotchWindowController.swift` occurs exactly once, as the final statement of `positionAndShow(on:)` (no `makeKeyAndOrderFront` anywhere in the file)
    - `grep -c "notchSpace.windows.insert(panel)" Islet/Notch/NotchWindowController.swift` returns exactly 1, inside the `if self.panel == nil` branch
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build</automated>
  </verify>
  <done>Resolver/render pipeline, updateVisibility(), and positionAndShow(on:) reconstructed, building, with the panel-creation sequence explicitly confirmed unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Reconstruct handlePointer, syncClickThrough, hover/click handlers, and the activity-dismiss pair</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift, the section from `handlePointer(at:)` through `syncActivityModels()` (as edited by Task 1 above — read the live current file) — covers `handlePointer`, `visibleContentZone`, `handleHoverEnter`, `syncClickThrough`, `handleHoverExit`, `handleClick`, `handlePower`, `scheduleActivityDismiss`, `syncActivityModels`
    .planning/phases/23-shell-parity-rewrite/23-RESEARCH.md (Pattern 2 single-arbiter click-through; Pitfall 1 — the full CR-01 incident reconstruction, "any rewrite of syncClickThrough() must diff its final body against the exact current implementation line-for-line, not just re-derive it from the surrounding comments' intent"; Pattern 4 coordinate-space invariant)
    .planning/phases/21-drag-out/21-CONTEXT.md (the CR-01 gotcha as project memory records it — the exact regression class this task must not reintroduce)
  </read_first>
  <action>
    Reconstruct `handlePointer(at:)` preserving: no coordinate conversion (both `point` and
    `hotZone`/`expandedZone` are global bottom-left, per Pitfall 4), the `activeZone` selection
    (`interaction.isExpanded ? (expandedZone ?? hotZone) : hotZone`), the WR-01 edge-tracking via
    explicit `pointerInZone` (never derived from `interaction.isHovering`, which is true for both
    `.hovering` and `.expanded` and would hide re-entries while expanded), and the trailing
    unconditional `syncClickThrough()` call whenever `interaction.isExpanded` (needed because
    `visibleContentZone()`'s boundary toggles with shelf item count and is never itself crossed
    by the enter/exit edge). Reconstruct `visibleContentZone()` preserving its exact mirror of
    `NotchPillView.blobShape`'s `hasShelf ? shelfRowHeight : 0` conditional. Reconstruct
    `handleHoverEnter()` preserving the haptic, the `graceWorkItem?.cancel()`, the
    `dismissWorkItem?.cancel()`/`mediaDismissWorkItem?.cancel()` hover-pause pair, the
    `.pointerEntered` transition inside the spring, and the trailing `syncClickThrough()`.
    Reconstruct `syncClickThrough()` EXACTLY as the CR-01-hardened current body — this is the
    single most important function in the entire rewrite: while `interaction.isExpanded`, the
    ONLY input is `visibleContentZone()?.contains(lastPointerLocation) ?? false`; while not
    expanded, the input is `pointerInZone`; NEVER OR the two together in the expanded branch (this
    is the literal CR-01 regression — the broader `pointerInZone`/`expandedZone` tracks the
    keep-open grace decision, not visible-and-clickable). The final line assigns
    `panel?.ignoresMouseEvents = !interactive` and must remain the ONLY assignment to
    `ignoresMouseEvents` anywhere in the file. Reconstruct `handleHoverExit()` preserving the
    `.pointerExited` transition, the grace-delay `DispatchWorkItem` (checks
    `!self.isDraggingShelfItem` before collapsing — Phase 21's drag-pin), the D-13
    natural-transition recheck (`self.updateVisibility()` inside the work item), the trailing
    `self.syncClickThrough()`, and the charging/paused-media dismiss-resume calls outside the
    work item. Reconstruct `handleClick()` preserving: it is the ONLY path to `.expanded`, the
    `wasExpanded` capture before the transition, the toast-clear-on-genuine-expand guard, the
    Phase-21-followup prune-missing-files-on-expand call, the D-13 second natural-transition
    recheck (`if !interaction.isExpanded { updateVisibility() }`), and the trailing unconditional
    `syncClickThrough()` (a toggle-shut click schedules no grace timer, so without this the window
    would keep swallowing clicks). Reconstruct `handlePower(_:)` and `scheduleActivityDismiss()`/
    `syncActivityModels()` preserving the launch-seed-no-splash guard, the
    `shouldTriggerSplash`-gated transition-only re-display, the in-place-%-tick-without-restarting-
    timer branch, and the one-shot `DispatchWorkItem` advance-the-queue idiom. If this pass
    surfaces any genuine deviation from these documented invariants — especially in
    `syncClickThrough()` — fix it immediately and flag it as a CR-01-class finding in the SUMMARY;
    if it already matches, confirm and note "CR-01 invariant verified unchanged."
  </action>
  <acceptance_criteria>
    - `Islet/Notch/NotchWindowController.swift` compiles (`xcodebuild build` succeeds)
    - `grep -c "panel?.ignoresMouseEvents" Islet/Notch/NotchWindowController.swift` returns exactly 1 (the sole writer, inside `syncClickThrough()`)
    - Inside `syncClickThrough()`'s expanded branch, `grep -A3 "if interaction.isExpanded {" Islet/Notch/NotchWindowController.swift | grep -c "pointerInZone"` returns 0 (pointerInZone never appears in the expanded branch — CR-01 gate)
    - `grep -c "visibleContentZone()?.contains(lastPointerLocation)" Islet/Notch/NotchWindowController.swift` returns exactly 1
    - `grep -c "func handleClick\|func handleHoverEnter\|func handleHoverExit\|func handlePointer" Islet/Notch/NotchWindowController.swift` returns 4
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build</automated>
  </verify>
  <done>handlePointer, syncClickThrough, the hover/click state machine, and the activity-dismiss pair reconstructed, building, with the CR-01 invariant explicitly re-verified line-for-line.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Global `.mouseMoved`/`.leftMouseUp` event stream → `handlePointer`/drag-release monitor | System-wide pointer copies, never consumed/never activates the app — an existing, already-threat-modeled boundary (T-02-06/T-02-07 from Phase 2), reconstructed here without semantic change |
| Private CGS `CGSCopyManagedDisplaySpaces`/`CGSSpace` calls → `updateVisibility()`/`positionAndShow()` | Undocumented syscalls with no Apple compatibility guarantee across macOS versions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03-01 | Information Disclosure | Click-through hit-testing (`syncClickThrough()`) | mitigate | Reconstructed as the sole `ignoresMouseEvents` writer with the CR-01 fix preserved exactly — a second writer or an OR'd condition would either swallow clicks meant for the app underneath the notch (availability regression) or widen interactivity into the reserved-but-invisible shelf band (unintended surface); both are covered by this task's grep-level acceptance criteria plus Plan 23-04's on-device CR-01 trace |
| T-23-03-02 | Elevation of Privilege | `positionAndShow(on:)` show path | mitigate | `orderFrontRegardless()` preserved as the only show call, `makeKeyAndOrderFront`/`activate` never introduced — grep-gated in this task's acceptance criteria, re-confirmed on-device in Plan 23-04's focus-safety UAT item |
| T-23-03-03 | Denial of Service | Un-entitled private CGS API calls (`isBuiltinDisplayInFullscreenSpace`) failing silently on a future macOS update | accept | Fail-safe discipline lives in `FullscreenSpaceProbe.swift` (out of scope, zero-diff) — nil/parse-failure already returns `false` (prefer show over wrong-hide); this plan's call site in `updateVisibility()` must keep consuming that return value as-is, without adding a second failure path that defaults to hiding |
| T-23-03-04 | Denial of Service | Double show/hide site introduced during reconstruction | mitigate | `updateVisibility()` reconstructed as the sole `orderOut`/`positionAndShow` call site — Pattern 5's documented pitfall ("a double show/hide site would race the clamshell and fullscreen observers into flicker / stuck state") is grep-gated in this task's acceptance criteria |
</threat_model>

<verification>
`xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build` after each task.
This is the section RESEARCH.md explicitly flags as requiring the full consolidated on-device UAT
(hover/click feel, fullscreen 3-trigger matrix, CR-01 hover-expand-move-down trace,
multi-Space/display repositioning) — that pass happens in Plan 23-04, which depends on this plan.
</verification>

<success_criteria>
- `updateVisibility()` is the sole show/hide arbiter; `syncClickThrough()` is the sole
  `ignoresMouseEvents` writer with the CR-01 fix intact.
- `positionAndShow(on:)`'s frame-set -> hosting-view-assign -> CGSSpace-join -> orderFrontRegardless
  sequence is unchanged.
- The hover/click/grace-collapse state machine's transition and dismiss-timer behavior is
  unchanged, ready for Plan 23-04's on-device confirmation.
</success_criteria>

<output>
Create `.planning/phases/23-shell-parity-rewrite/23-03-SUMMARY.md` when done
</output>
