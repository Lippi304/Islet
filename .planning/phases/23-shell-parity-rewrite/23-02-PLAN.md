---
phase: 23-shell-parity-rewrite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Notch/NotchWindowController.swift
autonomous: true
requirements: [ARCH-01]

must_haves:
  truths:
    - "NotchWindowController's field/property inventory, start()-time wiring, and toggle-gated monitor lifecycle behave identically to today after this plan"
    - "The hosting-view construction, live-settings-apply, Now-Playing presentation handling, and shelf-item handlers behave identically to today after this plan"
    - "deinit tears down every OS-level registration (observers, monitors, CGSSpace membership, work items) in the same order and completeness as today"
  artifacts:
    - path: "Islet/Notch/NotchWindowController.swift"
      provides: "The AppKit glue controller — properties, start(), monitor lifecycle, hosting-view/settings-apply, Now-Playing handlers, shelf handlers, deinit reconstructed and re-verified"
      min_lines: 1300
  key_links:
    - from: "NotchWindowController.start()"
      to: "DeviceCoordinator(...)"
      via: "constructed at start()-time with [weak self] reach-back closures, not at declaration time"
      pattern: "deviceCoordinator = DeviceCoordinator\\("
    - from: "NotchWindowController.deinit"
      to: "notchSpace.windows.remove(panel)"
      via: "FS-01 owner-driven teardown, mirroring every other OS-registered resource's stop()/removeObserver() in the same deinit"
      pattern: "notchSpace\\.windows\\.remove\\(panel\\)"
---

<objective>
Reconstruct the non-safety-critical two-thirds of `NotchWindowController.swift` in place — the
class header/property inventory, `start()` and its toggle-gated monitor lifecycle, and the
hosting-view/settings-apply/Now-Playing/shelf-handler/`deinit` tail — as a deliberate, line-by-line
verified pass against the current implementation. This plan does NOT touch the single-arbiter
safety-critical core (`updateVisibility()`, `positionAndShow()`, `syncClickThrough()`,
`handlePointer()`, hover/click handlers) — that is Plan 23-03's dedicated, fresh-context focus.

Purpose: ARCH-01 requires the shell to be "rebuilt" (not merely left alone) as a confidence
exercise against the unresolved Phase-22 drag-delivery mystery (RESEARCH.md Pitfall 2) — this
plan produces a fully re-verified, auditable reconstruction of every line outside the
highest-risk interaction core, in-place (per RESEARCH.md's explicit recommendation against a
parallel-build-then-swap, which risks double-registering this file's live OS registrations).
Output: `Islet/Notch/NotchWindowController.swift`, non-safety-critical sections reconstructed and
re-verified against RESEARCH.md/PATTERNS.md's documented invariants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-shell-parity-rewrite/23-CONTEXT.md
@.planning/phases/23-shell-parity-rewrite/23-RESEARCH.md
@.planning/phases/23-shell-parity-rewrite/23-PATTERNS.md
</context>

<interfaces>
<!-- Key seams this file wires into, unaffected by this phase (Success Criterion #5 — zero diff) -->

From Islet/Notch/DeviceCoordinator.swift + ActivityCoordinator.swift (untouched, zero-diff):
```swift
@MainActor
protocol ActivityCoordinator {
    associatedtype Reading
    func handle(_ reading: Reading)
    func activityPromoted()
}
// DeviceCoordinator.init(queueHead:enqueue:updateHead:presentTransientChange:renderPresentation:batteryForAddress:)
// DeviceCoordinator.started(at:), .handle(_:), .cancelPendingWork(), .clearPendingBatteryPolls(), .activityPromoted(), .reset()
```

From Islet/Shelf/ (untouched, zero-diff):
```swift
// ShelfCoordinator.append(_:), .remove(id:), .clear(), .logic.items, .pruneMissingFiles()
// ShelfItem(id:originalURL:localURL:filename:addedAt:)
// ShelfFileStore.makeSessionCopy(of:id:) throws -> URL
```

From Islet/Notch/NotchGeometry.swift / NotchInteractionState.swift (untouched, zero-diff):
```swift
// notchFrame(screenFrame:safeAreaTop:auxLeftWidth:auxRightWidth:widthFudge:) -> CGRect?
// notchSize(screenWidth:safeAreaTop:auxLeftWidth:auxRightWidth:widthFudge:) -> CGSize?
// expandedNotchFrame(collapsed:expandedSize:) -> CGRect
// wingsFrame(collapsed:wingsSize:) -> CGRect
// nextState(_:_:) -> InteractionPhase
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Reconstruct properties, start(), and toggle-gated monitor lifecycle</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift lines 1-482 (imports, class header, every stored property, start(), scheduleTrialExpiryCheck(), activityEnabled, startPowerMonitor, startNowPlayingMonitor, startBluetoothMonitor, startOutfitRefresh, refreshWeather, refreshCalendar, currentBuiltin) — read this exact range before editing, current line numbers may drift slightly, verify against the live file
    .planning/phases/23-shell-parity-rewrite/23-PATTERNS.md (section "Islet/Notch/NotchWindowController.swift" — imports/class-header excerpt, verified function-boundary map)
    .planning/phases/23-shell-parity-rewrite/23-RESEARCH.md (Pattern 7 one-shot DispatchWorkItem idiom; Runtime State Inventory table — confirms every OS registration here is launch-scoped, nothing to migrate)
  </read_first>
  <action>
    Reconstruct this section end-to-end via Edit: every stored property (the `panel`, `notchSpace`
    (`CGSSpace(level: 2147483647)`), `spaceObserver`/`appActivateObserver`, `hideInFullscreen`,
    `licenseState`/`pendingLockoutHide`, `interaction`, `chargingState`, `presentationState`,
    `shelfViewState`/`shelfCoordinator`, `outfitState`/`weatherService`/`calendarService`/
    `locationProvider`/`outfitRefreshTimer`/`lastLocation`, `transientQueue`, `bluetoothMonitor`,
    `deviceCoordinator`, `appliedAccentIndex`, `defaultsObserver`, `nowPlayingState`,
    `powerMonitor`/`nowPlayingMonitor`, `mediaDismissWorkItem`/`pausedTimeout`,
    `toastDismissWorkItem`, `dismissWorkItem`/`activityDuration`/`songToastDuration`,
    `trialExpiryWorkItem`, `lastActivity`/`didSeedInitialPower`, `wingsSize`, `mouseMonitor`/
    `graceWorkItem`, the Phase-21 drag-pin fields (`isDraggingShelfItem`,
    `dragPinSafetyNetWorkItem`/`dragPinSafetyNetDuration`, `dragReleaseMonitor`), `pointerInZone`,
    `lastPointerLocation`, `isCurrentlyVisible`, `hotZone`/`expandedZone`, `expandedSize`,
    `hotZonePadding`, `graceDelay`, `springResponse`/`springDamping`, and the DEBUG-only
    `didLogFirstHover` probe — every field's type, default value, and doc comment must be
    preserved exactly. Reconstruct `start()` preserving its exact registration order: construct
    `deviceCoordinator` (start()-time, `[weak self]` closures — per Phase 16 precedent, NOT at
    declaration time), call `updateVisibility()`, register the screen-parameters observer, the two
    NSWorkspace observers (`activeSpaceDidChangeNotification`, `didActivateApplicationNotification`),
    the global `.mouseMoved` monitor, the three toggle-gated monitor starts (power/nowPlaying/
    bluetooth, each gated on `activityEnabled(...)`), the unconditional `startOutfitRefresh()`,
    the DEBUG-only `seedDebugShelfItems()` call, the `defaultsObserver`, `renderPresentation()`,
    then `scheduleTrialExpiryCheck()`. Preserve `scheduleTrialExpiryCheck()`'s best-effort
    wall-clock-vs-Mach-time framing exactly. Reconstruct the toggle-gated monitor helpers
    (`activityEnabled`, `startPowerMonitor`, `startNowPlayingMonitor`, `startBluetoothMonitor`,
    `startOutfitRefresh`) preserving their idempotent `guard ... == nil else { return }` pattern
    (Pitfall 5 — never double-register an IOPS/monitor source on a fast toggle on/off/on), and
    `refreshWeather`/`refreshCalendar`/`currentBuiltin` preserving the D-01 "never re-request
    location, only use the one cached value" discipline. Per CONTEXT.md's explicit "do not force
    an extraction that adds risk" guidance, `pendingLockoutHide` (D-11/D-12/D-13) stays a plain
    inline field here — Plan 23-03 confirms and documents the reasoning against extracting it
    into its own coordinator (a single boolean guard with 3 call sites does not warrant a whole
    `ActivityCoordinator`-conforming type per the DeviceCoordinator precedent, which existed to
    manage 9 fields and multiple methods). If this line-by-line pass surfaces any genuine
    deviation from the documented invariants above, fix it and note the fix explicitly in the
    plan's SUMMARY; if the section already matches exactly, no functional edit is required beyond
    confirming it — do not fabricate a diff for its own sake.
  </action>
  <acceptance_criteria>
    - `Islet/Notch/NotchWindowController.swift` compiles (`xcodebuild build` succeeds)
    - `grep -n "private let notchSpace = CGSSpace(level: 2147483647)" Islet/Notch/NotchWindowController.swift` matches exactly once
    - `grep -n "deviceCoordinator = DeviceCoordinator(" Islet/Notch/NotchWindowController.swift` shows it inside `start()`, not at property-declaration scope (no `= DeviceCoordinator(` on the `private var deviceCoordinator` declaration line itself)
    - `grep -c "pendingLockoutHide" Islet/Notch/NotchWindowController.swift` is unchanged from the pre-edit count (confirms no extraction happened)
    - `grep -n "func start()" Islet/Notch/NotchWindowController.swift` exists exactly once
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build</automated>
  </verify>
  <done>Properties, start(), and monitor-lifecycle helpers reconstructed and building; deviceCoordinator still constructed at start()-time; pendingLockoutHide still inline.</done>
</task>

<task type="auto">
  <name>Task 2: Reconstruct hosting-view/settings-apply, Now-Playing handlers, shelf handlers, and deinit</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift lines 946-1378 (makeRootView, handleSettingsChanged, flushTransients, applyAccentIfChanged, handleNowPlaying, scheduleMediaDismiss, scheduleToastDismiss, handleAdapterTerminated, the Phase-20 shelf-item handlers, seedDebugShelfItems, deinit) — read this exact range before editing, current line numbers may drift slightly from Task 1's edit, verify against the live file
    .planning/phases/23-shell-parity-rewrite/23-PATTERNS.md (Shared Patterns section — one-shot DispatchWorkItem idiom, applies to every dismiss-scheduling function here)
    .planning/phases/23-shell-parity-rewrite/23-RESEARCH.md (Runtime State Inventory — confirms no persisted/migrated state in this section)
  </read_first>
  <action>
    Reconstruct this section end-to-end via Edit, preserving every invariant: `makeRootView(accentIndex:)`
    wires `NotchPillView` with all its existing closures (`onClick`, `onTogglePlayPause`, `onNext`,
    `onPrevious`, `onShelfItemTap`, `onShelfItemDelete`, `onShelfClearAll`,
    `onShelfItemDragStarted`) and injects `.activityAccent` via `ActivitySettings.accent(for:)`.
    `handleSettingsChanged()` preserves its exact per-toggle branch order (Charging → Devices →
    Now Playing → song-change-toast-off-clears-live-toast → `applyAccentIfChanged()` →
    re-render+`updateVisibility()`) and the "prefer stop" idle-CPU discipline on every toggle-off
    branch. `flushTransients(_:)` preserves the WR-2 "only reset the dismiss timer if the actual
    head changed" gate (captured via `oldHead` before `removeAll(where:)`). `applyAccentIfChanged()`
    preserves its "only re-host when the persisted index actually changed" guard.
    `handleNowPlaying(_:_:)` preserves the exact capture-before-overwrite ordering (`previous`,
    `previousPosition`, `hadPlayedSinceLaunch` all captured before their respective mutations),
    the artwork-latency nil-doesn't-clear-same-track rule, and the song-change-toast gate
    evaluated before any mutation (never schedule then suppress). `scheduleMediaDismiss`/
    `scheduleToastDismiss`/`handleAdapterTerminated` preserve the one-shot
    cancel-then-reschedule `DispatchWorkItem` idiom (Pattern 7) — never a recurring `Timer`. The
    Phase-20 shelf handlers (`handleShelfItemTap`, `resyncShelfViewState`,
    `handleShelfItemDelete`, `handleShelfClearAll`, `beginShelfItemDrag`, `endShelfItemDrag`) and
    the DEBUG-only `seedDebugShelfItems` preserve their exact bodies — `resyncShelfViewState`
    calling `syncClickThrough()` (not `updateVisibility()`) unconditionally afterward is a CR-01-era
    fix and must not regress. `deinit` preserves the exact teardown order and completeness:
    default-center observer removal, the two NSWorkspace-center observer removals (NOT the
    default center — a silent no-op leak if wrong), the defaults observer, the mouse monitor,
    `graceWorkItem?.cancel()`, the drag-pin safety-net cancel + release-monitor removal,
    `powerMonitor?.stop()` + `dismissWorkItem?.cancel()`, `bluetoothMonitor?.stop()` +
    `deviceCoordinator?.cancelPendingWork()`, `nowPlayingMonitor?.stop()` +
    `mediaDismissWorkItem?.cancel()`, `notchSpace.windows.remove(panel)` (FS-01 teardown),
    `trialExpiryWorkItem?.cancel()`, and `outfitRefreshTimer?.invalidate()` — every one of these
    removes exactly the resource its `start()`-time counterpart registered, mirroring the
    owner-driven-teardown discipline documented throughout. If this pass surfaces any genuine
    deviation from these documented invariants, fix it and note the fix explicitly in the SUMMARY;
    if the section already matches, no functional edit is required beyond confirming it.
  </action>
  <acceptance_criteria>
    - `Islet/Notch/NotchWindowController.swift` compiles (`xcodebuild build` succeeds)
    - `grep -n "func deinit" Islet/Notch/NotchWindowController.swift` — N/A (deinit has no `func` keyword); instead confirm `grep -c "notchSpace.windows.remove(panel)"` returns exactly 1, located inside `deinit`
    - `grep -c "wc.removeObserver" Islet/Notch/NotchWindowController.swift` returns 2 (spaceObserver + appActivateObserver, both off the NSWorkspace center)
    - `grep -c "deviceCoordinator?.cancelPendingWork()\|nowPlayingMonitor?.stop()\|powerMonitor.stop()\|bluetoothMonitor?.stop()" Islet/Notch/NotchWindowController.swift` returns 4 (all 4 monitor-teardown calls present)
    - `grep -c "func handleSettingsChanged\|func flushTransients\|func handleNowPlaying\|func makeRootView" Islet/Notch/NotchWindowController.swift` returns 4
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build</automated>
  </verify>
  <done>Hosting-view/settings-apply, Now-Playing handlers, shelf handlers, and deinit reconstructed and building, with every OS-registered resource torn down exactly where it was registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| UserDefaults (local, unauthenticated) → `handleSettingsChanged()` | Any process able to write this app's UserDefaults domain can flip a toggle; not a new boundary, already existing and accepted (single-user local app, no privilege boundary crossed) |
| OS Now-Playing/Bluetooth/power event streams → handlers | System-level event sources, not user-supplied input; already-hardened (Phase 4/6) code paths, reconstructed here without semantic change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | Denial of Service | `deinit` resource teardown | mitigate | Every OS-level registration (observers, monitors, CGSSpace membership, work items) is explicitly torn down in `deinit`, preserved exactly by this plan's Task 2 acceptance criteria — an incomplete teardown would leak IOKit/IOBluetooth/MediaRemote handles across repeated controller lifecycles |
| T-23-02-02 | Elevation of Privilege | `makeRootView`/hosting-view re-creation | accept | `NSHostingView` re-hosting on accent change never calls a focus-stealing API (`orderFrontRegardless`/`setFrame` only, both already covered by Plan 23-03) — no new surface introduced here |
| T-23-02-03 | Denial of Service | Double-registration during in-place rewrite | mitigate | Structural, not runtime: this plan edits the single live `NotchWindowController.swift` in place, never constructs a second parallel instance mid-rewrite, per RESEARCH.md's explicit anti-pattern warning against parallel-build-then-swap for this specific file |
</threat_model>

<verification>
`xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build` after each task.
Full behavioral (on-device) verification is deferred to Plan 23-04's consolidated UAT — this
plan's sections are integration-heavy AppKit glue with no automated test coverage of their own
(consistent with the codebase's existing convention, confirmed by RESEARCH.md's Validation
Architecture: no `NotchWindowControllerTests.swift` exists).
</verification>

<success_criteria>
- `NotchWindowController.swift`'s non-safety-critical two-thirds (properties, `start()`, monitor
  lifecycle, hosting-view/settings-apply, Now-Playing handlers, shelf handlers, `deinit`) compile
  and preserve every documented invariant.
- `pendingLockoutHide` remains inline (documented decision against forcing an extraction).
- No behavior change — this section still builds toward Success Criteria #1-3 being true on-device,
  verified in Plan 23-04.
</success_criteria>

<output>
Create `.planning/phases/23-shell-parity-rewrite/23-02-SUMMARY.md` when done
</output>
