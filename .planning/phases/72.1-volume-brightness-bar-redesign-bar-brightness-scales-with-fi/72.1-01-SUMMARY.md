---
phase: 72.1-volume-brightness-bar-redesign-bar-brightness-scales-with-fi
plan: 01
subsystem: ui
tags: [swift, swiftui, appstorage, debug-tooling, macos]

# Dependency graph
requires:
  - phase: 71-island-corner-rounding
    provides: The Wing Tuner 3-file DEBUG-only live-nudge pattern (ActivitySettings key, NotchPillView @AppStorage + read point, AppDelegate menu action) this plan mirrors exactly
provides:
  - "osdOpacityMinNudge / osdOpacityMaxNudge / osdRollSpeedNudge always-compiled Double read points in NotchPillView"
  - "DEBUG-only OSD Bar/Counter Tuner menu (6 delta actions + reset + print) in AppDelegate"
affects: [72.1-02]

# Tech tracking
tech-stack:
  added: []
  patterns: ["DEBUG-only live-nudge tuning axis (ActivitySettings key -> NotchPillView @AppStorage read point -> AppDelegate menu action), 4th instance of the Wing Tuner pattern in this codebase"]

key-files:
  created: []
  modified:
    - Islet/ActivitySettings.swift
    - Islet/Notch/NotchPillView.swift
    - Islet/AppDelegate.swift
    - IsletTests/ActivitySettingsTests.swift

key-decisions:
  - "Used Double (not CGFloat) for the 3 new read points since they feed a SwiftUI .opacity(Double) value and a spring response value, unlike the existing CGFloat geometry nudges"
  - "Kept debugOSDTunerReset/debugOSDTunerPrint as separate new functions rather than merging into debugWingTunerReset/Print, per plan instruction"
  - "Reused existing adjustWingNudge(_:by:) helper and existing Preview: OSD (Volume)/(Brightness) menu items rather than adding new ones"

requirements-completed: [D-05]

# Metrics
duration: 12min
completed: 2026-07-31
---

# Phase 72.1 Plan 01: OSD Bar/Counter Tuner Interface Summary

**3 new DEBUG-only `@AppStorage` nudge keys (opacityMin/opacityMax/rollSpeed) with always-compiled `Double` read points in `NotchPillView`, plus an AppDelegate "OSD Bar/Counter Tuner" submenu wired through the existing `adjustWingNudge` helper — the interface-first contract Plan 72.1-02's opacity ramp and digit-roll counter will consume.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-31T01:35:00Z
- **Completed:** 2026-07-31T01:47:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- 3 new `ActivitySettings` DEBUG-only keys under the existing Wing Tuner `#if DEBUG` block
- 3 new `NotchPillView` always-compiled `Double` read points (`osdOpacityMinNudge`/`osdOpacityMaxNudge`/`osdRollSpeedNudge`), each resolving to literal `0` outside DEBUG
- New "OSD Bar/Counter Tuner" DEBUG submenu in AppDelegate: 6 delta items (±0.05), Reset, Print — all wired through the pre-existing `adjustWingNudge(_:by:)` helper, no new helper added
- 3 new `ActivitySettingsTests` cases, all passing alongside the existing 22

## Task Commits

1. **Task 1: D-05 tuner keys + read points + tests** - `e165d94` (feat)
2. **Task 2: AppDelegate "OSD Bar/Counter Tuner" menu wiring** - `2e28309` (feat)

**Plan metadata:** (this commit)

## Files Created/Modified
- `Islet/ActivitySettings.swift` - 3 new `debugOSDOpacityMinNudgeKey`/`debugOSDOpacityMaxNudgeKey`/`debugOSDRollSpeedNudgeKey` constants
- `Islet/Notch/NotchPillView.swift` - 3 new `@AppStorage` decls + 3 new always-compiled `Double` computed properties
- `Islet/AppDelegate.swift` - new "OSD Bar/Counter Tuner" `NSMenu` (6 delta items + separator + reset + print), 6 new one-line `@objc` actions, `debugOSDTunerReset`, `debugOSDTunerPrint`
- `IsletTests/ActivitySettingsTests.swift` - `testOSDOpacityMinNudgeKeyName`, `testOSDOpacityMaxNudgeKeyName`, `testOSDRollSpeedNudgeKeyName`

## Decisions Made
- Double over CGFloat for the 3 read points (feeds `.opacity(Double)` and a spring `response: Double`, not geometry)
- Separate (not merged) reset/print functions from the existing Wing Tuner ones, per plan instruction
- No new "Preview: OSD" menu items added — existing `debugPreviewOSDVolume`/`debugPreviewOSDBrightness` reused as-is for on-device tuning

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

`osdOpacityMinNudge`/`osdOpacityMaxNudge`/`osdRollSpeedNudge` exist, compile, and are live-tunable on real hardware via the new DEBUG menu. Plan 72.1-02 can now build the OSD opacity ramp and digit-roll counter against these read points with zero further plumbing.

---
*Phase: 72.1-volume-brightness-bar-redesign-bar-brightness-scales-with-fi*
*Completed: 2026-07-31*
