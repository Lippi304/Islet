---
phase: 72.1
plan: 03
type: execute
wave: 3
depends_on: ["72.1-02"]
files_modified: []
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-04a, D-04b, D-05]
must_haves:
  truths:
    - "D-01/D-02/D-03: On real hardware, the Volume and Brightness bar fill visibly dims toward invisible near 0% and brightens to full opacity at 100%, identically for both"
    - "D-04/D-04a: On real hardware, the percent counter rolls/slides digits and stays legible during rapid held-key repeat — no fade/blend observed"
    - "D-04b: 100% renders without clipping in the live app"
    - "D-05: The DEBUG OSD Bar/Counter Tuner menu successfully live-nudges all 3 new values with visible effect"
  artifacts: []
  key_links: []
---

<objective>
On-device UAT checkpoint confirming Plan 72.1-01/72.1-02's opacity ramp, digit-roll counter, and DEBUG tuner actually read correctly on real hardware — the one class of behavior this phase's own VALIDATION.md classifies as manual-only (SwiftUI transition/animation rendering is not XCTest-testable in this codebase).

Purpose: Confirm the two visual changes read as intended before considering Phase 72.1 done — this project's established convention (every prior Wing Tuner/visual-redesign phase) never skips this gate.
Output: User-confirmed on-device approval, or a documented gap list for follow-up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72.1-volume-brightness-bar-redesign-bar-brightness-scales-with-fi/72.1-CONTEXT.md
@.planning/phases/72.1-volume-brightness-bar-redesign-bar-brightness-scales-with-fi/72.1-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: On-device OSD bar/counter redesign verification</name>
  <what-built>
    Plan 72.1-01 added a DEBUG-only "OSD Bar/Counter Tuner" menu (opacity min/max, roll speed, ±0.05 steps, Reset/Print). Plan 72.1-02 made the Volume/Brightness bar fill's opacity track its fill level (no floor — near-invisible at low values) and replaced the percent counter's fade-based transition with a digit-roll (never-fade) transition.
  </what-built>
  <how-to-verify>
    1. Build and run the Debug configuration on real hardware.
    2. Open the 🐞 debug menu → confirm "OSD Bar/Counter Tuner" submenu exists alongside "Wing Tuner", with Opacity Min/Max and Roll Speed ± items, Reset OSD Tuner, Print OSD Tuner Values.
    3. Use debug menu → Preview Wing → "Preview: OSD (Volume)" to trigger the Volume wing without needing real volume keys; separately trigger "Preview: OSD (Brightness)".
    4. Press real volume keys (or use System Settings) to move the OSD from near-0% up to 100% — confirm the green fill bar visibly dims toward barely-visible near 0% and brightens to full opacity at 100%, with no visible floor/clamp.
    5. Repeat step 4 for Brightness keys — confirm identical dim/bright behavior on the orange bar.
    6. Hold down a volume key (or brightness key) to trigger rapid successive changes — confirm the percent counter's digits slide/roll into place and stay fully legible throughout, with no fading/blending/ghosting of digits.
    7. Drive the value to exactly 100% (max volume/brightness) — confirm "100" renders fully, with no clipping or overlap in its slot next to the bar.
    8. Optionally use the new OSD Tuner's ± items while a Preview Wing is showing to confirm the opacity min/max and roll-speed nudges visibly change the live rendering, then click "Reset OSD Tuner" to confirm it returns to baseline.
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 steps read correctly, or describe exactly which step failed and what was observed (e.g. "step 6: digits still blend during rapid repeat" or "step 7: 100 clips against the bar").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | This plan performs zero code changes — it is a human-verification checkpoint only |

## STRIDE Threat Register

Not applicable — no code is modified by this plan. No new trust boundary, no new threat surface.
</threat_model>

<verification>
On-device checkpoint only (this project's established convention for view-layer visual/animation behavior, per 72.1-RESEARCH.md/72.1-VALIDATION.md — XCTest cannot assert on live SwiftUI transition rendering). If the checkpoint reveals gaps, `/gsd:plan-phase 72.1 --gaps` produces a follow-up plan; this plan itself makes no code changes.
</verification>

<success_criteria>
User has run the live debug build and typed "approved", confirming: opacity ramp reads correctly on both bars with no floor, digit-roll counter never fades under rapid change, 100% renders without clipping, and the D-05 DEBUG tuner works end-to-end.
</success_criteria>

<output>
Create `.planning/phases/72.1-volume-brightness-bar-redesign-bar-brightness-scales-with-fi/72.1-03-SUMMARY.md` when done
</output>
