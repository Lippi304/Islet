---
phase: 72.1-volume-brightness-bar-redesign-bar-brightness-scales-with-fi
verified: 2026-07-31T18:52:00Z
status: human_needed
score: 4/4 must-haves verified (2 superseded-by-approved-supersession, not gaps)
overrides_applied: 1
overrides:
  - must_have: "D-01/D-02/D-03: The Volume (green) and Brightness (orange) bar fill visibly dims as level drops and brightens as level rises, becoming nearly invisible near 0%, identically for both activities"
    reason: "Original 72.1-CONTEXT.md locked decision was tested on real hardware during the 72.1-03 UAT checkpoint and explicitly rejected by the user ('bar dimming toward invisible at low fraction looked bad'). Reverted via quick task 260731-61m (commit 07df8c1) to always-full-opacity, then re-approved by the user in Round 3 of the same checkpoint ('Ja passt soweit erstmal'). This is a deliberate, on-device-approved supersession of the original truth, not a missed requirement."
    accepted_by: "user (on-device, 72.1-03 checkpoint, documented in 72.1-03-SUMMARY.md)"
    accepted_at: "2026-07-31"
human_verification:
  - test: "Re-confirm on real hardware that the WR-01 place-value identity fix (commit 2b57127, applied AFTER the 72.1-03 checkpoint's final 'approved' signoff) still rolls the correct digit across 9<->10 and 99<->100 boundaries, and that the roll speed / rapid-repeat coalescing behavior approved in 72.1-03 Round 3 still holds with this later fix layered on top."
    expected: "Ones-place digit stays anchored to the rightmost slot and visibly rolls (e.g. 9->0) while a new tens-place digit inserts at the left, in both directions; no stuck/frozen digits under rapid key-repeat; 100% still renders without clipping."
    why_human: "SwiftUI transition/animation rendering on a live NSStatusItem window is not XCTest-testable in this codebase (per 72.1-VALIDATION.md/72.1-RESEARCH.md) — code inspection confirms the ForEach identity is now keyed by `placeValue` (not `offset`) and the build/tests pass, but the REVIEW.md's own note is explicit: 'Not yet re-verified on-device by the user for the specific 9<->10 / 99<->100 boundary crossings' after the WR-01 fix landed post-checkpoint."
---

# Phase 72.1: Volume/Brightness Bar Redesign Verification Report

**Phase Goal (as originally written in ROADMAP.md):** The Volume/Brightness OSD bar's fill opacity scales with level — dim near 0%, near-invisible at the floor, full opacity at 100% — identically for both bars, and the percent counter uses a roll/slide (odometer-style) digit transition instead of the previous fade, staying fully legible during rapid successive key-repeat changes. Both new value sets get a DEBUG-only live tuner.

**Actual current goal (on-device-approved supersession, per 72.1-03-SUMMARY.md and quick task 260731-61m):** The bar is intentionally always at full opacity (D-01/D-02/D-03 reverted after failing live UAT); only the digit-roll counter (D-04/D-04a/D-04b) and its DEBUG roll-speed tuner (D-05, trimmed to roll-speed-only) remain as originally specified.

**Verified:** 2026-07-31T18:52:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | D-01/D-02/D-03 (superseded): bar is always fully opaque, no fraction-driven dimming, identical for Volume/Brightness | PASSED (override) | `Islet/Notch/NotchPillView.swift:5048-5061` — `OSDLevelBar` fill `Capsule` has no `.opacity(...)` modifier at all (only `.fill(tint).frame(width:)` + one `.animation`); all opacity-nudge plumbing removed from `ActivitySettings.swift`, `AppDelegate.swift`, `ActivitySettingsTests.swift` (grep for `opacityMin\|opacityMax\|OpacityNudge\|osdOpacity` in `NotchPillView.swift` returns 0 hits). Matches the 72.1-03-SUMMARY.md Round 1 revert + Round 3 user approval. |
| 2 | D-04/D-04a: percent counter's digits roll/slide, never fade, including across digit-count boundaries (9<->10, 99<->100) | VERIFIED | `NotchPillView.swift:5072-5132` `DigitRollText` — only `.transition(.move(edge: .top))` used (two sites: inner `Text.id(place.digit)` and outer per-digit wrapper after `.clipped()`); `grep -c 'contentTransition(.numericText'` = 0, `grep -c 'push(from:'` = 0, no `.combined(with: .opacity)` anywhere. WR-01 (identity anchored to left offset, rolling the wrong digit at count boundaries) was found by code review and **fixed** in commit `2b57127` — identity now keyed by `placeValue = digits.count - 1 - offset` instead of raw `offset`, confirmed present on disk. |
| 3 | D-04b: 100% renders as 3 digits without clipping in the existing `percentTextWidth` slot | VERIFIED (per 72.1-03-SUMMARY.md on-device confirmation) | Call site `NotchPillView.swift:3914` unchanged `.frame(width: percentTextWidth, alignment: .leading)` slot; 72.1-03-SUMMARY.md Round 3 explicitly confirms "100% renders without clipping." No code change since that confirmation affects layout/width. |
| 4 | D-05: DEBUG-only OSD tuner lets a developer live-nudge roll speed; Release always resolves to 0 | VERIFIED | `NotchPillView.swift:233-241` `osdRollSpeedNudge` returns `debugOSDRollSpeedNudge` under `#if DEBUG`, literal `0` otherwise. `AppDelegate.swift:547-561,635-650` wires "OSD Bar/Counter Tuner" submenu (Roll Speed ±0.05, Reset, Print) through the existing `adjustWingNudge` helper. `ActivitySettingsTests.swift:135-137` `testOSDRollSpeedNudgeKeyName` passes. Opacity-half of this tuner was correctly and fully removed alongside D-01/D-02/D-03's revert (no orphaned keys/menu items/tests found by grep or by REVIEW.md). |

**Score:** 4/4 truths verified (1 via explicit accepted override for an on-device-rejected original design, 3 verified directly against current source)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `Islet/Notch/NotchPillView.swift` — `OSDLevelBar` | Fill Capsule, currently always-full-opacity (post-revert) | VERIFIED | Lines 5048-5061; no opacity ramp, matches approved supersession |
| `Islet/Notch/NotchPillView.swift` — `DigitRollText` | New per-digit roll component, replaces old numericText Text | VERIFIED | Lines 5072-5132; place-value-keyed identity (WR-01 fix), rapid-repeat coalescing (120ms threshold), move-only transitions |
| `Islet/ActivitySettings.swift` — `debugOSDRollSpeedNudgeKey` | DEBUG-only key, opacity keys removed | VERIFIED | Line 122; only the roll-speed key remains, no opacity keys present |
| `Islet/AppDelegate.swift` — "OSD Bar/Counter Tuner" submenu | Roll Speed ± + Reset/Print, opacity items removed | VERIFIED | Lines 547-561, 635-650; wired via `adjustWingNudge`, no opacity actions remain |
| `IsletTests/ActivitySettingsTests.swift` — `testOSDRollSpeedNudgeKeyName` | Passing test, opacity tests removed | VERIFIED | Full suite run: 23/23 pass, 0 failures |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `osdWings(for:)` `OSDLevelBar(fraction: fraction, tint: tint)` call site | `OSDLevelBar` fill Capsule | direct struct call (no opacity params — post-revert) | WIRED | Line 3905; matches reverted (2-param) signature, no dangling opacityMinNudge/opacityMaxNudge args |
| `osdWings(for:)` `DigitRollText(value: percent, rollResponse: 0.30 + osdRollSpeedNudge)` call site | `DigitRollText` struct | direct struct call, same `percentTextWidth`/`percentGap` layout slot | WIRED | Line 3914; `rollResponse` base retuned 0.15 -> 0.30 per Round 1 gap closure |
| `AppDelegate.swift debugOSDRollSpeedMinus/Plus()` | `ActivitySettings.debugOSDRollSpeedNudgeKey` | `adjustWingNudge(_:by:) -> UserDefaults.standard` | WIRED | Lines 637-638 |
| `NotchPillView.swift osdRollSpeedNudge` | `ActivitySettings.debugOSDRollSpeedNudgeKey` | `@AppStorage` | WIRED | Lines 191, 236-238 |

### Behavioral Spot-Checks / Build Verification

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full project compiles clean | `xcodebuild build -scheme Islet -destination 'platform=macOS'` | `** BUILD SUCCEEDED **` | PASS |
| ActivitySettingsTests suite (incl. 3 OSD-related tests) | `xcodebuild test -scheme Islet -destination 'platform=macOS' -only-testing:IsletTests/ActivitySettingsTests` | `Executed 23 tests, with 0 failures` | PASS |
| No debt markers (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER) in touched files | `grep -n -E "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` across all 4 modified files | 0 matches | PASS |
| No dead opacity-ramp code remains after revert | `grep -n "opacityMin\|opacityMax\|OpacityNudge\|osdOpacity"` in `NotchPillView.swift` | 0 matches | PASS |

### Requirements Coverage

*Confirmed per task instructions: no formal `.planning/REQUIREMENTS.md` entries exist for D-01..D-05 — this is an inserted phase (72.1) whose requirement source is `72.1-CONTEXT.md`'s locked decisions, not the REQUIREMENTS.md traceability table. `grep -n "D-0[1-5]"` against REQUIREMENTS.md returns no phase-72.1-scoped matches (only unrelated D-02/D-13..D-17 IDs from other phases, e.g. Phase 51-53 and Phase 72). This is expected and not a gap — ROADMAP.md's own Requirements line for Phase 72.1 states "no formal REQUIREMENTS.md IDs exist for this inserted phase" verbatim.*

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| D-01 | 72.1-02 | Opacity ramp (originally: opacity-driven brightness) | SUPERSEDED (approved) | Reverted per on-device rejection; bar always full opacity |
| D-02 | 72.1-02 | No opacity floor | SUPERSEDED (approved) | N/A after revert — no ramp exists to floor |
| D-03 | 72.1-02 | Identical treatment Volume/Brightness | SUPERSEDED (approved) | N/A after revert — both bars now identically full-opacity (trivially identical) |
| D-04 | 72.1-02 | Roll/slide odometer counter replaces fade | SATISFIED | `DigitRollText`, move-only transitions, no fade |
| D-04a | 72.1-02 | Never opacity fade/blend, digits always opaque | SATISFIED | Confirmed via grep (no `.opacity`/`.push`/`.combined` in transition) |
| D-04b | 72.1-02 | Net-new component (not a parameter tweak) | SATISFIED | New `private struct DigitRollText: View` added, not a tuned `.numericText()` |
| D-05 | 72.1-01 | DEBUG-only live tuner axis mirroring Wing Tuner | SATISFIED | Roll-speed tuner present and wired; opacity half correctly removed alongside D-01/D-02/D-03 revert |

### Anti-Patterns Found

None blocking. Two pre-existing REVIEW.md warnings remain open (both advisory, DEBUG-only surface, not blocking phase goal):

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `Islet/Notch/NotchPillView.swift` | 236 (`osdRollSpeedNudge`) / `AppDelegate.swift:637-638` | No lower-bound clamp on `debugOSDRollSpeedNudge` — repeated "Roll Speed -0.05" clicks can drive `rollResponse` to 0/negative, an invalid SwiftUI spring value | WARNING | DEBUG-only risk (Release always resolves to 0); same class of bug previously caused a real on-device freeze elsewhere in this codebase (corner-radius nudge, commit `c732446`) before it was clamped |
| `Islet/Notch/NotchPillView.swift` | 5083, 5119-5130 | Rapid-repeat coalescing logic (`rapidRepeatThreshold`, `isRapid`) has zero unit test coverage — no `DigitRollTextTests.swift` exists | WARNING | Breaks this codebase's own established test-seam discipline for view-logic (cf. `EqualizerBars.targetHeight`, `OSDActivityTests`); the specific on-device bug this logic fixes (stuck mid-transition digits) has no regression safety net |

Both were raised in `72.1-REVIEW.md` (WR-02, WR-03) and remain unaddressed as of this verification — confirmed still present by direct grep against current source. Neither blocks the phase goal (digit-roll counter works correctly on-device per 72.1-03-SUMMARY.md); they are code-quality debt, not functional gaps.

## Human Verification Required

### 1. Re-confirm digit-count boundary crossing correctness after the post-checkpoint WR-01 fix

**Test:** Build and run the Debug configuration on real hardware. Hold a volume or brightness key to drive the value across 9->10, 10->9, 99->100, and 100->99 boundaries. Watch which digit visually rolls.
**Expected:** The ones-place digit stays anchored to the rightmost slot and rolls in place (e.g., visible 9->0 roll) while a new tens-place digit slides in from the top at the newly-inserted leftmost slot — not the reverse (old bug: the leftmost slot rolled 9->1 while a fresh "0" silently appeared at the second slot). No fading anywhere. No stuck/frozen digits under continued rapid repeat. 100% still renders without clipping.
**Why human:** SwiftUI transition/animation rendering on a live `NSStatusItem` window cannot be asserted via XCTest in this codebase (per `72.1-VALIDATION.md`). The 72.1-03 UAT checkpoint's final "approved" signoff (Round 3, "Ja passt soweit erstmal") happened BEFORE the WR-01 place-value identity fix (commit `2b57127`) landed via code review — `72.1-REVIEW.md`'s own resolution note is explicit: "Not yet re-verified on-device by the user for the specific 9<->10 / 99<->100 boundary crossings." Code inspection confirms the fix is structurally correct (identity now keyed by `placeValue`, build succeeds, tests pass), but the exact on-device digit-count-boundary behavior this whole component exists to get right has not been re-confirmed live since the fix.

## Gaps Summary

No blocking gaps. All 7 requirement IDs (D-01 through D-05) are accounted for:
- D-01/D-02/D-03 are intentionally superseded by explicit on-device user rejection and re-approval (documented trail: 72.1-CONTEXT.md original lock -> 72.1-02 implementation -> 72.1-03 Round 1 rejection -> quick task 260731-61m revert -> 72.1-03 Round 3 approval). Verified the CURRENT approved behavior (always full opacity) is what's actually on disk — it is.
- D-04/D-04a/D-04b/D-05 are implemented, wired, building, and tested, including a code-review-driven correctness fix (WR-01, commit `2b57127`) applied after the original UAT approval.

The single open item is procedural, not a code gap: the WR-01 correctness fix for digit-count boundary crossings was never re-run through the human on-device checkpoint that the rest of this phase went through. Since the underlying bug (rolling the wrong digit at 9<->10/99<->100) was a genuine visual defect the whole DigitRollText component exists to avoid, and the fix landed after the user's final approval, this needs a human eyes-on pass before the phase can be considered fully closed — hence `status: human_needed` rather than `passed`.

Two advisory code-quality warnings (WR-02 unclamped roll-speed nudge, WR-03 missing test coverage for coalescing logic) remain open per `72.1-REVIEW.md` — DEBUG-only surface, non-blocking, noted for awareness.

---

*Verified: 2026-07-31T18:52:00Z*
*Verifier: Claude (gsd-verifier)*
