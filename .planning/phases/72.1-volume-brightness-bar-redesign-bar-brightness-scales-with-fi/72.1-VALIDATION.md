---
phase: 72.1
slug: volume-brightness-bar-redesign-bar-brightness-scales-with-fi
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-31
---

# Phase 72.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | XCTest (existing `IsletTests` target) |
| **Config file** | `Islet.xcodeproj` scheme "Islet" — no separate `.xctestplan` found |
| **Quick run command** | `xcodebuild test -scheme Islet -destination 'platform=macOS' -only-testing:IsletTests/ActivitySettingsTests` |
| **Full suite command** | `xcodebuild test -scheme Islet -destination 'platform=macOS'` |
| **Estimated runtime** | ~90 seconds (quick) / full suite per project history |

---

## Sampling Rate

- **After every task commit:** `xcodebuild build -scheme Islet -destination 'platform=macOS'` (compiles clean) + the new `ActivitySettingsTests` case
- **After every plan wave:** Full suite (`xcodebuild test -scheme Islet -destination 'platform=macOS'`) — expect the same pre-existing 6 known failures (`LicenseStateTests` x4, `SettingsViewTests` x2), zero new failures
- **Before `/gsd-verify-work`:** Full suite must be green (excluding the 6 known pre-existing failures) + on-device checkpoint below
- **Max feedback latency:** ~90 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 72.1-01-0N | TBD | TBD | D-05 | — | New `@AppStorage` tuner keys have exact expected string names | unit | `xcodebuild test -scheme Islet -only-testing:IsletTests/ActivitySettingsTests` | ✅ existing file (`ActivitySettingsTests.swift`), pattern precedent at line 131-133 (`testWingCornerRadiusNudgeKeyName`) | ⬜ pending |
| 72.1-0N-0N | TBD | TBD | D-01/D-02/D-03 | — | Fill `Capsule` alpha tracks `fraction`, no floor, applies identically to both bars | manual-only | — (see Manual-Only Verifications) | N/A | ⬜ pending |
| 72.1-0N-0N | TBD | TBD | D-04/D-04a/D-04b | — | Digits roll (never fade), stay legible under rapid successive changes, `100%` renders without clipping | manual-only | — (see Manual-Only Verifications) | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
*Task IDs are TBD placeholders — the planner assigns final plan/task IDs; update this map once PLAN.md files exist.*

---

## Wave 0 Requirements

- [ ] One new `ActivitySettingsTests` case per new D-05 `@AppStorage` key (3 keys → 3 one-line assertions), following the exact `testWingCornerRadiusNudgeKeyName` precedent (line 131-133) — add to existing `ActivitySettingsTests.swift`, no new test file needed.

*(No framework install needed — XCTest is already wired and used by 45+ existing test files.)*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Fill bar opacity ramps with level, no floor | D-01/D-02/D-03 | XCTest cannot assert on live SwiftUI transition/animation rendering — structural limitation, consistent with this project's existing pattern (`NotchPillViewTests.swift` never asserts rendered pixel output) | On-device: trigger Volume and Brightness OSD via keys across the 0–100% range; confirm bar visibly dims toward invisible near 0% and brightens to full opacity at 100%, identically for both green (volume) and orange (brightness) bars |
| Percent counter rolls, never fades, stays legible under rapid change | D-04/D-04a/D-04b | Same structural limitation — animation *feel* is not meaningfully XCTest-testable | On-device: hold down a volume/brightness key to trigger rapid repeat OSD updates; confirm digits slide/roll (not fade/blend) and remain readable throughout; confirm `100%` renders without clipping in the existing `percentTextWidth` slot |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
