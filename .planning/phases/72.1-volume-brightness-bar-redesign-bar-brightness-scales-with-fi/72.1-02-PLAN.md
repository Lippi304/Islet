---
phase: 72.1
plan: 02
type: execute
wave: 2
depends_on: ["72.1-01"]
files_modified: [Islet/Notch/NotchPillView.swift]
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-04a, D-04b]
must_haves:
  truths:
    - "D-01/D-02/D-03: The Volume (green) and Brightness (orange) bar fill visibly dims as level drops and brightens as level rises, becoming nearly invisible near 0%, identically for both activities"
    - "D-04/D-04a: The percent counter's digits slide/roll into place on value change — they never fade, blend, or cross-dissolve, including when the digit COUNT changes (9↔10, 99↔100)"
    - "D-04b: A 100% value renders as 3 digits without clipping inside the existing percentTextWidth slot"
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "OSDLevelBar fill Capsule with fraction-driven .opacity(...)"
      contains: ".opacity(opacityMin"
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "DigitRollText — new private per-digit roll component, replaces the old numericText Text"
      contains: "struct DigitRollText"
  key_links:
    - from: "osdWings(for:) OSDLevelBar(fraction:tint:) call site"
      to: "OSDLevelBar's fill Capsule .opacity(...) modifier"
      via: "same fraction value driving both .frame(width:) and .opacity(...)"
      pattern: "\\.opacity\\(opacityMin"
    - from: "osdWings(for:) percent Text call site"
      to: "DigitRollText(value: percent)"
      via: "direct struct call, same percentTextWidth/percentGap layout slot"
      pattern: "DigitRollText\\(value: percent"
    - from: "DigitRollText per-digit Text.id(digit)"
      to: ".transition(.move(edge: .top))"
      via: "identity change on digit character triggers SwiftUI insert/remove transition"
      pattern: "\\.transition\\(\\.move\\(edge:"
---

<objective>
Implement the two visual changes this phase exists for: (1) `OSDLevelBar`'s fill `Capsule` gets a fraction-driven opacity ramp (D-01/D-02/D-03 — no floor, identical for Volume/Brightness), and (2) the percent counter's `.contentTransition(.numericText(value:))` is replaced by a new `DigitRollText` component that rolls/slides digits and never fades (D-04/D-04a/D-04b).

Purpose: Deliver the actual user-visible redesign — the fade-based counter was confirmed unreadable during rapid key-repeat by the user's own on-device test (72.1-CONTEXT.md D-04).
Output: `OSDLevelBar`'s fill gains `.opacity(...)`; a new `DigitRollText` private view replaces the percent `Text` at its exact call site.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72.1-volume-brightness-bar-redesign-bar-brightness-scales-with-fi/72.1-CONTEXT.md
@.planning/phases/72.1-volume-brightness-bar-redesign-bar-brightness-scales-with-fi/72.1-RESEARCH.md
@.planning/phases/72.1-volume-brightness-bar-redesign-bar-brightness-scales-with-fi/72.1-PATTERNS.md
@.planning/phases/72.1-volume-brightness-bar-redesign-bar-brightness-scales-with-fi/72.1-UI-SPEC.md

<interfaces>
<!-- Read points Plan 72.1-01 (Wave 1) creates and this plan consumes. Verified by direct file read. -->

From Islet/Notch/NotchPillView.swift (added by Plan 72.1-01, same file/class as this plan's edits):
```swift
private var osdOpacityMinNudge: Double { /* #if DEBUG stored value #else 0 */ }
private var osdOpacityMaxNudge: Double { /* #if DEBUG stored value #else 0 */ }
private var osdRollSpeedNudge: Double { /* #if DEBUG stored value #else 0 */ }
```

Existing locals already computed in `osdWings(for:)` before the two call sites this plan edits (lines 3764-3782, UNCHANGED by this plan):
```swift
let percent: Int        // 0...100, already clamped by OSDActivity.swift
let tint: Color          // Color.green (volume) or Color.orange (brightness) — D-02 (Phase 39) FIXED, never accent-tinted
let fraction: CGFloat    // activity.isMuted ? 0.0 : CGFloat(percent) / 100.0
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: OSDLevelBar opacity ramp (D-01/D-02/D-03)</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift (lines 5040-5053, the exact current `OSDLevelBar` struct being modified)
    Islet/Notch/NotchPillView.swift (lines 3764-3800, `osdWings(for:)`'s `fraction`/`tint` computation feeding `OSDLevelBar`)
  </read_first>
  <action>
    In `Islet/Notch/NotchPillView.swift`'s `OSDLevelBar` struct (lines 5040-5053), add a `.opacity(...)` modifier to the fill `Capsule` (the one with `.fill(tint).frame(width: geo.size.width * fraction)`), computed as `opacityMin + (opacityMax - opacityMin) * Double(fraction)` where `opacityMin = 0.0 + osdOpacityMinNudge` and `opacityMax = 1.0 + osdOpacityMaxNudge` — read `osdOpacityMinNudge`/`osdOpacityMaxNudge` from the parent `NotchPillView` (Plan 72.1-01's read points) and pass them into `OSDLevelBar` as two new `init` parameters (same pattern as `fraction`/`tint` already being passed in — `OSDLevelBar` itself is a plain leaf struct, it does not declare its own `@AppStorage`). Update the single `OSDLevelBar(fraction: fraction, tint: tint)` call site inside `osdWings(for:)` (line ~3889) to also pass `opacityMinNudge: osdOpacityMinNudge, opacityMaxNudge: osdOpacityMaxNudge`.

    Do NOT add a second `.animation(...)` modifier — the existing `.animation(.spring(response: 0.15, dampingFraction: 0.86), value: fraction)` on the same `Capsule` already animates both `.frame(width:)` and the new `.opacity(...)` simultaneously, since both read `fraction` on the same view instance.

    Do NOT touch `tint`, the track `Capsule().fill(Color.white.opacity(0.15))`, or introduce any `Color(hue:saturation:brightness:)`/HSB interpolation — D-01 explicitly rejected hue/lightness changes in favor of alpha-only.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'struct OSDLevelBar' -A 20 Islet/Notch/NotchPillView.swift` shows the fill `Capsule` with an `.opacity(` expression referencing `opacityMin`/`opacityMax` (or equivalently-named locals derived from the two new init params) and `Double(fraction)`
    - `OSDLevelBar`'s `init` (implicit or explicit) accepts `opacityMinNudge`/`opacityMaxNudge` in addition to the existing `fraction`/`tint`
    - The single call site in `osdWings(for:)` passes both new parameters
    - Only one `.animation(...)` modifier exists on the fill `Capsule` (no duplicate added)
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero new errors/warnings
  </acceptance_criteria>
  <done>Fill Capsule's opacity ramps 0(+nudge)→1(+nudge) with fraction, wired to Plan 72.1-01's DEBUG nudges, build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DigitRollText component (D-04/D-04a/D-04b)</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <behavior>
    - Value 0-9 (single digit): one digit sub-view, its `Text` content updates in place via `.id()` identity change, triggering `.transition(.move(edge: .top))` — never `.opacity`, never `AnyTransition.push(from:)`.
    - Value 10-99 (two digits): both digit positions render independently; a same-digit-count boundary crossing like 39→40 changes BOTH digit identities in the same update — both positions transition simultaneously (correct odometer behavior, not a bug).
    - Digit-COUNT boundary crossings (9→10, 10→9, 99→100, 100→99): `ForEach(..., id: \.offset)` inserts or removes an entire digit position — a new/removed view identity with no view-diff continuity. This ONLY animates via `.move` (never falls back to SwiftUI's default opacity-fade insertion/removal) if the outer per-digit wrapper carries its OWN explicit `.transition(.move(edge: .top))`, in addition to the inner `Text`'s. Both the inner `Text.id(digit)` transition (for in-place digit character swaps) and the outer per-digit wrapper's transition (for whole-position insert/remove) are required — the inner one alone does NOT cover this case, since the newly-inserted/removed view is the wrapper, not the inner `Text`.
    - Value 100 (three digits, the real clamp ceiling from `OSDActivity.swift`): renders 3 digit sub-views without clipping inside the existing `percentTextWidth: CGFloat = 22` slot.
  </behavior>
  <read_first>
    Islet/Notch/NotchPillView.swift (lines 3880-3908, the exact current percent `Text` block being replaced, plus its `percentTextWidth`/`percentGap` layout slot at lines 3847-3848)
    Islet/Notch/NotchPillView.swift (lines 5040-5053, `OSDLevelBar` — placement precedent for where to add the new `DigitRollText` struct, same file, same visibility level, immediately after it)
  </read_first>
  <action>
    In `Islet/Notch/NotchPillView.swift`, add a new `private struct DigitRollText: View` immediately after `OSDLevelBar` (after line 5053). It takes `let value: Int` (0...100, already clamped upstream by `OSDActivity.swift` — no re-clamping needed) and a roll-speed input derived from the parent's `osdRollSpeedNudge` (Plan 72.1-01's read point), e.g. an `let rollResponse: Double` init param computed by the caller as `0.15 + osdRollSpeedNudge`. Body: split `String(value)` into `Character`s, render one per-digit wrapper per digit inside an `HStack(spacing: 0)`, structured as `ZStack { Text(String(digit)).id(digit).transition(.move(edge: .top)) }.frame(width: 8).clipped().transition(.move(edge: .top))` — note the `.transition(.move(edge: .top))` appears TWICE: once on the inner `Text` (fires when a digit's character changes in place, e.g. 4→5 at a fixed position) and once on the OUTER wrapper after `.clipped()` (fires when `ForEach(..., id: \.offset)` inserts or removes an entire digit position due to a digit-count change, e.g. 9→10 adding a tens position, or 100→99 removing a hundreds position). Without the outer wrapper's own explicit transition, SwiftUI falls back to its default opacity-fade for that insert/remove — which is exactly the fade D-04a forbids. Apply `.font(.system(size: 11, weight: .semibold, design: .monospaced))`, `.foregroundStyle(.white)` (D-02, Phase 39 locked — never accent-tinted), and `.animation(.spring(response: rollResponse, dampingFraction: 0.86), value: value)` on the outer `HStack`.

    Replace the existing percent `Text("\(percent)")...` block (lines 3898-3907, including its `.contentTransition(.numericText(value:))` and the Quick-task-260729-5pv comment above it — remove that comment, it documents the mechanism being replaced) with a call to `DigitRollText(value: percent, rollResponse: 0.15 + osdRollSpeedNudge)` inside the exact same `.frame(width: percentTextWidth, alignment: .leading)` slot, so `percentTextWidth`/`percentGap` layout math is untouched.

    Do NOT use `AnyTransition.push(from:)` (fades by design) and do NOT combine `.move` with `.opacity` in either transition (`.transition(.move(edge: .top).combined(with: .opacity))` reintroduces exactly the fade D-04a rejects) — `.move` alone, nothing combined, on BOTH the inner `Text` and the outer wrapper.

    On-device verification of the `value = 100` (3-digit) fit inside the 22pt `percentTextWidth` slot happens in Plan 72.1-03's checkpoint, not in this task — if it visibly clips during that checkpoint, the fix (minimal width increase) is scoped to Plan 72.1-03's gap-closure, not required here.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'struct DigitRollText' Islet/Notch/NotchPillView.swift` finds the new struct, placed after `OSDLevelBar`
    - `grep -n '.id(digit)' Islet/Notch/NotchPillView.swift` finds the identity-triggering line inside `DigitRollText`
    - `grep -c '\.transition(\.move(edge: \.top))' Islet/Notch/NotchPillView.swift` returns at least 2 inside `DigitRollText` (one on the inner `Text`, one on the outer per-digit wrapper after `.clipped()`) — confirms digit-count boundary crossings (9↔10, 99↔100) also transition via move, not the default opacity-fade
    - `grep -n '.transition(.move(edge:' Islet/Notch/NotchPillView.swift` finds only `.move` transitions, with no `.combined(with: .opacity)` anywhere in the same statement
    - `grep -c 'push(from:' Islet/Notch/NotchPillView.swift` returns 0 (confirms `AnyTransition.push` was never used)
    - `grep -c 'contentTransition(.numericText' Islet/Notch/NotchPillView.swift` returns 0 (confirms the old fade-based transition is fully removed, not left as dead code alongside the new one)
    - The `osdWings(for:)` call site now calls `DigitRollText(value: percent, rollResponse: 0.15 + osdRollSpeedNudge)` inside the unchanged `.frame(width: percentTextWidth, alignment: .leading)` slot
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero new errors/warnings
  </acceptance_criteria>
  <done>Old numericText Text is fully removed; DigitRollText renders in its place, both digit character swaps AND digit-count insert/remove transition via move only (never fade), build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed by this plan | Pure view-layer rendering of an already-clamped `Int` (`OSDActivity.swift`'s `min(100, max(0, percent))`, untouched by this plan) — no new untrusted input, no I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-72.1-02 | Denial of Service (rendering) | `DigitRollText` under rapid key-repeat | accept | SwiftUI's `.animation(_:value:)`/`.transition()` are designed to retarget cleanly mid-flight on rapid value changes (already proven by `OSDLevelBar`'s own `fraction` updates under the same key-repeat rate, per RESEARCH.md Pitfall 4) — no new mitigation needed beyond reusing the already-working spring |
| T-72.1-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero new dependencies, first-party SwiftUI APIs only |

No realistic STRIDE-relevant threat surface — this plan is two additive, purely visual rendering changes to already-validated data.
</threat_model>

<verification>
Run `xcodebuild build -scheme Islet -destination 'platform=macOS'` after both tasks — must compile clean. No automated test covers the visual/animation behavior itself (RESEARCH.md/VALIDATION.md: manual-only, structural SwiftUI testing limitation) — verified on-device in Plan 72.1-03.
</verification>

<success_criteria>
- `OSDLevelBar`'s fill `Capsule` opacity ramps with `fraction`, no floor, identical for Volume/Brightness
- Old `.contentTransition(.numericText(value:))` percent `Text` fully removed
- New `DigitRollText` renders digits via `.id()` + `.transition(.move(edge: .top))` on BOTH the inner `Text` and the outer per-digit wrapper, never `.opacity`/`.push(from:)`, so digit-count boundary crossings (9↔10, 99↔100) never fade
- Both changes wired to Plan 72.1-01's DEBUG nudges
- Project builds clean
</success_criteria>

<output>
Create `.planning/phases/72.1-volume-brightness-bar-redesign-bar-brightness-scales-with-fi/72.1-02-SUMMARY.md` when done
</output>
