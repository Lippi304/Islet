---
phase: 72-calendar-redesign-native-calendar-clone
plan: 04
type: execute
wave: 3
depends_on: ["72-01", "72-03"]
files_modified:
  - Islet/Notch/NotchWindowController.swift
autonomous: false
requirements: [CALVIEW-08, CALVIEW-09]

must_haves:
  truths:
    - "Editing an event through the redesigned Calendar view persists the change in Calendar.app (D-07/D-09)"
    - "Deleting an event through the redesigned Calendar view removes it from Calendar.app immediately, with no confirm dialog (D-08)"
    - "The rendered Calendar tab visually matches reference-macos-calendar-widgets.png for badges, weekday header, chevron spacing, and typography (CALVIEW-09)"
    - "The two-column layout's agenda scrolls to the tapped day and shows the whole visible month grouped by day, with no clipping at the current calendarWidth (CALVIEW-08)"
  artifacts:
    - path: "Islet/Notch/NotchWindowController.swift"
      provides: "handleEventEdit/handleEventDelete handlers + NotchPillView(...) closure wiring for onEventEdit/onEventDelete"
      contains: "func handleEventEdit"
  key_links:
    - from: "Islet/Notch/NotchPillView.swift (onEventEdit/onEventDelete closures, Plan 72-03)"
      to: "Islet/Notch/NotchWindowController.swift (handleEventEdit/handleEventDelete)"
      via: "[weak self] closure forwarding at the NotchPillView(...) call site, mirroring onQuickAdd's forwarding shape"
      pattern: "self\\?\\.handleEventEdit"
---

<objective>
Wire the view-layer's `onEventEdit`/`onEventDelete` report-intent closures (Plan 72-03) to real `CalendarService.updateEvent`/`deleteEvent` calls (Plan 72-01) via two new controller handlers mirroring `handleQuickAdd`'s exact shape, then run the consolidated on-device UAT checkpoint covering every manual-only verification item from `72-VALIDATION.md`: the two-column scrollable agenda (CALVIEW-08), visual fidelity against the reference screenshot (CALVIEW-09), and the full create/edit/delete round-trip against the real Calendar.app (D-07/D-08/D-09).

Purpose: Close the loop between the redesigned Calendar view and the real EventKit store, then get human confirmation that this phase's goal — a genuine 1:1 visual clone with a working scrollable agenda and event CRUD — is actually achieved on-device.
Output: `NotchWindowController.handleEventEdit`/`handleEventDelete`, `NotchPillView(...)`'s two new closure wire-ups, and an approved on-device UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-calendar-redesign-native-calendar-clone/72-CONTEXT.md
@.planning/phases/72-calendar-redesign-native-calendar-clone/72-VALIDATION.md
@.planning/phases/72-calendar-redesign-native-calendar-clone/reference-macos-calendar-widgets.png

<interfaces>
`handleQuickAdd`/`refreshCalendarMonth` (Islet/Notch/NotchWindowController.swift, lines
2391-2463) — the exact handler shape new methods mirror:
```
private func refreshCalendarMonth() {
    calendarService.fetchMonth(containing: calendarViewState.visibleMonth) { [weak self] events in
        self?.calendarViewState.monthEvents = events
    }
}

private func handleQuickAdd(_ kind: QuickAddKind, title: String, start: Date, end: Date?) {
    switch kind {
    case .event:
        calendarService.createEvent(title: title, start: start, end: end ?? start.addingTimeInterval(3600)) { [weak self] _ in
            self?.refreshCalendarMonth()
        }
    case .reminder:
        calendarService.createReminder(title: title, dueDate: start) { _ in }
    }
}
```

View-initializer closure-forwarding block (same file, lines 3122-3127) — the exact style the two
new closures are added to:
```
onSwitcherSelect: { [weak self] view in self?.handleSwitcherSelect(view) },
onCalendarMonthChange: { [weak self] delta in self?.handleCalendarMonthChange(delta) },
onCalendarDaySelect: { [weak self] day in self?.handleCalendarDaySelect(day) },
onQuickAdd: { [weak self] kind, title, start, end in self?.handleQuickAdd(kind, title: title, start: start, end: end) },
```

`CalendarService.updateEvent`/`deleteEvent` (Plan 72-01, `Islet/Calendar/CalendarService.swift`):
```
func updateEvent(id: String, title: String, start: Date, end: Date, completion: @escaping (Bool) -> Void)
func deleteEvent(id: String, completion: @escaping (Bool) -> Void)
```

`NotchPillView.onEventEdit`/`onEventDelete` (Plan 72-03, `Islet/Notch/NotchPillView.swift`):
```
var onEventEdit: (String, String, Date, Date) -> Void = { _, _, _, _ in }
var onEventDelete: (String) -> Void = { _ in }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: handleEventEdit/handleEventDelete handlers + NotchPillView closure wiring (D-09)</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift (lines 2388-2463 for refreshCalendarMonth/handleQuickAdd's exact shape; lines 3100-3134 for the view-initializer closure-forwarding block)
    Islet/Calendar/CalendarService.swift (updateEvent/deleteEvent signatures, added by Plan 72-01)
    Islet/Notch/NotchPillView.swift (onEventEdit/onEventDelete closure signatures, added by Plan 72-03)
  </read_first>
  <action>
    Add two new private methods directly after `handleQuickAdd` (after line 2463), mirroring its exact `[weak self]`-completion-closure shape: `private func handleEventEdit(id: String, title: String, start: Date, end: Date) { calendarService.updateEvent(id: id, title: title, start: start, end: end) { [weak self] _ in self?.refreshCalendarMonth() } }` and `private func handleEventDelete(id: String) { calendarService.deleteEvent(id: id) { [weak self] _ in self?.refreshCalendarMonth() } }` — both refresh the month on completion regardless of the underlying success/failure result, mirroring `handleQuickAdd`'s `_ in self?.refreshCalendarMonth()` and this codebase's established "settle silently on failure" discipline (T-28-05).
    Wire two new trailing closures into the `NotchPillView(...)` initializer call site immediately after the existing `onQuickAdd:` entry (line 3127): `onEventEdit: { [weak self] id, title, start, end in self?.handleEventEdit(id: id, title: title, start: start, end: end) },` and `onEventDelete: { [weak self] id in self?.handleEventDelete(id: id) },`.
  </action>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodebuild build -scheme Islet -destination 'platform=macOS' && xcodebuild test -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with no new errors
    - `xcodebuild test -scheme Islet -destination 'platform=macOS'` shows zero new failures against the ~569-tests/6-known-failures baseline
    - `grep -c "func handleEventEdit" Islet/Notch/NotchWindowController.swift` returns 1
    - `grep -c "func handleEventDelete" Islet/Notch/NotchWindowController.swift` returns 1
    - `grep -n "onEventEdit: { \[weak self\]" Islet/Notch/NotchWindowController.swift` returns 1
    - `grep -n "onEventDelete: { \[weak self\]" Islet/Notch/NotchWindowController.swift` returns 1
  </acceptance_criteria>
  <done>handleEventEdit/handleEventDelete call the real CalendarService methods and refresh the month afterward; both are wired into the NotchPillView(...) call site; build and full test suite are clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Consolidated on-device UAT — visual fidelity, agenda scroll, and event CRUD round-trip</name>
  <what-built>
    A redesigned expanded Calendar view: a scrollable, whole-month, day-grouped agenda (right column) that jumps-to-date when the month grid (left column) is tapped; a today=filled-red/selected=red-ring badge swap, a new weekday-letter header row, tightened chevron spacing, and a red month/year label (all against reference-macos-calendar-widgets.png); hover-reveal event delete (no confirm dialog) and a pre-filled edit popover with a full-title hover tooltip; and real EventKit update/delete wired through CalendarService.
  </what-built>
  <how-to-verify>
    1. Build and launch the app (Debug), open the Calendar tab.
    2. Compare the rendered month grid — weekday header row, chevron spacing, today's filled red circle, a different selected day's thin red outline ring (never both on one cell), and the red month/year label — side-by-side against `.planning/phases/72-calendar-redesign-native-calendar-clone/reference-macos-calendar-widgets.png` (CALVIEW-09). Confirm weekday letters align with the date columns beneath them.
    3. Confirm the right column shows every day with events across the WHOLE visible month (not just the selected day), grouped under "WEEKDAY, D MON" headers (D-01).
    4. Tap several different days in the month grid; confirm the agenda scrolls smoothly to that day's header instead of filtering or re-fetching the list (D-02).
    5. Hover an event row; confirm a trash icon fades in on the trailing edge (and is invisible without hover, D-08). Click it; confirm the event disappears immediately with no confirm dialog, then check Calendar.app to confirm it was actually deleted.
    6. Tap an event row (not the trash icon); confirm a popover opens pre-filled with that event's real title/start/end; change the title and tap Save; confirm the agenda updates and Calendar.app reflects the new title (D-07/D-09).
    7. Hover a truncated event title; confirm a native tooltip shows the full text (D-12).
    8. Create a new event via the existing "+ Add" popover; confirm it appears correctly grouped in the new agenda (regression check on the unmodified create path).
    9. Confirm no visual clipping or crowding at the current island width (472pt) after the cell/font size bumps.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| SwiftUI view -> NotchWindowController -> EventKit store | This plan closes the final hop of the seam Plan 72-01 already quarantined behind `CalendarService` — no new boundary is introduced, just the last wiring step (view report-intent closure -> controller handler -> existing service) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-08 | Tampering | `handleEventEdit`/`handleEventDelete` | mitigate | Both forward `id`/`title`/`start`/`end` unmodified straight to `CalendarService`, which already enforces T-14-06 (plain-String-only) and T-28-05 (never-crash-on-error) at the service layer (Plan 72-01) — no new validation logic is needed at the controller layer, matching `handleQuickAdd`'s existing precedent of doing zero additional validation beyond what the service/view already do |
| T-72-SC | Tampering (supply chain) | N/A | accept | Zero new packages/dependencies added by this plan |

</threat_model>

<verification>
Task 1: `xcodebuild build -scheme Islet -destination 'platform=macOS'` and `xcodebuild test -scheme Islet -destination 'platform=macOS'` both clean (zero new failures vs. the ~569/6-known baseline). Task 2: on-device UAT approved per the 9-step checklist above, covering every manual-only item in `72-VALIDATION.md`.
</verification>

<success_criteria>
- `handleEventEdit`/`handleEventDelete` exist, call the real `CalendarService` methods, and refresh the month afterward
- The `NotchPillView(...)` call site forwards `onEventEdit`/`onEventDelete` to these handlers
- On-device UAT approved: two-column scrollable day-grouped agenda with working scroll-to-day, visual match to `reference-macos-calendar-widgets.png`, working hover-delete/edit-popover/tooltip, and a confirmed Calendar.app round-trip for create/edit/delete
- Full Debug build and test suite remain clean
</success_criteria>

<output>
Create `.planning/phases/72-calendar-redesign-native-calendar-clone/72-04-SUMMARY.md` when done
</output>
