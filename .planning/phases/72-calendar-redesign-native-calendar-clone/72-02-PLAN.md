---
phase: 72-calendar-redesign-native-calendar-clone
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Notch/NotchPillView.swift
autonomous: true
requirements: [CALVIEW-09]

must_haves:
  truths:
    - "A locale-aware weekday-letter header row (via Calendar.current.veryShortWeekdaySymbols, rotated by calendar.firstWeekday — never a hardcoded string array) renders above the month grid, aligned to the same 7-column LazyVGrid layout as the day numbers"
    - "Today's date renders as a solid Color.red filled circle; a selected (non-today) day renders a thin Color.red outline ring; the two never both appear on the same cell (today wins visually) (D-03)"
    - "The month header's prev/next chevrons sit close to the month/year label (8pt gap) instead of at the row's outer edges"
    - "The month/year label renders in Color.red (was white); the day-number font is 11px (was 9px) inside a 22x22pt cell (was 18x18pt); the has-events dot indicator is unchanged (D-04: keep)"
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "rotatedWeekdaySymbols + weekdayHeaderRow computed properties, badge-swap logic and red accent in monthGridColumn, bumped calendarCellSize"
      contains: "private var rotatedWeekdaySymbols"
  key_links:
    - from: "Islet/Notch/NotchPillView.swift (monthGridColumn's LazyVGrid)"
      to: "Islet/Notch/NotchPillView.swift (weekdayHeaderRow)"
      via: "both reference Self.calendarCellSize/Self.calendarCellGap symbolically so columns stay aligned after the cell-size bump"
      pattern: "Self\\.calendarCellSize"
---

<objective>
Bring `monthGridColumn`'s visual language into 1:1 alignment with `reference-macos-calendar-widgets.png`: add the currently-nonexistent weekday-letter header row (RESEARCH.md Pitfall 2 confirms this is net-new, not a restyle), swap the today/selected badge styling (D-03, today=filled red / selected=red ring — a reversal of today's code), tighten the chevron spacing (D-10), bump the day-number font and cell size (D-11), and turn the month/year label red (D-05). The has-events dot indicator and the whole-month agenda rebuild are explicitly out of this plan's scope (D-04 resolved KEEP; agenda rebuild is Plan 72-03).

Purpose: Deliver CALVIEW-09's visual-fidelity requirement for the month-grid half of the two-column layout.
Output: A new `rotatedWeekdaySymbols`/`weekdayHeaderRow` pair, an inverted today/selected badge pair, tightened chevron spacing, a red month/year label, and a bumped cell/font size — all inside the existing `monthGridColumn` computed property.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-calendar-redesign-native-calendar-clone/72-CONTEXT.md
@.planning/phases/72-calendar-redesign-native-calendar-clone/72-UI-SPEC.md
@.planning/phases/72-calendar-redesign-native-calendar-clone/reference-macos-calendar-widgets.png

<interfaces>
Current `monthGridColumn` (Islet/Notch/NotchPillView.swift, lines 1630-1680) — the exact code
this plan edits:
```
private var monthGridColumn: some View {
    VStack(spacing: 8) {
        HStack {
            Button(action: { onCalendarMonthChange(-1) }) {
                Image(systemName: "chevron.left")
                    .font(.system(size: 12))
                    .foregroundStyle(.white.opacity(0.7))
            }
            .buttonStyle(.plain)
            Spacer()
            Text(calendarViewState.visibleMonth, format: .dateTime.month(.wide).year())
                .font(.system(size: 13, weight: .semibold, design: .rounded))
                .foregroundStyle(.white)
            Spacer()
            Button(action: { onCalendarMonthChange(1) }) {
                Image(systemName: "chevron.right")
                    .font(.system(size: 12))
                    .foregroundStyle(.white.opacity(0.7))
            }
            .buttonStyle(.plain)
        }
        LazyVGrid(columns: Array(repeating: GridItem(.fixed(Self.calendarCellSize), spacing: Self.calendarCellGap), count: 7),
                  spacing: Self.calendarCellGap) {
            ForEach(Array(daysInMonth(for: calendarViewState.visibleMonth).enumerated()), id: \.offset) { _, day in
                if let day {
                    let isSelected = Calendar.current.isDate(day, inSameDayAs: calendarViewState.selectedDay)
                    let isToday = Calendar.current.isDateInToday(day)
                    let hasEvents = calendarViewState.monthEvents.map { !events(on: day, events: $0).isEmpty } ?? false
                    ZStack(alignment: .bottom) {
                        Text(day, format: .dateTime.day())
                            .font(.system(size: 9, weight: isSelected ? .semibold : .regular, design: .rounded))
                            .foregroundStyle(.white)
                            .frame(width: Self.calendarCellSize, height: Self.calendarCellSize)
                            .background(Circle().fill(Color.white.opacity(isSelected ? 0.18 : 0)))
                            .overlay(Circle().strokeBorder(Color.white.opacity(isToday && !isSelected ? 0.6 : 0), lineWidth: 1))
                        if hasEvents {
                            Circle()
                                .fill(Color.white.opacity(0.6))
                                .frame(width: 2, height: 2)
                                .offset(y: -1)
                        }
                    }
                    .frame(width: Self.calendarCellSize, height: Self.calendarCellSize)
                    .onTapGesture { onCalendarDaySelect(day) }
                } else {
                    Color.clear.frame(width: Self.calendarCellSize, height: Self.calendarCellSize)
                }
            }
        }
    }
}
```

Related constants (Islet/Notch/NotchPillView.swift, lines 854-855):
```
static let calendarCellSize: CGFloat = 18
static let calendarCellGap: CGFloat = 2
```

Locale-aware weekday-header derivation (RESEARCH.md Pattern 3 — exact code to add, no analog
exists in this codebase today):
```
private var rotatedWeekdaySymbols: [String] {
    let calendar = Calendar.current
    let symbols = calendar.veryShortWeekdaySymbols   // index 0 = Sunday, always
    let firstWeekdayIndex = calendar.firstWeekday - 1 // Calendar.firstWeekday is 1-based
    return Array(symbols[firstWeekdayIndex...] + symbols[..<firstWeekdayIndex])
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Weekday header row (net-new) + chevron spacing (D-10)</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift (lines 1630-1680 for monthGridColumn; lines 854-855 for calendarCellSize/calendarCellGap)
    .planning/phases/72-calendar-redesign-native-calendar-clone/72-UI-SPEC.md (Layout Contract: "Weekday header row" and "Month header chevrons")
  </read_first>
  <action>
    Add a new private computed property `rotatedWeekdaySymbols: [String]` directly above `monthGridColumn` (before line 1630), implemented exactly per the `<interfaces>` block above — Foundation `Calendar` API only, no hardcoded weekday-letter array (Pitfall 2/Don't-Hand-Roll). Add a new private computed property `weekdayHeaderRow: some View` rendering `HStack(spacing: Self.calendarCellGap)` of 7 `Text` cells (one per `rotatedWeekdaySymbols` entry, `id`-free since it's a fixed 7-count `ForEach(Array(rotatedWeekdaySymbols.enumerated()), id: \.offset)`), each `.frame(width: Self.calendarCellSize)`, `.font(.system(size: 10, weight: .semibold, design: .rounded))`, `.foregroundStyle(.secondary)` — reference `Self.calendarCellSize`/`Self.calendarCellGap` symbolically, never a hardcoded `18`/`22`, so the row stays aligned with the day grid after Task 2's cell-size bump. Insert a call to `weekdayHeaderRow` into `monthGridColumn`'s outer `VStack(spacing: 8)` between the closing `}` of the chevron/month-label `HStack` (currently line 1650) and the `LazyVGrid` (currently line 1651).
    Change the chevron `HStack` (currently lines 1632-1650): replace its current two bare `Spacer()` calls (which push the chevrons to the row's outer edges) with a centered, tightly-spaced group — wrap the chevron-left `Button`, month/year `Text`, and chevron-right `Button` in an inner `HStack(spacing: 8)`, and center that inner `HStack` within the row using a `Spacer()` before and after it (`HStack { Spacer(); innerHStack; Spacer() }`). The chevrons must end up 8pt from the month/year label on each side, not at the grid's outer edges (D-10).
  </action>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodebuild build -scheme Islet -destination 'platform=macOS' && grep -c "private var rotatedWeekdaySymbols" Islet/Notch/NotchPillView.swift && grep -c "private var weekdayHeaderRow" Islet/Notch/NotchPillView.swift</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with no new errors
    - `grep -c "private var rotatedWeekdaySymbols" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "private var weekdayHeaderRow" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "calendar.veryShortWeekdaySymbols" Islet/Notch/NotchPillView.swift` returns 1 (locale-aware, not a hardcoded array)
    - `grep -n "weekdayHeaderRow" Islet/Notch/NotchPillView.swift` shows 2 occurrences (the property definition and its call site inside monthGridColumn)
    - `grep -c "HStack(spacing: 8)" Islet/Notch/NotchPillView.swift` returns at least 1 new occurrence for the tightened chevron group
  </acceptance_criteria>
  <done>weekdayHeaderRow renders above the day grid using locale-aware, alignment-matched symbols; chevrons sit 8pt from the month label instead of the row's outer edges; build is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Badge swap (D-03) + red month-label/badge accent (D-05) + font/cell-size bump (D-11)</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift (lines 1630-1680 for monthGridColumn's day-cell ZStack; line 854 for calendarCellSize)
    .planning/phases/72-calendar-redesign-native-calendar-clone/72-RESEARCH.md (Pitfall 3 — the paired-edit warning)
    .planning/phases/72-calendar-redesign-native-calendar-clone/72-UI-SPEC.md (Color table, Month grid day badge table)
  </read_first>
  <action>
    In `monthGridColumn`'s day-cell `ZStack` (the badge pair — currently `.background(Circle().fill(Color.white.opacity(isSelected ? 0.18 : 0)))` and `.overlay(Circle().strokeBorder(Color.white.opacity(isToday && !isSelected ? 0.6 : 0), lineWidth: 1))`), swap BOTH modifiers together as one paired edit (Pitfall 3 — a diff touching only one is wrong): change `.background` to `.background(Circle().fill(isToday ? Color.red : Color.clear))`; change `.overlay` to `.overlay(Circle().strokeBorder(isSelected && !isToday ? Color.red : Color.clear, lineWidth: 1.5))`. Today wins visually when a day is both today and selected — no ring is drawn in that case, per the reference PNG. Leave the `hasEvents` dot block entirely unchanged (D-04 resolved: KEEP, per UI-SPEC — do not remove or restyle it).
    Change the month/year label's `.foregroundStyle(.white)` to `.foregroundStyle(Color.red)` (D-05 — the reference screenshot's red "JULI"-style month label).
    Bump the day-number `Text`'s font size from `9` to `11`. Bump `Self.calendarCellSize` from `18` to `22` (the static let declaration at line 854) so the larger glyph doesn't collide with the has-events dot or the cell edge (D-11) — `Self.calendarCellGap` (line 855, value `2`) stays unchanged.
  </action>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodebuild build -scheme Islet -destination 'platform=macOS' && grep -n "static let calendarCellSize" Islet/Notch/NotchPillView.swift</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with no new errors
    - `grep -c "isToday ? Color.red : Color.clear" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "isSelected && !isToday ? Color.red : Color.clear" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "static let calendarCellSize: CGFloat = 22" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "static let calendarCellSize: CGFloat = 18" Islet/Notch/NotchPillView.swift` returns 0 (old literal fully replaced)
    - `grep -n "hasEvents" Islet/Notch/NotchPillView.swift | grep -c "Circle().fill(Color.white.opacity(0.6))"` returns 1 (has-events dot styling untouched, D-04)
  </acceptance_criteria>
  <done>Today renders as a solid red circle, a different selected day renders a thin red ring (never both), the month label is red, the day-number font/cell size are bumped, and the has-events dot is untouched; build is clean. Full visual confirmation against reference-macos-calendar-widgets.png happens at Plan 72-04's consolidated on-device UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | Pure in-process SwiftUI rendering-constant/layout change (badge colors, weekday labels derived from the system `Calendar` API, spacing constants); no network, no user-supplied input beyond already-quarantined `EKEvent` data unchanged by this plan, no file I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-04 | All STRIDE categories | `monthGridColumn`/`weekdayHeaderRow` | accept | No new attack surface — this task only changes SwiftUI View rendering constants (colors, font sizes, a Foundation `Calendar.veryShortWeekdaySymbols` read) with no new data ingestion, no new persisted state, and no interaction with untrusted external strings; same class of pure rendering-constant change as the already-shipped Phase 29 (SHAPE-01)/Phase 71 (SHAPE-02) work |

</threat_model>

<verification>
Run `xcodebuild build -scheme Islet -destination 'platform=macOS'` after each task. Run the full XCTest suite once both tasks land: `xcodebuild test -scheme Islet -destination 'platform=macOS'` — confirm zero *new* failures against the documented ~569-tests/6-known-failures baseline (STATE.md). Full visual comparison against `reference-macos-calendar-widgets.png` is deferred to Plan 72-04's consolidated on-device UAT (no SwiftUI snapshot infra exists in this project, per VALIDATION.md).
</verification>

<success_criteria>
- `weekdayHeaderRow` renders above the day grid, locale-aware and column-aligned with the `LazyVGrid` beneath it
- Today's badge is a solid red circle; a selected non-today day is a thin red ring; the two states never coexist on one cell
- Chevrons sit close to the month label (8pt gap); the month/year label is red; day-number font is 11px inside a 22x22pt cell
- The has-events dot indicator is unchanged
- Debug build succeeds with no new errors; full test suite has zero new failures
</success_criteria>

<output>
Create `.planning/phases/72-calendar-redesign-native-calendar-clone/72-02-SUMMARY.md` when done
</output>
