---
phase: 72-calendar-redesign-native-calendar-clone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Calendar/CalendarGlance.swift
  - Islet/Calendar/CalendarService.swift
  - IsletTests/CalendarGlanceTests.swift
autonomous: true
requirements: [CALVIEW-08]

must_haves:
  truths:
    - "eventsByDay(events:calendar:) groups events by calendar day (Dictionary(grouping:) keyed on startOfDay(for:), never a formatted String) and sorts groups ascending by day and each group's events ascending by start time, returning [] for an empty input without crashing"
    - "EventInput carries the real EKEvent.eventIdentifier as a new `id: String` field, populated at the single mapToEventInput(_:) call site so both fetchUpcoming and fetchMonth deliver it to the view layer"
    - "CalendarService exposes updateEvent(id:title:start:end:completion:) and deleteEvent(id:completion:), both looking up store.event(withIdentifier:) and settling completion(false) — never crashing — if the event no longer exists or the save/remove throws"
  artifacts:
    - path: "Islet/Calendar/CalendarGlance.swift"
      provides: "EventInput.id field + eventsByDay(events:calendar:) pure grouping function"
      contains: "func eventsByDay"
    - path: "Islet/Calendar/CalendarService.swift"
      provides: "updateEvent/deleteEvent protocol methods + EventKitService implementation + id-populating mapToEventInput"
      contains: "func updateEvent(id: String"
    - path: "IsletTests/CalendarGlanceTests.swift"
      provides: "eventsByDay unit test coverage (empty/single-day/multi-day/cross-month-boundary) + all 16 existing EventInput fixtures updated with the new id field"
      contains: "func testEventsByDayGroupsEventsOnSameDaySortedByStartTime"
  key_links:
    - from: "Islet/Calendar/CalendarService.swift (mapToEventInput)"
      to: "Islet/Calendar/CalendarGlance.swift (EventInput.id)"
      via: "ek.eventIdentifier ?? \"\" populates EventInput.id at the single mapToEventInput call site"
      pattern: "id: ek\\.eventIdentifier"
---

<objective>
Add the two pieces of new domain surface that the view-layer redesign (Plans 72-02/72-03) and controller wiring (Plan 72-04) depend on: a pure `eventsByDay(events:calendar:)` grouping function for the whole-month agenda (D-01/D-02), and `CalendarService.updateEvent`/`deleteEvent` (D-09) — both of which require `EventInput` to carry the real `EKEvent.eventIdentifier` first (RESEARCH.md Pitfall 1: `EventInput` currently has no identifier, so update/delete are unimplementable without this field landing first).

Purpose: Establish the data-layer contracts (EventInput.id, eventsByDay, updateEvent, deleteEvent) other plans build the UI against — interface-first, per this phase's own extension-by-analogy convention (every function here mirrors an existing sibling in the same file).
Output: `EventInput.id` field, `eventsByDay(events:calendar:)`, `CalendarService.updateEvent`/`deleteEvent` (protocol + `EventKitService` implementation), and updated unit test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-calendar-redesign-native-calendar-clone/72-CONTEXT.md
@.planning/phases/72-calendar-redesign-native-calendar-clone/72-RESEARCH.md

<interfaces>
Current `EventInput` (Islet/Calendar/CalendarGlance.swift, lines 15-22) — the struct this plan
extends:
```
struct EventInput: Equatable {
    let title: String
    let start: Date
    let end: Date
    let colorRed: Double
    let colorGreen: Double
    let colorBlue: Double
}
```

Current `events(on:events:calendar:)` (same file, lines 115-119) — the exact contract
`eventsByDay` must mirror (Foundation-only, `calendar: Calendar = .current` defaulted last,
total/never-crashes):
```
func events(on day: Date, events: [EventInput], calendar: Calendar = .current) -> [EventInput] {
    events
        .filter { calendar.isDate($0.start, inSameDayAs: day) }
        .sorted { $0.start < $1.start }
}
```

Current `CalendarService` protocol tail + `createEvent` implementation (Islet/Calendar/CalendarService.swift,
lines 35-46, 136-150) — the exact doc-comment + do/catch/completion(false) shape the new methods mirror:
```
    func createEvent(title: String, start: Date, end: Date, completion: @escaping (Bool) -> Void)
    ...
    func createEvent(title: String, start: Date, end: Date, completion: @escaping (Bool) -> Void) {
        let event = EKEvent(eventStore: store)
        event.title = title
        event.startDate = start
        event.endDate = end
        event.calendar = store.defaultCalendarForNewEvents
        do {
            try store.save(event, span: .thisEvent)
            completion(true)
        } catch {
            completion(false)
        }
    }
```

`mapToEventInput(_:)` (same file, lines 124-134) — the ONE call site both `fetchUpcoming` and
`fetchMonth` route through:
```
    private func mapToEventInput(_ ek: EKEvent) -> EventInput {
        var red = 1.0, green = 1.0, blue = 1.0
        if let rgb = ek.calendar.color.usingColorSpace(.deviceRGB) {
            red = Double(rgb.redComponent)
            green = Double(rgb.greenComponent)
            blue = Double(rgb.blueComponent)
        }
        return EventInput(title: ek.title ?? "", start: ek.startDate, end: ek.endDate,
                          colorRed: red, colorGreen: green, colorBlue: blue)
    }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EventInput.id field + eventsByDay(events:calendar:) pure grouping function</name>
  <files>Islet/Calendar/CalendarGlance.swift, IsletTests/CalendarGlanceTests.swift</files>
  <read_first>
    Islet/Calendar/CalendarGlance.swift (full file, 119 lines — EventInput struct lines 15-22, events(on:events:calendar:) lines 115-119)
    IsletTests/CalendarGlanceTests.swift (full file, 232 lines — testEventsOnDayReturnsOnlyMatchingDaySortedAscending/testEventsOnDayReturnsEmptyArrayForEmptyEventsWithoutCrashing, lines 111-134, is the exact fixture convention to mirror)
    .planning/phases/72-calendar-redesign-native-calendar-clone/72-RESEARCH.md (Pattern 1, Pitfall 5)
  </read_first>
  <behavior>
    - Test: eventsByDay(events: [], calendar:) returns [] without crashing (T-14-02 discipline)
    - Test: eventsByDay with 2+ events all on the same day returns a single group whose `events` array is sorted ascending by `start`
    - Test: eventsByDay with events across two different days returns two groups sorted ascending by `day` (earlier day first), each group's own events sorted ascending by start
    - Test: eventsByDay does not collide two different months' identical day-of-month (e.g. July 15 vs August 15) — grouping key must be the exact `startOfDay(for:)` Date, never a formatted day-of-month String (Pitfall 5)
  </behavior>
  <action>
    RED: In `IsletTests/CalendarGlanceTests.swift`, first add `id: "test-id"` (reuse this literal string across every fixture — the id's exact value is irrelevant to these pure-function tests) as a new argument to ALL 16 existing `EventInput(...)` construction call sites in this file, so the file compiles against `EventInput`'s new required field. Then add 4 new test methods implementing the `<behavior>` above, named `testEventsByDayReturnsEmptyArrayForEmptyEventsWithoutCrashing`, `testEventsByDayGroupsEventsOnSameDaySortedByStartTime`, `testEventsByDayGroupsAcrossMultipleDaysSortedAscendingByDay`, and `testEventsByDayDoesNotCollideAcrossMonthsWithSameDayOfMonth` — use the same fixed-UTC `Calendar(identifier: .gregorian)` construction already used by `testEventsOnDayReturnsOnlyMatchingDaySortedAscending` (lines 111-128). Run the CalendarGlanceTests filter now and confirm the 4 new tests fail (RED) — `eventsByDay` does not exist yet.
    GREEN: In `Islet/Calendar/CalendarGlance.swift`, add `let id: String` to the `EventInput` struct (after the `colorBlue` field, line 21). Add `eventsByDay(events: [EventInput], calendar: Calendar = .current) -> [(day: Date, events: [EventInput])]` directly after `events(on:events:calendar:)` (after line 119), implemented as: group `events` by `calendar.startOfDay(for: $0.start)` via `Dictionary(grouping:by:)`, then map the grouped dictionary into an array of `(day:events:)` tuples sorted ascending by the `day` key, with each tuple's `events` array itself sorted ascending by `start` — mirror `events(on:events:calendar:)`'s exact doc-comment convention (Phase/requirement reference, Foundation-only, total). Re-run the same test filter and confirm all 4 new tests pass (GREEN).
  </action>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodebuild test -scheme Islet -destination 'platform=macOS' -only-testing:IsletTests/CalendarGlanceTests</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild test -scheme Islet -destination 'platform=macOS' -only-testing:IsletTests/CalendarGlanceTests` passes, including all 4 new `eventsByDay` tests
    - `grep -c "func eventsByDay" Islet/Calendar/CalendarGlance.swift` returns 1
    - `grep -c "let id: String" Islet/Calendar/CalendarGlance.swift` returns 1
    - `grep -c "Dictionary(grouping:" Islet/Calendar/CalendarGlance.swift` returns 1
    - `grep -c "calendar.startOfDay(for:" Islet/Calendar/CalendarGlance.swift` returns 1 (grouping key is a Date, never a formatted String — Pitfall 5)
  </acceptance_criteria>
  <done>eventsByDay(events:calendar:) exists, is unit-tested (4 new passing tests), and EventInput.id compiles across every existing fixture in CalendarGlanceTests.swift.</done>
</task>

<task type="auto">
  <name>Task 2: CalendarService.updateEvent/deleteEvent (D-09) + populate EventInput.id from EKEvent.eventIdentifier</name>
  <files>Islet/Calendar/CalendarService.swift</files>
  <read_first>
    Islet/Calendar/CalendarService.swift (full file, 178 lines — protocol lines 15-47, createEvent lines 136-150, mapToEventInput lines 124-134)
    .planning/phases/72-calendar-redesign-native-calendar-clone/72-RESEARCH.md (Pitfall 1, Code Examples section — exact updateEvent/deleteEvent bodies)
  </read_first>
  <action>
    In the `CalendarService` protocol (after the `createReminder` declaration, line 46), add two new method declarations: `func updateEvent(id: String, title: String, start: Date, end: Date, completion: @escaping (Bool) -> Void)` and `func deleteEvent(id: String, completion: @escaping (Bool) -> Void)`, each with a doc comment matching this file's existing convention (MAIN thread delivery, D-09: no new permission request needed — reuses the write access already granted via `requestFullAccessToEvents()`, settles `false` on any error or missing event, never retries/re-prompts).
    Implement both on `EventKitService` (directly after `createEvent`, after line 150): each method looks up `store.event(withIdentifier: id) as? EKEvent`, guard-else `completion(false)` (event no longer exists, e.g. deleted externally — never crash, mirrors T-28-05); `updateEvent` sets `event.title = title` (T-14-06: plain String, never interpolated), `event.startDate = start`, `event.endDate = end`, then `try store.save(event, span: .thisEvent)`; `deleteEvent` calls `try store.remove(event, span: .thisEvent)`. Both wrap the throwing call in `do { ...; completion(true) } catch { completion(false) }`, matching `createEvent`'s exact shape.
    Update `mapToEventInput(_:)` (lines 124-134) to populate the new `EventInput.id` field: add `id: ek.eventIdentifier ?? ""` to its single `EventInput(...)` construction (Pitfall 1's documented safe fallback — an empty-string id degrades update/delete to a no-op via the guard-else above, never a crash).
  </action>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with no new errors
    - `grep -c "func updateEvent(id: String" Islet/Calendar/CalendarService.swift` returns 2 (protocol declaration + EventKitService implementation)
    - `grep -c "func deleteEvent(id: String" Islet/Calendar/CalendarService.swift` returns 2 (protocol declaration + EventKitService implementation)
    - `grep -c "store.event(withIdentifier: id)" Islet/Calendar/CalendarService.swift` returns 2 (one per new implementation)
    - `grep -n "id: ek.eventIdentifier" Islet/Calendar/CalendarService.swift` returns exactly 1 match (the single mapToEventInput call site)
  </acceptance_criteria>
  <done>CalendarService exposes updateEvent/deleteEvent backed by real EKEventStore lookups, and EventInput.id is populated from the real EKEvent.eventIdentifier at the one existing mapping call site; build is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| EventKit store (local, OS-level TCC-gated) | `CalendarService`/`EventKitService` is the sole quarantine seam between this app and `EKEventStore`; subscribed/shared calendars can inject arbitrary `EKEvent.title` text, but there is no network or IPC boundary crossed by this plan — everything is local, single-user, already-TCC-gated data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | Tampering | `mapToEventInput(_:)` / new `updateEvent`/`deleteEvent` | mitigate | `ek.title`/user-supplied `title` parameter is always passed through as a plain `String`, never interpolated into a format/log/shell string (T-14-06, unchanged discipline reused verbatim from `createEvent`) |
| T-72-02 | Tampering (identifier misuse) | `EventInput.id` used as `store.event(withIdentifier:)` lookup key | accept | Not attacker-influenceable in this app's real flow — identifiers originate from the local `EKEventStore`, round-trip through the SwiftUI view, back to the same store; never interpolated into any string, only ever used as a dictionary/store key |
| T-72-03 | Denial of Service (crash on missing/mutated event) | `updateEvent`/`deleteEvent` | mitigate | `store.event(withIdentifier:)` result is guard-else `completion(false)` before any force-unwrap or mutation (never `as! EKEvent`); the `do/catch -> completion(false)` shape (T-28-05) means a thrown save/remove error settles silently rather than crashing |
| T-72-SC | Tampering (supply chain) | N/A | accept | Zero new packages/dependencies added by this plan — Package Legitimacy Gate not applicable (Foundation/EventKit only, both already-linked system frameworks) |

</threat_model>

<verification>
Run `xcodebuild test -scheme Islet -destination 'platform=macOS' -only-testing:IsletTests/CalendarGlanceTests` after Task 1, and `xcodebuild build -scheme Islet -destination 'platform=macOS'` after Task 2. Run the full suite once both tasks land: `xcodebuild test -scheme Islet -destination 'platform=macOS'` — confirm zero *new* failures against the documented ~569-tests/6-known-failures baseline (STATE.md).
</verification>

<success_criteria>
- `eventsByDay(events:calendar:)` exists in `CalendarGlance.swift`, is unit-tested, and groups/sorts correctly including the cross-month-boundary edge case
- `EventInput.id` is populated from the real `EKEvent.eventIdentifier` at the single `mapToEventInput` call site
- `CalendarService.updateEvent`/`deleteEvent` exist on both the protocol and `EventKitService`, never crash on a missing event or a thrown save/remove error
- Full Debug build succeeds; full test suite has zero new failures
</success_criteria>

<output>
Create `.planning/phases/72-calendar-redesign-native-calendar-clone/72-01-SUMMARY.md` when done
</output>
