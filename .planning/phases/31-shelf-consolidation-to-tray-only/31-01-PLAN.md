---
phase: 31-shelf-consolidation-to-tray-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Notch/NotchPillView.swift
  - IsletTests/NotchPillViewTests.swift
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [TRAY-01]
user_setup: []

must_haves:
  truths:
    - "shelfStripVisible stays false and is locked by an automated regression test so the Tray-only gate can't silently regress (TRAY-01)"
    - "On-device hover->expand->move-down click-through trace against the shipped shelfStripVisible/visibleContentZone() change shows no CR-01-style phantom click-swallowing regression"
    - "TRAY-01 is marked delivered in ROADMAP.md and REQUIREMENTS.md, crediting quick task 260714-3k6 as the implementation source"
  artifacts:
    - path: "IsletTests/NotchPillViewTests.swift"
      provides: "Regression lock: shelfStripVisible == false, new test file"
      contains: "testShelfStripVisibleIsAlwaysFalse"
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "shelfStripVisible access bumped private -> internal for testability, per EqualizerBars.makeProfiles() precedent"
      contains: "var shelfStripVisible: Bool { false }"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TRAY-01 checked off and traceability status flipped to Complete"
      contains: "TRAY-01"
  key_links:
    - from: "IsletTests/NotchPillViewTests.swift"
      to: "NotchPillView.shelfStripVisible"
      via: "direct property assertion on a NotchPillView instance built with the #Preview constructor pattern"
      pattern: "view\\.shelfStripVisible"
---

<objective>
Close out Phase 31 (TRAY-01) without re-implementing anything: quick task 260714-3k6 already
shipped the `shelfStripVisible` gate and the matching `visibleContentZone()` simplification
(commits `3c6b6fb`/`4011ca1`/`bc1e73a`/`db11d72`) ahead of this phase's formal planning. This
plan (a) locks that behavior with a regression test the codebase currently cannot compile
(the property is `private`), (b) re-runs this project's mandatory on-device CR-01-class
click-through trace specifically against the shipped change — the quick task's own 3 UAT
rounds tested media-overflow/empty-state clearance, never this exact click-through path — and
(c) formally marks TRAY-01 delivered in ROADMAP.md/REQUIREMENTS.md, crediting the quick task
as the implementation source.

Purpose: prove the already-shipped Tray-only shelf gate is actually regression-safe and
close the paper trail, so Phase 32 (Tray Widening) can build on a fully verified Phase 31.
Output: one new test file, a one-line access-level bump, an on-device approval, and updated
ROADMAP.md/REQUIREMENTS.md bookkeeping. No product behavior changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-shelf-consolidation-to-tray-only/31-CONTEXT.md
@.planning/phases/31-shelf-consolidation-to-tray-only/31-PATTERNS.md
@.planning/quick/260714-3k6-notch-island-verbreitern-und-file-shelf-/260714-3k6-SUMMARY.md

<interfaces>
<!-- Exact current code the executor needs — no further exploration needed. -->

Islet/Notch/NotchPillView.swift line 58 — the gate to lock, currently uncompilable from a test
target because `private` is file-scoped:
```swift
private var shelfStripVisible: Bool { false }
```

Islet/Notch/NotchPillView.swift lines 1607-1609 — the exact access-level-for-testability
precedent already established in this same file for a different property (copy this comment
style verbatim, adapted):
```swift
// internal (not private): EqualizerBarsTests.swift calls this directly to sanity-check
// the extracted factory — `private` is file-scoped and would not compile from another
// file even under @testable import.
static func makeProfiles() -> [(low: CGFloat, high: CGFloat, period: Double, phase: Double)] {
```

Islet/Notch/NotchPillView.swift lines 1846-1857 — the `#Preview` construction, the only known
place in this codebase that builds a full `NotchPillView` with all 8 constructor arguments
(reuse this exact call shape in the new test):
```swift
let state = NotchInteractionState()
state.phase = .collapsed
return NotchPillView(interaction: state,
                     nowPlaying: NowPlayingState(),
                     presentationState: IslandPresentationState(.idle),
                     outfit: BasicOutfitState(),
                     shelfViewState: ShelfViewState(),
                     onboardingState: OnboardingViewState(),
                     viewSwitcherState: ViewSwitcherState(),
                     calendarViewState: CalendarViewState())
```

IsletTests/VisibilityDecisionTests.swift lines 1-16 — the file-header + doc-comment + single
XCTAssert-per-test convention every test file in this codebase follows (no third-party test
framework, no setup/teardown for pure-value assertions):
```swift
import XCTest
@testable import Islet

// ISL-05 / Pattern 7: the ONE visibility decision. ...
final class VisibilityDecisionTests: XCTestCase {

    func testTargetPresentNotFullscreenShows() {
        XCTAssertTrue(shouldShow(hasTarget: true, hideInFullscreen: true, isFullscreen: false, isLicensed: true))
    }
}
```

.planning/ROADMAP.md line 93 — the Phase 31 top-checklist line to flip to `[x]` + append
`(completed 2026-07-14)`:
```
- [ ] **Phase 31: Shelf Consolidation to Tray-Only** - Shelf-strip reveal removed from Home/Calendar/Weather, lives only on Tray
```

.planning/ROADMAP.md line 145 — the v1.5 Progress line to bump from 2/6 to 3/6:
```
**v1.5:** 2/6 phases complete (33%) — roadmap created 2026-07-13. Phases 29-34, 11/11 v1.5 requirements mapped. Phase 29 (SHAPE-01) completed 2026-07-14. Phase 30 (HOME-01/02/03) completed 2026-07-14.
```

.planning/ROADMAP.md lines 485-501 — the Phase 31 detail section, already updated at
planning time to reference this plan (unchecked):
```
**Plans**: 1 plan
**UI hint**: yes

Plans:
**Wave 1**

- [ ] 31-01-PLAN.md — Verify-and-close: shelfStripVisible regression test (access-level bump), on-device CR-01 click-through trace, formal TRAY-01 closeout (implementation shipped by quick task 260714-3k6)
```

.planning/REQUIREMENTS.md line 18 — the TRAY-01 checklist line to flip to `[x]`:
```
- [ ] **TRAY-01**: File-shelf content is visible only on the Tray tab — the additive shelf-strip-reveal on Home/Calendar/Weather is removed
```

.planning/REQUIREMENTS.md line 73 — the traceability table row to flip from `Pending` to `Complete`:
```
| TRAY-01 | Phase 31 | Pending |
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Access-level bump + shelfStripVisible regression test</name>
  <files>Islet/Notch/NotchPillView.swift, IsletTests/NotchPillViewTests.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift (line 58 shelfStripVisible; lines 1607-1609 makeProfiles()
    access-level precedent; lines 1846-1857 #Preview construction pattern) —
    IsletTests/VisibilityDecisionTests.swift (file-header/doc-comment/assertion convention) —
    .planning/phases/31-shelf-consolidation-to-tray-only/31-PATTERNS.md (full rationale for
    this exact blocker and fix)
  </read_first>
  <behavior>
    - Test 1 (testShelfStripVisibleIsAlwaysFalse): construct a NotchPillView with the
      #Preview's 8-argument shape, assert `view.shelfStripVisible == false`.
    - No other assertions needed — shelfStripVisible is a hardcoded literal (`{ false }`,
      zero branching), so one assertion fully covers it; do not add parametrized cases the
      way VisibilityDecisionTests does for functions with real branching.
  </behavior>
  <action>
    In Islet/Notch/NotchPillView.swift line 58, change
    `private var shelfStripVisible: Bool { false }` to `var shelfStripVisible: Bool { false }`
    (drop `private` only — internal is Swift's implicit default, no explicit `internal`
    keyword needed). Immediately above it, add a comment mirroring the `makeProfiles()`
    precedent verbatim in style: cite `NotchPillViewTests.swift`, TRAY-01/Phase 31, and that
    `private` is file-scoped and would not compile from another file even under
    `@testable import`. Do not touch the property's body (`{ false }`) or its call sites —
    this is the one-line access change D-03 explicitly permits, not the "implementation
    shape" it says to avoid.

    Create IsletTests/NotchPillViewTests.swift (new file, does not currently exist):
    `import XCTest` + `@testable import Islet`, a doc comment above the class citing TRAY-01,
    Phase 31, and quick task 260714-3k6 as the provenance of the gate being locked (follow
    VisibilityDecisionTests.swift's doc-comment convention), `final class NotchPillViewTests:
    XCTestCase`, and the single `testShelfStripVisibleIsAlwaysFalse()` test from the behavior
    block above, using the #Preview's exact 8-argument NotchPillView construction (all 6
    `@ObservedObject`/state args take plain no-arg or defaulted inits — copy the call shape,
    do not invent new constructor args).
  </action>
  <acceptance_criteria>
    - `grep -c "private var shelfStripVisible" Islet/Notch/NotchPillView.swift` returns 0
      (private keyword removed)
    - `grep -c "var shelfStripVisible: Bool { false }" Islet/Notch/NotchPillView.swift` returns 1
    - `IsletTests/NotchPillViewTests.swift` exists and contains
      `testShelfStripVisibleIsAlwaysFalse` and `XCTAssertFalse(view.shelfStripVisible`
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` reports
      BUILD SUCCEEDED (proves the access bump actually unblocks compilation of the new test
      file — this was uncompilable before this task)
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <done>
    Build succeeds with the new test file present. `shelfStripVisible` is no longer `private`.
    Test run itself (pass/fail) happens via manual Cmd-U per project memory
    `xcodebuild-test-headless-hang` — folded into Task 2's on-device checkpoint below.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device CR-01-class click-through trace against the shipped shelf gate</name>
  <read_first>
    Project memory `cr01-clickthrough-or-defeat-gotcha` — Islet/Notch/NotchWindowController.swift
    `visibleContentZone()` (line ~962) and its CR-01 comments (lines 274-276, 945-971,
    1023-1044) — .planning/phases/31-shelf-consolidation-to-tray-only/31-CONTEXT.md D-02
  </read_first>
  <what-built>
    Task 1 locked `shelfStripVisible == false` with an automated test. This checkpoint covers
    what a test cannot: quick task 260714-3k6 already removed the shelf-band reservation from
    both `NotchPillView`'s render-side layout and `NotchWindowController.visibleContentZone()`'s
    hit-test geometry, but its own 3 on-device UAT rounds only exercised media-overflow and
    empty-state clearance — never the specific hover->expand->move-down click-through trace this
    project's own CR-01 regression class requires for any `visibleContentZone()` change.
  </what-built>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <action>
    No file edits by default — build/run on-device (Cmd-R in Xcode) and perform the checklist
    in how-to-verify. The ONLY in-scope edit is a documented contingency: if step 2, 3, or 4
    below reveals an actual dead-click zone or phantom click-swallowing (a real CR-01-class
    regression), fix it in `visibleContentZone()` (NotchWindowController.swift, ~line 962) by
    re-deriving the rect purely from `switcherContentHeight`/`switcherRowHeight` with no
    residual shelf-height term, then re-run the full checklist. Do not touch
    `NotchPillView.shelfStripVisible`'s value or the 5 `blobShape` call sites — this task
    verifies the shipped gate, it does not change the gate itself.
  </action>
  <how-to-verify>
    Open Islet.xcodeproj in Xcode (Finder path:
    /Users/lippi304/conductor/workspaces/notch/algiers/Islet.xcodeproj), select the Islet
    scheme, Cmd-R to run on the real MacBook with the physical notch.

    1. Run `NotchPillViewTests` via Cmd-U (Test navigator or Product > Test). Confirm
       `testShelfStripVisibleIsAlwaysFalse` passes green alongside the existing suite.
    2. Hover the notch to expand it on Home with the shelf empty. Move the pointer slowly
       downward through the expanded content toward where the shelf band used to sit (below
       the Now-Playing/empty-state content, roughly the bottom ~1/4 of the expanded panel).
       Confirm the pointer never triggers an unexpected collapse or a "dead click" zone in
       that now-empty space.
    3. Drag a file onto the notch while on Home so the shelf has an item, then repeat the same
       slow downward pointer trace. Confirm the shelf strip still does not appear, and the
       click-through hit zone still matches the visible content exactly (no phantom band).
    4. Switch to Calendar, then Weather, with that same file still on the shelf, and repeat the
       downward trace once on each. Confirm identical behavior — no dead zone, no phantom
       click-swallowing.
    5. Switch to Tray. Confirm the shelf item is still visible there and click-to-open/trash
       still work (sanity check only — already confirmed by the quick task, not re-litigating
       D-04/D-05).
  </how-to-verify>
  <acceptance_criteria>
    All 5 steps confirmed with no dead-click zone, no unexpected collapse, and
    `testShelfStripVisibleIsAlwaysFalse` passing — user types "approved".
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe any issue (e.g. "dead click zone below content on Home", "unexpected collapse", "test failed")</resume-signal>
  <verify>
    <human-check>All 5 checklist items pass on-device on the real notch hardware; if the check-2/3/4 contingency was applied, `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` succeeds afterward too.</human-check>
  </verify>
  <done>User replies "approved"; NotchPillViewTests passes, and the CR-01 hover-expand-move-down trace shows no dead-click zone or phantom click-swallowing on Home/Calendar/Weather, with Tray unaffected.</done>
</task>

<task type="auto">
  <name>Task 3: Formal closeout — mark TRAY-01 delivered</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/ROADMAP.md lines 87-96 (v1.5 checklist), 129-145 (Progress section), 485-495
    (Phase 31 detail section) — .planning/REQUIREMENTS.md lines 16-22 (Tray section), 63-84
    (Traceability table) — .planning/phases/31-shelf-consolidation-to-tray-only/31-CONTEXT.md
    D-01/D-02 (crediting quick task 260714-3k6 as implementation source)
  </read_first>
  <action>
    In .planning/ROADMAP.md:
    - Line 93: flip `- [ ] **Phase 31: ...` to `- [x] **Phase 31: ...` and append
      `(completed 2026-07-14)`.
    - Line 145: bump the v1.5 Progress line from `2/6 phases complete (33%)` to
      `3/6 phases complete (50%)`, appending `Phase 31 (TRAY-01) completed 2026-07-14 —
      implementation shipped ahead of formal planning via quick task 260714-3k6, verified and
      closed by this phase's own plan.`
    - Phase 31 detail section (already updated at planning time to `**Plans**: 1 plan` with
      an unchecked `Plans:` list): change `**Plans**: 1 plan` to `**Plans**: 1/1 plans
      complete`, and flip the `- [ ] 31-01-PLAN.md — ...` checklist line to `- [x]`.

    In .planning/REQUIREMENTS.md:
    - Line 18: flip `- [ ] **TRAY-01**: ...` to `- [x] **TRAY-01**: ...`.
    - Line 73: flip `| TRAY-01 | Phase 31 | Pending |` to `| TRAY-01 | Phase 31 | Complete |`.
    - Update the coverage summary (lines 81-84) if a per-status breakdown exists elsewhere in
      the file; otherwise leave the 11-total/100%-mapped figures untouched (this task only
      changes status, not mapping).
  </action>
  <acceptance_criteria>
    - `grep -c "\[x\] \*\*Phase 31" .planning/ROADMAP.md` returns 1
    - `grep -c "TRAY-01 | Phase 31 | Complete" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "\[x\] \*\*TRAY-01\*\*" .planning/REQUIREMENTS.md` returns 1
  </acceptance_criteria>
  <verify>
    <automated>grep -q "\[x\] \*\*Phase 31" .planning/ROADMAP.md && grep -q "TRAY-01 | Phase 31 | Complete" .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <done>
    ROADMAP.md and REQUIREMENTS.md both show TRAY-01/Phase 31 as delivered, crediting quick
    task 260714-3k6 as the implementation source, with this plan as the verification/closure
    record.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Verify-and-close phase: one access-level bump (private -> internal) on an existing hardcoded-`false` computed property, one new pure-assertion test file, and documentation bookkeeping edits. No new input handling, no schema, no network/auth code, no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Denial of Service (local) | Click-through hit-testing (`visibleContentZone()`) | accept | Local-only, no attacker-controlled input; Task 2's on-device hover-trace explicitly re-verifies no dead-click regression, matching this project's established CR-01 mitigation discipline. |
</threat_model>

<verification>
- Task 1's build gate (`xcodebuild build -scheme Islet`) must report BUILD SUCCEEDED before
  Task 2's checkpoint opens.
- Task 2's on-device checkpoint covers: `NotchPillViewTests` passing via Cmd-U, and the CR-01
  hover->expand->move-down trace against the shipped `shelfStripVisible`/`visibleContentZone()`
  change on Home/Calendar/Weather.
- Task 3 only proceeds after Task 2's "approved" resume-signal.
</verification>

<success_criteria>
- `shelfStripVisible` is no longer `private`; `IsletTests/NotchPillViewTests.swift` exists and
  its single test passes.
- User approves the on-device CR-01-class click-through trace against the shipped change.
- ROADMAP.md and REQUIREMENTS.md both mark TRAY-01/Phase 31 as delivered, crediting quick task
  260714-3k6 as the implementation source.
</success_criteria>

<output>
Create `.planning/phases/31-shelf-consolidation-to-tray-only/31-01-SUMMARY.md` when done
</output>
