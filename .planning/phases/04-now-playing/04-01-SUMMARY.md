---
phase: 04-now-playing
plan: 01
subsystem: now-playing
tags: [swift, mediaremote, mediaremote-adapter, spm, xcodegen, xctest, tdd]

# Dependency graph
requires:
  - phase: 03-charging-activity
    provides: "The PowerActivity.swift + PowerActivityTests.swift pure-seam pattern this plan mirrors"
provides:
  - "MediaRemoteAdapter SPM package wired into the build (Embed & Sign), pinned by revision cf30c4f"
  - "Islet/Notch/NowPlayingPresentation.swift — the pure TrackSnapshot → NowPlayingPresentation seam (D-01 allowlist, playing/paused/none)"
  - "IsletTests/NowPlayingPresentationTests.swift — 5 green unit tests locking NOW-01 + NOW-03"
affects: [04-02 (NowPlayingMonitor imports the package + TrackSnapshot), 04-03 (media view renders NowPlayingPresentation), 04-04 (controller wiring)]

# Tech tracking
tech-stack:
  added: ["ejbills/mediaremote-adapter @ cf30c4f1af29b5829d859f088f8dbdf12611a046 (BSD-3, no tags — pinned by revision)"]
  patterns: ["Pure Foundation-only presentation seam unit-tested in ms (mirrors PowerActivity); D-12 health kept OFF the snapshot enum as an orthogonal @Published axis"]

key-files:
  created:
    - Islet/Notch/NowPlayingPresentation.swift
    - IsletTests/NowPlayingPresentationTests.swift
  modified:
    - project.yml
    - Islet.xcodeproj/project.pbxproj
    - Islet.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved

key-decisions:
  - "Pin mediaremote-adapter by exact revision cf30c4f (repo has ZERO git tags — from:/majorVersion: cannot resolve, Pitfall 4) and commit Package.resolved for a reproducible, supply-chain-safe build (T-04-01)"
  - "embed: true + codeSign: true on the Islet target — the framework is a RUNTIME resource (run.pl lives inside it), not link-time only (Pitfall 3)"
  - "NowPlayingPresentation has NO .unavailable case — D-12 health is an orthogonal @Published axis (Plan 02), keeping D-11 (no media → .none) from collapsing into D-12 (API unavailable)"
  - "A4: isPlaying nil (track loaded, state unknown) → .paused; artist nil → \"\" so the title still renders"

patterns-established:
  - "Pure now-playing seam: Foundation-only struct + enum + total free function, hand-constructable fixtures, exactly like PowerActivity/NotchGeometry"

requirements-completed: [NOW-01, NOW-03]

# Metrics
duration: 4min
completed: 2026-06-27
---

# Phase 4 Plan 01: Now-Playing Wave-0 Foundation Summary

**MediaRemoteAdapter SPM package wired (Embed & Sign, pinned by revision) plus a TDD'd Foundation-only NowPlayingPresentation seam locking the D-01 source allowlist and the playing/paused/none classification (D-11 ≠ D-12).**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-06-27T19:55:09Z
- **Completed:** 2026-06-27T19:58:27Z
- **Tasks:** 2
- **Files modified:** 5 (2 created, 3 modified)

## Accomplishments

- Wired the `MediaRemoteAdapter` package into `project.yml` as a top-level `packages:` block pinned by exact revision `cf30c4f`, with `embed: true` + `codeSign: true` on the Islet target; the framework embeds into `Islet.app/Contents/Frameworks/` and the app builds clean with no new Swift source.
- Created `NowPlayingPresentation.swift` — the pure `TrackSnapshot → NowPlayingPresentation` seam every downstream plan (02–04) imports: D-01 allowlist (Spotify + Apple Music only), empty/nil-title rejection, playing/paused/none mapping.
- TDD'd the classification logic with 5 green unit tests (RED → GREEN), running in ~2 ms.
- Full `IsletTests` suite stays green: 77 tests, 0 failures (the new 5 + all 72 pre-existing).

## Task Commits

1. **Task 1: Wire MediaRemoteAdapter SPM package (Embed & Sign)** — `0209af5` (chore)
2. **Task 2 (RED): Failing NowPlayingPresentation tests** — `9576b0a` (test)
3. **Task 2 (GREEN): Implement NowPlayingPresentation seam** — `d44176c` (feat)

_Task 2 is TDD: RED (failing test) → GREEN (implementation). No refactor commit — the GREEN implementation was already minimal._

## Files Created/Modified

- `Islet/Notch/NowPlayingPresentation.swift` (created) — `TrackSnapshot` struct, `NowPlayingPresentation` enum (.playing/.paused/.none), `allowedBundleIDs`, and the total `nowPlayingPresentation(from:)` function. Foundation-only.
- `IsletTests/NowPlayingPresentationTests.swift` (created) — `testAllowlistFiltersBundleID`, `testNoTitleMapsToNone`, `testPlayingVsPausedClassification`, `testNilSnapshotMapsToNone`, `testNilIsPlayingMapsToPaused`.
- `project.yml` (modified) — top-level `packages:` block + Islet target `dependencies:` (embed + codeSign).
- `Islet.xcodeproj/project.pbxproj` (modified) — regenerated by `xcodegen`.
- `Islet.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved` (created) — SPM lock for reproducible resolve.

## Exported Seam (for Plans 02–04)

```swift
struct TrackSnapshot: Equatable {
    let bundleIdentifier: String?
    let isPlaying: Bool?
    let title: String?
    let artist: String?
    let hasArtwork: Bool
}

enum NowPlayingPresentation: Equatable {
    case playing(title: String, artist: String)
    case paused(title: String, artist: String)
    case none
}

let allowedBundleIDs: Set<String> = ["com.spotify.client", "com.apple.Music"]
func nowPlayingPresentation(from s: TrackSnapshot?) -> NowPlayingPresentation
```

**Pinned adapter commit:** `cf30c4f1af29b5829d859f088f8dbdf12611a046` (ejbills/mediaremote-adapter, no tags).

## Decisions Made

None beyond the plan — followed it exactly. The plan's pre-specified decisions (revision pin, embed+codeSign, no `.unavailable` case, A4 nil-isPlaying→paused) are recorded in frontmatter `key-decisions`.

## Deviations from Plan

None — plan executed exactly as written.

The plan's verify commands hardcode the path `/Users/lippi304/conductor/workspaces/notch/algiers`; because this ran in an isolated worktree, every command was executed from the worktree root instead. This is an environment substitution, not a plan deviation — the same commands, same results.

## Issues Encountered

- **Worktree base mismatch (pre-task, infrastructure):** The worktree branch was initially based on a stray `Initial commit` (`15b83c5`) containing only `.gitkeep` — disconnected from real project history. Per the worktree-branch-check instruction, reset onto the correct base `2c7909290554bb391618f9b658af80d85bb0b121`, which materialized the full project tree (the earlier `--soft` reset left the tree empty, so a `--hard` reset to the correct base was required). All subsequent work proceeded on the correct base.
- **`--no-verify` blocked:** The orchestrator requested `--no-verify` on commits, but a repo hook (`block-no-verify`) rejects it. Commits were made normally with hooks enabled; all succeeded.
- The `com.apple.linkd.autoShortcut` connection errors in test output are unrelated macOS-service chatter from launching the XCTest host app — tests passed regardless.

## Known Stubs

None. This plan deliberately ships only build config + a Foundation-only pure function. `hasArtwork: Bool` is intentional scope (the real `NSImage` flows to the Plan-02 @Published model, never through this pure seam) — documented in the plan and the source doc-comment, not a stub.

## User Setup Required

None — no external service configuration required. The SPM package resolves automatically at build time.

## Next Phase Readiness

- Plans 02–04 can now `import MediaRemoteAdapter` and use `TrackSnapshot` / `NowPlayingPresentation` / `nowPlayingPresentation(from:)`.
- Wave-0 unit foundation is complete and green; the system IPC / process-lifecycle / render-loop behaviors remain on-device UAT (per 04-VALIDATION.md manual-only rows).
- Standing blocker carried from STATE.md: MediaRemote longevity is unknowable — re-verify the adapter against the installed macOS at each macOS update.

## Self-Check: PASSED

- Files verified on disk: `Islet/Notch/NowPlayingPresentation.swift`, `IsletTests/NowPlayingPresentationTests.swift`, `.planning/phases/04-now-playing/04-01-SUMMARY.md`, `Package.resolved` — all FOUND.
- Commits verified in git: `0209af5` (Task 1), `9576b0a` (Task 2 RED), `d44176c` (Task 2 GREEN) — all FOUND.

---
*Phase: 04-now-playing*
*Completed: 2026-06-27*
