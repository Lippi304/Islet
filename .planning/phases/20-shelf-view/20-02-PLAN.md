---
phase: 20-shelf-view
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - Islet/Notch/NotchWindowController.swift
  - IsletTests/ShelfViewStateTests.swift
autonomous: true
requirements: [SHELF-04, SHELF-05, SHELF-07]

must_haves:
  truths:
    - "Clicking a shelf item's own trash icon removes just that item from the strip and deletes its session-temp copy on disk"
    - "Clicking the delete-all icon clears every shelf item and every session-temp copy instantly, with no confirmation dialog (D-03)"
    - "Clicking a shelf item opens it in its default application; clicking an item whose local copy is gone is a silent no-op (D-04) — no dialog, no crash, item stays in the shelf"
    - "The expanded island's panel window reserves enough height for the shelf row up front so it never clips on-device, without resizing the live NSPanel mid-animation"
    - "A hand-seeded set of real shelf items exists at DEBUG launch so SHELF-03/04/05/07/09 are visually verifiable before Phase 22's real drag-in ships"
  artifacts:
    - path: "Islet/Notch/NotchWindowController.swift"
      provides: "shelfCoordinator + shelfViewState ownership, 3 shelf handlers wired into makeRootView, panel-sizing extension, DEBUG hand-seed"
      contains: "private let shelfCoordinator = ShelfCoordinator()"
    - path: "IsletTests/ShelfViewStateTests.swift"
      provides: "Real-disk-IO coverage of the ShelfViewState resync contract (SHELF-04/05) + the shouldOpenShelfItem gate (SHELF-07/D-04)"
      contains: "final class ShelfViewStateTests: XCTestCase"
  key_links:
    - from: "NotchWindowController.handleShelfItemDelete / handleShelfClearAll"
      to: "ShelfCoordinator.remove / ShelfCoordinator.clear"
      via: "direct method call, then shelfViewState.items = shelfCoordinator.logic.items resync"
      pattern: "shelfCoordinator\\.(remove|clear)"
    - from: "NotchWindowController.positionAndShow"
      to: "NotchPillView.shelfRowHeight"
      via: "expandedFrame's CGSize height addition, single source of truth"
      pattern: "NotchPillView\\.shelfRowHeight"
---

<objective>
Wire Phase 20-01's view layer to real data: `NotchWindowController` owns a `ShelfCoordinator` +
`ShelfViewState`, forwards tap/delete/clear-all intents through the new `NotchPillView` closures,
applies the D-04 missing-file guard before `NSWorkspace.shared.open`, and reserves the panel's
window height for the shelf band up front (never a live resize — Pitfall 4). A `#if DEBUG` hand-seed
gives real, on-disk sample files so SHELF-03/04/05/07/09 are visually verifiable ahead of Phase 22's
real drag-in. Depends on Plan 20-01 because it consumes `NotchPillView`'s new init parameters
(`shelfViewState:`, `onShelfItemTap:`, `onShelfItemDelete:`, `onShelfClearAll:`) and
`NotchPillView.shelfRowHeight`.

Purpose: Complete the controller-side wiring so the shelf strip is real, interactive, and
correctly reserved in the panel window before any live drag risk (Phase 21/22) is introduced.
Output: Live shelf interactions on-device; `ShelfViewStateTests.swift` proving the resync + D-04
gate contracts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-shelf-view/20-CONTEXT.md
@.planning/phases/19-shelf-data-model/19-CONTEXT.md

<interfaces>
<!-- Produced by Plan 20-01 — read that plan's SUMMARY if unclear, but these signatures are final. -->

From Islet/Shelf/ShelfViewState.swift (Plan 20-01):
```swift
final class ShelfViewState: ObservableObject {
    @Published var items: [ShelfItem] = []
}
func shouldOpenShelfItem(fileExists: Bool) -> Bool
```

From Islet/Notch/NotchPillView.swift (Plan 20-01 — new/changed surface this plan consumes):
```swift
static let shelfRowHeight: CGFloat = 56
// New init parameters (all after `outfit:` / after `onPrevious:`):
// shelfViewState: ShelfViewState                       (no default — must be injected)
// onShelfItemTap: (ShelfItem) -> Void = { _ in }
// onShelfItemDelete: (UUID) -> Void = { _ in }
// onShelfClearAll: () -> Void = {}
```

From Islet/Shelf/ShelfCoordinator.swift (Phase 19, LOCKED, consume-only):
```swift
@MainActor
final class ShelfCoordinator {
    private(set) var logic = ShelfLogic()
    @discardableResult func append(_ item: ShelfItem) -> Bool
    @discardableResult func remove(id: UUID) -> ShelfItem?
    @discardableResult func clear() -> [ShelfItem]
}
```

From Islet/Shelf/ShelfFileStore.swift (Phase 19, LOCKED, consume-only):
```swift
enum ShelfFileStore {
    static func makeSessionCopy(of sourceURL: URL, id: UUID) throws -> URL
    static func deleteSessionCopy(at localURL: URL)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Own ShelfCoordinator/ShelfViewState, wire handlers into makeRootView, reserve panel height</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift (whole file — already read in full this session; the
    controller is ~1168 lines, re-derive exact current line numbers from your own context before
    editing since line numbers below are approximate anchors, not guaranteed exact after any
    Plan-20-01-unrelated drift)
    Islet/Notch/NotchPillView.swift (Plan 20-01's output — confirm the exact final init parameter
    names/order before editing makeRootView)
    Islet/Shelf/ShelfViewState.swift (Plan 20-01's output — the `shouldOpenShelfItem` gate this task
    calls)
    Islet/Notch/NotchGeometry.swift (whole file — `expandedNotchFrame(collapsed:expandedSize:)`'s
    signature this task's panel-sizing change feeds into; no changes needed to this file itself)
  </read_first>
  <action>
    Add two new properties near the existing `nowPlayingState`/`chargingState` declarations (~lines
    80-139): `private let shelfCoordinator = ShelfCoordinator()` and `let shelfViewState =
    ShelfViewState()` — mirrors `nowPlayingState`'s non-private `let` (the view observes it via
    `@ObservedObject`) and `chargingState`'s private-coordinator convention. No `start()`-time
    construction needed (unlike `deviceCoordinator`, `ShelfCoordinator` has no `[weak self]`-capturing
    closures to bind).

    Modify `makeRootView(accentIndex:)` (currently ~lines 856-868): add `shelfViewState:
    shelfViewState,` as an argument to the `NotchPillView(...)` constructor call (placed after
    `outfit: outfitState,`), and add 3 new closure arguments after the existing
    `onPrevious: { [weak self] in self?.nowPlayingMonitor?.previousTrack() })`: `onShelfItemTap: {
    [weak self] item in self?.handleShelfItemTap(item) }`, `onShelfItemDelete: { [weak self] id in
    self?.handleShelfItemDelete(id) }`, `onShelfClearAll: { [weak self] in
    self?.handleShelfClearAll() }`.

    Add 3 new private handler methods (place them in a new `// MARK: - Phase 20 / SHELF-04/05/07 —
    shelf item handlers` section, near `handleAdapterTerminated` ~line 1110):
    `handleShelfItemTap(_ item: ShelfItem)` — guards with `shouldOpenShelfItem(fileExists:
    FileManager.default.fileExists(atPath: item.localURL.path))` and returns early (silent no-op,
    D-04) when false; otherwise calls `NSWorkspace.shared.open(item.localURL)`.
    `handleShelfItemDelete(_ id: UUID)` — calls `shelfCoordinator.remove(id: id)` then resyncs
    `shelfViewState.items = shelfCoordinator.logic.items`.
    `handleShelfClearAll()` — calls `shelfCoordinator.clear()` then resyncs `shelfViewState.items =
    shelfCoordinator.logic.items`. Neither delete handler needs a `withAnimation` wrapper — the
    shelf row's own `.transition(.opacity)` (Plan 20-01) already animates under SwiftUI's implicit
    animation for the `@Published` array mutation the same way other `@Published` array/list changes
    in this codebase do (no explicit spring needed for a list-content change, unlike a
    presentation/phase mutation).

    Modify `positionAndShow(on:)`'s `expandedFrame` computation (currently ~line 592: `let
    expandedFrame = expandedNotchFrame(collapsed: collapsedFrame, expandedSize: expandedSize)`):
    change the `expandedSize:` argument to `CGSize(width: expandedSize.width, height:
    expandedSize.height + NotchPillView.shelfRowHeight)` — the panel window reserves the shelf band
    UNCONDITIONALLY (transparent when the shelf is empty, per Pattern 2 in 20-RESEARCH.md), while the
    VISIBLE black shape (Plan 20-01's `blobShape`) still only grows into that space conditionally.
    This is the ONLY change to `positionAndShow`; `wingsFrame`/the panel union math (`let panelFrame =
    expandedFrame.union(wings)`) is untouched — it automatically covers the taller `expandedFrame`.
  </action>
  <acceptance_criteria>
    - `NotchWindowController.swift` declares `private let shelfCoordinator = ShelfCoordinator()` and
      `let shelfViewState = ShelfViewState()`
    - `makeRootView` passes `shelfViewState:`, `onShelfItemTap:`, `onShelfItemDelete:`,
      `onShelfClearAll:` to `NotchPillView(...)`
    - `handleShelfItemTap` calls `shouldOpenShelfItem(fileExists:...)` BEFORE any
      `NSWorkspace.shared.open` call — the guard precedes the side effect textually
    - `handleShelfItemDelete`/`handleShelfClearAll` each end with a `shelfViewState.items =
      shelfCoordinator.logic.items` resync line
    - `positionAndShow`'s `expandedFrame` computation adds `NotchPillView.shelfRowHeight` to
      `expandedSize.height` — grep confirms `NotchPillView.shelfRowHeight` appears in
      `NotchWindowController.swift`
    - `wingsFrame`/`panelFrame` union line is unmodified (still reads `expandedFrame.union(wings)`)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <done>NotchWindowController builds; shelf taps/deletes/clear-all route through the coordinator with the D-04 guard; the panel reserves shelfRowHeight unconditionally.</done>
</task>

<task type="auto">
  <name>Task 2: DEBUG hand-seed — real on-disk sample files for on-device UAT (Pitfall 5)</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift's `start()` method (the method this task adds one call
    to — re-read the current version of this file AFTER Task 1's edits land, since Task 1 modifies
    this same file first within this plan)
    Islet/Shelf/ShelfFileStore.swift (whole file — `makeSessionCopy(of:id:)`'s throwing signature and
    the `IsletShelf/<uuid>/` destination layout this seed must produce REAL files for)
    .planning/phases/20-shelf-view/20-RESEARCH.md Pitfall 5 section (why fabricated `ShelfItem`
    structs with synthetic URLs are explicitly forbidden here — icon lookup and click-to-open must be
    realistic)
  </read_first>
  <action>
    Add a `#if DEBUG`-gated private method `seedDebugShelfItems()` to `NotchWindowController`
    (place it near `startOutfitRefresh()`). It must: create a scratch directory at
    `NSTemporaryDirectory()/IsletShelfSeed/` via `FileManager.default.createDirectory(at:
    withIntermediateDirectories: true)`; write 3 REAL small files with distinct extensions so icon
    lookup produces distinct glyphs — `"Report.pdf"`, `"Photo.jpg"`, `"Notes.txt"` — each containing a
    few bytes of placeholder `Data` via `Data(string.utf8).write(to:)`; for each written file, call
    `ShelfFileStore.makeSessionCopy(of: source, id: UUID())` (via `try?`, skipping on failure),
    construct a `ShelfItem(id:originalURL:localURL:filename:addedAt:)` from the result, and call
    `shelfCoordinator.append(item)`; after all 3 are attempted, resync `shelfViewState.items =
    shelfCoordinator.logic.items`. Call `seedDebugShelfItems()` from inside `start()`, wrapped in
    `#if DEBUG ... #endif`, placed after the existing `startOutfitRefresh()` call (~line 324) — this
    is DEBUG-only and never compiles into Release (mirrors the existing `#if DEBUG` `didLogFirstHover`
    convention already in this file).
  </action>
  <acceptance_criteria>
    - `seedDebugShelfItems()` is wrapped in `#if DEBUG ... #endif` in both its declaration and its
      call site inside `start()`
    - The 3 seeded files are written as REAL bytes to `NSTemporaryDirectory()/IsletShelfSeed/` before
      `ShelfFileStore.makeSessionCopy` is called on them — no `ShelfItem` is constructed from a
      `localURL` that was never produced by `makeSessionCopy`
    - After seeding, `shelfViewState.items` is resynced from `shelfCoordinator.logic.items`
    - A Release build (`xcodebuild build -scheme Islet -configuration Release`) contains none of this
      seeding code (verify via the `#if DEBUG` guard, not a runtime check)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <done>Launching the Debug build populates the shelf with 3 real, distinctly-typed sample items visible in the expanded island; a Release build never runs this seed.</done>
</task>

<task type="auto">
  <name>Task 3: ShelfViewStateTests — resync contract (SHELF-04/05) + shouldOpenShelfItem gate (SHELF-07/D-04)</name>
  <files>IsletTests/ShelfViewStateTests.swift</files>
  <read_first>
    IsletTests/ShelfCoordinatorTests.swift (whole file — the exact real-disk-IO fixture convention
    to reuse: `fixturesDir` set up/torn down per test, `makeRealItem(named:)` writing real bytes then
    calling `ShelfFileStore.makeSessionCopy`)
    Islet/Shelf/ShelfViewState.swift (Plan 20-01's output — `ShelfViewState` and
    `shouldOpenShelfItem(fileExists:)`, the two things under test)
    Islet/Shelf/ShelfCoordinator.swift (the `append`/`remove`/`clear` methods these tests drive)
  </read_first>
  <action>
    Create `IsletTests/ShelfViewStateTests.swift`: `import XCTest`, `@testable import Islet`,
    `@MainActor final class ShelfViewStateTests: XCTestCase`. Reuse
    `ShelfCoordinatorTests.swift`'s exact `fixturesDir`/`setUp`/`tearDown`/`makeRealItem(named:)`
    fixture shape (a fresh `NSTemporaryDirectory()`-rooted scratch dir per test, real bytes written,
    `ShelfFileStore.makeSessionCopy` called for the `localURL`). Add 3 tests exercising the SAME
    resync pattern Task 1's handlers use (`viewState.items = coordinator.logic.items` called
    immediately after each mutation, exactly mirroring the controller's own resync line):
    `testAppendThenResyncReflectsInViewState` — append a real item to a fresh `ShelfCoordinator`,
    resync a fresh `ShelfViewState`, assert `viewState.items == [item]`.
    `testRemoveThenResyncReflectsInViewState` — append 2 real items, remove one by id, resync, assert
    `viewState.items` contains only the surviving item.
    `testClearThenResyncReflectsInViewState` — append 2 real items, clear, resync, assert
    `viewState.items.isEmpty`.
    Add one more test, `testShouldOpenShelfItemGate` (no fixture needed, pure): assert
    `shouldOpenShelfItem(fileExists: true) == true` and `shouldOpenShelfItem(fileExists: false) ==
    false` — direct-assertion style matching `IslandResolverTests.swift`'s
    `testNowPlayingHealthGateForcesNeutralWhenDisabled`.
  </action>
  <acceptance_criteria>
    - `IsletTests/ShelfViewStateTests.swift` exists, is a `@MainActor final class ShelfViewStateTests:
      XCTestCase`
    - Contains `testAppendThenResyncReflectsInViewState`, `testRemoveThenResyncReflectsInViewState`,
      `testClearThenResyncReflectsInViewState`, and `testShouldOpenShelfItemGate`
    - The 3 resync tests use REAL files via `ShelfFileStore.makeSessionCopy` (never a fabricated
      `ShelfItem` with a synthetic/non-existent `localURL`)
    - `testShouldOpenShelfItemGate` asserts both `true` and `false` inputs
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build-for-testing -scheme Islet -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <done>New test file compiles and is discoverable by XCTest; Cmd-U in Xcode confirms all 4 new tests pass (xcodebuild test hangs headlessly per project memory — build-for-testing is the automated gate, Cmd-U is the manual pass-confirmation).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `ShelfItem.localURL` → `NSWorkspace.shared.open` | A path constructed entirely upstream by Phase 19's already-shipped, locked `ShelfFileStore` (CR-01 guard confines it to `IsletShelf/`); this phase only reads and opens it |
| DEBUG seed → real disk writes | The hand-seed writes real files to `NSTemporaryDirectory()/IsletShelfSeed/`, compiled out of Release entirely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-20-02 | Tampering / Elevation of Privilege | `NotchWindowController.handleShelfItemTap` | mitigate | `NSWorkspace.shared.open(URL)` (native, typed, no shell) — never `Process`/shell-out; `shouldOpenShelfItem(fileExists:)` gates the call so a vanished local copy is a silent no-op rather than surfacing an unexpected system dialog |
| T-20-03 | Tampering | `handleShelfItemDelete`/`handleShelfClearAll` → `ShelfCoordinator.remove`/`clear` | accept | Deletion is confined to `IsletShelf/` by Phase 19's already-shipped, LOCKED `ShelfFileStore.deleteSessionCopy` CR-01 guard; this phase only calls the existing locked methods, introducing no new path-construction risk |
| T-20-05 | Tampering | DEBUG-only `seedDebugShelfItems` | accept | Compiled out of Release via `#if DEBUG`; writes only to `NSTemporaryDirectory()`, never a user-visible location |
</threat_model>

<verification>
Build gate after every task: `cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"`.
Wave/phase gate: manual Cmd-U in Xcode (GUI) confirms `ShelfViewStateTests` pass; on-device UAT with
the DEBUG hand-seed confirms the shelf strip renders 3 real items, per-item trash removes one, the
far-right trash clears all, clicking an item opens it, and the panel never clips — before running
`/gsd:verify-work 20`.
</verification>

<success_criteria>
- `NotchWindowController` owns `shelfCoordinator`/`shelfViewState`, wires all 3 handlers, and reserves
  `NotchPillView.shelfRowHeight` unconditionally in the panel frame
- Clicking a shelf item opens it (guarded by `shouldOpenShelfItem`); trash icons remove/clear with
  real disk-side effects via the already-shipped `ShelfCoordinator`
- The DEBUG build hand-seeds 3 real sample files at launch for on-device UAT
- `ShelfViewStateTests.swift` covers the resync contract and the D-04 gate
- `xcodebuild build -scheme Islet -configuration Debug` succeeds after every task
</success_criteria>

<output>
Create `.planning/phases/20-shelf-view/20-02-SUMMARY.md` when done
</output>
