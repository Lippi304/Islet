---
phase: 20-shelf-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Shelf/ShelfViewState.swift
  - Islet/Notch/ShelfItemView.swift
  - Islet/Notch/NotchPillView.swift
  - IsletTests/IslandResolverTests.swift
autonomous: true
requirements: [SHELF-03, SHELF-04, SHELF-05, SHELF-07, SHELF-09]

must_haves:
  truths:
    - "When the shelf has items and the island is expanded, a horizontally-scrolling strip appears below whatever else is showing (mediaExpanded / expandedIdle / mediaUnavailable), each item showing a file-type icon, with unbounded scroll capacity"
    - "Each shelf item renders its own small trash icon, visually and gesture-wise distinct from the item's own open-tap region"
    - "A single delete-all trash icon renders at the strip's far right"
    - "The shelf strip never renders during a Charging or Device splash — by construction, since those branches never call blobShape"
  artifacts:
    - path: "Islet/Shelf/ShelfViewState.swift"
      provides: "ObservableObject published mirror of ShelfCoordinator.logic.items + the shouldOpenShelfItem(fileExists:) pure D-04 gate"
      contains: "final class ShelfViewState: ObservableObject"
    - path: "Islet/Notch/ShelfItemView.swift"
      provides: "Leaf view: file icon (NSWorkspace.shared.icon(forFile:)) + filename caption + own scoped trash Button"
      contains: "struct ShelfItemView: View"
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "shelfRowHeight constant, blobShape extended with a shelfItems parameter, new shelfRow(_:) composer, 3 callers wired, 8 #Previews updated"
      contains: "static let shelfRowHeight"
  key_links:
    - from: "NotchPillView.expandedIsland / mediaExpanded / mediaUnavailable"
      to: "NotchPillView.shelfRow(_:)"
      via: "blobShape's VStack(spacing: 0) conditional composition"
      pattern: "shelfRow\\(shelfItems\\)"
    - from: "NotchPillView.shelfRow"
      to: "ShelfItemView"
      via: "ForEach(items, id: \\.id)"
      pattern: "ShelfItemView\\("
---

<objective>
Render the shelf strip inside the expanded island: file-type icons, per-item trash, one delete-all
trash, click-to-open wiring (as closures — no AppKit yet), and prove SHELF-09's gating is already
structural (no new resolver code) with a regression test. This is the VIEW layer only — Phase 19's
`ShelfItem`/`ShelfLogic`/`ShelfCoordinator`/`ShelfFileStore` are consumed read-only and never
modified; `NotchWindowController` wiring (owning a real `ShelfCoordinator`, the D-04 missing-file
guard, panel-height reservation, hand-seeding) is Plan 20-02's job, sequenced after this one because
it consumes the new `NotchPillView` init parameters and `NotchPillView.shelfRowHeight` this plan
creates.

Purpose: Prove the view + conditional-height-blob composition compiles and morphs correctly before
any controller/panel-sizing risk is introduced (per ROADMAP Phase 20 goal).
Output: `ShelfViewState.swift`, `ShelfItemView.swift`, an extended `NotchPillView.swift`, and one new
`IslandResolverTests.swift` case proving SHELF-09's "falls out for free" claim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-shelf-view/20-CONTEXT.md
@.planning/phases/20-shelf-view/20-UI-SPEC.md

<interfaces>
<!-- Phase 19 types this plan consumes read-only. Do NOT modify these 4 files. -->

From Islet/Shelf/ShelfItem.swift:
```swift
struct ShelfItem: Equatable {
    let id: UUID
    let originalURL: URL
    var localURL: URL
    let filename: String
    let addedAt: Date
}
```

From Islet/Shelf/ShelfCoordinator.swift (consumed by Plan 20-02, referenced here only for context):
```swift
@MainActor
final class ShelfCoordinator {
    private(set) var logic = ShelfLogic()
    func append(_ item: ShelfItem) -> Bool
    func remove(id: UUID) -> ShelfItem?
    func clear() -> [ShelfItem]
}
```

From Islet/Notch/NowPlayingState.swift (the ownership-contract analog `ShelfViewState` mirrors):
```swift
final class NowPlayingState: ObservableObject {
    @Published var presentation: NowPlayingPresentation = .none
    // ... plain published holder, no methods, no timers
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ShelfViewState (published mirror + D-04 gate) and ShelfItemView (leaf row item)</name>
  <files>Islet/Shelf/ShelfViewState.swift, Islet/Notch/ShelfItemView.swift</files>
  <read_first>
    Islet/Notch/NowPlayingState.swift (whole file — the ownership-contract analog: plain `@Published`
    holder, no methods, no timers, controller is the only writer)
    Islet/Notch/BatteryIndicator.swift (whole file — the "small reusable leaf SwiftUI view taking
    plain value params, no AppKit, no @ObservedObject" shape to mirror for ShelfItemView)
    Islet/Notch/NotchPillView.swift lines 622-632 (`transportButton` — the scoped-Button gesture
    discipline: `.buttonStyle(.plain)`, no ancestor `.onTapGesture` above it)
    Islet/Shelf/ShelfItem.swift (the `ShelfItem` struct shape this view/state renders)
    .planning/phases/20-shelf-view/20-UI-SPEC.md (Spacing Scale, Typography, Color, Copywriting
    Contract sections — exact pixel values for ShelfItemView)
  </read_first>
  <action>
    Create Islet/Shelf/ShelfViewState.swift: `import Foundation`, then `final class ShelfViewState:
    ObservableObject { @Published var items: [ShelfItem] = [] }` — a plain published holder, no
    methods, mirroring NowPlayingState's "controller is the only writer, view only observes"
    contract exactly (do not add any mutating method to this class — Plan 20-02's controller writes
    `.items` directly after every ShelfCoordinator mutation).
    In the same file, add one top-level (not private, not a class method) pure function:
    `func shouldOpenShelfItem(fileExists: Bool) -> Bool { fileExists }` — the D-04 missing-file-click
    gate as an explicit, testable seam (mirrors `songChangeToastGate`/`nowPlayingHealthGate` in
    Islet/Notch/IslandResolver.swift: a tiny pure decision function, controller applies it). Plan
    20-02's `NotchWindowController.handleShelfItemTap` will call this before `NSWorkspace.shared.open`.

    Create Islet/Notch/ShelfItemView.swift: `import AppKit` + `import SwiftUI`. `struct ShelfItemView:
    View` with 3 plain properties — `let item: ShelfItem`, `let onTap: () -> Void`, `let onDelete: ()
    -> Void` (no `@ObservedObject`, no AppKit side effects inside the view itself — mirrors
    BatteryIndicator's plain-value-in, no-external-state shape). Body: a `VStack(spacing: 2)`
    (icon-gap per UI-SPEC) containing `Image(nsImage: NSWorkspace.shared.icon(forFile:
    item.localURL.path)).resizable().frame(width: 28, height: 28)` (UI-SPEC: reuses
    transportButton's established 28×28 size) then a filename `Text(item.filename)` at
    `.font(.system(size: 9))`, `.lineLimit(1)`, `.truncationMode(.middle)`, `.frame(maxWidth: 44)` —
    V5 mitigation, `item.filename` is untrusted external data (T-20-01). Apply
    `.contentShape(Rectangle())` then `.onTapGesture { onTap() }` on the VStack (D-04's own scoped
    gesture — click-to-open). Add `.overlay(alignment: .topTrailing) { Button(action: onDelete) {
    Image(systemName: "xmark.circle.fill").font(.system(size: 11)).foregroundStyle(.white.opacity(0.8))
    }.buttonStyle(.plain) }` — Finding-15/D-05 precedent: the trash Button is a SIBLING overlay, never
    nested inside a gesture region the ancestor `.onTapGesture` could shadow. Add
    `.accessibilityLabel("Open \(item.filename)")` on the tappable VStack and
    `.accessibilityLabel("Remove \(item.filename)")` on the trash Button (UI-SPEC Copywriting
    Contract — icon-only controls need an accessible name).
  </action>
  <acceptance_criteria>
    - `Islet/Shelf/ShelfViewState.swift` exists, contains `final class ShelfViewState: ObservableObject`
      with exactly one `@Published var items: [ShelfItem] = []` and no other stored property or method
    - `shouldOpenShelfItem(fileExists: Bool) -> Bool` exists as a top-level (non-member) function in
      the same file, returning its argument unchanged
    - `Islet/Notch/ShelfItemView.swift` exists, contains `struct ShelfItemView: View` with `let item:
      ShelfItem`, `let onTap: () -> Void`, `let onDelete: () -> Void`
    - The 28×28 icon `Image` uses `NSWorkspace.shared.icon(forFile: item.localURL.path)` — never
      `icon(forFileType:)` or `icon(forContentType:)`
    - The filename `Text` has both `.lineLimit(1)` and `.truncationMode(.middle)`
    - The trash `Button` sits in an `.overlay`, never inside the same gesture chain as `.onTapGesture`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <done>Both new files compile as part of the Islet target; ShelfViewState has zero methods beyond the pure gate function; ShelfItemView renders icon+caption+scoped trash with no ancestor gesture ambiguity.</done>
</task>

<task type="auto">
  <name>Task 2: Extend NotchPillView — shelfRowHeight, shelf-aware blobShape, shelfRow, wire 3 callers + 8 #Previews</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift (whole file — already read in full this session; re-derive exact
    current line numbers from your own context before editing, do not assume the numbers below are
    still exact after Task 1's edits to other files, which do not touch this file)
    Islet/Shelf/ShelfViewState.swift (Task 1's output — the `@Published var items: [ShelfItem]`
    contract this view observes)
    Islet/Notch/ShelfItemView.swift (Task 1's output — the leaf view `shelfRow` composes)
    .planning/phases/20-shelf-view/20-UI-SPEC.md (Layout Notes section — shelfRowHeight=56pt math,
    item-gap=10px, row-padding=16px, delete-all icon=14pt, per-item trash=11pt, bottomCornerRadius=20
    already used by all 3 callers today, unchanged)
  </read_first>
  <action>
    Add a new static constant immediately after `toastExtraHeight` (currently declared at line ~128):
    `static let shelfRowHeight: CGFloat = 56` with a comment giving the box math per UI-SPEC (28pt
    icon + 2pt icon-gap + ~11pt caption line + ~7.5pt top/bottom padding ×2 ≈ 56pt) and noting this is
    the SAME constant Plan 20-02's `NotchWindowController.positionAndShow` panel-sizing math must read
    from — single source of truth, never re-derived.

    Add a new `@ObservedObject var shelfViewState: ShelfViewState` property declared directly after
    the existing `outfit: BasicOutfitState` property (no default value — mirrors `nowPlaying`/
    `presentationState`/`outfit`'s existing "controller always owns and injects a real instance"
    convention, never defaulted).

    Add 3 new closures directly after the existing `onPrevious: () -> Void = {}` property, each
    defaulted to a no-op so #Previews keep compiling without a controller: `var onShelfItemTap:
    (ShelfItem) -> Void = { _ in }`, `var onShelfItemDelete: (UUID) -> Void = { _ in }`, `var
    onShelfClearAll: () -> Void = {}`.

    Modify the `blobShape<Content: View>` helper (currently at lines 234-244): add a new parameter
    `shelfItems: [ShelfItem]` to its signature (after `alignment: Alignment = .center`, before the
    `@ViewBuilder content` parameter). Inside the function body: compute `let hasShelf =
    !shelfItems.isEmpty` and `let height = Self.expandedSize.height + (hasShelf ?
    Self.shelfRowHeight : 0)`. Change the `.frame(width: Self.expandedSize.width, height:
    Self.expandedSize.height)` line to use `height` instead of `Self.expandedSize.height`. Replace the
    `.overlay(alignment: alignment) { content() }` line with `.overlay(alignment: .top) {
    VStack(spacing: 0) { content().frame(width: Self.expandedSize.width, height:
    Self.expandedSize.height, alignment: alignment); if hasShelf { shelfRow(shelfItems)
    .transition(.opacity) } } }` — this preserves each caller's own `alignment` INSIDE content's own
    fixed-height box (D-01: the visible black shape only grows when items exist; content's own layout
    is byte-for-byte unchanged), and appends the shelf row below via one continuous `VStack` under the
    SAME `NotchShape`/`matchedGeometryEffect(id: "island")` (D-07: no second shape, no cross-fade).
    `bottomCornerRadius` stays exactly as each caller already passes it (all 3 callers already pass
    `20` today — no radius change needed). The single trailing `.onTapGesture { onClick() }` already
    attached to `blobShape`'s outer shape stays exactly where it is (after the `.overlay`) — it now
    also covers the shelf row's empty space "for free", which is what satisfies D-05 (see below).

    Update the 3 existing `blobShape` call sites to pass the new argument:
    `expandedIsland` (currently `blobShape(topCornerRadius: 6, bottomCornerRadius: 20) { ... }`,
    ~line 206) → add `shelfItems: shelfViewState.items` before the trailing closure.
    `mediaExpanded` (currently `blobShape(topCornerRadius: 6, bottomCornerRadius: 20, alignment: .top)
    { ... }`, ~line 573) → add `shelfItems: shelfViewState.items`.
    `mediaUnavailable` (currently `blobShape(topCornerRadius: 6, bottomCornerRadius: 20) { ... }`,
    ~line 638) → add `shelfItems: shelfViewState.items`.
    Per D-02, all 3 callers pass the SAME `shelfViewState.items` uniformly — no per-branch
    special-casing.

    Add one new private helper method `shelfRow(_ items: [ShelfItem]) -> some View` (place it directly
    after `blobShape`): `ScrollView(.horizontal) { HStack(spacing: 10) { ForEach(items, id: \.id) {
    item in ShelfItemView(item: item, onTap: { onShelfItemTap(item) }, onDelete: {
    onShelfItemDelete(item.id) }) }; Button(action: onShelfClearAll) { Image(systemName:
    "trash").font(.system(size: 14)).foregroundStyle(.white.opacity(0.7))
    }.buttonStyle(.plain).accessibilityLabel("Clear shelf") }.padding(.horizontal, 16) }
    .scrollIndicators(.never).frame(height: Self.shelfRowHeight)` — item-gap 10px, row-padding 16px,
    delete-all icon 14pt, per UI-SPEC's Spacing Scale/Layout Notes. Do NOT attach any
    `.onTapGesture`/gesture modifier to the `ScrollView`/`HStack` container itself — D-05 ("tapping
    empty space in the shelf strip collapses the island") falls out for free because `blobShape`'s
    own trailing `.onTapGesture { onClick() }` already wraps the entire overlay (content + shelf row
    alike), exactly like it already does for `expandedIsland`/`mediaUnavailable`'s content today; only
    `ShelfItemView`'s own scoped tap/trash gestures (Task 1) intercept before that ancestor gesture.

    Finally, update ALL 8 existing `#Preview` blocks (Collapsed, Expanded, Charging Wings, Device
    Wings, Media Wings playing, Media Wings paused, Media Expanded, Unavailable): insert
    `shelfViewState: ShelfViewState(),` as a new constructor argument immediately after each
    `outfit: ...` argument (or `outfit: BasicOutfitState(),` where that's the literal value used),
    so every preview still compiles against the new non-defaulted parameter.
  </action>
  <acceptance_criteria>
    - `NotchPillView.swift` contains exactly one `static let shelfRowHeight: CGFloat = 56`
    - `blobShape` signature includes a `shelfItems: [ShelfItem]` parameter
    - All 3 callers (`expandedIsland`, `mediaExpanded`, `mediaUnavailable`) pass `shelfItems:
      shelfViewState.items`
    - `shelfRow(_:)` exists as a private method using `ScrollView(.horizontal)`, `HStack(spacing:
      10)`, `ForEach(items, id: \.id)`, and a far-right delete-all `Button`
    - No `.onTapGesture` or `Button` wraps the `ScrollView`/`HStack` container in `shelfRow` itself
      (only `ShelfItemView`'s own internal gestures and the delete-all `Button` exist inside it)
    - All 8 `#Preview` blocks construct `NotchPillView` with a `shelfViewState:` argument — file
      compiles with zero missing-argument errors
    - `wings(for:)` / `deviceWings(for:)` / `mediaWingsOrToast` are UNCHANGED (still call
      `wingsShape`/build their own shape directly, never `blobShape`) — confirms SHELF-09 gating
      requires no new code in those branches
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <done>NotchPillView.swift builds; the 3 expanded-content branches share one conditionally-taller blobShape with the new shelf row appended below their existing content; the 3 wing/transient branches remain untouched.</done>
</task>

<task type="auto">
  <name>Task 3: SHELF-09 regression test — shelf-composing branches unreachable during a transient</name>
  <files>IsletTests/IslandResolverTests.swift</files>
  <read_first>
    IsletTests/IslandResolverTests.swift (whole file — already read in full; append near
    `testChargingOutranksDeviceAndMedia`/`testDeviceOutranksAmbientMedia`, following the exact same
    `resolve(...)` call-and-assert style used throughout the file)
    Islet/Notch/IslandResolver.swift (whole file — `resolve(...)`'s existing switch already returns
    `.charging`/`.device` before ever reaching `.nowPlayingExpanded`/`.expandedIdle`; this task adds
    test coverage only, no production code change)
  </read_first>
  <action>
    Add one new test method to `IslandResolverTests`, placed near `testDeviceOutranksAmbientMedia`:
    `testShelfComposingBranchesUnreachableDuringTransient`. Call `resolve(activeTransient:
    .charging(.charging(percent: 50)), nowPlaying: .playing(title: "Song", artist: "Artist"),
    nowPlayingHealthy: true, hasPlayedSinceLaunch: true, isExpanded: true)` and assert the result
    equals `.charging(.charging(percent: 50))` — proving that even with `isExpanded: true` (which
    would otherwise route to `.nowPlayingExpanded`/`.expandedIdle`, the branches `NotchPillView`
    composes the shelf row into per D-02), a standing transient always wins. Then call `resolve(
    activeTransient: .device(.connected(name: "AirPods Pro", glyph: .airpodsPro, battery: nil)),
    nowPlaying: .none, nowPlayingHealthy: true, hasPlayedSinceLaunch: true, isExpanded: true)` and
    assert the result equals `.device(.connected(name: "AirPods Pro", glyph: .airpodsPro, battery:
    nil))` — same proof for the device transient. Add a doc comment above the test explaining this is
    SHELF-09's regression coverage: the shelf strip is structurally absent during Charging/Device
    because those transients always outrank the `isExpanded` branches, with zero new production code
    in `IslandResolver.swift` (RESEARCH.md's "falls out for free" claim).
  </action>
  <acceptance_criteria>
    - `IsletTests/IslandResolverTests.swift` contains a new
      `testShelfComposingBranchesUnreachableDuringTransient` test function
    - The test asserts against `resolve(...)` with `isExpanded: true` for BOTH a charging and a
      device `activeTransient`, confirming the transient case wins in both
    - No production code in `Islet/Notch/IslandResolver.swift` is modified by this task
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build-for-testing -scheme Islet -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <done>New test compiles and is discoverable by XCTest; run via Cmd-U in Xcode confirms it passes (xcodebuild test hangs headlessly per project memory — build-for-testing is the automated gate here, Cmd-U is the manual pass-confirmation at wave/phase gate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Dropped-file metadata → SwiftUI render | `ShelfItem.filename` originates from an external file the user (or, from Phase 22 onward, a drag source) provided — untrusted string rendered directly in the UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-20-01 | Tampering (visual surface) | `ShelfItemView`'s filename `Text` | mitigate | `.lineLimit(1)` + `.truncationMode(.middle)` on the filename caption — same discipline already applied to calendar event titles / now-playing metadata elsewhere in `NotchPillView.swift` |
| T-20-04 | Information Disclosure (none — decorative only) | file-type icon lookup | accept | `NSWorkspace.shared.icon(forFile:)` is a read-only, side-effect-free AppKit call; no data leaves the process |
</threat_model>

<verification>
Build gate after every task: `cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"`.
Wave gate: manual Cmd-U in Xcode (GUI, not terminal — `xcodebuild test` hangs headlessly per project memory) confirms `IslandResolverTests` (including the new SHELF-09 case) and the existing `#Preview`s render without crashing.
</verification>

<success_criteria>
- `ShelfViewState.swift` and `ShelfItemView.swift` exist and compile
- `NotchPillView.swift`'s `blobShape` grows conditionally by `shelfRowHeight` (56pt) only when `shelfViewState.items` is non-empty, uniformly across `expandedIsland`/`mediaExpanded`/`mediaUnavailable`
- The shelf row renders file icons, per-item trash, and one far-right delete-all trash icon
- `IslandResolverTests.swift` proves SHELF-09's structural suppression with zero new resolver production code
- `xcodebuild build -scheme Islet -configuration Debug` succeeds after every task
</success_criteria>

<output>
Create `.planning/phases/20-shelf-view/20-01-SUMMARY.md` when done
</output>
