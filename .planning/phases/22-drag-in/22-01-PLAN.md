---
phase: 22-drag-in
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Notch/NotchPanel.swift
autonomous: false
requirements: [SHELF-01, SHELF-02]

must_haves:
  truths:
    - "An on-device, interactive test has empirically confirmed whether AppKit drag-destination delivery (draggingEntered/performDragOperation) reaches NotchPanel while ignoresMouseEvents == true, starting the drag from OUTSIDE the pill's hot zone -- closing Assumption A1, the single largest open technical risk of this milestone (per STATE.md Blockers/Concerns and 22-RESEARCH.md)"
    - "If the spike PASSES, Plans 22-02/22-03 proceed on the Pattern-1 (AppKit-direct registration on NotchPanel) architecture already scaffolded here"
    - "If the spike FAILS, no further Phase 22 plan is executed autonomously -- the result is reported back so the project returns to /gsd:discuss-phase 22 to decide the fallback (22-RESEARCH.md Open Question 2), since D-02 (reuse the existing hot-zone) is a locked decision, not implementation discretion"
  artifacts:
    - path: "Islet/Notch/NotchPanel.swift"
      provides: "Throwaway spike scaffold: registerForDraggedTypes([.fileURL]) in init + 4 NSLog-marked NSDraggingDestination override stubs, to be replaced by 22-03's permanent closure-forwarding architecture"
      contains: "registerForDraggedTypes([.fileURL])"
  key_links: []
---

<objective>
Resolve Assumption A1 (22-RESEARCH.md) before any other Phase 22 work happens: does a click-through,
non-activating NSPanel (ignoresMouseEvents == true) actually receive AppKit NSDraggingDestination
callbacks when a drag starts from OUTSIDE the pill's hot zone? This is a genuine, undocumented gap
(no Apple source and no community source states the interaction between ignoresMouseEvents and drag
delivery one way or the other) that can only be closed by an interactive, on-device test -- never by
code review or an automated test (project memory xcodebuild-test-headless-hang: `xcodebuild test`
hangs headlessly in this environment because the full Islet.app boot is required).

Purpose: Close the phase's single largest open risk FIRST (per this project's own established
convention of isolating the highest-uncertainty integration point, already used to sequence this
phase last in the v1.3 milestone) so Plans 22-02/22-03 are built on an empirically confirmed
architecture, not a guess.
Output: A throwaway, clearly-marked spike scaffold in NotchPanel.swift (registration + 4 minimal
NSLog-marked overrides) and a recorded PASS/FAIL verdict from an interactive on-device drag test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-drag-in/22-CONTEXT.md
@.planning/phases/22-drag-in/22-RESEARCH.md
@.planning/phases/22-drag-in/22-PATTERNS.md
@.planning/phases/22-drag-in/22-VALIDATION.md

<interfaces>
Current full contents of the ONLY file this plan touches (37 lines, already read in full this
session -- no further exploration needed before editing):

import AppKit

// ISL-02 / D-07 -- the borderless, non-activating overlay window.
//
// This is the ONLY AppKit window in the app (everything visible is SwiftUI hosted
// inside it). It is configured ONCE in `init`; in particular `.nonactivatingPanel`
// is set in the styleMask at init and never toggled later (AppKit does not fully
// re-apply activation behavior post-init -- see RESEARCH Anti-patterns).
final class NotchPanel: NSPanel {
    init(contentRect: NSRect) {
        super.init(
            contentRect: contentRect,
            styleMask: [.borderless, .nonactivatingPanel], // borderless + never activates the app (D-07)
            backing: .buffered,
            defer: false
        )
        isOpaque = false
        backgroundColor = .clear          // transparent window; the pill draws the black
        hasShadow = false                 // no drop shadow around the notch
        isMovable = false
        isMovableByWindowBackground = false
        isReleasedWhenClosed = false      // keep the object alive across show/hide
        ignoresMouseEvents = true
        level = .statusBar                // above normal windows; see A2 -- Plan 03 tunes vs the Tahoe menu bar
        collectionBehavior = [.canJoinAllSpaces, .fullScreenAuxiliary, .stationary] // ISL-02: all Spaces, above fullscreen-aux
    }
    // A non-activating overlay must NEVER take focus (D-07):
    override var canBecomeKey: Bool { false }
    override var canBecomeMain: Bool { false }
}

Confirmed via this session's own SDK header read (AppKit.framework/Headers/NSDragging.h):
NSDraggingDestination is an @optional Objective-C protocol -- NSWindow/NSPanel does NOT need an
explicit ": NSDraggingDestination" conformance declaration; overriding any of its methods directly
on the NSPanel subclass is sufficient (this is exactly what 22-RESEARCH.md's Pattern 1 describes as
"NSWindow providing a default forwarding implementation... to its delegate", and a direct subclass
override is the simplest form of that). registerForDraggedTypes(_:) is declared directly on
NSWindow (confirmed via the same header: "- (void)registerForDraggedTypes:(NSArray<NSPasteboardType> *)newTypes;").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the throwaway drag-destination spike on NotchPanel</name>
  <files>Islet/Notch/NotchPanel.swift</files>
  <read_first>
    Islet/Notch/NotchPanel.swift (whole file, 37 lines -- the exact current init/overrides this
    task extends)
    .planning/phases/22-drag-in/22-RESEARCH.md "## Recommended Spike" section (steps 1-6 -- this
    task implements steps 1-2 exactly)
    .planning/phases/22-drag-in/22-RESEARCH.md "## Common Pitfalls" Pitfall 1 (the exact risk this
    spike tests: ignoresMouseEvents = true silently swallowing the drop)
  </read_first>
  <action>
    In Islet/Notch/NotchPanel.swift, add registerForDraggedTypes([.fileURL]) as the new last line
    inside init, directly after the existing collectionBehavior = [...] line, with the inline
    comment "// Phase 22 spike (A1) -- see 22-RESEARCH.md Recommended Spike; registration is
    permanent, the overrides below are throwaway".

    Add four override methods directly after the existing canBecomeKey/canBecomeMain overrides,
    clearly bracketed with a "// SPIKE -- throwaway, 22-03 Task 1 replaces this block with the real
    closure-forwarding architecture. Do not build production logic on top of this." comment:
    - override func draggingEntered(_ sender: NSDraggingInfo) -> NSDragOperation -- call
      NSLog("SPIKE draggingEntered fired"), return .copy.
    - override func draggingUpdated(_ sender: NSDraggingInfo) -> NSDragOperation -- return .copy
      (no log needed; only entered/exited/perform are the meaningful spike signals).
    - override func draggingExited(_ sender: NSDraggingInfo?) -- call
      NSLog("SPIKE draggingExited fired").
    - override func performDragOperation(_ sender: NSDraggingInfo) -> Bool -- call
      NSLog with "SPIKE performDragOperation fired, urls: \(sender.draggingPasteboard.readObjects(forClasses: [NSURL.self], options: nil) ?? [])"),
      return true.

    This is a genuine throwaway: NSLog (not the DEBUG-gated print convention used elsewhere in this
    codebase) is deliberate here so the human tester can see output in Console.app even on a
    Release-style run -- 22-03 removes every line of this block.
  </action>
  <acceptance_criteria>
    - Islet/Notch/NotchPanel.swift contains registerForDraggedTypes([.fileURL]) inside init
    - The file contains exactly 4 new override functions: draggingEntered, draggingUpdated,
      draggingExited, performDragOperation, each calling into NSLog (except draggingUpdated)
    - xcodebuild build -scheme Islet -configuration Debug succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED" && grep -q "registerForDraggedTypes" Islet/Notch/NotchPanel.swift && [ "$(grep -cE 'override func draggingEntered|override func draggingUpdated|override func draggingExited|override func performDragOperation' Islet/Notch/NotchPanel.swift)" = "4" ]</automated>
  </verify>
  <done>NotchPanel.swift registers for .fileURL dragged types and has 4 throwaway override stubs logging via NSLog; build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device spike -- does the drag actually reach the click-through panel?</name>
  <read_first>
    .planning/phases/22-drag-in/22-RESEARCH.md "## Recommended Spike" steps 3-6 and "## Common
    Pitfalls" Pitfall 1's "Warning signs" paragraph (a drag that starts with the pointer ALREADY
    inside the hot zone would misleadingly "work" and mask the real risk -- the drag must start
    from OUTSIDE)
    .planning/STATE.md "Blockers/Concerns" -- the A1 unverified-assumption entry this task closes
  </read_first>
  <files>Islet/Notch/NotchPanel.swift</files>
  <action>
    No file edits in this task -- build and run the app on-device (Cmd-R in Xcode) and perform the
    interactive drag test described in how-to-verify below. This task's only output is the recorded
    PASSED/FAILED verdict.
  </action>
  <what-built>
    Islet/Notch/NotchPanel.swift now registers for .fileURL drag types and has 4 throwaway override
    stubs (Task 1) that NSLog when AppKit's drag-destination callbacks fire.
  </what-built>
  <how-to-verify>
    1. Open the project in Xcode and Build + Run on-device (Cmd-R) -- do NOT use `xcodebuild test`
       (project memory: it hangs headlessly because the full Islet.app boot is required).
    2. Open the Xcode console pane (or Console.app filtered to the Islet process) to watch for
       lines starting with "SPIKE".
    3. Move the mouse pointer somewhere clearly OUTSIDE the notch pill's hot zone first -- this
       confirms ignoresMouseEvents is currently true (click-through) before the drag begins.
    4. In Finder, pick up a single file (mouse-down and start dragging) and drag it toward the
       collapsed island pill at the top of the screen, entering the pill's hot zone from outside.
    5. Watch the console: does "SPIKE draggingEntered fired" appear as the file enters the pill?
    6. If yes, continue holding and drop the file onto the pill -- does "SPIKE performDragOperation
       fired, urls: [...]" appear, showing the correct dropped file's URL?
    7. Optionally also drag the file away without dropping, to confirm "SPIKE draggingExited fired"
       appears too.
  </how-to-verify>
  <acceptance_criteria>
    - PASSED verdict requires BOTH "SPIKE draggingEntered fired" and "SPIKE performDragOperation
      fired, urls: [...]" (with the correct file URL) to appear, with the drag having started from
      OUTSIDE the hot zone
    - A FAILED verdict is any outcome where draggingEntered never fires with the drag starting from
      outside the hot zone (Pitfall 1's core risk)
  </acceptance_criteria>
  <resume-signal>
    Reply "PASSED" if draggingEntered AND performDragOperation both fired with the drag starting
    from outside the hot zone -- Plans 22-02 and 22-03 proceed on the Pattern-1 architecture.
    Reply "FAILED -- <describe exactly what you observed>" if draggingEntered never fired. In that
    case STOP: do not execute 22-02/22-03 as planned -- they assume Pattern 1 works. Return to
    `/gsd:discuss-phase 22` to decide the fallback architecture (22-RESEARCH.md Open Question 2 --
    a wider always-interactive drop zone, or an ignoresMouseEvents=false-during-suspected-drag
    heuristic; both require a CONTEXT.md decision change since D-02 is currently locked).
  </resume-signal>
  <verify>
    <automated>MISSING -- manual on-device drag session only, no automated harness exists (22-VALIDATION.md)</automated>
  </verify>
  <done>A definitive PASSED or FAILED verdict has been recorded for Assumption A1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Finder/other app (drag source) -> NSPasteboard -> NotchPanel's registered drag destination | Inbound: this is the first time an external, untrusted file URL crosses INTO the app via drag (contrast Phase 21, which was outbound-only). This plan's spike overrides only NSLog the payload, never write/read/copy any file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-22-01-01 | Information Disclosure | Task 1's performDragOperation NSLog of dropped file URLs | accept | Throwaway, DEBUG-only-in-practice spike code, removed entirely by 22-03 Task 1 before any production build ships; the URLs logged are exactly what the user themselves is dragging, no new information is disclosed beyond the local Console log an on-device developer already controls. |
| T-22-01-02 | Denial of Service | performDragOperation returning true unconditionally, without extracting/copying anything | accept | The spike stub does no file I/O at all (only reads pasteboard metadata to log it) -- there is no resource-exhaustion surface introduced by this throwaway scaffold. |
| T-22-SC | Tampering (supply chain) | npm/pip/cargo installs | not applicable | Zero new external dependencies -- this plan only calls first-party AppKit APIs (registerForDraggedTypes, NSDraggingInfo, NSLog) already linked into the Islet target. |
</threat_model>

<verification>
Build gate after Task 1: cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED".

Phase-blocking gate: Task 2's on-device spike result. Per 22-VALIDATION.md, this is inherently
manual-only -- no XCTest harness can simulate a real OS drag session reaching a registered
NSDraggingDestination.
</verification>

<success_criteria>
- NotchPanel.swift registers for .fileURL dragged types and has 4 throwaway NSLog-marked override
  stubs
- xcodebuild build -scheme Islet -configuration Debug succeeds
- An on-device interactive test has produced a definitive PASSED or FAILED verdict for Assumption
  A1, recorded in this plan's SUMMARY.md
</success_criteria>

<output>
Create .planning/phases/22-drag-in/22-01-SUMMARY.md when done, recording the PASSED/FAILED verdict
verbatim -- this is the permanent record RESEARCH.md's own "Valid until" note asks for instead of
re-researching the question later.
</output>
