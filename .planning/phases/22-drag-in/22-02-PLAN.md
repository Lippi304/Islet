---
phase: 22-drag-in
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - Islet/Notch/NotchInteractionState.swift
  - IsletTests/InteractionStateTests.swift
  - Islet/Notch/DragDropSupport.swift
  - IsletTests/DragDropSupportTests.swift
  - IsletTests/ShelfFileStoreTests.swift
autonomous: true
requirements: [SHELF-01, SHELF-02]

must_haves:
  truths:
    - "A drag-enter event auto-expands the island from .collapsed or .hovering exactly like a click does (D-01), expressed as a pure, unit-tested state transition -- not embedded ad-hoc inside AppKit glue"
    - "Extracting file/folder URLs from a drop payload is a pure, unit-tested function that returns a folder URL as exactly ONE item, never enumerated (REQUIREMENTS.md Out of Scope, Pitfall 4)"
    - "The D-04 collapsed-only accept gate and the non-file/empty-payload rejection (CONTEXT.md Claude's Discretion) are expressed as one pure, unit-tested gate function, never a silent inline assumption"
    - "ShelfFileStore.makeSessionCopy (Phase 19, unmodified) is confirmed by a new test to correctly round-trip a directory URL, closing 22-RESEARCH.md Open Question 3 before 22-03 relies on it for dropped folders"
  artifacts:
    - path: "Islet/Notch/DragDropSupport.swift"
      provides: "fileURLs(from:) and shouldAcceptDrop(isExpanded:urls:) -- the phase's two genuinely novel pure seams"
      contains: "func fileURLs(from pasteboard: NSPasteboard) -> [URL]"
    - path: "Islet/Notch/NotchInteractionState.swift"
      provides: "InteractionEvent.dragEntered + nextState's two new (.collapsed/.hovering, .dragEntered) -> .expanded transitions"
      contains: "case dragEntered"
    - path: "IsletTests/DragDropSupportTests.swift"
      provides: "Unit coverage for both DragDropSupport.swift functions (22-VALIDATION.md Wave 0 requirement)"
      contains: "final class DragDropSupportTests"
    - path: "IsletTests/ShelfFileStoreTests.swift"
      provides: "testMakeSessionCopyHandlesDirectoryURL -- the folder round-trip test (22-VALIDATION.md Wave 0 requirement)"
      contains: "testMakeSessionCopyHandlesDirectoryURL"
  key_links:
    - from: "Islet/Notch/DragDropSupport.swift"
      to: "IsletTests/DragDropSupportTests.swift"
      via: "direct unit tests calling fileURLs(from:)/shouldAcceptDrop(isExpanded:urls:) with a real NSPasteboard"
      pattern: "fileURLs\\(from:"
    - from: "Islet/Notch/NotchInteractionState.swift"
      to: "IsletTests/InteractionStateTests.swift"
      via: "direct unit tests calling nextState(_, .dragEntered)"
      pattern: "nextState\\(\\.collapsed, \\.dragEntered\\)"
---

<objective>
Build the two pure, AppKit-glue-free seams 22-RESEARCH.md's Wave 0 flags as this phase's genuinely
testable surface, before any AppKit wiring is touched in 22-03: (1) a new .dragEntered event on the
existing nextState state machine driving D-01's auto-expand, and (2) DragDropSupport.swift's
fileURLs(from:)/shouldAcceptDrop(isExpanded:urls:) pair covering URL extraction and the D-04 +
non-file-payload rejection gate. Also closes Open Question 3 (folder round-trip through the
unmodified ShelfFileStore.makeSessionCopy).

Scope note (revision, 2026-07-10): this plan is UNCHANGED in substance from before the D-02
hot-zone/Mission-Control revision. Neither seam built here encodes any drop-zone GEOMETRY --
nextState's .dragEntered transition is purely about the phase state machine (.collapsed/.hovering ->
.expanded), and shouldAcceptDrop(isExpanded:urls:) is purely D-04's isExpanded gate + the non-file/
empty-payload rejection, with no spatial/screen-coordinate component at all. The revised D-02b
(drag-accept reuses the existing expandedZone rect) / D-02c (landing-margin-below-top-edge gate) /
D-05 (wider auto-expand trigger) / D-06 (hot-feedback fires with the same wider trigger) decisions in
22-CONTEXT.md are entirely 22-03's concern (AppKit glue reading real screen coordinates) -- this
plan's two pure functions are called by 22-03's handlers exactly as originally designed, unaffected
by which geometry gates WHEN they're called.

Purpose: Everything AppKit can deliver to this app (the dropped pasteboard, the drag-enter/exit
signal) reduces to plain values the instant it crosses into Swift -- keeping the actual business
logic pure and unit-tested, mirroring this project's own established pure-seam-first convention
(IslandResolver, nextState, shouldOpenShelfItem) rather than burying decisions inside AppKit
callback bodies where 22-03 will wire them.
Output: Two small, fully-tested pure functions plus one new pure state-machine transition, ready for
22-03 to call directly from NotchPanel's forwarded callbacks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-drag-in/22-CONTEXT.md
@.planning/phases/22-drag-in/22-RESEARCH.md
@.planning/phases/22-drag-in/22-PATTERNS.md
@.planning/phases/22-drag-in/22-VALIDATION.md
@.planning/phases/22-drag-in/22-01-SUMMARY.md

<interfaces>
Current full contents of Islet/Notch/NotchInteractionState.swift (41 lines, already read in full
this session):

import Foundation
import CoreGraphics

enum InteractionPhase: Equatable { case collapsed, hovering, expanded }
enum InteractionEvent: Equatable { case pointerEntered, pointerExited, clicked, graceElapsed }

func nextState(_ current: InteractionPhase, _ event: InteractionEvent) -> InteractionPhase {
    switch (current, event) {
    case (.collapsed, .pointerEntered): return .hovering   // D-01: hover affordance only
    case (.hovering,  .pointerExited):  return .hovering   // D-03: defer -- grace timer pending
    case (.hovering,  .graceElapsed):   return .collapsed  // D-03: grace elapsed, pointer out
    case (.hovering,  .clicked):        return .expanded   // D-02: expand on click only
    case (.collapsed, .clicked):        return .expanded   // click expands even if enter was missed
    case (.expanded,  .pointerExited):  return .expanded   // D-03: defer collapse
    case (.expanded,  .graceElapsed):   return .collapsed  // D-03: grace elapsed while expanded
    case (.expanded,  .pointerEntered): return .expanded   // stay expanded
    case (.expanded,  .clicked):        return .collapsed  // toggle shut
    default:                            return current     // idempotent no-ops
    }
}

final class NotchInteractionState: ObservableObject {
    @Published var phase: InteractionPhase = .collapsed
    @Published var collapsedNotchSize: CGSize?
    var isExpanded: Bool { phase == .expanded }
    var isHovering: Bool { phase == .hovering || phase == .expanded }
}

grep confirms InteractionEvent/InteractionPhase are referenced ONLY inside this one file's own
switch -- no other exhaustive switch elsewhere in the codebase depends on InteractionEvent's case
set, so adding a new case is source-compatible everywhere else.

Current full contents of IsletTests/InteractionStateTests.swift's nextState section (lines 9-71,
already read in full this session -- one XCTAssertEqual(nextState(...), ...) per test, no fixture,
exact style Task 1 mirrors):

func testHoveringClickExpands() {
    XCTAssertEqual(nextState(.hovering, .clicked), .expanded)
}
func testCollapsedClickExpands() {
    XCTAssertEqual(nextState(.collapsed, .clicked), .expanded)
}
func testExpandedPointerEnterStaysExpanded() {
    XCTAssertEqual(nextState(.expanded, .pointerEntered), .expanded)
}

Current relevant excerpt of Islet/Shelf/ShelfViewState.swift (the exact one-line pure-gate-function
convention Task 2's shouldAcceptDrop mirrors):

func shouldOpenShelfItem(fileExists: Bool) -> Bool { fileExists }

Current relevant excerpt of IsletTests/ShelfFileStoreTests.swift (101 lines, already read in full
this session -- the setUp/tearDown fixturesDir convention Task 2's new folder-round-trip test
reuses exactly, plus ShelfFileStore's real signature):

override func setUp() {
    super.setUp()
    fixturesDir = URL(fileURLWithPath: NSTemporaryDirectory())
        .appendingPathComponent("ShelfFileStoreTestsFixtures-\(UUID())", isDirectory: true)
    try? FileManager.default.createDirectory(at: fixturesDir, withIntermediateDirectories: true)
}
override func tearDown() {
    try? FileManager.default.removeItem(at: fixturesDir)
    fixturesDir = nil
    super.tearDown()
}
// ShelfFileStore.makeSessionCopy(of sourceURL: URL, id: UUID) throws -> URL
// -- already calls FileManager.default.copyItem(at:to:), which DOES recurse directory
// trees; this task adds a test case only, no production code change.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: nextState .dragEntered event -- the pure D-01 auto-expand seam</name>
  <files>Islet/Notch/NotchInteractionState.swift, IsletTests/InteractionStateTests.swift</files>
  <read_first>
    Islet/Notch/NotchInteractionState.swift (whole file, 41 lines)
    IsletTests/InteractionStateTests.swift (whole file, 95 lines -- the exact nextState test
    section style, lines 9-71)
  </read_first>
  <behavior>
    - nextState(.collapsed, .dragEntered) returns .expanded
    - nextState(.hovering, .dragEntered) returns .expanded
    - nextState(.expanded, .dragEntered) returns .expanded (idempotent no-op via the existing
      default arm -- D-04's collapsed-only accept gate is enforced by the CALLER never invoking
      this transition while already expanded, not by this pure function)
  </behavior>
  <action>
    In Islet/Notch/NotchInteractionState.swift, add a new case dragEntered to InteractionEvent
    (line 9), after graceElapsed: enum InteractionEvent: Equatable { case pointerEntered,
    pointerExited, clicked, graceElapsed, dragEntered }.

    In nextState(_:_:)'s switch, add two new cases directly after the existing
    "case (.collapsed, .clicked): return .expanded" line: "case (.hovering, .dragEntered): return
    .expanded" and "case (.collapsed, .dragEntered): return .expanded" -- with an inline comment
    "// Phase 22 / SHELF-01 (D-01/D-05): drag-enter auto-expands, same target as .clicked -- the
    CALLER (22-03) gates WHICH geometry triggers this event (D-02b/D-02c), this transition itself
    is geometry-agnostic". Do NOT add an explicit (.expanded, .dragEntered) case -- the existing
    "default: return current" arm already covers it.

    In IsletTests/InteractionStateTests.swift, add three new tests directly below
    testCollapsedClickExpands() (in the "MARK: nextState" section): testCollapsedDragEnteredExpands
    (asserts nextState(.collapsed, .dragEntered) == .expanded), testHoveringDragEnteredExpands
    (asserts nextState(.hovering, .dragEntered) == .expanded), and
    testExpandedDragEnteredIsIdempotent (asserts nextState(.expanded, .dragEntered) == .expanded) --
    identical one-line XCTAssertEqual shape to the surrounding tests, no fixture.
  </action>
  <acceptance_criteria>
    - InteractionEvent contains a dragEntered case
    - nextState's switch contains a (.hovering, .dragEntered) case and a (.collapsed, .dragEntered)
      case, both returning .expanded
    - InteractionStateTests.swift contains testCollapsedDragEnteredExpands,
      testHoveringDragEnteredExpands, and testExpandedDragEnteredIsIdempotent
    - xcodebuild build succeeds (test target compiles)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build-for-testing -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED" && grep -q "case dragEntered" Islet/Notch/NotchInteractionState.swift && grep -q "hovering, .dragEntered" Islet/Notch/NotchInteractionState.swift && grep -q "testCollapsedDragEnteredExpands" IsletTests/InteractionStateTests.swift</automated>
  </verify>
  <done>InteractionEvent.dragEntered exists with both transitions wired to .expanded; 3 new tests exist mirroring the file's existing style; test target compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DragDropSupport.swift (URL extraction + accept gate) + folder round-trip test</name>
  <files>Islet/Notch/DragDropSupport.swift, IsletTests/DragDropSupportTests.swift, IsletTests/ShelfFileStoreTests.swift</files>
  <read_first>
    Islet/Shelf/ShelfViewState.swift (whole file, 19 lines -- the one-line pure-gate-function
    convention to mirror)
    Islet/Notch/NotchGeometry.swift (top-of-file import + first function signature only -- the
    top-level-free-function-in-Islet/Notch/ convention this new file follows)
    IsletTests/ShelfLogicTests.swift lines 1-26 (the fixture-free test-file convention
    DragDropSupportTests.swift mirrors: one test method per behavior, no setUp/tearDown)
    IsletTests/ShelfFileStoreTests.swift (whole file, 101 lines -- the exact fixturesDir
    setUp/tearDown this task's new test reuses, and ShelfFileStore.makeSessionCopy's real signature)
  </read_first>
  <behavior>
    - fileURLs(from:) on a pasteboard holding 1 file URL returns that single URL
    - fileURLs(from:) on a pasteboard holding 3 file URLs returns all 3, in write order
    - fileURLs(from:) on a pasteboard holding 1 directory URL returns that ONE URL (never
      enumerated -- REQUIREMENTS.md Out of Scope, Pitfall 4)
    - fileURLs(from:) on a pasteboard holding only a plain string (no file representation) returns
      an empty array
    - shouldAcceptDrop(isExpanded: true, urls: [anyURL]) returns false (D-04)
    - shouldAcceptDrop(isExpanded: false, urls: []) returns false (empty/non-file payload rejection)
    - shouldAcceptDrop(isExpanded: false, urls: [anyURL]) returns true
    - ShelfFileStore.makeSessionCopy on a directory URL containing one nested file copies the whole
      directory tree -- the returned localURL is itself a directory, and the nested file exists
      underneath it with matching contents (Open Question 3; production code is UNCHANGED, this is
      a new test case proving the existing FileManager.copyItem call already handles it)
  </behavior>
  <action>
    Create Islet/Notch/DragDropSupport.swift (new file) with "import AppKit" and a header comment
    referencing Phase 22 / SHELF-01/SHELF-02 and 22-RESEARCH.md's Wave 0 gaps, mirroring
    NotchGeometry.swift's top-level-pure-function file shape. Add a function fileURLs that takes a
    pasteboard: NSPasteboard parameter and returns [URL], implemented as
    (pasteboard.readObjects(forClasses: [NSURL.self], options: nil) as? [URL]) ?? [] -- a folder
    URL is returned as-is, never enumerated. Add a second function shouldAcceptDrop that takes
    isExpanded: Bool and urls: [URL] and returns Bool, implemented as
    !isExpanded && !urls.isEmpty -- combines D-04's collapsed-only gate with the non-file/
    empty-payload rejection (CONTEXT.md Claude's Discretion item) into one silent no-op gate,
    mirroring shouldOpenShelfItem's shape. This function has NO spatial/screen-coordinate
    component -- 22-03's isWithinDragAcceptRegion(_:) (D-02b/D-02c) is a SEPARATE, additional gate
    applied only in the AppKit glue, not here.

    Create IsletTests/DragDropSupportTests.swift (new file, no setUp/tearDown, mirrors
    ShelfLogicTests.swift's fixture-free convention) with 7 tests covering exactly the 7 <behavior>
    cases above. For the pasteboard-based tests, construct a fresh, uniquely-named pasteboard per
    test via NSPasteboard(name: NSPasteboard.Name with a UUID-suffixed string), clearContents(),
    then writeObjects with an array of NSURL (single or multiple) before calling fileURLs(from:) --
    do NOT use NSPasteboard.general (would pollute the real system clipboard during CI/local test
    runs). For the directory-URL test, use URL(fileURLWithPath: NSTemporaryDirectory(), isDirectory:
    true) as a stand-in directory (no need to actually create it on disk -- fileURLs(from:) only
    reads pasteboard metadata, not the filesystem). For the non-file-payload test, use
    setString("hello", forType: .string) instead of writeObjects.

    In IsletTests/ShelfFileStoreTests.swift, add testMakeSessionCopyHandlesDirectoryURL directly
    below testMakeSessionCopyRejectsPathTraversalFilename (the last test in the file): inside
    fixturesDir, create a subdirectory via FileManager.default.createDirectory at
    fixturesDir.appendingPathComponent("nestedFolder", isDirectory: true) with
    withIntermediateDirectories: true, write one nested file (nestedFolder/inner.txt with known
    contents) inside it, call ShelfFileStore.makeSessionCopy(of: thatDirectoryURL, id: UUID()),
    then assert (a) the returned localURL exists as a directory
    (FileManager.default.fileExists(atPath:isDirectory:) with the isDirectory out-parameter true),
    and (b) localURL.appendingPathComponent("inner.txt") exists with contents equal to what was
    written. No production code change -- ShelfFileStore.swift is NOT in this task's files list.
  </action>
  <acceptance_criteria>
    - Islet/Notch/DragDropSupport.swift exists with exactly the two function signatures specified
    - IsletTests/DragDropSupportTests.swift exists with 7 test methods, all passing, none touching
      NSPasteboard.general
    - IsletTests/ShelfFileStoreTests.swift contains testMakeSessionCopyHandlesDirectoryURL and
      Islet/Shelf/ShelfFileStore.swift is byte-for-byte unchanged
    - xcodebuild build-for-testing succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build-for-testing -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED" && grep -q "func fileURLs(from pasteboard: NSPasteboard) -> " Islet/Notch/DragDropSupport.swift && grep -q "func shouldAcceptDrop(isExpanded: Bool, urls: " Islet/Notch/DragDropSupport.swift && [ "$(grep -c 'func test' IsletTests/DragDropSupportTests.swift)" = "7" ] && grep -q "testMakeSessionCopyHandlesDirectoryURL" IsletTests/ShelfFileStoreTests.swift</automated>
  </verify>
  <done>DragDropSupport.swift exists with fileURLs(from:) and shouldAcceptDrop(isExpanded:urls:); DragDropSupportTests.swift has 7 passing tests; ShelfFileStoreTests.swift has the new folder round-trip test; build-for-testing succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| A drop pasteboard's contents (constructed here only from test-owned data) -> fileURLs(from:) | This plan does not touch a real, externally-populated drag pasteboard -- that boundary is exercised by 22-03's actual AppKit wiring and its on-device checkpoint. Here, the function is exercised with test-constructed pasteboards only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-01 | Input Validation (V5) | shouldAcceptDrop(isExpanded:urls:) | mitigate | The empty-urls branch is exactly the mechanism that rejects a non-file-URL payload once 22-03 wires it to a real drop -- validated here in isolation with 3 dedicated unit tests (empty array, non-empty array, expanded-state combination) before any AppKit code depends on it. |
| T-22-02-02 | Tampering | fileURLs(from:) never enumerating a folder URL's contents | mitigate | Directly enforced and unit-tested here (testFileURLsReturnsFolderURLAsSingleItem) -- REQUIREMENTS.md's Out of Scope folder-spring-loading exclusion is a property of this pure function, provable independent of any real drag session. |
| T-22-SC | Tampering (supply chain) | npm/pip/cargo installs | not applicable | Zero new external dependencies -- this plan only adds first-party AppKit/Foundation code (NSPasteboard, NSURL, FileManager), already linked into the Islet target. |
</threat_model>

<verification>
Build gate after every task: cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build-for-testing -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED".

Manual Cmd-U in Xcode (project memory xcodebuild-test-headless-hang: xcodebuild test hangs
headlessly) confirms InteractionStateTests, DragDropSupportTests, and ShelfFileStoreTests all pass
green before 22-03 begins.
</verification>

<success_criteria>
- nextState(.collapsed/.hovering, .dragEntered) both return .expanded, unit-tested
- fileURLs(from:) and shouldAcceptDrop(isExpanded:urls:) exist in Islet/Notch/DragDropSupport.swift,
  each fully unit-tested (7 total new tests)
- ShelfFileStore.makeSessionCopy's directory round-trip is confirmed by a new test, with zero
  production code change to ShelfFileStore.swift
- xcodebuild build-for-testing -scheme Islet -configuration Debug succeeds after every task
</success_criteria>

<output>
Create .planning/phases/22-drag-in/22-02-SUMMARY.md when done
</output>
