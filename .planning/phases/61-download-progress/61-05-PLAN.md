---
phase: 61-download-progress
plan: 05
type: execute
wave: 4
depends_on: ["61-02", "61-03", "61-04"]
files_modified: []
autonomous: false
requirements: [DL-01, DL-02]

must_haves:
  truths:
    - "The scoped IsletTests suite (DownloadActivityTests, DownloadCoordinatorTests, IslandResolverTests) is green, and both Debug and Release configurations build successfully"
    - "On-device: starting a real browser download into ~/Downloads shows the spinner + 'Downloading…' HUD within a couple seconds, then a checkmark + real filename 'done' HUD for ~3s, then clears on its own (SC1/SC2)"
    - "On-device: two downloads in quick succession are each detected as one logical download apiece, with no double-fire and no dropped completion (SC3)"
    - "On-device: the feature is OFF by default, appears in the Settings grid, and produces zero indicator when disabled (SC4)"
    - "On-device: a cancelled download (temp file deleted before rename) produces no HUD at all (D-15); manually expanding the island cuts a standing 'done' splash short immediately (D-04)"
  artifacts: []
  key_links: []
---

<objective>
Close out Phase 61 with the automated build/test gate and the on-device checkpoint every prior plan
deferred to it: FSEvents timing/correlation behavior (RESEARCH.md's own stated MEDIUM confidence on
Pitfalls 2/3, recommending an on-device spike before full trust) plus all 4 ROADMAP Phase 61 Success
Criteria.

Purpose: this is the goal-backward verification wave — confirms the pure seams (61-01), the
coordinator (61-02), the wing rendering (61-03), and the real FSEvents monitor + controller wiring
(61-04) actually compose into a working, on-device-confirmed feature, not just green unit tests.
Output: a build/test gate pass plus a completed on-device UAT checklist, closing DL-01/DL-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/61-download-progress/61-CONTEXT.md
@.planning/phases/61-download-progress/61-RESEARCH.md
@.planning/phases/61-download-progress/61-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build + scoped test suite gate</name>
  <files></files>
  <read_first>.planning/PROJECT.md</read_first>
  <action>
Run a Debug build (`xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build`)
and confirm it succeeds with no new warnings attributable to this phase's files
(`DownloadActivity.swift`, `DownloadCoordinator.swift`, `DownloadMonitor.swift`, `IslandResolver.swift`,
`NotchWindowController.swift`, `NotchPillView.swift`). Run a Release build the same way and confirm it
also succeeds. Run the scoped test target via Xcode Cmd+U or
`xcodebuild test -project Islet.xcodeproj -scheme Islet -only-testing:IsletTests/DownloadActivityTests -only-testing:IsletTests/DownloadCoordinatorTests -only-testing:IsletTests/IslandResolverTests`
(per 61-VALIDATION.md's sampling rate — this scoping avoids the documented full-suite Bluetooth-monitor
hang in non-interactive sessions, PROJECT.md:425) and confirm every test in all 3 classes is green,
specifically the 8 new `DownloadActivityTests`, 9 new `DownloadCoordinatorTests`, and 4 new/extended
`IslandResolverTests` methods from Plans 61-01/61-02. If running headlessly is not possible in this
session, route to a manual Cmd+U pass per the documented workaround and confirm full-suite green aside
from the 2 pre-existing unrelated `CalendarGlanceTests` failures (STATE.md Phase 52).
  </action>
  <acceptance_criteria>
    <test_command>Scoped xcodebuild test (or Xcode Cmd+U) → DownloadActivityTests + DownloadCoordinatorTests + IslandResolverTests all green</test_command>
    <test_command>Debug build succeeds with zero new compiler warnings attributable to this phase's files</test_command>
    <test_command>Release build succeeds</test_command>
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build</automated>
  </verify>
  <done>Both Debug and Release builds succeed; the scoped DownloadActivityTests/DownloadCoordinatorTests/IslandResolverTests suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Consolidated on-device UAT — Download-Progress (all 4 ROADMAP Success Criteria)</name>
  <files></files>
  <read_first>.planning/ROADMAP.md "Phase 61: Download-Progress" Success Criteria (4 items)</read_first>
  <action>
No file edits in this task — build and run Islet on-device (Cmd-R in Xcode) and perform the 11-step
on-device checklist described in how-to-verify below. This task's only output is the recorded
approved/failed verdict covering all 4 ROADMAP Phase 61 Success Criteria plus RESEARCH.md's flagged
FSEvents rename-correlation confidence (Assumption A3).
  </action>
  <what-built>
A new collapsed-only transient HUD (Download-Progress) wired end-to-end: a real `FSEventStreamCreate`
watcher on `~/Downloads`, a per-file coordinator tracking each temp download independently (D-05/D-06),
ranked rank 5 in the TransientQueue (above Caps Lock/Update, below OSD), gated by its own Settings-grid
toggle (default OFF), showing a generic "Downloading…" spinner while in progress and the real final
filename + checkmark for ~3s on completion, with no HUD at all for a cancelled download.
  </what-built>
  <how-to-verify>
1. **Fresh-defaults check (SC4):** In Settings, confirm the "Download Progress" card shows its toggle
   OFF by default.
2. **Toggle ON + basic flow (SC1/SC2):** Toggle "Download Progress" ON. Start a real download of a
   reasonably large file (so the temp file persists a few seconds) in Chrome or Edge. Confirm: within
   a couple seconds, the collapsed island shows the spinner + "Downloading…" wing. Let it finish.
   Confirm the HUD switches to a green checkmark + the real final filename for ~3 seconds, then clears
   on its own with no further interaction.
3. **Firefox variant:** Repeat step 2 with Firefox if available (`.part` suffix) — confirm identical
   behavior.
4. **Safari variant (Pitfall 1 sanity check):** Repeat step 2 with Safari if available (`.download`
   bundle) — confirm the HUD appears/completes correctly and does NOT flicker or re-trigger repeatedly
   during the download (would indicate the top-level-path filtering is broken and the bundle's own
   internal writes are leaking through). If Safari isn't available on the test machine, note as
   untested and defer.
5. **Two rapid downloads (SC3, D-05/D-06):** Start file A, then start file B a few seconds later while
   A is still downloading. Confirm only ONE "Downloading…" HUD shows throughout (no duplicate/flicker).
   Let both finish. Confirm EACH file eventually gets its own "done" splash with its own correct
   filename — neither is silently dropped, even if one queues behind the other.
6. **Cancelled download (D-15):** Start a download, then cancel it from the browser's own download UI
   (or otherwise cause the temp file to be deleted without ever being renamed) before it completes.
   Confirm the "Downloading…" HUD simply disappears with NO done state, no error indicator.
7. **Manual-expand cuts a done splash short (D-04):** Let a download complete and its "done" splash
   start showing, then immediately click the island to expand it. Confirm the done splash is cut short
   immediately, matching the Charging/Device precedent.
8. **Collapsed-only gating (D-03):** Expand the island (any tab) while a download is actively in
   progress. Confirm the download HUD does NOT interrupt the expanded view.
9. **Priority (D-01, rank 5):** While a Charging or Device splash is actively showing, start a
   download. Confirm the standing Charging/Device splash is NOT interrupted — the download HUD queues
   behind it instead.
10. **Settings toggle gating (SC4):** Toggle "Download Progress" OFF in Settings, then start a real
    download. Confirm NO indicator appears at all.
11. **Regression check:** Confirm Charging, Device, Focus, OSD, Caps Lock, and Update HUDs still
    render/dismiss/preempt correctly, unaffected by Caps Lock/Update's rank shift to 6/7.
  </how-to-verify>
  <resume-signal>Type "approved" once all 11 steps pass, or describe which step failed and what was observed (especially step 4's Safari bundle-filtering result and step 5's concurrent-download identity result — both are the phase's highest-novelty, on-device-only-verifiable behaviors).</resume-signal>
  <done>All 11 on-device checklist steps confirmed; all 4 ROADMAP Phase 61 Success Criteria (DL-01, DL-02) closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| On-device verification vs. automated test coverage | Real FSEvents timing/correlation behavior (rename-pair pairing across a real browser's actual temp-file lifecycle, Safari bundle filtering) cannot be asserted via XCTest — this plan's checkpoint is the sole verification path, mirroring this codebase's own established precedent (CapsLockMonitor's Accessibility-gate verification, Phase 60's 60-05 gate). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-12 | Repudiation (of an unresolved research confidence flag) | RESEARCH.md Assumption A3 (FSEvents file-ID correlation reliability) | mitigate | Checkpoint steps 4/5 explicitly exercise the Safari-bundle-filtering and concurrent-download-identity paths RESEARCH.md flagged as MEDIUM confidence, recording an empirical pass/fail rather than leaving it assumed. |
| T-61-13 | Tampering (npm/pip/cargo) | N/A | accept | Zero new external packages this phase — Package Legitimacy Audit gate not applicable |
</threat_model>

<verification>
Task 1's automated build/test gate must be green before Task 2's on-device checkpoint is attempted.
Task 2 is a `gate="blocking"` checkpoint — do not auto-advance past it.
</verification>

<success_criteria>
- Debug + Release builds green; scoped DownloadActivityTests/DownloadCoordinatorTests/IslandResolverTests
  suite green.
- All 11 on-device checklist steps confirmed, closing all 4 ROADMAP Phase 61 Success Criteria
  (DL-01, DL-02).
</success_criteria>

<output>
Create `.planning/phases/61-download-progress/61-05-SUMMARY.md` when done
</output>
