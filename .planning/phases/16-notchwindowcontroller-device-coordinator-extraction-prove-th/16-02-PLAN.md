---
phase: 16-notchwindowcontroller-device-coordinator-extraction-prove-th
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - Islet/Notch/NotchWindowController.swift
  - .planning/phases/16-notchwindowcontroller-device-coordinator-extraction-prove-th/16-HUMAN-UAT.md
autonomous: false

requirements: [D-01, D-03]

must_haves:
  truths:
    - "NotchWindowController no longer declares any of the 9 device-splash fields or the 3 extracted methods — all device-splash state lives exclusively in DeviceCoordinator"
    - "BluetoothMonitor's own lifecycle (construction, start/stop, deinit teardown) is UNCHANGED and still owned directly by NotchWindowController (D-01)"
    - "Every one of the 6 rewired call sites preserves identical ordering/behavior to the pre-extraction code, including flushTransients(.device)'s unconditional pending-poll clear (Pitfall 12) and handleSettingsChanged's deviceLastShown-only reset asymmetry"
    - "A real Bluetooth device exercises all four D-03 scenarios (reconnect-flap debounce, launch-grace suppression, genuine disconnect, battery-poll promotion) on-device and all four pass, recorded in 16-HUMAN-UAT.md"
    - "The existing ~20-file IsletTests suite plus the new DeviceCoordinatorTests all stay green after the wiring change"
  artifacts:
    - path: "Islet/Notch/NotchWindowController.swift"
      provides: "controller wired to deviceCoordinator: DeviceCoordinator instead of inline device-splash bookkeeping"
    - path: ".planning/phases/16-notchwindowcontroller-device-coordinator-extraction-prove-th/16-HUMAN-UAT.md"
      provides: "D-03's mandatory on-device Bluetooth verification checklist and its recorded pass/fail results"
  key_links:
    - from: "Islet/Notch/NotchWindowController.swift"
      to: "Islet/Notch/DeviceCoordinator.swift"
      via: "BluetoothMonitor's onReading closure forwards to deviceCoordinator.handle(reading), not a private handleDevice method"
      pattern: "deviceCoordinator\\.handle\\("
    - from: "Islet/Notch/NotchWindowController.swift"
      to: "Islet/Notch/DeviceCoordinator.swift"
      via: "deinit calls deviceCoordinator.cancelPendingWork() alongside the unchanged bluetoothMonitor?.stop()"
      pattern: "deviceCoordinator\\.cancelPendingWork\\("
---

<objective>
Wire `NotchWindowController` to the `DeviceCoordinator`/`ActivityCoordinator` types created in
Plan 16-01: delete the 9 now-redundant fields and 3 now-redundant methods from the controller,
construct a single non-optional `deviceCoordinator` with the reach-back closures, and rewire
all 6 call sites identified in 16-RESEARCH.md's "Call Sites" table. Then produce and execute
the D-03-mandated on-device Bluetooth verification checklist — this is the phase's hard gate,
not a nice-to-have.

Purpose: Complete the zero-product-behavior-change extraction and prove, on real hardware,
that none of the 12 documented races regressed during the structural move.
Output: `NotchWindowController.swift` with the device-splash bookkeeping removed and
delegated; `16-HUMAN-UAT.md` recording the D-03 checklist results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-notchwindowcontroller-device-coordinator-extraction-prove-th/16-CONTEXT.md
@.planning/phases/16-notchwindowcontroller-device-coordinator-extraction-prove-th/16-RESEARCH.md
@.planning/phases/16-notchwindowcontroller-device-coordinator-extraction-prove-th/16-01-SUMMARY.md

<interfaces>
<!-- DeviceCoordinator's public surface from Plan 16-01 — this plan calls these exactly. -->
- `DeviceCoordinator(queueHead: () -> ActiveTransient?, enqueue: (ActiveTransient) -> Bool, updateHead: (ActiveTransient) -> Void, presentTransientChange: () -> Void, renderPresentation: () -> Void, batteryForAddress: (String) -> Int?)`
- `deviceCoordinator.handle(_ reading: DeviceReading)` — the new sole call target for `BluetoothMonitor`'s `onReading` closure
- `deviceCoordinator.activityPromoted()` — replaces both call sites of the old `triggerDeviceBatteryRefreshIfPromoted()`
- `deviceCoordinator.started(at: Date())` — replaces `startBluetoothMonitor()`'s inline `connectedDeviceAddresses.removeAll(); bluetoothStartedAt = Date()`
- `deviceCoordinator.reset()` — replaces `handleSettingsChanged`'s inline `deviceLastShown.removeAll()`
- `deviceCoordinator.clearPendingBatteryPolls()` — replaces `flushTransients(.device)`'s inline `pendingDeviceBatteryPolls.removeAll()`
- `deviceCoordinator.cancelPendingWork()` (nonisolated) — replaces `deinit`'s inline `deviceBatteryWork?.cancel()`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire NotchWindowController to DeviceCoordinator and delete the extracted fields/methods</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift:107-154 (the 9 fields to DELETE)
    Islet/Notch/NotchWindowController.swift:413-422 (`startBluetoothMonitor()` — construction call site to rewire)
    Islet/Notch/NotchWindowController.swift:859-976 (`handleDevice`/`triggerDeviceBatteryRefreshIfPromoted`/`scheduleDeviceBatteryRefresh` — the 3 methods to DELETE, now living in DeviceCoordinator)
    Islet/Notch/NotchWindowController.swift:1001-1041 (`handleSettingsChanged` — the devices toggle-off block at 1012-1018 to rewire; the charging/nowPlaying blocks in the SAME method must stay untouched)
    Islet/Notch/NotchWindowController.swift:1059-1078 (`flushTransients(_:)` — the `.device` case's `pendingDeviceBatteryPolls.removeAll()` at line 1071 to rewire; the unconditional-before-the-oldHead-guard ordering, Pitfall 12, must be preserved exactly)
    Islet/Notch/NotchWindowController.swift:811-836 (`scheduleActivityDismiss` — the `self.triggerDeviceBatteryRefreshIfPromoted()` call at line 830 to rewire)
    Islet/Notch/NotchWindowController.swift:1197-1241 (`deinit` — the `deviceBatteryWork?.cancel()` at line 1220 to rewire; `bluetoothMonitor?.stop()` at line 1219 stays UNCHANGED per D-01)
    Islet/Notch/DeviceCoordinator.swift (the new type from Plan 16-01 — read its actual final
    field/method names before wiring, in case Plan 16-01's executor deviated from the plan)
    Islet/Notch/ActivityCoordinator.swift (confirm the exact protocol method names)
  </read_first>
  <action>
    Delete the 9 fields at `NotchWindowController.swift:107-154`
    (`deviceLastShown`, `deviceSuppressedAtLaunch`, `deviceDebounce`, `connectedDeviceAddresses`,
    `bluetoothStartedAt`, `deviceLaunchGrace`, `deviceBatteryWork`, `pollingAddress`,
    `pendingDeviceBatteryPolls`). In their place, add a single `private lazy var deviceCoordinator:
    DeviceCoordinator` initialized with `[weak self]`-capturing closures matching the
    `<interfaces>` block's signature exactly: `queueHead: { [weak self] in self?.transientQueue.head }`,
    `enqueue: { [weak self] t in self?.transientQueue.enqueue(t) ?? false }`, `updateHead: {
    [weak self] t in self?.transientQueue.updateHead(t) }`, `presentTransientChange: { [weak self]
    in self?.presentTransientChange() }`, `renderPresentation: { [weak self] in
    self?.renderPresentation() }`, `batteryForAddress: { [weak self] addr in
    self?.bluetoothMonitor?.battery(forAddress: addr) }`. `deviceCoordinator` is NOT optional (it
    has no toggle-driven lifecycle — its bookkeeping simply sits idle when the Devices toggle is
    off, exactly as the old fields did). Delete `handleDevice(_:)`,
    `triggerDeviceBatteryRefreshIfPromoted()`, and `scheduleDeviceBatteryRefresh(address:attempt:)`
    in their entirety (lines 859-976) — this logic now lives in `DeviceCoordinator`. Rewire the 6
    call sites: (1) line 417-418's `connectedDeviceAddresses.removeAll(); bluetoothStartedAt =
    Date()` becomes `deviceCoordinator.started(at: Date())`; (2) line 419's `BluetoothMonitor {
    [weak self] reading in self?.handleDevice(reading) }` becomes `BluetoothMonitor { [weak self]
    reading in self?.deviceCoordinator.handle(reading) }`; (3) line 830's
    `self.triggerDeviceBatteryRefreshIfPromoted()` becomes `self.deviceCoordinator.activityPromoted()`;
    (4) line 1016's `deviceLastShown.removeAll()` becomes `deviceCoordinator.reset()` (this line
    runs alongside `bluetoothMonitor?.stop(); bluetoothMonitor = nil` and `flushTransients(.device)`
    — keep the same statement order); (5) line 1071's `pendingDeviceBatteryPolls.removeAll()`
    becomes `deviceCoordinator.clearPendingBatteryPolls()`, staying on the same line BEFORE the
    `guard transientQueue.head != oldHead else { return }` check at line 1073 (Pitfall 12 — do
    NOT move it after that guard); (6) line 1076's `self.triggerDeviceBatteryRefreshIfPromoted()`
    (inside `flushTransients`) becomes `self.deviceCoordinator.activityPromoted()`; (7) line 1220's
    `deviceBatteryWork?.cancel()` becomes `deviceCoordinator.cancelPendingWork()`, staying
    immediately after the unchanged `bluetoothMonitor?.stop()` at line 1219.
  </action>
  <acceptance_criteria>
    - `grep -c "deviceLastShown\|deviceSuppressedAtLaunch\|connectedDeviceAddresses\|bluetoothStartedAt\|deviceLaunchGrace\|deviceBatteryWork\|pollingAddress\|pendingDeviceBatteryPolls" Islet/Notch/NotchWindowController.swift` returns 0 (all 8 non-`deviceDebounce` fields fully removed from the controller — `deviceDebounce` too, verified by the next check)
    - `grep -c "deviceDebounce" Islet/Notch/NotchWindowController.swift` returns 0
    - `grep -c "func handleDevice\|func triggerDeviceBatteryRefreshIfPromoted\|func scheduleDeviceBatteryRefresh" Islet/Notch/NotchWindowController.swift` returns 0
    - `grep -c "private lazy var deviceCoordinator: DeviceCoordinator" Islet/Notch/NotchWindowController.swift` returns 1
    - `grep -c "deviceCoordinator.handle(" Islet/Notch/NotchWindowController.swift` returns 1 (the `BluetoothMonitor` onReading closure)
    - `grep -c "deviceCoordinator.activityPromoted()" Islet/Notch/NotchWindowController.swift` returns 2 (scheduleActivityDismiss + flushTransients)
    - `grep -c "deviceCoordinator.started(at:" Islet/Notch/NotchWindowController.swift` returns 1
    - `grep -c "deviceCoordinator.reset()" Islet/Notch/NotchWindowController.swift` returns 1
    - `grep -c "deviceCoordinator.clearPendingBatteryPolls()" Islet/Notch/NotchWindowController.swift` returns 1
    - `grep -c "deviceCoordinator.cancelPendingWork()" Islet/Notch/NotchWindowController.swift` returns 1
    - `grep -n "bluetoothMonitor?.stop()" Islet/Notch/NotchWindowController.swift` still present, immediately followed within the deinit body by the `deviceCoordinator.cancelPendingWork()` line (D-01 — BluetoothMonitor lifecycle unchanged)
    - `grep -n "deviceCoordinator.clearPendingBatteryPolls()" Islet/Notch/NotchWindowController.swift` appears BEFORE the line matching `guard transientQueue.head != oldHead` in `flushTransients` (Pitfall 12 ordering — verify with `grep -n` line numbers on both)
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -scheme Islet -configuration Debug</automated>
  </verify>
  <done>NotchWindowController.swift compiles with zero errors/warnings, contains none of the 9 extracted fields or 3 extracted methods, and all 6 call sites route through deviceCoordinator.</done>
</task>

<task type="auto">
  <name>Task 2: Create the D-03 on-device Bluetooth verification checklist</name>
  <files>.planning/phases/16-notchwindowcontroller-device-coordinator-extraction-prove-th/16-HUMAN-UAT.md</files>
  <read_first>
    .planning/phases/06-priority-resolver-settings-v1-ship/06-HUMAN-UAT.md (the exact
    frontmatter + `## Current Test` / `## Tests` / `## Summary` / `## Gaps` structure to mirror)
    .planning/phases/16-notchwindowcontroller-device-coordinator-extraction-prove-th/16-VALIDATION.md
    (the "Manual-Only Verifications" table — the 4 scenarios' exact wording to reuse)
  </read_first>
  <action>
    Create `16-HUMAN-UAT.md` with frontmatter `status: pending`, `phase:
    16-notchwindowcontroller-device-coordinator-extraction-prove-th`, `source: [16-CONTEXT.md]`,
    `started:` (current UTC timestamp), `updated:` (same). Under `## Current Test` write
    `[testing in progress]`. Under `## Tests` create exactly 4 numbered entries mirroring
    06-HUMAN-UAT.md's `### N. <title>` / `expected:` / `result:` shape, `result: pending` for
    all 4 initially: (1) "Reconnect-flap debounce" — expected: connect a real Bluetooth device,
    then trigger a second connection event for the SAME address within ~3s (e.g. toggle Bluetooth
    off/on quickly on the peripheral) — confirm only ONE splash fires, not two; (2) "Launch-grace
    suppression" — expected: have a device already connected BEFORE launching Islet — confirm no
    splash fires at launch, but a LATER genuine disconnect of that same device still fires a
    disconnect splash; (3) "Genuine disconnect edge" — expected: disconnect a connected real
    device — confirm a disconnect splash fires exactly once; (4) "Battery-poll promotion" —
    expected: connect device A, then connect device B while A's splash still stands (B enqueues
    behind A) — after A's splash dismisses/advances and B is promoted to head, confirm B's
    deferred battery percentage still appears (not blank, not A's stale value). Under `##
    Summary` write `total: 4`, `passed: 0`, `issues: 0`, `pending: 4`, `skipped: 0`, `blocked: 0`.
    Under `## Gaps` leave empty (populated only if a scenario fails).
  </action>
  <acceptance_criteria>
    - File exists at the exact path with frontmatter containing `status: pending`
    - `grep -c "^### " .planning/phases/16-notchwindowcontroller-device-coordinator-extraction-prove-th/16-HUMAN-UAT.md` returns 4
    - `grep -c "Reconnect-flap debounce\|Launch-grace suppression\|Genuine disconnect edge\|Battery-poll promotion" .planning/phases/16-notchwindowcontroller-device-coordinator-extraction-prove-th/16-HUMAN-UAT.md` returns 4
    - `grep -c "result: pending" .planning/phases/16-notchwindowcontroller-device-coordinator-extraction-prove-th/16-HUMAN-UAT.md` returns 4
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/16-notchwindowcontroller-device-coordinator-extraction-prove-th/16-HUMAN-UAT.md && grep -c "^### " .planning/phases/16-notchwindowcontroller-device-coordinator-extraction-prove-th/16-HUMAN-UAT.md | grep -q "^4$"</automated>
  </verify>
  <done>16-HUMAN-UAT.md exists with all 4 D-03 scenarios listed at status/result: pending, ready for Task 3's on-device execution.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Execute the D-03 on-device Bluetooth checklist and the full regression suite</name>
  <files>.planning/phases/16-notchwindowcontroller-device-coordinator-extraction-prove-th/16-HUMAN-UAT.md</files>
  <action>
    No code change in this task — build on real hardware, run the full test suite via Xcode's
    Cmd-U, then manually exercise the 4 on-device Bluetooth scenarios recorded in
    `16-HUMAN-UAT.md` (created by Task 2) and record each `result:` (pass/issue) plus the
    frontmatter `status:` and `## Summary` counts. See `<what-built>`/`<how-to-verify>` below for
    the exact steps.
  </action>
  <what-built>
    The `DeviceCoordinator` extraction is code-complete: `NotchWindowController` now delegates
    all device-splash bookkeeping to `deviceCoordinator`, `BluetoothMonitor`'s own lifecycle is
    untouched (D-01), and `xcodebuild build` compiles clean. This checkpoint is the phase's hard
    gate (D-03) — a pure structural move can still silently break timing-sensitive edge cases
    that no unit test can exercise (real IOBluetooth callbacks arrive off-main).
  </what-built>
  <how-to-verify>
    1. Open `Islet.xcodeproj` in Xcode, select the `Islet` scheme, press Cmd-U. Confirm ALL
       tests pass — the existing ~20-file `IsletTests` suite plus the new `DeviceCoordinatorTests`
       from Plan 16-01 (zero failures).
    2. Build and run `Islet.app` (Cmd-R), then work through all 4 scenarios recorded in
       `16-HUMAN-UAT.md`'s `## Tests` section with a real Bluetooth device (e.g. AirPods or any
       paired headphones): reconnect-flap debounce, launch-grace suppression, genuine disconnect,
       battery-poll promotion (connect a second device while the first's splash still stands).
    3. For each of the 4 scenarios, update its `result:` line in `16-HUMAN-UAT.md` from `pending`
       to `pass` or `issue` (with a one-line note if `issue`), update the frontmatter `status:` to
       `complete` once all 4 are resolved, and update the `## Summary` counts to match.
  </how-to-verify>
  <verify>
    <manual>Cmd-U shows zero failures; all 4 entries in 16-HUMAN-UAT.md read result: pass; frontmatter status: complete</manual>
  </verify>
  <done>16-HUMAN-UAT.md status: complete with all 4 D-03 scenarios recorded result: pass, and Cmd-U's full IsletTests suite is green.</done>
  <resume-signal>
    Reply "approved" once Cmd-U is green and all 4 `16-HUMAN-UAT.md` scenarios are recorded as
    `pass` (or describe which scenario failed and how, if any `issue` was recorded).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| IOBluetooth device → `BluetoothMonitor` → `deviceCoordinator.handle(_:)` | Unchanged from Plan 16-01 — the untrusted `reading.name` now crosses from `BluetoothMonitor`'s off-main callback (hopped to main) into the coordinator via the controller's forwarding closure instead of the old private `handleDevice` method. No new boundary introduced, only the internal call target changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-16-04 (carries T-05-01) | Tampering | `BluetoothMonitor`'s `onReading` closure → `deviceCoordinator.handle(reading)` | mitigate (already mitigated, do not regress) | The forwarding closure passes the same untrusted `DeviceReading` value unchanged — Task 1's acceptance criteria confirm the closure is a one-line forward (`self?.deviceCoordinator.handle(reading)`), no new string handling introduced at the call site. |
| T-16-05 (carries T-06-09) | Denial of Service | `flushTransients(.device)` / `handleSettingsChanged`'s devices-off branch | mitigate (already mitigated, do not regress) | Task 1's acceptance criteria pin the exact ordering of `deviceCoordinator.clearPendingBatteryPolls()` relative to the `oldHead` guard (Pitfall 12) and `deviceCoordinator.reset()`'s asymmetric scope (Pitfall: only `deviceLastShown`) — a toggle-off/on cycle cannot silently widen the flapping-device queue bound. |
| T-16-06 (carries T-06-12) | — (resource exhaustion) | `NotchWindowController.deinit` → `deviceCoordinator.cancelPendingWork()` | mitigate | Task 1's acceptance criteria confirm this call is adjacent to the unchanged `bluetoothMonitor?.stop()` in the SAME `nonisolated deinit`, so the one-shot battery-poll `DispatchWorkItem` is torn down synchronously at app-quit exactly as it was pre-extraction. |
</threat_model>

<verification>
- `xcodebuild build -scheme Islet -configuration Debug` succeeds after Task 1 (compile gate).
- Manual Cmd-U in Xcode: full `IsletTests` suite (20+ existing files + `DeviceCoordinatorTests`) green — Task 3's checkpoint.
- On-device: all 4 of D-03's mandatory Bluetooth scenarios pass, recorded in `16-HUMAN-UAT.md` — Task 3's checkpoint, the phase's hard gate per CONTEXT.md (not a nice-to-have).
</verification>

<success_criteria>
- `NotchWindowController.swift` contains zero references to the 9 extracted fields or the 3 extracted methods; all 6 call sites route through `deviceCoordinator`.
- `BluetoothMonitor`'s own construction/start/stop/deinit ownership is byte-for-byte unchanged (D-01).
- `16-HUMAN-UAT.md` exists with all 4 D-03 scenarios recorded `pass` and `status: complete`.
- Full `IsletTests` suite (existing + new) is green via manual Cmd-U.
- `xcodebuild build -scheme Islet -configuration Debug` is green.
</success_criteria>

<output>
Create `.planning/phases/16-notchwindowcontroller-device-coordinator-extraction-prove-th/16-02-SUMMARY.md` when done
</output>
