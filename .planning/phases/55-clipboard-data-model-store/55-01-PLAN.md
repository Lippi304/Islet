---
phase: 55-clipboard-data-model-store
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Clipboard/ClipboardItem.swift
  - Islet/Clipboard/ClipboardStore.swift
  - IsletTests/ClipboardStoreTests.swift
autonomous: true
requirements: []

must_haves:
  truths:
    - "ClipboardItem and ClipboardStore compile and are usable with zero AppKit/SwiftUI/NSPasteboard/Cocoa imports (only Foundation)"
    - "ClipboardItem's text/image content is represented by ONE associated-value Kind enum (case text(String), case image(Data)) — a constructed value can never be both text and image, or neither, at once"
    - "Appending a 31st item evicts the oldest entry (FIFO) so the store never exceeds a 30-item cap — proven for a run of genuinely-distinct items (SC-2)"
    - "Re-appending an item whose text exactly matches an existing item's text moves that EXISTING entry to the newest position with a refreshed timestamp — it does not create a second entry and does not silently no-op (D-02, the deliberate departure from ShelfLogic's dedupe-as-no-op behavior)"
    - "Re-appending an item whose image Data is byte-identical to an existing item's image Data moves that EXISTING entry to the newest position with a refreshed timestamp, same contract as the text case (D-02 applies to both Kind cases)"
    - "clear() removes every item in the store in one call — items.isEmpty is true immediately after, provable by construction (SC-3)"
    - "No file in Islet/Clipboard/ references IslandResolver, TransientQueue, or NotchWindowController by name — the store is its own independent axis before any monitor or menu exists (SC-4)"
    - "ClipboardStore.append accepts content of any size unconditionally — no size cap, truncation, or validation on individual item content (D-03, user's explicit choice; revisit only if it becomes a real problem in practice)"
  artifacts:
    - path: "Islet/Clipboard/ClipboardItem.swift"
      provides: "ClipboardItem pure value type (id, kind: Kind, timestamp) with associated-value Kind enum (text/image)"
      exports: ["ClipboardItem"]
      min_lines: 12
    - path: "Islet/Clipboard/ClipboardStore.swift"
      provides: "ClipboardStore pure reducer: append (D-02 dedupe-and-move-to-top, D-01 cap+FIFO evict), clear"
      exports: ["ClipboardStore"]
      min_lines: 15
    - path: "IsletTests/ClipboardStoreTests.swift"
      provides: "Unit coverage for evict-at-cap, text dedupe-move-to-top, image dedupe-move-to-top, and clear"
  key_links:
    - from: "Islet/Clipboard/ClipboardStore.swift"
      to: "ClipboardItem.Kind equality check"
      via: "dedupe guard inside append(_:) comparing $0.kind == item.kind"
      pattern: "\\.kind == item\\.kind"
    - from: "Islet/Clipboard/ClipboardStore.swift"
      to: "cap + FIFO eviction"
      via: "items.count > cap guard calling removeFirst() after a genuine new append"
      pattern: "items\\.count > cap"
---

<objective>
Build the clipboard's pure data model (`ClipboardItem`, its associated-value `Kind` enum) and
its append/evict-at-cap/dedupe/clear reducer (`ClipboardStore`) — isolated, Foundation-only,
unit-tested Swift. No AppKit, no `NSPasteboard`, no disk I/O, no encryption.

Purpose: Establish the append/evict-at-cap/clear lifecycle contract (mirroring `ShelfItem`/
`ShelfLogic`'s already-shipped shape from Phase 19, plus `TransientQueue`'s cap/FIFO mechanics
from `IslandResolver.swift`) before Phase 56 (encrypted persistence), Phase 57 (live pasteboard
monitor), and Phase 58 (menu wiring) touch it. D-02 is this phase's one deliberate departure
from the Shelf precedent: a duplicate moves the existing entry to the top with a refreshed
timestamp instead of silently no-opping — get this right here, since every downstream phase
inherits this contract unchanged.

Output: `Islet/Clipboard/ClipboardItem.swift`, `Islet/Clipboard/ClipboardStore.swift`, plus
`IsletTests/ClipboardStoreTests.swift` — all consumed for the first time by Phase 57's
`ClipboardMonitor` (feeds captured items in) and Phase 58's menu wiring (reads `items` out).
Neither exists yet; out of scope here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-clipboard-data-model-store/55-CONTEXT.md
@.planning/phases/55-clipboard-data-model-store/55-RESEARCH.md
@.planning/phases/55-clipboard-data-model-store/55-PATTERNS.md

# Direct analogs to mirror exactly (read before writing any code)
@Islet/Shelf/ShelfItem.swift
@Islet/Shelf/ShelfLogic.swift
@Islet/Notch/IslandResolver.swift
@IsletTests/ShelfLogicTests.swift
</context>

<interfaces>
<!-- Extracted from RESEARCH.md/PATTERNS.md's already-completed pattern analysis. Executor
should use these shapes directly — no further codebase exploration needed. -->

From `Islet/Shelf/ShelfItem.swift` (full file — the header-comment convention and
Foundation-only struct shape `ClipboardItem` must mirror, minus the Kind split it doesn't have):
```swift
import Foundation

// Phase 19 / SHELF-08 — the PURE shelf item value. Like DeviceReading and
// PowerActivity, this is a plain Foundation-only struct — no AppKit, no
// Cocoa file APIs — so ShelfLogic's append/remove/dedupe rules are
// unit-tested in milliseconds.
struct ShelfItem: Equatable {
    let id: UUID
    let originalURL: URL
    var localURL: URL
    let filename: String
    let addedAt: Date
}
```

From `Islet/Notch/IslandResolver.swift:287-303` (`TransientQueue` — the cap/FIFO mechanics
`ClipboardStore` must mirror for D-01):
```swift
struct TransientQueue {
    private(set) var head: ActiveTransient?
    private var pending: [ActiveTransient] = []
    let maxDepth = 2

    mutating func enqueue(_ t: ActiveTransient) -> Bool {
        if head == nil { head = t; return true }
        if head == t || pending.contains(t) { return false }
        pending.append(t)
        if pending.count > maxDepth { pending.removeFirst() }
        return false
    }
}
```

From `Islet/Shelf/ShelfLogic.swift:10-39` (`ShelfLogic` — the pure-reducer shape to mirror,
EXCEPT its dedupe branch, which D-02 explicitly inverts for `ClipboardStore`):
```swift
struct ShelfLogic: Equatable {
    private(set) var items: [ShelfItem] = []

    @discardableResult
    mutating func append(_ item: ShelfItem) -> Bool {
        guard !items.contains(where: { $0.originalURL == item.originalURL }) else { return false }
        items.append(item)
        return true
    }

    @discardableResult
    mutating func clear() -> [ShelfItem] {
        let removed = items
        items.removeAll()
        return removed
    }
}
```
CRITICAL: do NOT port this dedupe branch's silent-no-op semantics. D-02 requires a duplicate to
move the EXISTING entry to the newest position with a refreshed timestamp — never a no-op,
never a second entry. This is the single most likely implementation mistake per RESEARCH.md
Pitfall 1.

From `IsletTests/ShelfLogicTests.swift` (test file shape to mirror — no shared fixture, no
setUp/tearDown, one `func test...()` per behavior, fresh instance constructed inline):
```swift
import XCTest
@testable import Islet

final class ShelfLogicTests: XCTestCase {
    func testAppendAddsToEndInDropOrder() {
        var logic = ShelfLogic()
        // ... construct value, call mutating method, assert return + resulting state ...
    }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ClipboardItem — pure value type with associated-value Kind enum</name>
  <files>Islet/Clipboard/ClipboardItem.swift</files>
  <read_first>
    Islet/Shelf/ShelfItem.swift (header-comment convention + Foundation-only struct shape to mirror)
    .planning/phases/55-clipboard-data-model-store/55-RESEARCH.md (Pattern 1 — the associated-value
    Kind enum shape, and Pitfall 2 — why two optional properties instead of one enum is wrong)
    .planning/phases/55-clipboard-data-model-store/55-CONTEXT.md (Claude's Discretion section —
    the Kind enum's internal shape is this task's one open design call)
  </read_first>
  <action>
    Create `Islet/Clipboard/ClipboardItem.swift`: `import Foundation` only — no AppKit, no
    SwiftUI, no Cocoa. Header comment in the style of `ShelfItem.swift` lines 3-7 — cite "Phase
    55" and state this establishes the append/evict-at-cap/clear contract before Phase 56
    (persistence), Phase 57 (monitor), Phase 58 (menu wiring) exist; note this is the first
    associated-value enum in this codebase (`QuickAddKind`/`OSDKeyKind`/`PermissionKind` are all
    no-payload) so the shape below is standard Swift, not a codebase-specific pattern.

    Define `struct ClipboardItem: Equatable, Codable` with exactly three fields, in this order:
    `let id: UUID`, `var kind: Kind`, `var timestamp: Date`. Nest `enum Kind: Equatable, Codable`
    inside `ClipboardItem` with exactly two cases: `case text(String)`, `case image(Data)` — no
    other cases, no raw values, no manual `Equatable`/`Codable` conformance (both synthesize
    automatically since `String` and `Data` both conform). Add `Codable` to `ClipboardItem`
    itself even though this phase's own success criteria never encode/decode anything — Phase
    56 needs it and retrofitting later would require a type change (RESEARCH.md Pitfall 3).

    No custom `init` beyond the memberwise one; no methods on this type — it is a passive value,
    same role as `ShelfItem`.

    Run `xcodegen generate` from the repo root so `Islet.xcodeproj` picks up the new file
    (traditional group-based project — new files are not auto-discovered without regeneration).
  </action>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^import AppKit\|^import SwiftUI\|^import Cocoa" Islet/Clipboard/ClipboardItem.swift` returns 0
    - `grep -q "struct ClipboardItem: Equatable, Codable" Islet/Clipboard/ClipboardItem.swift` matches
    - `grep -q "enum Kind: Equatable, Codable" Islet/Clipboard/ClipboardItem.swift` matches
    - `grep -q "case text(String)" Islet/Clipboard/ClipboardItem.swift` matches
    - `grep -q "case image(Data)" Islet/Clipboard/ClipboardItem.swift` matches
    - `grep -c "^    case " Islet/Clipboard/ClipboardItem.swift` returns exactly 2 (no third Kind case)
    - `xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug` reports `BUILD SUCCEEDED`
  </acceptance_criteria>
  <done>ClipboardItem.swift exists, Foundation-only, with id/kind/timestamp fields and a nested Kind enum with exactly text(String)/image(Data) cases making an invalid "both" or "neither" state unrepresentable; BUILD SUCCEEDED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ClipboardStore — append (D-02 dedupe-and-move-to-top, D-01 cap+FIFO evict), clear</name>
  <files>Islet/Clipboard/ClipboardStore.swift, IsletTests/ClipboardStoreTests.swift</files>
  <read_first>
    Islet/Clipboard/ClipboardItem.swift (the version written in Task 1 — this reducer operates
    directly on it)
    Islet/Shelf/ShelfLogic.swift (the pure-reducer shape to mirror — private(set) items array,
    Equatable struct — EXCEPT its dedupe branch, which this task must invert per D-02)
    Islet/Notch/IslandResolver.swift (TransientQueue, lines ~287-303 — the `let maxDepth`/
    `removeFirst()` cap-eviction shape to adapt as `let cap = 30`)
    IsletTests/ShelfLogicTests.swift (test file shape — one XCTestCase, fresh instance per test,
    no setUp/tearDown, no shared fixture)
    .planning/phases/55-clipboard-data-model-store/55-CONTEXT.md (D-01: cap=30, plain inline
    let, FIFO evict; D-02: duplicate moves EXISTING entry to top with refreshed timestamp —
    never a no-op, never a second entry; D-03: no size validation, accept any content)
    .planning/phases/55-clipboard-data-model-store/55-RESEARCH.md (Pitfall 1 — the exact mistake
    to avoid: copying ShelfLogic's silent-no-op dedupe verbatim instead of D-02's move-to-top)
  </read_first>
  <behavior>
    - Test: appending 31 items with genuinely distinct text content (e.g. "item-0" through
      "item-30") leaves `store.items.count == 30`, and the item whose text was "item-0" (the
      first/oldest append) is no longer present anywhere in `items` (D-01: cap=30, FIFO evict
      oldest past cap)
    - Test: appending an item whose text exactly matches an already-present item's text does
      NOT change `store.items.count`, and the matching entry ends up at the newest position
      (last, per the chosen append-at-end/evict-via-removeFirst direction) with its `timestamp`
      equal to the newly-appended item's (fresh) timestamp — not the original entry's stale one
      (D-02, text case)
    - Test: appending an item whose image `Data` is byte-identical to an already-present item's
      image `Data` produces the same move-to-top-with-refreshed-timestamp contract as the text
      case above — proves D-02 applies uniformly across both `Kind` cases, not just text
    - Test: `clear()` leaves `store.items.isEmpty == true` after appending at least 2 items
      first (SC-3: provably empty by construction)
  </behavior>
  <action>
    Create `Islet/Clipboard/ClipboardStore.swift`: `import Foundation` only. Header comment
    citing "Phase 55" — state this mirrors `ShelfLogic`'s pure `mutating func` reducer shape and
    `TransientQueue`'s cap/FIFO mechanics, but explicitly departs from `ShelfLogic.append`'s
    dedupe behavior per D-02 (a duplicate moves the existing entry to the newest position with a
    refreshed timestamp, it never no-ops and never creates a second entry) — flag this as the
    one deliberate divergence from the Shelf precedent, matching RESEARCH.md's own framing.

    Define `struct ClipboardStore: Equatable` with `private(set) var items: [ClipboardItem] =
    []` and `let cap = 30` (D-01: plain inline instance constant, mirrors
    `TransientQueue.maxDepth` — NOT a shared/global constants file, NOT configurable).

    Implement `mutating func append(_ item: ClipboardItem)`: first, `if let index =
    items.firstIndex(where: { $0.kind == item.kind })` — a match means D-02's duplicate case:
    `items.remove(at: index)`, then `items.append(item)` (reinserts at the newest end carrying
    `item`'s own fresh `timestamp`), then `return`. If no match, this is a genuinely new item:
    `items.append(item)`, then `if items.count > cap { items.removeFirst() }` (D-01 FIFO evict).
    No throwing, no validation on content size (D-03 — accept unconditionally).

    Implement `mutating func clear()`: `items.removeAll()`. No return value needed (unlike
    `ShelfLogic.clear()`'s `-> [ShelfItem]` — this phase's success criteria only require
    provable emptiness, not a removed-items list).

    Create `IsletTests/ClipboardStoreTests.swift`: `import XCTest` + `@testable import Islet`.
    Header comment in the style of `ShelfLogicTests.swift`'s opening lines — cite "Phase 55",
    state this covers append/evict-at-cap/D-02-dedupe/clear, one test per behavior, fresh `var
    store = ClipboardStore()` per test, no setUp/tearDown, no shared fixture, no mocking
    framework. `final class ClipboardStoreTests: XCTestCase` implementing the 4 behaviors listed
    above as 4 separate `func test...()` methods. Use `Date(timeIntervalSinceReferenceDate: N)`
    for deterministic, distinguishable timestamps in the dedupe tests (mirrors
    `ShelfLogicTests.swift`'s pattern for time-based assertions) so the refreshed-vs-stale
    timestamp comparison is unambiguous. For the image dedupe test, construct two distinct
    `Data` fixtures via `Data([0x01, 0x02, 0x03])`-style literals so byte-identity is explicit
    and readable.

    After creating both files, run `xcodegen generate` from the repo root so `Islet.xcodeproj`
    picks up the new files.
  </action>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "struct ClipboardStore: Equatable" Islet/Clipboard/ClipboardStore.swift` matches
    - `grep -q "private(set) var items: \[ClipboardItem\]" Islet/Clipboard/ClipboardStore.swift` matches
    - `grep -q "let cap = 30" Islet/Clipboard/ClipboardStore.swift` matches
    - `grep -q "items.count > cap" Islet/Clipboard/ClipboardStore.swift` matches
    - `grep -q "\$0.kind == item.kind" Islet/Clipboard/ClipboardStore.swift` matches (D-02 dedupe key)
    - `grep -c "^import AppKit\|^import SwiftUI\|^import Cocoa\|NSPasteboard\|FileManager" Islet/Clipboard/ClipboardStore.swift` returns 0
    - `grep -rL "IslandResolver\|TransientQueue\|NotchWindowController" Islet/Clipboard/ClipboardItem.swift Islet/Clipboard/ClipboardStore.swift | wc -l | tr -d ' '` equals `2` (SC-4: neither file references either type — both listed as clean by `grep -L`)
    - `grep -c "func test" IsletTests/ClipboardStoreTests.swift` returns 4 or more
    - `xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug` reports `BUILD SUCCEEDED`
    - Manual Cmd-U run in Xcode (headless `xcodebuild test` hangs in this repo — pre-existing
      Bluetooth TCC-authorization wait during the full-app test-host boot, documented in
      PROJECT.md; the build gate above is the automated proof, test EXECUTION is verified via
      Xcode's Test Navigator, Cmd-U on `ClipboardStoreTests`) shows all 4 tests passing
  </acceptance_criteria>
  <done>ClipboardStore.swift exists, Foundation-only, with append() implementing D-01's cap+FIFO evict and D-02's dedupe-and-move-to-top (uniformly for text and image Kind cases), and clear(); ClipboardStoreTests.swift covers all 4 behaviors; neither Clipboard file references IslandResolver/TransientQueue/NotchWindowController; BUILD SUCCEEDED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed in this phase | `ClipboardItem`/`ClipboardStore` are pure, synchronous, in-memory Foundation types with no network, no `NSPasteboard`, no `FileManager`, no Keychain, no external input of any kind — `append(_:)`'s only caller in this phase is a unit test constructing values inline. The one real external-input boundary this phase's types will eventually sit behind — a live copy from `NSPasteboard` — belongs to Phase 57's `ClipboardMonitor`, which does not exist yet and is explicitly out of scope here (per CONTEXT.md's `<domain>` boundary and RESEARCH.md's Architectural Responsibility Map). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | Information Disclosure | Future: whatever feeds `ClipboardStore.append(_:)` (Phase 57's `ClipboardMonitor`) | accept (deferred) | `ClipboardStore` has no awareness of where its input originates and cannot itself distinguish sensitive from non-sensitive content — excluding `org.nspasteboard.ConcealedType`/`TransientType` copies (PRIV-01) is explicitly Phase 57's responsibility, not this phase's. Flagged here so a plan-checker doesn't mistake its absence for an oversight; RESEARCH.md's Security Domain section makes the same deferral explicitly. |
| T-55-02 | Denial of Service (self-inflicted, low severity) | `ClipboardStore.append(_:)` — unbounded per-item content size | accept | CONTEXT.md D-03 explicitly accepts this: no size cap or truncation on individual items in Phase 55/56, by the user's own choice ("simplest logic... revisit only if this becomes a real problem in practice"). The 30-item cap (D-01) still bounds the item *count*; only per-item *size* is unbounded. Not designed around speculatively. |
| T-55-03 | Tampering (encryption at rest) | Future: `ClipboardFileStore` (Phase 56) | accept (out of scope) | `ClipboardStore` never touches disk in this phase — no plaintext-at-rest exposure exists yet to mitigate. PRIV-02 (CryptoKit AES-GCM encryption) is Phase 56's responsibility. |

No package-manager installs occur in this phase (zero new dependencies, Foundation-only) — the Package Legitimacy Gate does not apply; `T-{phase}-SC` is omitted accordingly.

</threat_model>

<verification>
1. `xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug` reports `BUILD SUCCEEDED` after both tasks.
2. Manual Cmd-U in Xcode (headless `xcodebuild test` hangs in this repo — PROJECT.md-documented Bluetooth TCC wait) runs `ClipboardStoreTests`, all green.
3. `grep -rn "AppKit\|SwiftUI\|Cocoa\|NSPasteboard\|FileManager" Islet/Clipboard/*.swift` returns no matches — confirms Foundation-only (ROADMAP SC-1/SC-4).
4. `grep -rn "IslandResolver\|TransientQueue\|NotchWindowController" Islet/Clipboard/*.swift` returns no matches — confirms the store is its own independent axis (ROADMAP SC-4).
5. `grep -c "case " Islet/Clipboard/ClipboardItem.swift` shows exactly the two `Kind` cases (text/image) — no partial-state possibility.
</verification>

<success_criteria>
- `ClipboardItem` and `ClipboardStore` exist as pure value types, fully covered by unit tests, zero AppKit/SwiftUI/NSPasteboard dependency (ROADMAP SC-1).
- Appending past the 30-item cap evicts the oldest entry (FIFO), proven by unit test, with no partial-state possibility (ROADMAP SC-2).
- `clear()` removes every item in a single call — provably empty by construction (ROADMAP SC-3).
- The store has zero references to `IslandResolver`/`TransientQueue`/`NotchWindowController` — structurally independent before any monitor or menu exists (ROADMAP SC-4).
- D-02's dedupe-and-move-to-top contract (the deliberate departure from `ShelfLogic`) is proven for both `Kind` cases (text and image).
- `xcodebuild build -scheme Islet` reports `BUILD SUCCEEDED`.
</success_criteria>

<output>
Create `.planning/phases/55-clipboard-data-model-store/55-01-SUMMARY.md` when done
</output>
