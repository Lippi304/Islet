---
phase: 03-charging-activity
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - Islet/Notch/NotchPillView.swift
autonomous: true
requirements: [CHG-01, CHG-02]
tags: [swiftui, wings, charging, layout, ui]

must_haves:
  truths:
    - "When a ChargingActivity is non-nil the island renders the sideways WINGS layout (status symbol left, filling battery glyph + numeric % right), NOT the Phase-2 downward expanded blob"
    - "The battery glyph is ONE consistent SF Symbol that changes by state: bolt while charging, plain/green at full, plain on battery (D-04)"
    - "The battery glyph fills proportionally to the percentage via Image(systemName:variableValue:) (D-03)"
    - "When the activity is non-nil it takes precedence over a user-expanded island (D-11), rendered as a one-line if-ordering in the view"
    - "The view itself drives NO animation/timer (D-08) — it only renders the published state; the DEBUG previews compile both wings + collapsed states"
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "Wings/Alcove sideways layout branch + ChargingActivityState observation + D-11 precedence ordering"
      contains: "battery.100percent.bolt"
  key_links:
    - from: "Islet/Notch/NotchPillView.swift"
      to: "Islet/Notch/ChargingActivityState.swift"
      via: "@ObservedObject var charging: ChargingActivityState"
      pattern: "ChargingActivityState"
    - from: "Islet/Notch/NotchPillView.swift"
      to: "Islet/Notch/PowerActivity.swift"
      via: "switch over ChargingActivity cases"
      pattern: "case .charging"
---

<objective>
Add the WINGS / Alcove sideways layout to `NotchPillView` (D-01/D-03/D-05) and wire the D-11
precedence ordering, observing the `ChargingActivityState` created in Plan 01. This is the visual
half of the charging splash: a flat, wide strip — status symbol on the LEFT, a filling battery
glyph + numeric % on the RIGHT — distinct from the Phase-2 downward `expandedIsland`. It also sets
the SKELETON Phase 4 (Now Playing) reuses: art/content left, controls/title right.

Purpose: keep the existing focus-safe panel, the matchedGeometryEffect morph, and the idle-static
discipline (D-08) intact while adding a new rendering branch driven purely by the published
`ChargingActivity?`. No IOKit and no timers in this plan — Plan 03 supplies the live power events
and the ~3s auto-dismiss; this plan only renders whatever activity is published.

Output: extended `NotchPillView.swift` with a `wings(for:)` branch, the `@ObservedObject charging`
input, the D-11 precedence `if`, and updated DEBUG previews. The view compiles and previews; the
full suite stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-charging-activity/03-CONTEXT.md
@.planning/phases/03-charging-activity/03-RESEARCH.md

@Islet/Notch/NotchPillView.swift
@Islet/Notch/NotchShape.swift
@Islet/Notch/NotchInteractionState.swift
@Islet/Notch/PowerActivity.swift
@Islet/Notch/ChargingActivityState.swift

<interfaces>
<!-- Created by Plan 01 — render against these, do NOT re-explore the codebase. -->

From Islet/Notch/PowerActivity.swift:
```swift
enum ChargingActivity: Equatable {
    case charging(percent: Int)   // bolt glyph
    case full(percent: Int)       // full / green glyph, no bolt
    case onBattery(percent: Int)  // plain battery glyph (CHG-02)
}
```

From Islet/Notch/ChargingActivityState.swift:
```swift
final class ChargingActivityState: ObservableObject {
    @Published var activity: ChargingActivity?
}
```

Existing NotchPillView contract (do NOT break):
```swift
struct NotchPillView: View {
    @ObservedObject var interaction: NotchInteractionState
    var onClick: () -> Void = {}
    static let collapsedSize = CGSize(width: 200, height: 38)
    static let expandedSize  = CGSize(width: 360, height: 72)
}
```

SF Symbols verified available at the macOS-14 floor (RESEARCH § Environment Availability):
- "battery.100percent"        (charging→false / full / on-battery)
- "battery.100percent.bolt"   (charging→true)
- variable-value fill: Image(systemName: name, variableValue: Double(percent)/100.0)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the wings sideways layout branch + ChargingActivityState observation + D-11 precedence</name>
  <read_first>
    - Islet/Notch/NotchPillView.swift (read the FULL current file — the body ZStack, the collapsed/expanded branches, the size seeds, the #Preview blocks; the new branch must coexist without breaking the matchedGeometryEffect "island" morph)
    - Islet/Notch/PowerActivity.swift (the ChargingActivity enum cases the switch renders)
    - Islet/Notch/ChargingActivityState.swift (the @ObservedObject to add)
    - Islet/Notch/NotchShape.swift (the pill shape; the wings keep the same black NotchShape blob so the morph stays consistent)
    - .planning/phases/03-charging-activity/03-RESEARCH.md § "Pattern 3" (the glyph code, spring vocabulary, matchedGeometryEffect note) and § "Pattern 2" (the D-11 precedence if-ordering)
  </read_first>
  <action>
    Extend `Islet/Notch/NotchPillView.swift` (do NOT create a new file; do NOT touch NotchInteractionState).

    1. ADD a stored input to the struct, after `var onClick`:
         @ObservedObject var charging: ChargingActivityState
       This is non-defaulted, so update BOTH #Preview blocks (and the controller call in Plan 03) to pass it.

    2. CHANGE the body's branch ordering to implement D-11 precedence (charging briefly wins even when the
       user expanded the island). Replace the current `if interaction.isExpanded { expandedIsland } else
       { collapsedIsland }` with the three-way ordering from RESEARCH Pattern 2:
         if let activity = charging.activity {
             wings(for: activity)            // NEW sideways branch — D-11: charging wins
         } else if interaction.isExpanded {
             expandedIsland                  // existing Phase-2 downward expand
         } else {
             collapsedIsland                 // existing idle pill
         }
       Keep the outer `.frame(width: Self.expandedSize.width, height: Self.expandedSize.height, alignment: .top)`
       and `.onTapGesture { onClick() }` exactly as they are (D-10: click stays informational; the controller
       decides what a click does while an activity stands).

    3. ADD the wings layout function. Use the black NotchShape blob on the SAME shared matchedGeometryEffect
       `id: "island"` namespace so the pill morphs into the wings shape (no cross-fade, consistent with Phase 2),
       overlaid with an HStack: status symbol LEFT (D-05), a gap for the camera bridge, the filling battery glyph
       + % RIGHT (D-03). Use a flat wings size (wide, short). Implement:

         // CHG-01 / D-01 / D-03 / D-04 / D-05 — the WINGS / Alcove sideways layout: a flat, wide
         // strip flanking the notch. Status symbol LEFT, a single filling battery glyph + numeric %
         // RIGHT. ONE consistent glyph encodes all three states (bolt = charging, full/green at 100%,
         // plain = on battery — D-04), NOT three mini-scenes. The view drives NO animation (D-08); the
         // controller (Plan 03) wraps the activity mutation in withAnimation(.spring(...)).
         private func wings(for activity: ChargingActivity) -> some View {
             let isCharging: Bool
             let percent: Int
             let tint: Color
             switch activity {
             case .charging(let p): isCharging = true;  percent = p; tint = .white
             case .full(let p):     isCharging = false; percent = p; tint = .green   // D-04 green at full (discretion)
             case .onBattery(let p):isCharging = false; percent = p; tint = .white   // CHG-02 plain
             }
             let symbol = isCharging ? "battery.100percent.bolt" : "battery.100percent"
             return NotchShape(topCornerRadius: 6, bottomCornerRadius: 6)   // flatter than the downward blob
                 .fill(Color.black)
                 .matchedGeometryEffect(id: "island", in: ns)
                 .frame(width: Self.wingsSize.width, height: Self.wingsSize.height)
                 .overlay(
                     HStack(spacing: 0) {
                         Image(systemName: "bolt.fill")                       // D-05 status symbol LEFT (discretion)
                             .symbolRenderingMode(.hierarchical)
                             .foregroundStyle(isCharging ? Color.yellow : Color.white.opacity(0.6))
                             .padding(.leading, 10)
                         Spacer()                                             // clears the physical camera bridge
                         Image(systemName: symbol, variableValue: Double(percent) / 100.0)  // D-03 filling glyph
                             .symbolRenderingMode(.hierarchical)
                             .foregroundStyle(tint)
                         Text("\(percent)%")                                  // D-06 percent only — no time/wattage
                             .font(.system(size: 13, weight: .semibold, design: .rounded))
                             .monospacedDigit()
                             .foregroundStyle(.white)
                             .padding(.trailing, 10)
                             .padding(.leading, 4)
                     }
                     .frame(width: Self.wingsSize.width, height: Self.wingsSize.height)
                 )
         }

    4. ADD the wings size seed next to the existing size seeds (single source — Plan 03 feeds the SAME size to
       wingsFrame for the panel; must match the 360×40 seed used in Plan 01's wingsFrame tests):
         static let wingsSize = CGSize(width: 360, height: 40)

    5. UPDATE both #Preview blocks to pass a `charging:` argument. Add a THIRD preview "Charging Wings" that sets
       `let cs = ChargingActivityState(); cs.activity = .charging(percent: 47)` and renders the view so the wings
       branch compiles and is visible. The existing "Collapsed"/"Expanded" previews pass a fresh
       `ChargingActivityState()` (activity nil) so they still show the original branches.

    CONSTRAINTS: D-06 — render ONLY the percentage (no time-to-full, no wattage). D-08 — NO withAnimation, NO
    Timer, NO onAppear animation in this view. D-04 — exactly ONE battery glyph that switches variant/tint
    (do NOT add per-state separate animated scenes). Keep the matchedGeometryEffect `id: "island"` on the wings
    blob so the morph is shared. Do NOT add IOKit, do NOT touch NotchWindowController (that is Plan 03).
  </action>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' && xcodebuild test -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "@ObservedObject var charging: ChargingActivityState" Islet/Notch/NotchPillView.swift` succeeds
    - `grep -q "if let activity = charging.activity" Islet/Notch/NotchPillView.swift` succeeds (D-11 precedence ordering, charging first)
    - `grep -q "battery.100percent.bolt" Islet/Notch/NotchPillView.swift` succeeds (D-04 charging variant)
    - `grep -q "Image(systemName: symbol, variableValue:" Islet/Notch/NotchPillView.swift` succeeds (D-03 proportional fill)
    - `grep -q "static let wingsSize = CGSize(width: 360, height: 40)" Islet/Notch/NotchPillView.swift` succeeds (matches Plan 01 wingsFrame test seed)
    - `grep -c "matchedGeometryEffect(id: \"island\"" Islet/Notch/NotchPillView.swift` returns >= 3 (collapsed + expanded + wings share the morph identity)
    - `grep -c "withAnimation\|Timer(\|onAppear" Islet/Notch/NotchPillView.swift` returns 0 (D-08 — view drives no animation/clock)
    - `grep -c "import IOKit" Islet/Notch/NotchPillView.swift` returns 0 (no IOKit in the view)
    - `grep -q "Charging Wings" Islet/Notch/NotchPillView.swift` succeeds (third preview exists)
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` exits 0 and `xcodebuild test -scheme Islet -destination 'platform=macOS'` exits 0
  </acceptance_criteria>
  <done>
    NotchPillView renders the sideways wings layout (status symbol left, ONE filling battery glyph + % right) when
    `charging.activity` is non-nil, taking precedence over the expanded island (D-11) via a one-line if-ordering;
    the glyph switches bolt/full/plain by state and fills to the percentage; the view drives no animation; all
    three previews compile; the build and full test suite are green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | SwiftUI rendering only. The view consumes an already-validated, in-process `ChargingActivity` value object (percent already clamped in Plan 01). No external input, no network, no persistence, no IOKit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | wings layout content | accept | The only rendered data is battery percent (already user-visible in the menu bar). No PII, no secrets. Severity: low — accepted. |
| T-03-04 | Tampering | variableValue argument | mitigate | `Double(percent)/100.0` is fed a value already clamped to 0...100 by `powerActivity(from:)` (Plan 01, T-03-01), so the symbol fill ratio is always 0.0...1.0. Severity: low. |

No `high`-severity threats in this plan — it is pure SwiftUI rendering of an in-process value
object. The IOKit/`@convention(c)`/main-thread-hop surface is in Plan 03.
</threat_model>

<verification>
- `xcodegen generate` succeeds.
- `xcodebuild build -scheme Islet -destination 'platform=macOS'` exits 0 (the new branch + previews compile).
- `xcodebuild test -scheme Islet -destination 'platform=macOS'` exits 0 (full suite — no regression).
- The wings blob shares `matchedGeometryEffect(id: "island")` (morph consistency).
- The view contains no `withAnimation` / `Timer` / `onAppear` / `import IOKit` (D-08 + no system wiring).
</verification>

<success_criteria>
- A non-nil `ChargingActivity` renders the sideways wings layout (status symbol left, filling battery glyph + %
  right), distinct from the downward expanded blob, taking precedence over the expanded island (D-11).
- ONE consistent battery glyph encodes charging (bolt) / full (green) / on-battery (plain), filling to the
  percentage (D-03/D-04), showing percent only (D-06).
- The view drives no animation/timer/IOKit; build + full suite green; all three previews compile.
</success_criteria>

<output>
After completion, create `.planning/phases/03-charging-activity/03-02-SUMMARY.md`
</output>
