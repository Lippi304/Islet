---
phase: 03-charging-activity
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - Islet/Notch/PowerActivity.swift
  - Islet/Notch/ChargingActivityState.swift
  - Islet/Notch/NotchGeometry.swift
  - IsletTests/PowerActivityTests.swift
  - IsletTests/NotchGeometryTests.swift
autonomous: true
requirements: [CHG-01, CHG-02]
tags: [power, iokit, charging, tdd, geometry]

must_haves:
  truths:
    - "powerActivity(from:) returns .charging on AC + charging, .full when on AC but not charging, .onBattery when unplugged, and nil when no battery is present"
    - "Percent is clamped to 0...100 so a malformed reading never produces an out-of-range value"
    - "shouldTriggerSplash fires only on a category transition (e.g. onBattery→charging, charging→onBattery), not on a pure percent tick within the same category"
    - "wingsFrame centers a wide flat frame on the collapsed pill's midX and pins it to the top edge"
    - "The pure PowerActivityTests + the new NotchGeometryTests wings cases pass via xcodebuild"
  artifacts:
    - path: "Islet/Notch/PowerActivity.swift"
      provides: "Pure power→presentation seam: PowerReading struct, ChargingActivity enum, powerActivity(from:), shouldTriggerSplash(previous:next:)"
      contains: "func powerActivity(from"
    - path: "Islet/Notch/ChargingActivityState.swift"
      provides: "ObservableObject holding @Published var activity: ChargingActivity?"
      contains: "@Published var activity"
    - path: "IsletTests/PowerActivityTests.swift"
      provides: "RED→GREEN unit coverage of the full power→presentation matrix + the no-battery nil path + transition debounce"
      contains: "testNoBatteryMapsToNil"
    - path: "Islet/Notch/NotchGeometry.swift"
      provides: "wingsFrame(collapsed:wingsSize:) pure frame math (extends the existing pure seam)"
      contains: "func wingsFrame"
  key_links:
    - from: "IsletTests/PowerActivityTests.swift"
      to: "Islet/Notch/PowerActivity.swift"
      via: "@testable import Islet"
      pattern: "powerActivity\\(from"
    - from: "Islet/Notch/ChargingActivityState.swift"
      to: "Islet/Notch/PowerActivity.swift"
      via: "ChargingActivity enum reference"
      pattern: "ChargingActivity"
---

<objective>
Establish the pure, unit-tested seams for Phase 3 BEFORE any IOKit or AppKit wiring exists
(Wave 0 / Nyquist). This plan creates the contracts every later wave implements against:

1. `PowerActivity.swift` — the PURE power→presentation mapping (`PowerReading` input,
   `ChargingActivity` output enum, `powerActivity(from:)`, and a pure splash-debounce predicate
   `shouldTriggerSplash(previous:next:)`). No `import IOKit`, no `import AppKit` in this file.
2. `ChargingActivityState.swift` — the `ObservableObject` that publishes the current
   `ChargingActivity?` into SwiftUI (Pattern 2 — a separate `@Published` model, NOT a new
   `InteractionPhase` case; leaves the tested 3-state machine intact).
3. `wingsFrame(...)` added to the existing pure `NotchGeometry.swift` seam — the wide/flat
   sideways frame the wings layout (Plan 02) and the panel sizing (Plan 03) consume.
4. The failing-then-passing XCTest coverage (`PowerActivityTests.swift`, extended
   `NotchGeometryTests.swift`).

Purpose: the riskiest classification logic (charging vs full vs on-battery vs no-battery,
percent clamping, splash debounce) is verified by the agent in milliseconds, exactly mirroring
the Phase-1/2 `NotchGeometry` / `NotchInteractionState` TDD seam. Later waves only do the
untestable IOKit + SwiftUI wiring.

Output: 2 new `Islet/` source files, 1 extended source file, 1 new test file, 1 extended test
file, the project regenerated via `xcodegen generate`, and the full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-charging-activity/03-CONTEXT.md
@.planning/phases/03-charging-activity/03-RESEARCH.md
@.planning/phases/03-charging-activity/03-VALIDATION.md

@Islet/Notch/NotchGeometry.swift
@Islet/Notch/NotchInteractionState.swift
@IsletTests/NotchGeometryTests.swift
@IsletTests/InteractionStateTests.swift
@project.yml

<interfaces>
<!-- Contracts this plan CREATES. Downstream plans (02, 03) implement against these verbatim. -->
<!-- All values copied from 03-RESEARCH.md Pattern 1 + Pattern 4 — compile-verified there. -->

To be created in Islet/Notch/PowerActivity.swift (PURE — no IOKit/AppKit import):
```swift
// The minimal raw reading PowerSourceMonitor (Plan 03) lifts out of the IOPS dictionary.
// Plain values so tests construct it by hand (mirrors ScreenDescriptor's role).
struct PowerReading: Equatable {
    let isPresent: Bool      // false → no readable battery (desktop / empty source list)
    let isOnAC: Bool         // kIOPSPowerSourceStateKey == "AC Power"
    let isCharging: Bool     // kIOPSIsChargingKey
    let isCharged: Bool      // kIOPSIsChargedKey (true at full on AC)
    let percent: Int         // 0...100, clamped
}

// The presentation the splash renders (D-04: one glyph, three states).
enum ChargingActivity: Equatable {
    case charging(percent: Int)   // on AC, actively charging → bolt glyph
    case full(percent: Int)       // on AC, charged / plugged-but-full → full (green) glyph
    case onBattery(percent: Int)  // unplugged → plain battery glyph (CHG-02)
}

// TOTAL pure mapping. nil == "no splash" (no readable battery → graceful no-op).
func powerActivity(from r: PowerReading) -> ChargingActivity?

// Pure splash-debounce predicate (Pitfall 4): true only when the CATEGORY changed
// (charging/full/onBattery, ignoring the percent number), so a pure % tick within the
// same category does NOT re-fire a splash.
func shouldTriggerSplash(previous: ChargingActivity?, next: ChargingActivity?) -> Bool
```

To be created in Islet/Notch/ChargingActivityState.swift:
```swift
final class ChargingActivityState: ObservableObject {
    @Published var activity: ChargingActivity?
}
```

To be added to Islet/Notch/NotchGeometry.swift (same contract as expandedNotchFrame):
```swift
// Wide/flat sideways frame: centered on the collapsed pill's midX, pinned to the top
// edge (AppKit bottom-left origin → top edge is collapsed.maxY).
func wingsFrame(collapsed: CGRect, wingsSize: CGSize) -> CGRect
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED→GREEN the pure power→presentation seam (PowerActivity.swift + PowerActivityTests.swift)</name>
  <read_first>
    - Islet/Notch/NotchInteractionState.swift (mirror this pure-seam style: plain types + total function, no AppKit/IOKit)
    - IsletTests/InteractionStateTests.swift (mirror this XCTest idiom: one assertion-focused test per behavior, `@testable import Islet`)
    - .planning/phases/03-charging-activity/03-RESEARCH.md § "Pattern 1" and § "Pitfall 4" (the exact types, the mapping rules, and the debounce design)
    - .planning/phases/03-charging-activity/03-VALIDATION.md § "Per-Task Verification Map" (the exact test-function names)
  </read_first>
  <behavior>
    powerActivity(from:) — the locked test matrix (RED first, then implement to GREEN):
    - present + AC + charging + 47%        → .charging(percent: 47)     (test: testChargingMapsToCharging, CHG-01)
    - present + AC + charged + 100%         → .full(percent: 100)        (test: testChargedMapsToFull, CHG-01)
    - present + AC + NOT charging + NOT charged + 100% → .full(percent: 100)  (test: testOnACNotChargingMapsToFull — the "distinguish charging from plugged-but-full" criterion)
    - present + battery (not AC) + 63%      → .onBattery(percent: 63)    (test: testOnBatteryMapsToOnBattery, CHG-02)
    - NOT present (desktop / empty list)    → nil                        (test: testNoBatteryMapsToNil — locked no-op criterion)
    - present + AC + charging + percent -5  → .charging(percent: 0)      (test: testPercentClampedLow)
    - present + AC + charging + percent 150 → .charging(percent: 100)    (test: testPercentClampedHigh)
    shouldTriggerSplash(previous:next:):
    - .onBattery(50) → .charging(51)  → true   (test: testTransitionTriggersSplash — plug-in)
    - .charging(80)  → .onBattery(80) → true   (test: testUnplugTransitionTriggersSplash — CHG-02 unplug)
    - .charging(46)  → .charging(47)  → false  (test: testSameCategoryTickDoesNotTrigger — pure % tick)
    - nil            → .charging(20)  → true   (test: testNilToActivityTriggers — first reading after launch with a change)
    - .charging(20)  → nil            → false  (test: testActivityToNilDoesNotTrigger — clearing the splash is not itself a new splash)
  </behavior>
  <action>
    STEP 1 (RED): Create `IsletTests/PowerActivityTests.swift` with `import XCTest` + `@testable import Islet`,
    class `PowerActivityTests: XCTestCase`. Write ALL test functions listed in <behavior> with the exact
    names given. Construct `PowerReading` by hand in each test (mirror how InteractionStateTests builds states).
    Use `XCTAssertEqual` against the exact enum cases, and `XCTAssertNil` for the no-battery case.
    Example shape for one case:
      func testOnACNotChargingMapsToFull() {
          let r = PowerReading(isPresent: true, isOnAC: true, isCharging: false, isCharged: false, percent: 100)
          XCTAssertEqual(powerActivity(from: r), .full(percent: 100))
      }

    STEP 2 (still RED): Create `Islet/Notch/PowerActivity.swift` with ONLY `import Foundation` (NO `import IOKit`,
    NO `import AppKit`, NO `import SwiftUI`). Define `PowerReading` (struct, Equatable, all five `let` fields from
    the <interfaces> block), `ChargingActivity` (enum, Equatable, the three cases with `percent: Int` associated
    values), and the two function SIGNATURES with placeholder bodies that return wrong values (e.g. `return nil`)
    so the tests compile-and-fail. Run the tests to confirm RED.

    STEP 3 (GREEN): Implement the bodies VERBATIM from RESEARCH Pattern 1:
      func powerActivity(from r: PowerReading) -> ChargingActivity? {
          guard r.isPresent else { return nil }
          let p = min(max(r.percent, 0), 100)
          if r.isOnAC {
              if r.isCharging { return .charging(percent: p) }
              return .full(percent: p)
          }
          return .onBattery(percent: p)
      }
    For `shouldTriggerSplash`, classify each ChargingActivity? into a category that IGNORES the percent
    (e.g. an internal helper returning "charging"/"full"/"onBattery"/"none") and return `true` only when the
    previous category != the next category AND the next category is not "none". A nil→activity edge returns
    true; an activity→nil edge returns false (clearing the splash is not a new splash). Implement so EVERY
    <behavior> row passes.

    STEP 4: Run `xcodegen generate` (the two new files under Islet/ and IsletTests/ must be picked up), then run
    the quick command. CONSTRAINT: do NOT add an `InteractionPhase` case; do NOT import IOKit/AppKit/SwiftUI in
    PowerActivity.swift; do NOT add any timer or polling.
  </action>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild test -scheme Islet -only-testing:IsletTests/PowerActivityTests -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import IOKit\|import AppKit\|import SwiftUI" Islet/Notch/PowerActivity.swift` returns 0 (pure seam — no system frameworks)
    - `grep -q "func powerActivity(from" Islet/Notch/PowerActivity.swift` succeeds
    - `grep -q "func shouldTriggerSplash(previous:" Islet/Notch/PowerActivity.swift` succeeds
    - `grep -q "case charging(percent: Int)" Islet/Notch/PowerActivity.swift` succeeds (D-04 enum)
    - `grep -q "case onBattery(percent: Int)" Islet/Notch/PowerActivity.swift` succeeds (CHG-02 enum)
    - `grep -q "min(max(r.percent, 0), 100)" Islet/Notch/PowerActivity.swift` succeeds (clamp present)
    - All of: testChargingMapsToCharging, testOnACNotChargingMapsToFull, testNoBatteryMapsToNil, testPercentClampedHigh, testOnBatteryMapsToOnBattery, testTransitionTriggersSplash, testSameCategoryTickDoesNotTrigger exist in IsletTests/PowerActivityTests.swift (`grep -c "func test"` >= 12)
    - `xcodebuild test -scheme Islet -only-testing:IsletTests/PowerActivityTests -destination 'platform=macOS'` exits 0
    - `grep -c "InteractionPhase" Islet/Notch/PowerActivity.swift` returns 0 (did not fold charging into the user-gesture machine)
  </acceptance_criteria>
  <done>
    PowerActivity.swift is a pure, framework-free seam with the three-case ChargingActivity enum, the total
    powerActivity(from:) mapping (clamped, nil on no-battery), and the category-transition shouldTriggerSplash
    predicate; PowerActivityTests covers the full matrix and passes via xcodebuild.
  </done>
</task>

<task type="auto">
  <name>Task 2: ChargingActivityState model + wingsFrame geometry (with RED→GREEN wings tests)</name>
  <read_first>
    - Islet/Notch/NotchInteractionState.swift (mirror the ObservableObject + @Published pattern for ChargingActivityState)
    - Islet/Notch/NotchGeometry.swift (read expandedNotchFrame VERBATIM — wingsFrame uses the IDENTICAL center-on-midX + pin-to-top contract)
    - IsletTests/NotchGeometryTests.swift (mirror the expandedNotchFrame test style — exact arithmetic in comments, accuracy: 0.0001)
    - .planning/phases/03-charging-activity/03-RESEARCH.md § "Pattern 4" (panel sizing for wings) and § "Pattern 2" (the separate @Published model rationale)
    - Islet/Notch/PowerActivity.swift (created in Task 1 — ChargingActivityState references the ChargingActivity enum)
  </read_first>
  <action>
    PART A — ChargingActivityState.swift:
    Create `Islet/Notch/ChargingActivityState.swift` with `import Foundation` + `import Combine` (or just
    `import SwiftUI` for ObservableObject — match NotchInteractionState which uses `import Foundation` and
    `ObservableObject`). Define EXACTLY:
      final class ChargingActivityState: ObservableObject {
          @Published var activity: ChargingActivity?
      }
    Add a one-line doc comment noting this is Pattern 2: a SEPARATE @Published activity model alongside the
    untouched NotchInteractionState 3-state machine (NOT a new InteractionPhase case), so the Phase-2 tests stay
    intact and D-11 precedence is a one-line `if` in the view. Do NOT add methods, timers, or IOKit here — it is a
    plain published holder; the controller (Plan 03) sets `.activity`.

    PART B — wingsFrame in NotchGeometry.swift (RED→GREEN):
    First add the wings test cases to `IsletTests/NotchGeometryTests.swift` under a `// MARK: wingsFrame (CHG-01)`
    section (RED). Use the SAME collapsed pill fixture as the existing expandedNotchFrame tests
    (collapsed = CGRect(x: 610, y: 944, width: 292, height: 38), midX 756, maxY 982). Pick a wings seed size of
    `CGSize(width: 360, height: 40)` (wide + flat — sideways, D-01) so the union with the 360×72 expanded frame
    needs no runtime resize (Pattern 4 / A4). Tests:
      - testWingsFrameCentersOnMidXAndPinsTop: frame.midX == 756, frame.maxY == 982,
        origin.x == 756 - 180 == 576, origin.y == 982 - 40 == 942, width == 360, height == 40 (all accuracy 0.0001)
      - testWingsFrameOnNonZeroOriginScreen: collapsed = CGRect(x: 2530, y: 944, width: 292, height: 38),
        midX 2676 → origin.x == 2676 - 180 == 2496, origin.y == 942
      - testWingsFrameDegenerateEqualsCollapsedWhenSameSize: wingsSize == collapsed.size → frame == collapsed
    Then implement in `Islet/Notch/NotchGeometry.swift` (append after expandedNotchFrame), mirroring it EXACTLY:
      // CHG-01 — the WINGS frame (sideways/Alcove layout, D-01). Same contract as
      // expandedNotchFrame: centered on the collapsed pill's midX and pinned to the top
      // edge (AppKit bottom-left origin, so the top edge is maxY). The panel is sized to
      // the UNION of this and expandedNotchFrame up front (Pattern 4) so neither the
      // Phase-2 downward expand nor the Phase-3 sideways wings is ever resized mid-animation.
      func wingsFrame(collapsed: CGRect, wingsSize: CGSize) -> CGRect {
          let x = collapsed.midX - wingsSize.width / 2
          let y = collapsed.maxY - wingsSize.height
          return CGRect(x: x, y: y, width: wingsSize.width, height: wingsSize.height)
      }

    PART C: Run `xcodegen generate` so ChargingActivityState.swift is included, then run the full suite.
  </action>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild test -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "final class ChargingActivityState: ObservableObject" Islet/Notch/ChargingActivityState.swift` succeeds
    - `grep -q "@Published var activity: ChargingActivity?" Islet/Notch/ChargingActivityState.swift` succeeds
    - `grep -c "InteractionPhase" Islet/Notch/ChargingActivityState.swift` returns 0 (separate model, not a phase case)
    - `grep -q "func wingsFrame(collapsed: CGRect, wingsSize: CGSize)" Islet/Notch/NotchGeometry.swift` succeeds
    - `grep -q "collapsed.midX - wingsSize.width / 2" Islet/Notch/NotchGeometry.swift` succeeds (centers on midX)
    - `grep -q "collapsed.maxY - wingsSize.height" Islet/Notch/NotchGeometry.swift` succeeds (pins to top edge)
    - `grep -c "func testWingsFrame" IsletTests/NotchGeometryTests.swift` returns >= 3
    - `xcodebuild test -scheme Islet -destination 'platform=macOS'` exits 0 (full suite green — Phase-1/2 tests still pass, proving the 3-state machine was untouched)
  </acceptance_criteria>
  <done>
    ChargingActivityState publishes a ChargingActivity? (separate model — no InteractionPhase change), wingsFrame
    extends the pure geometry seam with center-on-midX + pin-to-top math, the new wings tests pass, and the FULL
    suite (incl. all Phase-1/2 tests) is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | Pure logic only. No IOKit read, no network, no user input, no persistence, no AppKit. The IOKit/`@convention(c)` boundary is introduced in Plan 03, not here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `powerActivity(from:)` percent handling | mitigate | Percent clamped via `min(max(r.percent, 0), 100)` so a malformed/out-of-range reading can never produce an invalid percent — covered by testPercentClampedLow/High. Severity: low. |
| T-03-02 | Denial of Service | `shouldTriggerSplash` debounce | mitigate | Category-transition predicate prevents splash re-fire on every percent tick (Pitfall 4) — bounds UI churn and keeps idle CPU low. Tested. Severity: low. |

No `high`-severity threats in this plan. The realistic surface (IOKit ownership, `@convention(c)`
context-pointer lifetime, main-thread hop) lives entirely in Plan 03 and is enumerated there.
This plan is pure, framework-free Swift — the gate passes with all-low severity.
</threat_model>

<verification>
- `xcodegen generate` succeeds (new files auto-discovered under Islet/ and IsletTests/).
- `xcodebuild test -scheme Islet -only-testing:IsletTests/PowerActivityTests -destination 'platform=macOS'` exits 0.
- `xcodebuild test -scheme Islet -destination 'platform=macOS'` exits 0 (full suite — Phase-1/2 tests intact).
- PowerActivity.swift imports no system framework (pure seam guard).
- No `InteractionPhase` case added (the Phase-2 machine is untouched — its tests still pass).
</verification>

<success_criteria>
- The pure `PowerReading` / `ChargingActivity` / `powerActivity(from:)` / `shouldTriggerSplash(previous:next:)`
  seam exists and passes the full RESEARCH test matrix incl. the no-battery `nil` path and percent clamping.
- `ChargingActivityState` publishes a `ChargingActivity?` as a separate model (no `InteractionPhase` change).
- `wingsFrame(collapsed:wingsSize:)` is added to the pure geometry seam and unit-tested (center on midX, pin to top).
- Full XCTest suite is green via `xcodebuild`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-charging-activity/03-01-SUMMARY.md`
</output>
