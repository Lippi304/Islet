---
phase: 33-weather-widget-redesign
plan: 2
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - Islet/Notch/NotchPillView.swift
  - Islet/Notch/NotchWindowController.swift
  - Islet/SettingsView.swift
autonomous: false
requirements: [WEATHER-01, WEATHER-02]

must_haves:
  truths:
    - "The Weather tab's compact card shows the resolved location name (or 'Local' fallback), condition icon, current temperature, and high/low"
    - "A Settings toggle switches Weather between compact and an extended card showing a forecast row, live, without relaunch"
    - "Toggling extended forecast animates the panel's height change via the existing spring, matching every other size transition in the app"
    - "Click-through hit-testing matches the extended card's real rendered geometry exactly, with no regression to the CR-01/WR-02 failure class"
    - "Weather degrades silently (no crash) on location/geocode permission denial or fetch failure"
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "weatherExtendedContentHeight constant, location/H-L in weatherFullContent, forecastRow view, blobShape height override wiring"
    - path: "Islet/Notch/NotchWindowController.swift"
      provides: "refreshWeather() rewired to the combined fetch + resolvePlaceName, positionAndShow's weatherExtendedFrame union member, visibleContentZone()'s .weatherExpanded branch"
    - path: "Islet/SettingsView.swift"
      provides: "Weather section with the Extended forecast toggle"
  key_links:
    - from: "Islet/Notch/NotchPillView.swift weatherFullView blobShape height: override"
      to: "Islet/Notch/NotchWindowController.swift positionAndShow weatherExtendedFrame union + visibleContentZone .weatherExpanded branch"
      via: "shared weatherExtendedContentHeight constant"
      pattern: "weatherExtendedContentHeight"
    - from: "Islet/SettingsView.swift Toggle(\"Extended forecast\")"
      to: "Islet/Notch/NotchPillView.swift @AppStorage(ActivitySettings.weatherExtendedKey)"
      via: "shared UserDefaults key"
      pattern: "weatherExtendedKey"
---

<objective>
Render the extended-vs-compact Weather card (location/H-L on the compact card, a forecast row when the Settings toggle is on) and wire the panel geometry (`positionAndShow` union + `visibleContentZone()` click-through branch) to match, using Plan 33-01's new `fetchCurrentAndForecast`/`resolvePlaceName`/`DailyForecast` types.

Purpose: Close out WEATHER-01 (compact card: location, icon, temp, H/L) and WEATHER-02 (Settings-gated extended forecast row, live-animated) while avoiding this project's twice-repeated CR-01/WR-02 click-through geometry regression class — the `blobShape` height override, the panel-frame union member, and the click-zone branch must all agree.
Output: Updated `weatherFullView`/`weatherFullContent`/`forecastRow` in `NotchPillView.swift`, rewired `refreshWeather()`/`positionAndShow`/`visibleContentZone()` in `NotchWindowController.swift`, a new "Weather" Settings section, and an on-device UAT checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-weather-widget-redesign/33-CONTEXT.md
@.planning/phases/33-weather-widget-redesign/33-UI-SPEC.md
@.planning/phases/33-weather-widget-redesign/33-PATTERNS.md

<interfaces>
<!-- From Plan 33-01 (Islet/Weather/WeatherService.swift, Islet/Notch/BasicOutfitState.swift, Islet/ActivitySettings.swift) — use these directly, no exploration needed. -->

```swift
// Islet/Weather/WeatherService.swift
struct DailyForecast: Equatable, Identifiable {
    var id: Date { date }
    let date: Date
    let category: WeatherCategory
    let high: Measurement<UnitTemperature>
    let low: Measurement<UnitTemperature>
}

struct WeatherGlance: Equatable {
    let category: WeatherCategory
    let temperature: Measurement<UnitTemperature>
    let high: Measurement<UnitTemperature>?
    let low: Measurement<UnitTemperature>?
}

protocol WeatherService: AnyObject {
    func fetchCurrentAndForecast(latitude: Double, longitude: Double,
                                  completion: @escaping (WeatherGlance?, [DailyForecast]?) -> Void)
    func resolvePlaceName(for location: CLLocation, completion: @escaping (String?) -> Void)
}
```

```swift
// Islet/Notch/BasicOutfitState.swift
@Published var weather: WeatherGlance?
@Published var calendar: CalendarGlance?
@Published var forecast: [DailyForecast]?
@Published var locationName: String?
```

```swift
// Islet/ActivitySettings.swift
static let weatherExtendedKey = "weather.extended"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Weather card view — location/H-L + extended forecast row</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift lines 715-800 (weatherFullView/weatherFullContent/weatherCategoryLabel/weatherFullUnavailable — current structure to extend)
    Islet/Notch/NotchPillView.swift lines 300-360 (switcherContentHeight/traySize/trayContentHeight doc-comment box-math convention to mirror for the new constant)
    Islet/Notch/NotchPillView.swift lines 795-812 (trayFullView — the exact `height:` override call-site precedent)
    Islet/Notch/NotchPillView.swift lines 1150-1200 (blobShape signature — `height ?? (showSwitcher ? switcherContentHeight : ...)` fallback ordering, already generalized, no change needed to blobShape itself)
    Islet/Notch/NotchPillView.swift lines 1480-1501 (weatherIcon(for:) — reuse verbatim at two sizes: 44pt compact, 20pt forecast chip)
    .planning/phases/33-weather-widget-redesign/33-UI-SPEC.md "Layout Contract" section (exact chip structure/sizes/5-day count/240pt starting-height math, typography roles: Display 32pt, Label 13pt, H/L 12pt, Caption 11pt)
  </read_first>
  <action>
    In NotchPillView.swift: (1) add `static let weatherExtendedContentHeight: CGFloat = 240` near `trayContentHeight` (~line 336), with the same box-math-in-a-comment convention as trayContentHeight's own doc comment: cameraClearance(42) + icon(44) + spacing(8) + temp(32) + spacing(8) + location/H-L label lines(~16) + section gap(16) + forecast chip stack(~56) + bottom inset(16) ~= 238, rounded to 240 — flag it explicitly as a starting point for on-device tuning, exactly like trayContentHeight's own iteration history. (2) Add `@AppStorage(ActivitySettings.weatherExtendedKey) private var weatherExtended = false` as a property on the view struct. (3) In `weatherFullView`'s `blobShape(...)` call, add `height: weatherExtended ? Self.weatherExtendedContentHeight : nil`. (4) Extend `weatherFullContent(_ weather: WeatherGlance)`: add a location-name label (13pt Label role, `.secondary`, `design: .rounded`) reading `outfit.locationName ?? "Local"`, placed ABOVE the existing icon (D-01/D-02 — same slot always rendered, no layout shift while the real name resolves); add an H/L readout line directly under the existing category label, reusing the 13pt `.secondary` style, formatted as `"H:\(high) L:\(low)"` using the existing `.formatted(.measurement(width: .narrow, numberFormatStyle: .number.precision(.fractionLength(0))))` convention on `weather.high`/`weather.low`, omitted entirely (no empty line) if either is nil — do not restructure the existing icon/temperature pairing. (5) Add a new `forecastRow(_ forecast: [DailyForecast]) -> some View` directly below `weatherFullContent(...)`'s call site inside `weatherFullView`'s content closure, rendered only `if weatherExtended, let forecast = outfit.forecast`: an `HStack(spacing: 8)` of `Array(forecast.prefix(5))` day-chips, each a `VStack(spacing: 4)` — weekday label (`Text(day.date, format: .dateTime.weekday(.abbreviated))`, 11pt Caption, `.secondary`) -> `weatherIcon(for: day.category).font(.system(size: 20))` -> H/L text (`"\(high)/\(low)"` using the same locale-aware measurement formatting, 12pt semibold, `.monospacedDigit()`, `.foregroundStyle(.white)`) — wrap the row in `.padding(.horizontal, 16)` and `.padding(.top, 16)`.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "weatherExtendedContentHeight" Islet/Notch/NotchPillView.swift` returns at least 2 (constant declaration + blobShape call-site use)
    - `grep -c "func forecastRow" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "outfit.locationName" Islet/Notch/NotchPillView.swift` returns at least 1
    - `grep -c "@AppStorage(ActivitySettings.weatherExtendedKey)" Islet/Notch/NotchPillView.swift` returns 1
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors
  </acceptance_criteria>
  <done>weatherFullContent shows location + H/L, forecastRow renders a 5-chip row when weatherExtended is true and outfit.forecast is populated, blobShape's height override is wired, build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Controller fetch wiring + panel geometry three-site rule + Settings toggle</name>
  <files>Islet/Notch/NotchWindowController.swift, Islet/SettingsView.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift lines 570-610 (startLocationOnce/startOutfitRefresh/refreshWeather — current fetch dispatch to rewire)
    Islet/Notch/NotchWindowController.swift lines 790-820 (positionAndShow — expandedFrame/wings/onboardingFrame/trayFrame union chain, the exact template for weatherExtendedFrame)
    Islet/Notch/NotchWindowController.swift lines 960-1000 (visibleContentZone — isOnboardingActive/.trayExpanded branch ordering, the exact template for .weatherExpanded)
    Islet/Notch/NotchWindowController.swift line 525 (activityEnabled(_:) helper — do NOT reuse it for weatherExtended, since it defaults an absent key to true; read `UserDefaults.standard.bool(forKey:)` directly instead, which correctly defaults absent keys to false)
    Islet/Notch/IslandResolver.swift lines 48, 68, 97 (confirms the `.weatherExpanded` presentation case and `selectedView == .weather` resolution already exist and are untouched by this task)
    Islet/SettingsView.swift lines 145-186 (Section("Activities")/Section("Fullscreen") — the exact @AppStorage-plus-Toggle-in-Section template to mirror for the new Weather section)
  </read_first>
  <action>
    In NotchWindowController.swift: (1) In `startLocationOnce()`'s completion closure, alongside the existing `self?.lastLocation = location` / `self?.refreshWeather()` calls, add (guarded by `if let location`) `self?.weatherService.resolvePlaceName(for: location) { [weak self] name in self?.outfitState.locationName = name }` — resolved once per location fetch, not re-resolved on every 15-minute refresh tick. (2) Rewrite `refreshWeather()`'s body to call `weatherService.fetchCurrentAndForecast(latitude:longitude:completion:)` instead of the removed `fetchCurrent`, writing BOTH `self?.outfitState.weather = glance` AND `self?.outfitState.forecast = forecast` from the SAME completion callback (atomic delivery — populate `forecast` unconditionally regardless of the toggle state; the toggle only gates what `NotchPillView` renders, not what the controller fetches). (3) In `positionAndShow`, add a 4th union member: `let weatherExtendedFrame = expandedNotchFrame(collapsed: collapsedFrame, expandedSize: CGSize(width: expandedSize.width, height: NotchPillView.weatherExtendedContentHeight + NotchPillView.switcherRowHeight))`, and fold it into the existing `panelFrame = expandedFrame.union(wings).union(onboardingFrame).union(trayFrame)` chain as `.union(weatherExtendedFrame)` — include it unconditionally (same static-upper-bound approach `trayFrame` already uses, no toggle-gating needed for a reservation-only union member). (4) In `visibleContentZone()`, insert a new branch ahead of the existing final `else` (after the `.trayExpanded` branch): `else if case .weatherExpanded = presentationState.presentation, UserDefaults.standard.bool(forKey: ActivitySettings.weatherExtendedKey) { contentSize = CGSize(width: expandedSize.width, height: NotchPillView.weatherExtendedContentHeight + switcherHeight) }`. In SettingsView.swift: (5) add `@AppStorage(ActivitySettings.weatherExtendedKey) private var weatherExtended = false` near the other `@AppStorage` declarations, and a new `Section("Weather") { Toggle("Extended forecast", isOn: $weatherExtended) }` inside `generalSection`'s `Form`, placed alongside `Section("Activities")`/`Section("Fullscreen")` — no `.onChange` handler needed (plain app-owned `@AppStorage`, same as every Activities toggle). Steps (3) and (4) implement the geometry three-site rule together with Task 1's `blobShape` `height:` override — do not consider this plan functionally complete until the Task 3 on-device checkpoint confirms all three agree (Pitfall 4 / this project's CR-01/WR-02-precedented bug class).
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fetchCurrentAndForecast" Islet/Notch/NotchWindowController.swift` returns 1
    - `grep -c "resolvePlaceName" Islet/Notch/NotchWindowController.swift` returns 1
    - `grep -c "weatherExtendedFrame" Islet/Notch/NotchWindowController.swift` returns 1
    - `grep -c "weatherExpanded" Islet/Notch/NotchWindowController.swift` returns at least 1 (the new visibleContentZone branch)
    - `grep -c "Extended forecast" Islet/SettingsView.swift` returns 1
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors
  </acceptance_criteria>
  <done>refreshWeather uses the combined fetch and writes weather+forecast atomically, location name resolves once per location fetch, positionAndShow's union and visibleContentZone's branch both account for weatherExtendedContentHeight, Settings has the Weather > Extended forecast toggle, build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device UAT — location/H-L, live toggle, click-through trace, permission-denial degrade</name>
  <files>Islet/Notch/NotchPillView.swift, Islet/Notch/NotchWindowController.swift, Islet/SettingsView.swift</files>
  <action>Build and run Islet with Task 1 and Task 2's changes in place, then execute the 5-step on-device verification below. No further code changes in this task — it is verification-only, confirming the geometry three-site rule (blobShape height override + positionAndShow union + visibleContentZone branch) agrees in practice.</action>
  <what-built>
    The compact Weather card now shows a resolved location name (or "Local" fallback) and high/low alongside the existing icon/temperature. A new Settings "Weather &gt; Extended forecast" toggle live-switches to a forecast row (up to 5 day-chips). Panel geometry (`positionAndShow` union) and click-through hit-testing (`visibleContentZone()`) were updated in lockstep with the new content height.
  </what-built>
  <how-to-verify>
    1. Build and run Islet, open the Weather tab (switcher icon) — confirm the location name (or "Local"), icon, current temperature, and H/L readout all render, with no visible layout shift as the real location name resolves in.
    2. Open Settings > General, toggle "Extended forecast" on while the Weather tab happens to be open — confirm the card animates (spring) to the taller extended size and a forecast row appears. Note the exact chip count that fits cleanly without crowding (5 is the built default; drop to 4 if 5 visibly crowds on real hardware).
    3. With the extended toggle ON, run the hover -&gt; expand -&gt; move-down click-through trace: hover to expand, click into the Weather tab, then move the pointer down across the new forecast-row area and confirm no misclicks, no dead zones, and no premature collapse (CR-01/WR-02 regression check).
    4. Toggle "Extended forecast" off — confirm the card animates back to the compact size immediately, no relaunch needed.
    5. In System Settings, revoke Location permission for Islet (or otherwise simulate denial) and relaunch — confirm the Weather tab shows the existing "Wetter nicht verfuegbar" fallback with no crash.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (exact forecast day count that fit, any clipping/click-through problems, weatherExtendedContentHeight value that needed tuning).</resume-signal>
  <verify>
    <human-check>5-step on-device verification above, including the mandatory hover→expand→move-down click-through trace</human-check>
  </verify>
  <done>All 5 checks pass and the checkpoint is approved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| OS mouse-event stream -> NotchPanel | `visibleContentZone()`'s click-through hit-test rect gates which pixels the click-through panel treats as interactive vs. pass-through |
| UserDefaults toggle -> panel geometry decision | `weatherExtendedKey`, read independently by SwiftUI (`NotchPillView`) for rendering and AppKit (`NotchWindowController`) for panel-frame/hit-test sizing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04 | Denial of Service | `visibleContentZone()`'s new `.weatherExpanded` branch / `positionAndShow`'s `weatherExtendedFrame` union | mitigate | The `blobShape` height override (Task 1), the panel-frame union member, and the click-zone branch (Task 2) are wired in this same plan and re-verified via the on-device hover->expand->move-down trace (Task 3 checkpoint) — closes off the CR-01/WR-02 click-swallowing/dead-zone regression class this project has hit twice before |
| T-33-05 | Tampering | `@AppStorage(weatherExtendedKey)` UserDefaults toggle, read independently by SwiftUI and AppKit | accept | Local-only user preference, single shared UserDefaults key string constant (no drift risk since both read sites reference `ActivitySettings.weatherExtendedKey`), no privilege escalation surface |
| T-33-06 | Information Disclosure | Resolved location name rendered directly in the Weather card UI | accept | Same location precision already implicitly shown via current-weather conditions since Phase 14; the notch panel is local-only, never transmitted off-device by this feature |
| T-33-SC | Tampering | npm/pip/cargo installs | n/a | Zero third-party packages this plan |
</threat_model>

<verification>
- `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors after both code tasks
- Manual Cmd-U (full IsletTests suite) passes
- On-device checkpoint (Task 3) approved: location/H-L render, live toggle animates, click-through trace clean, permission-denial degrades silently
</verification>

<success_criteria>
- WEATHER-01: compact card shows location, condition icon, current temperature, and high/low
- WEATHER-02: Settings toggle live-switches to an extended forecast row, no relaunch required
- No CR-01/WR-02-class click-through regression on the new extended geometry
- Weather still degrades silently (no crash) on permission denial
</success_criteria>

<output>
Create `.planning/phases/33-weather-widget-redesign/33-02-SUMMARY.md` when done
</output>
