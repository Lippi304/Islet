---
phase: 15-architecture-refactor-notchwindowcontroller-notchpillview-de
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Notch/NotchGeometry.swift
  - Islet/Notch/NotchPillView.swift
autonomous: true
requirements: [P15-ITEM1, P15-ITEM2]

must_haves:
  truths:
    - "D-01: This plan is scoped to Phase 15's mechanical/low-risk fixes only — NotchWindowController's coordinator extraction is Phase 16, not touched here"
    - "D-02: expandedNotchFrame and wingsFrame produce byte-identical output to before the refactor (pure DRY, no behavior change)"
    - "D-02: expandedIsland, mediaExpanded, and mediaUnavailable render identically to before, sharing one blobShape() helper"
    - "collapsedIsland remains its own distinct case (DEBUG tint, hover scale, dev offset) — not folded into blobShape()"
    - "mediaExpanded's tap-to-toggle stays present on its top HStack — no gesture recognizer ends up sitting above the transport buttons"
    - "mediaExpanded's content stays top-pinned (alignment: .top) inside blobShape — camera-clearance padding is not re-centered by the extraction"
  artifacts:
    - path: "Islet/Notch/NotchGeometry.swift"
      provides: "private topPinnedFrame(collapsed:size:) helper backing expandedNotchFrame/wingsFrame"
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "private blobShape(topCornerRadius:bottomCornerRadius:alignment:content:) helper used by expandedIsland/mediaExpanded/mediaUnavailable"
  key_links:
    - from: "Islet/Notch/NotchGeometry.swift:expandedNotchFrame"
      to: "topPinnedFrame(collapsed:size:)"
      via: "single-line delegating call"
      pattern: "topPinnedFrame\\(collapsed:"
    - from: "Islet/Notch/NotchPillView.swift:expandedIsland"
      to: "blobShape(topCornerRadius:bottomCornerRadius:content:)"
      via: "single-line delegating call"
      pattern: "blobShape\\("
---

<objective>
DRY up two independent, byte-for-byte code duplications flagged by the Phase 15 architecture
audit: (1) `NotchGeometry.swift`'s `expandedNotchFrame`/`wingsFrame` share an identical body, and
(2) `NotchPillView.swift`'s `expandedIsland`/`mediaExpanded`/`mediaUnavailable` each repeat the
same `NotchShape → .fill → .matchedGeometryEffect → .frame → .overlay → .onTapGesture` chain that
`wingsShape(content:)` already solved for the three wing variants ("Finding 12" precedent).

Purpose: eliminate copy-paste drift risk with zero product-behavior change (CONTEXT.md D-02) —
both extractions keep every existing public signature and every existing render output identical.
Output: `topPinnedFrame(collapsed:size:)` in NotchGeometry.swift; `blobShape(topCornerRadius:
bottomCornerRadius:content:)` in NotchPillView.swift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-architecture-refactor-notchwindowcontroller-notchpillview-de/15-CONTEXT.md
@.planning/phases/15-architecture-refactor-notchwindowcontroller-notchpillview-de/15-PATTERNS.md

Regression gate: `xcodebuild build-for-testing` compiles the app + IsletTests target (does not
run tests — this project's `xcodebuild test` hangs headlessly because tests boot the full
Islet.app which opens an NSPanel/MediaRemote/IOBluetooth; see project memory
`xcodebuild-test-headless-hang`). No new test files are needed for this plan — the existing
`IsletTests/NotchGeometryTests.swift` (13 cases) is the regression gate for Task 1, and Xcode's
DEBUG `#Preview` blocks at the bottom of NotchPillView.swift are the compile-time regression gate
for Task 2.
</context>

<interfaces>
<!-- Both fixes are self-referential extractions — no cross-file interface to consume. -->

Current duplicate bodies to collapse (`NotchGeometry.swift:64-79`):
```swift
func expandedNotchFrame(collapsed: CGRect, expandedSize: CGSize) -> CGRect {
    let x = collapsed.midX - expandedSize.width / 2
    let y = collapsed.maxY - expandedSize.height
    return CGRect(x: x, y: y, width: expandedSize.width, height: expandedSize.height)
}
func wingsFrame(collapsed: CGRect, wingsSize: CGSize) -> CGRect {
    let x = collapsed.midX - wingsSize.width / 2
    let y = collapsed.maxY - wingsSize.height
    return CGRect(x: x, y: y, width: wingsSize.width, height: wingsSize.height)
}
```

Analog to mirror (`NotchPillView.swift:221-233`, `wingsShape(content:)`):
```swift
private func wingsShape<Content: View>(@ViewBuilder content: () -> Content) -> some View {
    NotchShape(topCornerRadius: 6, bottomCornerRadius: 6)
        .fill(Color.black)
        .matchedGeometryEffect(id: "island", in: ns)
        .frame(width: Self.wingsSize.width, height: Self.wingsSize.height)
        .overlay(content().frame(width: Self.wingsSize.width, height: Self.wingsSize.height))
        .onTapGesture { onClick() }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DRY expandedNotchFrame/wingsFrame via topPinnedFrame</name>
  <files>Islet/Notch/NotchGeometry.swift</files>
  <read_first>
    Islet/Notch/NotchGeometry.swift (whole file, 79 lines)
    IsletTests/NotchGeometryTests.swift (whole file — the regression gate; do not modify it)
  </read_first>
  <action>
    Extract a private func `topPinnedFrame(collapsed: CGRect, size: CGSize) -> CGRect` containing
    the shared body currently duplicated in `expandedNotchFrame`/`wingsFrame` (lines 64-79): `let x
    = collapsed.midX - size.width / 2`, `let y = collapsed.maxY - size.height`, `return
    CGRect(x: x, y: y, width: size.width, height: size.height)`. Rewrite `expandedNotchFrame(
    collapsed:expandedSize:)` as a one-line wrapper: `topPinnedFrame(collapsed: collapsed, size:
    expandedSize)`. Rewrite `wingsFrame(collapsed:wingsSize:)` the same way: `topPinnedFrame(
    collapsed: collapsed, size: wingsSize)`. Keep both function signatures, external parameter
    labels (`expandedSize:`, `wingsSize:`), and return types byte-identical to today — every call
    site (`NotchWindowController.swift`'s `positionAndShow`, calling both functions) must compile
    unchanged. Keep the existing doc comments above each function as-is; add one short comment
    above `topPinnedFrame` noting it is the shared body per CONTEXT.md item 1. Do not touch
    `hasNotch`, `notchSize`, or `notchFrame` (lines 1-58) — they are unrelated to this dup.
  </action>
  <verify>
    <automated>xcodebuild build-for-testing -project Islet.xcodeproj -scheme Islet -configuration Debug -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - source: NotchGeometry.swift contains `private func topPinnedFrame(collapsed: CGRect, size: CGSize) -> CGRect`
    - source: `expandedNotchFrame`'s body is a single-line call to `topPinnedFrame(collapsed: collapsed, size: expandedSize)`
    - source: `wingsFrame`'s body is a single-line call to `topPinnedFrame(collapsed: collapsed, size: wingsSize)`
    - test-command: run the existing 13 `IsletTests/NotchGeometryTests.swift` cases via Xcode Cmd-U (or `xcodebuild test -only-testing:IsletTests/NotchGeometryTests` — manual, since headless `xcodebuild test` hangs in this project) — all pass unmodified, proving byte-identical output
  </acceptance_criteria>
  <done>expandedNotchFrame/wingsFrame delegate to one topPinnedFrame helper; NotchGeometryTests.swift's existing width/coordinate-flip assertions still hold with zero test-file changes.</done>
</task>

<task type="auto">
  <name>Task 2: Extract blobShape() for expandedIsland/mediaExpanded/mediaUnavailable</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift (whole file, 846 lines — pay particular attention to lines
    166-233 [collapsedIsland/expandedIsland/wingsShape], 476-534 [mediaExpanded], 551-564
    [mediaUnavailable], and the #Preview blocks at the bottom which are the compile-time
    regression gate)
  </read_first>
  <action>
    Add a new private func `blobShape&lt;Content: View&gt;(topCornerRadius: CGFloat,
    bottomCornerRadius: CGFloat, alignment: Alignment = .center, @ViewBuilder content: () -&gt;
    Content) -&gt; some View` mirroring `wingsShape(content:)` at lines 221-233, but with an added
    `alignment` parameter (default `.center`, matching today's `expandedIsland`/`mediaUnavailable`
    default `.overlay(content())` centering): `NotchShape(topCornerRadius: topCornerRadius,
    bottomCornerRadius: bottomCornerRadius).fill(Color.black).matchedGeometryEffect(id: "island",
    in: ns).frame(width: Self.expandedSize.width, height: Self.expandedSize.height).overlay(
    alignment: alignment) { content() }.onTapGesture { onClick() }` — this is the executable form
    of SwiftUI's `overlay(alignment:content:)` (a trailing-closure content builder attached to the
    shape view, NOT a bare content value passed as an argument). Rewrite `expandedIsland` (lines
    194-214) to return `blobShape(topCornerRadius: 6, bottomCornerRadius: 20) { <the existing
    HStack overlay content, unchanged> }` (omit `alignment:` — defaults to `.center`, matching
    today's plain `.overlay(content())`), dropping its own duplicate `NotchShape`/`.fill`/
    `.matchedGeometryEffect`/`.frame`/`.onTapGesture` chain. Rewrite `mediaUnavailable` (lines
    551-564) the same way (also omitting `alignment:`), dropping its duplicate chain and keeping
    its `Text("Now Playing nicht verfügbar")` overlay content unchanged. For `mediaExpanded` (lines
    476-534) — CRITICAL nuance verified this session, and the reason `blobShape` needs the
    `alignment` parameter at all: today's code deliberately uses `.overlay(alignment: .top) {
    VStack {...} }` instead of default-center — the code's own adjacent comment explains that
    default `.overlay` CENTERS, which with ~84pt content in a 128pt blob would leave only ~22pt top
    clearance, not enough to clear the 32pt camera band; top-pinning makes the clearance exact.
    Route `mediaExpanded` through `blobShape(topCornerRadius: 6, bottomCornerRadius: 20, alignment:
    .top) { <the existing VStack content that was previously inside `.overlay(alignment: .top) {
    ... }`, unchanged, INCLUDING its own inner .onTapGesture on the top row and its own
    `.padding(.top, 32)`/`.padding(.bottom, 12)`/`.padding(.horizontal, 19)` modifiers> }` —
    passing `alignment: .top` explicitly preserves today's camera-clearance pinning exactly; do NOT
    let this call site fall back to the default `.center`. Also keep mediaExpanded's own inner
    `.onTapGesture` exactly where it is (scoped to the inner top-row `VStack`/`HStack` at line 513,
    NOT the outer shape — Finding 15's invariant: no gesture recognizer sits above the transport
    buttons at lines 518-528). blobShape's outer `.onTapGesture` and mediaExpanded's inner
    `.onTapGesture` both call the identical `onClick()` closure, so having both attached introduces
    no behavior conflict (same action fires either way) and the transport buttons at lines 518-528
    remain outside BOTH tap-scoped regions. Leave `collapsedIsland` (lines 170-184) completely
    untouched — it keeps its own `NotchShape()` / `.fill(collapsedFill)` / `.scaleEffect(...)` /
    `.offset(y: devOffset)` chain, explicitly excluded per CONTEXT.md's "not a clean fit" nuance
    (DEBUG-only tint, hover scale, dev offset that the other three cases lack). Do not modify
    `wingsShape`, `wings(for:)`, `mediaWings(_:art:)`, `deviceWings(for:)`, or `EqualizerBars` —
    out of scope for this task (EqualizerBars is Plan 15-05).
  </action>
  <verify>
    <automated>xcodebuild build-for-testing -project Islet.xcodeproj -scheme Islet -configuration Debug -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - source: NotchPillView.swift contains `private func blobShape&lt;Content: View&gt;(topCornerRadius: CGFloat, bottomCornerRadius: CGFloat, alignment: Alignment = .center` with an `@ViewBuilder content` parameter
    - source: `blobShape`'s body attaches the overlay as `.overlay(alignment: alignment) { content() }` (not a bare `.overlay(content())`)
    - source: `expandedIsland`'s body calls `blobShape(topCornerRadius: 6, bottomCornerRadius: 20)` with no explicit `alignment:` argument (uses the `.center` default)
    - source: `mediaUnavailable`'s body calls `blobShape(topCornerRadius: 6, bottomCornerRadius: 20)` with no explicit `alignment:` argument
    - source: `mediaExpanded`'s body calls `blobShape(topCornerRadius: 6, bottomCornerRadius: 20, alignment: .top)` — explicit `.top`, proving the camera-clearance pinning survives the extraction — AND still contains its own `.onTapGesture { onClick() }` attached to the top HStack (near line 513) AND still contains `.padding(.top, 32)` on the moved content
    - source: `collapsedIsland`'s body still contains `NotchShape()` (no radius args), `.fill(collapsedFill)`, `.scaleEffect(`, `.offset(y: devOffset)` — unchanged
    - behavior: Xcode canvas `#Preview("Collapsed")`, `#Preview("Expanded")`, `#Preview("Media Expanded")`, `#Preview("Unavailable")` all render without a compile/runtime error (manual Xcode canvas check — not scriptable, but the automated build already proves they compile)
    - behavior: in the Xcode canvas `#Preview("Media Expanded")`, the album art / title / EqualizerBars row visually starts below the camera band (same ~32pt top clearance as before the extraction) — not re-centered toward the middle of the blob (manual Xcode canvas check; this is the concrete, checkable gate for the zero-behavior-change requirement on this item)
  </acceptance_criteria>
  <done>expandedIsland/mediaExpanded/mediaUnavailable share one blobShape() helper identical in output to their prior individual chains; mediaExpanded's alignment: .top argument preserves its camera-clearance pinning; collapsedIsland stays its own distinct case; build succeeds; all four affected DEBUG previews still compile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure internal refactor of geometry math and SwiftUI view composition — no external input, no network, no persistence, no new code path reachable from outside the process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-00a | N/A | NotchGeometry.swift / NotchPillView.swift (this plan) | accept | Pure structural extraction — same runtime output, different code shape (CONTEXT.md D-02). No trust boundary is crossed by either change; nothing here touches external input, credentials, or persisted data. |
</threat_model>

<verification>
`xcodebuild build-for-testing -project Islet.xcodeproj -scheme Islet -configuration Debug
-destination 'platform=macOS'` succeeds after both tasks. Existing `NotchGeometryTests.swift` (13
cases) passes unmodified via Xcode Cmd-U. All 8 `#Preview` blocks in NotchPillView.swift compile
and render in the Xcode canvas.
</verification>

<success_criteria>
Zero duplicate frame-geometry formula; zero duplicate blob-shape chain across
expandedIsland/mediaExpanded/mediaUnavailable; collapsedIsland stays intentionally distinct;
mediaExpanded's tap-gesture scoping invariant (no recognizer above the transport buttons) holds;
no product-visible behavior change.
</success_criteria>

<output>
Create `.planning/phases/15-architecture-refactor-notchwindowcontroller-notchpillview-de/15-01-SUMMARY.md` when done
</output>
