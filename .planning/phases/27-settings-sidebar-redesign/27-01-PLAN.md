---
phase: 27
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/ActivitySettings.swift
  - IsletTests/ActivitySettingsTests.swift
  - Islet/AppDelegate.swift
autonomous: true
requirements: [VISUAL-03]

must_haves:
  truths:
    - "MaterialStyle and all 3 new per-element accent indices parse from UserDefaults with a clamp-to-default fallback, never crashing on a corrupted/out-of-range value"
    - "An existing user's accent look is preserved across the upgrade — the 3 new per-element accent keys seed once from the old single accentIndexKey on first launch, without visibly resetting to the default swatch"
  artifacts:
    - path: "Islet/ActivitySettings.swift"
      provides: "MaterialStyle enum, 4 new @AppStorage keys, 4 new EnvironmentKeys, migrateLegacyAccentIfNeeded()"
      contains: "enum MaterialStyle"
    - path: "IsletTests/ActivitySettingsTests.swift"
      provides: "pure-logic clamp + migration test coverage"
      min_lines: 20
  key_links:
    - from: "Islet/AppDelegate.swift"
      to: "Islet/ActivitySettings.swift"
      via: "migrateLegacyAccentIfNeeded() call before controller.start()"
      pattern: "migrateLegacyAccentIfNeeded"
---

<objective>
Establish the data-model foundation for Phase 27's Theming feature (VISUAL-03 part 1/2): a `MaterialStyle` enum + 4 new `@AppStorage` keys + 4 new `EnvironmentKey`s in `Islet/ActivitySettings.swift`, plus a one-time migration function that seeds the 3 new per-element accent keys from the existing single `accentIndexKey` so no existing user's accent silently resets on upgrade (D-08).

Purpose: This is the interface-contract plan — Plan 02 (NotchPillView/NotchWindowController) and Plan 03 (SettingsView) both consume the keys/EnvironmentKeys/enum defined here. Landing this first (Wave 1) lets Plans 02/03 run in parallel in Wave 2 with zero file overlap.
Output: `ActivitySettings.swift` extended with the Theming data model; `ActivitySettingsTests.swift` (new file) pinning the clamp-to-default and migration behavior; `AppDelegate.swift` wired to run the migration once at launch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-settings-sidebar-redesign/27-CONTEXT.md
@.planning/phases/27-settings-sidebar-redesign/27-RESEARCH.md
</context>

<interfaces>
<!-- Current Islet/ActivitySettings.swift (full file, 53 lines) — the exact structure this task extends -->

```swift
enum ActivitySettings {
    static let chargingKey   = "activity.charging"
    static let nowPlayingKey = "activity.nowPlaying"
    static let songChangeToastKey = "activity.songChangeToast"
    static let deviceKey     = "activity.device"
    static let accentIndexKey = "accentIndex"   // KEEP — becomes the legacy migration-source key
    static let hideInFullscreenKey = "notch.hideInFullscreen"
    static let onboardingCompletedKey = "onboarding.completed"

    static let palette: [Color] = [.white, .blue, .green, .orange, .pink, .purple]
    static let defaultAccentIndex = 0

    static func accent(for index: Int) -> Color {
        palette.indices.contains(index) ? palette[index] : palette[defaultAccentIndex]
    }
}

private struct ActivityAccentKey: EnvironmentKey { static let defaultValue: Color = .white }
extension EnvironmentValues {
    var activityAccent: Color {
        get { self[ActivityAccentKey.self] }
        set { self[ActivityAccentKey.self] = newValue }
    }
}
```

Test-style analogs to mirror (both already in `IsletTests/`):
- `LicenseStateTests.swift` — plain XCTest, no fakes needed for pure value transforms.
- `TrialManagerTests.swift` — the exact `UserDefaults(suiteName: "SomeTests-\(UUID().uuidString)")!` isolation pattern for any test that touches UserDefaults, so no real `UserDefaults.standard` I/O ever runs during a test.

`Islet/AppDelegate.swift` line 26-29 ordering precedent (the pattern this plan's migration call must follow):
```swift
// TRIAL-01/D-10: must run before controller.start() so LicenseState.shared
// ...
let isFirstLaunch = TrialManager.shared.recordFirstLaunchIfNeeded()
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MaterialStyle enum + 4 new keys/EnvironmentKeys in ActivitySettings.swift</name>
  <files>Islet/ActivitySettings.swift, IsletTests/ActivitySettingsTests.swift</files>
  <read_first>
    Islet/ActivitySettings.swift (full file — the exact block being extended, see interfaces above)
    IsletTests/LicenseStateTests.swift (plain-XCTest style to mirror — no fakes needed here)
  </read_first>
  <behavior>
    - `ActivitySettings.MaterialStyle(rawValue: "gradient") == .gradient`
    - `ActivitySettings.MaterialStyle(rawValue: "solidBlack") == .solidBlack`
    - `ActivitySettings.MaterialStyle(rawValue: "corrupted-value") == nil` (callers apply `?? .gradient`)
    - `ActivitySettings.accent(for: 999) == ActivitySettings.palette[ActivitySettings.defaultAccentIndex]` (regression pin of the existing clamp, now load-bearing for 3 more call sites)
    - `ActivitySettings.materialStyleKey == "theming.materialStyle"`, `ActivitySettings.nowPlayingAccentKey == "accent.nowPlaying"`, `ActivitySettings.chargingAccentKey == "accent.charging"`, `ActivitySettings.deviceAccentKey == "accent.device"`
  </behavior>
  <action>
    In `Islet/ActivitySettings.swift`, add alongside the existing key block (after `accentIndexKey`): `enum MaterialStyle: String, CaseIterable { case gradient, solidBlack }`, `static let materialStyleKey = "theming.materialStyle"`, `static let nowPlayingAccentKey = "accent.nowPlaying"`, `static let chargingAccentKey = "accent.charging"`, `static let deviceAccentKey = "accent.device"`. Update the existing `accentIndexKey`'s comment to note it is now read ONLY as the legacy migration source (Task 2), never as a live rendering key. Do NOT touch `palette`, `defaultAccentIndex`, or `accent(for:)` — unchanged.

    Replace the single `ActivityAccentKey`/`activityAccent` `EnvironmentKey` block with 4 keys following the identical private-struct + computed-property idiom: `NowPlayingAccentKey`/`nowPlayingAccent`, `ChargingAccentKey`/`chargingAccent`, `DeviceAccentKey`/`deviceAccent` (each `EnvironmentKey` with `defaultValue: Color = .white`, matching today's default), plus `IslandMaterialStyleKey`/`islandMaterialStyle` (`EnvironmentKey` with `defaultValue: MaterialStyle = .gradient`). Update the file-header doc comment above this block (currently describing the single `activityAccent` mechanism) to describe the 3 per-element accent keys + the material-style key instead.

    Create `IsletTests/ActivitySettingsTests.swift` (new file) mirroring `LicenseStateTests.swift`'s plain-XCTest-no-fakes style — every function under test here is a pure value transform, no injected dependency needed. Add one test method per behavior listed above (5 methods).
  </action>
  <acceptance_criteria>
    - `grep -v '^\s*//' Islet/ActivitySettings.swift | grep -c "case gradient, solidBlack"` == 1
    - `grep -c "static let materialStyleKey" Islet/ActivitySettings.swift` == 1
    - `grep -c "static let nowPlayingAccentKey\|static let chargingAccentKey\|static let deviceAccentKey" Islet/ActivitySettings.swift` == 3
    - `grep -v '^\s*//' Islet/ActivitySettings.swift | grep -c "var nowPlayingAccent: Color\|var chargingAccent: Color\|var deviceAccent: Color\|var islandMaterialStyle: MaterialStyle"` == 4
    - `IsletTests/ActivitySettingsTests.swift` exists and contains 5 `func test` methods covering the 5 behaviors above
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` exits 0
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <done>ActivitySettings.swift exposes MaterialStyle + 4 new keys + 4 new EnvironmentKeys; ActivitySettingsTests.swift exists with 5 passing pure-logic tests; project builds clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: migrateLegacyAccentIfNeeded() + AppDelegate launch wiring</name>
  <files>Islet/ActivitySettings.swift, Islet/AppDelegate.swift, IsletTests/ActivitySettingsTests.swift</files>
  <read_first>
    Islet/ActivitySettings.swift (post-Task-1 state — the 4 keys/enum this task's function reads/writes)
    Islet/AppDelegate.swift (full file — the exact launch-ordering precedent at lines 25-29 this task's call site must follow)
    IsletTests/TrialManagerTests.swift (the `UserDefaults(suiteName: "...\(UUID().uuidString)")!` isolation pattern to reuse verbatim — do not touch UserDefaults.standard in a test)
  </read_first>
  <behavior>
    - Given a fresh `UserDefaults(suiteName:)` instance with NO keys set at all, calling `ActivitySettings.migrateLegacyAccentIfNeeded(defaults:)` writes nothing (fresh install — the 3 new keys' own defaults apply; there is no legacy value to seed from).
    - Given a suite with only the legacy `accentIndexKey` set to `3` and none of the 3 new keys set (simulating an existing pre-Phase-27 user), calling `migrateLegacyAccentIfNeeded(defaults:)` writes `3` to all of `nowPlayingAccentKey`, `chargingAccentKey`, `deviceAccentKey`.
    - Given a suite where the 3 new keys are already set (already migrated, or the user already changed one in the new Theming section), calling `migrateLegacyAccentIfNeeded(defaults:)` again does NOT overwrite them (idempotent — never clobbers a later Theming-section change).
  </behavior>
  <action>
    Add `static func migrateLegacyAccentIfNeeded(defaults: UserDefaults = .standard)` to `Islet/ActivitySettings.swift`: guard that none of `nowPlayingAccentKey`/`chargingAccentKey`/`deviceAccentKey` already has a value (`defaults.object(forKey:) == nil` for all 3) — if any is already set, return immediately (idempotent). Then guard `let legacy = defaults.object(forKey: accentIndexKey) as? Int` — if the legacy key is absent or not an Int, return (fresh install, nothing to seed). Write `legacy` to all 3 new keys via `defaults.set(legacy, forKey:)`.

    In `Islet/AppDelegate.swift`, call `ActivitySettings.migrateLegacyAccentIfNeeded()` inside `applicationDidFinishLaunching` BEFORE `controller.start(isFirstLaunch:)` — mirror the file's existing ordering comment/pattern at line 26 for `TrialManager.shared.recordFirstLaunchIfNeeded()` (same "must run before controller.start()" constraint, since the controller's panel-creation path reads the 3 accent keys on first show).

    Append the 3 behaviors above as test methods to `IsletTests/ActivitySettingsTests.swift`, each using an isolated `UserDefaults(suiteName: "ActivitySettingsTests-\(UUID().uuidString)")!` instance exactly as `TrialManagerTests.swift` does.
  </action>
  <acceptance_criteria>
    - `grep -c "static func migrateLegacyAccentIfNeeded" Islet/ActivitySettings.swift` == 1
    - `grep -n "migrateLegacyAccentIfNeeded\|controller.start(isFirstLaunch" Islet/AppDelegate.swift` shows the migration call's line number BEFORE the `controller.start(isFirstLaunch:` line number
    - `IsletTests/ActivitySettingsTests.swift` contains 3 additional `func test` methods (fresh-install no-op, seed-from-legacy, idempotent-no-clobber), each constructing its own `UserDefaults(suiteName:)` instance
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` exits 0
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
    <human-check>Cmd-U in Xcode (IsletTests scheme) — confirm all 8 ActivitySettingsTests methods pass. Manual per this project's established constraint: `xcodebuild test` hangs (hosts the full NSPanel/MediaRemote/IOBluetooth stack).</human-check>
  </verify>
  <done>migrateLegacyAccentIfNeeded() exists, runs once at launch before controller.start(), is idempotent, and is covered by 3 isolated-UserDefaults tests; Debug build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| UserDefaults (local, user/`defaults write`-editable) → app read paths | Every new key this plan introduces is read back by app code; a corrupted or tampered value must never crash or index out of bounds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | `ActivitySettings.MaterialStyle(rawValue:)` parsing a corrupted/unknown string | mitigate | Every read site applies `?? .gradient`; Task 1's test pins `MaterialStyle(rawValue: "corrupted-value") == nil` so the fallback is exercised, not assumed |
| T-27-02 | Tampering | Reading the 3 new accent-index UserDefaults keys | mitigate | Routed exclusively through the existing `ActivitySettings.accent(for:)` clamp (`palette.indices.contains(index) ? ... : palette[defaultAccentIndex]`) — never a raw `palette[index]` subscript |
| T-27-03 | Tampering | `migrateLegacyAccentIfNeeded` reading a corrupted (non-Int) legacy `accentIndexKey` value | mitigate | `as? Int` optional-cast guard — silently no-ops on type mismatch rather than force-casting/crashing |
</threat_model>

<verification>
Both tasks gate on `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` exiting 0. Task 2 additionally requires a manual Cmd-U pass confirming all `ActivitySettingsTests` methods pass (this project's established constraint: `xcodebuild test` hangs because the test target hosts the full app).
</verification>

<success_criteria>
- `Islet/ActivitySettings.swift` exposes `MaterialStyle`, 4 new `@AppStorage` keys, 4 new `EnvironmentKey`s, and `migrateLegacyAccentIfNeeded()`.
- `IsletTests/ActivitySettingsTests.swift` exists with 8 passing tests covering clamp-to-default parsing and migration seeding/idempotency.
- `AppDelegate.applicationDidFinishLaunching` runs the migration before `controller.start(isFirstLaunch:)`.
- Debug build succeeds with zero warnings from these changes.
</success_criteria>

<output>
Create `.planning/phases/27-settings-sidebar-redesign/27-01-SUMMARY.md` when done
</output>
