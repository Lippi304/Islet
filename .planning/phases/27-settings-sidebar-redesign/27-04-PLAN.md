---
phase: 27
plan: 04
type: execute
wave: 3
depends_on: ["27-02", "27-03"]
files_modified: []
autonomous: false
requirements: [SETTINGS-01, VISUAL-03]

must_haves:
  truths:
    - "Debug and Release builds both succeed with zero remaining references to the deleted single-value accent/material symbols"
    - "On-device UAT confirms all 4 sidebar sections, live theming across pill/expanded/wings, and no section-switch state staleness"
  artifacts: []
  key_links: []
---

<objective>
Close out Phase 27: verify the full build (Debug + Release) across every file Plans 01-03 touched, confirm no dead references to the deleted single-value `activityAccent`/`accentIndexKey` mechanism remain anywhere in `Islet/`, and run the on-device UAT pass that is this phase's only way to validate the SwiftUI view-hierarchy behavior (SETTINGS-01) and live-theming rendering (VISUAL-03) per `27-VALIDATION.md`'s Manual-Only Verifications table.

Purpose: Plans 02 and 03 changed the same underlying preference pipeline from two different ends (render layer, UI layer) — this plan is the integration point that proves they actually agree, both structurally (build/grep) and behaviorally (on-device).
Output: A verified, working Settings Sidebar Redesign + Theming feature, ready for `/gsd:verify-work 27`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-settings-sidebar-redesign/27-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build gate + dead-reference grep sweep</name>
  <files></files>
  <read_first>
    Islet/SettingsView.swift (post-27-03 final state)
    Islet/ActivitySettings.swift (post-27-01 final state)
    Islet/Notch/NotchPillView.swift (post-27-02 final state)
    Islet/Notch/NotchWindowController.swift (post-27-02 final state)
    Islet/Diagnostics.swift (post-27-03 final state)
  </read_first>
  <action>
    Run `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` and `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Release` (Release-parity discipline — this project has a documented prior Release-only library-validation crash that Debug never surfaces). Grep the whole `Islet/` tree for zero remaining references to the 3 deleted single-value symbols: `activityAccent` (replaced by the 3 per-element EnvironmentKeys), `appliedAccentIndex` (replaced by `AppliedTheme`), and the `SettingsView`-level `@AppStorage(ActivitySettings.accentIndexKey) private var accentIndex` binding (the constant `accentIndexKey` itself legitimately remains as the Plan 01 migration source — only the now-dead `SettingsView` binding to it must be gone). Confirm `NavigationView` (the deprecated pre-`NavigationSplitView` API) appears 0 times anywhere in `Islet/`.
  </action>
  <acceptance_criteria>
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` exits 0
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Release` exits 0
    - `grep -rn "activityAccent" Islet/` returns 0 matches
    - `grep -rn "appliedAccentIndex" Islet/` returns 0 matches
    - `grep -rn "@AppStorage(ActivitySettings.accentIndexKey)" Islet/` returns 0 matches
    - `grep -rn '\bNavigationView\b' Islet/` returns 0 matches (word-boundary grep so `NavigationSplitView` is never a false positive)
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug && xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Release && ! grep -rn "activityAccent\|appliedAccentIndex" Islet/</automated>
  </verify>
  <done>Debug and Release builds both succeed; zero dead references to the deleted single-value accent/material symbols remain in Islet/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device UAT — sidebar sections, section-switch state sync, live theming</name>
  <files></files>
  <action>
    No file edits in this task — build and run the app on-device (Cmd-R in Xcode) and perform the full Settings walkthrough described in how-to-verify below. This task's only output is the recorded PASSED/FAILED verdict covering this phase's 4 ROADMAP Success Criteria and SETTINGS-01/VISUAL-03.
  </action>
  <what-built>
    A NavigationSplitView Settings window (General/Workspace/System/About sidebar sections, every previously-existing control relocated) plus a new System (Theming) section (Gradient/Solid Black material picker + 3 independent Now Playing/Charging/Device accent pickers) that live-applies to the notch shell via the existing UserDefaults-observer re-host pipeline, extended in Plan 02.
  </what-built>
  <how-to-verify>
    1. Open `Islet.xcodeproj` in Xcode, Cmd-R to run the Debug build.
    2. Open Settings (menu bar icon → Settings). Confirm 4 sidebar rows: General, Workspace, System, About.
    3. Click General: confirm the 4 activity toggles (Charging / Now Playing / Song-Change Toast / Devices), Launch-at-login toggle, "Hide notch in fullscreen" toggle, and "Save Diagnostic Report…" button are all present. Flip a toggle and confirm the notch shell reacts (e.g. disabling Now Playing hides the equalizer wing). Save a diagnostic report and open the saved `.txt` — confirm it lists 3 separate "Now Playing Accent" / "Charging Accent" / "Device Accent" lines, not the old single "Accent index" line.
    4. Click Workspace: confirm the "Nothing to configure yet" placeholder renders, centered, no crash.
    5. Click System: confirm the Gradient/Solid Black segmented picker and 3 accent swatch rows (Now Playing / Charging / Device) render. Toggle to Solid Black — confirm the collapsed pill, expanded island, and all 3 activity wings switch to a flat black fill live, with no app restart. Toggle back to Gradient — confirm the black-to-transparent gradient returns.
    6. With System still open, change ONLY the Now Playing swatch — confirm ONLY the equalizer bars (both the collapsed media wing and the expanded Now Playing view) change color; the Charging battery glyph and Device glyph keep their own independent colors. Repeat independently for the Charging swatch (confirm only the charging glyph changes) and the Device swatch (confirm only the device left-wing glyph changes).
    7. Click About: confirm the License block (current trial/licensed state) and Version label render correctly, matching what the old Appearance/General tabs showed before this phase.
    8. Rapidly click General → About → General → About 5+ times in a row: confirm the license countdown/status text and login-item toggle NEVER flash a stale or default value for a single frame (this is the concrete on-device check for RESEARCH.md Pitfall 1 / ROADMAP Success Criterion 3).
    9. Quit and relaunch the app (simulating an existing user re-opening after the upgrade): reopen Settings → System and confirm the 3 accent swatches still show whichever colors were selected before quitting (persistence + the Plan 01 migration/live-apply round-trip both hold).
    10. Confirm the Settings window itself renders cleanly at its current frame — sidebar and detail columns both fully visible, no overlap or squeeze. Note the actual on-screen size in your response; if it looks cramped or oversized, that's fine to flag as a follow-up tuning note rather than a blocking failure (per this project's established on-device sizing-iteration pattern).
  </how-to-verify>
  <resume-signal>Type "approved" once all 10 steps pass, or describe exactly which step/behavior failed.</resume-signal>
  <verify>
    <human-check>All 10 how-to-verify steps confirmed on-device by the user; resume-signal explicitly approved.</human-check>
  </verify>
  <done>User has typed "approved" for the full on-device Settings walkthrough, confirming ROADMAP.md Phase 27's 4 Success Criteria and SETTINGS-01/VISUAL-03 are observably true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| (inherited from Plans 01-03) | This plan performs no new code changes — it verifies the trust boundaries and mitigations already established in 27-01/27-02/27-03's threat models |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-08 | Tampering (regression check) | Whole-phase integration | mitigate | Task 1's grep sweep is the closing verification that no plan left a stale reference to a deleted unchecked-access pattern (`activityAccent`/`appliedAccentIndex`) that could reintroduce a crash path Plans 01-02 already closed |
</threat_model>

<verification>
Task 1 gates on both Debug and Release `xcodebuild build` succeeding plus the dead-reference grep sweep returning zero matches. Task 2 is a blocking on-device checkpoint covering every Manual-Only Verification in `27-VALIDATION.md` (SETTINGS-01 sidebar/state-sync behavior, VISUAL-03 live-theming rendering).
</verification>

<success_criteria>
- Debug and Release builds both succeed.
- Zero dead references to the deleted single-value accent/material symbols anywhere in `Islet/`.
- On-device UAT (all 10 steps) approved by the user.
- SETTINGS-01 and VISUAL-03 are both fully implemented and verified, closing out Phase 27.
</success_criteria>

<output>
Create `.planning/phases/27-settings-sidebar-redesign/27-04-SUMMARY.md` when done
</output>
