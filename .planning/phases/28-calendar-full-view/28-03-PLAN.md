---
phase: 28-calendar-full-view
plan: 03
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - Islet/Shelf/ShelfViewState.swift
  - Islet/Notch/IslandResolver.swift
  - IsletTests/IslandResolverTests.swift
  - Islet/Notch/NotchPillView.swift
autonomous: true
requirements: [CALVIEW-01, CALVIEW-02, CALVIEW-03]

must_haves:
  truths:
    - "A third view (Calendar) is reachable via a Home/Tray/Calendar switcher pill below the expanded island, showing a month grid and the selected day's event list"
    - "Selecting a day with no events shows an explicit 'No events today' empty state, not a blank area"
    - "The user can quick-add a Calendar Event or a Reminder (their choice) without leaving the island"
    - "Today is selected by default when the calendar view opens, and prev/next month navigation works"
  artifacts:
    - path: "Islet/Shelf/ShelfViewState.swift"
      provides: "forcedByTray flag + isVisible computed property — the ONE source of truth for shelf-row visibility"
      contains: "var isVisible: Bool"
    - path: "Islet/Notch/IslandResolver.swift"
      provides: ".calendarExpanded IslandPresentation case + resolve(...) selectedView parameter, referencing Plan 01's SelectedView type"
      contains: "case calendarExpanded"
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "switcherRow (3-icon Home/Tray/Calendar pill), .calendarExpanded switch arm rendering calendarFullView (month grid + day list + empty state + quick-add popover), blobShape extended with shelfVisible/showSwitcher params"
      contains: "case .calendarExpanded:"
  key_links:
    - from: "Islet/Notch/NotchPillView.swift blobShape(...)"
      to: "Islet/Shelf/ShelfViewState.swift isVisible"
      via: "shelfVisible: shelfViewState.isVisible passed at every blobShape call site that used to read shelfItems.isEmpty directly"
      pattern: "shelfViewState\\.isVisible"
    - from: "Islet/Notch/NotchPillView.swift body switch"
      to: "Islet/Notch/IslandResolver.swift IslandPresentation.calendarExpanded"
      via: "case .calendarExpanded: calendarFullView, added as one more pure switch arm"
      pattern: "case \\.calendarExpanded"
    - from: "Islet/Notch/NotchPillView.swift calendarFullView"
      to: "Islet/Calendar/CalendarGlance.swift events(on:events:) / daysInMonth(for:)"
      via: "the month grid and day list both read calendarViewState.monthEvents through these Plan 01 pure functions"
      pattern: "events\\(on:"
---

<objective>
Build the entire Calendar Full View render layer: the shared shelf-visibility fix that
prevents the CR-01 click-through regression class, the resolver's `.calendarExpanded` case
paired atomically with its `NotchPillView` switch arm, the 3-icon Home/Tray/Calendar switcher
pill, and the month grid + day list + empty state + quick-add popover content.

Purpose: this is the view-layer heart of the phase — CALVIEW-01/02/03 all render here. The
plan is sequenced so every task lands a fully-compiling, non-stub increment: Task 1 fixes
shelf visibility (no enum changes, safe standalone), Task 2 adds the `.calendarExpanded` case
AND its real switch arm together (Swift's exhaustive-switch requirement means these cannot be
split across tasks/plans without breaking the build), Task 3 adds the quick-add popover on top
of Task 2's already-real grid/list.
Output: a fully navigable, renderable Calendar Full View wired into `NotchPillView`'s existing
single-arbiter switch, ready for Plan 04 to wire real controller behavior (data fetch,
permission requests, panel geometry) on top of.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-calendar-full-view/28-CONTEXT.md
@.planning/phases/28-calendar-full-view/28-RESEARCH.md
@.planning/phases/28-calendar-full-view/28-PATTERNS.md
@.planning/phases/28-calendar-full-view/28-UI-SPEC.md
</context>

<interfaces>
<!-- Contracts from Plan 01 this plan consumes directly, no exploration needed. -->

From Islet/Notch/ViewSwitcherState.swift (Plan 01):
enum SelectedView: Equatable { case home, tray, calendar }
final class ViewSwitcherState: ObservableObject { @Published var selectedView: SelectedView = .home }

From Islet/Calendar/CalendarViewState.swift (Plan 01):
final class CalendarViewState: ObservableObject {
    @Published var visibleMonth: Date = Date()
    @Published var selectedDay: Date = Date()
    @Published var monthEvents: [EventInput]?   // nil = not yet loaded
}
enum QuickAddKind: Equatable { case event, reminder }

From Islet/Calendar/CalendarGlance.swift (Plan 01):
func daysInMonth(for date: Date, calendar: Calendar = .current) -> [Date?]
func events(on day: Date, events: [EventInput], calendar: Calendar = .current) -> [EventInput]
struct EventInput: Equatable { let title: String; let start: Date; let end: Date; let colorRed/colorGreen/colorBlue: Double }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ShelfViewState.isVisible/forcedByTray — the single source of truth for shelf visibility</name>
  <files>Islet/Shelf/ShelfViewState.swift, Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Shelf/ShelfViewState.swift
    Islet/Notch/NotchPillView.swift (blobShape, lines ~637-666; body outer .frame, lines ~276-280)
    .planning/phases/28-calendar-full-view/28-RESEARCH.md (Pitfall 3)
  </read_first>
  <action>
    In Islet/Shelf/ShelfViewState.swift, add `@Published var forcedByTray = false` and a
    computed `var isVisible: Bool { !items.isEmpty || forcedByTray }` to `ShelfViewState`.
    This becomes the ONE source of truth every shelf-visibility check must read — per the
    project's own `cr01-clickthrough-or-defeat-gotcha` memory, never patch one call site with
    an inline OR while leaving siblings on the old check.

    In Islet/Notch/NotchPillView.swift's `blobShape` helper: rename its internal
    `let hasShelf = !shelfItems.isEmpty` line to `let hasShelf = shelfVisible`, and add a new
    `shelfVisible: Bool` parameter to the function signature (placed right after the existing
    `shelfItems: [ShelfItem]` parameter, before `@ViewBuilder content`). Update ALL 4 existing
    call sites of `blobShape(...)` (in `expandedIsland`, `onboardingCarousel`, and the two
    media-expanded call sites) to pass the new argument: the 3 sites that already pass
    `shelfItems: shelfViewState.items` now ALSO pass `shelfVisible: shelfViewState.isVisible`;
    `onboardingCarousel`'s call (which passes `shelfItems: []`, D-06: shelf never shows during
    onboarding) now ALSO passes `shelfVisible: false` explicitly (unchanged behavior).

    In the same file, update the body's outer `.frame(...)` height calculation (currently
    `Self.expandedSize.height + (shelfViewState.items.isEmpty ? 0 : Self.shelfRowHeight)`) to
    read `shelfViewState.isVisible ? Self.shelfRowHeight : 0` instead of the `.items.isEmpty`
    check.
  </action>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var isVisible: Bool" Islet/Shelf/ShelfViewState.swift` returns `1`
    - `grep -c "forcedByTray" Islet/Shelf/ShelfViewState.swift` returns `2` (the `@Published` field + its use inside `isVisible`)
    - `grep -c "shelfViewState.items.isEmpty" Islet/Notch/NotchPillView.swift` returns `0` (no remaining reads of the old raw check in this file)
    - `grep -c "shelfVisible: shelfViewState.isVisible" Islet/Notch/NotchPillView.swift` returns `3` (expandedIsland + the 2 media-expanded call sites)
  </acceptance_criteria>
  <done>ShelfViewState.isVisible exists and every shelf-visibility read in NotchPillView.swift
  routes through it — no remaining direct `.items.isEmpty` checks for visibility.</done>
</task>

<task type="auto">
  <name>Task 2: IslandResolver .calendarExpanded case + switcher pill + calendarFullView (grid + day list + empty state)</name>
  <files>Islet/Notch/IslandResolver.swift, IsletTests/IslandResolverTests.swift, Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/IslandResolver.swift
    IsletTests/IslandResolverTests.swift
    Islet/Notch/NotchPillView.swift (switch presentation block lines ~241-261, navCircleButton lines ~577-587, blobShape lines ~637-666, expandedIsland lines ~322-337, calendarColumn lines ~924-945)
    .planning/phases/28-calendar-full-view/28-UI-SPEC.md (Switcher pill + Calendar full view + Typography + Color sections)
    .planning/phases/28-calendar-full-view/28-RESEARCH.md (Pattern 3, Anti-Patterns)
  </read_first>
  <behavior>
    - Test: `resolve(..., isExpanded: true, selectedView: .calendar)` (no active transient, no
      now-playing) returns `.calendarExpanded`.
    - Test: `resolve(..., isExpanded: true, selectedView: .calendar, nowPlaying: .playing(...))`
      returns `.nowPlayingExpanded(...)`, NOT `.calendarExpanded` — media still outranks it.
    - Test: `resolve(..., activeTransient: .charging(...), isExpanded: true, selectedView: .calendar)`
      returns `.charging(...)` — a transient always outranks the calendar selection.
    - Test: `resolve(..., isExpanded: true, selectedView: .tray)` returns `.expandedIdle` — Tray
      is deliberately NOT its own `IslandPresentation` case (D-02).
    - Test: `resolve(..., isExpanded: false, selectedView: .calendar)` returns `.idle`.
    - Test: `resolve(..., onboardingStep: .welcome, isExpanded: true, selectedView: .calendar)`
      returns `.onboarding(.welcome)` — forced onboarding still outranks everything.
  </behavior>
  <action>
    In Islet/Notch/IslandResolver.swift: add `case calendarExpanded` to `IslandPresentation`,
    placed after `expandedIdle`. Add a new defaulted parameter `selectedView: SelectedView =
    .home` (the type from Plan 01's `ViewSwitcherState.swift`, same target, no import needed)
    to `resolve(...)`, inserted before the existing `onboardingStep: OnboardingStep? = nil`
    parameter (keeps every existing call site source-compatible). Inside the `if isExpanded {
    ... }` block, after the two `nowPlaying` checks and before `return .expandedIdle`, add:
    `if selectedView == .calendar { return .calendarExpanded }`. Add no branch for `.tray` —
    per Research's explicit recommendation, Tray's force-reveal is a view/controller concern
    (already handled via `ShelfViewState.forcedByTray` from Task 1), not a resolver case.
    Extend IsletTests/IslandResolverTests.swift with the 6 behaviors above, matching the file's
    existing labeled-call + `XCTAssertEqual` style.

    In Islet/Notch/NotchPillView.swift, in the SAME task (these must land together — adding
    the enum case above makes the body's `switch presentation` non-exhaustive until this arm
    exists):

    1. Add `case .calendarExpanded: calendarFullView` to the `switch presentation` block
       (after `.expandedIdle`), as one more pure switch arm — no re-deciding precedence.

    2. Add a new `switcherRowHeight: CGFloat = 44` static constant (alongside
       `shelfRowHeight`/`toastExtraHeight`). Add a `showSwitcher: Bool = false` parameter to
       `blobShape` (after the `shelfVisible` parameter from Task 1); when true, grow
       `totalHeight` by `Self.switcherRowHeight` and insert a `switcherRow` view into the
       inner `VStack` between `content()` and the conditional `shelfRow(...)` (so content ->
       switcher -> shelf, top to bottom). Add a computed `private var showsSwitcherRow: Bool`
       mirroring `isOnboardingPresentation`'s existing pattern: `true` for `.expandedIdle` and
       `.calendarExpanded`, `false` otherwise. Update the body's outer `.frame(...)` height
       calculation from Task 1 to also add `(showsSwitcherRow ? Self.switcherRowHeight : 0)`.
       Update `expandedIsland`'s `blobShape(...)` call to pass `showSwitcher: true`; leave the
       2 media-expanded call sites and `onboardingCarousel` omitting the parameter (default
       `false`, unchanged visual).

    3. Add a `switcherRow` view: an `HStack(spacing: 8)` centered, containing 3
       `navCircleButton(systemName:filled:action:)` calls — `"house.fill"` (Home),
       `"tray.fill"` (Tray), `"calendar"` (Calendar) — reusing the existing helper verbatim.
       `filled:` is `true` for whichever matches `viewSwitcherState.selectedView`, `false`
       otherwise. Each button's `action:` calls a new `onSwitcherSelect: (SelectedView) -> Void
       = { _ in }` closure with `.home`/`.tray`/`.calendar` respectively. Add a new
       `@ObservedObject var viewSwitcherState: ViewSwitcherState` property to `NotchPillView`
       (non-defaulted, same ownership convention as `shelfViewState`/`outfit`) and add
       `viewSwitcherState: viewSwitcherState` to every `#Preview` construction alongside the
       existing preview instances (mirrors how `shelfViewState`/`onboardingState` were added to
       previews in their own phases).

    4. Add `calendarFullView`: a computed property calling
       `blobShape(topCornerRadius: 6, bottomCornerRadius: 32, shelfItems: shelfViewState.items,
       shelfVisible: shelfViewState.isVisible, showSwitcher: true)` with content: an `HStack`
       with the month grid on the LEFT (~190pt wide) and the day list on the RIGHT (fills
       remainder), separated by a thin `Color.white.opacity(0.1)` divider, `.padding(
       .horizontal, 16)` (matches `expandedIsland`'s existing padding). Add a new
       `@ObservedObject var calendarViewState: CalendarViewState` property (non-defaulted,
       same convention) and closures `onCalendarMonthChange: (Int) -> Void = { _ in }` (passes
       `-1`/`+1`) and `onCalendarDaySelect: (Date) -> Void = { _ in }`.

       Month grid: a header row (month/year label via `Text(calendarViewState.visibleMonth,
       format: .dateTime.month(.wide).year())`, 13px semibold rounded, flanked by
       `chevron.left`/`chevron.right` buttons calling `onCalendarMonthChange(-1)`/`(+1)`, 12px
       `.white.opacity(0.7)` per UI-SPEC), then a `LazyVGrid` with 7 columns, cell size 28×28pt,
       4px spacing, iterating `daysInMonth(for: calendarViewState.visibleMonth)` — each
       non-nil day renders its day-of-month number (11px regular rounded, 11px semibold for
       the day matching `calendarViewState.selectedDay` via `Calendar.current.isDate(_:
       inSameDayAs:)`), tappable via `.onTapGesture { onCalendarDaySelect(day) }`; nil cells
       render as empty space (no view).

       Day list: compute `let dayEvents = calendarViewState.monthEvents.map { events(on:
       calendarViewState.selectedDay, events: $0) } ?? nil` (nil while not yet loaded — render
       nothing/blank, never the empty state, per Pitfall 4). When `dayEvents` is a non-nil
       EMPTY array, render the CALVIEW-02 empty state: "No events today" (13px semibold
       rounded, white) + "Tap **+ Add** to create one." (11px regular rounded, secondary,
       bold-emphasize "+ Add" per the Copywriting Contract). When `dayEvents` is a non-nil
       non-empty array, render each event as a row reusing `calendarColumn`'s exact
       title/color-dot/time convention (12px title with `.lineLimit(1)`/`.truncationMode(
       .tail)` — T-14-06 mandatory), wrapped in a `ScrollView(.vertical)` when there are more
       than ~3-4 rows (per UI-SPEC's Layout Contract, the 144pt content box is short).
  </action>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "case calendarExpanded" Islet/Notch/IslandResolver.swift` returns `1`
    - `grep -c "selectedView: SelectedView = .home" Islet/Notch/IslandResolver.swift` returns `1`
    - `grep -c "case .calendarExpanded:" Islet/Notch/NotchPillView.swift` returns `1`
    - `grep -c "func daysInMonth\|daysInMonth(for:" Islet/Notch/NotchPillView.swift` returns at least `1` (the grid actually calls the Plan 01 pure function, not a re-implementation)
    - `grep -c "\.lineLimit(1)" Islet/Notch/NotchPillView.swift` includes at least one NEW occurrence beyond the existing `calendarColumn` one (day-list event titles are bounded)
    - Cmd-U (manual, next wave-merge gate) shows all 6 new `IslandResolverTests` methods passing
    - Build succeeds with the new `.calendarExpanded` case fully handled — no "switch must be exhaustive" compiler error
  </acceptance_criteria>
  <done>Tapping the Calendar switcher icon (once Plan 04 wires the tap handler) would render a
  real month grid + day list + empty state, all reading through Plan 01's pure functions; the
  resolver correctly ranks calendarExpanded below transients/media/onboarding.</done>
</task>

<task type="auto">
  <name>Task 3: Quick-add popover (Event/Reminder choice) on the calendar full view</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift (chipButton lines ~594-607, the calendarFullView added in Task 2)
    Islet/SettingsView.swift (segmented Picker convention, around lines 238-242)
    .planning/phases/28-calendar-full-view/28-UI-SPEC.md (Copywriting Contract, Layout Contract)
  </read_first>
  <action>
    Add a "+ Add" `chipButton`-style trigger at the top-right of the day-list column (reuses
    `chipButton(_:fontSize:action:)` verbatim) that toggles a local `@State private var
    isQuickAddShowing = false` inside a small new private `QuickAddPopover: View` struct
    (file-scope private struct, mirroring `OnboardingDoneStep`'s existing pattern of a
    self-contained struct holding its own transient `@State`) — do NOT hold this state on
    `NotchPillView` itself (would leak across every other presentation case, same reasoning
    already documented for `OnboardingDoneStep`'s Launch-at-Login `@State`).

    `QuickAddPopover` holds `@State private var kind: QuickAddKind = .event` and `@State
    private var title = ""`, and takes an `onSubmit: (QuickAddKind, String) -> Void` closure.
    Renders: a `Picker("Type", selection: $kind) { Text("Event").tag(QuickAddKind.event);
    Text("Reminder").tag(QuickAddKind.reminder) }.pickerStyle(.segmented)` (copies
    `SettingsView.swift`'s exact existing segmented-Picker convention verbatim — do not
    hand-roll a custom toggle), a `TextField("What's this for?", text: $title)` (12px regular
    rounded per UI-SPEC Typography), and a `chipButton` submit whose label swaps with `kind`
    ("Add Event" / "Add Reminder", never a generic "Save", per the Copywriting Contract) that
    calls `onSubmit(kind, title)` then clears `title` and dismisses the popover.

    Wire `NotchPillView`'s new `onQuickAdd: (QuickAddKind, String) -> Void = { _, _ in }`
    closure (added alongside the other closures from Task 2) into `QuickAddPopover`'s
    `onSubmit`. Present `QuickAddPopover` via SwiftUI's `.popover(isPresented:)` anchored to
    the "+ Add" button, matching the "in-panel popover, not a separate window" requirement
    from UI-SPEC's Verification Notes (CALVIEW-03's "without leaving the island" wording).
  </action>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "struct QuickAddPopover" Islet/Notch/NotchPillView.swift` returns `1`
    - `grep -c "pickerStyle(.segmented)" Islet/Notch/NotchPillView.swift` returns at least `1` new occurrence for the quick-add toggle
    - `grep -c "onQuickAdd" Islet/Notch/NotchPillView.swift` returns at least `2` (property declaration + call site)
    - `grep -c "\"Add Event\"\|\"Add Reminder\"" Islet/Notch/NotchPillView.swift` returns `2` (both submit-label variants present, never a generic "Save")
  </acceptance_criteria>
  <done>The day-list column has a working "+ Add" trigger that opens an in-panel popover
  offering Event/Reminder choice + a title field + a kind-labeled submit button, reporting
  intent via onQuickAdd — no EventKit/EKEventStore code in this view file (AppKit-free view
  layer, matches the project's established convention).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| EventKit → render | `EventInput.title` (untrusted, subscribed/shared calendars) rendered in the new day-list rows |
| User → EKEventStore/EKReminderStore (via Plan 04) | The quick-add TextField's free-text title, forwarded unmodified via `onQuickAdd` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Information Disclosure | Day-list event-title rendering in `calendarFullView` | mitigate | `.lineLimit(1)`/`.truncationMode(.tail)` on every event title row, mirroring `calendarColumn`'s existing T-14-06 treatment exactly |
| T-28-03 | Tampering (low risk) | Quick-add `TextField` free text | accept | Native SwiftUI `TextField` bound to a plain `String`, forwarded as-is via a closure — no format-string/shell/log interpolation anywhere in this view; the value is only ever assigned to `EKEvent.title`/`EKReminder.title` properties in Plan 04, never executed or rendered elsewhere untrusted |
</threat_model>

<verification>
Build gate (`xcodebuild build`) green after every task commit. Cmd-U (manual, IsletTests
scheme) after Task 2 merges: all `IslandResolverTests` methods (existing + the 6 new ones)
pass. Full on-device rendering verification (switcher tap actually navigating, real month
data, real quick-add round-trip) is deferred to Plan 04's checkpoint, once the controller
wires real state/permission/EventKit behavior behind these views.
</verification>

<success_criteria>
- ShelfViewState.isVisible is the single source of truth for shelf-row visibility in NotchPillView.swift
- IslandPresentation.calendarExpanded exists and resolve(...) picks it correctly per precedence tests
- NotchPillView renders a real switcher pill, month grid, day list, empty state, and quick-add popover for .calendarExpanded — no placeholder/stub content
</success_criteria>

<output>
Create `.planning/phases/28-calendar-full-view/28-03-SUMMARY.md` when done
</output>
