---
phase: 28-calendar-full-view
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Calendar/CalendarService.swift
  - project.yml
autonomous: true
requirements: [CALVIEW-03, CALVIEW-04]
user_setup: []

must_haves:
  truths:
    - "The user can quick-add a Calendar Event or a Reminder (their choice) without leaving the island"
    - "The full calendar view and the existing Home-glance next-event feature both read through the same CalendarService/EventKitService — no duplicated EventKit fetch/mapping logic"
  artifacts:
    - path: "Islet/Calendar/CalendarService.swift"
      provides: "fetchMonth(containing:completion:), createEvent(title:start:end:completion:), createReminder(title:dueDate:completion:) added to the SAME CalendarService protocol/EventKitService conformer"
      contains: "func fetchMonth"
    - path: "project.yml"
      provides: "NSRemindersUsageDescription + NSRemindersFullAccessUsageDescription Info.plist keys (D-05)"
      contains: "INFOPLIST_KEY_NSRemindersFullAccessUsageDescription"
  key_links:
    - from: "Islet/Calendar/CalendarService.swift createReminder(...)"
      to: "EKEventStore.requestFullAccessToReminders()"
      via: "lazy, first-use-only permission request fired from inside createReminder, never at launch"
      pattern: "requestFullAccessToReminders"
---

<objective>
Extend the existing `CalendarService`/`EventKitService` protocol-isolation seam (do NOT create
a second service) with three new capabilities: a month-range event fetch for the grid, calendar
event creation, and Reminder creation gated behind a lazy, first-use-only permission request.
Add the two new Reminders Info.plist keys this requires.

Purpose: CALVIEW-04 explicitly mandates one shared EventKit layer — this plan is the ONLY
place EventKit-facing code changes in this phase, so the month view and the existing
Home-glance `fetchUpcoming` never diverge into duplicated fetch/mapping logic.
Output: `CalendarService.swift` gains 3 new protocol methods + conformer implementations;
`project.yml` gains the 2 Reminders usage-description keys D-05 requires.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-calendar-full-view/28-CONTEXT.md
@.planning/phases/28-calendar-full-view/28-RESEARCH.md
@.planning/phases/28-calendar-full-view/28-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: fetchMonth(containing:completion:) — month-range event fetch</name>
  <files>Islet/Calendar/CalendarService.swift</files>
  <read_first>
    Islet/Calendar/CalendarService.swift
    Islet/Calendar/CalendarGlance.swift
    .planning/phases/28-calendar-full-view/28-RESEARCH.md (Code Examples: "Month-range fetch")
  </read_first>
  <action>
    Add `func fetchMonth(containing date: Date, completion: @escaping ([EventInput]) -> Void)`
    to the `CalendarService` protocol (after the existing `fetchUpcoming`), and implement it
    on `EventKitService` using the SAME `self.store` instance `fetchUpcoming` already uses —
    never a second `EKEventStore()`. Contract: `completion` is ALWAYS delivered on the main
    thread (same contract as `fetchUpcoming`); on Calendar access denial, settle `completion([])`
    (never `nil` — this is an array-returning method, not the glance-returning
    `fetchUpcoming`). Implementation shape: `store.requestFullAccessToEvents()` (same call
    fetchUpcoming already makes — idempotent, no duplicate prompt), widen the date range via
    `calendar.dateInterval(of: .month, for: date)` instead of `fetchUpcoming`'s 2-day window,
    `store.calendars(for: .event)` (all calendars, same as fetchUpcoming — no per-calendar
    filter), map each `EKEvent` to `EventInput` using the IDENTICAL mapping fetchUpcoming
    already does (title as plain untrusted `String`, RGB from `ek.calendar.color`). This is
    the exact function already vetted in 28-RESEARCH.md's Code Examples section — reuse it,
    do not redesign the mapping.
  </action>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func fetchMonth" Islet/Calendar/CalendarService.swift` returns `1` (once in the protocol, once in the conformer — actually check `grep -c "fetchMonth"` returns `2`)
    - `grep -v '^\s*//' Islet/Calendar/CalendarService.swift | grep -c "EKEventStore()"` returns `1` (single store instance, CALVIEW-04 structural check)
    - The new method's completion closure type is `([EventInput]) -> Void`, never `(CalendarGlance?) -> Void`
  </acceptance_criteria>
  <done>fetchMonth exists on both the protocol and EventKitService, reuses the existing store
  instance, and mirrors fetchUpcoming's main-thread-completion + plain-String-title
  contract.</done>
</task>

<task type="auto">
  <name>Task 2: createEvent + createReminder (lazy Reminders permission) + project.yml keys</name>
  <files>Islet/Calendar/CalendarService.swift, project.yml</files>
  <read_first>
    Islet/Calendar/CalendarService.swift
    Islet/Location/LocationProvider.swift
    project.yml (lines 55-70)
    .planning/phases/28-calendar-full-view/28-RESEARCH.md (Code Examples: "Lazy Reminders permission request")
    .planning/phases/28-calendar-full-view/28-CONTEXT.md (D-04, D-05, D-06)
  </read_first>
  <action>
    Add two more methods to the `CalendarService` protocol + `EventKitService` conformer:

    `func createEvent(title: String, start: Date, end: Date, completion: @escaping (Bool) -> Void)`
    — D-06 confirms NO new permission work is needed here (the store already calls
    `requestFullAccessToEvents()` elsewhere in this file and full write access is already
    granted once the user has been through Calendar access once). Construct `EKEvent(eventStore:
    self.store)`, set `.title = title` (plain String, no interpolation), `.startDate = start`,
    `.endDate = end`, `.calendar = store.defaultCalendarForNewEvents`, save via
    `store.save(_:span: .thisEvent)` inside a `do/catch`, settle `completion(true)` on success
    and `completion(false)` on any thrown error — never crash.

    `func createReminder(title: String, dueDate: Date?, completion: @escaping (Bool) -> Void)`
    — D-04 (LOCKED): this is the ONLY call site in the entire codebase that may request
    Reminders access, and it must request it lazily, INSIDE this function, on first
    invocation — never at launch, never during onboarding. Mirror
    `LocationProvider.requestOnce`'s "settle once, silent degrade on denial, never retry/nag"
    shape, but call the store's own async
    `store.requestFullAccessToReminders()` directly (EKEventStore already exposes this,
    unlike CLLocationManager's delegate-callback shape — no new delegate wrapper needed).
    Reuse `self.store` — the SAME `EKEventStore` instance `fetchUpcoming`/`fetchMonth` use,
    never a second store (CALVIEW-04, mirrors the anti-pattern warning in
    28-RESEARCH.md). On denial, settle `completion(false)` and stop (no retry). On grant,
    construct `EKReminder(eventStore: self.store)`, set `.title = title` (T-14-06: plain
    String, never interpolated), `.calendar = store.defaultCalendarForNewReminders()`, and if
    `dueDate` is non-nil set `.dueDateComponents` via
    `Calendar.current.dateComponents([.year, .month, .day, .hour, .minute], from: dueDate)`.
    Save via `store.save(_:commit: true)` inside `do/catch`, settle `completion(true)`/`
    completion(false)` accordingly (never crash on a nil `defaultCalendarForNewReminders()` —
    the do/catch around save already covers that). This is the exact function already vetted
    in 28-RESEARCH.md's Code Examples section — reuse it, do not redesign the permission flow.

    In project.yml, immediately after the existing `INFOPLIST_KEY_NSCalendarsFullAccessUsageDescription`
    line (around line 65), add BOTH of these new keys (D-05 — Pitfall 2 warns adding only one
    silently breaks first-touch), same German-string style/indentation as the 4 existing
    Bluetooth/Location/Calendar keys:
    `INFOPLIST_KEY_NSRemindersUsageDescription` and
    `INFOPLIST_KEY_NSRemindersFullAccessUsageDescription`, both with a one-line German
    description of why Islet needs Reminders (e.g. "Islet legt eine neue Erinnerung an, wenn
    du das im Kalender-Schnellzugriff auswählst.").
  </action>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func createEvent" Islet/Calendar/CalendarService.swift` returns `2` (protocol + conformer)
    - `grep -c "func createReminder" Islet/Calendar/CalendarService.swift` returns `2` (protocol + conformer)
    - `grep -c "requestFullAccessToReminders" Islet/Calendar/CalendarService.swift` returns exactly `1` (the ONLY call site in the codebase)
    - `grep -c "INFOPLIST_KEY_NSRemindersUsageDescription" project.yml` returns `1`
    - `grep -c "INFOPLIST_KEY_NSRemindersFullAccessUsageDescription" project.yml` returns `1`
    - `grep -v '^\s*//' Islet/Calendar/CalendarService.swift | grep -c "EKEventStore()"` still returns `1` (no second store introduced)
  </acceptance_criteria>
  <done>createEvent and createReminder both exist and compile; createReminder requests
  Reminders access lazily on its own first call (never at launch); project.yml has both new
  Reminders Info.plist keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| App → EventKit (EKEventStore save) | User-typed quick-add title crosses into EKEvent.title/EKReminder.title, a system-managed store other apps (Calendar.app, Reminders.app, subscribed calendars) can later read back |
| macOS TCC → app | Reminders permission grant/denial gates createReminder's entire effect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02 | Elevation of Privilege (UX-trust erosion) | `createReminder(...)`'s permission timing | mitigate | `requestFullAccessToReminders()` fires ONLY from inside this function, on the user's first "Reminder" quick-add tap — never at launch/onboarding (D-04); verified structurally by the single-call-site acceptance criterion above |
| T-28-05 | Tampering (write path) | `createEvent`/`createReminder` `.save(...)` | mitigate | Both wrapped in `do/catch`, settling `completion(false)` on any thrown error — never force-try, never crash on a malformed/nil default calendar |
| T-28-SC | Supply Chain | N/A — no new package | accept | This phase adds zero new SPM/CocoaPods dependencies; Reminders ships via the already-linked EventKit system framework |
</threat_model>

<verification>
Build gate (`xcodebuild build`) green after every task commit. CALVIEW-04's single-store
structural check (`grep -c "EKEventStore()"` == 1) must hold after both tasks. Manual on-device
UAT (quick-add round-trip + Reminders permission prompt timing) is deferred to Plan 04's
checkpoint task, once the UI (Plan 03) and controller wiring (Plan 04) exist to actually
trigger these methods.
</verification>

<success_criteria>
- CalendarService protocol has exactly 4 methods total (fetchUpcoming, fetchMonth, createEvent, createReminder), all on ONE conformer
- Reminders permission is requested from exactly one call site in the whole codebase, inside createReminder
- project.yml has both new Reminders Info.plist keys
</success_criteria>

<output>
Create `.planning/phases/28-calendar-full-view/28-02-SUMMARY.md` when done
</output>
