---
phase: 28
slug: calendar-full-view
status: draft
shadcn_initialized: false
preset: not applicable — native SwiftUI/AppKit app, no component-registry tooling
created: 2026-07-13
---

# Phase 28 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**Scope note:** This phase adds two NEW pieces of notch-hosted chrome — (1) a 3-icon Home/Tray/Calendar switcher pill below the expanded island, and (2) a new `.calendarExpanded` presentation: month grid (left) + selected day's event list (right) + quick-add. Both render inside the **existing** `blobShape`/`islandFill` gradient chrome (Phase 25) at the **existing** `expandedSize` width (360pt) — this spec does not redefine the shell material, corner radii, or spring constants, only the new content and the new switcher control. All typography/color/spacing values below reuse this file's own established `NotchPillView.swift` conventions (verified by direct code read, not invented) rather than introducing a parallel design language.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — native macOS Swift/SwiftUI app, no component-registry tooling (shadcn/etc. do not apply to this stack) |
| Component library | none — hand-built `NotchShape`/`NotchPillView`/`blobShape()`, same file this phase extends |
| Icon library | SF Symbols (`Image(systemName:)`) — established convention throughout `NotchPillView.swift` (weather, device, onboarding nav, shelf trash icons) |
| Font | San Francisco system font, `.system(size:weight:design: .rounded)` — same design variant used everywhere else in the notch chrome (idle glance, onboarding, media transport) |

---

## Spacing Scale

This phase does not introduce a new spacing scale — it reuses `NotchPillView.swift`'s existing in-chrome numbers verbatim. Declared here for traceability (all multiples of 4, per this file's own established practice):

| Token | Value | Usage in this phase |
|-------|-------|----------------------|
| xs | 4px | Month-grid cell internal gap (day number to event-dot indicator) |
| sm | 8px | Switcher pill: icon-to-icon gap; event-list row internal spacing |
| md | 16px | Screen content padding for calendar/switcher content (matches `expandedIsland`'s existing `.padding(.horizontal, 16)`) |
| lg | 24px | Month-grid ↔ day-list column gap |
| xl | 32px | Not used this phase (reserved token, unchanged) |

Exceptions:
- Switcher pill sits in its own reserved row **below** the existing `blobShape`, matching the shelf row's precedent (`shelfRowHeight`, Phase 20) rather than being squeezed inside the 360×144 content box — see Layout below.
- Month-grid day cell: 28×28pt tap target (below the 44pt HIG minimum, but this is a dense, glanceable data-grid inside a small notch panel, not a primary action — matches the existing shelf-item icon tap targets' precedent of prioritizing density over the 44pt guideline in this chrome).

---

## Layout Contract

### Switcher pill (D-01/D-02)

A new fixed-height row, always below whatever the current `blobShape` content is (idle glance, media, or the new calendar view) — **not** inside the 360×144 content box, mirroring how the Phase 20 shelf row is appended below content inside the same continuous shape.

| Property | Value | Source |
|----------|-------|--------|
| Row height | 44pt (reuses `Self.shelfRowHeight`-style reserved-row pattern; new constant, do not repurpose `shelfRowHeight` itself since shelf and switcher are independently visible) | New — sized for 4 circular icon buttons at comfortable tap size |
| Icon button diameter | 36pt (reuses `navCircleDiameter` exactly — same circular nav-button visual language as onboarding's Back/Next/Finish) | Reused: `navCircleDiameter` |
| Icon gap | 8px between the 4 buttons, horizontally centered as a group | New |
| Active icon | Solid white filled circle, black glyph (reuses `navCircleButton(filled: true)` exactly) | Reused: `navCircleButton` |
| Inactive icon | Outlined stroke circle, white glyph at `.white.opacity(0.6)` (matches this file's existing "unselected/idle" opacity convention seen on device glyphs, e.g. line 734/846) | New combination of existing tokens |
| Icons (SF Symbols) | Home: `house.fill` / Tray: `tray.fill` / Calendar: `calendar` / Weather: `cloud.sun.fill` | New — Home/Tray reuse the exact concept names ROADMAP already uses; `calendar` is the direct SF Symbol match for CALVIEW; `cloud.sun.fill` added 28-04 round 4 for the new Weather tab |
| Order | Home, Tray, Calendar, Weather (left to right) — Weather appended as the 4th icon, the existing three left untouched | 28-04 round 4, user-specified |
| Visibility | Shown whenever the island is expanded into a long-lived, user-entered state, AND hidden during brief time-sensitive transients (Charging/Device splash, the small collapsed Now-Playing wings glance) — same suppression precedent as the shelf row (SHELF-09) | Resolves Claude's-Discretion item in `28-CONTEXT.md` — visible during `.expandedIdle`, `.calendarExpanded`, `.weatherExpanded`, `.nowPlayingExpanded` (both healthy and "nicht verfügbar"), and the Tray/shelf-forced state; still hidden during `.charging`/`.device`/`.nowPlayingWings`/`.onboarding`. Amended 28-04 on-device UAT round 3: user reported "no navigation while music is playing" — `.nowPlayingExpanded` was originally excluded but on real use is a long-lived state that should keep the switcher, unlike the transient wings glance. Amended again 28-04 round 4 (precedence fix + "smart Home"): Calendar/Weather selections are now checked BEFORE Now-Playing in the resolver, so they stay reachable via the switcher regardless of playback state; only Home falls back to showing Now-Playing when something is playing (and the idle glance otherwise) — see `28-CONTEXT.md`'s D-01/D-02 addendum for the full rationale. |

### Calendar full view (`.calendarExpanded`, D-07/D-08)

Reuses `blobShape(topCornerRadius: 6, bottomCornerRadius: 32, shelfItems: ...)` exactly like `expandedIsland` — same 360pt width, same `expandedSize.height` (144pt) base, growing by `shelfRowHeight` only if the shelf independently has items (unchanged mechanism, Phase 20 D-07 precedent: no second shape).

| Region | Value | Source |
|--------|-------|--------|
| Content padding | `.padding(.horizontal, 16)` (matches `expandedIsland`'s existing padding exactly) | Reused |
| Layout | `HStack` — month grid LEFT (fixed width ~190pt), day event-list RIGHT (fills remainder, ~ 130pt) inside the 360pt content box, `Divider()` or 1px `Color.white.opacity(0.1)` rule between them | New, informed by Droppy reference (grid left / list right) |
| Month grid | 7-column `LazyVGrid`, cell size 28×28pt, `4px` internal gap between cells | New |
| Month grid header | Month name + prev/next chevron buttons (`chevron.left`/`chevron.right`, 12pt, `.white.opacity(0.7)`) flanking the centered month label | New — reuses this file's existing small-chevron icon weight/opacity convention (e.g. weather-column glyphs) |
| Day-list row | `HStack(spacing: 8)` — small color dot (from `EventInput.colorRed/Green/Blue`, existing Phase 14 convention) + event title (`.lineLimit(1)`, T-14-06 truncation precedent) + time | New, reuses Phase 14's event-color-dot data already flowing through `EventInput`/`CalendarGlance` |
| Day-list scroll | `ScrollView(.vertical)` when day has >3-4 events (144pt content box is short — do not attempt to fit unlimited events unscrolled) | New |

**28-04 round 4 visual pass (Droppy reference caveat):** the specific calendar month-grid
screenshot notes.md cites (images 6-7, "month grid + Today list", "New Task popover +
empty-state") does not actually exist among the 31 PNGs in
`.planning/research/inspiration/` — on direct inspection every one of the 31 files is a
Droppy **Settings** screenshot (General/Droplets/Shelf/Basket/Clipboard/Lock
Screen/Droppy Cloud/HUDs/Theming/Accessibility/License/About), never a live notch-overlay
capture of the switcher pill or the calendar view. `notes.md`'s image numbering does not
match the files on disk. Applied the closest faithful substitute instead of inventing an
unrelated style: Droppy's own dominant, product-wide visual language — circular/capsule
badges, seen everywhere in the real screenshots (the License "Trial Active" chip, the Lock
Screen "Rounded — Compact circular layout" battery/weather rings, every settings row's
rounded-card container). Applied to the month grid: a filled circle behind the selected day
(was font-weight only), a thin ring around today when not selected, and a small dot under
days that have events — all within the same 28×28pt D-locked cell (no height-math change).
Applied to the day list: each event row now sits in a subtle rounded card
(`Color.white.opacity(0.06)`, 8pt corner radius) instead of a bare row, matching the
rounded-card language used throughout every real Droppy screenshot on file.

---

## Weather full view (`.weatherExpanded`, 28-04 round 4 — user-confirmed scope expansion)

New 4th switcher tab, added after Calendar. **Current-conditions-only** — the existing
`WeatherGlance`/`WeatherKitService` seam (`Islet/Weather/WeatherService.swift`) only ever
fetches CURRENT category + temperature, no forecast/hourly/multi-day data. This view
deliberately renders only that existing data, styled larger than the small `weatherColumn`
glance; it does **not** add a forecast call or a new data model — that would need a new
WeatherKit fetch and is flagged back to the user as a separate follow-up decision, not
silently built.

| Region | Value | Source |
|--------|-------|--------|
| Content padding | `.padding(.top, 32)` camera-clearance pin (matches `mediaExpanded`/`calendarFullView`'s convention) | Reused |
| Layout | Centered `VStack`: large weather icon (44pt, reuses `weatherIcon(for:)`'s exact SF Symbol mapping) over the temperature (32pt semibold, same locale-aware `.formatted(.measurement(...))` string `weatherColumn` already uses) over a plain category label (13pt secondary — "Sunny"/"Cloudy"/"Rain"/"Snow") | New, reuses existing icon/formatting logic verbatim per the plan's explicit instruction |
| Empty/unavailable state | "Wetter nicht verfügbar" — mirrors `mediaUnavailable`'s "nicht verfügbar" tone/style exactly | New, reused convention |
| Content height | Fits inside the existing `expandedSize.height` (144pt) base — unlike `calendarExpanded`, no dedicated `weatherContentHeight` override was needed (icon 44 + temp 32 + label ~14 + 32pt clearance + inset ≈ 144pt); all three geometry call sites therefore need no per-case height change beyond `showsSwitcherRow`'s existing uniform switcher-row addition | Verified against `NotchPillView.calendarContentHeight`'s own worked-math convention |

---

## Typography

Reuses this file's exact established type scale — no new sizes, no new weights introduced (2 weights: regular + semibold, matching every other section of `NotchPillView.swift`):

| Role | Size | Weight | Design | Usage |
|------|------|--------|--------|-------|
| Month label (grid header) | 13px | semibold | `.rounded` | "July 2026" header text |
| Day-number (grid cell, unselected) | 11px | regular | `.rounded` | Each of the 28-31 day cells |
| Day-number (grid cell, selected/today) | 11px | semibold | `.rounded` | Selected day gets weight bump, not a new size |
| Event-list title | 12px | regular | `.rounded` | Matches `expandedIsland`'s calendar-column title size exactly (existing `calendarColumn` convention, line ~501) |
| Event-list time | 11px | regular | `.rounded` | Matches existing secondary-metadata size (line ~502/880) |
| Empty-state heading | 13px | semibold | `.rounded` | "No events today" |
| Empty-state body | 11px | regular | `.rounded` | Secondary line under the heading |
| Quick-add field text | 12px | regular | `.rounded` | Title `TextField`, Event/Reminder segmented label |

**Declared: regular + semibold only** — identical convention to every existing section of this file (no `.medium`/`.bold` introduced beyond the 2 pre-existing exceptions already in the codebase, e.g. line 494's `.medium` weather glyph, which this phase does not touch).

---

## Color

Reuses the existing chrome color language — no new hues introduced beyond the one already-shipped calendar-event accent (Phase 14's per-event `colorRed/Green/Blue`):

| Role | Value | Usage |
|------|-------|-------|
| Dominant (chrome fill) | Existing `islandFill` gradient (Phase 25) — unchanged | Behind all new content, same as every other presentation |
| Text on chrome | `Color.white` | Month label, selected day-number, event titles |
| Secondary text | `.secondary` (existing convention) | Unselected day numbers, event times, empty-state body |
| Accent (10%) | Per-event calendar color, `Color(red:green:blue:)` from `EventInput` (existing Phase 14 data, unchanged) | Reserved for: the small color dot next to each event-list row, and a thin selected-day ring/fill on the month grid — nowhere else. Never applied to the switcher pill, quick-add controls, or empty-state icon. |
| Switcher active state | `Color.white` fill + black glyph (reuses `navCircleButton(filled: true)`) | The one active switcher icon only |
| Switcher inactive state | `Color.white.opacity(0.6)` glyph, no fill, `Color.white.opacity(0.4)` stroke (reuses existing `navCircleButton(filled: false)` stroke opacity) | The two inactive switcher icons |
| Quick-add control chrome | `RoundedRectangle` + `Color.white.opacity(0.12)` fill (reuses `chipButton`'s exact existing convention) | Event/Reminder segmented toggle, "Add" button |
| Destructive | n/a | No destructive actions in this phase (no delete/edit — quick-add is create-only, CALVIEW-03/Out-of-Scope) |

Accent reserved for: the per-event color dot and month-grid selected-day indicator only — never the switcher pill, never quick-add UI, never empty-state.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (quick-add trigger) | "+ Add" (small button, top-right of the day-list column, opens the quick-add popover) |
| Quick-add: type choice | Segmented control: "Event" · "Reminder" (D-03, exact user-facing nouns, mirrors REQUIREMENTS.md CALVIEW-03 wording) |
| Quick-add: title field placeholder | "What's this for?" |
| Quick-add: submit button | "Add Event" / "Add Reminder" (label swaps with the selected segment — always verb + the exact noun being created, never a generic "Save") |
| Empty state heading | "No events today" |
| Empty state body | "Tap **+ Add** to create one." (bold "+ Add" matches the literal button label above, per this project's "point at the real control" copy convention already used in onboarding's permission rows) |
| Reminders permission (lazy first-use, D-04) | One-line reason, mirroring Phase 26's exact pattern: "Islet needs Reminders access to save this." shown inline above the system prompt trigger, not a separate screen |
| Destructive confirmation | Not applicable — this phase has no delete/edit surface (Out of Scope: full CRUD) |

---

## Registry Safety

**Not applicable.** Native Swift/AppKit/SwiftUI project, no shadcn or any component-registry tooling. `LazyVGrid`, `ScrollView`, `Picker`/segmented control, and every other control this phase uses are first-party SwiftUI APIs already in use elsewhere in this codebase (e.g. `LazyVGrid` pattern is new to this phase but is a standard SwiftUI layout primitive, not a third-party dependency).

---

## Verification Notes for Executor

- Switcher pill is a **new reserved row**, not squeezed into the existing 360×144 content box — implement it the same architectural way as the Phase 20 shelf row (appended inside `blobShape`'s outer `VStack`, growing the total shape height), so it composes correctly with the shelf row's own independent height growth (D-02's reconciliation concern — both rows must be able to coexist without one clobbering the other's space).
- `navCircleButton`/`navCircleDiameter` (36pt) and `chipButton`'s `RoundedRectangle`/`Color.white.opacity(0.12)` styling are **existing private helpers in `NotchPillView.swift`** — reuse them directly for the switcher icons and quick-add controls respectively; do not re-implement equivalent styling from scratch.
- Month/day-bucketing pure logic (day cells, "today" detection, prev/next month math) must follow `CalendarGlance.swift`'s established discipline: Foundation-only, `now`/reference-date always an explicit parameter, never `Date()` inline — this is a hard project convention (T-14 discipline), not a stylistic preference.
- The switcher pill's visibility-suppression logic (hidden during Charging/Device/Now-Playing wings) should reuse the same conditional structure already gating shelf-row visibility (SHELF-09 precedent) — do not invent a second suppression mechanism.
- 44pt switcher-row height and 28×28pt month-grid cells are starting points for on-device tuning, per this project's established sizing-iteration convention (Phases 18/20/21/23/25/26 all tuned dimensions post-implementation) — flag for on-device pass during execution.
- Quick-add is a **popover/inline expansion inside the existing 360pt-wide panel**, not a separate window — matches the "without leaving the island" wording in CALVIEW-03 and the Droppy reference's own in-panel "New Task" popover (image 7).

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS (N/A — no registry tooling in this stack)

**Approval:** pending
