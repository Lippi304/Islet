---
phase: 08-fullscreen-enter-flash-elimination
plan: 03
type: execute
wave: 1
depends_on: ["08-01"]
# Wave-1 branch note: this plan and 08-02-PLAN.md are mutually exclusive alternatives, both
# declared wave:1/depends_on:["08-01"] because standard wave/depends_on frontmatter has no field
# for "run exactly one of these based on a prior plan's recorded decision." The actual enforcement
# mechanism is Task 0 below (the first task, before any irreversible action): it reads
# 08-01-SUMMARY.md's recorded decision and hard-fails as a no-op if it is not option-c. 08-02-PLAN.md
# carries the mirror-image guard for option-a/option-b. Do not rely on tooling to skip one of these
# plans automatically — Task 0 is what makes an accidental dual-dispatch fail safe.
files_modified:
  - Islet/Notch/FullscreenSpaceProbe.swift
  - Islet/Notch/NotchWindowController.swift
  - .planning/phases/08-fullscreen-enter-flash-elimination/08-ESCALATION.md
autonomous: false
requirements: [FS-01]
user_setup: []

must_haves:
  truths:
    - "No code change is shipped when no proactive pre-transition signal was found to exist at the application layer (D-03)"
    - "The v1.0 reactive updateVisibility()/orderOut behavior is left byte-for-byte exactly as it shipped before this phase's exploratory probe code"
    - "A written root-cause escalation report exists, citing this phase's concrete on-device evidence (not a restatement of prior conclusions without new investigation, per D-07)"
    - "The escalation is surfaced to the user for an explicit scope decision (accept as permanent technical debt vs. formally descope FS-01), not silently shipped as a partial mitigation (D-04)"
    - "Task 0 hard-fails as a no-op (no files modified) if 08-01-SUMMARY.md's recorded decision is option-a or option-b, preventing this plan from running against the fix-path precondition"
  artifacts:
    - path: "Islet/Notch/FullscreenSpaceProbe.swift"
      provides: "Reverted to its exact pre-08-01 state — no exploratory CGS notify-proc bindings remain"
      contains: "isBuiltinDisplayInFullscreenSpace"
    - path: "Islet/Notch/NotchWindowController.swift"
      provides: "Reverted to its exact pre-08-01 state — no exploratory probe registration/logging remains"
      contains: "func updateVisibility"
    - path: ".planning/phases/08-fullscreen-enter-flash-elimination/08-ESCALATION.md"
      provides: "The written root-cause escalation report per D-04, with this phase's concrete on-device evidence and an explicit request for the user's scope decision"
      contains: "## Root Cause"
  key_links:
    - from: ".planning/phases/08-fullscreen-enter-flash-elimination/08-ESCALATION.md"
      to: ".planning/phases/08-fullscreen-enter-flash-elimination/08-01-SUMMARY.md"
      via: "The escalation report cites 08-01's concrete on-device timing evidence as the basis for the disproven verdict"
      pattern: "08-01"
---

<!--
PRECONDITION — READ BEFORE EXECUTING:
This plan executes ONLY if 08-01-PLAN.md's recorded decision was option-c (Candidate A disproven).
If 08-01's SUMMARY.md recorded option-a or option-b, DO NOT execute this plan.
Execute 08-02-PLAN.md (the fix path) instead. 08-02 and 08-03 are mutually exclusive alternatives
gated on the same Wave-0 evidence — never run both.

ENFORCEMENT: this is not just a prose warning. Task 0 below (the first task, before Task 1's
irreversible revert) is a machine-checkable guard — it reads 08-01-SUMMARY.md and hard-fails this
plan as a no-op if the recorded decision is option-a or option-b. If both 08-02 and 08-03 are
accidentally dispatched in the same wave, Task 0 (here) or 08-02's Task 0 (mirror-image) makes the
wrong one fail safe instead of silently corrupting the codebase.
-->

<objective>
Wave 1 (escalation path) of Phase 8: when 08-01's on-device probe confirms the new CGS event
106/107 candidate does NOT provide a usable proactive pre-transition signal (never fires
cross-process, or fires no earlier than the existing reactive signals), fully revert the
exploratory Wave-0 code, produce a written root-cause escalation report grounded in this phase's
own concrete on-device evidence (not a re-statement of the prior Phase-2/Phase-6 conclusions), and
surface an explicit scope decision to the user per D-03/D-04.

This plan and 08-02-PLAN.md (the fix path) are mutually exclusive: exactly one of them is "live"
per 08-01's recorded decision. That exclusivity is enforced by Task 0 below (a hard-fail
precondition guard), not by wave/depends_on frontmatter or by execute-phase's wave tooling — neither
has a field for branch semantics like this.

Purpose: honor REQUIREMENTS.md's "Out of Scope: best-effort/partial fullscreen-flash reduction" —
if elimination is genuinely impossible at the application layer, the correct terminal state is a
documented escalation, never a silently-shipped partial mitigation.
Output: the two source files restored to their exact pre-phase state, plus a new escalation report
and a recorded user decision on how FS-01 should be closed out.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-fullscreen-enter-flash-elimination/08-CONTEXT.md
@.planning/phases/08-fullscreen-enter-flash-elimination/08-RESEARCH.md
@.planning/phases/08-fullscreen-enter-flash-elimination/08-01-SUMMARY.md
@.planning/debug/resolved/fullscreen-enter-flash.md
@.planning/phases/02-hover-expand-fullscreen-hardening/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Precondition guard — verify Wave-0 decision permits the escalation path</name>
  <files>.planning/phases/08-fullscreen-enter-flash-elimination/08-01-SUMMARY.md</files>
  <read_first>
    .planning/phases/08-fullscreen-enter-flash-elimination/08-01-SUMMARY.md (the recorded Wave-0
    option-a/option-b/option-c decision this entire plan is gated on — MUST be read before touching
    any file listed in this plan's `files_modified`)
  </read_first>
  <action>
    Read `08-01-SUMMARY.md` and extract the recorded decision (search for the literal strings
    `option-a`, `option-b`, or `option-c` in the summary's decision-record section). This plan
    (08-03, the escalation path) is machine-gated to execute ONLY when the recorded decision is
    option-c.

    If the recorded decision is option-a or option-b: HALT IMMEDIATELY. Do not proceed to Task 1,
    Task 2, or Task 3 of this plan. Do not revert or otherwise modify
    `Islet/Notch/FullscreenSpaceProbe.swift` or `Islet/Notch/NotchWindowController.swift`, and do
    not create `08-ESCALATION.md`. Report to the user, verbatim: "08-03-PLAN.md precondition
    failed: 08-01-SUMMARY.md recorded {option-a|option-b} (Candidate A confirmed), which requires
    the fix path (08-02-PLAN.md), not the escalation path (08-03-PLAN.md). This plan is a
    no-op — run 08-02-PLAN.md instead." Then stop execution of this plan entirely. This guard is
    the actual enforcement mechanism for the mutual exclusivity between 08-02 and 08-03 —
    wave/depends_on frontmatter and execute-phase's wave tooling have no field or mechanism for
    "run exactly one of these two plans based on a prior plan's recorded decision," so this hard
    read-then-fail check is what prevents an accidental dual-dispatch from corrupting the codebase
    (in this plan's case, a git revert of Wave-0 fix code that 08-02 may simultaneously be
    building on).

    If the recorded decision is option-c: proceed to Task 1.
  </action>
  <acceptance_criteria>
    - `08-01-SUMMARY.md` has been read and its recorded decision (option-a/option-b/option-c) identified before any other file in this plan is touched
    - If option-a or option-b was recorded: this plan halts at Task 0 with zero modifications to `Islet/Notch/FullscreenSpaceProbe.swift`, `Islet/Notch/NotchWindowController.swift`, or `08-ESCALATION.md`, and the user is told to run 08-02-PLAN.md instead
    - If option-c was recorded: execution proceeds to Task 1
  </acceptance_criteria>
  <verify>
    <automated>grep -oE "option-[abc]" .planning/phases/08-fullscreen-enter-flash-elimination/08-01-SUMMARY.md | head -1</automated>
  </verify>
  <done>Either the plan has halted with the exact option-a/option-b mismatch message and no code changed, or the option-c decision is confirmed and Task 1 begins.</done>
</task>

<task type="auto">
  <name>Task 1: Revert all Wave-0 exploratory probe code</name>
  <files>Islet/Notch/FullscreenSpaceProbe.swift, Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/FullscreenSpaceProbe.swift (as left by 08-01 Task 1 — the appended
    CGSRegisterNotifyProc/CGSRemoveNotifyProc bindings, CGSNotifyProc typealias, and the 106/107
    constants)
    Islet/Notch/NotchWindowController.swift (as left by 08-01 Task 1 — the `#if DEBUG`-gated
    `fullscreenProbeCallback`/`probeContext`/registration/teardown, the temporary `[FS-01 probe]`
    prints inside `spaceObserver`/`appActivateObserver`, and `handleFullscreenProbeEvent(type:)`)
  </read_first>
  <action>
    Determine the git commit that introduced 08-01's probe changes (`git log --oneline -3 --
    Islet/Notch/FullscreenSpaceProbe.swift Islet/Notch/NotchWindowController.swift`) and revert
    BOTH files to their state immediately prior to that commit — either via `git checkout
    &lt;pre-08-01-commit&gt; -- Islet/Notch/FullscreenSpaceProbe.swift
    Islet/Notch/NotchWindowController.swift`, or by manually removing every addition Task 1 of
    08-01-PLAN.md made (the constants, typealias, bindings, callback, context property, `start()`
    registration, `deinit` teardown, `handleFullscreenProbeEvent`, and the temporary
    `[FS-01 probe]` prints inside the two existing `NSWorkspace` observer closures — restoring
    them to their original one-line `{ [weak self] _ in self?.updateVisibility() }` bodies). The
    goal is a file state byte-identical to what existed before Phase 8 began — D-03 requires
    leaving the v1.0/Phase-2/Phase-6 reactive behavior EXACTLY as it shipped, not almost.
  </action>
  <acceptance_criteria>
    - `git diff HEAD~<N> -- Islet/Notch/FullscreenSpaceProbe.swift Islet/Notch/NotchWindowController.swift` (comparing against the pre-Phase-8 commit) shows zero differences, OR a manual diff review confirms byte-for-byte equivalence
    - `grep -c 'CGSRegisterNotifyProc\|CGSRemoveNotifyProc\|fullscreenProbeCallback\|FS-01 probe' Islet/Notch/FullscreenSpaceProbe.swift Islet/Notch/NotchWindowController.swift` is 0 across both files
    - `xcodebuild build -scheme Islet` succeeds
    - `xcodebuild test -scheme Islet` (full suite) passes, matching the pre-Phase-8 baseline exactly
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -scheme Islet</automated>
  </verify>
  <done>Both files are restored to their exact pre-Phase-8 state; the shipped v1.0 reactive fullscreen behavior is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Write the root-cause escalation report</name>
  <files>.planning/phases/08-fullscreen-enter-flash-elimination/08-ESCALATION.md</files>
  <read_first>
    .planning/phases/08-fullscreen-enter-flash-elimination/08-01-SUMMARY.md (this phase's own
    concrete on-device evidence — the raw [FS-01 probe] timestamps/observations that led to the
    option-c verdict)
    .planning/phases/08-fullscreen-enter-flash-elimination/08-RESEARCH.md (Candidate B —
    `SLSManagedDisplayIsAnimating` — documented as an untried fallback, for completeness)
    .planning/debug/resolved/fullscreen-enter-flash.md (the Phase-6 re-confirmation debug session)
    .planning/phases/02-hover-expand-fullscreen-hardening/02-04-SUMMARY.md (the original Phase-2
    root-cause diagnosis)
    .planning/REQUIREMENTS.md (FS-01 text + the Out-of-Scope row excluding partial mitigation)
  </read_first>
  <action>
    Write `.planning/phases/08-fullscreen-enter-flash-elimination/08-ESCALATION.md` with these
    sections: `## Root Cause` (the window-server compositor timing gap, citing Phase 2/Phase 6's
    prior diagnoses AND this phase's own new finding — quote the specific [FS-01 probe] evidence
    from 08-01-SUMMARY.md showing why CGS event 106/107 does not close the gap: either it never
    fired cross-process, or it fired no earlier than the existing `activeSpaceDidChangeNotification`/
    `didActivateApplicationNotification` signals); `## What Was Tried This Phase` (the concrete new
    candidate investigated, satisfying D-07 — not a re-statement of prior conclusions);
    `## Untried Fallback` (document `SLSManagedDisplayIsAnimating`, RESEARCH.md's secondary
    candidate — a poll requiring a new `SkyLight.framework` linker setting plus a
    new-fullscreen-Space disambiguator against ordinary Space switches — as an available but
    unexplored option, should the user want a further investigation session); `## Requested
    Decision` (per D-04: ask the user to choose between accepting this as permanent technical debt
    vs. formally descoping FS-01 in REQUIREMENTS.md/ROADMAP.md, vs. requesting a follow-up session
    to try the untried fallback).
  </action>
  <acceptance_criteria>
    - `.planning/phases/08-fullscreen-enter-flash-elimination/08-ESCALATION.md` exists and contains the headings `## Root Cause`, `## What Was Tried This Phase`, `## Untried Fallback`, and `## Requested Decision`
    - The `## What Was Tried This Phase` section quotes or cites specific evidence from `08-01-SUMMARY.md` (not a generic restatement)
    - The `## Requested Decision` section names all three options explicitly: accept as technical debt, formally descope FS-01, or request a follow-up investigation
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/08-fullscreen-enter-flash-elimination/08-ESCALATION.md && grep -c '^## ' .planning/phases/08-fullscreen-enter-flash-elimination/08-ESCALATION.md</automated>
  </verify>
  <done>A complete, evidence-grounded escalation report exists, ready to present to the user.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Surface the escalation report — record the user's FS-01 scope decision</name>
  <files>.planning/phases/08-fullscreen-enter-flash-elimination/08-ESCALATION.md</files>
  <action>
    No code changes. Present `08-ESCALATION.md` to the user and record their selection among the
    three `<options>` below (per D-04 — this must be an explicit, recorded decision, never a
    silent carry-over).
  </action>
  <decision>
    How should FS-01 be resolved, given that this phase's on-device investigation found no
    proactive fullscreen-transition signal that closes the compositor-timing gap at the
    application layer?
  </decision>
  <context>
    See `.planning/phases/08-fullscreen-enter-flash-elimination/08-ESCALATION.md` for the full
    root-cause evidence (this phase's own on-device CGS event 106/107 timing probe, plus the prior
    Phase-2/Phase-6 diagnoses it corroborates). Per D-04, this must NOT be silently resolved by
    shipping a partial/best-effort mitigation — REQUIREMENTS.md explicitly excludes that outcome.
  </context>
  <options>
    <option id="option-accept">
      <name>Accept as permanent technical debt</name>
      <pros>Closes the phase now; no further investigation spend. Matches the pre-existing STATE.md blocker note that this was already accepted as deferred once before.</pros>
      <cons>The ~1-frame flash remains in shipped builds indefinitely.</cons>
    </option>
    <option id="option-descope">
      <name>Formally descope FS-01</name>
      <pros>Makes the decision explicit and traceable in REQUIREMENTS.md/ROADMAP.md rather than an implicit carry-over.</pros>
      <cons>Requires an explicit REQUIREMENTS.md/ROADMAP.md edit to reflect the descope.</cons>
    </option>
    <option id="option-investigate-b">
      <name>Request a follow-up investigation of the untried SLSManagedDisplayIsAnimating fallback</name>
      <pros>One documented avenue (Candidate B) remains untried on-device; a future session could still close the gap.</pros>
      <cons>Requires a new `project.yml` linker setting (`SkyLight.framework`) and a new Space-switch disambiguator design (untested reasoning per RESEARCH.md) — a genuinely new investigation phase, not a quick fix.</cons>
    </option>
  </options>
  <verify>
    <human-check>The user has reviewed 08-ESCALATION.md and explicitly selected one of the three options.</human-check>
  </verify>
  <done>A recorded, explicit user decision (option-accept/option-descope/option-investigate-b) closes out the phase.</done>
  <resume-signal>Select: option-accept, option-descope, or option-investigate-b</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Exploratory/private-API code → shipped codebase | This plan's own scope is to ENSURE no exploratory private-CGS-API code from the Wave-0 probe survives into a state that could ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering (unreviewed leftover private-API code) | `Islet/Notch/FullscreenSpaceProbe.swift`, `Islet/Notch/NotchWindowController.swift` | mitigate | Task 1's acceptance criteria require a byte-for-byte diff against the pre-Phase-8 commit before this plan is considered done — no partial/half-reverted probe code can remain |
| T-08-06 | Repudiation (undocumented scope decision) | Phase closure | mitigate | The escalation report + checkpoint:decision produce a written, timestamped record of why no fix shipped and what the user explicitly decided, rather than a silent carry-over |
| T-08-07 | Tampering (dual-dispatch of mutually exclusive branch plans) | 08-02/08-03 wave-1 plan pair | mitigate | Task 0 in this plan (and its mirror in 08-02) reads 08-01-SUMMARY.md's recorded decision and hard-fails as a no-op before any irreversible action (in this plan's case, a git revert) if the precondition doesn't match, since wave/depends_on frontmatter cannot express "run exactly one of these" |

</threat_model>

<verification>
`xcodebuild test -scheme Islet` passes against the reverted, pre-Phase-8-identical codebase.
`08-ESCALATION.md` exists with all required sections. A recorded user decision
(option-accept/option-descope/option-investigate-b) closes out the phase.
</verification>

<success_criteria>
No code change ships for FS-01 when no proactive signal was found; a complete, evidence-based
escalation report exists; the user has made an explicit, recorded scope decision — satisfying
D-03/D-04 without silently shipping a partial mitigation.
</success_criteria>

<output>
Create `.planning/phases/08-fullscreen-enter-flash-elimination/08-03-SUMMARY.md` when done.
</output>
