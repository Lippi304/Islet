---
phase: 08-fullscreen-enter-flash-elimination
plan: 02
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - Islet/Notch/FullscreenSpaceProbe.swift
  - Islet/Notch/FullscreenDetector.swift
  - Islet/Notch/NotchWindowController.swift
  - IsletTests/VisibilityDecisionTests.swift
autonomous: false
requirements: [FS-01]
user_setup: []

must_haves:
  truths:
    - "Entering native fullscreen on the built-in display (green-button, menu bar, fullscreen video) shows zero visible island flash during or after the transition, across repeated trials"
    - "The fix uses a genuinely new proactive detection/timing signal (CGS event 106/107) distinct from v1.0's reactive orderOut approach, not a debounce or best-effort reduction"
    - "The island still hides for the full duration of fullscreen and still restores correctly on exit"
    - "updateVisibility() remains the SOLE show/hide call site — the new signal feeds it, never bypasses it"
  artifacts:
    - path: "Islet/Notch/FullscreenSpaceProbe.swift"
      provides: "Permanent (non-DEBUG-gated) CGSRegisterNotifyProc/CGSRemoveNotifyProc bindings + kCGSClientEnterFullscreen/kCGSClientExitFullscreen constants, now load-bearing"
      contains: "CGSRegisterNotifyProc"
    - path: "Islet/Notch/NotchWindowController.swift"
      provides: "Permanent fullscreenTransitionCallback + selfContext registered/torn-down in start()/deinit, calling handleFullscreenTransitionEvent(type:) which ONLY mutates state and calls updateVisibility()"
      contains: "handleFullscreenTransitionEvent"
    - path: "Islet/Notch/FullscreenDetector.swift"
      provides: "shouldShow(...) — extended with pendingFullscreenTransition ONLY if 08-01 recorded option-b; otherwise untouched"
      contains: "func shouldShow"
    - path: "IsletTests/VisibilityDecisionTests.swift"
      provides: "Boolean-matrix coverage for the new pendingFullscreenTransition axis (option-b only)"
      contains: "shouldShow(hasTarget:"
  key_links:
    - from: "Islet/Notch/NotchWindowController.swift (fullscreenTransitionCallback)"
      to: "Islet/Notch/NotchWindowController.swift (updateVisibility)"
      via: "handleFullscreenTransitionEvent calls updateVisibility() — never orderOut/positionAndShow directly"
      pattern: "handleFullscreenTransitionEvent"
    - from: "Islet/Notch/NotchWindowController.swift (updateVisibility)"
      to: "Islet/Notch/FullscreenDetector.swift (shouldShow)"
      via: "updateVisibility passes the fullscreen + (if option-b) pendingFullscreenTransition inputs into the single pure predicate"
      pattern: "shouldShow\\(hasTarget:"
---

<!--
PRECONDITION — READ BEFORE EXECUTING:
This plan executes ONLY if 08-01-PLAN.md's recorded decision was option-a or option-b.
If 08-01's SUMMARY.md recorded option-c (Candidate A disproven), DO NOT execute this plan.
Execute 08-03-PLAN.md (the escalation path) instead. 08-02 and 08-03 are mutually exclusive
alternatives gated on the same Wave-0 evidence — never run both.
-->

<objective>
Wave 1 (fix path) of Phase 8: build the real, permanent FS-01 fix using the CGS event 106/107
signal that 08-01's on-device probe confirmed works (option-a: simple 2-observer design, or
option-b: bounded pendingFullscreenTransition design) — replacing the Wave-0 DEBUG-only
instrumentation with shipped, non-DEBUG-gated code, extending test coverage, and re-verifying
on-device that the flash is gone with no regression.

Purpose: close FS-01 as a genuine root-cause elimination (not a debounce, not a partial
mitigation) per REQUIREMENTS.md's "Out of Scope: best-effort/partial reduction" exclusion.
Output: a permanently wired proactive fullscreen-transition signal feeding the existing single
`updateVisibility()` arbiter (Pattern 7), with the Wave-0 probe's throwaway logging fully removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fullscreen-enter-flash-elimination/08-CONTEXT.md
@.planning/phases/08-fullscreen-enter-flash-elimination/08-RESEARCH.md
@.planning/phases/08-fullscreen-enter-flash-elimination/08-PATTERNS.md
@.planning/phases/08-fullscreen-enter-flash-elimination/08-01-SUMMARY.md

<interfaces>
<!-- Current pure predicate + its full test suite, read in full during planning. Extend EXACTLY
     this way — no exploration needed. -->

From Islet/Notch/FullscreenDetector.swift (lines 25-31, the ONLY function this plan may extend):
```swift
func shouldShow(hasTarget: Bool, hideInFullscreen: Bool, isFullscreen: Bool) -> Bool {
    hasTarget && !(hideInFullscreen && isFullscreen)
}
```
If 08-01 recorded option-b, extend to (RESEARCH.md "Integration Point" — the ONE explicitly
permitted exception to "FullscreenDetector.swift stays untouched", per 08-CONTEXT.md canonical_refs):
```swift
func shouldShow(hasTarget: Bool, hideInFullscreen: Bool, isFullscreen: Bool, pendingFullscreenTransition: Bool) -> Bool {
    hasTarget && !(hideInFullscreen && (isFullscreen || pendingFullscreenTransition))
}
```

From IsletTests/VisibilityDecisionTests.swift (full file, 6 existing tests, one assertion each,
e.g.):
```swift
func testTargetPresentFullscreenWithHideFlagHides() {
    XCTAssertFalse(shouldShow(hasTarget: true, hideInFullscreen: true, isFullscreen: true))
}
```
If option-b, EVERY existing call site above needs the new 4th parameter added (compile-breaking
otherwise, default it explicitly to `false` at each existing call site rather than relying on a
default parameter value, to keep every test's intent explicit) PLUS two new cases:
```swift
func testPendingTransitionAloneHidesLikeFullscreen() {
    XCTAssertFalse(shouldShow(hasTarget: true, hideInFullscreen: true, isFullscreen: false, pendingFullscreenTransition: true))
}
func testPendingTransitionIgnoredWhenHideFlagOff() {
    XCTAssertTrue(shouldShow(hasTarget: true, hideInFullscreen: false, isFullscreen: false, pendingFullscreenTransition: true))
}
```

From Islet/Notch/NotchWindowController.swift — the Wave-0 probe scaffold this plan promotes/removes
(see 08-01-PLAN.md Task 1: `fullscreenProbeCallback`, `probeContext`, the `#if DEBUG`-gated
registration in `start()`/`deinit`, the temporary `[FS-01 probe]` prints inside `spaceObserver`/
`appActivateObserver`, and `handleFullscreenProbeEvent(type:)`) — ALL of this is removed/replaced
by this plan's permanent equivalent (see Task 2).

From Islet/Notch/NotchWindowController.swift `updateVisibility()` (lines 407-441 pre-Wave-0, the
SOLE show/hide arbiter — Pattern 7):
```swift
private func updateVisibility() {
    let descriptors = NSScreen.screens.map { $0.descriptor }
    let target = selectTargetScreen(from: descriptors)
    let fullscreen = isBuiltinDisplayInFullscreenSpace(builtinUUID: currentBuiltin()?.uuid)
    if shouldShow(hasTarget: target != nil, hideInFullscreen: hideInFullscreen, isFullscreen: fullscreen), let target {
        positionAndShow(on: target)
    } else {
        panel?.orderOut(nil)
        hotZone = nil; expandedZone = nil; pointerInZone = false
    }
}
```
If option-b, this call site must pass `pendingFullscreenTransition: pendingFullscreenTransition`
(the new controller-owned property) through to `shouldShow(...)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finalize the pure predicate layer (option-b only) and its tests</name>
  <files>Islet/Notch/FullscreenDetector.swift, IsletTests/VisibilityDecisionTests.swift</files>
  <read_first>
    .planning/phases/08-fullscreen-enter-flash-elimination/08-01-SUMMARY.md (the recorded
    option-a/option-b decision this task branches on)
    Islet/Notch/FullscreenDetector.swift (full file, 31 lines)
    IsletTests/VisibilityDecisionTests.swift (full file, 40 lines — every existing call site)
  </read_first>
  <behavior>
    - IF 08-01 recorded option-a: make NO changes to FullscreenDetector.swift or
      VisibilityDecisionTests.swift in this task — the 3-parameter shouldShow(...) signature and
      its 6 existing tests already fully cover the shipped behavior. Confirm this by re-running
      the existing suite unchanged (see verify).
    - IF 08-01 recorded option-b:
      - Test: shouldShow(hasTarget: true, hideInFullscreen: true, isFullscreen: false, pendingFullscreenTransition: true) -> false (a pending transition alone hides, same as isFullscreen)
      - Test: shouldShow(hasTarget: true, hideInFullscreen: false, isFullscreen: false, pendingFullscreenTransition: true) -> true (hideInFullscreen off neutralizes pendingFullscreenTransition exactly like it neutralizes isFullscreen)
      - Test: shouldShow(hasTarget: false, hideInFullscreen: true, isFullscreen: false, pendingFullscreenTransition: true) -> false (no target still dominates)
  </behavior>
  <action>
    Read 08-01-SUMMARY.md first to determine the branch. If option-b: add the 4th parameter
    `pendingFullscreenTransition: Bool` to `shouldShow(...)` in FullscreenDetector.swift exactly as
    shown in the interfaces block above (OR the condition into the existing `hideInFullscreen &&`
    clause), updating the doc comment above it to describe the new input as "Phase 8 (FS-01): a
    bounded, self-healing override set the instant a proactive CGS enter-fullscreen event fires,
    cleared on steady-state catch-up / exit event / timeout — see NotchWindowController". Then in
    VisibilityDecisionTests.swift, add `pendingFullscreenTransition: false` as an explicit 4th
    argument to ALL 6 existing test call sites (keeps their original intent unchanged and unbroken
    under the new signature) and append the 3 new test functions from the `<behavior>` block above,
    named `testPendingTransitionAloneHidesLikeFullscreen`, `testPendingTransitionIgnoredWhenHideFlagOff`,
    `testNoTargetHidesEvenWithPendingTransition`, mirroring the file's existing one-assertion-per-test
    style exactly.
  </action>
  <acceptance_criteria>
    - If option-a: `git diff Islet/Notch/FullscreenDetector.swift IsletTests/VisibilityDecisionTests.swift` is empty
    - If option-b: `shouldShow` has 4 parameters including `pendingFullscreenTransition: Bool`
    - If option-b: `IsletTests/VisibilityDecisionTests.swift` contains `testPendingTransitionAloneHidesLikeFullscreen`, `testPendingTransitionIgnoredWhenHideFlagOff`, and `testNoTargetHidesEvenWithPendingTransition`
    - If option-b: all 6 pre-existing test functions still compile and pass with the new 4th argument
    - `xcodebuild test -scheme Islet -only-testing:IsletTests/VisibilityDecisionTests` passes with zero failures
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -scheme Islet -only-testing:IsletTests/VisibilityDecisionTests</automated>
  </verify>
  <done>The pure predicate layer matches exactly what 08-01 determined is needed, fully covered by unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the permanent fullscreen-transition signal, remove Wave-0 probe scaffolding</name>
  <files>Islet/Notch/FullscreenSpaceProbe.swift, Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    .planning/phases/08-fullscreen-enter-flash-elimination/08-01-SUMMARY.md (the recorded decision)
    Islet/Notch/NotchWindowController.swift (as left by 08-01 Task 1 — the `#if DEBUG`-gated probe
    scaffold: `fullscreenProbeCallback`, `probeContext`, its `start()`/`deinit` registration, the
    temporary `[FS-01 probe]` prints inside `spaceObserver`/`appActivateObserver`, and
    `handleFullscreenProbeEvent(type:)`)
    Islet/Notch/NotchWindowController.swift `scheduleDeviceBatteryRefresh` (lines 791-819, the
    one-shot cancel-and-replace `DispatchWorkItem` idiom to mirror for the option-b safety timeout)
    Islet/Notch/NotchWindowController.swift `updateVisibility()` (lines 407-441 — the SOLE show/hide
    arbiter this new signal must feed, never bypass)
  </read_first>
  <action>
    In FullscreenSpaceProbe.swift: promote the `kCGSClientEnterFullscreen`/`kCGSClientExitFullscreen`
    constants and the `CGSNotifyProc`/`CGSRegisterNotifyProc`/`CGSRemoveNotifyProc` bindings added in
    08-01 Task 1 from probe-only to permanent, shipped bindings (they are load-bearing now, no code
    change needed here beyond removing any probe-only comment referring to them as temporary).

    In NotchWindowController.swift:
    1. Remove the `#if DEBUG`-gated `fullscreenProbeCallback`/`probeContext`/registration/teardown
       and `handleFullscreenProbeEvent(type:)` added in 08-01 Task 1 in their entirety.
    2. Revert the temporary `[FS-01 probe]` print statements inside the `spaceObserver`/
       `appActivateObserver` closures (lines 256-263) back to their original one-line bodies
       (`{ [weak self] _ in self?.updateVisibility() }`) — no diagnostic logging ships.
    3. Add PERMANENT (non-DEBUG-gated) equivalents: `private let fullscreenTransitionCallback:
       CGSNotifyProc` (unwraps `userData` via `Unmanaged<NotchWindowController>` and
       `DispatchQueue.main.async`-dispatches into `handleFullscreenTransitionEvent(type:)`), and
       `private lazy var selfContext = Unmanaged.passUnretained(self).toOpaque()`.
    4. In `start()`, alongside the existing `spaceObserver`/`appActivateObserver` registration:
       `CGSRegisterNotifyProc(fullscreenTransitionCallback, kCGSClientEnterFullscreen, selfContext)`
       and the mirror call for `kCGSClientExitFullscreen`.
    5. In `deinit`, alongside the existing observer teardown: `CGSRemoveNotifyProc` for both event
       codes with the identical `fullscreenTransitionCallback`/type/`selfContext` triple used at
       registration.
    6. Implement `handleFullscreenTransitionEvent(type: UInt32)`:
       - IF 08-01 recorded option-a: the body is simply `updateVisibility()` — no new state, no
         predicate change. Both 106 and 107 just re-run the single arbiter.
       - IF 08-01 recorded option-b: add a new controller property
         `private var pendingFullscreenTransition = false` and
         `private var pendingFullscreenTimeout: DispatchWorkItem?`. On `type ==
         kCGSClientEnterFullscreen`: set `pendingFullscreenTransition = true`, cancel any existing
         `pendingFullscreenTimeout`, schedule a fresh one-shot `DispatchWorkItem` (mirroring
         `scheduleDeviceBatteryRefresh`'s cancel-and-replace shape) that clears
         `pendingFullscreenTransition = false` and calls `updateVisibility()` after a ~1.0s bounded
         safety timeout (fail-safe-to-visible — an unconfirmed pending flag must self-clear, never
         hide permanently), then call `updateVisibility()` immediately. On `type ==
         kCGSClientExitFullscreen`: cancel `pendingFullscreenTimeout`, clear
         `pendingFullscreenTransition = false`, call `updateVisibility()`. Also add the steady-state
         catch-up clear: inside `updateVisibility()`, immediately after reading `fullscreen` via
         `isBuiltinDisplayInFullscreenSpace(...)`, if `fullscreen == true` clear
         `pendingFullscreenTransition = false` and cancel `pendingFullscreenTimeout` (the real
         signal has caught up — steady-state takes over, per RESEARCH.md's 3-way clear-path design).
       - In EITHER variant, `handleFullscreenTransitionEvent` must ONLY mutate state and call
         `updateVisibility()` — it must NEVER call `panel?.orderOut`/`positionAndShow` directly
         (Pattern 7 — the second-show/hide-path anti-pattern RESEARCH.md explicitly forbids).
    7. If option-b, update the `updateVisibility()` call site to pass
       `pendingFullscreenTransition: pendingFullscreenTransition` as the 4th argument to
       `shouldShow(...)`; if option-a, `updateVisibility()`'s `shouldShow(...)` call is unchanged
       (still 3 arguments).
    8. In `deinit`, also cancel `pendingFullscreenTimeout` (option-b only), mirroring the existing
       `graceWorkItem?.cancel()` / `deviceBatteryWork?.cancel()` teardown lines.
  </action>
  <acceptance_criteria>
    - `grep -c 'fullscreenProbeCallback' Islet/Notch/NotchWindowController.swift` is 0 (Wave-0 probe scaffold fully removed)
    - `grep -c '\[FS-01 probe\]' Islet/Notch/NotchWindowController.swift` is 0 (all temporary diagnostic prints removed)
    - `Islet/Notch/NotchWindowController.swift` contains `fullscreenTransitionCallback` and `handleFullscreenTransitionEvent` OUTSIDE any `#if DEBUG` block
    - `Islet/Notch/NotchWindowController.swift`'s `deinit` calls `CGSRemoveNotifyProc` for both `kCGSClientEnterFullscreen` and `kCGSClientExitFullscreen`
    - `handleFullscreenTransitionEvent` never calls `panel?.orderOut` or `positionAndShow` directly (only `updateVisibility()`)
    - If option-b: `pendingFullscreenTransition` is threaded into the `shouldShow(...)` call site inside `updateVisibility()`
    - `xcodebuild build -scheme Islet` succeeds with zero errors and zero warnings about unused `#if DEBUG` symbols
    - `xcodebuild test -scheme Islet` (full suite) passes
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -scheme Islet</automated>
  </verify>
  <done>The permanent fullscreen-transition signal is wired end-to-end through the single updateVisibility() arbiter, Wave-0 probe code is fully removed, and the full test suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device UAT — zero flash across the full D-05 trigger matrix, no regressions</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <action>
    No code changes. Build and run Islet on-device (real notch hardware — this is a visual
    regression check with no automated screen-flash detector, per 08-VALIDATION.md's
    Manual-Only Verifications table) and walk through the steps in `<how-to-verify>` below.
  </action>
  <what-built>
    A permanent, proactive CGS event 106/107 signal now feeds NotchWindowController's single
    updateVisibility() arbiter (option-a: directly; option-b: via a bounded pendingFullscreenTransition
    flag ANDed into FullscreenDetector.shouldShow). All Wave-0 DEBUG probe/logging code has been
    removed.
  </what-built>
  <how-to-verify>
    Run the app (Xcode Run or a Debug build is fine — no DEBUG-only code remains that affects
    behavior) and repeat the full D-05 trigger matrix, each method at least 3 trials:
    1. Green-button click on any app window.
    2. Menu bar "View > Enter Full Screen".
    3. A fullscreen video app (QuickTime Player or Safari video fullscreen).
    For each trial confirm: (a) ZERO visible island flash at any point during or after entering
    fullscreen, (b) the island stays fully hidden for the ENTIRE duration of fullscreen (no
    reappearing), (c) exiting fullscreen restores the island correctly and promptly. Additionally
    confirm no regression on ordinary (non-fullscreen) behavior: a normal Space switch, a merely
    maximized window (not true fullscreen), and clamshell/external-display-only still behave
    exactly as before (island visible on a normal desktop Space, stays hidden appropriately for
    maximized-is-not-fullscreen per D-09, hidden when no built-in target per D-04).
  </how-to-verify>
  <verify>
    <human-check>D-05 trigger matrix (green-button, menu-bar, video app; ≥3 trials each) shows zero flash, correct hide-throughout/restore-on-exit, and no regression to normal-Space/maximized/clamshell behavior.</human-check>
  </verify>
  <done>User has typed "approved" (or the described issues have been triaged and resolved) confirming zero flash across all trials and no regressions.</done>
  <resume-signal>Type "approved" if zero flash across all trials and no regressions, or describe exactly what you observed (which trigger method, which trial, what appeared)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WindowServer (private CGS API) → app process | Raw, undocumented C callback (`CGSRegisterNotifyProc`) delivered from the OS window-server connection into this un-sandboxed app, with no main-thread guarantee and no Apple-documented payload contract; this plan promotes it from throwaway probe to a PERMANENT, shipped code path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Denial of Service (crash) | `fullscreenTransitionCallback` (NotchWindowController.swift) | mitigate | Callback body only hops via `DispatchQueue.main.async` before touching `self`/AppKit/`@Published` state — never touches controller state on the raw callback thread |
| T-08-02 | Tampering (use-after-free / dangling pointer) | `selfContext` (`Unmanaged<NotchWindowController>`) | mitigate | `CGSRemoveNotifyProc` called in `deinit` with the EXACT same `fullscreenTransitionCallback`/`type`/`selfContext` triple used at registration, mirroring `spaceObserver`/`appActivateObserver`/`powerMonitor` teardown in the same `deinit` |
| T-08-03 | Information Disclosure / instability | CGS event payload (`data`/`dataLength` params) | accept (mitigated by design) | The callback only reads the `type` parameter; `data`/`dataLength` are never dereferenced or parsed — no untrusted payload parsing surface |
| T-08-04 | Denial of Service (self-inflicted — permanent hide) | `pendingFullscreenTransition` flag (option-b only) | mitigate | Bounded ~1.0s `DispatchWorkItem` safety-timeout (cancel-and-replace, mirroring `scheduleDeviceBatteryRefresh`) PLUS the steady-state catch-up clear inside `updateVisibility()` PLUS the 107 exit-event clear — three independent clear paths, never an unbounded override, per the fail-safe-to-visible philosophy established in `FullscreenSpaceProbe.swift` |
| T-08-05 | Elevation of Privilege / architecture violation | `handleFullscreenTransitionEvent` | mitigate | Body is restricted (by code review / acceptance criteria) to mutating state + calling `updateVisibility()` only — never calls `panel?.orderOut`/`positionAndShow` directly, preserving Pattern 7 (the single show/hide arbiter) |

</threat_model>

<verification>
`xcodebuild test -scheme Islet` (full suite) passes. On-device: the final checkpoint's D-05 trigger
matrix (≥3 trials per method) confirms zero flash, correct hide-throughout/restore-on-exit, and no
regression to normal-Space/maximized/clamshell behavior.
</verification>

<success_criteria>
FS-01 is closed as a genuine root-cause elimination: the island never visibly flashes when entering
native fullscreen (any of the 3 D-05 methods, repeated trials), existing fullscreen hide/restore
behavior is unregressed, and `updateVisibility()` remains the sole show/hide call site.
</success_criteria>

<output>
Create `.planning/phases/08-fullscreen-enter-flash-elimination/08-02-SUMMARY.md` when done.
</output>
