---
phase: 06-priority-resolver-settings-v1-ship
plan: 05
type: execute
wave: 3
depends_on: ["06-04"]
files_modified:
  - project.yml
autonomous: false
requirements: [APP-04]
user_setup:
  - service: "Apple Developer ID + notary credentials"
    why: "Real Developer-ID sign -> notarize -> staple + clean-second-Mac open (success criterion 3) — DEFERRED carry-over (D-15), no $99/yr account yet. The release runs as a placeholder-gated DRY-RUN this phase."
must_haves:
  truths:
    - "project.yml ships version 0.x (D-14), product name Islet (D-13)"
    - "scripts/release.sh runs UNCHANGED, exits 0 with the loud SKIP banner, and produces dist/Islet.dmg via hdiutil UDZO"
    - "The Now Playing launch-time health check + the 'nicht verfuegbar' fallback still pass on the currently installed macOS (D-16)"
  artifacts:
    - path: "project.yml"
      provides: "MARKETING_VERSION 0.x (D-14) — the private first-release version"
      contains: "MARKETING_VERSION"
    - path: "dist/Islet.dmg"
      provides: "The dry-run distributable (ad-hoc signed, NOT notarized — D-15)"
  key_links:
    - from: "scripts/release.sh"
      to: "dist/Islet.dmg"
      via: "hdiutil create -format UDZO"
      pattern: "format UDZO"
---

<objective>
Close out v1 with the ship gate (APP-04, dry-run per D-15). Three jobs:
1. **Version bump (D-13/D-14):** set `project.yml` to the private-release version `0.x`
   (currently `MARKETING_VERSION: "1.0"`), keep `PRODUCT_NAME: Islet`. `1.0` is reserved
   for the later public/sellable launch.
2. **Release dry-run (D-15):** run the EXISTING Phase-0 `scripts/release.sh` UNCHANGED. With
   the Developer-ID/notary placeholders unfilled it ad-hoc signs, builds the DMG via `hdiutil`
   UDZO, and exits 0 with the loud SKIP banner. The real Developer-ID sign -> notarize ->
   staple + clean-second-Mac open is the DEFERRED carry-over (no account yet).
3. **Now Playing health re-check (D-16):** on-device, confirm the `NowPlayingMonitor` launch
   health check flips `isHealthy=true` (a callback arrived) on the currently installed macOS 26,
   and that the "Now Playing nicht verfuegbar" fallback is intact — each macOS update is a
   Now-Playing regression event (Phase-4 standing blocker). This cannot be unit-tested (it
   spawns a real perl child — RESEARCH Pitfall 6).

Purpose: APP-04 — the app ships as a (dry-run) notarizable build; v1 closes.
Output: `project.yml` at 0.x + `dist/Islet.dmg` (dry-run) + a logged D-16 health verdict.

DEFERRED carry-overs (do NOT block the phase): the REAL Developer-ID notarize/staple +
second-Mac open (needs the $99/yr account, D-15); the public name + 1.0 (D-13/D-14).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-priority-resolver-settings-v1-ship/06-CONTEXT.md
@.planning/phases/06-priority-resolver-settings-v1-ship/06-RESEARCH.md

# The version source + the release pipeline (runs UNCHANGED) + the health check to re-verify:
@project.yml
@scripts/release.sh
@Islet/Notch/NowPlayingMonitor.swift

<interfaces>
<!-- Exact strings to change / expect. Extracted from the codebase. -->

From project.yml (the Islet target settings — change ONLY the version; keep name/bundle id):
  MARKETING_VERSION: "1.0"        -> change to "0.1" (D-14); becomes CFBundleShortVersionString
  CURRENT_PROJECT_VERSION: "1"    build number (may stay "1" or bump — discretion)
  PRODUCT_NAME: Islet             D-13 unchanged
  PRODUCT_BUNDLE_IDENTIFIER: com.lippi304.islet  unchanged

From scripts/release.sh (runs UNCHANGED — the expected dry-run output strings):
  "-> No Developer ID set: AD-HOC signing for local dry-run (D-03)."
  hdiutil create -volname "Islet" -srcfolder ... -ov -format UDZO "dist/Islet.dmg"
  "SKIPPING notarize + staple — placeholders not filled (Phase 6 step)."
  "Phase-0 dry run complete: dist/Islet.dmg (ad-hoc signed, NOT notarized)."
  exit 0

From Islet/Notch/NowPlayingMonitor.swift (the D-16 health check to re-verify on-device):
  func runHealthCheck(then setHealthy: @escaping (Bool) -> Void)
  // callback within 3s -> healthy(true); else healthy(false) -> "nicht verfuegbar"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump project.yml to version 0.x (private first release)</name>
  <read_first>
    - project.yml (the Islet target `settings.base` — MARKETING_VERSION currently "1.0"; confirm PRODUCT_NAME: Islet stays)
    - 06-CONTEXT.md D-13 (name stays Islet), D-14 (version 0.1 / 0.x — 1.0 reserved for the public launch)
    - Memory note: the SettingsView Version label reads CFBundleShortVersionString = MARKETING_VERSION, so this is what the user sees
  </read_first>
  <action>
Edit `project.yml`: change the Islet target's `MARKETING_VERSION: "1.0"` to `MARKETING_VERSION: "0.1"`
(D-14 — private first release). Keep `CURRENT_PROJECT_VERSION` as-is (or bump to "2" — discretion; the
build number is not user-facing). Do NOT change `PRODUCT_NAME: Islet`, `CFBundleDisplayName: Islet`, or
`PRODUCT_BUNDLE_IDENTIFIER: com.lippi304.islet` (D-13 — name unchanged for v1).

Run `xcodegen generate`; build the app target so the regenerated Info.plist carries 0.1.
Commit: `chore(06-05): bump version to 0.1 for the private v1 release (D-14)`
  </action>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && grep -q 'MARKETING_VERSION: "0.1"' project.yml && grep -q 'PRODUCT_NAME: Islet' project.yml && xcodegen generate >/dev/null && xcodebuild build -scheme Islet -destination 'platform=macOS' 2>&1 | tail -3 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'MARKETING_VERSION: "0.1"' project.yml` succeeds (D-14)
    - `grep -q 'MARKETING_VERSION: "1.0"' project.yml` FAILS (old value gone)
    - `grep -q 'PRODUCT_NAME: Islet' project.yml` succeeds (D-13 name unchanged)
    - `grep -q 'PRODUCT_BUNDLE_IDENTIFIER: com.lippi304.islet' project.yml` succeeds (bundle id unchanged)
    - `xcodebuild build -scheme Islet` prints `BUILD SUCCEEDED`
  </acceptance_criteria>
  <done>project.yml ships version 0.1 with the product name Islet and bundle id unchanged; the app builds with the new version.</done>
</task>

<task type="auto">
  <name>Task 2: Run the release pipeline as the D-15 dry-run (DMG via hdiutil, SKIP banner, exit 0)</name>
  <read_first>
    - scripts/release.sh FULLY (it runs UNCHANGED — confirm the placeholder gate at the DEVELOPER_ID/NOTARY_PROFILE checks, the ad-hoc sign branch, the hdiutil UDZO step, the SKIP banner block, and the exit 0)
    - 06-CONTEXT.md D-15 (release runs as a DRY-RUN — placeholders stay, ad-hoc fallback exits 0 with the SKIP banner, hdiutil UDZO DMG, no create-dmg)
    - 06-RESEARCH.md "v1 Ship" + "Environment Availability" (all CLIs present; create-dmg absent; the dry-run is run-and-confirm, not a code change)
    - STATE.md decision: "[00-03] release.sh placeholder-gates Developer-ID/notary steps; ad-hoc fallback exits 0 with a loud SKIP banner — runs unchanged at Phase 6"
  </read_first>
  <action>
Do NOT modify `scripts/release.sh` (D-15: it runs UNCHANGED). Confirm the Developer-ID/notary placeholders
are still the sentinels (`__DEVELOPER_ID__` / `__NOTARY_PROFILE__`) so the dry-run path is taken — if they
were filled, this plan does NOT fill them (the real run is the carry-over).

Run the pipeline from the repo root: `./scripts/release.sh`. Expect it to archive the Release config,
ad-hoc sign, stage + create `dist/Islet.dmg` via hdiutil UDZO, print the "SKIPPING notarize + staple"
banner, and exit 0.

If the archive step fails for an environment reason (e.g. a signing-to-run-locally archive quirk on the
Tahoe toolchain), do NOT patch the script's logic — diagnose and, if it is a genuine environment gap,
record it in the SUMMARY as a dry-run finding (the script is intentionally unchanged). The expected
SUCCESS outcome is: `dist/Islet.dmg` exists + exit 0 + the SKIP banner printed.

This task does NOT commit build artifacts (`dist/` and `build/` are gitignored from Phase 0). The
deliverable is the produced DMG + the logged dry-run result, captured in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && ./scripts/release.sh > /tmp/islet_release.log 2>&1 ; CODE=$? ; grep -q "SKIPPING notarize" /tmp/islet_release.log && test -f dist/Islet.dmg && test "$CODE" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/release.sh` is unchanged: `git diff --quiet scripts/release.sh` succeeds (no modification)
    - `grep -q '__DEVELOPER_ID__' scripts/release.sh` succeeds (placeholders still in place — dry-run path)
    - Running `./scripts/release.sh` exits 0
    - `/tmp/islet_release.log` contains the line "SKIPPING notarize + staple"
    - `test -f dist/Islet.dmg` succeeds (DMG produced via hdiutil UDZO)
  </acceptance_criteria>
  <done>The unchanged release.sh runs as the D-15 dry-run: ad-hoc signs, builds dist/Islet.dmg via hdiutil UDZO, prints the SKIP banner, and exits 0. Real notarization remains the deferred carry-over.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device re-verify the Now Playing health check + "nicht verfuegbar" fallback (D-16)</name>
  <files>(no files modified — on-device re-verification of the Now Playing health check only)</files>
  <action>This is a human-verify checkpoint, not an autonomous task. Build + run the v1 app locally and perform the verification steps below. No source files change. Record the D-16 verdict (healthy yes/no, commands work yes/no, macOS build) in the SUMMARY.</action>
  <what-built>
    The v1 build at version 0.1 with the integrated resolver/settings/device work. D-16 requires
    confirming the MediaRemote launch-time health check still passes on the CURRENTLY installed macOS 26
    (each macOS update is a Now-Playing regression event — the Phase-4 standing blocker). This spawns a
    real perl child via mediaremote-adapter, so it cannot be unit-tested (RESEARCH Pitfall 6) — it is an
    on-device gate.
  </what-built>
  <how-to-verify>
    Build + launch the v1 app: `xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS'`,
    then run the built `Islet.app` (from DerivedData or via Xcode).
    1. With media playing (Apple Music or Spotify) at launch: open the island expanded — it shows the
       now-playing controls (NOT "Now Playing nicht verfuegbar"). This confirms `runHealthCheck` flipped
       `isHealthy=true` within the 3s window (a callback arrived) on this macOS build (D-16 healthy path).
    2. Exercise transport: play/pause, next, previous from the expanded island work (RESEARCH note:
       macOS 26.1 had a command-flakiness regression fixed by 26.2 — verify commands work on the installed
       build; if a command no longer works, log it as a macOS-regression finding).
    3. Confirm the fallback path is intact: the "Now Playing nicht verfuegbar" string still exists and
       renders if the bridge is unavailable (you do not need to force a block — confirm the code path /
       string is present and that nothing playing + healthy shows date/time, not the error).
    Record the verdict (healthy yes/no; commands work yes/no; macOS build) in the SUMMARY.
    DEFERRED — do NOT block on: the real Developer-ID notarize/staple + clean-second-Mac open (no account).
  </how-to-verify>
  <resume-signal>Type "approved" if the health check passes (now-playing shows, transport works) — log the macOS build + any command regression. Or describe the failure (if reading is blocked, note that Now-Playing degrades to the graceful "nicht verfuegbar" fallback — D-16 caught it).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release toolchain -> distributable | `codesign`/`hdiutil`/`notarytool`/`stapler` produce the shipped artifact; integrity of signing + the (deferred) notarization is the concern. |
| notary credentials -> release.sh | The script references a keychain profile NAME only; real credentials live in the macOS keychain, never in the repo. |
| MediaRemoteAdapter perl child -> app | (Inherited, unchanged) The D-16 health re-check spawns the perl child; isolated in NowPlayingMonitor; this plan only re-verifies it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Tampering | Distributable integrity (hardened runtime / signing) in the dry-run | accept | The dry-run ad-hoc signs ("Sign to Run Locally") with hardened runtime ON (project.yml `ENABLE_HARDENED_RUNTIME: YES`); it is for local testing only, NOT public distribution. Real Developer-ID signing + notarization (the actual integrity guarantee for users) is the deferred carry-over (D-15) — explicitly accepted as out of scope this phase. |
| T-06-15 | Information disclosure | Notary credentials leaking into the repo | mitigate | `release.sh` uses a keychain profile NAME only and is run UNCHANGED with the placeholders unfilled; no secret is read or committed in the dry-run. Acceptance criterion confirms placeholders remain. |
| T-06-16 | Denial of Service (resource leak) | The perl child spawned by the D-16 health check left running | mitigate | The health check uses the existing `NowPlayingMonitor` whose `deinit`/`stop()` terminates the child (T-04-12, unchanged); quitting the app via the menu tears it down. No new spawn path is added by this plan. |

No threat reaches **high** severity for the dry-run: ad-hoc local signing is intentional and the real integrity step is a tracked carry-over; no secrets are touched; the perl child is torn down. The security gate passes for this phase. (Note: the REAL notarization carry-over must satisfy the full signing/integrity controls when the Developer account exists.)
</threat_model>

<verification>
- `project.yml` ships `MARKETING_VERSION: "0.1"`, `PRODUCT_NAME: Islet`, bundle id unchanged; app builds.
- `./scripts/release.sh` (unchanged) exits 0, prints the SKIP banner, produces `dist/Islet.dmg` via hdiutil UDZO.
- On-device checkpoint: the Now Playing health check passes on the installed macOS 26 (now-playing shows, transport works) and the "nicht verfuegbar" fallback is intact (D-16).
- DEFERRED carry-overs (do NOT block): real Developer-ID notarize/staple + clean-second-Mac open (D-15); public name + 1.0 (D-13/D-14).
</verification>

<success_criteria>
- APP-04 (dry-run): the v1 app builds at version 0.1 (Islet), and the unchanged release pipeline produces a DMG and exits 0 with the SKIP banner — the notarizable pipeline is proven end-to-end short of the real credentials.
- D-16: the Now Playing launch health check + graceful fallback are confirmed on the current macOS.
- The deferred carry-overs are logged, not blocking — v1 closes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-priority-resolver-settings-v1-ship/06-05-SUMMARY.md`
</output>
