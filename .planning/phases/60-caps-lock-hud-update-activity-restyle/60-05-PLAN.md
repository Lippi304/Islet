---
phase: 60-caps-lock-hud-update-activity-restyle
plan: 05
type: execute
wave: 4
depends_on: ["60-02", "60-03", "60-04"]
files_modified: []
autonomous: false
requirements: [CAPS-01, UPDATE-01]

must_haves:
  truths:
    - "Full Cmd+U IsletTests suite is green (aside from the 2 pre-existing unrelated CalendarGlanceTests failures)"
    - "Both Debug and Release configurations build successfully"
    - "On-device: toggling real Caps Lock shows the correct on/off HUD content, auto-dismisses ~1-2s, and produces no HUD at all when Accessibility is not yet trusted"
    - "On-device: a simulated update-available event shows the Update HUD with correct icon/label/version pill, and tapping it reliably triggers the install flow with zero click-through across repeated taps"
    - "On-device: both activities are OFF by default on a fresh/reset toggle state, and each Settings toggle correctly gates its own HUD"
  artifacts: []
  key_links: []
---

<objective>
Close out Phase 60 with the automated build/test gate and the on-device checkpoint every prior
plan in this phase deferred to it: the 2 explicitly-flagged unresolved risks (RESEARCH.md Pitfall 3
— NSEvent Accessibility live-reconcile-vs-relaunch; Pitfall 4 — Update wing's non-universal tap
target vs. the Phase 40 click-through regression class) plus all 4 ROADMAP Phase 60 Success
Criteria.

Purpose: this is the goal-backward verification wave — confirms the pure seams (60-01), controller
wiring (60-02), Settings UI (60-03), and wing rendering (60-04) actually compose into working,
on-device-confirmed features, not just green unit tests.
Output: a build/test gate pass plus a completed on-device UAT checklist, closing CAPS-01/UPDATE-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/60-caps-lock-hud-update-activity-restyle/60-CONTEXT.md
@.planning/phases/60-caps-lock-hud-update-activity-restyle/60-RESEARCH.md
@.planning/phases/60-caps-lock-hud-update-activity-restyle/60-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build + full test suite gate</name>
  <files></files>
  <read_first>.planning/PROJECT.md</read_first>
  <action>
Run a Debug build (Xcode Cmd+B or `xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build`)
and confirm it succeeds with no new warnings introduced by this phase's edits. Run a Release build
the same way and confirm it also succeeds — additionally grep the Release build's product/symbol
table to confirm `debugSpikeSimulateUpdateAvailable` (Plan 60-02's DEBUG-only spike hook) is absent,
mirroring the Phase 49 precedent of an explicit Release-exclusion check. Run the full `IsletTests`
suite via Xcode Cmd+U (per PROJECT.md — headless `xcodebuild test` is documented to hang in this
project, do not attempt it) and confirm every test is green except the 2 pre-existing, unrelated
`CalendarGlanceTests` failures already documented in STATE.md (Phase 52) — specifically confirm all
6 new `IslandResolverTests` methods and both new `ActivitySettingsTests` methods from Plan 60-01
pass.
  </action>
  <acceptance_criteria>
    <test_command>Xcode Cmd+U → full IsletTests suite green except the 2 documented pre-existing CalendarGlanceTests failures</test_command>
    <cli_output>Release build's symbol table / build log contains zero occurrences of "debugSpikeSimulateUpdateAvailable"</cli_output>
    <test_command>Debug build succeeds with zero new compiler warnings attributable to this phase's files (CapsLockActivity.swift, UpdateActivity.swift, CapsLockMonitor.swift, IslandResolver.swift, NotchWindowController.swift, NotchPillView.swift, ActivitySettings.swift, SettingsView.swift, AppDelegate.swift)</test_command>
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build</automated>
  </verify>
  <done>Both Debug and Release builds succeed; full Cmd+U suite green aside from the 2 documented pre-existing failures; Release excludes the DEBUG spike symbol.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Consolidated on-device UAT — Caps Lock + Update HUD (all 4 ROADMAP Success Criteria)</name>
  <files></files>
  <read_first>.planning/ROADMAP.md "Phase 60: Caps Lock HUD + Update-Activity Restyle" Success Criteria (4 items)</read_first>
  <action>
No file edits in this task — build and run Islet on-device (Cmd-R in Xcode) and perform the 11-step
on-device checklist described in how-to-verify below. This task's only output is the recorded
approved/failed verdict covering all 4 ROADMAP Phase 60 Success Criteria plus RESEARCH.md's 2
explicitly flagged open questions (Pitfall 3 live-reconcile, Pitfall 4 tap-target reliability).
  </action>
  <what-built>
Two new collapsed-only transient HUDs (Caps Lock on/off, Update Available) wired end-to-end: real
global `.flagsChanged` monitor gated on Accessibility trust, Sparkle's existing update-check callback
extended with a second island-HUD signal alongside the unchanged menu-bar dot, both ranked below OSD
in the TransientQueue, both gated by their own Settings-grid toggle (default OFF), and a DEBUG-only
menu item (`🐞 → Spike: Simulate Update Available`) to test the Update HUD without a real newer
Sparkle release.
  </what-built>
  <how-to-verify>
1. **Fresh-defaults check (SETTINGS-05 / Pitfall 1):** In Settings, confirm both "Caps Lock" and
   "Update Available" cards show their toggle OFF by default (if either was already flipped ON from
   prior testing, toggle it OFF, quit Islet, delete its UserDefaults domain or just trust the
   existing OFF-by-default confirmation from Plan 60-01's ActivitySettingsTests, then relaunch and
   re-check).
2. **Caps Lock — Accessibility NOT yet granted:** If Islet is not currently Accessibility-trusted,
   toggle Caps Lock ON in Settings, then physically press the Caps Lock key. Confirm: NO HUD appears
   (silent no-op, not a crash) — this is the expected pre-permission state.
3. **Caps Lock — grant Accessibility:** Tap the Caps Lock card's options chevron, confirm the new
   "Caps Lock HUD" popover appears with the correct copy, tap "Open System Settings", grant
   Accessibility access to Islet in the System Settings pane that opens.
4. **Caps Lock — live-reconcile check (RESEARCH.md Pitfall 3, open question):** WITHOUT relaunching
   Islet, press Caps Lock again. Record whether the HUD now fires immediately (live reconcile works)
   or requires an app relaunch first. If a relaunch IS required, this is an acceptable documented
   limitation for this phase (not a blocker) — note it for the SUMMARY.
5. **Caps Lock — HUD content (D-03, SC1/SC2):** With Accessibility granted (relaunched if needed per
   step 4), press Caps Lock ON: confirm a HUD shows the `capslock.fill` icon (left) + "Caps Lock On"
   text (right), auto-dismissing after roughly 1-2 seconds with no click needed. Press Caps Lock OFF:
   confirm the same icon + "Caps Lock Off" text, same auto-dismiss timing. Repeat 3x rapid on/off
   toggles to confirm no stuck/duplicate HUD state.
6. **Update HUD — trigger + content (SC3):** Open the 🐞 debug status-bar menu → "Spike: Simulate
   Update Available". Confirm a HUD appears in the collapsed island showing the
   `arrow.triangle.2.circlepath` icon + "Update" label (left) and a version pill reading "v1.99"
   (right), auto-dismissing after ~3 seconds. Confirm the existing menu-bar red dot ALSO
   appears/stays visible (D-02 — unremoved, unaffected).
7. **Update HUD — tap-target reliability (RESEARCH.md Pitfall 4, HIGHEST RISK item):** Trigger the
   spike again; before the ~3s auto-dismiss, tap the Update HUD at several different points across
   its rendered width (left icon area, middle, right version-pill area). Confirm EVERY tap reliably
   triggers Sparkle's real update-check/install dialog (`checkForUpdates()`), with zero instances of
   the tap passing through to whatever is behind Islet. Repeat this 5+ times across separate trigger
   cycles to build confidence this isn't a coincidental first-tap success.
8. **Collapsed-only gating (D-07):** Expand the island (click to Home/Tray/any tab), then trigger
   both the Caps Lock press and the Update spike while expanded. Confirm NEITHER HUD interrupts the
   expanded view — both should be inert until the island is collapsed again.
9. **Priority (D-05, rank 5/6):** While a Charging or Focus HUD is actively showing, trigger a Caps
   Lock press or the Update spike. Confirm the standing Charging/Focus/Device/OSD splash is NOT
   interrupted — the new activity should queue behind it (or simply not preempt), consistent with
   ranking below OSD.
10. **Settings toggle gating (SC4):** Toggle "Caps Lock" OFF in Settings, then press the physical
    Caps Lock key — confirm no HUD. Toggle "Update Available" OFF, then re-trigger the debug spike —
    confirm no HUD (the menu-bar dot may still appear/stay, per D-02, since that path is independent
    of this toggle).
11. **Regression check:** Confirm Charging, Device, Focus, and Volume/Brightness (OSD) HUDs still
    render and tap-to-expand correctly, unaffected by `wingsShape`'s new optional `onTap` parameter.
  </how-to-verify>
  <resume-signal>Type "approved" once all 11 steps pass, or describe which step failed and what was observed (especially step 4's live-reconcile-vs-relaunch finding and step 7's tap-reliability result — both are open research questions this checkpoint is meant to resolve empirically).</resume-signal>
  <done>All 11 on-device checklist steps confirmed; RESEARCH.md's Pitfall 3/4 open questions resolved with a recorded empirical answer; all 4 ROADMAP Phase 60 Success Criteria (CAPS-01, UPDATE-01) closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| On-device verification vs. automated test coverage | Accessibility TCC state and click-through/hot-zone geometry cannot be asserted via XCTest — this plan's checkpoint is the sole verification path for both, per this codebase's own established precedent (OSDInterceptor, Phase 40/42/48 tap-target regressions) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-13 | Repudiation (of a resolved-vs-unresolved research question) | Pitfall 3 (live-reconcile) | mitigate | Checkpoint step 4 explicitly records the empirical answer rather than leaving it assumed; if relaunch is required, documented as an accepted limitation, not silently ignored |
| T-60-14 | Spoofing/Tampering | Update HUD tap target | mitigate | Checkpoint step 7 requires 5+ repeated-tap trials across the wing's full rendered width before UPDATE-01 is considered done — carries forward RESEARCH.md Pitfall 4's explicit verification gate |
| T-60-15 | Tampering (npm/pip/cargo) | N/A | accept | Zero new external packages this phase — Package Legitimacy Audit gate not applicable |
</threat_model>

<verification>
Task 1's automated build/test gate must be green before Task 2's on-device checkpoint is attempted.
Task 2 is a `gate="blocking"` checkpoint — do not auto-advance past it.
</verification>

<success_criteria>
- Debug + Release builds green; Release excludes the DEBUG spike symbol; full Cmd+U suite green
  (aside from the 2 documented pre-existing failures).
- All 11 on-device checklist steps confirmed, closing all 4 ROADMAP Phase 60 Success Criteria
  (CAPS-01, UPDATE-01) and resolving RESEARCH.md's 2 explicitly flagged open questions
  (Pitfall 3 live-reconcile, Pitfall 4 tap-target reliability) with a recorded empirical answer.
</success_criteria>

<output>
Create `.planning/phases/60-caps-lock-hud-update-activity-restyle/60-05-SUMMARY.md` when done
</output>
