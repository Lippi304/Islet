---
phase: 64-quick-notes-obsidian-export
plan: 02
type: tdd
wave: 2
depends_on: ["64-01"]
files_modified:
  - Islet/QuickNotes/QuickNotesVaultWriter.swift
  - IsletTests/QuickNotesVaultWriterTests.swift
autonomous: true
requirements: [NOTES-02]

must_haves:
  truths:
    - "Appending a note to a non-existent vault file creates it with today's day heading, no leading blank line"
    - "Appending a second note the same day does not repeat the day heading"
    - "Appending a note on a new day inserts a blank line and a new day heading, leaving every prior byte in the file untouched"
    - "Appending to a file that does not end in a newline (a hand-edited vault file) inserts a newline before new content, never concatenating onto the last existing line"
    - "The write is a tail-read (bounded window) plus FileHandle.seekToEndOfFile()+write(_:) — never a full-file read-modify-write"
  artifacts:
    - path: "Islet/QuickNotes/QuickNotesVaultWriter.swift"
      provides: "append(note:to:at:) throws, isValidVaultFolder(atPath:) -> Bool"
  key_links:
    - from: "QuickNotesVaultWriter.append"
      to: "QuickNotesFormatter.formatEntry / isoDate"
      via: "direct static call to build the day-heading + bullet payload"
      pattern: "QuickNotesFormatter\\.(formatEntry|isoDate)"
    - from: "QuickNotesVaultWriter.append"
      to: "target .md file"
      via: "FileHandle.seekToEndOfFile() + write(_:), never a whole-file rewrite"
      pattern: "seekToEndOfFile"
---

<objective>
Build `QuickNotesVaultWriter` — the ONE genuinely new, data-loss-critical subsystem in this phase: a bounded tail-read of the target Obsidian vault `.md` file to decide whether today's `## YYYY-MM-DD` heading already exists (D-06), followed by an append-only `FileHandle` write (D-07) that never reads or rewrites the whole file. Also owns `isValidVaultFolder(atPath:)`, the pure D-11 path-validity check later plans call before showing the capture popover and in Settings' current-path display.

Purpose: this is the file real user data lives in — a bug here corrupts or destroys the user's own Obsidian notes, not just Islet's own state (Pitfall 7/11). It gets its own dedicated TDD plan, isolated from every other concern, so RED→GREEN cycles get full attention.
Output: `QuickNotesVaultWriter.swift`, fully covered by `QuickNotesVaultWriterTests.swift` against real temp-directory fixture files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/64-quick-notes-obsidian-export/64-CONTEXT.md
@.planning/phases/64-quick-notes-obsidian-export/64-RESEARCH.md

@Islet/QuickNotes/QuickNotesFormatter.swift

<interfaces>
<!-- From Plan 64-01 (already implemented, do not redefine) -->
enum QuickNotesFormatter {
    static func isoDate(_ date: Date) -> String
    static func formatEntry(text: String, at date: Date) -> String
}

<!-- Reference implementation this task builds from — 64-RESEARCH.md lines 244-301 (Pattern 3),
     composed from Foundation's FileHandle docs + this phase's own locked D-05/D-06/D-07 spec.
     Treat the byte-window size (4096) and UTF-8-boundary handling as the target design, verify
     via the tests below rather than assuming correctness. -->
enum QuickNotesVaultWriter {
    struct WriteError: Error { let underlying: Error }

    static func append(note text: String, to fileURL: URL, at date: Date) throws {
        let fm = FileManager.default
        if !fm.fileExists(atPath: fileURL.path) {
            fm.createFile(atPath: fileURL.path, contents: nil)
        }
        let handle = try FileHandle(forWritingTo: fileURL)
        defer { try? handle.close() }

        let tailInfo = try readTail(of: fileURL, windowBytes: 4096)
        let today = QuickNotesFormatter.isoDate(date)
        var payload = ""

        if tailInfo.sizeBytes > 0 && !tailInfo.endsWithNewline {
            payload += "\n"
        }
        if tailInfo.lastHeadingDate != today {
            if tailInfo.sizeBytes > 0 { payload += "\n" }
            payload += "## \(today)\n\n"
        }
        payload += QuickNotesFormatter.formatEntry(text: text, at: date)

        handle.seekToEndOfFile()
        handle.write(Data(payload.utf8))
    }

    private static func readTail(of fileURL: URL, windowBytes: Int) throws
        -> (sizeBytes: UInt64, endsWithNewline: Bool, lastHeadingDate: String?) {
        let handle = try FileHandle(forReadingFrom: fileURL)
        defer { try? handle.close() }
        let size = try handle.seekToEnd()
        guard size > 0 else { return (0, true, nil) }

        let offset = size > UInt64(windowBytes) ? size - UInt64(windowBytes) : 0
        try handle.seek(toOffset: offset)
        let tailData = handle.readDataToEndOfFile()
        let tailString = String(decoding: tailData, as: UTF8.self)

        let endsWithNewline = tailData.last == 0x0A
        let lines = tailString.split(separator: "\n", omittingEmptySubsequences: false)
        let lastHeadingLine = lines.last { $0.hasPrefix("## ") }
        let lastHeadingDate = lastHeadingLine.map { String($0.dropFirst(3)) }

        return (size, endsWithNewline, lastHeadingDate)
    }
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write failing QuickNotesVaultWriterTests against real fixture files</name>
  <files>IsletTests/QuickNotesVaultWriterTests.swift</files>
  <read_first>.planning/phases/64-quick-notes-obsidian-export/64-CONTEXT.md (D-05 exact code block, D-06, D-07), .planning/phases/64-quick-notes-obsidian-export/64-RESEARCH.md (Pattern 3, Pitfall 7, Pitfall 11, Assumption A3), IsletTests/ClipboardFileStoreTests.swift (fixturesDir setUp/tearDown pattern to mirror)</read_first>
  <behavior>
    - Test: `append` to a path that does not yet exist creates the file with content exactly `"## <today>\n\n- HH:mm <text>\n"` — NO leading blank line before the very first heading in a brand-new file.
    - Test: two sequential `append` calls on the same day produce exactly ONE `"## <today>"` line and two `"- HH:mm ..."` bullet lines in the resulting file.
    - Test: pre-seed a fixture file with a prior day's heading + one entry (e.g. `"## 2026-07-24\n\n- 09:00 Yesterday.\n"`), then `append` with today's date — assert the file's ORIGINAL bytes (the full `"## 2026-07-24..."` block) are present unchanged as a prefix/substring of the new file, AND a blank line + new `"## <today>"` heading + today's bullet were appended after it.
    - Test: pre-seed a fixture file whose content does NOT end in `"\n"` (simulating a hand-edited vault file per D-07), then `append` — assert a newline was inserted before any new content (no two lines concatenated together).
    - Test: `append` with a multi-line note (embedded `"\n"`) writes the continuation line indented by exactly two spaces (reuses `QuickNotesFormatter`, but assert it end-to-end through the actual file write).
    - Test: `isValidVaultFolder(atPath:)` returns `true` for an existing directory path, `false` for a path that does not exist, and `false` for a path that exists but is a regular file (not a directory).
    - Use the `fixturesDir` setUp/tearDown pattern from `ClipboardFileStoreTests.swift` (fresh `NSTemporaryDirectory()`-rooted UUID directory per test, removed in tearDown); build fixture `Date`s via `Calendar.current.date(from: DateComponents(...))` for deterministic day/time values.
  </behavior>
  <action>
    Write `IsletTests/QuickNotesVaultWriterTests.swift` with the above test methods against `QuickNotesVaultWriter.append(note:to:at:)` and `QuickNotesVaultWriter.isValidVaultFolder(atPath:)` — these two functions do not exist yet, so the file will fail to compile/all tests fail. This is the expected RED state.
  </action>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet build 2>&1 | grep -i "error" | grep -i "QuickNotesVaultWriter"</automated>
  </verify>
  <acceptance_criteria>
    - Build fails specifically on unresolved `QuickNotesVaultWriter` references (confirms tests were written before the implementation, true RED)
    - `IsletTests/QuickNotesVaultWriterTests.swift` contains at least 7 distinct `func test...()` methods per the behaviors above
  </acceptance_criteria>
  <done>QuickNotesVaultWriterTests.swift exists with all 7 behaviors as failing/non-compiling tests, committed as `test(64-02): add failing tests for QuickNotesVaultWriter`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement QuickNotesVaultWriter to pass all tests</name>
  <files>Islet/QuickNotes/QuickNotesVaultWriter.swift</files>
  <read_first>IsletTests/QuickNotesVaultWriterTests.swift (the tests just written), .planning/phases/64-quick-notes-obsidian-export/64-RESEARCH.md (Pattern 3, lines 244-301)</read_first>
  <action>
    Create `Islet/QuickNotes/QuickNotesVaultWriter.swift` implementing `enum QuickNotesVaultWriter` per the reference implementation in this plan's `<interfaces>` block: `append(note text: String, to fileURL: URL, at date: Date) throws` (creates the file if missing via `FileManager.default.createFile(atPath:contents:)`, opens `FileHandle(forWritingTo:)`, tail-reads via a private `readTail(of:windowBytes:)` with a 4096-byte window, builds the payload calling `QuickNotesFormatter.isoDate(_:)` and `QuickNotesFormatter.formatEntry(text:at:)`, then `handle.seekToEndOfFile()` + `handle.write(Data(payload.utf8))` — never a full-file read or rewrite). Add `static func isValidVaultFolder(atPath path: String) -> Bool` as a pure `FileManager.default.fileExists(atPath:isDirectory:)` check returning `true` only when the path exists AND `isDirectory` is true — this function takes a path directly (no `UserDefaults` reads inside this file; callers own reading the stored path).
    Do not read or hold the whole file in memory at any point — `readTail` must only ever load the last `windowBytes` bytes via `handle.seek(toOffset:)` + `readDataToEndOfFile()`, never `Data(contentsOf:)` on the full file.
  </action>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet build</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild -project Islet.xcodeproj -scheme Islet build` exits 0
    - Manual Cmd-U: all `QuickNotesVaultWriterTests` methods pass
    - `grep -c "Data(contentsOf: fileURL)" Islet/QuickNotes/QuickNotesVaultWriter.swift` returns 0 (confirms no full-file read path exists)
    - `grep -c "seekToEndOfFile" Islet/QuickNotes/QuickNotesVaultWriter.swift` returns 1 (the one and only write path)
  </acceptance_criteria>
  <done>QuickNotesVaultWriter.swift exists, compiles, and every test from Task 1 passes — committed as `feat(64-02): implement QuickNotesVaultWriter append + tail-read`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Islet app → user-chosen Obsidian vault `.md` file (outside app sandbox/ownership) | External user storage; Islet only ever appends, the user owns and may hand-edit this file at any time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01 | Tampering | `QuickNotesVaultWriter.append` | mitigate | Append-only `FileHandle.seekToEndOfFile()` + `write(_:)`, never read-modify-write-whole-file (D-07); bounded tail-read (4096 bytes) only ever informs the day-heading decision, never the write path itself (Pitfall 7) |
| T-64-06 | Tampering | `readTail` on a hand-edited file with no trailing newline | mitigate | Explicit `endsWithNewline` check inserts a separating newline before any new content, never concatenates onto an existing line (D-07, Task 1 test 4) |
| T-64-11 | Tampering | `FileHandle.seekToEndOfFile()`+`write(_:)` is two syscalls, not POSIX `O_APPEND`'s single atomic operation | accept | Documented, accepted residual risk per `research/PITFALLS.md`'s own Technical Debt table — Islet is single-process, single-writer for this file; re-examine only if a second writer to the same file is ever proposed (RESEARCH.md Pitfall 11) |

</threat_model>

<verification>
Build must be green after Task 2. Manual Cmd-U confirms all `QuickNotesVaultWriterTests` pass, including the byte-preservation assertion for prior vault content (the single highest-stakes test in the phase per Pitfall 7/11).
</verification>

<success_criteria>
QuickNotesVaultWriter.append never corrupts or loses existing vault-file content across all 7 tested scenarios (new file, same-day repeat, new-day transition, missing-trailing-newline, multi-line indentation, plus the 3 isValidVaultFolder cases), and the write path is provably tail-read + append-only, never a full-file rewrite.
</success_criteria>

<output>
Create `.planning/phases/64-quick-notes-obsidian-export/64-02-SUMMARY.md` when done
</output>
