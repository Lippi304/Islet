---
phase: 64-quick-notes-obsidian-export
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/ActivitySettings.swift
  - Islet/SettingsView.swift
autonomous: true
requirements: [NOTES-02]

must_haves:
  truths:
    - "The user can pick an Obsidian vault folder via a native folder-only NSOpenPanel from the Quick Notes Settings card (D-08)"
    - "The chosen folder path persists as a plain UserDefaults string, no security-scoped bookmark (D-09 — Islet is not sandboxed)"
    - "The Quick Notes Settings card's options button opens a popover showing the currently chosen path (or 'not set'), mirroring the Focus/OSD/Caps-Lock onOptionsTap popover pattern exactly (D-10)"
  artifacts:
    - path: "Islet/ActivitySettings.swift"
      provides: "quickNotesVaultFolderPathKey — the shared UserDefaults key string"
    - path: "Islet/SettingsView.swift"
      provides: "quickNotesVaultPickerView + .popover(isPresented: $showQuickNotesVaultPicker) attached to the Produktivität categorySection"
  key_links:
    - from: "quickNotes ActivityCardData.onOptionsTap"
      to: "showQuickNotesVaultPicker = true"
      via: "closure, same as focus/osd/capsLock cards"
      pattern: "onOptionsTap: \\{ showQuickNotesVaultPicker = true \\}"
    - from: "quickNotesVaultPickerView's Choose Folder button"
      to: "ActivitySettings.quickNotesVaultFolderPathKey"
      via: "NSOpenPanel(directory mode) -> @AppStorage write"
      pattern: "canChooseDirectories = true"
---

<objective>
Wire the already-registered "Quick Notes" Settings card's `onOptionsTap` (currently `nil`, Phase 59) to a vault-folder picker popover: a native `NSOpenPanel` in directory-selection mode plus a current-path display, following the exact 3-part wiring (`@State` Bool → `onOptionsTap` closure → `.popover(isPresented:)`) already used by the Focus/OSD/Caps-Lock cards in this same file.

Purpose: gives the user a way to choose where their vault `.md` file lives (D-08/D-10) before the capture popover (Plan 64-04) can check path validity or the vault writer (Plan 64-02) can append anywhere.
Output: one new `ActivitySettings` key, one new Settings popover, zero new UI paradigms.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/64-quick-notes-obsidian-export/64-CONTEXT.md
@.planning/phases/64-quick-notes-obsidian-export/64-UI-SPEC.md
@.planning/phases/64-quick-notes-obsidian-export/64-PATTERNS.md

@Islet/ActivitySettings.swift
@Islet/SettingsView.swift

<interfaces>
<!-- ActivitySettings.swift line 40 — already exists, do not touch the toggle key itself -->
static let quickNotesKey = "activity.quickNotes"   // already in defaultsToFalseKeys (line 55)

<!-- SettingsView.swift lines 242-245 — the card is already registered in productivityCards, only
     onOptionsTap needs to change from nil -->
ActivityCardData(id: "quickNotes", title: "Quick Notes",
                  description: "Capture a quick text note straight into your Obsidian vault.",
                  icon: "note.text", iconColor: .secondary,
                  isOn: $quickNotesEnabled, isNew: true, onOptionsTap: nil),

<!-- SettingsView.swift lines 400-416 — the exact popover-attachment pattern to mirror, one more
     .popover(isPresented:) chained onto the SAME categorySection call for "Produktivität"
     (line 418), not a new generic per-card router -->
categorySection(title: "System-HUDs", cards: systemHUDCards)
    .popover(isPresented: $showFocusPermissionExplanation) { focusPermissionExplanationView }
    .popover(isPresented: $showOSDPermissionExplanation) { osdPermissionExplanationView }
    .popover(isPresented: $showCapsLockPermissionExplanation) { capsLockPermissionExplanationView }
categorySection(title: "Medien", cards: mediaCards)
categorySection(title: "Produktivität", cards: productivityCards)

<!-- SettingsView.swift lines 661-688 — the exact popover-body shape to mirror (spacing 8, title
     15pt semibold, body 12pt, HStack of a dismiss + primary action button, padding 16, fixed
     width 280) -->
private var focusPermissionExplanationView: some View {
    VStack(alignment: .leading, spacing: 8) {
        Text("Allow Focus Status Access").font(.system(size: 15, weight: .semibold))
        Text("Islet needs permission to detect when Focus or Do Not Disturb is on.")
            .font(.system(size: 12)).lineSpacing(12 * 0.4)
        HStack {
            Button("Not Now") { showFocusPermissionExplanation = false }
            Spacer()
            Button("Continue") { /* ... */ }.keyboardShortcut(.defaultAction)
        }
    }
    .padding(16)
    .frame(width: 280)
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ActivitySettings key + SettingsView state/wiring skeleton</name>
  <files>Islet/ActivitySettings.swift, Islet/SettingsView.swift</files>
  <read_first>Islet/ActivitySettings.swift, Islet/SettingsView.swift (lines 40-72 state declarations, 185-250 card declarations, 395-420 popover attachments)</read_first>
  <action>
    In `Islet/ActivitySettings.swift`, add `static let quickNotesVaultFolderPathKey = "quickNotes.vaultFolderPath"` immediately after the existing `quickNotesKey` (line 40) — a plain `String` UserDefaults key, NOT a Bool, so it must NOT be added to `defaultsToFalseKeys` (line 53-56).
    In `Islet/SettingsView.swift`: add `@State private var showQuickNotesVaultPicker = false` alongside the existing `showFocusPermissionExplanation`/`showOSDPermissionExplanation`/`showCapsLockPermissionExplanation` declarations (near lines 43/52/56); add `@AppStorage(ActivitySettings.quickNotesVaultFolderPathKey) private var quickNotesVaultFolderPath: String = ""` alongside line 71's `quickNotesEnabled` declaration (empty string means "not yet chosen"); change the `quickNotes` card's `onOptionsTap: nil` (line 245) to `onOptionsTap: { showQuickNotesVaultPicker = true }`; chain one more `.popover(isPresented: $showQuickNotesVaultPicker) { quickNotesVaultPickerView }` onto the existing `categorySection(title: "Produktivität", cards: productivityCards)` call (line 418) — do NOT attach it to the `System-HUDs` section's chain (lines 408-416), it belongs on its own section since Quick Notes lives in Produktivität.
  </action>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet build 2>&1 | grep -c "error: cannot find 'quickNotesVaultPickerView'"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "quickNotesVaultFolderPathKey" Islet/ActivitySettings.swift` returns 1
    - `grep -c "quickNotesVaultFolderPathKey" Islet/ActivitySettings.swift` string is absent from `defaultsToFalseKeys` array (`grep -A3 "defaultsToFalseKeys" Islet/ActivitySettings.swift | grep -c quickNotesVaultFolderPathKey` returns 0)
    - `grep -c "onOptionsTap: { showQuickNotesVaultPicker = true }" Islet/SettingsView.swift` returns 1
    - `grep -c "showQuickNotesVaultPicker" Islet/SettingsView.swift` returns at least 3 (declaration, onOptionsTap, popover attachment)
    - Build fails ONLY on the not-yet-created `quickNotesVaultPickerView` reference (expected at this point — Task 2 creates it)
  </acceptance_criteria>
  <done>The key exists in ActivitySettings, the card's onOptionsTap and the section's .popover attachment are wired; the only remaining compile error is the missing quickNotesVaultPickerView body (Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: quickNotesVaultPickerView (folder picker + current-path display)</name>
  <files>Islet/SettingsView.swift</files>
  <read_first>Islet/SettingsView.swift (lines 661-688, focusPermissionExplanationView, the shape to mirror), .planning/phases/64-quick-notes-obsidian-export/64-RESEARCH.md (Code Example 2, NSOpenPanel directory-mode config)</read_first>
  <action>
    Add a new `private var quickNotesVaultPickerView: some View` to `Islet/SettingsView.swift` (near the other 3 explanation views, after line 688), following `focusPermissionExplanationView`'s exact `VStack(alignment: .leading, spacing: 8)` / `.padding(16)` / `.frame(width: 280)` shape but with folder-picker content instead of a permission request: a title `Text("Obsidian Vault Folder")` (15pt semibold), a body `Text` showing `quickNotesVaultFolderPath.isEmpty ? "No folder selected." : quickNotesVaultFolderPath` (12pt, `.secondary` when empty), a `Button("Choose Folder…")` whose action constructs an `NSOpenPanel` with `canChooseDirectories = true`, `canChooseFiles = false`, `allowsMultipleSelection = false`, `prompt = "Choose"`, `message = "Choose your Obsidian vault folder"`, and on `panel.runModal() == .OK` sets `quickNotesVaultFolderPath = url.path` (the `@AppStorage` binding — do NOT use `UserDefaults.standard.set` directly since the property wrapper already owns that write), and a `Button("Done") { showQuickNotesVaultPicker = false }` with `.keyboardShortcut(.defaultAction)`, laid out in an `HStack` mirroring the `Not Now`/`Continue` row shape.
  </action>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet build</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild -project Islet.xcodeproj -scheme Islet build` exits 0
    - `grep -c "canChooseDirectories = true" Islet/SettingsView.swift` returns 1
    - `grep -c "canChooseFiles = false" Islet/SettingsView.swift` returns 1
    - Manual on-device check (per 64-UI-SPEC.md): open Settings, click Quick Notes card's options button, popover shows "No folder selected.", clicking "Choose Folder…" opens a native folder picker, selecting a folder updates the displayed path
  </acceptance_criteria>
  <done>quickNotesVaultPickerView compiles, displays the current path or "No folder selected.", and Choose Folder... persists a real chosen path via the shared @AppStorage key.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → `NSOpenPanel` → `UserDefaults` | The stored path only ever comes from the panel's own return value, never user-typed text — no path-injection/traversal surface (RESEARCH.md V5 assessment) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-07 | Tampering | Stored vault path never validated at Settings-display time | accept | The Settings card only displays and lets the user CHANGE the path; the D-11 revalidation-on-popover-open guard lives in Plan 64-04's AppDelegate wiring (the actual write path), not here — displaying a stale path in Settings with no live re-check is low severity (informational only) |

</threat_model>

<verification>
Build green after each task. On-device: Settings → Quick Notes card → options button opens the picker popover, folder selection persists and displays correctly across a Settings-window close/reopen.
</verification>

<success_criteria>
User can choose an Obsidian vault folder from Settings via a native directory-mode NSOpenPanel; the path persists as a plain UserDefaults string under the shared ActivitySettings key and displays correctly in the popover.
</success_criteria>

<output>
Create `.planning/phases/64-quick-notes-obsidian-export/64-03-SUMMARY.md` when done
</output>
