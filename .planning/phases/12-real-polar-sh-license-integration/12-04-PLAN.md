---
phase: 12-real-polar-sh-license-integration
plan: 04
type: execute
wave: 3
depends_on: [12-03]
files_modified: []
autonomous: false
requirements: [LIC-01, LIC-02]
must_haves:
  truths:
    - "Buy Now opens the real Polar.sh €7.99 checkout in the default browser (LIC-01)"
    - "Pasting the real purchased key validates online and shows the success state (LIC-02)"
    - "A garbage key shows \"not recognized\"; an offline/blocked attempt shows \"server not reachable\" + Retry"
    - "After one online validation, quitting + airplane mode + relaunch keeps the island fully working with no re-prompt (LIC-02 / success criterion 3)"
    - "Both the Debug and Release builds succeed (Release-only library-validation crash history)"
  artifacts: []
  key_links:
    - from: "on-device app"
      to: "api.polar.sh + Keychain"
      via: "real key paste → validate → persist → offline relaunch"
      pattern: "manual on-device verification"
---

<objective>
Prove Phase 12 against reality: run the Debug AND Release build gates, then a human on-device
pass that exercises every LIC-01/LIC-02 success criterion — Buy Now → real checkout, real-key
online validation, the invalid-key and unreachable error states, and the decisive
offline-after-first-validation relaunch. The real license key is supplied LIVE by the user at
this step (never recorded in planning docs). Unit tests authored in waves 1-2 are also run here
via Cmd-U (headless `xcodebuild test` hangs in this project — build is the CI gate, Cmd-U is the
manual run).

Purpose: this is the phase acceptance gate before `/gsd:verify-work 12`. It confirms the drop-in
actually validates a real key and stays entitled offline, and that no Release-only regression
(library-validation crash) slipped in.
Output: verification observations recorded in the SUMMARY; no code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-real-polar-sh-license-integration/12-CONTEXT.md
@.planning/phases/12-real-polar-sh-license-integration/12-RESEARCH.md
@.planning/phases/12-real-polar-sh-license-integration/12-VALIDATION.md
@.planning/phases/12-real-polar-sh-license-integration/12-01-SUMMARY.md
@.planning/phases/12-real-polar-sh-license-integration/12-02-SUMMARY.md
@.planning/phases/12-real-polar-sh-license-integration/12-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Debug + Release build gate</name>
  <files></files>
  <read_first>
    - .planning/phases/12-real-polar-sh-license-integration/12-RESEARCH.md (Sampling Rate, lines 515-521 — the Release build gate rationale; Environment Availability 465-475 — dev Keychain-prompt caveat)
    - .planning/STATE.md (lines 109-116 — the resolved Release library-validation crash / disable-library-validation entitlement history)
  </read_first>
  <action>
    Regenerate the project and run BOTH build configurations. Release is a mandatory extra gate because this project has a documented Release-only crash mode (embedded MediaRemoteAdapter library validation under Hardened Runtime — resolved via the `disable-library-validation` entitlement, but re-verify every phase that touches launch-time state). Run `xcodegen generate`, then `xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug`, then `xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Release`. Both must report BUILD SUCCEEDED. Do NOT run `xcodebuild test` (it hangs — the unit-test run is the human's Cmd-U in Task 2).
  </action>
  <verify>
    <automated>xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -2 | grep -q 'BUILD SUCCEEDED' && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Release 2>&1 | tail -2 | grep -q 'BUILD SUCCEEDED'</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild build ... -configuration Debug` exits 0 (BUILD SUCCEEDED).
    - `xcodebuild build ... -configuration Release` exits 0 (BUILD SUCCEEDED).
  </acceptance_criteria>
  <done>Both Debug and Release builds pass with all Phase 12 files compiled in.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The real Polar.sh license integration: `PolarLicenseService` (online validate), a
    Keychain-cached `LicenseManager` (offline-after-first-validation), the persisted branch in
    `LicenseState`, and the SettingsView swap with a distinct reachable-vs-invalid error split +
    Retry. All automated build gates (Debug + Release) are green.
  </what-built>
  <how-to-verify>
    Open the built app in Xcode (Product → Run, or launch the Release build). Then, in Xcode, run
    the unit tests with Cmd-U and confirm the Licensing suites are green (LicenseManagerTests,
    PolarLicenseServiceTests, plus the existing LicenseServiceTests / TrialManagerTests).

    On-device manual pass (open the menu-bar icon → Settings → License section):
    1. LIC-01 — Click "Buy Islet — €7.99". Confirm the DEFAULT BROWSER opens and lands on the
       real Polar.sh €7.99 checkout page (via the lippi304.xyz redirect).
    2. LIC-02 invalid — Paste an obviously wrong key (e.g. "NOPE-1234"), click Activate. Confirm
       it shows "✗ That key wasn't recognized." (the .invalidKey state), NOT the unreachable state.
    3. LIC-02 unreachable — Turn on Airplane Mode (or block network), paste any key, click
       Activate. Confirm it shows "⚠ Server not reachable." WITH a Retry button — NOT "not
       recognized". Turn networking back on and click Retry to confirm it re-attempts.
    4. LIC-02 success — With networking ON, paste the REAL purchased key (you supply it live),
       click Activate. Confirm "✓ License activated" and that the island unlocks live (if the
       trial were expired) without needing a relaunch.
    5. LIC-02 / SC3 offline-after-first-validation — Quit Islet fully. Enable Airplane Mode.
       Relaunch Islet. Confirm the island is fully working (visible, expands, activities) and
       Settings shows "Licensed ✓" — with NO key re-prompt and NO network needed. This is the
       decisive "validate once, then trust forever" (D-01) proof.
    6. Paid-key-never-locked — Confirm that at no point did a network error (step 3) flip an
       already-validated key into a locked/invalid state.
  </how-to-verify>
  <resume-signal>Type "approved" if all six behaviors pass, or describe any that failed (which step, what you saw).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| On-device app → api.polar.sh | Real network round-trip with the real purchased key |
| App → Keychain | Real SecItem* persistence verified on-device (fakes only in unit tests) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Denial of Service (to the user) | offline relaunch + airplane-mode activate | mitigate | Manually verified: airplane-mode activate shows unreachable+Retry (not invalid); a validated key stays entitled offline across relaunch (steps 3, 5, 6). |
| T-12-07 | Denial of Service | Release-only library-validation crash | mitigate | Explicit Release build gate (Task 1) re-verifies the launch path after new launch-time state (memory: Release-only crashes don't appear in Debug). |
| T-12-SC | Tampering | package installs | accept | N/A — no packages; verification-only plan. |
</threat_model>

<verification>
- Task 1: Debug + Release builds both BUILD SUCCEEDED.
- Task 2 (human): all six LIC-01/LIC-02 on-device behaviors pass; Cmd-U Licensing suites green.
</verification>

<success_criteria>
- LIC-01: Buy Now opens the real Polar checkout.
- LIC-02: real key validates online → success; invalid key → "not recognized"; offline → "server not reachable" + Retry.
- LIC-02 / SC3: offline relaunch after one validation stays fully entitled with no re-prompt.
- Both Debug and Release builds pass; unit suites green via Cmd-U.
</success_criteria>

<output>
Create `.planning/phases/12-real-polar-sh-license-integration/12-04-SUMMARY.md` when done
</output>
