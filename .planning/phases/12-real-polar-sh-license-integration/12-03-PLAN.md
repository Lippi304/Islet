---
phase: 12-real-polar-sh-license-integration
plan: 03
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - Islet/Licensing/LicenseState.swift
  - Islet/SettingsView.swift
autonomous: true
requirements: [LIC-02]
must_haves:
  truths:
    - "At launch, a present granted Keychain record short-circuits LicenseState.status to .licensed with no network call"
    - "Pasting a valid key and clicking Activate uses the real PolarLicenseService and persists a granted LicenseRecord on success"
    - "A .unreachable failure shows a distinct \"server not reachable\" message with a manual Retry button, separate from the \".invalidKey\" \"not recognized\" message"
    - "A successful activation still live-unlocks the island via the existing updateVisibility() nudge path"
  artifacts:
    - path: "Islet/Licensing/LicenseState.swift"
      provides: "persisted-license branch in status (LicenseManager.shared.isLicensed → .licensed)"
      contains: "LicenseManager.shared.isLicensed"
    - path: "Islet/SettingsView.swift"
      provides: "PolarLicenseService call-site swap + record persistence on success + D-04 .unreachable/.invalidKey split + Retry button"
      contains: "PolarLicenseService()"
  key_links:
    - from: "LicenseState.status"
      to: "LicenseManager.shared.isLicensed"
      via: "cached persisted-record read, after DEBUG override and before sessionActivated"
      pattern: "LicenseManager\\.shared\\.isLicensed"
    - from: "SettingsView.activate .success"
      to: "LicenseManager.shared.recordValidation"
      via: "persist granted record for offline-after-first-validation"
      pattern: "recordValidation"
    - from: "SettingsView.activate .failure(.unreachable)"
      to: "ActivationPhase.unreachable + Retry button"
      via: "D-04 error split"
      pattern: "case .unreachable"
---

<objective>
Wire the wave-1 building blocks into the app's live surfaces — the LAST integration step:
1. `LicenseState.status` reads the persisted granted record (via `LicenseManager.shared.isLicensed`)
   so a validated key survives relaunch OFFLINE with zero network call (LIC-02 / success criterion 3).
2. `SettingsView` swaps `StubLicenseService()` → `PolarLicenseService()` (the one-line seam swap),
   persists a granted `LicenseRecord` on `.success`, and implements the D-04 error split:
   `.unreachable` → a distinct "server not reachable" state with a manual **Retry** button, kept
   separate from `.invalidKey` → "not recognized". A key with a present Keychain cache is NEVER
   locked out by a network error.

Purpose: this is where LIC-02 becomes observably true end-to-end. It depends on BOTH wave-1 plans
(needs `LicenseManager`/`recordValidation` from 12-01 and `PolarLicenseService` from 12-02).
Output: edits to `Islet/Licensing/LicenseState.swift` and `Islet/SettingsView.swift`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-real-polar-sh-license-integration/12-CONTEXT.md
@.planning/phases/12-real-polar-sh-license-integration/12-RESEARCH.md
@.planning/phases/12-real-polar-sh-license-integration/12-PATTERNS.md
@.planning/phases/12-real-polar-sh-license-integration/12-01-SUMMARY.md
@.planning/phases/12-real-polar-sh-license-integration/12-02-SUMMARY.md

<interfaces>
<!-- Wave-1 outputs this plan consumes -->
From Islet/Licensing/KeychainLicenseStore.swift (12-01):
  final class LicenseManager {
      static let shared: LicenseManager
      var isLicensed: Bool                                   // cached, granted-record presence
      @discardableResult func recordValidation(key: String) -> Bool   // persists granted record + updates cache
  }
From Islet/Licensing/PolarLicenseService.swift (12-02):
  final class PolarLicenseService: LicenseService { init(session: HTTPSession = URLSessionHTTP()) }

<!-- Live edit sites (current state) -->
From Islet/Licensing/LicenseState.swift:
  var status: LicenseStatus {
      #if DEBUG ... override ... #endif        // lines 41-49 — stays first
      if sessionActivated { return .licensed }  // line 54 — NEW persisted branch goes ABOVE this
      guard let start = TrialManager.shared.trialStartDate() ... // trial fallback unchanged
  }
  var isEntitled: Bool { switch status { case .trial, .licensed: return true; case .trialExpired: return false } }  // no change
From Islet/SettingsView.swift:
  private enum ActivationPhase { case idle, validating, success, failure }         // line 17 — add .unreachable
  private let licenseService: LicenseService = StubLicenseService()                // line 20 — swap
  @ViewBuilder private var statusLine ... case .failure: Text("✗ That key wasn't recognized.")  // lines 149-160 — add .unreachable case + Retry
  private func activate() { ... case .failure: activationPhase = .failure }        // lines 164-184 — split by error + persist on success
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the persisted-license branch to LicenseState.status</name>
  <files>Islet/Licensing/LicenseState.swift</files>
  <read_first>
    - Islet/Licensing/LicenseState.swift (whole file, lines 1-80 — the status computed property 40-68; note the DEBUG override precedence and the sessionActivated short-circuit)
    - Islet/Licensing/KeychainLicenseStore.swift (12-01 output — LicenseManager.shared.isLicensed signature)
    - .planning/phases/12-real-polar-sh-license-integration/12-PATTERNS.md (lines 169-213 — where the persisted branch hooks in + the hot-path caching rule)
  </read_first>
  <action>
    In `status`, insert ONE new branch: after the `#if DEBUG ... #endif` override block (ends line 49) and BEFORE `if sessionActivated { return .licensed }` (line 54), add `if LicenseManager.shared.isLicensed { return .licensed }`. Add a short comment noting this is the persisted (survives-relaunch, offline) entitlement and that `LicenseManager` reads the Keychain ONCE and caches in memory (so `status` on the `updateVisibility()` hot path never re-hits the Keychain — the memory-2401 flood mitigation). Make NO other change: the DEBUG override still wins in dev, `sessionActivated` still covers same-session activation, the trial fallback is unchanged, and `isEntitled` already maps `.licensed → true`.
  </action>
  <verify>
    <automated>grep -q 'LicenseManager.shared.isLicensed' Islet/Licensing/LicenseState.swift && grep -n 'LicenseManager.shared.isLicensed\|if sessionActivated' Islet/Licensing/LicenseState.swift | head -2 | awk -F: 'NR==1{a=$1}NR==2{b=$1}END{exit !(a<b)}'</automated>
  </verify>
  <acceptance_criteria>
    - `LicenseState.swift` `status` contains `if LicenseManager.shared.isLicensed { return .licensed }`.
    - That branch appears AFTER the `#if DEBUG` override block and BEFORE the `if sessionActivated` line.
    - No change to `isEntitled`, to the trial fallback, or to the DEBUG override precedence.
  </acceptance_criteria>
  <done>A present granted Keychain record makes LicenseState.status resolve to .licensed at launch with no network call.</done>
</task>

<task type="auto">
  <name>Task 2: Swap to PolarLicenseService, persist on success, and split .unreachable from .invalidKey (D-04 + Retry)</name>
  <files>Islet/SettingsView.swift</files>
  <read_first>
    - Islet/SettingsView.swift (lines 14-20 — the seam declaration; 129-160 — buyNowButton + licenseEntry + statusLine; 162-184 — activate())
    - Islet/Licensing/PolarLicenseService.swift (12-02 output — PolarLicenseService init) and Islet/Licensing/KeychainLicenseStore.swift (12-01 output — LicenseManager.recordValidation)
    - .planning/phases/12-real-polar-sh-license-integration/12-PATTERNS.md (lines 216-246 — call-site swap + D-04 error-split guidance)
    - .planning/phases/12-real-polar-sh-license-integration/12-RESEARCH.md (D-04, lines 46-49)
  </read_first>
  <action>
    Four edits, all local to SettingsView, no new pattern:
    1. Line 20: change `private let licenseService: LicenseService = StubLicenseService()` → `= PolarLicenseService()`. The type stays the PROTOCOL (`LicenseService`), so nothing else in the property changes.
    2. Line 17 enum: add an `.unreachable` case → `private enum ActivationPhase { case idle, validating, success, failure, unreachable }`.
    3. `statusLine` (lines 149-160): add `case .unreachable:` rendering a distinct message (e.g. `Text("⚠ Server not reachable.").foregroundStyle(.secondary)` — do NOT use red, red is reserved for the invalid-key `.failure` per the existing color rule) FOLLOWED BY a `Button("Retry") { activate() }` so the user can manually re-attempt (D-04 — no silent auto-retry). Keep the existing `.failure` case text ("✗ That key wasn't recognized.") for `.invalidKey`.
    4. `activate()` (lines 164-184): keep `activationPhase = .validating` and the existing `.success` branch, but ALSO persist the validated key on success — after `LicenseState.shared.sessionActivated = true` and the existing nudge write (lines 176-177, KEEP them — the nudge fires the updateVisibility() live-unlock path), call `LicenseManager.shared.recordValidation(key: enteredKey.trimmingCharacters(in: .whitespacesAndNewlines))` so the next launch short-circuits offline. Replace the single `case .failure:` with a switch on the error: `case .failure(.invalidKey): activationPhase = .failure` and `case .failure(.unreachable): activationPhase = .unreachable`. A key that already has a present Keychain cache is inherently never locked out by this path (LicenseState.status returns .licensed before any activation attempt is needed).
    Preserve everything else in the file verbatim.
  </action>
  <verify>
    <automated>xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -3 | grep -q 'BUILD SUCCEEDED' && grep -q 'PolarLicenseService()' Islet/SettingsView.swift && grep -q 'recordValidation' Islet/SettingsView.swift && grep -q 'case .unreachable' Islet/SettingsView.swift && grep -q 'Retry' Islet/SettingsView.swift</automated>
  </verify>
  <acceptance_criteria>
    - Line 20 now instantiates `PolarLicenseService()` (no `StubLicenseService` remains at the call site).
    - `ActivationPhase` has an `.unreachable` case; `statusLine` renders a distinct non-red "server not reachable" message plus a `Retry` button that calls `activate()`.
    - `activate()` splits `.failure(.invalidKey)` → `.failure` phase and `.failure(.unreachable)` → `.unreachable` phase.
    - On `.success`, `LicenseManager.shared.recordValidation(key:)` is called AND the existing `sessionActivated = true` + nudge write are preserved.
    - `xcodegen generate` then `xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug` exits 0 (BUILD SUCCEEDED).
  </acceptance_criteria>
  <done>SettingsView uses the real PolarLicenseService, persists a granted record on success, and shows distinct reachable-vs-invalid states with a manual Retry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SettingsView (UI, @State) ↔ LicenseService completion | Completion mutates @State/LicenseState on the main thread (contract from 12-02) |
| LicenseState.status ↔ Keychain (via LicenseManager) | Hot-path read must be cache-served, not a live Keychain hit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Elevation of Privilege | persisted entitlement | mitigate | Success persists a Codable `LicenseRecord` via `LicenseManager.recordValidation` (12-01), never a bool; `LicenseState` reads it, not UserDefaults. |
| T-12-04 | Denial of Service (to the user) | D-04 error split | mitigate | `.unreachable` is a distinct, retryable state (manual Retry); a cached granted key returns `.licensed` before any network attempt, so a network error can never lock a paid key. |
| T-12-06 | Elevation of Privilege | LicenseState hot-path read | mitigate | The persisted branch calls `LicenseManager.isLicensed` (in-memory cached), keeping the Keychain off the `updateVisibility()` path (memory 2401). |
| T-12-SC | Tampering | package installs | accept | N/A — no packages added (edits only). |
</threat_model>

<verification>
- `xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug` → BUILD SUCCEEDED.
- Source assertions: `LicenseManager.shared.isLicensed` in LicenseState.status (correctly ordered), `PolarLicenseService()` at the call site, `recordValidation` on success, `.unreachable` case + `Retry` in SettingsView.
- End-to-end behavior (real key, offline relaunch, error states) is exercised at 12-04's on-device checkpoint.
</verification>

<success_criteria>
- A validated key survives relaunch offline (persisted branch in LicenseState).
- Activation uses the real service and persists a granted record on success.
- `.unreachable` and `.invalidKey` are distinct user-facing states; `.unreachable` offers manual Retry (D-04).
- A paid, cached key is never locked out by a network error.
</success_criteria>

<output>
Create `.planning/phases/12-real-polar-sh-license-integration/12-03-SUMMARY.md` when done
</output>
