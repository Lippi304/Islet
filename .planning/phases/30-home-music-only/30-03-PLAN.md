---
phase: 30-home-music-only
plan: 03
type: execute
wave: 3
depends_on: ["30-02"]
files_modified: []
autonomous: false
requirements: [HOME-01, HOME-02, HOME-03]

must_haves:
  truths:
    - "While something is playing, Home shows live Now-Playing transport controls, unchanged from today (HOME-01)"
    - "When paused/stopped, Home shows the last-played track's real cover art + title, WITH the same transport controls as the live state, including the D-05 hover background (HOME-02)"
    - "When nothing has been played this session, Home shows the explicit empty state (music.note icon, 'Nothing Playing', 'Start something in Spotify or Music.') instead of any glance content (HOME-03)"
    - "The old time/weather/calendar idle glance appears nowhere on Home in any of the 3 sub-states, and Weather/Calendar's own switcher tabs are unaffected by this phase's changes"
  artifacts: []
  key_links: []
---

<objective>
Confirm on real notch hardware that all 3 Home sub-states (live, last-played, empty) behave and
render exactly per HOME-01/02/03 and 30-UI-SPEC.md, closing out the phase before
`/gsd:verify-work 30`.

Purpose: Plans 01-02 build everything through the build gate and Cmd-U unit tests, but artwork
stickiness (a real `NSImage` from a live MediaRemote callback) and the hover-background visual
polish are only meaningfully provable on-device, per this project's established convention
(Phases 7/18/20/21/23/25/26/28/29 all closed with an on-device checkpoint for exactly this class
of behavior).
Output: user-confirmed on-device approval of all 3 states, or a specific list of issues to close
via a follow-up gap-closure plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-home-music-only/30-CONTEXT.md
@.planning/phases/30-home-music-only/30-UI-SPEC.md
@.planning/phases/30-home-music-only/30-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: On-device UAT — all 3 Home sub-states, hover background, Weather/Calendar regression check</name>
  <files>none — verification only, no edits expected</files>
  <action>
    No file edits by default. Open `Islet.xcodeproj` in Xcode (Finder path:
    `/Users/lippi304/conductor/workspaces/notch/algiers/Islet.xcodeproj`), select the `Islet`
    scheme, and Cmd-R to run on the real MacBook with the physical notch, then work through the
    5-point checklist in how-to-verify below.
  </action>
  <what-built>
    Home is now a music-only surface with 3 sub-states: live Now-Playing (unchanged), a new
    last-played state (cover+title+transport controls, fed from the sticky `lastKnownTrack`),
    and a new empty state (icon+heading+body, D-09/D-10 copy). The old time/weather/calendar
    idle glance is deleted. Transport buttons show a new hover background.
  </what-built>
  <how-to-verify>
    In Xcode, open the Islet project folder and run the app on the real notch MacBook (Cmd-R).
    1. HOME-01 (live): Start playback in Spotify or Music. Hover the notch to expand to Home.
       Confirm live transport controls (⏪⏯⏩) render exactly as before this phase — title,
       artist, album art, equalizer bars, progress bar.
    2. D-05 (hover): While Home is expanded and something is playing, hover the mouse over each
       of the 3 transport buttons one at a time. Confirm a rounded-rectangle background appears
       under the icon on hover and disappears when the mouse leaves. Confirm the 2 reserved
       (invisible) Shuffle/Repeat corner slots show no hover box.
    3. HOME-02 (last-played + artwork stickiness): Pause the track in the source app (or stop
       playback entirely). Re-expand Home. Confirm it shows the LAST track's real cover art and
       title (not blank/placeholder), with the SAME transport controls and hover background as
       state 1 — controls must NOT be hidden or disabled. Tap Play: confirm it attempts to
       resume (no crash, no error UI if the source app can't be reached).
    4. HOME-03 (empty state): Fully quit Spotify and Music (or relaunch Islet fresh with neither
       app having played anything this session). Expand Home. Confirm the empty state shows: a
       music-note icon, heading "Nothing Playing", body "Start something in Spotify or Music." —
       and confirm NO time/weather/calendar content appears anywhere in this state.
    5. Regression: switch to the Weather and Calendar tabs via the switcher pill. Confirm both
       still render their own dedicated full views correctly (unaffected by this phase's Home
       changes).
  </how-to-verify>
  <resume-signal>Type "approved" once all 5 checks pass, or describe exactly which check failed and what was observed instead.</resume-signal>
  <verify>
    <human-check>All 5 checklist items pass on-device on the real notch hardware.</human-check>
  </verify>
  <done>User has typed "approved" confirming all 3 Home sub-states, the D-05 hover background, and the Weather/Calendar regression check all behave correctly on-device.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Not applicable — on-device manual verification of an internal presentation-state change, no
attack surface (per 30-RESEARCH.md Security Domain).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | (none applicable) | Home view (manual verification only) | accept | No new trust boundary; verification-only plan |
</threat_model>

<verification>
All 5 on-device checks in the checkpoint above pass. Full `IsletTests` suite (Cmd-U) green as a
final regression sweep before closing the phase.
</verification>

<success_criteria>
User types "approved" after confirming all 3 Home sub-states and the D-05 hover background
behave exactly per HOME-01/02/03 and 30-UI-SPEC.md, with no regression to Weather/Calendar.
</success_criteria>

<output>
Create `.planning/phases/30-home-music-only/30-03-SUMMARY.md` when done
</output>
