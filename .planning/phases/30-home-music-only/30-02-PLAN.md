---
phase: 30-home-music-only
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - Islet/Notch/NotchWindowController.swift
  - Islet/Notch/NotchPillView.swift
autonomous: true
requirements: [HOME-02]

must_haves:
  truths:
    - "The most-recently-playing track's title/artist/artwork survive the transition to nowPlaying == .none, so Home's last-played state shows the real last track, not empty strings/nil art"
    - "lastKnownTrack is overwritten every time a NEW track starts .playing (D-08), never frozen on first capture"
    - "Each of the 3 real transport buttons (prev/playpause/next) shows a rounded-rectangle hover background while the mouse is over it (D-05); the 2 reserved Shuffle/Repeat placeholder slots get no hover box"
  artifacts:
    - path: "Islet/Notch/NotchWindowController.swift"
      provides: "lastKnownTrack capture inside handleNowPlaying(), following the file's existing capture-before-mutate discipline, BEFORE the existing artwork nil-clear logic runs"
      contains: "nowPlayingState.lastKnownTrack = LastPlayedTrack("
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "TransportButton hover-background view, used at mediaExpanded's 3 real transport-button call sites"
      contains: "isHovering"
  key_links:
    - from: "Islet/Notch/NotchWindowController.swift handleNowPlaying()"
      to: "Islet/Notch/NowPlayingState.swift lastKnownTrack"
      via: "capture-before-mutate assignment inside the existing withAnimation spring block, before the artwork nil-clear branch"
      pattern: "lastKnownTrack = LastPlayedTrack"
---

<objective>
Make Plan 01's `.homeLastPlayed` state show REAL data (not the empty-string/nil placeholders it
renders today) by capturing `lastKnownTrack` at the one point the real MediaRemote callback
lands, and add the D-05 transport-button hover background — the phase's one purely cosmetic
addition, unaffected by any enum/switch exhaustiveness.

Purpose: closes the gap between Plan 01's routing (correct but data-less until this plan lands)
and the phase's actual on-device behavior. Both tasks touch disjoint concerns in the same two
files Plan 01 already modified (sequential wave, no same-wave file conflict).
Output: `handleNowPlaying()` populates `lastKnownTrack` with the same capture-before-mutate
discipline already used for `hasPlayedSinceLaunch`/`previous`/`previousPosition`; all 3 real
transport buttons in `mediaExpanded` get a hover-triggered rounded-rectangle background.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-home-music-only/30-CONTEXT.md
@.planning/phases/30-home-music-only/30-RESEARCH.md
@.planning/phases/30-home-music-only/30-PATTERNS.md
@.planning/phases/30-home-music-only/30-UI-SPEC.md
</context>

<interfaces>
<!-- Contract from Plan 01 this plan populates/reads directly, no exploration needed. -->

From Islet/Notch/NowPlayingState.swift (Plan 01):
struct LastPlayedTrack { let title: String; let artist: String; let artwork: NSImage? }
final class NowPlayingState: ObservableObject { ... @Published var lastKnownTrack: LastPlayedTrack? = nil }
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture lastKnownTrack in handleNowPlaying()</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift (handleNowPlaying(), lines 1528-1619 — the full function, including the existing hadPlayedSinceLaunch/previous/previousPosition capture-before-mutate block at lines 1530-1555 and the artwork nil-clear branch at lines 1573-1577)
    Islet/Notch/NowPlayingState.swift (lastKnownTrack field, added in Plan 01)
    .planning/phases/30-home-music-only/30-PATTERNS.md (section "NotchWindowController.swift (handleNowPlaying(), controller/event-driven)")
    .planning/phases/30-home-music-only/30-RESEARCH.md (Pitfall 1, Pitfall 5)
  </read_first>
  <behavior>
    - A `.playing(title: "Song", artist: "Artist")` snapshot with non-nil `art` sets
      `nowPlayingState.lastKnownTrack` to `LastPlayedTrack(title: "Song", artist: "Artist",
      artwork: art)`.
    - A SUBSEQUENT `.playing` snapshot for a DIFFERENT track overwrites `lastKnownTrack` with the
      new track's data (D-08 — never frozen on first capture).
    - A later `.paused` or `.none` snapshot for the SAME session does NOT clear
      `lastKnownTrack` — it must still hold the last `.playing` track's data (Pitfall 1: this is
      the opposite of `nowPlayingState.artwork`'s own clear-on-`.none` behavior; the two fields
      are deliberately independent).
  </behavior>
  <action>
    Inside `handleNowPlaying(_:_:)`'s existing `withAnimation(.spring(...))` block, add the
    `lastKnownTrack` capture immediately BEFORE the existing `if let art { nowPlayingState.artwork
    = art } else if p == .none || !isSameTrack(previous, p) { nowPlayingState.artwork = nil }`
    logic (Pitfall 1 — must run before that nil-clear, using `art`/`p`, which are already in
    scope at that point). Guard the assignment with `if case .playing(let title, let artist) = p`
    and set `nowPlayingState.lastKnownTrack = LastPlayedTrack(title: title, artist: artist,
    artwork: art ?? nowPlayingState.artwork)` — the `art ?? nowPlayingState.artwork` fallback
    mirrors the existing artwork-latency handling (a `.playing` callback with a momentarily-nil
    `art` for the same track keeps whatever art is already showing, same as line 1570's own
    comment describes for `nowPlayingState.artwork`). Do NOT clear `lastKnownTrack` anywhere in
    this function — no `.paused`/`.none` branch should touch it. Do NOT add any app-targeted
    "resume specific app" parameter or bundle-identifier plumbing (Pitfall 5 — D-06 explicitly
    accepts a generic transport command with no visible error if the source app is unreachable;
    `MediaController`/the adapter wrapper exposes no such API, do not build one).
  </action>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "nowPlayingState.lastKnownTrack = LastPlayedTrack" Islet/Notch/NotchWindowController.swift` finds the new assignment
    - The assignment line appears BEFORE the `if let art {` artwork nil-clear block in the same function (verify by reading the surrounding ~15 lines, not just grep line number, since grep alone can't prove ordering)
    - `grep -c "nowPlayingState.lastKnownTrack = nil" Islet/Notch/NotchWindowController.swift` returns `0` (never cleared)
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` succeeds with zero errors
  </acceptance_criteria>
  <done>lastKnownTrack is populated on every real .playing callback, survives the transition to
  .paused/.none, and is never cleared by this function.</done>
</task>

<task type="auto">
  <name>Task 2: Transport-button hover background (D-05)</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift (transportButton, lines 1549-1557; its 3 call sites inside mediaExpanded, lines 1529-1538)
    .planning/phases/30-home-music-only/30-UI-SPEC.md (Layout & Interaction Contract — Property table: shape/size/fill/trigger/scope/animation defaults for D-05)
  </read_first>
  <action>
    `transportButton(_:action:)` is currently a plain function returning `some View` — it cannot
    hold `@State` directly. Convert it into a small private `View` struct (e.g. `struct
    TransportButton: View`) with stored `let systemName: String`, `let action: () -> Void`, and
    `@State private var isHovering = false`. Its body renders the existing
    `Image(systemName:)` icon (unchanged: `.font(.system(size: 16, weight: .semibold))`,
    `.foregroundStyle(.white)`, `.frame(width: 28, height: 28)`) inside a `.background(
    RoundedRectangle(cornerRadius: 8, style: .continuous).fill(isHovering ?
    Color.white.opacity(0.12) : Color.clear))`, wrapped in an outer `.frame(width: 32, height:
    32)`, `.buttonStyle(.plain)`, and `.onHover { isHovering = $0 }` — matching 30-UI-SPEC.md's
    documented default values exactly (these are starting values, expected to be tuned on-device
    per this project's established convention; do not treat them as unchangeable). Update
    `mediaExpanded`'s 3 call sites (`"backward.fill"`/`onPrevious`, `"playpause.fill"`/
    `onTogglePlayPause`, `"forward.fill"`/`onNext`) to construct `TransportButton(systemName:
    action:)` instead of calling the old function. Leave the two reserved Shuffle/Repeat
    `Color.clear.frame(width: 28, height: 28)` placeholder slots UNCHANGED — they get no hover
    box (30-UI-SPEC.md: "Scope: Only the 3 real buttons... the reserved... slots get no hover
    box — they are not interactive").
  </action>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "struct TransportButton" Islet/Notch/NotchPillView.swift` finds the new view struct
    - `grep -n "@State private var isHovering" Islet/Notch/NotchPillView.swift` finds the hover state
    - `grep -n "RoundedRectangle(cornerRadius: 8" Islet/Notch/NotchPillView.swift` finds the hover-background shape
    - `grep -c "TransportButton(systemName:" Islet/Notch/NotchPillView.swift` returns `3` (all 3 real transport buttons converted)
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` succeeds with zero errors
  </acceptance_criteria>
  <done>All 3 real transport buttons show a rounded-rectangle hover background; the 2 reserved
  placeholder slots are unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Not applicable — controller-internal state capture and a purely visual SwiftUI hover affordance.
No user input, network, auth, or crypto surface (per 30-RESEARCH.md Security Domain).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02 | (none applicable) | `handleNowPlaying()` / `TransportButton` | accept | Internal state capture + visual hover affordance; no new trust boundary |
</threat_model>

<verification>
- `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` succeeds after each task.
- Manual on-device check (covered by Plan 03's checkpoint, not this plan): play a track, pause/stop it in the source app, confirm Home's last-played state shows the real title/artist/cover art with transport controls, and confirm the hover background appears on each of the 3 real buttons.
</verification>

<success_criteria>
- `lastKnownTrack` holds real, up-to-date data reflecting the most recently playing track at all
  times after at least one track has played this session.
- All 3 real transport buttons show the D-05 hover background; reserved slots do not.
</success_criteria>

<output>
Create `.planning/phases/30-home-music-only/30-02-SUMMARY.md` when done
</output>
