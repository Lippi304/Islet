---
phase: 66-men-bar-overflow-ice-style-mvp
plan: 02
type: execute
wave: 1
depends_on: ["66-01"]
files_modified:
  - Islet/Notch/MenuBarOverflowStore.swift
  - IsletTests/MenuBarOverflowStoreTests.swift
autonomous: true
requirements: [MENUBAR-02]

must_haves:
  truths:
    - "The hidden/visible bundle-ID assignment persists across relaunch (D-03) via one UserDefaults key holding a Set<String>, never one key per bundle ID"
    - "A malformed or empty bundle-ID string is never written into the persisted set (Security Domain V5 guard)"
    - "The store exposes a pure diff function identifying which currently-running bundle IDs have a stored 'hidden' assignment that still needs re-applying (Pitfall 1), so Plan 66-03's Controller never re-derives this logic"
  artifacts:
    - path: "Islet/Notch/MenuBarOverflowStore.swift"
      provides: "hide(bundleID:)/unhide(bundleID:)/isHidden(bundleID:)/hiddenBundleIDs + itemsNeedingReapplication(runningBundleIDs:)"
      exports: ["MenuBarOverflowStore"]
    - path: "IsletTests/MenuBarOverflowStoreTests.swift"
      provides: "unit coverage of persistence round-trip, malformed-key guard, and the reapplication diff"
  key_links:
    - from: "IsletTests/MenuBarOverflowStoreTests.swift"
      to: "Islet/Notch/MenuBarOverflowStore.swift"
      via: "direct calls against an isolated UserDefaults(suiteName:) instance"
      pattern: "UserDefaults\\(suiteName:"
---

<objective>
Build the D-03 persistence store: a small, isolated `UserDefaults`-backed record of which other
apps' bundle IDs the user has hidden behind the chevron, plus the pure diff logic Plan 66-03's
Controller needs to re-apply a hidden assignment whenever that bundle ID's status item reappears
(RESEARCH.md Pitfall 1 — Ice's own source does not solve this generically, so this codebase must).

Purpose: Isolate the ONE piece of this phase that is genuinely pure, testable business logic
(mirrors this codebase's `ActivitySettings` key-namespace convention, generalized from fixed keys
to an arbitrary, growing bundle-ID set) — built and unit-tested before the fragile CGS-mechanism
Controller (Plan 66-03) consumes it, so a Controller bug can never be confused with a persistence
bug.
Output: `MenuBarOverflowStore.swift` + `MenuBarOverflowStoreTests.swift`, both headless-testable
(no Accessibility/CGS calls, no TCC wait).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-men-bar-overflow-ice-style-mvp/66-CONTEXT.md
@.planning/phases/66-men-bar-overflow-ice-style-mvp/66-RESEARCH.md
@.planning/phases/66-men-bar-overflow-ice-style-mvp/66-PATTERNS.md

<interfaces>
From `Islet/Notch/MeetingMonitor.swift` (already read in full, this codebase) — the bundle-ID
membership pattern to reuse for the reapplication diff:
```swift
NSWorkspace.shared.runningApplications.contains { $0.bundleIdentifier.map(targetBundleIDs.contains) ?? false }
```

From `IsletTests/ActivitySettingsTests.swift` — the isolated-suite test pattern this file's tests
must mirror (never touch `.standard`, always a fresh `UUID()`-suffixed suite name):
```swift
let defaults = UserDefaults(suiteName: "ActivitySettingsTests-\(UUID().uuidString)")!
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MenuBarOverflowStore — hide/unhide/isHidden CRUD (TDD)</name>
  <files>Islet/Notch/MenuBarOverflowStore.swift, IsletTests/MenuBarOverflowStoreTests.swift</files>
  <read_first>
    Islet/ActivitySettings.swift
    IsletTests/ActivitySettingsTests.swift
  </read_first>
  <behavior>
    - Test: `hide(bundleID: "com.example.app")` then `isHidden(bundleID: "com.example.app")` returns `true`; a bundle ID never hidden returns `false`.
    - Test: `unhide(bundleID:)` removes a previously hidden bundle ID; `isHidden` then returns `false`.
    - Test: `hide(bundleID: "")` (empty string) is a no-op — `hiddenBundleIDs` count does not change and `isHidden(bundleID: "")` returns `false` (Security Domain V5 guard against malformed keys).
    - Test: the assignment persists across two separate `MenuBarOverflowStore` instances backed by the SAME `UserDefaults(suiteName:)` instance (construct instance A, `hide(bundleID:)`, construct instance B against the same suite, confirm `isHidden` on B returns `true` — proves D-03's "survives relaunch" contract at the store layer).
  </behavior>
  <action>
    Create `MenuBarOverflowStore.swift`: a small `final class MenuBarOverflowStore` (not an enum,
    since it wraps a `UserDefaults` instance member for testability — mirrors this file's own
    `defaults:` injection need, distinct from `ActivitySettings`'s static-enum shape since that
    enum has no per-test isolation requirement). `init(defaults: UserDefaults = .standard)`. One
    storage key: `static let hiddenBundleIDsKey = "menuBarOverflow.hiddenBundleIDs"`, stored as
    `Set<String>` via `defaults.set(Array(set), forKey:)` / read back via
    `Set(defaults.stringArray(forKey:) ?? [])` (a plain `[String]`-backed `Set`, no custom
    `Codable` wrapper needed — 66-PATTERNS.md's own recommendation). Expose `func
    hide(bundleID: String)`, `func unhide(bundleID: String)`, `func isHidden(bundleID: String) ->
    Bool`, `var hiddenBundleIDs: Set<String> { get }`. `hide`/`unhide` guard `!bundleID.isEmpty`
    before any read-modify-write — an empty string must never enter the persisted set.
    Create `IsletTests/MenuBarOverflowStoreTests.swift` mirroring `ActivitySettingsTests.swift`'s
    isolated-suite pattern (`UserDefaults(suiteName: "MenuBarOverflowStoreTests-\(UUID().uuidString)")!`)
    for every test case, with the 4 cases from `<behavior>` above.
    Run `xcodegen generate` after creating both files, before building.
  </action>
  <acceptance_criteria>
    - `grep -c "func hide(bundleID" Islet/Notch/MenuBarOverflowStore.swift` returns 1
    - `grep -c "bundleID.isEmpty" Islet/Notch/MenuBarOverflowStore.swift` returns at least 2 (both hide and unhide guard)
    - `grep -c "UserDefaults(suiteName:" IsletTests/MenuBarOverflowStoreTests.swift` returns at least 4
    - `IsletTests/MenuBarOverflowStoreTests` suite passes
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild test -project Islet.xcodeproj -scheme Islet -only-testing:IsletTests/MenuBarOverflowStoreTests 2>&1 | tail -40</automated>
  </verify>
  <done>MenuBarOverflowStore.swift compiles and exposes hide/unhide/isHidden/hiddenBundleIDs; all 4 test cases pass; empty-string guard verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: itemsNeedingReapplication diff (TDD, resolves RESEARCH.md Pitfall 1)</name>
  <files>Islet/Notch/MenuBarOverflowStore.swift, IsletTests/MenuBarOverflowStoreTests.swift</files>
  <read_first>
    Islet/Notch/MeetingMonitor.swift
  </read_first>
  <behavior>
    - Test: given `hiddenBundleIDs = ["com.a", "com.b"]` and `runningBundleIDs = ["com.a", "com.c"]`, `itemsNeedingReapplication(runningBundleIDs:)` returns `["com.a"]` — only bundle IDs that are BOTH stored-hidden AND currently running need their move re-applied (`com.b` isn't running, `com.c` isn't hidden).
    - Test: an empty `hiddenBundleIDs` set returns an empty result regardless of `runningBundleIDs`.
    - Test: the function never mutates `hiddenBundleIDs` (pure — a read-only intersection) — assert `hiddenBundleIDs` is unchanged after the call.
  </behavior>
  <action>
    Add `func itemsNeedingReapplication(runningBundleIDs: Set<String>) -> Set<String>` to
    `MenuBarOverflowStore`, implemented as `hiddenBundleIDs.intersection(runningBundleIDs)` — a
    pure, one-line set intersection (mirrors `MeetingMonitor.isTargetAppRunning`'s
    `Set<String>.contains` membership-check shape, generalized to an intersection since this
    needs the full matching subset, not just a Bool). This is the exact function Plan 66-03's
    Controller calls from its `NSWorkspace.didLaunchApplicationNotification` observer and its
    poll-timer fallback — it must take zero dependency on `NSWorkspace` itself (the caller
    supplies `runningBundleIDs`), keeping this file free of any live-system read, per this file's
    own "pure store" scope. Add the 3 test cases from `<behavior>` to
    `MenuBarOverflowStoreTests.swift`.
  </action>
  <acceptance_criteria>
    - `grep -c "func itemsNeedingReapplication" Islet/Notch/MenuBarOverflowStore.swift` returns 1
    - `grep -c "import AppKit\|import Foundation" Islet/Notch/MenuBarOverflowStore.swift` returns at least 1 and `grep -c "NSWorkspace" Islet/Notch/MenuBarOverflowStore.swift` returns 0 (confirms the file stays free of any live-system read)
    - `itemsNeedingReapplication` test cases pass
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodebuild test -project Islet.xcodeproj -scheme Islet -only-testing:IsletTests/MenuBarOverflowStoreTests 2>&1 | tail -40</automated>
  </verify>
  <done>itemsNeedingReapplication compiles, is pure (no NSWorkspace import), and all 3 new test cases pass alongside Task 1's 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `NSWorkspace`-sourced bundle-ID strings → `UserDefaults` keys/set members | OS-supplied strings (not raw user text), but must still be validated before use as storage identifiers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-05 | Tampering (malformed/empty key corrupting the persisted set) | `MenuBarOverflowStore.hide`/`.unhide` | mitigate | Explicit `!bundleID.isEmpty` guard before every read-modify-write, unit-tested (Task 1, `<behavior>` case 3) |
| T-66-06 | Information Disclosure | `menuBarOverflow.hiddenBundleIDs` UserDefaults value | accept | A list of other installed apps' bundle identifiers is not sensitive data — mirrors NOTES-03's precedent that not everything needs AES-GCM parity with Clipboard History |
</threat_model>

<verification>
`xcodebuild test -project Islet.xcodeproj -scheme Islet -only-testing:IsletTests/MenuBarOverflowStoreTests` passes (7 total test cases across both tasks); full project build stays green.
</verification>

<success_criteria>
- `MenuBarOverflowStore` compiles and exposes `hide`/`unhide`/`isHidden`/`hiddenBundleIDs`/`itemsNeedingReapplication`
- Persistence round-trips across two instances sharing one `UserDefaults` suite
- Empty/malformed bundle IDs are never persisted
- `itemsNeedingReapplication` is a pure function with zero `NSWorkspace` dependency
</success_criteria>

<output>
Create `.planning/phases/66-men-bar-overflow-ice-style-mvp/66-02-SUMMARY.md` when done
</output>
</threat_model>
