---
phase: 66-men-bar-overflow-ice-style-mvp
plan: 07
type: execute
wave: 3
depends_on: ["66-06"]
files_modified:
  - Islet/Notch/MenuBarOverflowPermissionExplanationView.swift
  - Islet/SettingsView.swift
  - Islet/AppDelegate.swift
  - Islet/ActivitySettings.swift
  - Islet.xcodeproj/project.pbxproj
autonomous: true
requirements: [MENUBAR-04]

must_haves:
  truths:
    - "The first time Islet ever launches while Accessibility is not yet trusted, a one-time popover explains exactly why Menu Bar Overflow needs Accessibility access, distinct from Islet's other permission prompts"
    - "That popover never reappears on any later launch, even if dismissed via Not Now while still denied"
    - "While Accessibility is denied, Settings shows a visible, clearly-worded row explaining the gap and deep-links straight to the correct System Settings pane — the feature never fails silently"
    - "Once Accessibility is granted, both the popover trigger and the Settings row disappear — nothing in the UI keeps nagging about an already-satisfied permission"
  artifacts:
    - path: "Islet/Notch/MenuBarOverflowPermissionExplanationView.swift"
      provides: "Standalone one-time explanation view, hostable via NSHostingController and anchored to Islet's main status item"
      exports: ["MenuBarOverflowPermissionExplanationView"]
    - path: "Islet/SettingsView.swift"
      provides: "A standalone 'Menu Bar Overflow' permission row, visible only while untrusted, NOT a 6th PermissionKind case"
      contains: "Accessibility Needed"
    - path: "Islet/AppDelegate.swift"
      provides: "One-time launch-triggered NSPopover anchored to the main statusItem button"
      contains: "menuBarOverflowPermissionPopover"
  key_links:
    - from: "Islet/AppDelegate.swift applicationDidFinishLaunching"
      to: "Islet/Notch/MenuBarOverflowPermissionExplanationView.swift"
      via: "one-time NSPopover(NSHostingController) show, gated on AXIsProcessTrusted() == false and a persisted show-once flag"
      pattern: "hasShownMenuBarOverflowPermissionExplanationKey"
    - from: "Islet/SettingsView.swift permissionsSection"
      to: "System Settings Privacy & Security > Accessibility"
      via: "x-apple.systempreferences deep link, identical URL to the popover's own button"
      pattern: "Privacy_Accessibility"
---

<objective>
Build MENUBAR-04's reopened Accessibility permission surface: a one-time, self-explaining popover
shown the first time Islet launches while untrusted (anchored to Islet's own always-present main
status item, since the chevron itself does not exist yet while untrusted), plus a standalone,
always-honest Settings row that appears only while the permission is missing. This is this phase's
first-ever Settings surface and first-ever launch-time (non-toggle-triggered) popover.

Purpose: Satisfy CONTEXT.md's explicit "visible, not silent" requirement for MENUBAR-04, using this
codebase's own already-shipped OSD/Caps Lock permission-explanation pattern as the template.
Output: A working one-time popover + Settings row, both reactive to a live Accessibility grant/deny.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-men-bar-overflow-ice-style-mvp/66-CONTEXT.md
@.planning/phases/66-men-bar-overflow-ice-style-mvp/66-UI-SPEC.md
@.planning/phases/66-men-bar-overflow-ice-style-mvp/66-PATTERNS.md
@.planning/phases/66-men-bar-overflow-ice-style-mvp/66-06-SUMMARY.md

<interfaces>
<!-- Existing shipped popover-explanation shape to clone (Islet/SettingsView.swift:770-792,
     capsLockPermissionExplanationView) — this plan builds the FREESTANDING equivalent, since
     AppDelegate.swift (not a SwiftUI View) needs a concrete View TYPE to host, not a computed
     property on the SettingsView struct. -->
```swift
// Islet/SettingsView.swift:770-792 (existing, shipped, INSIDE SettingsView struct — clone the
// visual shape, not the containing type, into a new standalone struct)
private var capsLockPermissionExplanationView: some View {
    VStack(alignment: .leading, spacing: 8) {
        Text("Caps Lock HUD").font(.system(size: 15, weight: .semibold))
        Text("Islet needs Accessibility access to detect Caps Lock. ...")
            .font(.system(size: 12)).lineSpacing(12 * 0.4)
        HStack {
            Button("Not Now") { showCapsLockPermissionExplanation = false }
            Spacer()
            Button("Open System Settings") {
                NSWorkspace.shared.open(URL(string: "x-apple.systempreferences:com.apple.preference.security?Privacy_Accessibility")!)
                showCapsLockPermissionExplanation = false
            }.keyboardShortcut(.defaultAction)
        }
    }.padding(16).frame(width: 280)
}
```

<!-- Existing precedent for a standalone (non-SettingsView-member) SwiftUI view hosted from
     AppDelegate via NSPopover — Islet/QuickNotes/QuickNotesPopoverView.swift is the file-shape
     precedent; Islet/AppDelegate.swift:43-48,378 is the popover-construction + show precedent. -->
```swift
// Islet/AppDelegate.swift:43-48 (existing, shipped)
private lazy var quickNotesPopover: NSPopover = {
    let popover = NSPopover()
    popover.behavior = .transient
    popover.contentViewController = NSHostingController(rootView: QuickNotesPopoverView(controller: quickNotesController))
    return popover
}()
// Islet/AppDelegate.swift:378 (existing, shipped) — anchors to a status item's own button
quickNotesPopover.show(relativeTo: button.bounds, of: button, preferredEdge: .minY)
```

<!-- Existing permissionRow shape to clone visually as a STANDALONE row, not a PermissionKind
     case — Islet/SettingsView.swift:566-582 (call sites), 600-636 (permissionRow/statusView) -->
```swift
// Islet/SettingsView.swift:624-636 (existing, shipped) — reuse this exact denied-state visual
@ViewBuilder private func statusView(for status: PermissionStatus) -> some View {
    case .denied:
        Label("Denied", systemImage: "xmark.circle.fill").foregroundStyle(.red)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standalone permission-explanation view + new persistence keys</name>
  <files>Islet/Notch/MenuBarOverflowPermissionExplanationView.swift, Islet/ActivitySettings.swift</files>
  <read_first>
    Islet/SettingsView.swift (lines 764-792, capsLockPermissionExplanationView — the exact shape to clone)
    Islet/QuickNotes/QuickNotesPopoverView.swift (the standalone-View-file precedent AppDelegate already hosts)
    .planning/phases/66-men-bar-overflow-ice-style-mvp/66-UI-SPEC.md (Permission Contract section — exact copy/dimensions)
  </read_first>
  <action>
    Create `Islet/Notch/MenuBarOverflowPermissionExplanationView.swift` with a top-level
    `struct MenuBarOverflowPermissionExplanationView: View` (NOT a member of `SettingsView` — it
    must be independently instantiable from `AppDelegate.swift`). Clone
    `capsLockPermissionExplanationView`'s exact visual shape (`VStack(alignment: .leading, spacing: 8)`,
    16px padding, 280px fixed width, 15px-semibold title, 12px-regular body with `.lineSpacing(12 * 0.4)`,
    an `HStack` with `"Not Now"` / `"Open System Settings"` buttons). Title: `"Menu Bar Overflow"`.
    Body copy, verbatim per UI-SPEC.md: `"Islet needs Accessibility access to hide and reveal other
    apps' menu bar icons. Islet only repositions menu bar items — it never reads, modifies, or sends
    anything else on your Mac."`. Since this struct cannot reach `SettingsView`'s private `@State`,
    give it one `let onDismiss: () -> Void` property; both buttons call `onDismiss()` ("Open System
    Settings" additionally calls `NSWorkspace.shared.open(URL(string:
    "x-apple.systempreferences:com.apple.preference.security?Privacy_Accessibility")!)` first, same
    URL already proven working elsewhere in this codebase).
    Add one new key constant to `Islet/ActivitySettings.swift`, grouped near
    `menuBarOverflowHiddenAssignmentsKey`: `static let hasShownMenuBarOverflowPermissionExplanationKey
    = "menuBarOverflow.hasShownPermissionExplanation"`.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -configuration Release -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "struct MenuBarOverflowPermissionExplanationView: View" Islet/Notch/MenuBarOverflowPermissionExplanationView.swift` equals 1
    - `grep -c "onDismiss" Islet/Notch/MenuBarOverflowPermissionExplanationView.swift` is at least 2 (property + both button call sites)
    - `grep -c "hasShownMenuBarOverflowPermissionExplanationKey" Islet/ActivitySettings.swift` equals 1
    - The file compiles standalone (verified in Task 3's build)
  </acceptance_criteria>
  <done>A standalone, independently-instantiable SwiftUI view exists with UI-SPEC.md's exact copy and dimensions, ready to be hosted by AppDelegate.swift.</done>
</task>

<task type="auto">
  <name>Task 2: Settings row — visible only while untrusted, never a PermissionKind case</name>
  <files>Islet/SettingsView.swift</files>
  <read_first>
    Islet/SettingsView.swift (permissionsSection lines 560-594, permissionRow/statusView lines 600-636, onAppear/onChange(of: appearsActive) lines 352-365)
    .planning/phases/66-men-bar-overflow-ice-style-mvp/66-UI-SPEC.md (Degraded state / Settings row table)
  </read_first>
  <action>
    Add one `@State private var menuBarOverflowAccessibilityTrusted = AXIsProcessTrusted()` near the
    file's other permission `@State` declarations. Update it inside `refreshPermissionStatuses()`
    (called from both `.onAppear` and `.onChange(of: appearsActive)`, so the row reacts the same way
    every other permission row already does) with one added line:
    `menuBarOverflowAccessibilityTrusted = AXIsProcessTrusted()`.
    Inside `permissionsSection`'s `VStack(spacing: 8)` (SettingsView.swift ~567-582), after the
    existing 5 `permissionRow(...)` calls, add: `if !menuBarOverflowAccessibilityTrusted {
    menuBarOverflowPermissionRow }`. Add a new private computed property
    `menuBarOverflowPermissionRow: some View` cloning `permissionRow`'s exact `HStack(spacing: 8)`
    visual shape (icon left `.foregroundStyle(.secondary)`, `VStack(alignment: .leading, spacing: 4)`
    of label+reason, `Spacer()`, status pill right, whole-row `Button`/`.buttonStyle(.plain)` tap
    target) but as a STANDALONE view, not a `permissionRow(kind:...)` call — it must NOT add a 6th
    `PermissionKind` case and must NOT be counted in `grantedPermissionCount`'s "X of 5" text. Icon:
    `"menubar.rectangle"`. Label: `"Menu Bar Overflow"`. Reason: `"Hide other apps' menu bar icons
    behind a chevron"`. Status pill: clone the existing denied-state visual exactly
    (`Label("Accessibility Needed", systemImage: "xmark.circle.fill").foregroundStyle(.red)`). Tap
    action: always deep-links to `x-apple.systempreferences:com.apple.preference.security?Privacy_Accessibility`
    (no `PermissionStatus`-branching — Accessibility has no `notYetAsked`/request-API path, matching
    the existing OSD/Caps Lock deep-link-only precedent).
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -configuration Release -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "menuBarOverflowAccessibilityTrusted" Islet/SettingsView.swift` is at least 3 (declaration + refresh + row condition)
    - `grep -c "menuBarOverflowPermissionRow" Islet/SettingsView.swift` is at least 2 (definition + call site)
    - `grep -c "Accessibility Needed" Islet/SettingsView.swift` equals 1
    - `grep -c "case menuBarOverflow" Islet/PermissionStatus.swift` equals 0 (confirms no 6th PermissionKind case was added)
  </acceptance_criteria>
  <done>A standalone Settings row appears only while Accessibility is untrusted, disappears once granted (re-checked on appear/active), and deep-links to the correct System Settings pane — without touching PermissionKind or the 5-permission rollup count.</done>
</task>

<task type="auto">
  <name>Task 3: AppDelegate wiring — one-time launch-triggered popover anchored to the main status item</name>
  <files>Islet/AppDelegate.swift, Islet.xcodeproj/project.pbxproj</files>
  <read_first>
    Islet/AppDelegate.swift (lines 1-90 for the quickNotesPopover property precedent and statusItem construction; lines 260-288 for applicationDidFinishLaunching's current tail, where menuBarOverflowController.start() is called)
    Islet/Notch/MenuBarOverflowPermissionExplanationView.swift (Task 1's output)
  </read_first>
  <action>
    Add a new `private lazy var menuBarOverflowPermissionPopover: NSPopover` property, cloning
    `quickNotesPopover`'s exact construction shape (`NSPopover()`, `.behavior = .transient`,
    `.contentViewController = NSHostingController(rootView: MenuBarOverflowPermissionExplanationView(onDismiss:
    { [weak self] in self?.menuBarOverflowPermissionPopover.performClose(nil) }))`).
    In `applicationDidFinishLaunching`, immediately after the existing `menuBarOverflowController.start()`
    call (line 287), add: if `AXIsProcessTrusted() == false` AND
    `!UserDefaults.standard.bool(forKey: ActivitySettings.hasShownMenuBarOverflowPermissionExplanationKey)`,
    then set that UserDefaults flag to `true` immediately (so it can never show twice even if the
    popover itself fails to present), and dispatch `showMenuBarOverflowPermissionPopover()`
    asynchronously on the main queue (mirrors `hideSettingsWindowOnLaunch`'s `DispatchQueue.main.async`
    pattern a few lines above, since `statusItem.button`'s window may not be laid out on the very first
    run-loop pass). Add the new private method `showMenuBarOverflowPermissionPopover()` that guards on
    `statusItem.button` existing and calls
    `menuBarOverflowPermissionPopover.show(relativeTo: button.bounds, of: button, preferredEdge: .minY)`
    — the exact same anchor (`statusItem`, Islet's main capsule.fill icon) and call shape as
    `quickNotesPopover.show(...)` at line 378, per UI-SPEC.md's explicit instruction that the chevron
    itself does not exist yet at this point (untrusted) so the popover must anchor to the always-present
    main item instead.
    Run `xcodegen generate` (registers the new file from Task 1) and
    `xcodebuild build -scheme Islet -configuration Release -destination 'platform=macOS'`.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -configuration Release -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "menuBarOverflowPermissionPopover" Islet/AppDelegate.swift` is at least 3 (property + trigger call + show method)
    - `grep -c "hasShownMenuBarOverflowPermissionExplanationKey" Islet/AppDelegate.swift` is at least 2 (read check + write-true)
    - `grep -c "showMenuBarOverflowPermissionPopover" Islet/AppDelegate.swift` is at least 2 (definition + call site)
    - `xcodebuild build -scheme Islet -configuration Release -destination 'platform=macOS'` prints `BUILD SUCCEEDED`
  </acceptance_criteria>
  <done>A one-time popover, anchored to Islet's main status item, appears at launch exactly once while Accessibility is untrusted, and the persisted flag prevents it from ever appearing again regardless of grant/deny outcome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Islet process ↔ System Settings Privacy pane | Deep-link only (`x-apple.systempreferences:`), no programmatic grant/bypass attempted |
| Islet process ↔ its own UserDefaults domain | The one-time-shown flag and the trusted/untrusted UI state are both read from local, unauthenticated storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-02 | Denial of Service (self-inflicted, UX-level) | Settings row / launch popover | mitigate | The feature's degraded state is always visible (absent chevron already ensured by Plan 66-06's gate + this plan's Settings row + one-time popover) — never a silent no-op with no explanation anywhere |
| T-66-06 | Spoofing (UX) | Deep-link URL construction | accept | The `x-apple.systempreferences:` URL is a hardcoded literal identical to 4 other already-shipped call sites in this codebase — no user or remote input is ever interpolated into it |
</threat_model>

<verification>
- `xcodebuild build -scheme Islet -configuration Release -destination 'platform=macOS'` succeeds after all 3 tasks.
- `grep -c "case menuBarOverflow" Islet/PermissionStatus.swift` remains 0.
</verification>

<success_criteria>
- A one-time, correctly-worded popover appears at launch while untrusted, anchored to the main status item, and never reappears.
- A Settings row visibly explains the missing permission and deep-links correctly, disappearing once granted.
</success_criteria>

<output>
Create `.planning/phases/66-men-bar-overflow-ice-style-mvp/66-07-SUMMARY.md` when done
</output>
