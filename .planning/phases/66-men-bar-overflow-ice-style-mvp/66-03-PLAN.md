---
phase: 66-men-bar-overflow-ice-style-mvp
plan: 03
type: execute
wave: 2
depends_on: ["66-02"]
files_modified:
  - Islet/Notch/MenuBarOverflowBridging.swift
  - IsletTests/MenuBarOverflowManualSpike.swift
  - Islet/SettingsView.swift
  - Islet.xcodeproj/project.pbxproj
autonomous: true
requirements: [MENUBAR-01, MENUBAR-03]

must_haves:
  truths:
    - "No Settings on/off toggle or card exists anywhere for Menübar-Overflow (D-02/D-04 forbid it) — the stale Phase-59-era placeholder card is fully removed"
    - "No superseded private-CGS-API spike code (Plan 66-01's NO-GO'd mechanism) remains in the codebase to confuse a future reader or accidentally get re-wired"
  artifacts:
    - path: "Islet/SettingsView.swift"
      provides: "No menuBarOverflow @AppStorage binding, no menuBarOverflow ActivityCardData entry"
  key_links: []
---

<objective>
Remove the two categories of stale artifact this phase's mechanism pivot leaves behind: (1) Plan 66-01's superseded private-CGS-API spike files (NO-GO'd, D-06 discretion — delete, do not repurpose), and (2) the pre-existing Phase-59-era `menuBarOverflowKey` Settings toggle/card, which D-02/D-04 now forbid outright (this feature has zero Settings surface).

Purpose: Prevent a future reader from mistaking the deleted-mechanism spike code for live implementation, and remove the on/off toggle whose semantics directly contradict this phase's locked "activates automatically, no toggle" decision.
Output: `MenuBarOverflowBridging.swift` and `MenuBarOverflowManualSpike.swift` deleted; `SettingsView.swift`'s stale card/binding removed. Sequenced as Wave 2 (depends_on 66-02) solely because both plans regenerate the shared, tool-generated `project.pbxproj` via `xcodegen generate` — no source-level dependency on 66-02's new controller exists.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-men-bar-overflow-ice-style-mvp/66-CONTEXT.md
@.planning/phases/66-men-bar-overflow-ice-style-mvp/66-PATTERNS.md
@.planning/phases/66-men-bar-overflow-ice-style-mvp/66-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the superseded Ice-mechanism spike files</name>
  <files>Islet/Notch/MenuBarOverflowBridging.swift, IsletTests/MenuBarOverflowManualSpike.swift, Islet.xcodeproj/project.pbxproj</files>
  <read_first>
    Islet/Notch/MenuBarOverflowBridging.swift (confirm scope before deleting — private CGS* shim + moveMenuBarItem(), the file 66-01-SUMMARY.md's NO-GO verdict covers)
    IsletTests/MenuBarOverflowManualSpike.swift (confirm scope before deleting — Cmd-U-only manual spike test exercising the above)
    .planning/phases/66-men-bar-overflow-ice-style-mvp/66-01-SUMMARY.md (the NO-GO verdict this deletion resolves — read the "On-device verdict" section to confirm these files are dead, not salvageable)
  </read_first>
  <action>
    Delete `Islet/Notch/MenuBarOverflowBridging.swift` and `IsletTests/MenuBarOverflowManualSpike.swift` in full — per CONTEXT.md D-06 ("Claude's discretion whether to delete or repurpose") and 66-PATTERNS.md's explicit "do not build on these" instruction, delete rather than repurpose: the private-CGS mechanism is confirmed NO-GO on this hardware/macOS version (66-01-SUMMARY.md), so no part of either file's logic (window enumeration, synthetic-drag `moveMenuBarItem`, the Cmd-U spike harness) transfers to the new public-API mechanism built in Plan 66-02. Do not search for or remove any other reference to these two files' internal symbols (`menuBarItemWindows()`, `currentFrame(windowID:)`, `moveMenuBarItem`) elsewhere in the codebase — 66-01-SUMMARY.md confirms these were spike-only, never called from production code (`AppDelegate.swift`/`NotchWindowController.swift`), so deleting the 2 files is a complete, self-contained removal. Run `xcodegen generate` to regenerate `project.pbxproj` without the deleted files' references, then `xcodebuild build -scheme Islet -configuration Release -destination 'platform=macOS'` to confirm the build is clean with both files gone.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -configuration Release -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    CLI check: `test -f Islet/Notch/MenuBarOverflowBridging.swift && echo EXISTS || echo GONE` outputs `GONE`.
    CLI check: `test -f IsletTests/MenuBarOverflowManualSpike.swift && echo EXISTS || echo GONE` outputs `GONE`.
    Source assertion: `grep -rc "MenuBarOverflowBridging\|moveMenuBarItem\|MenuBarOverflowManualSpike" Islet IsletTests 2>/dev/null | grep -v ':0' | wc -l` == 0 (no remaining reference to either deleted file's symbols anywhere in the app or test target).
    Test command: `xcodebuild build -scheme Islet -configuration Release -destination 'platform=macOS'` — BUILD SUCCEEDED, zero errors, with both files absent from the project.
  </acceptance_criteria>
  <done>Both superseded spike files are deleted from disk and from project.pbxproj; no remaining source reference to their symbols exists; Release build succeeds clean without them.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the stale menuBarOverflow Settings card/toggle</name>
  <files>Islet/SettingsView.swift</files>
  <read_first>
    Islet/SettingsView.swift lines 60-95 (the `@AppStorage` block containing `menuBarOverflowEnabled` at line 74, alongside its sibling v1.10 toggles for context on the exact declaration shape to remove)
    Islet/SettingsView.swift lines 225-253 (the `ActivityCardData` array containing the `id: "menuBarOverflow"` entry at lines 243-247, alongside its siblings for context)
    Islet/ActivityCard.swift (the `ActivityCardData` struct + `isComingSoon` field this entry currently sets `true` — confirms the entry is a disabled placeholder card, not live functionality, safe to remove outright)
  </read_first>
  <action>
    In `Islet/SettingsView.swift`, remove line 74: `@AppStorage(ActivitySettings.menuBarOverflowKey) private var menuBarOverflowEnabled = false`. Remove the entire `ActivityCardData(id: "menuBarOverflow", ...)` entry (lines 243-247, the block spanning `title: "Menu Bar Overflow"` through `isComingSoon: true),`) from the Activities card array — per D-02/D-04, this feature has zero Settings surface (no toggle, no permission card, no "coming soon" placeholder either, since it now ships as a real always-on mechanism rather than a future placeholder). Leave every sibling `ActivityCardData` entry (`downloadProgress`, `update`, etc.) and every sibling `@AppStorage` declaration completely untouched — this is a surgical removal of exactly the menuBarOverflow lines, not a reformat of the surrounding array or property block.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -configuration Release -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    Source assertion: `grep -c "menuBarOverflow" Islet/SettingsView.swift` == 0 (no @AppStorage binding, no ActivityCardData entry, no residual reference).
    Source assertion: `grep -c "ActivityCardData(id:" Islet/SettingsView.swift` — count is exactly 1 less than it was before this task (one card removed, none added), confirmed by comparing against the pre-edit count of sibling entries (downloadProgress, update, osd, calendarCountdown, capsLock remain).
    Test command: `xcodebuild build -scheme Islet -configuration Release -destination 'platform=macOS'` — BUILD SUCCEEDED, zero errors (confirms no dangling reference to the removed `menuBarOverflowEnabled` binding elsewhere in the file).
  </acceptance_criteria>
  <done>SettingsView.swift contains zero references to menuBarOverflow (binding or card); Release build succeeds clean; every sibling Activities card/toggle is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan only deletes dead spike code and a disabled placeholder UI card — no new trust boundary, no new data flow, no new attack surface introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-04 | Tampering (residual) | Deleted `MenuBarOverflowBridging.swift`'s private-CGS/synthetic-CGEvent surface | mitigate | Full deletion removes the entire superseded private-API/synthetic-event attack surface from the codebase rather than leaving unreferenced-but-present fragile code that a future change could accidentally re-wire |
</threat_model>

<verification>
- `test -f Islet/Notch/MenuBarOverflowBridging.swift` and `test -f IsletTests/MenuBarOverflowManualSpike.swift` both fail (files gone).
- `grep -rc "menuBarOverflow" Islet/SettingsView.swift` == 0.
- `xcodebuild build -scheme Islet -configuration Release -destination 'platform=macOS'` succeeds clean after both tasks.
</verification>

<success_criteria>
Both superseded Ice-mechanism spike files are gone from disk and from the Xcode project. The Settings app shows no Menübar-Overflow card or toggle of any kind — consistent with D-02/D-04's "no Settings surface for this feature" decision.
</success_criteria>

<output>
Create `.planning/phases/66-men-bar-overflow-ice-style-mvp/66-03-SUMMARY.md` when done
</output>
