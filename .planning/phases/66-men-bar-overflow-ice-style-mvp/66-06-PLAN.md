---
phase: 66-men-bar-overflow-ice-style-mvp
plan: 06
type: execute
wave: 4
depends_on: ["66-04", "66-05"]
files_modified: []
autonomous: false
requirements: [MENUBAR-01, MENUBAR-02, MENUBAR-03, MENUBAR-04]

must_haves:
  truths:
    - "A chevron appears leftmost among Islet's own menu-bar items, visually separating a visible from a hidden section, once Accessibility is granted"
    - "Cmd-dragging a real third-party menu-bar icon across the chevron moves it into the hidden section and the assignment survives a relaunch/Dock-relaunch"
    - "Clicking the chevron reveals/hides the hidden section inline, with a human-confirmed verdict on whether space is genuinely reclaimed or only occluded"
    - "Denying Accessibility permission produces no chevron at all and Settings shows the accurate Permission required card with a working deep link"
  artifacts: []
  key_links: []
---

<objective>
Close out Phase 66 with a build-gate sweep and the full on-device UAT the ROADMAP's own Success
Criteria #2-#5 require — the end-to-end integration of everything Plans 66-01 through 66-05 built
in isolation, now exercised together as a real feature a user can actually see and use.

Purpose: Every prior plan verified its own file in isolation (build success, grep assertions, unit
tests where headless-safe). None of them could exercise the real chevron + real Cmd-drag + real
reveal/hide + real permission-denied UI together, because the pieces didn't all exist yet. This
plan is that final integration proof.
Output: A phase-closing SUMMARY recording the on-device verdict for SC#2 through SC#5 (SC#1 was
already closed by Plan 66-01's checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-men-bar-overflow-ice-style-mvp/66-CONTEXT.md
@.planning/phases/66-men-bar-overflow-ice-style-mvp/66-VALIDATION.md
@.planning/phases/66-men-bar-overflow-ice-style-mvp/66-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build gate + dead-reference sweep</name>
  <files></files>
  <read_first>
    Islet/AppDelegate.swift
    Islet/SettingsView.swift
    Islet/ActivitySettings.swift
  </read_first>
  <action>
    Run `xcodegen generate && xcodebuild build -scheme Islet -configuration Release -destination
    'platform=macOS'` and confirm zero errors/warnings introduced by this phase's 5 prior plans.
    Grep-confirm `MenuBarOverflowController()` is instantiated exactly once across the whole
    `Islet/` source tree (in `AppDelegate.swift` only — no second, accidental instantiation
    anywhere else). Grep-confirm zero remaining references to `menuBarOverflowKey` or
    `menuBarOverflowEnabled` anywhere in `Islet/`/`IsletTests/` (Plan 66-05's removal should be
    complete). Run the `MenuBarOverflowStoreTests` and `ActivitySettingsTests` suites to confirm
    both headless-safe suites are still green after all wiring changes.
  </action>
  <acceptance_criteria>
    - `xcodegen generate && xcodebuild build -scheme Islet -configuration Release -destination 'platform=macOS'` succeeds with zero errors
    - `grep -rc "MenuBarOverflowController()" Islet/ | grep -v ":0" | wc -l` reports exactly 1 file (AppDelegate.swift)
    - `grep -rc "menuBarOverflowKey\|menuBarOverflowEnabled" Islet/ IsletTests/ | grep -v ":0"` returns empty
    - `MenuBarOverflowStoreTests` and `ActivitySettingsTests` both pass
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -configuration Release -destination 'platform=macOS' 2>&1 | tail -30 && xcodebuild test -project Islet.xcodeproj -scheme Islet -only-testing:IsletTests/MenuBarOverflowStoreTests -only-testing:IsletTests/ActivitySettingsTests 2>&1 | tail -40</automated>
  </verify>
  <done>Full Release build is clean; MenuBarOverflowController is instantiated exactly once (AppDelegate.swift); no dead menuBarOverflowKey/menuBarOverflowEnabled references remain; both headless test suites pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The complete, wired Menübar-Overflow feature: a permission-gated chevron in the menu bar
    (D-01/D-04), Cmd-drag-to-hide detection with cross-relaunch persistence (D-03), inline
    click-to-reveal/hide (D-05), and a live Settings permission-status card.
  </what-built>
  <how-to-verify>
    1. Confirm Accessibility is currently GRANTED for Islet (System Settings -> Privacy &
       Security -> Accessibility). Launch/relaunch Islet. Confirm a chevron icon appears in the
       menu bar, positioned leftmost among Islet's own items (SC#2).
    2. Open Settings -> Permissions. Confirm the new card shows "Menu Bar Overflow" / green
       "Active — drag menu bar icons across the chevron to hide them." — not "Permission
       required".
    3. Cmd-drag a real other app's menu-bar icon leftward, past the chevron. Confirm it visually
       moves into the hidden section (SC#3).
    4. Click the chevron. Confirm the previously-hidden icon appears inline in the menu bar
       (glyph flips to `chevron.right`). Click again — confirm it disappears again (glyph flips
       back to `chevron.left`). Report whether the vacated position, when hidden, is genuinely
       reclaimed (an adjacent icon shifts to fill it) or only occluded (SC#4 — cross-check against
       Plan 66-01's spike verdict; they should agree).
    5. Quit and relaunch the app whose icon you hid. Confirm Islet automatically re-hides its icon
       without you re-dragging it (D-03/Pitfall 1).
    6. Open System Settings -> Privacy & Security -> Accessibility and revoke Islet's permission.
       Quit and relaunch Islet. Confirm NO chevron appears anywhere in the menu bar (SC#5/D-04),
       and Settings -> Permissions shows "Permission required" (red) with a working "Open System
       Settings" button that lands on the Accessibility pane.
    7. Re-grant Accessibility while Islet is still running (do not relaunch). Within ~5 seconds,
       confirm the chevron appears automatically (D-02's "activates automatically... no relaunch"
       health-check contract) and the Settings card flips to "Active" on next Settings visit.
  </how-to-verify>
  <resume-signal>
    Report PASS (all 7 steps confirmed) or describe which step failed/diverged — a divergence at
    step 4 (occlusion-only, contradicting Plan 66-01's RECLAIMED finding) or step 6 (chevron still
    appears while denied) blocks phase closeout and needs a gap-closure plan before
    `/gsd:verify-work` runs.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| End-to-end integrated feature surface (chevron, drag, reveal/hide, permission card) | Aggregate of all trust boundaries introduced in Plans 66-01 through 66-05 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-14 | (aggregate) All of T-66-01 through T-66-13 | Whole feature | mitigate | Confirmed via this plan's on-device checklist — each prior plan's individual mitigation is re-verified in the fully integrated, real-hardware context, not just in isolation |
</threat_model>

<verification>
Build gate passes; both headless test suites pass; the 7-step on-device checklist above is confirmed PASS by the user.
</verification>

<success_criteria>
- SC#2 through SC#5 (ROADMAP.md) are each explicitly confirmed on real hardware
- The RECLAIMED-vs-OCCLUDED finding from Plan 66-01 is cross-checked and still holds with the real chevron
- Permission-denied degradation shows zero chevron and an accurate Settings card
- Mid-session grant activates the feature within the health-check's ~5s window, no relaunch required
</success_criteria>

<output>
Create `.planning/phases/66-men-bar-overflow-ice-style-mvp/66-06-SUMMARY.md` when done — this closes Phase 66.
</output>
</threat_model>
