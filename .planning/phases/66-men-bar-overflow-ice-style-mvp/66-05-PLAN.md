---
phase: 66-men-bar-overflow-ice-style-mvp
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Notch/MenuBarOverflowBridging.swift
  - IsletTests/MenuBarOverflowManualSpike.swift
  - Islet/AppDelegate.swift
  - Islet.xcodeproj/project.pbxproj
autonomous: false
requirements: [MENUBAR-04]

must_haves:
  truths:
    - "Real Ice.app is a genuinely launchable binary on this machine (not a dangling Caskroom symlink), so it can serve as a live comparison reference for the rest of this phase"
    - "MenuBarOverflowBridging.menuBarItemWindows() never runs any CGS enumeration call while AXIsProcessTrusted() is false — the gate is a guard, not a diagnostic print"
    - "The 66-01 NO-GO's root cause (missing permission gate vs. Cmd-U launch-context artifact vs. genuine mechanism bug) is confirmed on-device with concrete evidence, before any production Controller rewiring begins"
  artifacts:
    - path: "Islet/Notch/MenuBarOverflowBridging.swift"
      provides: "Restored private-CGS window-enumeration/frame-read/synthetic-drag shim, now hard-gated on AXIsProcessTrusted()"
      contains: "guard AXIsProcessTrusted() else { return [] }"
    - path: "IsletTests/MenuBarOverflowManualSpike.swift"
      provides: "Restored Cmd-U-only manual spike, upgraded to print the gate's pass/block state explicitly"
      contains: "gate PASSES"
    - path: "Islet/AppDelegate.swift"
      provides: "A DEBUG-only debug-menu action that runs the exact same enumeration from a genuine open-launched instance, to rule out the Cmd-U test-host-launch hypothesis independently of the permission hypothesis"
      contains: "Spike: Print Menu Bar Overflow Enumeration"
  key_links:
    - from: "IsletTests/MenuBarOverflowManualSpike.swift"
      to: "Islet/Notch/MenuBarOverflowBridging.swift"
      via: "MenuBarOverflowBridging.menuBarItemWindows() call"
      pattern: "MenuBarOverflowBridging\\.menuBarItemWindows"
    - from: "Islet/AppDelegate.swift debug menu action"
      to: "Islet/Notch/MenuBarOverflowBridging.swift"
      via: "same menuBarItemWindows() call, from a real launch instead of Cmd-U"
      pattern: "debugSpikePrintMenuBarOverflowEnumeration"
---

<objective>
Restore Plan 66-01's deleted private-CGS spike (`MenuBarOverflowBridging.swift` /
`MenuBarOverflowManualSpike.swift`) from git history, add the one concrete fix RESEARCH.md
identified as the leading, cheapest-to-test cause of the original NO-GO — an actual
`AXIsProcessTrusted()` gate instead of a diagnostic print — and run RESEARCH.md's Pattern 1
cheapest-first elimination on-device against a freshly reinstalled, genuinely-working real
Ice.app. This is Wave 0: per CONTEXT.md's D-07 and this phase's own two-NO-GO precedent, no
production Controller/Store code (Plans 66-06/66-07) may be written until this plan's
on-device checkpoint confirms enumeration itself now works.

Purpose: Prove — or disprove — that the 66-01 NO-GO was a missing permission gate / launch-context
artifact rather than a genuinely broken mechanism, before investing further implementation effort.
Output: A restored, hard-gated `MenuBarOverflowBridging.swift`, a real launchable Ice.app on this
machine, and an on-device verdict (GO/NO-GO) on whether CGS enumeration now succeeds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-men-bar-overflow-ice-style-mvp/66-CONTEXT.md
@.planning/phases/66-men-bar-overflow-ice-style-mvp/66-RESEARCH.md
@.planning/phases/66-men-bar-overflow-ice-style-mvp/66-PATTERNS.md
@.planning/phases/66-men-bar-overflow-ice-style-mvp/66-01-SUMMARY.md

<interfaces>
<!-- Exact restored source, git commit adfbd70 (`git show adfbd70:<path>`). Executor restores
verbatim then applies ONLY the gate edit described in Task 1 — do not redesign any signature. -->

From `git show adfbd70:Islet/Notch/MenuBarOverflowBridging.swift` (208 lines, restore as-is):
```swift
enum MenuBarOverflowBridging {
    struct MenuBarItemWindow {
        let windowID: CGWindowID
        let ownerPID: pid_t
        let bundleIdentifier: String
        let frame: CGRect
    }
    static func menuBarItemWindows() -> [MenuBarItemWindow] { /* CGSGetWindowCount -> CGSGetProcessMenuBarWindowList -> per-window owner/bundle/frame resolution */ }
    static func currentFrame(windowID: CGWindowID) -> CGRect?
    @discardableResult
    static func moveMenuBarItem(windowID: CGWindowID, toX destinationX: CGFloat, maxAttempts: Int = 5) -> Bool
}
```

From `git show adfbd70:IsletTests/MenuBarOverflowManualSpike.swift` (69 lines, restore as-is,
then apply Task 1's print-to-branch upgrade to the `AXIsProcessTrusted()` line only):
```swift
final class MenuBarOverflowManualSpike: XCTestCase {
    @MainActor
    func testManualMechanism() {
        print("[MenuBarOverflowSpike] AXIsProcessTrusted() = \(AXIsProcessTrusted())")   // <- upgrade this line only
        let windows = MenuBarOverflowBridging.menuBarItemWindows()
        // ... unchanged below
    }
}
```

Existing codebase Accessibility-gate pattern to clone the SHAPE of (not the file):
```swift
// Islet/Notch/OSDInterceptor.swift:46
static var isAccessibilityTrusted: Bool { AXIsProcessTrusted() }
```

Existing debug-menu construction pattern (`Islet/AppDelegate.swift:504-535`, `setupDebugMenu()`)
— clone this shape for the new debug-menu action, `debugMenu.addItem(withTitle:action:keyEquivalent:)`
+ `@objc private func debug...()`, same as the 11 existing entries.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reinstall Ice, restore the 66-01 spike, add the Accessibility gate, add a real-launch debug hook</name>
  <files>Islet/Notch/MenuBarOverflowBridging.swift, IsletTests/MenuBarOverflowManualSpike.swift, Islet/AppDelegate.swift, Islet.xcodeproj/project.pbxproj</files>
  <read_first>
    Islet/Notch/OSDInterceptor.swift (isAccessibilityTrusted pattern, line 46)
    Islet/AppDelegate.swift (current setupDebugMenu() at lines 504-535, and the 5 existing @objc debugSpike* methods immediately below it, for the exact method-declaration convention to match)
    .planning/phases/66-men-bar-overflow-ice-style-mvp/66-RESEARCH.md (Pattern 1, Pitfall 1, Pitfall 2, Pitfall 4)
  </read_first>
  <action>
    1. Run `brew reinstall --cask jordanbaird-ice` (RESEARCH.md Pitfall 4 — the current Caskroom entry is a dangling symlink to a deleted `/Applications/Ice.app`; this is a hard Wave-0 blocker, not optional). Confirm afterward with `test -d /Applications/Ice.app && echo PRESENT`.
    2. Restore both deleted files verbatim: `git show adfbd70:Islet/Notch/MenuBarOverflowBridging.swift > Islet/Notch/MenuBarOverflowBridging.swift` and `git show adfbd70:IsletTests/MenuBarOverflowManualSpike.swift > IsletTests/MenuBarOverflowManualSpike.swift`.
    3. In the restored `MenuBarOverflowBridging.swift`, edit `static func menuBarItemWindows() -> [MenuBarItemWindow]` to add `guard AXIsProcessTrusted() else { return [] }` as the literal first line of the function body, before the existing `var totalCount: Int32 = 0` line. This is the single concrete fix RESEARCH.md Pitfall 1 identifies — the restored version only ever printed this value, never branched on it.
    4. In the restored `MenuBarOverflowManualSpike.swift`, replace the line `print("[MenuBarOverflowSpike] AXIsProcessTrusted() = \(AXIsProcessTrusted())")` with a `let trusted = AXIsProcessTrusted()` binding followed by `print("[MenuBarOverflowSpike] AXIsProcessTrusted() = \(trusted) — \(trusted ? "gate PASSES, enumeration will run" : "gate BLOCKS, expect 0 windows below")")`. Leave every other line in the file unchanged.
    5. In `Islet/AppDelegate.swift`'s `#if DEBUG setupDebugMenu()` method (lines 504-535), add one new `debugMenu.addItem(withTitle: "Spike: Print Menu Bar Overflow Enumeration", action: #selector(debugSpikePrintMenuBarOverflowEnumeration), keyEquivalent: "")` alongside the 11 existing entries, and add a new `@objc private func debugSpikePrintMenuBarOverflowEnumeration()` method (grouped with the other `@objc private func debugSpike*()` methods) whose body calls `MenuBarOverflowBridging.menuBarItemWindows()` and prints the count plus each result's `bundleIdentifier`/`windowID` — this exists so the identical enumeration code can be exercised from a genuine `open`-launched instance, independent of the Cmd-U test-host launch path (RESEARCH.md Pitfall 2).
    6. Run `xcodegen generate` to re-register both restored files in `project.pbxproj` (both were removed from it by Plan 66-03's cleanup).
    7. Build: `xcodebuild build -scheme Islet -configuration Release -destination 'platform=macOS'` — must succeed with zero errors.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -configuration Release -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `test -d /Applications/Ice.app` exits 0 (real bundle present, not a dangling symlink)
    - `grep -A2 "static func menuBarItemWindows" Islet/Notch/MenuBarOverflowBridging.swift | grep -c "guard AXIsProcessTrusted"` equals 1
    - `grep -c "gate PASSES" IsletTests/MenuBarOverflowManualSpike.swift` equals 1
    - `grep -c "Spike: Print Menu Bar Overflow Enumeration" Islet/AppDelegate.swift` equals 1
    - `grep -c "debugSpikePrintMenuBarOverflowEnumeration" Islet/AppDelegate.swift` is at least 2 (the addItem call site + the @objc method definition)
    - `xcodebuild build -scheme Islet -configuration Release -destination 'platform=macOS'` prints `BUILD SUCCEEDED`
  </acceptance_criteria>
  <done>Ice.app is genuinely reinstalled and present, the restored shim never runs a CGS call while untrusted, the manual spike's console output makes the gate state unmissable, a real-launch debug hook exists for Pattern 1 Step 3, and the Release build is clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device Pattern 1 elimination checkpoint</name>
  <what-built>
    A restored private-CGS menu-bar-item enumeration shim, now hard-gated on Accessibility trust
    (was previously print-only); a genuinely reinstalled real Ice.app; a DEBUG-only menu action
    that runs the identical enumeration code from a real app launch instead of Cmd-U.
  </what-built>
  <how-to-verify>
    Work through RESEARCH.md's Pattern 1 cheapest-first elimination order, in this exact sequence:

    1. **Re-validate the live reference.** `open -a Ice`. Confirm real Ice.app still genuinely
       hides/shows a real third-party menu-bar icon today (it was found reinstalled from a
       dangling symlink this session, so re-confirm the "it works here" premise directly, not
       from memory of an earlier session).
    2. **Test the permission-gate hypothesis.** Run `tccutil reset Accessibility com.lippi304.islet`
       to force a clean untrusted state. In Xcode, run `MenuBarOverflowManualSpike.testManualMechanism`
       via Cmd-U (this single test only). Confirm the console prints "gate BLOCKS, expect 0 windows
       below" and the enumeration below it is empty.
    3. Grant Accessibility to whichever process System Settings prompts for (Islet or the Xcode
       test runner) in System Settings > Privacy & Security > Accessibility. Re-run the same Cmd-U
       test. Confirm the console now prints "gate PASSES" and lists one or more real other-process
       menu-bar-item windows (bundle IDs + frames), including something you can visually confirm is
       a real icon currently in your menu bar.
    4. **Test the launch-context hypothesis, independent of Step 3.** Build and launch Islet.app
       directly (not via Xcode Cmd-U — a normal `open` launch, or run the built Release product).
       With Accessibility now trusted, click the 🐞 debug menu's new "Spike: Print Menu Bar Overflow
       Enumeration" item. Confirm the printed result matches Step 3's Cmd-U result (same non-empty
       set of real menu-bar windows) — this rules out (or confirms) whether the Cmd-U test-host
       launch path itself was ever part of the problem.
    5. Report which of Steps 2-4 explains the original 66-01 NO-GO, or state plainly if enumeration
       still fails under both a trusted Cmd-U run and a trusted real launch (in which case this is a
       NO-GO and the finding must go back to `/gsd:discuss-phase 66` — do not attempt further fixes
       here per this project's own NO-GO precedent from Plans 66-01/66-04).
  </how-to-verify>
  <resume-signal>
    Reply "GO" plus which step explained it (enables Plans 66-06/66-07 to proceed), or "NO-GO" plus
    the console output from Steps 2-4 (halts the phase, returns to /gsd:discuss-phase 66).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Islet process ↔ macOS WindowServer/CGS | Private, undocumented CGS symbols cross into out-of-process window-server state; the only trust gate available is the OS's own Accessibility (TCC) grant |
| Islet process ↔ other apps' menu-bar-item windows | Synthetic CGEvent drag targets a specific third-party process's window by windowID/pid |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | Elevation of Privilege / Information Disclosure | `MenuBarOverflowBridging.menuBarItemWindows()` | mitigate | `guard AXIsProcessTrusted() else { return [] }` as the literal first line — no CGS call ever runs while untrusted (Task 1, RESEARCH.md Pitfall 1) |
| T-66-02 | Denial of Service (self-inflicted) | Manual spike / debug-menu diagnostics | accept | These are DEBUG-only, developer-facing diagnostics, never shipped to Release users — no production-facing silent failure risk from this plan alone (that gate is Plan 66-07's Settings-row/popover work) |
| T-66-SC | Tampering (supply chain) | `brew reinstall --cask jordanbaird-ice` | accept | Ice is an already-installed, previously-audited (per RESEARCH.md's cask receipt check) external reference app used only for live comparison — never linked into Islet's own build or bundled as a dependency |
</threat_model>

<verification>
- `xcodebuild build -scheme Islet -configuration Release -destination 'platform=macOS'` succeeds after Task 1.
- Task 2's on-device checkpoint returns an explicit GO or NO-GO verdict with supporting console evidence before Plan 66-06 may begin.
</verification>

<success_criteria>
- Real Ice.app is present and confirmed working on this machine.
- `MenuBarOverflowBridging.menuBarItemWindows()` never executes a CGS call while `AXIsProcessTrusted()` is false.
- The on-device checkpoint produces a concrete, evidence-backed GO/NO-GO verdict distinguishing the permission-gate hypothesis from the launch-context hypothesis.
</success_criteria>

<output>
Create `.planning/phases/66-men-bar-overflow-ice-style-mvp/66-05-SUMMARY.md` when done
</output>
