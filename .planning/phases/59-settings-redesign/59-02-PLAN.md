---
phase: 59-settings-redesign
plan: 02
type: execute
wave: 2
depends_on: ["59-01"]
files_modified:
  - Islet/SettingsView.swift
  - IsletTests/SettingsViewTests.swift
autonomous: false
requirements: [SETTINGS-04, SETTINGS-05]

must_haves:
  truths:
    - "Opening Settings > Activities shows a categorized 2-column grid (System-HUDs / Medien / Produktivität) of 15 ActivityCards instead of the old flat toggle list (SETTINGS-04)"
    - "Each card's toggle enables/disables its activity exactly like the old inline Toggle did — verified on-device (SC2)"
    - "The 8 new v1.10 activity cards show a 'Neu' badge and read OFF on a fresh install (SC3)"
    - "A pre-v1.10 user's persisted toggle state for the 7 existing activities survives the upgrade unchanged — verified against a pre-seeded UserDefaults domain, not just a fresh install (SC4)"
    - "Focus Mode and Volume & Brightness cards' options-chevron opens the exact same existing permission-explanation popover, relocated not redesigned (D-08)"
    - "'Launch Islet at login' and 'Automatically Check for Updates' remain plain toggles, not cards (D-05)"
  artifacts:
    - path: "Islet/SettingsView.swift"
      provides: "activitiesSection rebuilt as categorized LazyVGrid, 8 new @AppStorage properties"
      contains: "LazyVGrid"
    - path: "IsletTests/SettingsViewTests.swift"
      provides: "card array count/order/isNew assertions"
      contains: "systemHUDCards"
  key_links:
    - from: "Islet/SettingsView.swift activitiesSection"
      to: "Islet/ActivityCard.swift"
      via: "ActivityCard(data:) inside ForEach(cards)"
      pattern: "ActivityCard\\(data:"
    - from: "Islet/SettingsView.swift onOptionsTap closures"
      to: "showFocusPermissionExplanation / showOSDPermissionExplanation"
      via: "existing @State bool + .popover(isPresented:)"
      pattern: "onOptionsTap: \\{"
---

<objective>
Wire the 15-card categorized grid into `SettingsView.activitiesSection`, replacing the flat `Form`/`Section("Activities")` toggle list, using the `ActivityCard`/`ActivityCardData` contract Plan 59-01 built. Add the 8 new default-OFF `@AppStorage` properties, relocate Focus/OSD's existing permission popovers behind the card's options-chevron, and close the phase with an on-device UAT checkpoint covering SC2-SC5.

Purpose: Delivers the user-visible SETTINGS-04/05 outcome — this is where the phase goal ("Droppy-style Live-Activity card grid replaces ad-hoc Activities settings; new-activity default-OFF convention locked in") actually becomes true.
Output: Rebuilt `activitiesSection`, card-array unit tests, on-device UAT sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-settings-redesign/59-CONTEXT.md
@.planning/phases/59-settings-redesign/59-UI-SPEC.md
@.planning/phases/59-settings-redesign/59-RESEARCH.md
@.planning/phases/59-settings-redesign/59-01-SUMMARY.md
</context>

<interfaces>
<!-- ActivityCardData/ActivityCard contract from Plan 59-01 — use directly, no exploration needed. -->

From Islet/ActivityCard.swift (Plan 59-01):
```
struct ActivityCardData: Identifiable {
    let id: String
    let title: String
    let description: String
    let icon: String          // SF Symbol name
    let iconColor: Color
    let isOn: Binding<Bool>
    let isNew: Bool
    let onOptionsTap: (() -> Void)?
}

struct ActivityCard: View {
    let data: ActivityCardData
    var body: some View { ... }
}
```

From Islet/ActivitySettings.swift (Plan 59-01, 8 new keys, all still un-defaulted — default `= false` is set where this plan declares the `@AppStorage` properties):
`capsLockKey`, `downloadProgressKey`, `menuBarOverflowKey`, `timerKey`, `meetingHUDKey`, `quickNotesKey`, `quickActionsKey`, `codingProgressKey`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 8 new @AppStorage properties + build the 3 categorized card arrays</name>
  <files>Islet/SettingsView.swift, IsletTests/SettingsViewTests.swift</files>
  <read_first>
    Islet/SettingsView.swift lines 1-56 (existing `@AppStorage` property declaration block/style — chargingEnabled, nowPlayingEnabled, focusEnabled, osdSuppressionEnabled, etc.)
    Islet/SettingsView.swift lines 90-137 (`SidebarSection` — precedent for bumping a private computed structure to `internal` for `@testable import` access)
    Islet/ActivitySettings.swift (accent(for:) function + palette, chargingAccentIndex/nowPlayingAccentIndex/deviceAccentIndex property names at SettingsView.swift lines 85-87)
    IsletTests/SettingsViewTests.swift (full file — existing `XCTestCase` style testing `SettingsView.SidebarSection`)
    .planning/phases/59-settings-redesign/59-UI-SPEC.md (Grid Structure → Category Assignment table, Copywriting Contract table — exact titles/icons/descriptions/category per card)
  </read_first>
  <behavior>
    - Test: `SettingsView().systemHUDCards.count == 8`
    - Test: `SettingsView().mediaCards.count == 2`
    - Test: `SettingsView().productivityCards.count == 5`
    - Test: `systemHUDCards` first 5 entries have `isNew == false` (existing-first, D-03) and the last 3 have `isNew == true`
    - Test: `productivityCards` all 5 entries have `isNew == true` (D-07 — Produktivität is 100% new this phase)
    - Test: `mediaCards` both entries have `isNew == false`
  </behavior>
  <action>
    In `Islet/SettingsView.swift`, add 8 new `@AppStorage` properties alongside the existing block (after `autoUpdateCheckEnabled` at line 56), each reading `ActivitySettings.<key>` and defaulting `= false` with NO exceptions (Pitfall 1 — do not copy `chargingEnabled`'s `= true` pattern): `capsLockEnabled`, `downloadProgressEnabled`, `menuBarOverflowEnabled`, `timerEnabled`, `meetingHUDEnabled`, `quickNotesEnabled`, `quickActionsEnabled`, `codingProgressEnabled`.

    Add three `internal` (not `private`) computed properties — `systemHUDCards`, `mediaCards`, `productivityCards` — each returning `[ActivityCardData]`, bumped to `internal` mirroring `SidebarSection`'s existing private→internal testability-bump precedent (comment at lines 90-95) so `IsletTests` can assert on them via `@testable import Islet`. Build each array from 59-UI-SPEC.md's locked Copywriting Contract table, existing-first then new (D-03):

    `systemHUDCards` (8, existing 5 then new 3): Charging (`bolt.fill`, iconColor `ActivitySettings.accent(for: chargingAccentIndex)`, isOn `$chargingEnabled`, isNew false, onOptionsTap nil, description "Shows a brief charging animation when you plug in."), Devices (`antenna.radiowaves.left.and.right`, iconColor `.secondary`, isOn `$deviceEnabled`, isNew false, onOptionsTap nil, description "Flashes a connect/disconnect HUD for Bluetooth accessories like AirPods."), Focus Mode (`moon.fill`, iconColor `.secondary`, isOn `$focusEnabled`, isNew false, onOptionsTap `{ showFocusPermissionExplanation = true }`, description "Shows a moon icon in the pill while a system Focus is active."), Volume & Brightness (`speaker.wave.3.fill`, iconColor `.secondary`, isOn `$osdSuppressionEnabled`, isNew false, onOptionsTap `{ showOSDPermissionExplanation = true }`, description "Replaces macOS's own volume/brightness overlay with Islet's."), Calendar Countdown (`calendar`, iconColor `.secondary`, isOn `$calendarCountdownEnabled`, isNew false, onOptionsTap nil, description "Counts down to your next calendar event right in the notch."), Caps Lock (`capslock.fill`, iconColor `.secondary`, isOn `$capsLockEnabled`, isNew true, onOptionsTap nil, description "Flashes an on/off indicator whenever Caps Lock is toggled."), Download Progress (`arrow.down.circle.fill`, iconColor `.secondary`, isOn `$downloadProgressEnabled`, isNew true, onOptionsTap nil, description "Shows a live indicator while a file downloads to your Mac."), Menu Bar Overflow (`menubar.rectangle`, iconColor `.secondary`, isOn `$menuBarOverflowEnabled`, isNew true, onOptionsTap nil, description "Hides overflow menu-bar icons behind a chevron, Ice-style.").

    `mediaCards` (2, both existing): Now Playing (`play.fill`, iconColor `ActivitySettings.accent(for: nowPlayingAccentIndex)`, isOn `$nowPlayingEnabled`, isNew false, onOptionsTap nil, description "Shows live playback with transport controls for the current track."), Song-Change Toast (`text.bubble.fill`, iconColor `.secondary`, isOn `$songChangeToastEnabled`, isNew false, onOptionsTap nil, description "Flashes the new track's title for a moment whenever the song changes.").

    `productivityCards` (5, all new): Timer (`timer`, iconColor `.secondary`, isOn `$timerEnabled`, isNew true, onOptionsTap nil, description "Start a countdown or Pomodoro timer that lives in the notch."), Meeting HUD (`video.fill`, iconColor `.secondary`, isOn `$meetingHUDEnabled`, isNew true, onOptionsTap nil, description "Shows a call timer and mic-mute control during Zoom/Teams calls."), Quick Notes (`note.text`, iconColor `.secondary`, isOn `$quickNotesEnabled`, isNew true, onOptionsTap nil, description "Capture a quick text note straight into your Obsidian vault."), Quick Actions (`bolt.horizontal.fill`, iconColor `.secondary`, isOn `$quickActionsEnabled`, isNew true, onOptionsTap nil, description "A row of one-tap system actions — mute mic, lock screen, and more."), Coding Progress (`checklist`, iconColor `.secondary`, isOn `$codingProgressEnabled`, isNew true, onOptionsTap nil, description "Shows your Claude Code session's live todo progress.").

    Each `ActivityCardData.id` should be a short lowercase-camelCase string unique within its array (e.g. `"charging"`, `"capsLock"`) — not user-visible, only used for `Identifiable`/`ForEach`.

    In `IsletTests/SettingsViewTests.swift`, add the 6 behavior-block assertions above in a new `// MARK: Phase 59 / SETTINGS-04` section, instantiating `SettingsView()` directly (mirrors this file's existing bare-instantiation-free style — `SettingsView()` has an implicit zero-arg init since every stored property carries a default).
  </action>
  <acceptance_criteria>
    - `grep -cE '@AppStorage\(ActivitySettings\.(capsLock|downloadProgress|menuBarOverflow|timer|meetingHUD|quickNotes|quickActions|codingProgress)Key\) private var \w+Enabled = false' Islet/SettingsView.swift` returns 8
    - `grep -c 'internal var systemHUDCards\|var systemHUDCards' Islet/SettingsView.swift` returns 1, same for `mediaCards`/`productivityCards`
    - `xcodebuild -project Islet.xcodeproj -scheme Islet build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet build</automated>
  </verify>
  <done>3 card arrays (8/2/5 = 15 total) exist as testable computed properties, all 8 new @AppStorage properties default false, tests assert counts/ordering/isNew flags.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild activitiesSection as the categorized 2-column grid</name>
  <files>Islet/SettingsView.swift</files>
  <read_first>
    Islet/SettingsView.swift lines 258-348 (current `activitiesSection` — full flat Form/Section being replaced)
    Islet/SettingsView.swift lines 570-630 (`focusPermissionExplanationView`/`osdPermissionExplanationView` — reused unmodified, only their `.popover` attachment point moves)
    Islet/SettingsView.swift lines 428-456 (`permissionsSection` — closest precedent for a custom-row list inside `ScrollView(.vertical) { Form { ... } }`, and for the "ONE ScrollView per section" convention)
    .planning/phases/59-settings-redesign/59-UI-SPEC.md (Layout Contract table — spacing/padding values, Grid Structure section)
  </read_first>
  <action>
    Replace `activitiesSection`'s body (`Islet/SettingsView.swift` lines 262-348) entirely. Keep exactly ONE `ScrollView(.vertical)` wrapping everything (Pitfall 3 / Phase 51's own overflow-fix precedent — never nest a second `ScrollView`). Structure: `ScrollView(.vertical) { VStack(alignment: .leading, spacing: 20) { ... }.padding(20) }`.

    Inside the outer `VStack`, first element: a `Form` containing exactly the 2 non-card toggles (D-05) — `Toggle("Launch Islet at login", isOn: $launchAtLogin)` with its existing unmodified `.onChange` do/catch block (moved verbatim from the current file), and `Toggle("Automatically Check for Updates", isOn: $autoUpdateCheckEnabled)` (moved verbatim, no `.onChange`/`.popover` — Phase 40's existing no-op-permission convention). Do not wrap these two in a `Section` header — they were never under one in the D-05 "separate general/non-activity" framing.

    Then, for each of the 3 categories in this exact order (D-01: System-HUDs, Medien, Produktivität), add a `categorySection(title:cards:)` call — a new private helper method taking `title: String, cards: [ActivityCardData]`, rendering `Text(title).font(.headline)` (category header, 8pt gap to the grid per Layout Contract) followed by `LazyVGrid(columns: [GridItem(.flexible(), spacing: 12), GridItem(.flexible(), spacing: 12)], spacing: 12) { ForEach(cards) { card in ActivityCard(data: card) } }`. Call it as `categorySection(title: "System-HUDs", cards: systemHUDCards)`, `categorySection(title: "Medien", cards: mediaCards)`, `categorySection(title: "Produktivität", cards: productivityCards)`.

    Delete the old `Section("Activities") { ... }` block and every inline `Toggle("Charging", ...)`-style row, the standalone `if let hint = ActivitySettings.focusPermissionStatusHint(...)`/`osdPermissionStatusHint(...)` `Text` rows (the card contract has no dedicated hint-text slot — the options-chevron itself is the re-trigger affordance the hint's `onTapGesture` used to provide; do not invent a new slot for it). The `.popover(isPresented: $showFocusPermissionExplanation) { focusPermissionExplanationView }` and `.popover(isPresented: $showOSDPermissionExplanation) { osdPermissionExplanationView }` modifiers move from the old inline `Toggle` rows onto `ActivityCard(data:)`'s call sites for the Focus Mode and Volume & Brightness cards specifically (or onto the categorySection/grid container — whichever keeps exactly 2 popover attachments, one per card, not a shared router per RESEARCH.md's Anti-Patterns).

    `focusPermissionStatusHint`/`osdPermissionStatusHint` functions in `ActivitySettings.swift` become dead code after this — do NOT delete them in this task (out of this plan's `files_modified` scope; leave for a future cleanup pass, not required to satisfy SETTINGS-04/05).
  </action>
  <acceptance_criteria>
    - `grep -c 'ScrollView(.vertical)' Islet/SettingsView.swift` inside `activitiesSection`'s body shows exactly 1 (use `sed -n '/private var activitiesSection/,/^    }$/p' Islet/SettingsView.swift | grep -c 'ScrollView'`)
    - `grep -c 'LazyVGrid' Islet/SettingsView.swift` returns 3 (one per category)
    - `grep -c 'ActivityCard(data:' Islet/SettingsView.swift` returns >= 3 (categorySection call sites reuse the same `ForEach` line)
    - `grep -c 'Section("Activities")' Islet/SettingsView.swift` returns 0 (old flat list removed)
    - `grep -c 'showFocusPermissionExplanation = true\|showOSDPermissionExplanation = true' Islet/SettingsView.swift` returns 2 (both relocated triggers still present)
    - `xcodebuild -project Islet.xcodeproj -scheme Islet build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet build</automated>
  </verify>
  <done>activitiesSection renders as a single-ScrollView categorized 2-column card grid (15 cards across 3 categories) plus a 2-toggle non-card Form block; Focus/OSD popovers relocated onto their cards' options-chevron unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device UAT — grid rendering, toggle behavior, default-OFF, upgrade-preservation, resolver table review</name>
  <files></files>
  <action>
    This task is verification-only — no code changes unless a check fails, in which case report exactly which step failed and what happened instead. Follow the 12 steps in `<how-to-verify>` below in order, using the real (not simulated) Islet.app process on-device.
  </action>
  <what-built>
    A categorized 2-column card grid (System-HUDs 8 / Medien 2 / Produktivität 5 = 15 cards) replacing the old flat Activities toggle list, with "Neu" badges on the 8 new v1.10 cards, an options-chevron on Focus Mode/Volume & Brightness reopening their existing permission popovers, a 2-toggle non-card block (Launch at login / Auto-update) at the top, and a resolver-priority reference table in IslandResolver.swift.
  </what-built>
  <how-to-verify>
    1. Build and run Islet (Debug). Open Settings > Activities.
    2. Confirm the grid renders 3 category headers in order: System-HUDs, Medien, Produktivität — each a 2-column grid, not a flat list.
    3. Confirm System-HUDs shows 5 existing cards (Charging, Devices, Focus Mode, Volume & Brightness, Calendar Countdown) BEFORE 3 "Neu"-badged cards (Caps Lock, Download Progress, Menu Bar Overflow).
    4. Confirm Medien shows Now Playing + Song-Change Toast, neither badged "Neu".
    5. Confirm Produktivität shows 5 cards (Timer, Meeting HUD, Quick Notes, Quick Actions, Coding Progress), all "Neu"-badged.
    6. Toggle Charging OFF then ON — confirm the real Charging HUD stops/resumes appearing when you plug in power (SC2, same behavior as before this phase).
    7. Tap the options-chevron on Focus Mode — confirm the SAME "Allow Focus Status Access" popover from before this redesign appears; tap Volume & Brightness's chevron — confirm the SAME "Replace System OSD" popover appears.
    8. Quit Islet, delete its UserDefaults domain (or use a fresh test user), relaunch, open Settings > Activities — confirm all 8 new cards (Caps Lock through Coding Progress) show their toggle OFF (SC3).
    9. Pre-seed a value simulating an upgrading user: `defaults write com.yourcompany.Islet activity.charging -bool NO` (adjust bundle ID if different), relaunch Islet, open Settings > Activities — confirm the Charging card's toggle reads OFF, not the code's compiled-in ON default (SC4 — proves the existing key is read through unchanged, not silently reset).
    10. Confirm "Launch Islet at login" and "Automatically Check for Updates" still appear as plain toggles at the top of the Activities pane, not as cards.
    11. Scroll the Activities pane to the bottom — confirm every category and both non-card toggles are reachable (no clipped content, Pitfall 3).
    12. Open `Islet/Notch/IslandResolver.swift`, review the new SC5 doc-comment table against `resolve()`'s actual tier logic — confirm the 4 documented tiers match reality and each of the 8 new activities has a `rank TBD` flag, not a fabricated numeric rank.
  </how-to-verify>
  <verify>
    <human-check>All 12 steps above behave as expected on-device — grid structure/ordering/badging (steps 2-5), real toggle behavior (step 6), relocated popovers (step 7), fresh-install default-OFF (step 8), pre-seeded-domain upgrade preservation (step 9), non-card toggles present (step 10), full scroll reachability (step 11), and resolver table accuracy (step 12).</human-check>
  </verify>
  <done>SC2 (toggle behavior), SC3 (fresh-install default-OFF), SC4 (pre-seeded-domain upgrade preservation), and SC5 (resolver table accuracy) all confirmed on real hardware; grid renders 15 cards across 3 correctly-ordered/badged categories.</done>
  <resume-signal>Type "approved" or describe issues (e.g. which step failed and what was observed)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | Same local, single-user, no-network settings surface as Plan 59-01 — this plan only wires UI, no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-03 | Tampering | Pre-seeded/tampered `UserDefaults` values read into card toggles | accept | Same trust level as every other `@AppStorage`-backed toggle already in this codebase (RESEARCH.md Security Domain) — a corrupted/malicious local value at worst flips a toggle, no code execution or data exposure path exists |
| T-59-04 | Denial of Service | Grid overflow beyond the fixed 600×380 Settings window | mitigate | Single shared `ScrollView(.vertical)` wraps the entire section (Task 2 acceptance criteria, Pitfall 3) — verified on-device in Task 3 step 11 |

</threat_model>

<verification>
- `xcodebuild -project Islet.xcodeproj -scheme Islet build` green after Tasks 1-2
- Manual Cmd-U full `IsletTests` suite green (existing `ActivitySettingsTests`, new card-array tests, `SidebarSection`/`IslandResolverTests` regression)
- On-device UAT (Task 3) approved — SC2/SC3/SC4/SC5 all confirmed
</verification>

<success_criteria>
- Settings > Activities renders the categorized 15-card grid, 3 categories, correct existing-then-new ordering
- All 8 new activities default OFF and carry a "Neu" badge; all 7 existing activities' toggle state is provably preserved across a simulated upgrade
- Focus Mode / Volume & Brightness options-chevron reopens the exact pre-existing popover
- Resolver-priority table exists and is confirmed accurate against real `IslandResolver.swift` code
</success_criteria>

<output>
Create `.planning/phases/59-settings-redesign/59-02-SUMMARY.md` when done
</output>
