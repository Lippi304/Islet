---
phase: 59-settings-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/ActivitySettings.swift
  - Islet/ActivityCard.swift
  - Islet/Notch/IslandResolver.swift
  - IsletTests/ActivitySettingsTests.swift
autonomous: true
requirements: [SETTINGS-04, SETTINGS-05]

must_haves:
  truths:
    - "New v1.10 activity keys exist in ActivitySettings and default OFF, with the 7 existing activity keys byte-for-byte unchanged (SETTINGS-05 foundation)"
    - "A reusable ActivityCard view + ActivityCardData contract exists that Plan 59-02 can wire 15 cards against without redesigning the component (SETTINGS-04 foundation)"
    - "A resolver-priority reference table exists in IslandResolver.swift documenting current tiers plus a reserved, rank-TBD slot for each of the 8 new v1.10 activities (SC5)"
  artifacts:
    - path: "Islet/ActivitySettings.swift"
      provides: "8 new @AppStorage key string constants for the v1.10 activities"
      contains: "static let capsLockKey"
    - path: "Islet/ActivityCard.swift"
      provides: "ActivityCardData struct + ActivityCard view component"
      exports: ["ActivityCardData", "ActivityCard"]
    - path: "Islet/Notch/IslandResolver.swift"
      provides: "SC5 resolver-priority doc-comment table"
      contains: "rank TBD"
  key_links:
    - from: "Islet/ActivityCard.swift"
      to: "Islet/SettingsView.swift (Plan 59-02)"
      via: "ActivityCardData(isOn: Binding<Bool>, onOptionsTap: (() -> Void)?)"
      pattern: "struct ActivityCardData"
---

<objective>
Build the foundation Plan 59-02 wires against: 8 new default-OFF `@AppStorage` keys for the not-yet-built v1.10 activities (SETTINGS-05), the reusable `ActivityCard` view + data contract (SETTINGS-04), and the SC5 resolver-priority reference table documenting where all v1.10 activities will eventually slot into `IslandResolver`.

Purpose: Establishes the card component contract and key namespace BEFORE any grid-wiring code is written, so Plan 59-02 implements against a fixed interface instead of co-designing it live (Interface-First Task Ordering).
Output: `Islet/ActivityCard.swift` (new), `Islet/ActivitySettings.swift` (+8 keys), `Islet/Notch/IslandResolver.swift` (+doc table), `IsletTests/ActivitySettingsTests.swift` (+tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-settings-redesign/59-CONTEXT.md
@.planning/phases/59-settings-redesign/59-UI-SPEC.md
@.planning/phases/59-settings-redesign/59-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 8 new default-OFF activity keys to ActivitySettings.swift</name>
  <files>Islet/ActivitySettings.swift, IsletTests/ActivitySettingsTests.swift</files>
  <read_first>
    Islet/ActivitySettings.swift (full file — key-declaration block lines 14-33, existing style/comment convention)
    IsletTests/ActivitySettingsTests.swift (full file — `testNewKeyNames`/`testFocusKeyName` assertion style, lines 32-37 and 77-79)
  </read_first>
  <behavior>
    - Test: `ActivitySettings.capsLockKey == "activity.capsLock"`
    - Test: `ActivitySettings.downloadProgressKey == "activity.downloadProgress"`
    - Test: `ActivitySettings.menuBarOverflowKey == "activity.menuBarOverflow"`
    - Test: `ActivitySettings.timerKey == "activity.timer"`
    - Test: `ActivitySettings.meetingHUDKey == "activity.meetingHUD"`
    - Test: `ActivitySettings.quickNotesKey == "activity.quickNotes"`
    - Test: `ActivitySettings.quickActionsKey == "activity.quickActions"`
    - Test: `ActivitySettings.codingProgressKey == "activity.codingProgress"`
    - Regression test: the 7 existing activity keys are byte-for-byte unchanged — `chargingKey == "activity.charging"`, `nowPlayingKey == "activity.nowPlaying"`, `songChangeToastKey == "activity.songChangeToast"`, `deviceKey == "activity.device"`, `calendarCountdownKey == "activity.calendarCountdown"`, `focusKey == "activity.focus"`, `osdSuppressionKey == "activity.osdSuppression"`
  </behavior>
  <action>
    In `Islet/ActivitySettings.swift`, inside the `enum ActivitySettings` key-declaration block (after the existing `autoUpdateCheckKey` at line 33), add 8 new `static let` key constants in the same flat style as the existing 7: `capsLockKey = "activity.capsLock"`, `downloadProgressKey = "activity.downloadProgress"`, `menuBarOverflowKey = "activity.menuBarOverflow"`, `timerKey = "activity.timer"`, `meetingHUDKey = "activity.meetingHUD"`, `quickNotesKey = "activity.quickNotes"`, `quickActionsKey = "activity.quickActions"`, `codingProgressKey = "activity.codingProgress"`. Add a one-line comment above the block noting these are Phase 59 / SETTINGS-05 keys for activities built in Phases 60-67, all consumed as `false`-default `@AppStorage` properties declared in `SettingsView.swift` (Plan 59-02) — do NOT add a default-value parameter or migration function here; per RESEARCH.md's Don't-Hand-Roll table, no migration code is needed for brand-new keys (absent-key ⇒ `@AppStorage`'s own compiled-in default literal is the only mechanism required). Do not touch any of the 7 existing key declarations or `migrateLegacyAccentIfNeeded`.

    In `IsletTests/ActivitySettingsTests.swift`, add a new `// MARK: Phase 59 / SETTINGS-04/05` section with one `testNewV110KeyNames()` asserting all 8 new key strings, and one `testExistingActivityKeyNamesUnchanged()` asserting the 7 existing key strings listed in the Behavior block above — this is the real regression guard for SC4 (no silent flip): since the key NAME is what a card's `Binding` reads/writes through, an unchanged key name guarantees an unchanged persisted value across the upgrade.
  </action>
  <acceptance_criteria>
    - `grep -c 'static let \(capsLock\|downloadProgress\|menuBarOverflow\|timer\|meetingHUD\|quickNotes\|quickActions\|codingProgress\)Key = "activity\.' Islet/ActivitySettings.swift` returns 8
    - `grep -n 'static let chargingKey\|static let nowPlayingKey\|static let songChangeToastKey\|static let deviceKey\|static let calendarCountdownKey\|static let focusKey\|static let osdSuppressionKey' Islet/ActivitySettings.swift` shows all 7 lines unchanged from their pre-plan values
    - `xcodebuild -project Islet.xcodeproj -scheme Islet build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet build</automated>
  </verify>
  <done>ActivitySettings.swift has 15 total `activity.*`-namespaced keys (7 unchanged + 8 new), tests assert both sets, build is green.</done>
</task>

<task type="auto">
  <name>Task 2: Create ActivityCard.swift (data contract + view component)</name>
  <files>Islet/ActivityCard.swift</files>
  <read_first>
    Islet/SettingsView.swift lines 468-490 (`permissionRow(kind:label:icon:reason:status:)` — closest analog: icon + VStack(title, secondary description) + trailing control layout shape)
    .planning/phases/59-settings-redesign/59-UI-SPEC.md (Card Component Contract, Layout Contract, Typography, Color sections)
  </read_first>
  <action>
    Create `Islet/ActivityCard.swift` (new top-level file, mirrors this codebase's flat-file convention — no `Views/` subfolder exists). Define:

    `struct ActivityCardData: Identifiable` with fields: `id: String`, `title: String`, `description: String`, `icon: String` (SF Symbol name), `iconColor: Color`, `isOn: Binding<Bool>`, `isNew: Bool`, `onOptionsTap: (() -> Void)?`. Do NOT add a `category` field or a separate `CardCategory` enum — Plan 59-02's own card arrays (`systemHUDCards`/`mediaCards`/`productivityCards`) already partition by category, so a category field on the data struct itself is unused, unrequested structure (RESEARCH.md Pattern 2's own code example passes category as a plain `title: String` param to `categorySection`, never as a data-model field).

    `struct ActivityCard: View` taking `let data: ActivityCardData`, rendering per 59-UI-SPEC.md's Card Component Contract: an `HStack` with the 20×20pt `Image(systemName: data.icon)` tinted `data.iconColor`, then a `VStack(alignment: .leading, spacing: 4)` with `Text(data.title)` (13pt, semibold — `.system(size: 13, weight: .semibold)`) and `Text(data.description)` (11pt regular — `.system(size: 11)`, `.foregroundStyle(.secondary)`), then `Spacer()`, then `Toggle("", isOn: data.isOn).labelsHidden()`, then — only when `data.onOptionsTap != nil` — a `Button` with `Image(systemName: "chevron.right")` (`.secondary` color) calling the closure, `.buttonStyle(.plain)`. Wrap the whole `HStack` in `.padding(12)` and a `RoundedRectangle(cornerRadius: 10)` background filled `Color(nsColor: .controlBackgroundColor)`. When `data.isNew`, overlay a small "Neu" badge pill (9pt semibold text, `Color.accentColor.opacity(0.85)` fill) pinned to the card's top-trailing corner via `.overlay(alignment: .topTrailing)`.

    No `@AppStorage` and no direct `UserDefaults` access in this file — the view only ever reads/writes through the `Binding<Bool>` passed in, keeping it fully decoupled from persistence (Plan 59-02 owns every `@AppStorage` declaration).
  </action>
  <acceptance_criteria>
    - `grep -c 'struct ActivityCardData' Islet/ActivityCard.swift` returns 1, with fields `id`, `title`, `description`, `icon`, `iconColor`, `isOn`, `isNew`, `onOptionsTap` all present (`grep -c` for each field name returns >= 1)
    - `grep -c 'struct ActivityCard' Islet/ActivityCard.swift` returns 1 (the View, distinct from `ActivityCardData`)
    - `grep -c '@AppStorage\|UserDefaults' Islet/ActivityCard.swift` returns 0 (no persistence coupling)
    - `grep -c 'CardCategory' Islet/ActivityCard.swift` returns 0 (no unrequested enum)
    - `xcodebuild -project Islet.xcodeproj -scheme Islet build` succeeds (file compiles, even though nothing references it yet)
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet build</automated>
  </verify>
  <done>Islet/ActivityCard.swift exists, compiles standalone, exports ActivityCardData + ActivityCard with the exact fields Plan 59-02 will construct 15 instances of.</done>
</task>

<task type="auto">
  <name>Task 3: Add SC5 resolver-priority reference table to IslandResolver.swift</name>
  <files>Islet/Notch/IslandResolver.swift</files>
  <read_first>
    Islet/Notch/IslandResolver.swift lines 1-95 (file-header comment convention, `IslandPresentation` enum lines 61-77 with its named-rank trailing comments, `ActiveTransient` enum lines 81-85)
  </read_first>
  <action>
    Add a new doc-comment block in `Islet/Notch/IslandResolver.swift`, placed immediately above the `enum IslandPresentation` declaration (before line 61), titled `// Phase 59 / SETTINGS-04/05 (SC5) — Resolver-Priority Reference Table`. Zero functional/code changes — comment only, no new `IslandPresentation` or `ActiveTransient` cases.

    Content: document the CURRENT 4 tiers exactly as implemented today — Tier 0: `.onboarding` forced-flow (never pre-empted). Tier 1: `ActiveTransient` queue, ranked `charging > device > focus (collapsed-only) > osd (collapsed-only)`. Tier 2: `isExpanded` branch, ranked `pendingDrop (quickActionPicker) > selectedView (calendarExpanded/weatherExpanded/trayExpanded) > nowPlayingExpanded > home fallback (homeLastPlayed/homeEmpty)`. Tier 3: non-expanded ambient, ranked `calendarCountdown (always wins) > launch-gated nowPlayingWings > idle`.

    Then list each of the 8 new v1.10 activities with a `// rank TBD — confirm in that activity's own phase discussion` trailing flag, using RESEARCH.md's Open Question 1 findings as the best-guess placement: Caps Lock (Phase 60) — likely `ActiveTransient` tier, same pattern as Charging. Download Progress (Phase 61) — likely ambient or short-lived transient, presence+completion signal. Timer/Pomodoro (Phase 62) — persistent transient, generalizes `ActiveTransient.isPersistent` beyond Focus. Meeting HUD (Phase 63) — persistent transient, depends on Phase 62's generalization. Quick Notes (Phase 64) — likely no `IslandPresentation` case at all (menu-bar-only UI, never touches the pill). Quick Actions bar (Phase 65) — relationship unclear, possibly an always-visible strip rather than a presentation case. Menübar-Overflow (Phase 66) — likely no `IslandPresentation` case (menu-bar-only UI). Coding Progress (Phase 67) — likely ambient tier, same shape as Calendar Countdown.

    Do not renumber or touch any existing trailing rank comment on the enum cases themselves (e.g. `// D-02 rank 1 transient`) — this new block is purely additive documentation sitting above the enum.
  </action>
  <acceptance_criteria>
    - `grep -c 'rank TBD' Islet/Notch/IslandResolver.swift` returns 8 (one per new v1.10 activity)
    - `grep -c 'Resolver-Priority Reference Table' Islet/Notch/IslandResolver.swift` returns 1
    - `git diff Islet/Notch/IslandResolver.swift | grep -c '^-'` shows zero removed non-comment lines (comment-only diff) — no line starting with `case ` or containing `enum ` is removed
    - `xcodebuild -project Islet.xcodeproj -scheme Islet build` succeeds
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet build</automated>
  </verify>
  <done>IslandResolver.swift documents its current 4-tier precedence plus a reserved, rank-TBD slot for each of the 8 new v1.10 activities, with zero functional change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | This plan adds local `UserDefaults`-backed boolean keys, a pure SwiftUI presentation component, and a doc-comment — no network call, no external input, no new permission grant. Matches RESEARCH.md's Security Domain finding that no ASVS category meaningfully applies to this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | Tampering | 8 new `activity.*` UserDefaults keys | accept | Same trust level as the 7 existing activity flags already stored unencrypted in `UserDefaults.standard` — a local attacker with write access to the sandbox container already has full app control; no new exposure introduced (RESEARCH.md Security Domain) |
| T-59-02 | Information Disclosure | `ActivityCard.swift` static illustration icons/descriptions | accept | Purely cosmetic UI strings authored by Claude (D-12), no user data rendered, no live values (D-11) |

</threat_model>

<verification>
- `xcodebuild -project Islet.xcodeproj -scheme Islet build` green after each task
- All 3 new-file/modified-file acceptance_criteria grep checks pass
- Manual Cmd-U full `IsletTests` suite run once all 3 tasks land (per 59-VALIDATION.md sampling rate — per-wave-merge gate)
</verification>

<success_criteria>
- `Islet/ActivityCard.swift` exists, exports `ActivityCardData`/`ActivityCard`, zero `@AppStorage`/`UserDefaults` coupling
- `Islet/ActivitySettings.swift` has 15 total `activity.*` keys (7 unchanged + 8 new, all new ones un-defaulted here — default lives in Plan 59-02's `@AppStorage` declarations)
- `Islet/Notch/IslandResolver.swift` has the SC5 doc table, zero functional diff
- Build green, `IsletTests` suite green
</success_criteria>

<output>
Create `.planning/phases/59-settings-redesign/59-01-SUMMARY.md` when done
</output>
