---
phase: 72.1.1
plan: 03
type: execute
wave: 3
depends_on: ["72.1.1-01", "72.1.1-02"]
files_modified: [Islet/Notch/NotchPillView.swift, Islet/Notch/LiquidGlassShader.swift]
autonomous: false
requirements: [GLASS-01, GLASS-02, GLASS-03]
must_haves:
  truths:
    - "Glass reads across the WHOLE island surface (collapsed pill, expanded island, all wings), not confined to a rim (D-01)"
    - "Text/icons/album art stay fully legible everywhere the glass applies (D-02) — if not, glass intensity was dialed back, never readability"
    - "Glass is visibly present in every island state: collapsed, mid-animation, compact expanded, full expanded (D-03)"
    - "The <macOS 26 legacy shader fallback independently satisfies full coverage + legibility, verified via a forced-branch check"
    - "The D-04 transition-continuity fix from Plan 72.1.1-01 still holds after the surface-area change (no flicker regression)"
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "liquidGlassEffectLayer's native branch passes the full base `shape` (not LiquidGlassRimRingShape) to .glassEffect(_:in:), tint driven by glassTintNudge, a new specular top-highlight driven by glassGlossNudge"
      contains: ".glassEffect(.regular.tint(Color.black.opacity(0.7"
    - path: "Islet/Notch/LiquidGlassShader.swift"
      provides: "LiquidGlassParameters.collapsed/.expanded retuned for full-coverage rim-band width and reduced edge/center opacity contrast"
      contains: "borderWidth: 0.30"
  key_links:
    - from: "liquidGlassEffectLayer native branch"
      to: "glassTintNudge / glassGlossNudge (Plan 72.1.1-02)"
      via: "CGFloat computed properties read directly in the tint/highlight expressions"
      pattern: "glassTintNudge"
    - from: "legacyLiquidGlassEffectLayer"
      to: "glassLegacyEdgeOpacityNudge / glassLegacyCenterOpacityNudge / glassLegacyBorderWidthNudge"
      via: "a locally-adjusted copy of LiquidGlassParameters built at the call site, never mutating the shared static struct"
      pattern: "glassLegacyEdgeOpacityNudge"
---

<objective>
Implement D-01 (full-surface glass coverage, superseding the rim-only scope) on top of Plan 72.1.1-01's transition-continuity fix, for both the native (macOS 26.0+) and legacy (<macOS 26, still user-facing at deployment target 15.0) rendering paths — wired to Plan 72.1.1-02's DEBUG tuner axes so every new literal is live-tunable on-device, per this project's own established convention (Phase 35 needed 5 UAT rounds; nothing here should be hardcoded as final).

RESEARCH.md Pitfall 1 requires this to build on an already-verified D-04 fix (Plan 72.1.1-01), not the other way around — scaling the glass region from a thin rim to the full island multiplies compositor cost during every spring-animated transition, so this plan's own checkpoint must explicitly re-confirm the transition fix still holds after the surface-area change.

Purpose: Deliver the actual visual target the user described ("von oben schwarz nach unten durchsichtiger... genauso soll das mit dem Liquid Glass gehen") — full-surface glossy glass, not a rim — while keeping text/icon readability non-negotiable (D-02).
Output: Full-surface Liquid Glass on both the native and legacy rendering paths, tunable via the DEBUG menu, on-device UAT approved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72.1.1-liquid-glass-design-rework-for-the-island-reference-componen/72.1.1-CONTEXT.md
@.planning/phases/72.1.1-liquid-glass-design-rework-for-the-island-reference-componen/72.1.1-RESEARCH.md
@.planning/phases/72.1.1-liquid-glass-design-rework-for-the-island-reference-componen/72.1.1-UI-SPEC.md
@.planning/phases/72.1.1-liquid-glass-design-rework-for-the-island-reference-componen/72.1.1-PATTERNS.md
@.planning/milestones/v1.6-phases/35-liquid-glass-material/35-CONTEXT.md
</context>

<interfaces>
State this plan builds on (post Plan 72.1.1-01, before this plan's edits) — `liquidGlassEffectLayer`'s native branch (`Islet/Notch/NotchPillView.swift:747-773`):
Native branch currently wraps `.glassEffect(.regular.tint(Color.black.opacity(0.35)), in: LiquidGlassRimRingShape(base: shape, bandWidth: rimWidth))` in a `GlassEffectContainer` with `.glassEffectID("islandGlass", in: glassNS)` (Plan 72.1.1-01's fix, unchanged by this plan except for the arguments named below).

DEBUG tuner read points available from Plan 72.1.1-02 (`Islet/Notch/NotchPillView.swift`, always-compiled `CGFloat` properties, `0` in Release):
`glassTintNudge`, `glassGlossNudge`, `glassLegacyEdgeOpacityNudge`, `glassLegacyCenterOpacityNudge`, `glassLegacyBorderWidthNudge`.

`LiquidGlassParameters` struct (`Islet/Notch/LiquidGlassShader.swift:34-93`) — `static let collapsed`/`.expanded`, both `borderWidth`/`blurWidth`/`distortionScale`/`redOffset`/`greenOffset`/`blueOffset`/`saturation`/`backgroundOpacity`/`edgeOpacity`/`centerOpacity`/`fringeOpacity` fields, all `CGFloat`. These are `static let` — cannot read `@AppStorage`-backed instance properties directly; nudges must be applied where `legacyLiquidGlassEffectLayer` consumes `parameters`, not inside the struct's static initializers (mirrors this file's own existing convention of applying scale-offset nudges at the read site, e.g. `islandWidthScaleOffset`, never inside a shared static geometry constant).
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Native path — full-surface shape, tunable tint, specular highlight</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift (lines 704-836 in full — `liquidGlassRimMask`, `LiquidGlassRimRingShape`, `liquidGlassRimBandWidth`, `liquidGlassEffectLayer`, `legacyLiquidGlassEffectLayer`, current post-Plan-72.1.1-01 state)
    .planning/phases/72.1.1-liquid-glass-design-rework-for-the-island-reference-componen/72.1.1-UI-SPEC.md (Island Shell Material Contract section)
  </read_first>
  <action>
    In `liquidGlassEffectLayer`'s native branch, change the `.glassEffect(_:in:)` call's `in:` argument from `LiquidGlassRimRingShape(base: shape, bandWidth: rimWidth)` to the bare `shape` (the pre-D-20a full-coverage code path, D-01). Change the tint from `Color.black.opacity(0.35)` to `Color.black.opacity(0.7 + glassTintNudge)` — `0.7` is the D-20-era starting literal, `glassTintNudge` is Plan 72.1.1-02's live nudge.

    Add a specular top-highlight as a sibling layer inside the same `GlassEffectContainer`, composited above the tinted glass: a `shape.stroke(LinearGradient(colors: [Color.white.opacity(0.15 + glassGlossNudge), Color.clear], startPoint: .top, endPoint: .center), lineWidth: 1.5)`, clipped to `shape`, `.allowsHitTesting(false)` — this is the concrete default "glossier" mechanism (a rim-light stroke); if on-device UAT (Task 3) finds the native compositor's own built-in specular already sufficient without it, note that in the checkpoint response rather than silently deleting the layer (the `glassGlossNudge` axis must still exist and be reachable even if dialed to a low starting value).

    Since `LiquidGlassRimRingShape`'s only consumer was this native branch, run `grep -n "LiquidGlassRimRingShape\|liquidGlassRimBandWidth"` across `Islet/Notch/NotchPillView.swift` before removing anything — if the grep confirms zero remaining call sites for either symbol after this task's edit, remove the now-dead `LiquidGlassRimRingShape` struct (lines ~718-726) and `liquidGlassRimBandWidth` function (lines ~728-730); if any other call site is found, leave both in place and note it.

    Do not touch `legacyLiquidGlassEffectLayer`, the `#available(macOS 26.0, *)` branch structure, or any of the 4 shell call sites — this task's scope is entirely inside `liquidGlassEffectLayer`'s native branch.
  </action>
  <acceptance_criteria>
    - `.glassEffect(_:in:)`'s `in:` argument is the bare `shape` variable, not `LiquidGlassRimRingShape(...)`.
    - Tint expression contains `0.7 + glassTintNudge`.
    - A new `.stroke(LinearGradient(...` highlight layer exists inside the native branch, referencing `glassGlossNudge`.
    - `GlassEffectContainer`/`.glassEffectID("islandGlass", in: glassNS)` from Plan 72.1.1-01 are still present and unchanged.
    - `grep -c "\.overlay(liquidGlassEffectLayer(shape:" Islet/Notch/NotchPillView.swift` returns `5` (4 presentation-state call sites plus `resumePreviewWings`, Phase 53's idle hover-preview wings; the looser `liquidGlassEffectLayer(shape:` pattern also matches the function definition and a comment, returning 7).
    - `xcodebuild build -scheme Islet -configuration Debug` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodebuild build -scheme Islet -configuration Debug 2>&1 | tail -30 && grep -c "\.overlay(liquidGlassEffectLayer(shape:" Islet/Notch/NotchPillView.swift</automated>
  </verify>
  <done>Native branch renders full-shape glass with tunable tint and a tunable specular highlight; build succeeds; structural grep (`\.overlay(liquidGlassEffectLayer(shape:` pattern) unchanged at 5.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Legacy shader fallback — retune for full coverage</name>
  <files>Islet/Notch/LiquidGlassShader.swift, Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/LiquidGlassShader.swift (full file, `LiquidGlassParameters.collapsed`/`.expanded`)
    Islet/Notch/NotchPillView.swift (lines 775-836, `legacyLiquidGlassEffectLayer`)
  </read_first>
  <action>
    In `LiquidGlassShader.swift`, retune the starting literals (RESEARCH.md Pattern 3 direction: widen the rim band, reduce dark-center-vs-edge contrast so the frost no longer reads as thin-rim-only):
    - `.collapsed`: `borderWidth: 0.07` -> `0.30`, `blurWidth: 1.2` -> `5.0`, `edgeOpacity: 0.08` -> `0.40`, `centerOpacity: 0.90` -> `0.62`.
    - `.expanded`: `borderWidth: 0.05` -> `0.28`, `blurWidth: 2.5` -> `7.0`, `edgeOpacity: 0.10` -> `0.45`, `centerOpacity: 0.92` -> `0.65`.
    Leave `distortionScale`, `redOffset`/`greenOffset`/`blueOffset`, `saturation`, `backgroundOpacity`, `fringeOpacity` unchanged on both — this task only touches the rim-band/opacity-contrast fields.

    Since `LiquidGlassParameters.collapsed`/`.expanded` are `static let` and cannot read `@AppStorage`, apply Plan 72.1.1-02's `glassLegacyEdgeOpacityNudge`/`glassLegacyCenterOpacityNudge`/`glassLegacyBorderWidthNudge` inside `legacyLiquidGlassEffectLayer` itself: at the top of the function, build a local `let tunedParameters = LiquidGlassParameters(borderWidth: parameters.borderWidth + glassLegacyBorderWidthNudge, blurWidth: parameters.blurWidth, distortionScale: parameters.distortionScale, redOffset: parameters.redOffset, greenOffset: parameters.greenOffset, blueOffset: parameters.blueOffset, saturation: parameters.saturation, backgroundOpacity: parameters.backgroundOpacity, edgeOpacity: parameters.edgeOpacity + glassLegacyEdgeOpacityNudge, centerOpacity: parameters.centerOpacity + glassLegacyCenterOpacityNudge, fringeOpacity: parameters.fringeOpacity)`, and use `tunedParameters` for the rest of the function body in place of the `parameters` argument (the `shaders`/`rimMask` local computations and every `shape.fill(...)` call below them).
  </action>
  <acceptance_criteria>
    - `LiquidGlassParameters.collapsed.borderWidth == 0.30`, `.expanded.borderWidth == 0.28` (grep confirms both literals present).
    - `legacyLiquidGlassEffectLayer` builds a local `tunedParameters` value incorporating `glassLegacyEdgeOpacityNudge`/`glassLegacyCenterOpacityNudge`/`glassLegacyBorderWidthNudge`, and uses it (not the raw `parameters` argument) for `shaders`/`rimMask`/fill calls.
    - `xcodebuild build -scheme Islet -configuration Debug` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && grep -n "borderWidth: 0.30\|borderWidth: 0.28" Islet/Notch/LiquidGlassShader.swift && xcodebuild build -scheme Islet -configuration Debug 2>&1 | tail -30</automated>
  </verify>
  <done>Legacy shader parameters retuned for full coverage and independently tunable via the DEBUG menu; build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device UAT — full-surface coverage, both rendering paths</name>
  <what-built>Native path: full-shape `.glassEffect()` with tunable tint + specular highlight, built on Plan 72.1.1-01's transition fix. Legacy path (<macOS 26 fallback): retuned `LiquidGlassParameters` for the same full-coverage intent, independently tunable.</what-built>
  <how-to-verify>
    1. Build and run in Debug on the macOS 26 device. Confirm glass reads across the WHOLE island surface — collapsed pill, expanded island, all wings — not confined to a rim/edge (D-01).
    2. Confirm text, track titles, calendar/weather content, icons, and album art stay fully legible everywhere (D-02). If not, use the new "Liquid Glass Tuner" DEBUG menu to dial `Glass Tint` and/or `Glass Gloss` down live, and report the values that read correctly.
    3. Confirm glass is visibly present in all 4 states: collapsed pill (idle), mid collapse<->expand animation, compact/small expanded state, full expanded island (D-03).
    4. Re-confirm the D-04 transition fix from Plan 72.1.1-01 still holds now that the glass region is larger — repeat several collapse<->expand cycles and tab switches, watching specifically for any flat-black/flat-color frame (RESEARCH.md Pitfall 1's named regression risk).
    5. Force the legacy (<macOS 26) fallback path locally (temporarily invert the `#available(macOS 26.0, *)` condition or comment out the native branch so `legacyLiquidGlassEffectLayer` always runs), rebuild, and confirm it independently reads as full-coverage + legible — not just a visual approximation of the native path. Use "Legacy Edge Opacity"/"Legacy Center Opacity"/"Legacy Border Width" DEBUG tuner items if it needs adjustment. Revert the forced branch before finishing.
    6. Confirm no cursor/pointer-follow reactivity anywhere (D-05 negative check).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues, including which DEBUG tuner axis (and delta) made it look correct, so the value can be baked into source.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan changes only local SwiftUI rendering literals and shader parameters. No new network call, file I/O, IPC, or user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-72.1.1-05 | Denial of Service (perf) | Full-shape `.glassEffect()` / widened legacy shader rim band, both animated every transition | accept | RESEARCH.md Pitfall 1's named risk — mitigated by verifying the D-04 fix (Plan 72.1.1-01) still holds under the larger surface area (Task 3, step 4) before this plan is considered done; not a hard security control, purely a UX/perf watch-item per UI-SPEC's Interaction/Animation Contract. |
| T-72.1.1-06 | Tampering | 5 `debug.glassTuner.*` keys read here (introduced in Plan 72.1.1-02) | accept | Already `#if DEBUG`-gated end-to-end; this plan only adds new READ sites for those keys, no new write surface. |

No ASVS-relevant findings — this phase touches only local UI rendering (RESEARCH.md Security Domain: "Not applicable").
</threat_model>

<verification>
Both Debug builds (Task 1, Task 2) succeed. Structural grep for `\.overlay(liquidGlassEffectLayer(shape:` stays at 5. On-device UAT (Task 3) approved, including the forced legacy-branch check, before Plan 72.1.1-04 proceeds.
</verification>

<success_criteria>
D-01 (full-surface coverage) and D-02 (readability precedence) are satisfied on BOTH rendering paths (native macOS 26+ and the <macOS 26 legacy shader fallback), D-03 (state coverage) holds across all 4 states, and D-04's transition fix (Plan 72.1.1-01) is confirmed intact at the larger surface area — all verified on-device, not assumed.
</success_criteria>

<output>
Create `.planning/phases/72.1.1-liquid-glass-design-rework-for-the-island-reference-componen/72.1.1-03-SUMMARY.md` when done
</output>
