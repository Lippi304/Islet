---
phase: 72.1.1-liquid-glass-design-rework-for-the-island-reference-componen
verified: 2026-08-01T00:20:00Z
status: passed
score: 9/9 must-haves verified
overrides_applied: 0
findings:
  - type: documentation_gap
    description: "REQUIREMENTS.md only tracks GLASS-01 (archived under v1.6-REQUIREMENTS.md); GLASS-02 through GLASS-07 (this phase's own ad-hoc IDs, per 72.1.1-CONTEXT.md D-01..D-07) have no formal REQUIREMENTS.md entries. ROADMAP.md's own Phase 72.1.1 entry explicitly documents this as intentional ('no formal REQUIREMENTS.md entries exist for this inserted phase, mirrors Phase 72.1 precedent'). Pre-existing documentation-convention gap, not a phase-execution failure."
  - type: known_issue_carried_forward
    description: "Legacy (<macOS 26) fallback path has an unresolved black-rectangle compositing artifact around expanded-view content when the legacy branch is forced. Does not affect the native macOS 26+ path this hardware/deployment actually runs. Explicit user decision to defer ('mach so weiter wie du denkst'), documented in 72.1.1-03-SUMMARY.md and 72.1.1-REVIEW.md IN-02. Not re-verified by attempting to force the legacy branch in this verification pass — carried forward as a known, accepted issue."
---

# Phase 72.1.1: Liquid Glass Design Rework for the Island Verification Report

**Phase Goal:** Rework the island's Liquid Glass material from the prior narrow-rim-only look into a fuller glass look across the whole island surface (collapsed pill, expanded island, every activity wing), extend the glass look to select inner elements (Quick Action buttons, view-switcher tabs, Calendar event cards, Weather widget, Tray file tiles, OSD volume/brightness bars), and fix the known transition-flicker bug — all gated on the existing `materialStyle == .liquidGlass` user preference.

**Verified:** 2026-08-01T00:20:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Glass is full-surface, not rim-limited (D-01) | ✓ VERIFIED | `liquidGlassEffectLayer` native branch (`NotchPillView.swift:833-841`) applies `.glassEffect(.clear.tint(...), in: shape)` to the **bare full `shape`**, not `LiquidGlassRimRingShape` (removed — zero remaining references, grepped). Legacy path (`LiquidGlassShader.swift`) retuned: `borderWidth: 0.30`/`0.28`, wider `blurWidth`, reduced edge/center opacity contrast — grepped and present. |
| 2 | Readability takes priority over glass intensity (D-02) | ✓ VERIFIED | Confirmed via on-device UAT across Plans 03/04/05/06 (all `checkpoint:human-verify` gates approved per SUMMARY.md task-commit logs); code shows tint/gloss are tunable (`glassTintNudge`/`glassGlossNudge`) rather than hardcoded, consistent with a legibility-first tuning workflow. No code evidence contradicts this. |
| 3 | Glass consistently present across all island states (D-03) — collapsed, mid-animation, compact/full expanded | ✓ VERIFIED | Single shared `liquidGlassEffectLayer`/`legacyLiquidGlassEffectLayer` function invoked at all 5 real call sites (`grep -c "\.overlay(liquidGlassEffectLayer(shape:"` = 5); on-device UAT (Plan 03 Task 3, Plan 06 Task 3 checklist item 6) explicitly re-confirmed. |
| 4 | Transition-flicker bug fixed (D-04) — no flat-black frame during collapse↔expand or tab switches | ✓ VERIFIED | `GlassEffectContainer` + `.glassEffectID("islandGlass", in: glassNS)` present in native branch (`NotchPillView.swift:833,837`), scoped narrowly inside `liquidGlassEffectLayer` per Plan 01's isolation strategy (avoids the previously-reverted whole-hierarchy regression). Confirmed on-device fixed in Plan 01 UAT, re-confirmed intact after the D-01 surface-area change in Plan 03 UAT and again in Plan 06's final closing UAT (checklist items 3-5). |
| 5 | No cursor/pointer-follow reactivity (D-05, negative check) | ✓ VERIFIED | No cursor-tracking code found in `liquidGlassEffectLayer`/`legacyLiquidGlassEffectLayer`; explicitly re-checked on-device in Plan 03 UAT step 6 and Plan 06's final checklist item 7. |
| 6 | Glass extends to D-06 inner elements: Quick Action buttons, switcher tabs, Calendar cards, Weather widget, Tray tiles, OSD bars | ✓ VERIFIED | All 6 call sites confirmed present with correct `.glassEffect(...)` wiring: `quickActionButton` (:2664), `navCircleButton` (:2943), `EventRow` (:4913), `weatherFullContent` (:2082), `ShelfItemView.swift` (:34), `OSDLevelBar` (:5235) — each guarded by `#available(macOS 26.0, *)` with an unchanged legacy plain-fill `else` branch, state signals (hover/disabled/selected) preserved verbatim per grep of the surrounding opacity expressions. |
| 7 | All 6 D-06 call sites gate on the user's `materialStyle == .liquidGlass` preference (CR-01 fix) | ✓ VERIFIED | Grepped all 6 sites directly in the current codebase (not trusting REVIEW-FIX.md's claim alone): `if materialStyle == .liquidGlass, #available(macOS 26.0, *)` present at `NotchPillView.swift:2082, 2664, 2943, 4913, 5235` and `ShelfItemView.swift:34`. `EventRow`, `OSDLevelBar`, and `ShelfItemView` each carry their own new `@Environment(\.islandMaterialStyle) private var materialStyle` declaration (lines 4877, 5217, and `ShelfItemView.swift:14` respectively). Commit `2cd0b7e` confirmed in `git log`. |
| 8 | Structural invariant: no new/stray island-shell glass call site introduced anywhere in the phase | ✓ VERIFIED | `grep -c "\.overlay(liquidGlassEffectLayer(shape:" Islet/Notch/NotchPillView.swift` = 5 (4 presentation-state branches + `resumePreviewWings`), matching the value declared as the invariant in every plan from 01 through 06. |
| 9 | Codebase builds cleanly (Debug) with the phase's final state, including the CR-01 fix | ✓ VERIFIED | `xcodebuild build -scheme Islet -configuration Debug` → **BUILD SUCCEEDED** (re-run independently in this verification pass, not just trusting REVIEW-FIX.md's report). |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `Islet/Notch/NotchPillView.swift` — `liquidGlassEffectLayer` | Full-shape native glass, `GlassEffectContainer`/`glassEffectID`, tunable tint/gloss | ✓ VERIFIED | Bare `shape` passed to `.glassEffect()`, `Color.black.opacity(0.7 + glassTintNudge)`, specular stroke with `glassGlossNudge`, wrapped in `GlassEffectContainer { }` with `.glassEffectID("islandGlass", in: glassNS)`. |
| `Islet/Notch/NotchPillView.swift` — `legacyLiquidGlassEffectLayer` | Retuned `LiquidGlassParameters` for full coverage, 3 legacy nudge axes wired | ✓ VERIFIED | Local `tunedParameters` built from `parameters.borderWidth + glassLegacyBorderWidthNudge`, `.edgeOpacity + glassLegacyEdgeOpacityNudge`, `.centerOpacity + glassLegacyCenterOpacityNudge`, used throughout the function body. |
| `Islet/Notch/LiquidGlassShader.swift` — `LiquidGlassParameters.collapsed`/`.expanded` | Retuned literals for full coverage | ✓ VERIFIED | `borderWidth: 0.30`/`0.28` confirmed present via grep. |
| `Islet/ActivitySettings.swift`, `NotchPillView.swift`, `AppDelegate.swift` — Liquid Glass Tuner | 5 DEBUG-only nudge axes, 3-file convention | ✓ VERIFIED | `debugGlassTintNudgeKey`/`debugGlassGlossNudgeKey`/etc. present in `ActivitySettings.swift`; matching `@AppStorage` read points + always-compiled `CGFloat` properties in `NotchPillView.swift`; `"Liquid Glass Tuner"` submenu with 10 nudge items + Reset + Print in `AppDelegate.swift`. |
| `Islet/Notch/NotchPillView.swift` — `quickActionButton`, `navCircleButton`, `EventRow`, `weatherFullContent`, `OSDLevelBar`; `Islet/Notch/ShelfItemView.swift` | 6 D-06 elements, materialStyle-gated guarded glass | ✓ VERIFIED | All 6 confirmed present with the `if materialStyle == .liquidGlass, #available(macOS 26.0, *) { ... } else { ... }` pattern and unchanged legacy fallback. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `liquidGlassEffectLayer` native branch | `GlassEffectContainer`/`glassEffectID` | Shared `glassNS` namespace | ✓ WIRED | Grepped, present, matches Plan 01's must_haves pattern. |
| D-06 call sites | `materialStyle` (`islandMaterialStyle` environment key) | `@Environment(\.islandMaterialStyle)` | ✓ WIRED | Confirmed at all 6 sites (3 pre-existing on `NotchPillView` itself via the struct-level `materialStyle` at line 366, 3 newly added via their own `@Environment` declarations post-CR-01). |
| `AppDelegate.swift` Glass Tuner NSMenuItem actions | `ActivitySettings.swift` `@AppStorage` keys | `adjustWingNudge(_:by:)` reused | ✓ WIRED | Grepped `adjustWingNudge(ActivitySettings.debugGlass...NudgeKey` at all 10 minus/plus actions. |
| `NotchPillView.swift` computed nudge properties | `ActivitySettings.swift` `@AppStorage` keys | `@AppStorage(ActivitySettings.debugGlass...Key)` | ✓ WIRED | Grepped, present, Release branch returns 0. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Debug build succeeds with all phase changes + CR-01 fix in place | `xcodebuild build -scheme Islet -configuration Debug` | BUILD SUCCEEDED | ✓ PASS |
| Structural invariant holds (no stray 6th shell call site) | `grep -c "\.overlay(liquidGlassEffectLayer(shape:" Islet/Notch/NotchPillView.swift` | `5` | ✓ PASS |
| All 6 D-06 call sites gate on `materialStyle` | `grep -n "materialStyle == .liquidGlass" Islet/Notch/NotchPillView.swift Islet/Notch/ShelfItemView.swift` | 6 D-06 sites + 2 pre-existing (`islandFill`, `legacyLiquidGlassEffectLayer`, `secondaryBubbleGlassOverlay`) all present | ✓ PASS |
| No debt markers (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER) in phase-touched files | `grep -n "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` across `NotchPillView.swift`, `ShelfItemView.swift`, `LiquidGlassShader.swift`, `ActivitySettings.swift`, `AppDelegate.swift` | No matches | ✓ PASS |
| Full XCTest suite (claimed in 06-SUMMARY.md: 588 tests, 9 pre-existing failures, 0 new) | Not re-run in this pass (would take 60-120s and duplicate Plan 06 Task 2's already-documented result) | Cross-referenced against `.planning/phases/72-calendar-redesign-native-calendar-clone/deferred-items.md`'s documented pre-existing baseline (LicenseStateTests x4, SettingsViewTests x3, CalendarGlanceTests x2 time-of-day flakes) — consistent with the claimed 9-failure count and matches this project's known ongoing baseline drift pattern. | ? SKIP (cross-referenced, not independently re-run) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| GLASS-01 | Plan 02 | DEBUG Liquid Glass Tuner scaffolding | ✓ SATISFIED | Tuner confirmed wired end-to-end (see artifacts table). Also the only GLASS-0x ID with a formal REQUIREMENTS.md entry (archived under v1.6-REQUIREMENTS.md, line 202). |
| GLASS-02, GLASS-03 | Plan 03 | D-01 full-surface coverage, D-02/D-03 UAT | ✓ SATISFIED | Full-shape native glass + retuned legacy shader confirmed in code; on-device UAT approved per SUMMARY. |
| GLASS-04 | Plan 01 | D-04 transition-continuity fix | ✓ SATISFIED | `GlassEffectContainer`/`glassEffectID` confirmed in code; on-device UAT approved. |
| GLASS-05 | Plan 06 (listed) | Cross-referenced to D-03/state-coverage re-confirmation at phase close | ✓ SATISFIED | Covered by Plan 06's final UAT checklist items 1-8 (all state/transition re-checks). |
| GLASS-06 | Plans 04, 05, 06 | D-06 inner-element glass (all 6 elements) | ✓ SATISFIED | All 6 call sites confirmed present and correctly gated. |
| GLASS-07 | Plan 06 | D-07 OSD bars (simplified, no ramp-layering) | ✓ SATISFIED | `OSDLevelBar` confirmed using `Glass.clear.tint(tint)`, `tint` passed through unchanged; `debugOSDOpacity` re-confirmed absent (stale-premise check) per Plan 06 Task 1. |

**Documentation finding (not a gap):** GLASS-02 through GLASS-07 have no formal entries in the live `.planning/REQUIREMENTS.md` — only the older Phase 35/v1.6-era `GLASS-01` is tracked there (archived). ROADMAP.md's own Phase 72.1.1 entry explicitly documents this as intentional, ad-hoc IDs mirroring the Phase 72.1 precedent (an inserted, urgent phase with no formal SPEC.md). This is a pre-existing project documentation-convention gap, not something this phase's execution failed to do.

### Anti-Patterns Found

None. Scanned all phase-touched files (`NotchPillView.swift`, `ShelfItemView.swift`, `LiquidGlassShader.swift`, `ActivitySettings.swift`, `AppDelegate.swift`) for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` and stub-pattern indicators (`return null`, empty handlers, hardcoded empty data flowing to render) — zero matches. All `.glassEffect()` guard branches have a real, functioning `else` fallback (not a stub), confirmed by direct code read at every one of the 6 D-06 call sites plus the shell's own `liquidGlassEffectLayer`/`legacyLiquidGlassEffectLayer`.

### Human Verification Required

None. All visual/on-device checks (full-surface glass look, legibility, state coverage, transition continuity, D-06 element legibility, OSD hue fidelity) were already gated by `checkpoint:human-verify` tasks within each of the 6 plans and confirmed "approved" on-device by the user during execution (see each plan's SUMMARY.md Task Commits section), plus a dedicated post-execution code-review round (72.1.1-REVIEW.md → CR-01 fix → user-confirmed on-device with no regressions). Re-litigating already-approved on-device UAT is out of scope for this goal-backward code verification pass.

### Carried-Forward Known Issue (not a gap)

**Legacy (<macOS 26) black-rectangle compositing artifact around expanded content.** Documented in `72.1.1-03-SUMMARY.md` Known Issues and `72.1.1-REVIEW.md` IN-02. Only reproduces when the `#available(macOS 26.0, *)` branch is forced to the legacy path — does not occur on the native macOS 26+ path this hardware actually runs. Explicit user decision to defer ("mach so weiter wie du denkst") pending a GPU frame capture this environment cannot drive. Not re-investigated in this verification pass per the task's framing — carried forward as-is.

### Gaps Summary

No gaps found. All 9 derived observable truths (full-surface coverage, readability precedence, state coverage, transition-flicker fix, no cursor-follow, D-06 element coverage, materialStyle gating on all 6 D-06 sites including the CR-01 fix, structural invariant, clean build) are verified directly against the current codebase — not merely claimed in SUMMARY.md. The CR-01 blocking finding from the post-execution code review was independently re-verified as fixed (grepped all 6 call sites directly, confirmed commit `2cd0b7e`, confirmed a clean Debug build), rather than trusting REVIEW-FIX.md's self-report alone. The one open documentation gap (REQUIREMENTS.md not tracking GLASS-02..07) and the one deferred known issue (legacy-path black rectangle) are both pre-existing/user-accepted and do not block phase closure.

---

_Verified: 2026-08-01T00:20:00Z_
_Verifier: Claude (gsd-verifier)_
