---
phase: 72.1.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Islet/Notch/NotchPillView.swift]
autonomous: false
requirements: [GLASS-04, GLASS-03]
must_haves:
  truths:
    - "No flat-black/flat-color frame appears at any point during collapse<->expand animation"
    - "No flat-black/flat-color frame appears at any point during view-switcher tab switches"
    - "The island does NOT regress to reading as permanently frosted glass everywhere (the previously-reverted wide-scope failure mode)"
    - "Today's rim-only glass visual scope is unchanged by this plan (D-01 full-surface scaling is explicitly out of scope here)"
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "glassNS @Namespace + GlassEffectContainer/.glassEffectID(\"islandGlass\", in: glassNS) scoped strictly inside liquidGlassEffectLayer's native branch"
      contains: "glassEffectID(\"islandGlass\""
  key_links:
    - from: "liquidGlassEffectLayer (native branch)"
      to: "GlassEffectContainer + glassEffectID"
      via: "shared glassNS Namespace, one wrap per call, same ID at all 4 shell call sites since they all invoke this one function"
      pattern: "GlassEffectContainer"
---

<objective>
Fix the known Liquid Glass transition-flicker bug (`.planning/debug/resolved/liquid-glass-black-during-transition.md`, CONTEXT.md D-04) by scoping a `GlassEffectContainer` + shared `.glassEffectID` strictly inside `liquidGlassEffectLayer` — narrower than the previously-reverted whole-`presentationSwitch`-wide attempt that caused a worse regression (entire island permanently frosted).

This plan intentionally does NOT touch D-01's full-surface scaling (still `LiquidGlassRimRingShape`, unchanged rim-only shape) — RESEARCH.md Pitfall 1 requires isolating the transition-continuity fix from the surface-area change so any later regression can be attributed to the right cause. Full-surface coverage is Plan 72.1.1-03's job, built on top of this fix.

Purpose: Resolve a real, previously-deferred bug (D-04) using the API Apple documents for exactly this class of problem, verified narrowly before any other Liquid Glass change lands on top of it.
Output: `liquidGlassEffectLayer`'s native (`macOS 26.0+`) branch renders glass continuously through every state transition; on-device UAT confirms the fix and confirms no regression to the wide-scope failure mode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72.1.1-liquid-glass-design-rework-for-the-island-reference-componen/72.1.1-CONTEXT.md
@.planning/phases/72.1.1-liquid-glass-design-rework-for-the-island-reference-componen/72.1.1-RESEARCH.md
@.planning/phases/72.1.1-liquid-glass-design-rework-for-the-island-reference-componen/72.1.1-UI-SPEC.md
@.planning/debug/resolved/liquid-glass-black-during-transition.md
</context>

<interfaces>
Current code being changed (`Islet/Notch/NotchPillView.swift:747-773`):

liquidGlassEffectLayer's native branch currently is a bare per-call-site `.glassEffect(_:in:)`:
```
Color.clear
    .frame(width: size.width, height: size.height)
    .glassEffect(.regular.tint(Color.black.opacity(0.35)), in: LiquidGlassRimRingShape(base: shape, bandWidth: rimWidth))
    .allowsHitTesting(false)
```

The existing struct-level `@Namespace private var ns` (line 463) drives `matchedGeometryEffect(id: "island", in: ns)` at all 4 shell call sites — do NOT reuse this namespace for glass (cross-interference risk, see RESEARCH.md Pattern 2). Declare a SEPARATE `@Namespace private var glassNS` alongside it.

All 4 shell call sites (`collapsedIsland` ~1378, `blobShape` ~2937, `wingsShape` ~3219, `mediaWingsOrToast` ~3401) call `liquidGlassEffectLayer(shape:size:parameters:)` with the identical signature — since the container/ID wrap lives INSIDE this one function, none of the 4 call sites need any edit in this plan.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scope GlassEffectContainer + shared glassEffectID inside liquidGlassEffectLayer</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift (lines 440-470 for the existing `ns` Namespace declaration and surrounding struct properties; lines 747-836 for `liquidGlassEffectLayer` and `legacyLiquidGlassEffectLayer` in full)
    .planning/debug/resolved/liquid-glass-black-during-transition.md (documents the prior whole-hierarchy `GlassEffectContainer` attempt and why it regressed — do not repeat that scope)
    .planning/phases/72.1.1-liquid-glass-design-rework-for-the-island-reference-componen/72.1.1-RESEARCH.md (Pattern 2, Pitfall 1)
  </read_first>
  <action>
    Add a new stored property `@Namespace private var glassNS` next to the existing `@Namespace private var ns` declaration (around line 463) — a dedicated namespace for glass-morph tracking, separate from the shape's own `matchedGeometryEffect` namespace.

    In `liquidGlassEffectLayer`'s `#available(macOS 26.0, *)` branch (lines 750-766), wrap the existing `Color.clear.frame(...).glassEffect(...).allowsHitTesting(false)` chain in a `GlassEffectContainer { ... }` closure, and add `.glassEffectID("islandGlass", in: glassNS)` immediately after the `.glassEffect(...)` call (before `.allowsHitTesting(false)`). Do NOT change the `.glassEffect(...)` call's arguments themselves in this task — `LiquidGlassRimRingShape(base: shape, bandWidth: rimWidth)` and `Color.black.opacity(0.35)` stay byte-for-byte unchanged; only the wrapping container and the identity ID are new. Do NOT wrap `legacyLiquidGlassEffectLayer`, the `#available` branch structure, or anything outside this one function — the container's scope must be exactly this function's native-branch output, never `presentationSwitch` or any shell call site.

    Also update the stale explanatory comment block immediately above the `Color.clear` chain (lines 752-762, the comment starting "Debug session liquid-glass-black-during-transition, round 2 —"). Its closing sentence currently reads "...the brief flicker during transitions remains a known, lower-priority issue (see Resolution note in the debug file)" — this becomes contradictory once this task's fix lands, since the flicker is no longer an open issue. Trim or reword that closing sentence to drop the "still open" framing while keeping the historical warning (the reverted whole-presentationSwitch-scoped GlassEffectContainer attempt and why it regressed) — that context remains valid and should stay in the comment.
  </action>
  <acceptance_criteria>
    - `@Namespace private var glassNS` exists as a distinct stored property from `ns`.
    - `liquidGlassEffectLayer`'s native branch wraps its `.glassEffect(...)` chain in `GlassEffectContainer { ... }`.
    - The wrapped chain includes `.glassEffectID("islandGlass", in: glassNS)`.
    - `LiquidGlassRimRingShape(base: shape, bandWidth: rimWidth)` and `Color.black.opacity(0.35)` are unchanged (grep confirms both strings still present verbatim in the file).
    - `grep -c "\.overlay(liquidGlassEffectLayer(shape:" Islet/Notch/NotchPillView.swift` returns `5` (no new/removed shell call site — the 5 real call sites are the 4 presentation-state branches plus `resumePreviewWings`, Phase 53's idle hover-preview wings; the looser `liquidGlassEffectLayer(shape:` pattern also matches the function definition and a comment reference, returning 7).
    - `xcodebuild build -scheme Islet -configuration Debug` succeeds with no new errors.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodebuild build -scheme Islet -configuration Debug 2>&1 | tail -30 && grep -c "\.overlay(liquidGlassEffectLayer(shape:" Islet/Notch/NotchPillView.swift</automated>
  </verify>
  <done>Build succeeds, structural grep (`\.overlay(liquidGlassEffectLayer(shape:` pattern) still returns 5, `glassEffectID("islandGlass"` is present exactly once in the file.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device UAT — transition continuity fix, isolated from D-01 scaling</name>
  <what-built>A `GlassEffectContainer` + shared `.glassEffectID("islandGlass", in: glassNS)` scoped strictly inside `liquidGlassEffectLayer`, applied to TODAY's existing rim-only glass shape (no surface-area change yet — that is Plan 72.1.1-03).</what-built>
  <how-to-verify>
    1. Build and run the app in Debug on the macOS 26 (Tahoe) device.
    2. If possible, first temporarily comment out the `GlassEffectContainer`/`.glassEffectID` wrap (revert to the bare `.glassEffect(...)` call) and confirm the ORIGINAL flat-black transition bug still reproduces on today's code — then restore the fix. This confirms the fix is actually being tested against a real repro, not a bug that already silently stopped occurring (RESEARCH.md Open Question 3).
    3. With the fix in place: hover the notch repeatedly to trigger collapse<->expand several times in a row. Confirm the rim-glass renders continuously through the morph — no flat-black or flat-color frame at any point.
    4. Switch view-switcher tabs (Home / Calendar / Weather / Tray) repeatedly. Confirm no flat-black frame during the switch.
    5. Confirm the island still reads as "thin rim of glass, solid dark center" — exactly like before this plan — at every state. If the WHOLE island reads as uniformly frosted glass at any point, that is the previously-reverted wide-scope regression re-appearing at a narrower scope; report this explicitly, do not approve.
  </how-to-verify>
  <resume-signal>Type "approved" or describe exactly what you saw (e.g., "still flickers on tab switch", "whole island frosted now", "looks correct").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan changes only local SwiftUI view-composition/animation code (a decorative overlay layer). No new network call, file I/O, IPC, or user input is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-72.1.1-01 | Denial of Service (perf) | `liquidGlassEffectLayer` native branch (`GlassEffectContainer`) | accept | Rendering-only cost (compositor work per RESEARCH.md Pitfall 1); on-device UAT (Task 2) is the explicit gate for any perceived jank/frame-drop, not a hard security control. No untrusted input drives this code path. |
| T-72.1.1-02 | Tampering | N/A — no persisted state, no new `@AppStorage` key in this plan | accept | This plan introduces no new UserDefaults key; nothing to tamper with. |

No ASVS-relevant findings — this phase touches only local UI rendering (RESEARCH.md Security Domain: "Not applicable").
</threat_model>

<verification>
`xcodebuild build -scheme Islet -configuration Debug` succeeds. `grep -c "\.overlay(liquidGlassEffectLayer(shape:" Islet/Notch/NotchPillView.swift` == 5. On-device UAT (Task 2) approved before Plan 72.1.1-02/03 proceed.
</verification>

<success_criteria>
The transition-flicker bug (D-04) is fixed and confirmed fixed on-device, in isolation from any full-surface-coverage change, with zero regression to the previously-reverted wide-scope frosting failure mode.
</success_criteria>

<output>
Create `.planning/phases/72.1.1-liquid-glass-design-rework-for-the-island-reference-componen/72.1.1-01-SUMMARY.md` when done
</output>
