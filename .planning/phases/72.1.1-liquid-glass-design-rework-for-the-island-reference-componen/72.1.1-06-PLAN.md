---
phase: 72.1.1
plan: 06
type: execute
wave: 6
depends_on: ["72.1.1-05"]
files_modified: [Islet/Notch/NotchPillView.swift]
autonomous: false
requirements: [GLASS-07, GLASS-02, GLASS-03, GLASS-05, GLASS-06]
must_haves:
  truths:
    - "OSDLevelBar shows glass on top of its current always-full-opacity fill, with the fixed green/orange hue unchanged (D-07, simplified per Pitfall 4 — no opacity-ramp coordination logic, since that mechanic was reverted before this phase)"
    - "OSDLevelBar compiles at the project's 15.0 deployment target — the new .glassEffect() call is guarded by #available(macOS 26.0, *) with a legacy plain-fill fallback, matching the codebase's existing convention"
    - "The structural invariant (exactly 5 real .overlay(liquidGlassEffectLayer(shape:...)) call sites — 4 presentation-state branches plus resumePreviewWings, Phase 53's idle hover-preview wings — no 6th added anywhere in the phase) holds"
    - "The full XCTest suite shows zero NEW failures beyond this project's documented pre-existing baseline"
    - "Every item on UI-SPEC's On-Device UAT Gate checklist is confirmed, as the phase's final closing gate"
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "OSDLevelBar's filled Capsule gains a guarded .glassEffect(.regular.tint(tint), in: Capsule()) branch (legacy branch keeps the plain .fill(tint)), tint (green/orange) passed through unchanged"
      contains: "Capsule().fill(Color.clear).glassEffect(.regular.tint(tint)"
  key_links:
    - from: "OSDLevelBar"
      to: "existing tint parameter (Color.green / Color.orange, Phase 39 D-02 locked)"
      via: "if #available(macOS 26.0, *) { Capsule().glassEffect(.regular.tint(tint), in: Capsule()) } else { Capsule().fill(tint) } — tint passed straight through in both branches, never re-derived"
      pattern: "glassEffect\\(\\.regular\\.tint\\(tint\\)"
---

<objective>
Apply the phase's final D-06 element — OSD Volume/Brightness bars (`OSDLevelBar`) — per the simplified D-07 (RESEARCH.md Pitfall 4: the opacity-ramp mechanic D-07 originally said to "layer on top of" was reverted same-day by quick task `260731-61m`; `OSDLevelBar` today has a plain always-full-opacity fill, so this is a single-layer glass addition with no ramp-coordination logic needed), then close out the phase with the mandatory structural-invariant check and a comprehensive final on-device UAT sweep covering every item on UI-SPEC's "On-Device UAT Gate" checklist.

The new `.glassEffect(...)` call is wrapped in the same `if #available(macOS 26.0, *) { ... } else { ... }` guard already established in this file (`NotchPillView.swift:750`, `:4561`), since the project's deployment target is macOS 15.0 and `.glassEffect(_:in:isEnabled:)` is `@available(macOS 26.0, *)`. The `else` branch is the exact pre-existing `.fill(tint)`, unchanged.

Purpose: Complete D-06 coverage and provide the phase's hard merge gate — confirming the full set of changes from Plans 72.1.1-01 through 06 hold together, not just individually, and that everything still compiles at the declared deployment target.
Output: OSD bars render with glass on their fixed-hue fill on macOS 26+ (plain fill unchanged below that); `grep -c "\.overlay(liquidGlassEffectLayer(shape:"` confirmed at 5; full XCTest suite green; final on-device UAT approved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72.1.1-liquid-glass-design-rework-for-the-island-reference-componen/72.1.1-CONTEXT.md
@.planning/phases/72.1.1-liquid-glass-design-rework-for-the-island-reference-componen/72.1.1-UI-SPEC.md
@.planning/phases/72.1.1-liquid-glass-design-rework-for-the-island-reference-componen/72.1.1-RESEARCH.md
@.planning/phases/72.1.1-liquid-glass-design-rework-for-the-island-reference-componen/72.1.1-VALIDATION.md
</context>

<interfaces>
Current `OSDLevelBar` (`Islet/Notch/NotchPillView.swift:5048-5061`):
```
private struct OSDLevelBar: View {
    let fraction: CGFloat
    let tint: Color

    var body: some View {
        GeometryReader { geo in
            ZStack(alignment: .leading) {
                Capsule().fill(Color.white.opacity(0.15))                       // empty track
                Capsule().fill(tint).frame(width: geo.size.width * fraction)    // filled (D-02 fixed tint)
                    .animation(.spring(response: 0.15, dampingFraction: 0.86), value: fraction)
            }
        }
    }
}
```
`tint` is passed in as `Color.green` (volume) or `Color.orange` (brightness) by the caller (Phase 39 D-02, locked) — this task never re-derives or replaces that value, only adds glass on top of the existing filled `Capsule`.

Existing `#available` guard convention this task MUST mirror (`Islet/Notch/NotchPillView.swift:750-765`, also `:4561-4567`):
`if #available(macOS 26.0, *) { <native .glassEffect(...) variant> } else { <legacy plain-fill fallback> }`.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Apply glass to OSDLevelBar's filled capsule</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift (lines 5040-5061, `OSDLevelBar` in full)
    Islet/Notch/NotchPillView.swift (lines 745-770, the `if #available(macOS 26.0, *) { } else { }` convention to mirror)
    .planning/phases/72.1.1-liquid-glass-design-rework-for-the-island-reference-componen/72.1.1-RESEARCH.md (Pitfall 4)
    .planning/phases/72.1.1-liquid-glass-design-rework-for-the-island-reference-componen/72.1.1-UI-SPEC.md (Inner Elements Contract, "OSD bars" table)
  </read_first>
  <action>
    Before implementing, run `grep -rn "debugOSDOpacity" Islet/ActivitySettings.swift Islet/Notch/NotchPillView.swift` to reconfirm zero results (Pitfall 4's stale-premise check — confirms there is genuinely no live opacity-ramp mechanic left to coordinate with). If any match is found, STOP and do not proceed with this task — flag it back instead, since it would mean D-07's "reverted" premise no longer holds.

    Change `Capsule().fill(tint).frame(width: geo.size.width * fraction)` to `Group { if #available(macOS 26.0, *) { Capsule().fill(Color.clear).glassEffect(.regular.tint(tint), in: Capsule()) } else { Capsule().fill(tint) } }.frame(width: geo.size.width * fraction)` — the `.fill(Color.clear)` immediately before `.glassEffect(...)` is REQUIRED: a bare `Capsule` used as a View renders opaquely filled with the current foreground style (black/primary), and since `.glassEffect()` composites its material as a background behind the view's own content, an unfilled `Capsule` in front of it would draw opaque and hide the glass — losing the green/orange color signal entirely (this matches the `NotchPillView.swift:763-766` precedent, which always fills before compositing glass). `tint` (the caller-supplied `Color.green`/`Color.orange`) is passed straight into `.glassEffect(.regular.tint(tint), in: Capsule())` in the native branch, and straight into the unchanged `.fill(tint)` in the legacy branch. Do not touch the empty-track `Capsule().fill(Color.white.opacity(0.15))` layer (out of scope — that is background chrome, not the D-07 fill this task targets), and do not touch the trailing `.animation(.spring(response: 0.15, dampingFraction: 0.86), value: fraction)` modifier — it must remain attached to the same view chain (outside the `Group`, still driven by `fraction`).
  </action>
  <acceptance_criteria>
    - `grep -c "debugOSDOpacity" Islet/ActivitySettings.swift Islet/Notch/NotchPillView.swift` returns `0` before this task's edit is considered valid.
    - `OSDLevelBar`'s filled capsule chain reads `Group { if #available(macOS 26.0, *) { Capsule().fill(Color.clear).glassEffect(.regular.tint(tint), in: Capsule()) } else { Capsule().fill(tint) } }.frame(width: geo.size.width * fraction)` — grep confirms `.fill(Color.clear)` appears immediately before this `.glassEffect(` call site.
    - The empty-track `Capsule().fill(Color.white.opacity(0.15))` line is unchanged.
    - The `.animation(.spring(response: 0.15, dampingFraction: 0.86), value: fraction)` modifier is unchanged and still attached to the filled capsule's chain.
    - `xcodebuild build -scheme Islet -configuration Debug` succeeds.
    - `xcodebuild build -scheme Islet -configuration Release` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && grep -c "debugOSDOpacity" Islet/ActivitySettings.swift Islet/Notch/NotchPillView.swift; grep -c "Capsule().fill(Color.clear).glassEffect(" Islet/Notch/NotchPillView.swift && xcodebuild build -scheme Islet -configuration Debug 2>&1 | tail -30 && xcodebuild build -scheme Islet -configuration Release 2>&1 | tail -30</automated>
  </verify>
  <done>OSDLevelBar's filled capsule renders via a guarded .glassEffect() (macOS 26+), preceded by `.fill(Color.clear)` so the glass renders instead of an opaque black/primary-colored capsule, with the exact same tint/animation behavior as before, unchanged legacy fallback below that; both Debug and Release builds succeed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Structural invariant + full build/test verification</name>
  <files>none (verification only — no source files modified by this task)</files>
  <read_first>
    .planning/phases/72.1.1-liquid-glass-design-rework-for-the-island-reference-componen/72.1.1-RESEARCH.md (Validation Architecture table)
    .planning/deferred-items.md (pre-existing documented XCTest failure baseline, if present)
  </read_first>
  <action>
    Run `grep -c "\.overlay(liquidGlassEffectLayer(shape:" Islet/Notch/NotchPillView.swift` and confirm the output is exactly `5` (the 4 presentation-state call sites plus `resumePreviewWings`, Phase 53's idle hover-preview wings — the looser `liquidGlassEffectLayer(shape:` pattern also matches the function definition at line 748 and a comment at line 4546, returning 7, so use the `.overlay(`-prefixed pattern) — this confirms Plans 72.1.1-01 through 06 added zero new island-shell call sites (RESEARCH.md's own structural-invariant precedent, mirroring the project's WR-02/D-13/D-14 convention). Then run the full Debug build (`xcodebuild build -scheme Islet -configuration Debug`) and the full XCTest suite (`xcodebuild test -scheme Islet -destination 'platform=macOS'`); confirm zero NEW failures beyond this project's pre-existing documented baseline (check `.planning/deferred-items.md` for the known-failure list, e.g. `LicenseStateTests`/`SettingsViewTests` failures already documented as pre-existing).

    Note on verification latency: the `xcodebuild test` run in this task's `<verify>` intentionally takes longer (roughly 60-120s) than every other task's build-only check in this phase (typically under 30s) — this is expected, deliberate phase-closing verification (the project's per-wave sampling cadence reserves the full test suite for wave-closing gates, not per-task checks), not a Nyquist-rule violation.
  </action>
  <acceptance_criteria>
    - `grep -c "\.overlay(liquidGlassEffectLayer(shape:" Islet/Notch/NotchPillView.swift` outputs `5`.
    - `xcodebuild build -scheme Islet -configuration Debug` succeeds.
    - `xcodebuild test -scheme Islet -destination 'platform=macOS'` completes, and every failure (if any) matches the pre-existing documented baseline in `.planning/deferred-items.md` — no new failure introduced by this phase's changes.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && grep -c "\.overlay(liquidGlassEffectLayer(shape:" Islet/Notch/NotchPillView.swift && xcodebuild build -scheme Islet -configuration Debug 2>&1 | tail -20 && xcodebuild test -scheme Islet -destination 'platform=macOS' 2>&1 | tail -60</automated>
  </verify>
  <done>Structural grep (`\.overlay(liquidGlassEffectLayer(shape:` pattern) confirms 5, Debug build succeeds, full test suite (expected ~60-120s runtime — phase-closing gate, not a per-task fast check) shows no new failures vs. the documented baseline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final on-device UAT — full phase closing gate</name>
  <what-built>OSD bars now show glass on their fixed-hue fill (Task 1, guarded, legacy fallback unchanged), and the phase's structural/test invariants are confirmed (Task 2). This is the phase's final, comprehensive on-device sweep across everything built in Plans 72.1.1-01 through 06, per UI-SPEC's "On-Device UAT Gate" checklist.</what-built>
  <how-to-verify>
    Work through UI-SPEC's full checklist on the macOS 26 device:
    1. Island shell: glass reads across the WHOLE surface (pill/expanded/wings), not confined to a rim/edge (D-01).
    2. Island shell: text/icons/album art stay fully legible at all times (D-02/D-03).
    3. Transition continuity: no flat-black/flat-color frame during collapse<->expand morph (D-04).
    4. Transition continuity: no flat-black/flat-color frame during tab switches (D-04).
    5. Transition continuity: no regression to the "whole island permanently frosted" failure mode.
    6. Every island state shows glass: collapsed pill, mid-animation, compact expanded, full expanded (D-03).
    7. No cursor/pointer-follow distortion anywhere (D-05 — explicit negative check).
    8. Legacy (<macOS 26) fallback independently satisfies full coverage + legibility (forced-branch spot-check, or trust Plan 72.1.1-03's Task 3 result if unchanged since).
    9. Quick Action buttons: glass applied, hover-brightness-step (D-11) still visually distinct, disabled-dim still visually distinct.
    10. View-switcher tabs: selected vs. unselected state remains unambiguous after glass treatment.
    11. Calendar event cards: text/time/color-dot fully legible with glass applied.
    12. Weather widget: card background + glass applied, 44pt icon and 32pt temperature readout fully legible.
    13. Tray file tiles: tile background + glass applied, delete badge stays visually distinct on top.
    14. OSD bars: glass applied on top of the current always-full-opacity fill, green/orange hue unchanged.
    15. Confirm `grep -c "\.overlay(liquidGlassEffectLayer(shape:" Islet/Notch/NotchPillView.swift` still returns exactly 5 (already confirmed automated in Task 2 — visually spot-check nothing looks like a stray 6th shell instance).
  </how-to-verify>
  <resume-signal>Type "approved" (closes the phase) or list which checklist item(s) failed, with enough detail (which element, what was wrong) to route to a follow-up gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Local SwiftUI rendering change only — no new input, network, or IPC surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|-----------------|
| T-72.1.1-11 | Tampering | Stale-key regression check (`debugOSDOpacity` grep) | mitigate | Task 1 requires re-confirming the Phase 72.1 opacity-ramp mechanic is genuinely absent before implementing D-07's simplified form — prevents silently building against a stale premise (RESEARCH.md Pitfall 4). |
| T-72.1.1-12 | Repudiation (traceability) | Phase-wide structural/test regression | mitigate | Task 2's grep + full XCTest suite run is the automated closing gate, catching any accidental new shell call site or unrelated regression before the final on-device checkpoint. |
| T-72.1.1-15 | Tampering (build integrity) | Unconditional `.glassEffect()` at a 15.0 deployment target | mitigate | Task 1 wraps the new `.glassEffect()` call in `if #available(macOS 26.0, *) { } else { }`, matching this file's existing convention (`:750`, `:4561`) — prevents a compile failure on the declared deployment target and a Release-build regression. |

No ASVS-relevant findings — this phase touches only local UI rendering (RESEARCH.md Security Domain: "Not applicable").
</threat_model>

<verification>
`grep -c "debugOSDOpacity"` returns 0 before Task 1's edit. Debug and Release builds succeed. `grep -c "\.overlay(liquidGlassEffectLayer(shape:"` returns 5. Full XCTest suite shows no new failures vs. `.planning/deferred-items.md` (Task 2's automated verify runs ~60-120s — this is the phase-closing gate, not a per-task fast check). Final on-device UAT (Task 3) approved.
</verification>

<success_criteria>
D-07 (OSD bars, simplified) is implemented with a guarded `.glassEffect()` call and an unchanged legacy fallback, the phase's structural invariant (exactly 4 shell call sites) and test suite are confirmed clean, and the full UI-SPEC On-Device UAT Gate checklist is approved — closing Phase 72.1.1.
</success_criteria>

<output>
Create `.planning/phases/72.1.1-liquid-glass-design-rework-for-the-island-reference-componen/72.1.1-06-SUMMARY.md` when done
</output>
