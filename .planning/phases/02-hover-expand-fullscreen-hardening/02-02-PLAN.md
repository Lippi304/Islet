---
phase: 02-hover-expand-fullscreen-hardening
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - Islet/Notch/NotchPillView.swift
  - Islet/Notch/NotchShape.swift
autonomous: true
requirements: [ISL-04]
must_haves:
  truths:
    - "The pill renders a collapsed layout and an expanded date/time layout driven by one isExpanded flag"
    - "The collapsed↔expanded transition is a single matchedGeometryEffect morph (corner radius + frame interpolate), NOT a cross-fade"
    - "The expanded layout shows a small date/time readout (Phase-2 placeholder) and is only modestly larger than the notch"
    - "Hovering applies a subtle bounce/scale to the pill without expanding it"
    - "When idle and collapsed (release build), the pill is pure black, static, with no animation driver"
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "collapsed↔expanded morph via matchedGeometryEffect + shared @Namespace, bound to NotchInteractionState, with the date/time placeholder and hover bounce"
      contains: "matchedGeometryEffect"
    - path: "Islet/Notch/NotchShape.swift"
      provides: "the morphing pill silhouette whose radii/frame animate (unchanged path math; may expose animatable radii)"
      contains: "struct NotchShape"
  key_links:
    - from: "Islet/Notch/NotchPillView.swift"
      to: "Islet/Notch/NotchInteractionState.swift"
      via: "@ObservedObject / @EnvironmentObject NotchInteractionState driving isExpanded + isHovering"
      pattern: "NotchInteractionState"
    - from: "Islet/Notch/NotchPillView.swift"
      to: "withAnimation(.spring(...))"
      via: "matchedGeometryEffect on a shared @Namespace (single morph, no cross-fade)"
      pattern: "matchedGeometryEffect"
---

<objective>
Build the SwiftUI morph (ISL-04): turn the static Phase-1 pill into a collapsed↔expanded
Dynamic-Island morph driven by `NotchInteractionState.isExpanded`, using a single
`matchedGeometryEffect` on a shared `@Namespace` (NO cross-fade), with a compact date/time
placeholder as the expanded target (D-05) and a subtle hover bounce (D-01 visual half — the
haptic + the actual monitor are Plan 03).

This plan is the VIEW LAYER only. It does NOT touch NotchPanel, NotchWindowController, or any
event monitor — it binds to the `NotchInteractionState` ObservableObject from Plan 02-01 and
animates off its published `phase`. Plan 03 owns the controller that drives that state from the
real pointer monitor and sizes the panel to the expanded frame.

Purpose: give the morph a visible expanded target so the spring has something to animate to, and
prove the matchedGeometryEffect morph (the single most important "liquid island" trick per
CLAUDE.md) in isolation before wiring the focus-safe input in Plan 03.

Output:
- `NotchPillView.swift`: bound to `NotchInteractionState`; collapsed layout (the existing black
  NotchShape) and expanded layout (NotchShape grown + a small date/time `Text`) sharing ONE
  `@Namespace` via `matchedGeometryEffect(id:in:)`; a `.scaleEffect` hover bounce keyed off
  `isHovering`; all driven by `withAnimation(.spring(...))` at the binding.
- `NotchShape.swift`: unchanged path math; confirm its `topCornerRadius`/`bottomCornerRadius` are
  plain animatable `CGFloat`s so SwiftUI interpolates them during the morph.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-hover-expand-fullscreen-hardening/02-CONTEXT.md
@.planning/phases/02-hover-expand-fullscreen-hardening/02-RESEARCH.md
@.planning/phases/02-hover-expand-fullscreen-hardening/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 02-01 (Islet/Notch/NotchInteractionState.swift) — consume these verbatim. -->
```swift
enum InteractionPhase: Equatable { case collapsed, hovering, expanded }
final class NotchInteractionState: ObservableObject {
    @Published var phase: InteractionPhase = .collapsed
    var isExpanded: Bool { phase == .expanded }
    var isHovering: Bool { phase == .hovering || phase == .expanded }
}
```

<!-- Current NotchPillView.swift (Phase 1 — STATIC, you are replacing the body): -->
```swift
import SwiftUI
struct NotchPillView: View {
    var body: some View {
        NotchShape().fill(fillColor).offset(y: devOffset)
    }
    private var fillColor: Color {       // DEBUG: Color.red.opacity(0.6); RELEASE: Color.black
        #if DEBUG ... #else ... #endif
    }
    private var devOffset: CGFloat {     // DEBUG: 8 ; RELEASE: 0
        #if DEBUG ... #else ... #endif
    }
}
```

<!-- Current NotchShape.swift — radii are plain CGFloats, already animatable: -->
```swift
struct NotchShape: Shape {
    var topCornerRadius: CGFloat = 6
    var bottomCornerRadius: CGFloat = 14
    func path(in rect: CGRect) -> Path { /* asymmetric hanging-pill path */ }
}
```
Note: the `#if DEBUG` red tint + `devOffset: 8` are the Phase-1 dev-visibility affordance
(D-02). KEEP that pattern for the COLLAPSED state so the pill is still visible during dev; the
EXPANDED state is inherently visible (it has the date/time + larger frame).
</interfaces>

<spring_starting_values>
From RESEARCH Pattern 4 (tune on-device in Plan 05, these are the seeds — D-07 "snappy & playful
with slight bounce"):
```swift
withAnimation(.spring(response: 0.35, dampingFraction: 0.65)) { ... }   // snappy + slight overshoot
// hover bounce scale: ~1.06 collapsed→hovering, same spring.
```
Do NOT use `.transition(.opacity)` or two cross-faded subtrees — D-07/ISL-04 forbid cross-fade.
Do NOT add CALayer / Core Animation — CLAUDE.md forbids it; SwiftUI gives the morph.
</spring_starting_values>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bind NotchPillView to NotchInteractionState and add the expanded date/time layout</name>
  <read_first>
    - Islet/Notch/NotchPillView.swift (FULL — you are rewriting the body; preserve the #if DEBUG tint + devOffset pattern for the COLLAPSED state, D-02)
    - Islet/Notch/NotchShape.swift (FULL — confirm radii are plain CGFloat; do NOT change the path math)
    - Islet/Notch/NotchInteractionState.swift (FULL — from Plan 02-01; the ObservableObject you bind to)
    - .planning/phases/02-hover-expand-fullscreen-hardening/02-CONTEXT.md (D-05 date/time placeholder; D-06 compact; D-08 idle static)
    - .planning/phases/02-hover-expand-fullscreen-hardening/02-RESEARCH.md (Pattern 4 morph skeleton + Pattern 4 panel-resize note: expanded-sized window so content isn't clipped — Plan 03 sizes the panel; here just build the content for the expanded size)
    - CLAUDE.md (§ "Animation approach" — matchedGeometryEffect + shared @Namespace + spring; § "What NOT to Use" — no CALayer, no cross-fade)
  </read_first>
  <files>Islet/Notch/NotchPillView.swift</files>
  <action>
    Rewrite `NotchPillView` to take a `@ObservedObject var interaction: NotchInteractionState`
    (add an initializer; the controller in Plan 03 will own the instance and pass it in via
    `NSHostingView(rootView: NotchPillView(interaction: state))`). For preview/back-compat, give
    NO default that breaks the hosting-view call — the controller always injects it.

    Layout contract (D-05/D-06):
    - The view fills a FIXED expanded-sized container (the panel is sized to the expanded frame in
      Plan 03, so the morph never clips — RESEARCH Pattern 4 note). Use a top-leading/top-centered
      container so the collapsed pill sits flush at the top edge over the notch and the expanded
      content grows DOWNWARD from the notch.
    - COLLAPSED (`!interaction.isExpanded`): the existing `NotchShape().fill(collapsedFill)` at the
      collapsed notch size, carrying `.matchedGeometryEffect(id: "island", in: ns)`. Keep the
      `#if DEBUG` red tint (collapsed) + devOffset so dev visibility (D-02) is preserved; release =
      `Color.black`.
    - EXPANDED (`interaction.isExpanded`): a `NotchShape(topCornerRadius:bottomCornerRadius:)` (you
      MAY use slightly larger bottom radius for the bigger blob — tune in Plan 05) filled black,
      grown to the compact expanded size (D-06: only modestly larger than the notch, e.g. ~360×72
      as a seed — match the expandedSize Plan 03 will pass to `expandedNotchFrame`), carrying the
      SAME `.matchedGeometryEffect(id: "island", in: ns)`, with a small date/time readout overlaid:
      ```swift
      Text(Date.now, format: .dateTime.hour().minute())   // compact time; D-05 placeholder
      ```
      (a single small white `Text`, or HStack of time + short date — keep it COMPACT, D-06). The
      date/time is a static placeholder; real activity content is Phase 3+ — add a `// D-05:
      Phase-2 placeholder only — real activity content arrives Phase 3+` comment.
    - Use ONE shared `@Namespace private var ns`. The collapsed and expanded blobs MUST share the
      same `matchedGeometryEffect(id: "island", in: ns)` so SwiftUI MORPHS the single shape
      (corner radius + frame interpolate) instead of cross-fading two views.
    - Hover bounce (D-01 visual): `.scaleEffect(interaction.isHovering && !interaction.isExpanded ? 1.06 : 1.0)` on the collapsed blob (subtle "you're in" — the haptic is Plan 03).

    Do NOT drive any animation from inside this view (no internal `withAnimation`, no timer, no
    `onAppear` animation) — the animation is applied at the state MUTATION in Plan 03's controller
    (`withAnimation(.spring(response: 0.35, dampingFraction: 0.65)) { state.phase = ... }`), and
    SwiftUI animates the dependent `matchedGeometryEffect`/`scaleEffect` automatically. This keeps
    the idle/collapsed pill provably static (D-08) — no driving clock in the view.

    Keep the file SwiftUI-only (`import SwiftUI`); no AppKit.
  </action>
  <verify>
    <automated>xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "matchedGeometryEffect(id: \"island\"" Islet/Notch/NotchPillView.swift` returns ≥2 (the SAME id on both the collapsed and expanded blob — a single morph, not a cross-fade)
    - `grep -n "@Namespace" Islet/Notch/NotchPillView.swift` matches exactly one namespace declaration
    - `grep -n "NotchInteractionState" Islet/Notch/NotchPillView.swift` matches (the view binds to the Plan 02-01 ObservableObject) AND `grep -n "@ObservedObject\|@EnvironmentObject" Islet/Notch/NotchPillView.swift` matches
    - `grep -n "isExpanded" Islet/Notch/NotchPillView.swift` matches (collapsed vs expanded branch driven by the state)
    - `grep -n "Date.now\|Date()" Islet/Notch/NotchPillView.swift` matches AND a `Text(` with `.dateTime` is present (D-05 date/time placeholder)
    - `grep -n "scaleEffect" Islet/Notch/NotchPillView.swift` matches with `isHovering` (D-01 hover bounce)
    - `grep -c "import AppKit\|import Cocoa" Islet/Notch/NotchPillView.swift` returns 0 (SwiftUI-only)
    - `grep -c "transition(.opacity\|\.opacity(" Islet/Notch/NotchPillView.swift` does NOT introduce a cross-fade between two island subtrees (no `.transition(.opacity)`); grep for `transition(.opacity` returns 0
    - `grep -c "import QuartzCore\|CALayer\|CADisplayLink" Islet/Notch/NotchPillView.swift` returns 0 (no Core Animation — CLAUDE.md)
    - `xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS'` exits 0 (BUILD SUCCEEDED)
  </acceptance_criteria>
  <done>NotchPillView binds to NotchInteractionState and renders a collapsed black pill and a compact expanded date/time blob sharing one matchedGeometryEffect namespace; a hover scale bounce keys off isHovering; no cross-fade, no CALayer, no internal animation driver; the app builds.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm NotchShape morphs cleanly and add a SwiftUI preview proving the two layouts</name>
  <read_first>
    - Islet/Notch/NotchShape.swift (FULL — verify topCornerRadius/bottomCornerRadius are plain CGFloat stored properties so SwiftUI interpolates them across the morph; the asymmetric path must remain valid as radii/frame change)
    - Islet/Notch/NotchPillView.swift (the view from Task 1 you are previewing)
    - .planning/phases/02-hover-expand-fullscreen-hardening/02-RESEARCH.md (Pattern 4: the morph is the easy part; Pitfall 4: content clipped mid-animation if the container is too small — preview at the EXPANDED size)
    - .planning/phases/02-hover-expand-fullscreen-hardening/02-CONTEXT.md (D-06 compact expanded; D-08 idle static)
  </read_first>
  <files>Islet/Notch/NotchShape.swift, Islet/Notch/NotchPillView.swift</files>
  <action>
    Step A — NotchShape: verify (and only if necessary, adjust) that `topCornerRadius` and
    `bottomCornerRadius` are plain `var ... : CGFloat` stored properties (they are, per the
    current file) so SwiftUI's `Shape` animation interpolates them when the morph changes the
    radii/frame. The asymmetric hanging-pill `path(in:)` math is correct and MUST NOT be rewritten;
    if the expanded blob needs a different bottom radius, pass it via the existing initializer
    parameters from NotchPillView — do NOT fork the shape. Add a one-line comment noting the radii
    are animatable for the Phase-2 morph. If the shape is already correct, make NO code change to
    NotchShape.swift beyond that comment.

    Step B — Preview: add a `#Preview` (or `PreviewProvider`) to NotchPillView.swift that
    constructs a `NotchInteractionState`, shows the view in BOTH `.collapsed` and `.expanded`
    phases (two preview instances or a stateful toggle), at the EXPANDED container size, against a
    light background so the black blob is visible. This is a build-time correctness artifact: it
    proves both layouts compile and render without forcing the agent to run the app. Wrap the
    preview in `#if DEBUG` so it never ships in release.

    Step C — build: `xcodegen generate` (picks up any new file refs) then
    `xcodebuild build -scheme Islet -destination 'platform=macOS'` to confirm the preview + shape
    compile.
  </action>
  <verify>
    <automated>xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "var topCornerRadius: CGFloat" Islet/Notch/NotchShape.swift` and `grep -n "var bottomCornerRadius: CGFloat" Islet/Notch/NotchShape.swift` both match (plain animatable stored properties; unchanged)
    - NotchShape.swift `path(in:)` is byte-identical to its Phase-1 implementation except for at most an added comment line (the path math is NOT rewritten) — `git diff Islet/Notch/NotchShape.swift` shows only comment additions
    - `grep -n "#Preview\|PreviewProvider" Islet/Notch/NotchPillView.swift` matches
    - The preview block is guarded by `#if DEBUG` (grep shows `#if DEBUG` wrapping the preview)
    - The preview constructs a `NotchInteractionState` and shows both an expanded (`phase = .expanded`) and a collapsed instance
    - `xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS'` exits 0 (BUILD SUCCEEDED)
  </acceptance_criteria>
  <done>NotchShape's radii are confirmed animatable (path math untouched); a DEBUG-only preview renders both the collapsed pill and the expanded date/time blob, proving both layouts compile; the app builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | Pure SwiftUI view + Shape. No window, no event monitor, no input parsing, no I/O, no persistence, no network. The date/time placeholder reads only `Date.now` (local clock, public). The focus/event-hijacking surface is entirely in Plan 03 (the panel + monitor). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | The date/time readout (D-05 placeholder) | accept | Displays only the local system clock — no PII, no secrets, no remote data. Severity LOW — below HIGH block threshold. |
| T-02-04 | Denial of Service | A runaway animation / driving clock in the view | mitigate | The view holds NO timer / no internal `withAnimation` / no `onAppear` animation (D-08 idle-static); animation is applied only at the controller's state mutation (Plan 03). Acceptance criteria assert no Core Animation and no transition cross-fade, keeping the idle pill provably static and CPU near zero. |

No network, auth, user input, or persistence in this plan → STRIDE Spoofing/Repudiation/Tampering/Elevation are not applicable to a pure view layer.
</threat_model>

<verification>
- `xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS'` exits 0.
- `xcodebuild test -scheme Islet -destination 'platform=macOS'` (full suite) remains green — this
  plan changes no pure logic, so all Plan 02-01 + Phase-1 tests stay green.
- The collapsed and expanded island share ONE matchedGeometryEffect id (single morph, no
  cross-fade); no CALayer / Core Animation; no internal animation driver (idle-static, D-08).
- NotchShape path math is unchanged (only an animatability comment may be added).
</verification>

<success_criteria>
- ISL-04: the pill morphs collapsed↔expanded via a single matchedGeometryEffect on a shared
  @Namespace, driven by NotchInteractionState.isExpanded — ready for Plan 03 to flip inside
  `withAnimation(.spring(...))`.
- D-05: the expanded state shows a compact date/time placeholder.
- D-06: the expanded layout is only modestly larger than the notch (compact, ~360×72 seed).
- D-01 (visual half): a subtle hover scale bounce keys off isHovering.
- D-08: the collapsed/idle pill has no animation driver and stays static.
- A DEBUG preview proves both layouts compile and render.
</success_criteria>

<output>
After completion, create `.planning/phases/02-hover-expand-fullscreen-hardening/02-02-SUMMARY.md`
documenting: the final NotchPillView initializer signature (so Plan 03 instantiates it correctly
in NSHostingView), the chosen expanded size seed (so Plan 03 passes the matching expandedSize to
expandedNotchFrame), the matchedGeometryEffect id used, and confirmation NotchShape's path math
was left unchanged.
</output>
