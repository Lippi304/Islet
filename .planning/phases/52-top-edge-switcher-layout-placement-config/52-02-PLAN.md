---
phase: 52
plan: 02
type: execute
wave: 2
depends_on: ["52-01"]
files_modified: ["Islet/Notch/NotchPillView.swift", "Islet/Notch/NotchWindowController.swift", "IsletTests/NotchPillViewTests.swift"]
autonomous: true
requirements: [SWITCH-03, SWITCH-04]

must_haves:
  truths:
    - "The pill switcher's left-to-right icon order is driven by the 4 slot @AppStorage values (D-03) — reassigning a slot in Settings reorders switcherRow immediately, no relaunch"
    - "In top-edge layout mode, the pill row is fully absent and the expanded content area's height is unchanged from pill mode (D-06) — only the +switcherRowHeight addition is removed"
    - "In top-edge layout mode, 2 navCircleButtons render left of the camera cutout and 2 render right, reading the same 4 slot values as the pill (D-03), reusing navCircleButton verbatim (D-04/D-05)"
    - "Pill-mode presentations (Home/Tray/Calendar/Weather/NowPlaying/Onboarding) show zero regression: tabWidth/tabHeight, shelfStripVisible, and testTabWidthHeightMatchesKnownPerCaseValues all still pass unmodified"
    - "NotchWindowController's click-through hit-test rect (visibleContentZone) shrinks in lockstep with the rendered content when top-edge mode removes the pill row — no dead-zone/phantom-click regression (CR-01 discipline)"
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "data-driven switcherRow, topEdgeSwitcherRow, layout-aware totalHeight/blobShape height math"
      contains: "private var topEdgeSwitcherRow"
    - path: "Islet/Notch/NotchWindowController.swift"
      provides: "visibleContentZone() switcherHeight term gated on switcherLayout == .pill"
      contains: "switcherRowShowing && layout == .pill"
  key_links:
    - from: "Islet/Notch/NotchPillView.swift switcherRow/topEdgeSwitcherRow"
      to: "Islet/Notch/ViewSwitcherState.swift orderedSlotIcons(...)"
      via: "shared orderedSlotViews computed property"
      pattern: "orderedSlotIcons\\("
    - from: "Islet/Notch/NotchPillView.swift totalHeight"
      to: "Islet/Notch/NotchWindowController.swift visibleContentZone()"
      via: "switcherLayout == .pill gate mirrored in both (three-site rule)"
      pattern: "== \\.pill"
---

<objective>
Make the pill switcher's icon order data-driven from the 4 slot preferences (D-03), fix the `blobShape`/outer-frame/`visibleContentZone` "three-site" height math so top-edge mode removes exactly the pill row's height and nothing else (D-06, RESEARCH.md Pitfall 1), and add the top-edge icon row itself (D-04/D-05, SWITCH-03).

Purpose: this is the phase's core rendering work — everything Plan 52-01 defined as data now becomes visible behavior, without regressing the already-shipped pill mode or reintroducing the CR-01 click-through/dead-zone failure class.
Output: `NotchPillView.swift` renders both switcher layouts from one shared slot-order source; `NotchWindowController.swift`'s click-through geometry stays exactly in sync.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-top-edge-switcher-layout-placement-config/52-CONTEXT.md
@.planning/phases/52-top-edge-switcher-layout-placement-config/52-RESEARCH.md
@.planning/phases/52-top-edge-switcher-layout-placement-config/52-PATTERNS.md
@.planning/phases/52-top-edge-switcher-layout-placement-config/52-UI-SPEC.md
</context>

<interfaces>
<!-- New symbols Plan 52-01 landed — use these directly, no exploration needed. -->

From `Islet/Notch/ViewSwitcherState.swift` (post-52-01):
```swift
enum SelectedView: String, Equatable, Hashable, CaseIterable {
    case home, tray, calendar, weather
}
func orderedSlotIcons(leftOuter: SelectedView, leftInner: SelectedView, rightInner: SelectedView, rightOuter: SelectedView) -> [SelectedView]
```

From `Islet/ActivitySettings.swift` (post-52-01):
```swift
enum SwitcherLayout: String, CaseIterable { case pill, topEdge }   // bare alias for ActivitySettings.SwitcherLayout
static let switcherLayoutKey = "switcher.layout"
static let switcherSlotLeftOuterKey = "switcher.slot.leftOuter"
static let switcherSlotLeftInnerKey = "switcher.slot.leftInner"
static let switcherSlotRightInnerKey = "switcher.slot.rightInner"
static let switcherSlotRightOuterKey = "switcher.slot.rightOuter"
```

From `Islet/Notch/NotchGeometry.swift` (post-52-01):
```swift
func topEdgeCutoutGap(screenWidth: CGFloat, safeAreaTop: CGFloat, auxLeftWidth: CGFloat?, auxRightWidth: CGFloat?, widthFudge: CGFloat = 4) -> CGFloat
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Data-driven switcherRow (D-03)</name>
  <files>Islet/Notch/NotchPillView.swift, IsletTests/NotchPillViewTests.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift (lines 116-146 the @ObservedObject/@AppStorage declaration block incl. `weatherStyle` at line 124 — the exact pattern to mirror; lines 2034-2057 the current hardcoded `switcherRow`)
    Islet/Notch/ViewSwitcherState.swift (post-52-01 state)
    Islet/ActivitySettings.swift (post-52-01 state)
    IsletTests/NotchPillViewTests.swift (full file, esp. `testTabWidthHeightMatchesKnownPerCaseValues`'s `UserDefaults.standard` save/restore/defer idiom, lines 86-95 — reuse this exact idiom for slot-key overrides)
  </read_first>
  <behavior>
    - A fresh `NotchPillView` (no UserDefaults overrides) reads `orderedSlotViews == [.home, .tray, .calendar, .weather]` (SWITCH-04 default, zero regression vs. today's hardcoded order)
    - Overriding the 4 slot keys in `UserDefaults.standard` (e.g. leftOuter=.weather, leftInner=.calendar, rightInner=.tray, rightOuter=.home) makes `orderedSlotViews` reflect that override exactly
    - `switcherRow` renders exactly 4 `navCircleButton`s regardless of slot assignment (no conditional child count — Phase 45 structural-identity rule)
  </behavior>
  <action>
    Add 4 new `@AppStorage` vars to `NotchPillView` directly below the existing `weatherStyle` declaration (line 124), one per slot key from the Interfaces block: `slotLeftOuter: SelectedView = .home` (`switcherSlotLeftOuterKey`), `slotLeftInner: SelectedView = .tray` (`switcherSlotLeftInnerKey`), `slotRightInner: SelectedView = .calendar` (`switcherSlotRightInnerKey`), `slotRightOuter: SelectedView = .weather` (`switcherSlotRightOuterKey`) — this exact default set is SWITCH-04's locked default and matches today's `switcherRow` order byte-for-byte.

    Add an `internal` (not `private` — `NotchPillViewTests.swift` asserts it directly, same testability precedent as `shelfStripVisible`/`tabWidth`/`tabHeight`) computed property `var orderedSlotViews: [SelectedView]` that calls `orderedSlotIcons(leftOuter: slotLeftOuter, leftInner: slotLeftInner, rightInner: slotRightInner, rightOuter: slotRightOuter)` (the free function from Plan 52-01; deliberately named differently from that function so there is no identifier collision).

    Add a `private func icon(for view: SelectedView) -> (systemName: String, action: () -> Void)` that switches over `view` and returns the exact 4 `(systemName, action)` pairs `switcherRow` currently hardcodes (lines 2043-2054): `.home` -> `("house.fill", { onSwitcherSelect(.home) })`, `.tray` -> `("tray.fill", { onSwitcherSelect(.tray) })`, `.calendar` -> `("calendar", { onSwitcherSelect(.calendar) })`, `.weather` -> `("cloud.sun.fill", { onSwitcherSelect(.weather) })`. This is extracted once so Task 3's `topEdgeSwitcherRow` reuses it verbatim (D-03: one shared source feeds both layouts).

    Replace `switcherRow`'s body (lines 2041-2057) with an `HStack(spacing: 8)` containing `ForEach(orderedSlotViews, id: \.self)`, where each iteration reads `icon(for: view)` and calls `navCircleButton(systemName:filled:action:)` with `filled: viewSwitcherState.selectedView == view` — always exactly 4 `ForEach` children regardless of slot assignment (no `AnyView`, no conditional count).

    Add a new test to `IsletTests/NotchPillViewTests.swift` covering the two Behavior cases above (default order + override), using the exact `UserDefaults.standard` save/restore/`defer` idiom `testTabWidthHeightMatchesKnownPerCaseValues` already established for `weatherStyleKey`, applied to all 4 slot keys.
  </action>
  <acceptance_criteria>
    - `grep -c "@AppStorage(ActivitySettings.switcherSlotLeftOuterKey)" Islet/Notch/NotchPillView.swift` == 1 (and 1 each for the other 3 slot keys)
    - `grep -c "var orderedSlotViews: \[SelectedView\]" Islet/Notch/NotchPillView.swift` == 1
    - `grep -c "ForEach(orderedSlotViews, id: \\\\.self)" Islet/Notch/NotchPillView.swift` == 1
    - `grep -c "func testOrderedSlotViews" IsletTests/NotchPillViewTests.swift` -ge 2
    - `cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild test -scheme Islet -only-testing:IsletTests/NotchPillViewTests 2>&1 | tail -30` shows all tests passing
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild test -scheme Islet -only-testing:IsletTests/NotchPillViewTests 2>&1 | tail -30 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <done>switcherRow renders via ForEach over orderedSlotViews (always 4 children); orderedSlotViews defaults to [.home,.tray,.calendar,.weather] and reflects UserDefaults overrides; existing NotchPillViewTests (testShelfStripVisibleIsAlwaysFalse, testTabWidthHeightMatchesKnownPerCaseValues) remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Height-math three-site fix (Pitfall 1, D-06)</name>
  <files>Islet/Notch/NotchPillView.swift, Islet/Notch/NotchWindowController.swift, IsletTests/NotchPillViewTests.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift (lines 92-109 tabWidth/tabHeight — the "compute a value, don't branch the View" + internal-for-testing precedent to mirror; lines 973-1000 the outer body `.frame` height ternary being extracted; lines 1973-2032 `blobShape`)
    Islet/Notch/NotchWindowController.swift (lines 1439-1510 `visibleContentZone()`, esp. lines 1442-1443 the `switcherHeight` computation and line 1474's `ActivitySettings.WeatherStyle(rawValue: UserDefaults.standard.string(forKey:) ?? "") ?? .medium` safe-fallback read — the exact convention to mirror for reading `switcherLayout`)
    .planning/phases/52-top-edge-switcher-layout-placement-config/52-RESEARCH.md (Pitfall 1 — full detail on why this is a 3-site fix, not a 1-site fix)
  </read_first>
  <behavior>
    - For `.homeEmpty`, `.calendarExpanded`, and `.trayExpanded` presentations, `view.totalHeight` in `.pill` layout equals today's unmodified value; in `.topEdge` layout it equals exactly today's value minus `NotchPillView.switcherRowHeight` (44) — content height (`switcherContentHeight`/`trayContentHeight`) itself never changes
    - `blobShape`'s internal `baseHeight` (the content box) is identical in both layouts; only `totalHeight`'s `+switcherRowHeight` term and the `switcherRow` view's presence are gated by layout
  </behavior>
  <action>
    Add `@AppStorage(ActivitySettings.switcherLayoutKey) private var switcherLayout: SwitcherLayout = .pill` to `NotchPillView`, next to the slot vars added in Task 1.

    Give the private `blobShape` function a new parameter `switcherLayout: ActivitySettings.SwitcherLayout = .pill`, inserted after the existing `showSwitcher: Bool = false` parameter. Inside `blobShape`, leave `baseHeight`'s computation completely untouched (`height ?? (showSwitcher ? Self.switcherContentHeight : Self.expandedSize.height)`) — this is what keeps D-06's "content area keeps its current height/size" true in both layouts. Compute `let showsPillRow = showSwitcher && switcherLayout == .pill` and change `totalHeight`'s `+ (showSwitcher ? Self.switcherRowHeight : 0)` term to `+ (showsPillRow ? Self.switcherRowHeight : 0)`; change the VStack's `if showSwitcher { switcherRow }` to `if showsPillRow { switcherRow }`. Pass `switcherLayout: switcherLayout` at the ONE `tabContentView` call site (line 896) — do not add a second `blobShape` call site anywhere (Phase 45 structural-identity discipline, reiterated in RESEARCH.md's Anti-Patterns).

    Extract the outer `body`'s `.frame` height ternary (lines 993-999) into a new `internal` (not `private` — same testability precedent as `tabWidth`/`tabHeight`) computed property `var totalHeight: CGFloat` on `NotchPillView`, with identical branch structure but with each `+ Self.switcherRowHeight` term (both the `isTrayPresentation` branch and the `showsSwitcherRow` branch) additionally gated `&& switcherLayout == .pill`. Change `body`'s `.frame(width: ..., height: totalHeight, alignment: .top)` to read this new property instead of the inline ternary — mirrors `tabWidth`/`tabHeight`'s "compute a value, don't branch the View" convention exactly.

    In `Islet/Notch/NotchWindowController.swift`'s `visibleContentZone()`, read the layout mode the same safe-fallback way `weatherStyle` is already read at line 1474: `let layout = ActivitySettings.SwitcherLayout(rawValue: UserDefaults.standard.string(forKey: ActivitySettings.switcherLayoutKey) ?? "") ?? .pill`. Change `let switcherHeight = switcherRowShowing ? NotchPillView.switcherRowHeight : 0` (line 1443) to `let switcherHeight = (switcherRowShowing && layout == .pill) ? NotchPillView.switcherRowHeight : 0` — this single shared `switcherHeight` variable already feeds every downstream branch (trayExpanded/weatherExpanded/calendarExpanded/default), so no other line in the function needs to change.

    Do NOT touch `positionAndShow()`'s panel-frame union members (the `expandedFrame`/`trayFrame`/`weatherExpandedFrame`/`outputPanelExpandedFrame` reservations, lines ~1041-1098). These are static upper-bound reservations that already add `+ switcherRowHeight` unconditionally, and remain a valid (slightly oversized) reservation for top-edge mode — matching this codebase's established "size the panel once to the union of every possible content size" convention (the visible black shape/click-through zone alone decide the actual size, per the file's own existing doc comments).

    Add a new test to `IsletTests/NotchPillViewTests.swift` covering the Behavior case above: for `.homeEmpty`, `.calendarExpanded`, and `.trayExpanded`, construct views with `ActivitySettings.switcherLayoutKey` overridden to `"pill"` then `"topEdge"` (save/restore via `defer`, mirroring the existing `weatherStyleKey` idiom) and assert the `topEdge` value equals the `pill` value minus `NotchPillView.switcherRowHeight` exactly.
  </action>
  <acceptance_criteria>
    - `grep -c "var totalHeight: CGFloat" Islet/Notch/NotchPillView.swift` == 1
    - `grep -c "switcherLayout: ActivitySettings.SwitcherLayout = .pill" Islet/Notch/NotchPillView.swift` == 1
    - `grep -c "showsPillRow" Islet/Notch/NotchPillView.swift` -ge 2
    - `grep -c "switcherRowShowing && layout == .pill" Islet/Notch/NotchWindowController.swift` == 1
    - `grep -c "func testTotalHeight" IsletTests/NotchPillViewTests.swift` -ge 1
    - `cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -10` ends with `** BUILD SUCCEEDED **`
    - `cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodebuild test -scheme Islet -only-testing:IsletTests/NotchPillViewTests 2>&1 | tail -30` shows all tests passing
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -10 | grep -q "BUILD SUCCEEDED" && xcodebuild test -scheme Islet -only-testing:IsletTests/NotchPillViewTests 2>&1 | tail -30 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <done>blobShape/totalHeight both exclude +switcherRowHeight in top-edge mode while baseHeight/switcherContentHeight stay unchanged; NotchWindowController.visibleContentZone() mirrors the same gate; new totalHeight regression test passes; Debug build succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: topEdgeSwitcherRow view + wiring (D-04/D-05, SWITCH-03)</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift (lines 1895-1908 `navCircleDiameter`/`navCircleButton`; lines 605-609 `cameraClearance`; lines 1973-2032 `blobShape` in its post-Task-2 state)
    Islet/Notch/NotchGeometry.swift (post-52-01, `topEdgeCutoutGap`)
    Islet/Notch/DisplayResolver.swift, Islet/Notch/NSScreen+Notch.swift (`selectTargetScreen`/`ScreenDescriptor`/`.descriptor` — the exact independent-read pattern to reuse, no controller plumbing)
    .planning/phases/52-top-edge-switcher-layout-placement-config/52-UI-SPEC.md (Top-Edge Layout Geometry Contract)
    .planning/phases/52-top-edge-switcher-layout-placement-config/52-RESEARCH.md (Anti-Patterns — no `.offset`/`.position` in this shape/content stack)
  </read_first>
  <behavior>
    - `topEdgeSwitcherRow` renders exactly 4 `navCircleButton`s split 2-and-2 around a `Color.clear` center spacer, using `orderedSlotViews`/`icon(for:)` from Task 1 (same source as the pill, D-03)
    - The center spacer's width equals `topEdgeCutoutGap(...)`'s real formula output, never `auxLeftWidth + auxRightWidth`
    - `topEdgeSwitcherRow` only renders when `showSwitcher && switcherLayout == .topEdge`; it never coexists with `switcherRow` in the same frame
  </behavior>
  <action>
    Add a computed property `private var topEdgeCutoutWidth: CGFloat` that independently resolves the live built-in notched screen (no controller plumbing, per RESEARCH.md Pattern 2): `guard let target = selectTargetScreen(from: NSScreen.screens.map { $0.descriptor }) else { return 0 }; return topEdgeCutoutGap(screenWidth: target.frame.width, safeAreaTop: target.safeAreaTop, auxLeftWidth: target.auxLeftWidth, auxRightWidth: target.auxRightWidth)`.

    Add `private var topEdgeSwitcherRow: some View`, built from `orderedSlotViews`/`icon(for:)` (Task 1): an `HStack(spacing: 0)` containing 3 children — a leading `HStack(spacing: 8)` with `navCircleButton` calls for `orderedSlotViews[0]` and `orderedSlotViews[1]`, a `Color.clear.frame(width: topEdgeCutoutWidth)` center spacer, and a trailing `HStack(spacing: 8)` with `navCircleButton` calls for `orderedSlotViews[2]` and `orderedSlotViews[3]` — each `navCircleButton` call passes `filled: viewSwitcherState.selectedView == view` exactly like `switcherRow` does (D-05 falls out for free, no new selection-state logic). Give the outer `HStack` `.frame(height: Self.cameraClearance)`. Use plain `HStack(spacing: 0)` + `Color.clear.frame(width:)` — never `.offset(x:y:)`/`.position(x:y:)` (Phase 39's documented `GeometryReader` origin/size misreport lesson, reiterated in RESEARCH.md Anti-Patterns).

    Wire `topEdgeSwitcherRow` into `blobShape`'s content overlay (the `content()` call inside the `VStack` from Task 2's edited state): apply `.overlay(alignment: .top) { if showSwitcher && switcherLayout == .topEdge { topEdgeSwitcherRow } }` directly on the `content()` call — `content()` already reserves the `cameraClearance` band via its own `.padding(.top, Self.cameraClearance)`, so the top-edge icons render inside that already-reserved band with zero additional height reservation (D-06 preserved). Do NOT shrink `navCircleDiameter` (36pt) to force-fit the 42pt `cameraClearance` band (D-04 is locked verbatim reuse) — leave the tight ~3pt margin as-is; Plan 52-04's on-device checkpoint verifies the real physical fit (Pitfall 3).
  </action>
  <acceptance_criteria>
    - `grep -c "private var topEdgeSwitcherRow" Islet/Notch/NotchPillView.swift` == 1
    - `grep -c "private var topEdgeCutoutWidth" Islet/Notch/NotchPillView.swift` == 1
    - `grep -c "switcherLayout == .topEdge" Islet/Notch/NotchPillView.swift` -ge 1
    - `grep -c "topEdgeCutoutGap(" Islet/Notch/NotchPillView.swift` == 1
    - `awk '/private var topEdgeSwitcherRow/,/^    }$/' Islet/Notch/NotchPillView.swift | grep -c "\.offset(\|\.position("` == 0 (no offset/position primitives inside the new row)
    - `cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -10` ends with `** BUILD SUCCEEDED **`
    - `xcodebuild test -scheme Islet -only-testing:IsletTests/NotchPillViewTests -only-testing:IsletTests/NotchGeometryTests 2>&1 | tail -30` shows all tests passing (pre-existing regression tests untouched)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -10 | grep -q "BUILD SUCCEEDED" && xcodebuild test -scheme Islet -only-testing:IsletTests/NotchPillViewTests -only-testing:IsletTests/NotchGeometryTests 2>&1 | tail -30 | grep -q "TEST SUCCEEDED"</automated>
  </verify>
  <done>topEdgeSwitcherRow exists, reuses navCircleButton/orderedSlotViews/icon(for:) verbatim, uses topEdgeCutoutGap(...) for its center spacer, uses no offset/position primitives, and renders only when switcherLayout == .topEdge inside blobShape's existing cameraClearance band; Debug build succeeds; all pre-existing NotchPillViewTests/NotchGeometryTests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| UserDefaults (app-owned prefs) | `switcher.layout` read directly by `NotchWindowController` (AppKit layer, no SwiftUI `@AppStorage` property wrapper) — a manual `UserDefaults.standard.string(forKey:)` read, same trust tier and same manual-read pattern this file already uses for `weatherStyleKey` at line 1474. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-02 | Tampering | `NotchWindowController.visibleContentZone()`'s manual `ActivitySettings.SwitcherLayout(rawValue:)` read | mitigate | `?? .pill` fallback on a corrupted/missing stored value (identical convention to the pre-existing `weatherStyleKey` read one line below it), verified by this task's build gate — a decode failure here degrades to the existing, already-shipped pill-mode click-through geometry, never a crash or an out-of-bounds hit-test rect. |
</threat_model>

<verification>
Task 1 gates on the switcherRow-reorder tests plus a Debug build. Task 2 gates on the new totalHeight regression test (both layouts, 3 representative presentations) plus a Debug build. Task 3 gates on a build-only check (the physical fit/visual clearance itself is a Manual-Only Verification per 52-VALIDATION.md, deferred to Plan 52-04's on-device checkpoint) plus a full re-run of all pre-existing NotchPillViewTests/NotchGeometryTests to confirm zero regression.
</verification>

<success_criteria>
- switcherRow's icon order derives from the 4 slot @AppStorage values (D-03), defaulting to today's exact order.
- Top-edge mode's totalHeight excludes switcherRowHeight while its content-box height stays identical to pill mode (D-06).
- NotchWindowController's click-through zone shrinks in lockstep with the rendered content in top-edge mode.
- topEdgeSwitcherRow exists, reuses navCircleButton verbatim, uses the correct cutout-gap formula, and never uses `.offset`/`.position`.
- All pre-existing regression tests (testShelfStripVisibleIsAlwaysFalse, testTabWidthHeightMatchesKnownPerCaseValues, every NotchGeometryTests case) remain green.
</success_criteria>

<output>
Create `.planning/phases/52-top-edge-switcher-layout-placement-config/52-02-SUMMARY.md` when done
</output>
