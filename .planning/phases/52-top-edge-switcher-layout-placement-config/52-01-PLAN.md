---
phase: 52
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["Islet/Notch/ViewSwitcherState.swift", "Islet/ActivitySettings.swift", "Islet/Notch/NotchGeometry.swift", "IsletTests/NotchPillViewTests.swift", "IsletTests/ActivitySettingsTests.swift", "IsletTests/NotchGeometryTests.swift"]
autonomous: true
requirements: [SWITCH-03, SWITCH-04]

must_haves:
  truths:
    - "SelectedView round-trips through @AppStorage (UserDefaults) via its String raw value, with a corrupted/unknown stored value falling back safely rather than crashing (V5)"
    - "orderedSlotIcons(leftOuter:leftInner:rightInner:rightOuter:) returns the 4 slot values in left-to-right order, and the SWITCH-04 default (Home+Tray left, Calendar+Weather right) produces [.home, .tray, .calendar, .weather] — byte-identical to today's pill order, zero regression"
    - "A SwitcherLayout preference (.pill default, .topEdge alternate) and 4 independent per-slot icon preferences exist as @AppStorage-backed keys, following ActivitySettings' one-key-per-value convention (not an encoded array)"
    - "topEdgeCutoutGap(...) computes the real camera-cutout width (screenWidth - auxLeftWidth - auxRightWidth + widthFudge), never the sum of the two side strips"
  artifacts:
    - path: "Islet/Notch/ViewSwitcherState.swift"
      provides: "SelectedView: String, Equatable, Hashable, CaseIterable + orderedSlotIcons(...) pure function"
      contains: "enum SelectedView: String"
    - path: "Islet/ActivitySettings.swift"
      provides: "SwitcherLayout enum + switcherLayoutKey + 4 slot @AppStorage keys"
      contains: "enum SwitcherLayout: String, CaseIterable"
    - path: "Islet/Notch/NotchGeometry.swift"
      provides: "topEdgeCutoutGap(...) pure function"
      contains: "func topEdgeCutoutGap"
  key_links:
    - from: "Islet/ActivitySettings.swift switcherSlot*Key constants"
      to: "Islet/Notch/ViewSwitcherState.swift SelectedView(rawValue:)"
      via: "@AppStorage round-trip (consumed by Plans 52-02/52-03)"
      pattern: "SelectedView\\(rawValue:"
---

<objective>
Establish the pure data/config contracts Plans 52-02 (NotchPillView rendering) and 52-03 (Settings UI) both build against: `SelectedView` becomes `@AppStorage`-compatible, a `SwitcherLayout` enum + 4 slot keys are added to `ActivitySettings`, `orderedSlotIcons(...)` gives both the pill and the future top-edge row one shared left-to-right ordering source (D-03), and `topEdgeCutoutGap(...)` gives the top-edge row the correct camera-cutout width formula (RESEARCH.md Pitfall 2).

Purpose: interface-first ordering — these are the contracts later plans render against; landing them first means Plans 52-02/52-03 write against real types, not forward-declared ones.
Output: `Islet/Notch/ViewSwitcherState.swift`, `Islet/ActivitySettings.swift`, `Islet/Notch/NotchGeometry.swift` extended; matching unit tests in `IsletTests/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-top-edge-switcher-layout-placement-config/52-CONTEXT.md
@.planning/phases/52-top-edge-switcher-layout-placement-config/52-RESEARCH.md
@.planning/phases/52-top-edge-switcher-layout-placement-config/52-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SelectedView @AppStorage-compatible + orderedSlotIcons pure function</name>
  <files>Islet/Notch/ViewSwitcherState.swift, IsletTests/NotchPillViewTests.swift</files>
  <read_first>
    Islet/Notch/ViewSwitcherState.swift (full file, 18 lines — current `SelectedView: Equatable` enum)
    Islet/ActivitySettings.swift (lines 49-52, WeatherStyle's `: String, CaseIterable` convention — the pattern to mirror)
    IsletTests/NotchPillViewTests.swift (full file — existing test class shape/imports)
  </read_first>
  <behavior>
    - orderedSlotIcons(leftOuter: .home, leftInner: .tray, rightInner: .calendar, rightOuter: .weather) returns [.home, .tray, .calendar, .weather] (SWITCH-04 default, byte-identical to today's switcherRow order)
    - orderedSlotIcons(leftOuter: .weather, leftInner: .weather, rightInner: .home, rightOuter: .tray) returns [.weather, .weather, .home, .tray] — duplicates are allowed, no validation (Claude's Discretion, matches this codebase's no-Picker-validation convention)
    - SelectedView(rawValue: "home") == .home; SelectedView(rawValue: "corrupted") == nil (caller must apply a `?? .home` fallback at every read site, mirroring WeatherStyle's convention — enforced in Plans 52-02/52-03, not here)
  </behavior>
  <action>
    Change `enum SelectedView: Equatable { case home; case tray; case calendar; case weather }` to `enum SelectedView: String, Equatable, Hashable, CaseIterable { case home, tray, calendar, weather }`. This is additive and behavior-preserving: the only existing call sites compare via `==`, none pattern-match on `.rawValue`. Declare `Hashable` explicitly (not just implied by `Equatable`) so `ForEach(_:id: \.self)` compiles cleanly in Plan 52-02 without relying on implicit raw-value-enum synthesis.

    Below the enum, in the same file, add a free function `orderedSlotIcons(leftOuter: SelectedView, leftInner: SelectedView, rightInner: SelectedView, rightOuter: SelectedView) -> [SelectedView]` that returns `[leftOuter, leftInner, rightInner, rightOuter]`. This is the ONE shared ordering projection: Plan 52-02's pill `switcherRow` and top-edge `topEdgeSwitcherRow` both call it, so there is exactly one place that turns 4 independent slot values into a left-to-right array (D-03). Do not add any deduplication/validation logic — duplicate slot assignments are intentionally allowed (see Behavior above).

    Add a new test method to `IsletTests/NotchPillViewTests.swift` (plain assertion, no `NotchPillView` construction needed) asserting the two `orderedSlotIcons(...)` cases from Behavior above, plus a `SelectedView(rawValue:)` round-trip/corrupted-value test.
  </action>
  <acceptance_criteria>
    - `grep -c "enum SelectedView: String, Equatable, Hashable, CaseIterable" Islet/Notch/ViewSwitcherState.swift` == 1
    - `grep -c "func orderedSlotIcons" Islet/Notch/ViewSwitcherState.swift` == 1
    - `grep -c "func testOrderedSlotIcons" IsletTests/NotchPillViewTests.swift` -ge 1
    - `cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild test -scheme Islet -only-testing:IsletTests/NotchPillViewTests 2>&1 | tail -20` shows all tests passing (existing `testShelfStripVisibleIsAlwaysFalse`/`testTabWidthHeightMatchesKnownPerCaseValues` unmodified and still green)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild test -scheme Islet -only-testing:IsletTests/NotchPillViewTests 2>&1 | tail -30 | grep -q "** TEST SUCCEEDED **"</automated>
  </verify>
  <done>SelectedView is String/Equatable/Hashable/CaseIterable; orderedSlotIcons(...) exists and is tested for both the default order and a duplicate-assignment case; SelectedView(rawValue:) corrupted-value case returns nil; existing NotchPillViewTests still pass unmodified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ActivitySettings SwitcherLayout enum + switcher keys</name>
  <files>Islet/ActivitySettings.swift, IsletTests/ActivitySettingsTests.swift</files>
  <read_first>
    Islet/ActivitySettings.swift (full file — esp. lines 46-52 weatherStyleKey/WeatherStyle block and lines 136-140 the WeatherStyle/MaterialStyle typealias block, the exact patterns to mirror)
    IsletTests/ActivitySettingsTests.swift (full file — existing test class shape)
  </read_first>
  <behavior>
    - ActivitySettings.SwitcherLayout(rawValue: "pill") == .pill; (rawValue: "topEdge") == .topEdge; (rawValue: "corrupted") == nil
    - ActivitySettings.switcherLayoutKey == "switcher.layout"
    - ActivitySettings.switcherSlotLeftOuterKey == "switcher.slot.leftOuter" (and the analogous leftInner/rightInner/rightOuter keys)
  </behavior>
  <action>
    Inside `enum ActivitySettings`, add `enum SwitcherLayout: String, CaseIterable { case pill, topEdge }` and `static let switcherLayoutKey = "switcher.layout"`, following the exact shape of the existing `weatherStyleKey`/`WeatherStyle` block (lines 46-52). Add 4 plain `static let` String constants for the slot keys (NOT a nested enum — these are keys, not a value type): `switcherSlotLeftOuterKey = "switcher.slot.leftOuter"`, `switcherSlotLeftInnerKey = "switcher.slot.leftInner"`, `switcherSlotRightInnerKey = "switcher.slot.rightInner"`, `switcherSlotRightOuterKey = "switcher.slot.rightOuter"` — one independent `@AppStorage` key per slot (RESEARCH.md Don't Hand-Roll: never a single encoded array). Add `typealias SwitcherLayout = ActivitySettings.SwitcherLayout` near the existing `typealias WeatherStyle = ActivitySettings.WeatherStyle` / `typealias MaterialStyle = ActivitySettings.MaterialStyle` block (lines 136-140) so Plans 52-02/52-03 can write the bare `SwitcherLayout` name at their call sites, exactly like `WeatherStyle` already does.

    Add test methods to `IsletTests/ActivitySettingsTests.swift`: the 3 key-name assertions from Behavior above, plus `SwitcherLayout(rawValue:)` parsing `.pill`/`.topEdge` and returning nil for a corrupted value (mirrors `testMaterialStyleParsesGradient`/`testMaterialStyleParsesCorruptedValueToNil`'s existing shape).
  </action>
  <acceptance_criteria>
    - `grep -c "enum SwitcherLayout: String, CaseIterable" Islet/ActivitySettings.swift` == 1
    - `grep -c "static let switcherLayoutKey" Islet/ActivitySettings.swift` == 1
    - `grep -c "static let switcherSlot" Islet/ActivitySettings.swift` == 4
    - `grep -c "typealias SwitcherLayout = ActivitySettings.SwitcherLayout" Islet/ActivitySettings.swift` == 1
    - `cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild test -scheme Islet -only-testing:IsletTests/ActivitySettingsTests 2>&1 | tail -20` shows all tests passing
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild test -scheme Islet -only-testing:IsletTests/ActivitySettingsTests 2>&1 | tail -30 | grep -q "** TEST SUCCEEDED **"</automated>
  </verify>
  <done>SwitcherLayout enum + switcherLayoutKey + 4 slot keys + bare typealias all exist; ActivitySettingsTests covers key names and SwitcherLayout parsing/corrupted-fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: NotchGeometry topEdgeCutoutGap pure function</name>
  <files>Islet/Notch/NotchGeometry.swift, IsletTests/NotchGeometryTests.swift</files>
  <read_first>
    Islet/Notch/NotchGeometry.swift (full file — esp. `notchSize(...)`, lines 27-37)
    IsletTests/NotchGeometryTests.swift (full file — esp. `testNotchSizeWidthFormulaAndHeight`, lines 28-39, the exact input/output pair to reuse)
  </read_first>
  <behavior>
    - topEdgeCutoutGap(screenWidth: 1512, safeAreaTop: 38, auxLeftWidth: 612, auxRightWidth: 612, widthFudge: 4) == 292 (identical formula/inputs to testNotchSizeWidthFormulaAndHeight)
    - topEdgeCutoutGap(screenWidth: 1512, safeAreaTop: 38, auxLeftWidth: nil, auxRightWidth: 612) == 0 (no-notch/degenerate fallback, never nil/crash)
  </behavior>
  <action>
    Add a pure function directly below `notchSize(...)` in `Islet/Notch/NotchGeometry.swift`: `topEdgeCutoutGap(screenWidth: CGFloat, safeAreaTop: CGFloat, auxLeftWidth: CGFloat?, auxRightWidth: CGFloat?, widthFudge: CGFloat = 4) -> CGFloat`, implemented as `notchSize(screenWidth:safeAreaTop:auxLeftWidth:auxRightWidth:widthFudge:)?.width ?? 0`. This is the exact value (RESEARCH.md Pitfall 2) Plan 52-02's top-edge row must reserve as its center `Color.clear.frame(width:)` spacer — the real camera-cutout width, never `auxLeftWidth + auxRightWidth` (that sums the two strips beside the notch, not the notch itself). Add the 2 test cases from Behavior above to `IsletTests/NotchGeometryTests.swift`, placed near `testNotchSizeWidthFormulaAndHeight`/`testNotchSizeNilWhenAuxMissing`.
  </action>
  <acceptance_criteria>
    - `grep -c "func topEdgeCutoutGap" Islet/Notch/NotchGeometry.swift` == 1
    - `grep -c "func testTopEdgeCutoutGap" IsletTests/NotchGeometryTests.swift` -ge 2
    - `cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild test -scheme Islet -only-testing:IsletTests/NotchGeometryTests 2>&1 | tail -20` shows all tests passing (existing hasNotch/notchSize/notchFrame/expandedNotchFrame/wingsFrame tests unmodified and still green)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild test -scheme Islet -only-testing:IsletTests/NotchGeometryTests 2>&1 | tail -30 | grep -q "** TEST SUCCEEDED **"</automated>
  </verify>
  <done>topEdgeCutoutGap(...) exists, uses notchSize(...)'s existing formula (never auxLeftWidth+auxRightWidth directly), falls back to 0 on a missing-notch input; NotchGeometryTests covers both the real-width case and the nil-fallback case; all pre-existing NotchGeometryTests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| UserDefaults (app-owned prefs) | New `switcher.*` @AppStorage keys are local app preferences, same trust tier as existing `weather.style`/`theming.materialStyle` — no network, no IPC, no privilege boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | `SelectedView(rawValue:)` / `SwitcherLayout(rawValue:)` decoding a corrupted/manually-edited `UserDefaults` value (e.g. `defaults write`) | mitigate | Both types return `nil` on an unrecognized raw value (tested this plan); every READ site in Plans 52-02/52-03 applies a `?? .home`/`?? .pill` fallback, matching this codebase's existing `WeatherStyle`/`MaterialStyle` convention — enforced by those plans' acceptance criteria, not this one (this plan ships the decodable types, not the read sites). |
</threat_model>

<verification>
Each task gates on its own targeted `xcodebuild test` run plus grep assertions on the new symbols. No UI, no controller wiring — this plan is pure data/model/geometry-function additions, verifiable entirely by unit tests.
</verification>

<success_criteria>
- `SelectedView` is `@AppStorage`-compatible (String/Equatable/Hashable/CaseIterable) with zero behavior change at existing call sites.
- `orderedSlotIcons(...)` exists as the single shared slot-ordering source Plan 52-02 will consume for both switcher layouts.
- `ActivitySettings.SwitcherLayout` + `switcherLayoutKey` + 4 slot keys exist, following the one-key-per-value convention.
- `topEdgeCutoutGap(...)` exists and uses the verified `notchSize(...)` formula.
- All 3 tasks' new tests pass; no pre-existing test regresses.
</success_criteria>

<output>
Create `.planning/phases/52-top-edge-switcher-layout-placement-config/52-01-SUMMARY.md` when done
</output>
