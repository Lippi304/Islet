---
phase: 52
plan: 04
type: execute
wave: 3
depends_on: ["52-02", "52-03"]
files_modified: []
autonomous: false
requirements: [SWITCH-03, SWITCH-04]

must_haves:
  truths:
    - "Full XCTest suite passes (or Cmd-U confirms, per this project's documented headless-test-hang fallback) after Plans 52-01/52-02/52-03 land together"
    - "On real notched hardware, the 36pt navCircleButton visibly fits inside the 42pt cameraClearance band with no clipping (D-04, Pitfall 3)"
    - "On real notched hardware, the top-edge icons' cutout-gap spacing visually clears the physical camera housing (Pitfall 2)"
    - "Reassigning a slot in Settings reorders BOTH the pill and the top-edge layout immediately (D-03)"
    - "Switching Settings' Layout toggle between Pill and Top Edge live-updates the island with no relaunch, and pill-mode shows zero regression versus pre-Phase-52 behavior (SC#5)"
  artifacts: []
  key_links: []
---

<objective>
Run the full automated regression gate across all 3 prior plans landing together, then verify on real notched hardware the two things source code alone cannot confirm: whether the 36pt `navCircleButton` visually fits the 42pt `cameraClearance` band (D-04/Pitfall 3), and whether the computed cutout-gap spacing actually clears the physical camera housing (Pitfall 2) — plus a full walkthrough of ROADMAP Phase 52's 5 success criteria.

Purpose: Plans 52-01/52-02/52-03 are individually unit-tested but were never run together, and 2 of this phase's must-haves (D-04, Pitfall 2) are explicitly flagged in 52-VALIDATION.md as Manual-Only Verifications — no automated command can confirm them.
Output: Full green regression suite + a recorded on-device UAT verdict closing SWITCH-03/SWITCH-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-top-edge-switcher-layout-placement-config/52-CONTEXT.md
@.planning/phases/52-top-edge-switcher-layout-placement-config/52-VALIDATION.md
@.planning/phases/52-top-edge-switcher-layout-placement-config/52-02-SUMMARY.md
@.planning/phases/52-top-edge-switcher-layout-placement-config/52-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build + test regression gate</name>
  <files></files>
  <read_first>
    IsletTests/NotchPillViewTests.swift (post-52-02 state — testShelfStripVisibleIsAlwaysFalse, testTabWidthHeightMatchesKnownPerCaseValues, plus the new orderedSlotViews/totalHeight tests)
    IsletTests/NotchGeometryTests.swift (post-52-01 state — plus the new topEdgeCutoutGap tests)
    IsletTests/ActivitySettingsTests.swift (post-52-01 state — plus the new SwitcherLayout tests)
    IsletTests/SettingsViewTests.swift (post-52-03 state — visibleSections tests)
  </read_first>
  <action>
    Run the full test suite: `cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild test -scheme Islet 2>&1 | tail -80`. This project has a documented `xcodebuild test` headless-hang precedent (per STATE.md) — if the CLI run hangs or times out, fall back to a manual Cmd-U pass inside Xcode and record that fallback explicitly in the SUMMARY. Confirm specifically that the following remain green: `testShelfStripVisibleIsAlwaysFalse`, `testTabWidthHeightMatchesKnownPerCaseValues` (both pre-existing, unmodified), every `NotchGeometryTests` case (pre-existing plus the new `topEdgeCutoutGap` cases), the new `orderedSlotIcons`/`orderedSlotViews`/`totalHeight` tests from Plans 52-01/52-02, the new `SwitcherLayout` parsing tests from Plan 52-01, and the new `visibleSections(hasNotch:)` tests from Plan 52-03.

    Then run a Release build to confirm the Release configuration also compiles clean with the new code paths: `xcodebuild build -scheme Islet -configuration Release 2>&1 | tail -20` — this codebase's established Debug+Release dual-gate convention (e.g. Phase 51's Task 2).
  </action>
  <acceptance_criteria>
    - Full test suite run (CLI or Cmd-U-confirmed) shows zero failures across `NotchPillViewTests`, `NotchGeometryTests`, `ActivitySettingsTests`, `SettingsViewTests`
    - `xcodebuild build -scheme Islet -configuration Release 2>&1 | tail -20` ends with `** BUILD SUCCEEDED **`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -configuration Release 2>&1 | tail -20 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <done>Full test suite is green (CLI-confirmed or Cmd-U-confirmed with the fallback documented), Release build succeeds, and every regression test named above is confirmed passing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device UAT — both switcher layouts, fit/clearance, reorder propagation</name>
  <files></files>
  <action>
    No file edits — build and run the app on-device (Cmd-R in Xcode, on a real notched MacBook) and walk through the steps below. This task's only output is the recorded PASSED/FAILED verdict covering ROADMAP Phase 52's 5 success criteria plus the 2 Manual-Only Verifications flagged in 52-VALIDATION.md (Pitfall 2, Pitfall 3/D-04).
  </action>
  <what-built>
    A new opt-in top-edge switcher layout (2 icons flanking the camera cutout on each side) alongside today's pill-below-the-island switcher, both driven by 4 user-configurable icon-placement slots, controlled from a new "Switcher" Settings section that is hidden entirely on non-notch displays.
  </what-built>
  <how-to-verify>
    1. Open `Islet.xcodeproj` in Xcode, Cmd-R to run the Debug build. Expand the island (hover or click) to any tab — confirm the pill switcher below the island looks and behaves exactly as before (Home/Tray/Calendar/Weather icons, tap to switch tabs) — no regression (SC#5).
    2. Open Settings -> confirm a new "Switcher" sidebar row (icon: 2x2 grid glyph) appears alongside the other sections (SC#1).
    3. Click Switcher: confirm a "Layout" segmented picker (Pill / Top Edge) and an "Icon Placement" section with 4 dropdowns (Left Outer, Left Inner, Right Inner, Right Outer) are present, each showing Home/Tray/Calendar/Weather as options with icons. Confirm the 4 dropdowns currently read Home / Tray / Calendar / Weather respectively (SC#3's default).
    4. Switch the Layout picker to "Top Edge". Expand the island on any tab: confirm the pill row below the island is now completely gone, and 2 icons appear flanking the camera cutout on the left and 2 on the right, at the very top edge of the expanded island (SC#1, SC#2). Confirm the expanded content area itself (Home/Tray/Calendar/Weather content) is the same size as before — no visible shrink/regrow glitch.
    5. Fit check (D-04/Pitfall 3): zoom in visually or look closely at the 4 top-edge icons — confirm none of them are clipped top/bottom by the camera-clearance band, and none are cut off by the island's own rounded top corners (the two outer icons sit closest to the tightest corner).
    6. Clearance check (Pitfall 2): confirm the empty gap between the left pair and right pair of icons visually clears the real camera housing with no icon overlapping or touching it, and no excessively large empty gap either.
    7. Tap each of the 4 top-edge icons in turn: confirm each one switches to the correct tab (Home/Tray/Calendar/Weather) and the tapped icon shows the same filled/highlighted state the pill uses today (D-05).
    8. Go back to Settings -> Switcher, reassign one dropdown (e.g. swap "Left Outer" from Home to Weather). Return to the expanded island (still in Top Edge layout): confirm the top-edge row immediately reflects the new position with no relaunch (SC#4).
    9. Switch the Layout picker back to "Pill". Expand the island: confirm the pill row is back, and its left-to-right icon order now ALSO reflects the same reassignment from step 8 (D-03 — both layouts share one ordering source).
    10. Try assigning the same icon to two different slots (e.g. Home to both Left Outer and Right Outer) — confirm the app does not crash and both slots simply show the duplicated icon (documented no-validation behavior).
    11. If a non-notch display is available (external monitor, or an older/no-notch Mac): confirm the entire "Switcher" sidebar row disappears from Settings when Islet targets that display (D-08). If no such display is available, note this step as skipped rather than failed.
    12. Rapidly toggle Pill <-> Top Edge a few times while hovering/expanded: confirm no crash, no stuck-expanded state, no visible glitch beyond the expected layout change itself.
  </how-to-verify>
  <resume-signal>Type "approved" once all applicable steps pass, or describe exactly which step/behavior failed.</resume-signal>
  <verify>
    <human-check>All 12 how-to-verify steps confirmed on-device by the user (step 11 may be explicitly skipped if no non-notch display is available); resume-signal explicitly approved.</human-check>
  </verify>
  <done>User has typed "approved" for the full on-device walkthrough, confirming ROADMAP Phase 52's 5 success criteria, D-04's physical fit, and Pitfall 2's cutout-gap clearance are all observably true on real hardware.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| (inherited, unchanged) | This plan runs and verifies existing code from Plans 52-01/52-02/52-03 — no new input surface, no new file edits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-05 | (verification-only) | Whole phase | accept | This plan is a pure verification gate; all STRIDE mitigations for the phase's actual code changes were already assessed and closed in Plans 52-01/52-02/52-03's own threat models (T-52-01 through T-52-04). |
</threat_model>

<verification>
Task 1 gates on the full automated test suite plus a Release build. Task 2 is a blocking on-device checkpoint covering ROADMAP Phase 52's 5 success criteria plus the 2 Manual-Only Verifications (D-04/Pitfall 3 fit, Pitfall 2 clearance) that 52-VALIDATION.md flags as impossible to confirm from source alone.
</verification>

<success_criteria>
- Full automated test suite green (or Cmd-U-confirmed), Release build succeeds.
- On-device UAT approved, covering all 5 ROADMAP Phase 52 success criteria.
- D-04 (36pt icon fits 42pt band) and Pitfall 2 (cutout-gap clears real camera housing) both visually confirmed on real hardware.
- D-03 (shared reorder source) confirmed propagating to both layouts live.
</success_criteria>

<output>
Create `.planning/phases/52-top-edge-switcher-layout-placement-config/52-04-SUMMARY.md` when done
</output>
