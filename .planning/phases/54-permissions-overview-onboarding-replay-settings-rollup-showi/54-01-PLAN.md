---
phase: 54-permissions-overview-onboarding-replay-settings-rollup-showi
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - Islet/PermissionStatus.swift
  - IsletTests/PermissionStatusTests.swift
autonomous: true
requirements: [ARCH-P2]

must_haves:
  truths:
    - "Given a raw framework authorization enum value, the pure mapper functions deterministically resolve it to exactly one of PermissionStatus.granted/.denied/.notYetAsked"
    - "The Calendar+Reminders combined-row resolution (D-13) always shows the WORST of the two underlying statuses: denied beats notYetAsked beats granted"
    - "Each of the 5 permission kinds has a live, side-effect-free status-read function callable from SettingsView without any new monitor class"
  artifacts:
    - path: "Islet/PermissionStatus.swift"
      provides: "PermissionStatus 3-state enum, PermissionKind enum with deep-link anchors, pure mapping functions, and 5 live status-read functions"
      contains: "enum PermissionStatus"
    - path: "IsletTests/PermissionStatusTests.swift"
      provides: "Unit coverage for every pure mapper and the D-13 combine function"
      contains: "final class PermissionStatusTests"
  key_links:
    - from: "Islet/PermissionStatus.swift's live-read functions"
      to: "Islet/PermissionStatus.swift's pure mapper functions"
      via: "direct function call (locationPermissionStatus() calls mapCLAuthorization(_:))"
      pattern: "mapCLAuthorization\\(CLLocationManager\\(\\)\\.authorizationStatus\\)"
---

<objective>
Create the pure, Foundation-plus-framework-reads permission-status model this phase's
Settings UI (Plan 03) will consume: a 3-state `PermissionStatus` enum, per-framework pure
mapping functions (unit-tested without mocking any system framework), the D-13 worst-of-two
Calendar+Reminders combine rule, and the 5 live status-read functions (Location, Bluetooth,
Focus, Calendar, Reminders) plus a best-effort Input Monitoring read via `IOHIDCheckAccess`.

Purpose: give Plan 03's SettingsView a ready-made, already-tested read layer — no new
monitor classes, no mocking, mirrors this codebase's existing "pure seam + thin framework
glue" split (`OnboardingFlow.swift`, `IslandResolver.swift`).
Output: `Islet/PermissionStatus.swift` (new), `IsletTests/PermissionStatusTests.swift` (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/54-permissions-overview-onboarding-replay-settings-rollup-showi/54-CONTEXT.md
@.planning/phases/54-permissions-overview-onboarding-replay-settings-rollup-showi/54-RESEARCH.md
@.planning/phases/54-permissions-overview-onboarding-replay-settings-rollup-showi/54-PATTERNS.md
@Islet/Notch/OnboardingFlow.swift
@Islet/Location/LocationProvider.swift
@Islet/Calendar/CalendarService.swift
@Islet/Notch/FocusModeMonitor.swift
@IsletTests/OnboardingFlowTests.swift
</context>

<interfaces>
<!-- Existing framework read precedents this file's live-read functions must reuse/mirror. -->

From Islet/Notch/NotchWindowController.swift:1896 (existing, confirmed working):
```swift
CBManager.authorization == .allowedAlways
```

From Islet/Notch/FocusModeMonitor.swift:69-71 (existing static helper — do not re-derive):
```swift
static var isAuthorized: Bool {
    INFocusStatusCenter.default.authorizationStatus == .authorized
}
```

From Islet/Location/LocationProvider.swift:28 (existing read):
```swift
manager.authorizationStatus   // CLAuthorizationStatus
```

From Islet/Calendar/CalendarService.swift (existing write-access request calls this phase's
"not yet asked" tap actions in Plan 03 will reuse — read-only, no change here):
```swift
try? await store.requestFullAccessToEvents()
try? await store.requestFullAccessToReminders()
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PermissionStatus enum + pure mapping functions + PermissionKind deep-link anchors</name>
  <files>Islet/PermissionStatus.swift, IsletTests/PermissionStatusTests.swift</files>
  <read_first>Islet/Notch/OnboardingFlow.swift, IsletTests/OnboardingFlowTests.swift, .planning/phases/54-permissions-overview-onboarding-replay-settings-rollup-showi/54-RESEARCH.md, .planning/phases/54-permissions-overview-onboarding-replay-settings-rollup-showi/54-CONTEXT.md</read_first>
  <behavior>
    - `mapCLAuthorization(_ status: CLAuthorizationStatus) -> PermissionStatus`: `.authorizedAlways`/`.authorized` -> `.granted`; `.denied`/`.restricted` -> `.denied`; `.notDetermined` -> `.notYetAsked`; `@unknown default` -> `.notYetAsked`.
    - `mapEKAuthorization(_ status: EKAuthorizationStatus) -> PermissionStatus`: `.fullAccess`/`.writeOnly`/`.authorized` -> `.granted` (this plan's explicit discretion call per RESEARCH.md Pitfall 5/Open Question 2: write-only access still counts as granted, never treated as denied); `.denied`/`.restricted` -> `.denied`; `.notDetermined` -> `.notYetAsked`; `@unknown default` -> `.notYetAsked`.
    - `mapCBManagerAuthorization(_ status: CBManagerAuthorization) -> PermissionStatus`: `.allowedAlways` -> `.granted`; `.denied`/`.restricted` -> `.denied`; `.notDetermined` -> `.notYetAsked`; `@unknown default` -> `.notYetAsked`.
    - `mapINFocusAuthorization(_ status: INFocusStatusAuthorizationStatus) -> PermissionStatus` (adjust the exact Intents type name if Xcode's autocomplete shows a different one — same mapping regardless): `.authorized` -> `.granted`; `.denied`/`.restricted` -> `.denied`; `.notDetermined` -> `.notYetAsked`; `@unknown default` -> `.notYetAsked`.
    - `combinedCalendarReminderStatus(event: PermissionStatus, reminder: PermissionStatus) -> PermissionStatus` (D-13, worst-of-two): if either is `.denied` -> `.denied`; else if either is `.notYetAsked` -> `.notYetAsked`; else -> `.granted`.
    - Test 1: `testMapCLAuthorizationGranted` — `.authorizedAlways` and `.authorized` both map to `.granted`.
    - Test 2: `testMapCLAuthorizationDenied` — `.denied` and `.restricted` both map to `.denied`.
    - Test 3: `testMapCLAuthorizationNotYetAsked` — `.notDetermined` maps to `.notYetAsked`.
    - Test 4: `testMapEKAuthorizationFullAccessAndWriteOnlyAreGranted` — both `.fullAccess` and `.writeOnly` map to `.granted`.
    - Test 5: `testMapEKAuthorizationDenied` / Test 6: `testMapEKAuthorizationNotYetAsked`.
    - Test 7-9: same 3-state coverage for `mapCBManagerAuthorization`.
    - Test 10-12: same 3-state coverage for `mapINFocusAuthorization`.
    - Test 13: `testCombinedCalendarReminderStatusDeniedWinsOverNotYetAsked` — `(.denied, .notYetAsked)` -> `.denied`.
    - Test 14: `testCombinedCalendarReminderStatusNotYetAskedWinsOverGranted` — `(.granted, .notYetAsked)` -> `.notYetAsked`.
    - Test 15: `testCombinedCalendarReminderStatusGrantedOnlyWhenBothGranted` — `(.granted, .granted)` -> `.granted`.
    - Test 16: `testPermissionKindDeepLinkAnchorsAreAllNonEmpty` — every `PermissionKind.allCases` case has a non-empty `deepLinkAnchor` string.
  </behavior>
  <action>
    Write the failing tests first in `IsletTests/PermissionStatusTests.swift` (plain
    `XCTestCase`, no `@MainActor`, mirrors `IsletTests/OnboardingFlowTests.swift`'s pure
    total-function test shape — literal enum-case inputs, no framework instantiation, no
    mocking). Then create `Islet/PermissionStatus.swift` with `import Foundation`,
    `import CoreLocation`, `import EventKit`, `import CoreBluetooth`, `import Intents` at
    the top, and: `enum PermissionStatus: Equatable { case granted, denied, notYetAsked }`;
    the 4 pure mapper functions and the combine function exactly as specified in
    `<behavior>` above (D-13's worst-of-two rule, locked); and
    `enum PermissionKind: String, CaseIterable { case location, calendarReminders,
    bluetooth, focus, inputMonitoring }` with a computed `var deepLinkAnchor: String`
    returning, per case: location -> `"Privacy_LocationServices"`; calendarReminders ->
    `"Privacy_Calendars"` (D-13: the combined row always routes to the general Calendar
    pane, never picks one of the two sub-permissions); bluetooth -> `"Privacy_Bluetooth"`;
    focus -> `"Privacy_Focus"`; inputMonitoring -> `"Privacy_ListenEvent"`. Add a doc
    comment above `deepLinkAnchor` noting these 5 anchor strings are MEDIUM-confidence
    (community-sourced, not yet on-device-verified on this macOS version — RESEARCH.md
    Assumptions A1/A2) and must be spot-checked during Plan 03's on-device UAT. Run the
    tests, confirm they fail (RED), then confirm they pass once the implementation lands
    (GREEN) — no REFACTOR step expected for functions this small.
  </action>
  <verify>
    <automated>xcodebuild test -scheme Islet -destination 'platform=macOS' -only-testing:IsletTests/PermissionStatusTests 2>&1 | tail -30</automated>
  </verify>
  <done>All 16 test cases pass; `Islet/PermissionStatus.swift` exports `PermissionStatus`, `PermissionKind`, 4 pure mappers, and `combinedCalendarReminderStatus`; zero AppKit imports in this file.</done>
  <acceptance_criteria>
    - `grep -c "case granted, denied, notYetAsked" Islet/PermissionStatus.swift` returns 1 (exact 3-state enum, no 4th case added).
    - `grep -c "func combinedCalendarReminderStatus" Islet/PermissionStatus.swift` returns 1.
    - `grep -c "@unknown default" Islet/PermissionStatus.swift` returns 4 (one exhaustive-switch safety net per mapper).
    - `xcodebuild test -only-testing:IsletTests/PermissionStatusTests` exits 0 with all 16 tests reported passed.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Live status-read functions (framework glue, Input Monitoring best-effort)</name>
  <files>Islet/PermissionStatus.swift</files>
  <read_first>Islet/PermissionStatus.swift (as written by Task 1), Islet/Notch/DropInterceptTap.swift, Islet/Notch/OSDInterceptor.swift, .planning/phases/54-permissions-overview-onboarding-replay-settings-rollup-showi/54-RESEARCH.md</read_first>
  <action>
    In the same `Islet/PermissionStatus.swift`, add `import IOKit.hid` and 6 thin,
    side-effect-free live-read functions, each a one-line call into Task 1's pure mappers:
    `func locationPermissionStatus() -> PermissionStatus` calling
    `mapCLAuthorization(CLLocationManager().authorizationStatus)`; `func
    bluetoothPermissionStatus() -> PermissionStatus` calling
    `mapCBManagerAuthorization(CBManager.authorization)` (mirrors the confirmed-working
    read at `NotchWindowController.swift:1896` — do NOT add a status property to
    `BluetoothMonitor`, this is a static framework-level read); `func
    focusPermissionStatus() -> PermissionStatus` calling
    `mapINFocusAuthorization(INFocusStatusCenter.default.authorizationStatus)`; `func
    calendarEventPermissionStatus() -> PermissionStatus` calling
    `mapEKAuthorization(EKEventStore.authorizationStatus(for: .event))`; `func
    reminderPermissionStatus() -> PermissionStatus` calling
    `mapEKAuthorization(EKEventStore.authorizationStatus(for: .reminder))`; `func
    inputMonitoringPermissionStatus() -> PermissionStatus` switching on
    `IOHIDCheckAccess(kIOHIDRequestTypeListenEvent)`: `.kIOHIDAccessTypeGranted` ->
    `.granted`, `.kIOHIDAccessTypeDenied` -> `.denied`, default (including
    `.kIOHIDAccessTypeUnknown`) -> `.notYetAsked`. Add a doc comment directly above
    `inputMonitoringPermissionStatus()` documenting D-03/Pitfall 4's known limitation
    verbatim: no official "request access" API exists for Input Monitoring on macOS;
    `OSDInterceptor`'s unconditional launch-time `.cghidEventTap` (not
    `DropInterceptTap`'s `.cgSessionEventTap`, which gates on Accessibility instead) is
    what actually triggers this TCC prompt in this app, so by the time a user opens
    Settings, this read is very likely already resolved to granted/denied — a residual
    `.notYetAsked`/`.kIOHIDAccessTypeUnknown` read has no reliable in-app trigger; Plan
    03's UI must NOT construct a new `CGEventTap` to nudge it.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS' 2>&1 | tail -30</automated>
  </verify>
  <done>Debug build succeeds with all 6 live-read functions compiling against Task 1's mappers, zero new monitor/timer classes added.</done>
  <acceptance_criteria>
    - `grep -c "^func.*PermissionStatus {" Islet/PermissionStatus.swift` returns 6 (Task 1's `combinedCalendarReminderStatus` doesn't match this grep pattern since it isn't a zero-arg read; count the 6 new read functions specifically via `grep -cE "^func (location|bluetooth|focus|calendarEvent|reminder|inputMonitoring)PermissionStatus\(\)"`).
    - `grep -c "class.*Monitor" Islet/PermissionStatus.swift` returns 0 (no new monitor class — Don't Hand-Roll table).
    - `xcodebuild build -scheme Islet` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| PermissionStatus.swift -> macOS TCC/privacy subsystem | Read-only status queries against `CLLocationManager`, `EKEventStore`, `CBManager`, `INFocusStatusCenter`, `IOHIDCheckAccess` — all local, first-party framework reads, no network, no persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Information Disclosure | Live status-read functions | accept | Local, read-only OS API calls; no network transmission, no logging, no PII beyond standard TCC granted/denied/not-determined state |
| T-54-02 | Tampering | EK/CL/CB/INFocus enum mapping | mitigate | Every mapper is an exhaustive `switch` with `@unknown default -> .notYetAsked` (never silently mis-mapped to `.granted` if Apple adds a new enum case in a future SDK) |
| T-54-SC | Tampering (supply chain) | N/A — zero package installs | accept | This plan adds no npm/pip/cargo/SPM dependency; all 5 frameworks (`CoreLocation`, `EventKit`, `CoreBluetooth`, `Intents`, `IOKit.hid`) are first-party Apple frameworks already linked in the project (RESEARCH.md Package Legitimacy Audit: not applicable) |

</threat_model>

<verification>
Run `xcodebuild test -scheme Islet -destination 'platform=macOS' -only-testing:IsletTests/PermissionStatusTests` — all 16 tests pass. Run `xcodebuild build -scheme Islet -destination 'platform=macOS'` — Debug build green with the 6 live-read functions added.
</verification>

<success_criteria>
`Islet/PermissionStatus.swift` exists and exports a fully-tested `PermissionStatus`
3-state enum, a `PermissionKind` deep-link-anchor lookup, 4 pure mapping functions, the
D-13 combine rule, and 6 live status-read functions — ready for Plan 03 to consume with
zero further framework research needed.
</success_criteria>

<output>
Create `.planning/phases/54-permissions-overview-onboarding-replay-settings-rollup-showi/54-01-SUMMARY.md` when done
</output>
