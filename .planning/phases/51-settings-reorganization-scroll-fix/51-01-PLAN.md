---
phase: 51
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["Islet/SettingsView.swift"]
autonomous: false
requirements: [SETTINGS-02, SETTINGS-03]

must_haves:
  truths:
    - "User can scroll the Activities sidebar section to reach its last control (Automatically Check for Updates toggle), which is unreachable today (SETTINGS-02)"
    - "Sidebar shows exactly 7 sections in this order: Activities, Appearance, Fullscreen, Weather, Diagnostics, Workspace, About (SETTINGS-03, D-06)"
    - "Every control that existed before the split (Launch at Login, 8 activity toggles + their permission popovers/hints, Fullscreen toggle, Weather style picker, Save Diagnostic Report button, Appearance style picker, 3 accent swatch rows, Workspace placeholder, About license/version/credits) still exists and functions identically after the split"
    - "Switching sidebar sections does not desync or lose state (license status, login-item toggle) — same on-appear/on-active re-sync behavior as before"
  artifacts:
    - path: "Islet/SettingsView.swift"
      provides: "7-section NavigationSplitView sidebar with scrollable detail panes"
      min_lines: 550
      contains: "case activities, appearance, fullscreen, weather, diagnostics, workspace, about"
  key_links:
    - from: "SidebarSection.allCases ForEach (sidebar Button rows)"
      to: "body's switch selection dispatch"
      via: "selection state"
      pattern: "case \\.activities:"
---

<objective>
Split `SettingsView.swift`'s single crowded "General" tab into 5 dedicated sidebar sections (Activities, Appearance, Fullscreen, Weather, Diagnostics) plus the unchanged Workspace/About, and fix the scroll-cutoff bug that currently makes Weather/Diagnostics controls unreachable below the fixed 380pt window height — per 51-CONTEXT.md D-01 through D-06 and 51-UI-SPEC.md.

Purpose: SETTINGS-02 (scroll fix) and SETTINGS-03 (dedicated sections) both require restructuring the same `SidebarSection` enum and the same monolithic `generalSection`/`systemSection` computed properties — one cohesive edit to one file, not two.
Output: `Islet/SettingsView.swift` restructured to a 7-case sidebar, zero product behavior lost, all previously-unreachable controls scrollable into view.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-settings-reorganization-scroll-fix/51-CONTEXT.md
@.planning/phases/51-settings-reorganization-scroll-fix/51-PATTERNS.md
@.planning/phases/51-settings-reorganization-scroll-fix/51-UI-SPEC.md
</context>

<interfaces>
<!-- Current SettingsView.swift structure the executor is restructuring. Extracted so no codebase exploration is needed. -->

Current `SidebarSection` enum (lines 80-103, `Islet/SettingsView.swift`):
```swift
private enum SidebarSection: String, CaseIterable, Identifiable {
    case general, workspace, system, about
    var id: String { rawValue }
    var title: String { /* switch: general->"General", workspace->"Workspace", system->"System", about->"About" */ }
    var icon: String { /* switch: general->"gearshape", workspace->"tray", system->"paintbrush", about->"info.circle" */ }
}
@State private var selection: SidebarSection? = .general
```

Sidebar row rendering (lines 106-132) — manual `Button`-based `ForEach(SidebarSection.allCases)`, NOT `List(selection:)` (proven broken on this setup, Plan 27-04 comment at lines 107-112). This mechanism is UNCHANGED — adding enum cases is sufficient, it auto-picks them up via `allCases`.

Body dispatch switch (lines 134-146):
```swift
switch selection {
case .general: generalSection
case .workspace: workspaceSection
case .system: systemSection
case .about: aboutSection
case .none: generalSection
}
```

`generalSection` (lines 194-302) — the monolithic Form containing, in order: Launch-at-Login toggle (196-215), `Section("Activities")` (219-274, 8 toggles + Focus/OSD popovers + hint texts), `Section("Fullscreen")` (279-281), `Section("Weather")` (287-293), `Section("Diagnostics")` (297-299).

`systemSection` (lines 428-446) — Appearance Style picker + 3 accent swatch rows, content untouched, only renamed per D-01.

`workspaceSection` (373-384) and `aboutSection` (388-424) are UNCHANGED in content — only need `ScrollView` wrapping (Task 2), not restructuring.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Restructure SidebarSection enum + extract 5 new detail-pane sections</name>
  <files>Islet/SettingsView.swift</files>
  <read_first>
    Islet/SettingsView.swift (full file — already contains every piece being relocated)
    .planning/phases/51-settings-reorganization-scroll-fix/51-PATTERNS.md (exact before/after code for every piece of this task)
    .planning/phases/51-settings-reorganization-scroll-fix/51-UI-SPEC.md (Icon Contract table, Sidebar Structure, Interaction Contract)
  </read_first>
  <action>
    In `SidebarSection` (lines 80-103): replace `case general, workspace, system, about` with `case activities, appearance, fullscreen, weather, diagnostics, workspace, about` (D-06 order). Update `title` switch: .activities->"Activities", .appearance->"Appearance", .fullscreen->"Fullscreen", .weather->"Weather", .diagnostics->"Diagnostics", .workspace->"Workspace" (unchanged), .about->"About" (unchanged). Update `icon` switch per UI-SPEC Icon Contract: .activities->"bolt", .appearance->"paintbrush" (D-01/D-04, carried over from .system), .fullscreen->"arrow.up.left.and.arrow.down.right", .weather->"cloud.sun", .diagnostics->"stethoscope", .workspace->"tray" (unchanged), .about->"info.circle" (unchanged). Change `@State private var selection: SidebarSection? = .general` to `= .activities` (new leading section).

    Update the body dispatch switch (lines 134-146): replace `.general: generalSection` / `.system: systemSection` cases with `.activities: activitiesSection`, `.appearance: appearanceSection`, `.fullscreen: fullscreenSection`, `.weather: weatherSection`, `.diagnostics: diagnosticsSection`. Keep `.workspace: workspaceSection` / `.about: aboutSection` cases unchanged. Change the `.none` fallback from `generalSection` to `activitiesSection`.

    Delete the `generalSection` computed property (lines 194-302) entirely, replacing it with 4 new computed properties built from its content, each wrapped in `ScrollView(.vertical) { Form { ... } .padding(20) }` per UI-SPEC's Interaction Contract (uniform ScrollView wrapping across all new sections, not just Activities):
    - `activitiesSection`: merges the Launch-at-Login `Toggle` + `.onChange` (lines 196-215, per D-02) with the full `Section("Activities")` block (lines 219-274: 8 toggles, Focus/OSD `.popover`/hint-`Text` wiring) — moved verbatim, zero logic changes.
    - `fullscreenSection`: the `Section("Fullscreen")` block (lines 279-281).
    - `weatherSection`: the `Section("Weather")` block (lines 287-293).
    - `diagnosticsSection`: the `Section("Diagnostics")` block (lines 297-299, per D-03 — its own dedicated section, not folded elsewhere).

    Rename `systemSection` (lines 428-446) to `appearanceSection` (D-01) — content (Appearance Style picker + 3 accent swatch rows) is unchanged, only wrap it in the same `ScrollView(.vertical) { Form { ... }.padding(20) }` pattern for consistency with the other 4 new sections. Update its doc comment reference from "System (Theming)" to "Appearance" if present.

    Do not touch `focusPermissionExplanationView`, `osdPermissionExplanationView`, `swatchRow(selection:)`, `buyNowButton`, `licenseEntry`, `statusLine`, `activate()`, `saveDiagnosticReport()`, `workspaceSection`, or `aboutSection` — these are called from their relocated call sites unchanged (Task 2 handles Workspace/About's ScrollView wrapping).
  </action>
  <acceptance_criteria>
    - `grep -c "case activities, appearance, fullscreen, weather, diagnostics, workspace, about" Islet/SettingsView.swift` == 1
    - `grep -c "private var activitiesSection" Islet/SettingsView.swift` == 1
    - `grep -c "private var fullscreenSection" Islet/SettingsView.swift` == 1
    - `grep -c "private var weatherSection" Islet/SettingsView.swift` == 1
    - `grep -c "private var diagnosticsSection" Islet/SettingsView.swift` == 1
    - `grep -c "private var appearanceSection" Islet/SettingsView.swift` == 1
    - `grep -c "private var generalSection" Islet/SettingsView.swift` == 0
    - `grep -c "private var systemSection" Islet/SettingsView.swift` == 0
    - `grep -c "ScrollView" Islet/SettingsView.swift` -ge 5
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` exits 0
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <done>SidebarSection has 7 cases in D-06 order with correct titles/icons/default selection; activitiesSection/fullscreenSection/weatherSection/diagnosticsSection/appearanceSection all exist as ScrollView-wrapped Form sections containing their relocated controls verbatim; generalSection/systemSection no longer exist; Debug build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Uniform ScrollView wrapping on Workspace/About + build/grep verification gate</name>
  <files>Islet/SettingsView.swift</files>
  <read_first>
    Islet/SettingsView.swift (post-Task-1 state)
    .planning/phases/51-settings-reorganization-scroll-fix/51-UI-SPEC.md (Interaction Contract: "Apply consistently to all 7 sections")
  </read_first>
  <action>
    Wrap `workspaceSection`'s body (currently a bare `VStack` with no `Form`) and `aboutSection`'s body (currently `Form { ... }.padding(20)`) each in `ScrollView(.vertical) { ... }`, matching the pattern applied to the 5 new sections in Task 1 — completing the UI-SPEC's "apply consistently to all 7 sections" rule so no section has inconsistent scroll behavior even though only Activities is expected to need it in practice (D-05). Preserve `workspaceSection`'s existing `.frame(maxWidth: .infinity, maxHeight: .infinity)` centering modifier on the inner VStack, not on the ScrollView itself, so the placeholder still centers when content is short.

    Run a full Debug + Release build gate and confirm zero remaining references to the deleted `generalSection`/`systemSection` symbols anywhere in the file or elsewhere in `Islet/`.
  </action>
  <acceptance_criteria>
    - `grep -c "ScrollView" Islet/SettingsView.swift` -ge 7
    - `grep -rn "generalSection\|systemSection" Islet/` returns 0 matches
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` exits 0
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Release` exits 0
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug && xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Release && ! grep -rn "generalSection\|systemSection" Islet/</automated>
  </verify>
  <done>All 7 sidebar sections (Activities, Appearance, Fullscreen, Weather, Diagnostics, Workspace, About) wrap their content in ScrollView; Debug and Release builds both succeed; zero dead references to the deleted generalSection/systemSection symbols remain anywhere in Islet/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device UAT — scroll reachability + all 7 sections functional</name>
  <files></files>
  <action>
    No file edits — build and run the app on-device (Cmd-R in Xcode) and walk through the steps below. This task's only output is the recorded PASSED/FAILED verdict covering SETTINGS-02 (scroll fix) and SETTINGS-03 (dedicated sections).
  </action>
  <what-built>
    Settings' sidebar now shows 7 sections (Activities, Appearance, Fullscreen, Weather, Diagnostics, Workspace, About) in that order, replacing the old 4-section General/Workspace/System/About layout. Every control that existed before still exists, relocated into its new section, with the previously-unreachable Weather/Diagnostics controls (and Activities' full toggle list) now scrollable within the fixed 520x380 window.
  </what-built>
  <how-to-verify>
    1. Open `Islet.xcodeproj` in Xcode, Cmd-R to run the Debug build.
    2. Open Settings (menu bar icon -> Settings). Confirm 7 sidebar rows top-to-bottom: Activities, Appearance, Fullscreen, Weather, Diagnostics, Workspace, About — with the icons bolt/paintbrush/arrow.up.left.and.arrow.down.right/cloud.sun/stethoscope/tray/info.circle respectively. Confirm Activities is selected by default (window opens on it).
    3. Click Activities: confirm Launch-at-Login toggle + all 8 activity toggles (Charging, Now Playing, Song-Change Toast, Devices, Calendar Countdown, Focus Mode HUD, Replace System Volume/Brightness OSD, Automatically Check for Updates) are present. Scroll down within the section and confirm "Automatically Check for Updates" (the last toggle, previously unreachable) is now fully visible and clickable — this is the SETTINGS-02 fix. Flip a toggle (e.g. Now Playing) and confirm the notch shell reacts.
    4. Click Appearance: confirm the Gradient/Solid Black/Liquid Glass segmented picker and 3 accent swatch rows (Now Playing/Charging/Device) render and function exactly as the old "System" tab did.
    5. Click Fullscreen: confirm the "Hide notch in fullscreen" toggle is present and functions.
    6. Click Weather: confirm the Medium/Large segmented picker is present, scroll down and confirm it's fully visible (previously unreachable below the fold) — the other half of the SETTINGS-02 fix.
    7. Click Diagnostics: confirm "Save Diagnostic Report..." button is present, scroll down and confirm it's fully visible and clickable; click it and confirm a save dialog appears.
    8. Click Workspace: confirm the "Nothing to configure yet" placeholder still renders centered, no regression.
    9. Click About: confirm License block (current trial/licensed state), Version label, and Credits text all render correctly.
    10. Rapidly click through all 7 sections 2 full loops: confirm no stale/flashing state (license status, login toggle) and no crash.
    11. Confirm the Settings window stays fixed at its current size (no resize handle, no growth) throughout — per D-05.
  </how-to-verify>
  <resume-signal>Type "approved" once all 11 steps pass, or describe exactly which step/behavior failed.</resume-signal>
  <verify>
    <human-check>All 11 how-to-verify steps confirmed on-device by the user; resume-signal explicitly approved.</human-check>
  </verify>
  <done>User has typed "approved" for the full on-device Settings walkthrough, confirming SETTINGS-02 (scroll reachability) and SETTINGS-03 (7 dedicated sections, all controls functional) are both observably true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| (inherited, unchanged) | This phase is a pure UI reorganization of already-shipped controls — no new user input, network call, or persistence path is introduced. The one existing trust boundary in this file (license-key `TextField` -> `LicenseService.activate`) is relocated into `aboutSection` unchanged, not modified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Tampering (regression) | `Islet/SettingsView.swift` restructuring | accept | No new input surface is introduced; Task 2's grep sweep (`generalSection\|systemSection` zero matches) plus the Debug+Release build gate is the mitigation against a silently-broken relocation, not a security control. |
</threat_model>

<verification>
Task 1 gates on the enum/section-extraction grep checks plus a Debug build. Task 2 gates on uniform ScrollView coverage, a dead-symbol grep sweep, and both Debug and Release builds succeeding. Task 3 is a blocking on-device checkpoint covering SETTINGS-02 (scroll reachability of Activities/Weather/Diagnostics) and SETTINGS-03 (all 7 sections present and functional) per ROADMAP.md Phase 51's success criteria.
</verification>

<success_criteria>
- SidebarSection enum has exactly 7 cases in D-06's locked order (Activities, Appearance, Fullscreen, Weather, Diagnostics, Workspace, About).
- Every previously-existing Settings control still exists and functions identically, just relocated.
- Weather and Diagnostics controls (and Activities' full toggle list) are reachable via scrolling within the fixed 520x380 window — the SETTINGS-02 bug is fixed.
- Debug and Release builds both succeed with zero dead references to the deleted generalSection/systemSection symbols.
- On-device UAT (all 11 steps) approved by the user.
</success_criteria>

<output>
Create `.planning/phases/51-settings-reorganization-scroll-fix/51-01-SUMMARY.md` when done
</output>
