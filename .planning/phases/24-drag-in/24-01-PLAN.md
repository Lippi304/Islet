---
phase: 24-drag-in
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Islet/Notch/NotchWindowController.swift]
autonomous: false
requirements: [SHELF-01]
must_haves:
  truths:
    - "Console output (NSLog) confirms .leftMouseDragged and .leftMouseUp global monitors registered in Islet fire during a real Finder-initiated INBOUND drag, observed on-device"
    - "The existing fileURLs(from:) pure seam correctly extracts dragged file/folder URL(s) when read against NSPasteboard(name: .drag) instead of a destination's draggingPasteboard"
  artifacts:
    - path: "Islet/Notch/NotchWindowController.swift"
      provides: "DEBUG-only throwaway spike instrumentation for the two candidate drag-approach global monitors (A1-equivalent probe)"
      contains: "leftMouseDragged"
  key_links:
    - from: "start()"
      to: "spike .leftMouseDragged handler"
      via: "NSEvent.addGlobalMonitorForEvents(matching: [.leftMouseDragged])"
      pattern: "addGlobalMonitorForEvents\\(matching: \\[\\.leftMouseDragged\\]\\)"
---

<objective>
Build and on-device-validate an isolated, throwaway spike that answers this phase's single largest unknown (RESEARCH.md Assumption A1): do `.leftMouseDragged`/`.leftMouseUp` global `NSEvent` monitors registered in Islet actually fire reliably during a real Finder-initiated INBOUND drag toward the notch, on this project's exact panel/run-loop configuration?

Per CONTEXT.md D-05 (locked): this spike MUST be built and confirmed FIRST, in complete isolation from the accept/shelf-landing logic, before any production `DragApproachDetector` code is written. Per D-06 (locked): validation is capped at 2 on-device rounds (this implementation attempt + one fix-and-retry round) before the mechanism itself is treated as a blocker again. Per D-08 (locked): if still unreliable after the capped 2 rounds, STOP and return to `/gsd:discuss-phase 24` — do not debug indefinitely, do not proceed to Plan 24-02.

Purpose: De-risk the phase's genuinely novel surface (global-monitor-based inbound drag detection) before investing in the full accept/shelf-landing implementation — mirrors this project's own successful 22-01 spike methodology exactly, and is why this is Wave 1's ENTIRE scope.
Output: Throwaway `#if DEBUG`-only diagnostic monitors in `NotchWindowController.swift` (superseded/removed by Plan 24-02, which is gated on this plan's PASSED verdict) + a recorded on-device PASSED/FAILED verdict in this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-drag-in/24-CONTEXT.md
@.planning/phases/24-drag-in/24-RESEARCH.md
</context>

<interfaces>
<!-- The exact same-file precedent this spike must mirror shape-for-shape. -->

From Islet/Notch/NotchWindowController.swift (existing, unmodified by this plan):
```swift
private var mouseMonitor: Any?
// ... in start():
mouseMonitor = NSEvent.addGlobalMonitorForEvents(matching: [.mouseMoved]) { [weak self] _ in
    self?.handlePointer(at: NSEvent.mouseLocation)
}
// ... in deinit:
if let m = mouseMonitor { NSEvent.removeMonitor(m) }
```

From Islet/Notch/DragDropSupport.swift (existing, reused unchanged — do not modify this file in this plan):
```swift
func fileURLs(from pasteboard: NSPasteboard) -> [URL] {
    (pasteboard.readObjects(forClasses: [NSURL.self], options: nil) as? [URL]) ?? []
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add throwaway DEBUG-only drag-approach spike instrumentation</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift (read the full `mouseMonitor` property declaration at line 208, its arm block in `start()` at lines 320-326, and its `deinit` removal at line 1338 — this is the EXACT shape to mirror; also read the `#if DEBUG ... didLogFirstHover ... #endif` probe block at lines 267-275 and 722-727 — the project's own established convention for a DEBUG-only diagnostic probe)
    Islet/Notch/DragDropSupport.swift (confirm `fileURLs(from:)`'s exact signature — reuse unchanged, do not modify this file)
  </read_first>
  <action>
    Add two throwaway `#if DEBUG`-only stored properties near the existing `mouseMonitor`/`dragReleaseMonitor` block (~line 219): `private var spikeDragApproachMonitor: Any?` and `private var spikeDragEndMonitor: Any?`, plus `private var spikeDragPasteboardChangeCount = NSPasteboard(name: .drag).changeCount`. Wrap all three in `#if DEBUG ... #endif` (never compiles into Release — mirrors `didLogFirstHover`'s existing guard).

    In `start()`, immediately after the existing `mouseMonitor = NSEvent.addGlobalMonitorForEvents(matching: [.mouseMoved]) { ... }` block (~line 326), add a `#if DEBUG` block that arms both spike monitors:
    - `spikeDragApproachMonitor = NSEvent.addGlobalMonitorForEvents(matching: [.leftMouseDragged]) { [weak self] _ in self?.handleSpikeDragApproachTick() }`
    - `spikeDragEndMonitor = NSEvent.addGlobalMonitorForEvents(matching: [.leftMouseUp]) { [weak self] _ in self?.handleSpikeDragApproachEnd() }`

    Add two new `#if DEBUG`-only private methods (place near `handlePointer(at:)`):
    - `handleSpikeDragApproachTick()`: reads `let pasteboard = NSPasteboard(name: .drag)`; compares `pasteboard.changeCount` against `spikeDragPasteboardChangeCount`; on any delta, updates the stored baseline and `NSLog`s three things — the changeCount delta, `fileURLs(from: pasteboard)`, and `NSEvent.mouseLocation` — prefixed with a fixed tag string `"[SPIKE-24]"` so the on-device Console output is easy to filter.
    - `handleSpikeDragApproachEnd()`: unconditionally (no `isDragApproaching`-style guard — this spike is purely observational, not gated) `NSLog`s the final `NSEvent.mouseLocation` plus `fileURLs(from: NSPasteboard(name: .drag))`, prefixed with the same `"[SPIKE-24]"` tag.

    In `deinit`, immediately after the existing `if let m = mouseMonitor { NSEvent.removeMonitor(m) }` line (~1338), add a `#if DEBUG` block removing both spike monitors: `if let m = spikeDragApproachMonitor { NSEvent.removeMonitor(m) }` and `if let m = spikeDragEndMonitor { NSEvent.removeMonitor(m) }`.

    Do not touch `NotchInteractionState.swift`, `DragDropSupport.swift`, `NotchPanel.swift`, or any Shelf file — this task is 100% isolated to throwaway DEBUG-only additions in `NotchWindowController.swift`.
  </action>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build` succeeds with zero errors/warnings
    - `grep -c "leftMouseDragged" Islet/Notch/NotchWindowController.swift` returns >= 1
    - `grep -c "spikeDragApproachMonitor\|spikeDragEndMonitor" Islet/Notch/NotchWindowController.swift` returns >= 4 (declaration + arm + 2x deinit removal, at minimum)
    - `xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Release build` succeeds and the spike code is excluded (confirm via `grep -A2 "#if DEBUG" Islet/Notch/NotchWindowController.swift` shows the spike block is inside a DEBUG guard, not top-level)
  </acceptance_criteria>
  <done>Build succeeds in both Debug and Release configurations; spike monitors are DEBUG-only and mirror `mouseMonitor`'s exact arm/disarm shape.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device spike validation — confirm or refute Assumption A1</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    .planning/phases/24-drag-in/24-RESEARCH.md (the "Recommended Spike" section, steps 1-7, and Assumption A1/A2/A4 in the Assumptions Log — this checkpoint executes that exact protocol)
  </read_first>
  <action>
    No file edits in this task by default — build and run the app on-device (Cmd-R in Xcode) and
    perform the on-device spike protocol described in how-to-verify below. The only in-scope edit
    is the documented D-06 contingency: ONE targeted fix-and-retry adjustment to Task 1's spike
    instrumentation if the first attempt fails (e.g. adjusting the event mask or pasteboard-read
    timing based on what the console output DID show).
  </action>
  <acceptance_criteria>
    - Console.app (or Xcode's own console) filtered on "[SPIKE-24]" shows `.leftMouseDragged` ticks with a changed pasteboard count AND the correct file URL for a single-file Finder drag toward the notch
    - The same is confirmed for: multiple files selected together, and a folder (returned as ONE URL, not enumerated)
    - `.leftMouseUp` logs the correct final pointer location and a still-readable URL
    - An Escape-cancelled drag (start dragging, press Escape before releasing) does not crash the app and does not leave any visibly broken state
  </acceptance_criteria>
  <what-built>
    Throwaway DEBUG-only `.leftMouseDragged`/`.leftMouseUp` global monitors that log to Console via `NSLog("[SPIKE-24] ...")` whenever a real OS drag-and-drop session is detected via `NSPasteboard(name: .drag)` changeCount deltas, with dragged file URLs extracted via the existing `fileURLs(from:)` seam.
  </what-built>
  <how-to-verify>
    1. Build and run on-device via Xcode (Cmd-R — NOT `xcodebuild test`, which hangs headlessly per project memory `xcodebuild-test-headless-hang`).
    2. Open Console.app (or the Xcode debug console) and filter/search for "[SPIKE-24]".
    3. With the pointer starting well OUTSIDE the notch/pill area (mirroring 22-01's own methodology to rule out any dependency on prior hover state), drag a single file from Finder toward the notch. Confirm `[SPIKE-24]` lines appear showing a changeCount delta, the correct file path, and a moving pointer location as the drag progresses; confirm a final `[SPIKE-24]` line appears on release with the correct location and URL.
    4. Repeat with multiple files selected together (confirm all URLs logged in order), then a folder (confirm exactly ONE URL logged, the folder's own path — not its contents).
    5. Repeat once more, but press Escape before releasing the mouse button instead of dropping. Confirm the app does not crash and no [SPIKE-24] line reports a bogus/stale drop.
    6. Record the verdict: **PASSED** (all of the above observed reliably across the trials) or **FAILED** (any trial silently produced no `[SPIKE-24]` output, or produced incorrect URLs/locations).
  </how-to-verify>
  <resume-signal>
    Reply with the verdict.

    - If **PASSED**: reply "approved" (or "passed") to proceed to Plan 24-02 (full accept/shelf-landing implementation).
    - If **FAILED** on this first attempt: per D-06, this plan may be revised ONCE for a single fix-and-retry round (e.g. adjusting the event mask, the pasteboard-reading timing, or logging granularity based on what the console output DID show) — describe what you observed and Claude will propose one targeted fix, then re-run this same checkpoint.
    - If **FAILED again after that one fix-and-retry round**: per D-08 (locked), STOP here. Do not proceed to Plan 24-02. Reply "blocked" and this phase returns to `/gsd:discuss-phase 24` with the spike's findings — the `DragApproachDetector` global-monitor mechanism itself is the blocker, not an implementation detail to keep debugging.
  </resume-signal>
  <verify>
    <human-check>Console output filtered on "[SPIKE-24]" shows reliable .leftMouseDragged/.leftMouseUp delivery with correct file URLs across single-file, multi-file, folder, and Escape-cancelled trials.</human-check>
  </verify>
  <done>User replies "approved"/"passed" (mechanism confirmed, unblocking Plan 24-02) or "blocked" (mechanism unreliable after the capped 2 rounds — phase returns to /gsd:discuss-phase 24 per D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Finder (drag source) → Islet's global `NSEvent` monitors | Untrusted-origin drag content (file paths) crosses into Islet's process via a passive systemwide pasteboard read, not a registered drag-destination callback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information Disclosure | `handleSpikeDragApproachTick`/`handleSpikeDragApproachEnd` `NSLog` of dragged file paths | mitigate | Entire spike is wrapped in `#if DEBUG` — never compiles into Release; Console output is local-machine-only diagnostic, not persisted or transmitted |
| T-24-02 | Tampering | `NSPasteboard(name: .drag)` content persists after a drag ends (documented in RESEARCH.md Pattern 4 caveat) | accept (spike is observational-only, no accept/append logic exists yet in this plan) | Deferred to Plan 24-02, which gates every real accept decision on a `changeCount`-delta session-tracking flag (`isDragApproaching`), never on raw pasteboard presence alone |
</threat_model>

<verification>
- `xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build` succeeds (Task 1 gate)
- `xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Release build` succeeds with spike code excluded (Task 1 gate)
- On-device Cmd-R manual verification per Task 2's checkpoint (this phase's core risk is fundamentally not unit-testable — real Window Server drag session required)
</verification>

<success_criteria>
Task 2's checkpoint returns a recorded PASSED verdict (after at most one fix-and-retry round per D-06) confirming `.leftMouseDragged`/`.leftMouseUp` global monitors reliably observe a real inbound Finder drag on this project's exact configuration — unblocking Plan 24-02. If FAILED after the capped 2 rounds, this plan's SUMMARY records the FAILED verdict and findings, and the phase returns to `/gsd:discuss-phase 24` per D-08 instead of proceeding.
</success_criteria>

<output>
Create `.planning/phases/24-drag-in/24-01-SUMMARY.md` when done, recording the on-device PASSED/FAILED verdict and any surprising timing/content-availability findings (mirrors how `22-01-SUMMARY.md` recorded Phase 22's own A1 resolution).
</output>
