---
phase: 26
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - Islet/Notch/NotchPillView.swift
  - Islet/Notch/OnboardingViewState.swift
autonomous: true
requirements: [ONBOARD-01, ONBOARD-02]

must_haves:
  truths:
    - "The onboarding carousel renders all 4 steps (Welcome, Trial/License/Buy, Permissions, Done) inside the real expanded notch chrome (blobShape), matching the shared Phase-25 gradient material -- not a separate window or shape"
    - "The Permissions step shows one row per permission (Bluetooth, Calendar, Location) each with its own one-line reason and its own independent Grant control (D-02)"
    - "A denied/skipped permission shows a quiet grey 'Not granted' state, never an error icon or dialog (D-03)"
    - "The Done step's Launch-at-Login toggle reads and writes the real SMAppService state via the existing LaunchAtLogin wrapper -- not a new/duplicate flag (D-10)"
  artifacts:
    - path: Islet/Notch/OnboardingViewState.swift
      provides: "per-permission granted/not-granted @Published state, controller-owned, view-rendered-only"
      contains: "final class OnboardingViewState: ObservableObject"
    - path: Islet/Notch/NotchPillView.swift
      provides: "onboardingSize constant, .onboarding switch case, onboardingCarousel(_:) view, blobShape height parameter, new init params (onboardingState + 5 closures)"
      contains: "static let onboardingSize"
  key_links:
    - from: Islet/Notch/NotchPillView.swift
      to: Islet/Notch/OnboardingFlow.swift
      via: "switch step inside onboardingCarousel(_ step: OnboardingStep)"
      pattern: "func onboardingCarousel"
    - from: Islet/Notch/NotchPillView.swift
      to: Islet/Notch/OnboardingViewState.swift
      via: "@ObservedObject var onboardingState: OnboardingViewState read inside the Permissions rows"
      pattern: "onboardingState\\."
---

<objective>
Build the notch-hosted onboarding carousel view: the new `.onboarding` presentation case, the
4-step content (Welcome / Trial-License-Buy / Permissions / Done) per `26-UI-SPEC.md`'s exact
copy/layout/color/typography contract, and the new `OnboardingViewState` that tracks each
permission row's granted/not-granted display state. This plan defines the CONTRACT (init
parameters, closures, constants) that Plan 26-04 wires to real controller behavior -- per this
codebase's own established "separate @Published model, controller-owned, view-render-only"
convention (mirrors `outfit`/`shelfViewState`/`presentationState`).

Purpose: give the view layer everything it needs to render the full onboarding flow, with all new
closures defaulted to no-ops so DEBUG #Previews and any unit construction still compile without a
controller -- exactly like `onClick`/`onShelfItemTap` already work.
Output: `OnboardingViewState.swift`, the extended `NotchPillView.swift`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-onboarding-flow/26-CONTEXT.md
@.planning/phases/26-onboarding-flow/26-UI-SPEC.md
@.planning/phases/26-onboarding-flow/26-PATTERNS.md
Islet/Notch/OnboardingFlow.swift
</context>

<interfaces>
<!-- Exact current NotchPillView.swift structure, read verbatim this session. Match this style. -->

Existing init params (lines 16-96) -- non-defaulted @ObservedObject data models come FIRST,
defaulted no-op closures come AFTER:
```
struct NotchPillView: View {
    @ObservedObject var interaction: NotchInteractionState
    @ObservedObject var nowPlaying: NowPlayingState
    @ObservedObject var presentationState: IslandPresentationState
    @ObservedObject var outfit: BasicOutfitState
    @ObservedObject var shelfViewState: ShelfViewState
    @Environment(\.activityAccent) private var accent
    var onClick: () -> Void = {}
    var onTogglePlayPause: () -> Void = {}
    var onNext: () -> Void = {}
    var onPrevious: () -> Void = {}
    var onShelfItemTap: (ShelfItem) -> Void = { _ in }
    var onShelfItemDelete: (UUID) -> Void = { _ in }
    var onShelfClearAll: () -> Void = {}
    var onShelfItemDragStarted: () -> Void = {}
    ...
    static let collapsedSize = CGSize(width: 200, height: 38)
    static let expandedSize = CGSize(width: 360, height: 144)
    static let wingsSize = CGSize(width: 290, height: 32)
    static let shelfRowHeight: CGFloat = 56

    var body: some View {
        ZStack(alignment: .top) {
            switch presentation {
            case .charging(let a):            wings(for: a)
            case .device(let d):              deviceWings(for: d)
            case .nowPlayingWings(let p):      mediaWingsOrToast(p)
            case .nowPlayingExpanded(let p, true):  mediaExpanded(p, art: nowPlaying.artwork)
            case .nowPlayingExpanded(_, false):     mediaUnavailable
            case .expandedIdle:                expandedIsland
            case .idle:                        collapsedIsland
            }
        }
        ...
    }

    private var expandedIsland: some View {
        blobShape(topCornerRadius: 6, bottomCornerRadius: 32, shelfItems: shelfViewState.items) {
            HStack(spacing: 0) { ... }
                .padding(.horizontal, 16)
        }
    }

    private func blobShape<Content: View>(topCornerRadius: CGFloat,
                                           bottomCornerRadius: CGFloat,
                                           alignment: Alignment = .center,
                                           shelfItems: [ShelfItem],
                                           @ViewBuilder content: () -> Content) -> some View {
        let hasShelf = !shelfItems.isEmpty
        let height = Self.expandedSize.height + (hasShelf ? Self.shelfRowHeight : 0)
        return NotchShape(topCornerRadius: topCornerRadius, bottomCornerRadius: bottomCornerRadius)
            .fill(Self.islandMaterial)
            .matchedGeometryEffect(id: "island", in: ns)
            .frame(width: Self.expandedSize.width, height: height)
            .overlay(alignment: .top) {
                VStack(spacing: 0) {
                    content()
                        .frame(width: Self.expandedSize.width, height: Self.expandedSize.height, alignment: alignment)
                    if hasShelf { shelfRow(shelfItems).transition(.opacity) }
                }
            }
            .onTapGesture { onClick() }
    }
}
```
8 `#Preview` blocks exist at lines ~900-1033, each constructing `NotchPillView(interaction:...,
nowPlaying:..., presentationState:..., outfit:..., shelfViewState:..., ...)`. Every one of these
needs the new `onboardingState:` argument added once it becomes a non-defaulted parameter.

From Islet/SettingsView.swift (D-10's exact mirror target, read verbatim, DO NOT MODIFY this file):
```
// lines 6, 67-86
@State private var launchAtLogin = LaunchAtLogin.isEnabled
...
Toggle("Launch Islet at login", isOn: $launchAtLogin)
    .onChange(of: launchAtLogin) { _, on in
        do {
            let result = try LaunchAtLogin.set(on)
            if on && LaunchAtLogin.requiresApproval {
                launchAtLogin = true
                LaunchAtLogin.openLoginItemsSettings()
            } else {
                launchAtLogin = result
            }
        } catch {
            launchAtLogin = LaunchAtLogin.isEnabled
        }
    }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: OnboardingViewState + blobShape height parameter + onboardingSize constant</name>
  <files>Islet/Notch/OnboardingViewState.swift, Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/IslandPresentationState.swift (the exact "plain @Published holder" analog -- OnboardingViewState mirrors this shape with 3 Bool? fields instead of 1 enum)
    Islet/Notch/NotchPillView.swift lines 1-330 (blobShape's current hardcoded Self.expandedSize usage -- see interfaces block above)
    .planning/phases/26-onboarding-flow/26-UI-SPEC.md "Panel & Layout Contract" section (onboardingSize = 360x240, "single fixed panel size... no per-step resize")
  </read_first>
  <action>
    Create Islet/Notch/OnboardingViewState.swift mirroring IslandPresentationState.swift's structure
    exactly: `import Foundation`, `final class OnboardingViewState: ObservableObject` with
    `@Published var bluetoothGranted: Bool?`, `@Published var calendarGranted: Bool?`,
    `@Published var locationGranted: Bool?` (all default `nil` -- nil means "not yet attempted this
    session", not "denied"; true/false reflect the real outcome of the last grant attempt). Doc-comment
    referencing D-03 (quiet degrade -- nil and false both render the same neutral "Not granted" state,
    the view does not distinguish "never asked" from "denied").

    In Islet/Notch/NotchPillView.swift:
    1. Add `static let onboardingSize = CGSize(width: 360, height: 240)` as a sibling constant to
       `expandedSize`/`wingsSize` (near line 134), with a comment citing 26-UI-SPEC.md's Panel & Layout
       Contract and noting this is a fixed size for ALL 4 steps (no per-step resize, matching the
       existing wingsSize/expandedSize "size once" convention).
    2. Extend `blobShape<Content: View>(...)`'s signature with a new parameter:
       `height: CGFloat? = nil` (insert after `alignment: Alignment = .center`, before `shelfItems:`).
       Inside the function body, change `let height = Self.expandedSize.height + (hasShelf ? Self.shelfRowHeight : 0)`
       to `let baseHeight = height ?? Self.expandedSize.height` then
       `let totalHeight = baseHeight + (hasShelf ? Self.shelfRowHeight : 0)`, and use `totalHeight`
       everywhere the old `height` local was used for the OUTER `.frame(width:height:)`. Also change the
       inner `content().frame(width: Self.expandedSize.width, height: Self.expandedSize.height, alignment: alignment)`
       to `content().frame(width: Self.expandedSize.width, height: baseHeight, alignment: alignment)` --
       BOTH the outer shape frame and the inner content frame must grow together, or the shape will be
       240pt tall while the content stays clipped to the old 144pt box. This is a backward-compatible
       extension: every existing call site (`expandedIsland`, `mediaExpanded`, `mediaUnavailable`) omits
       the new parameter and gets the exact same `Self.expandedSize.height` behavior as before (verify by
       diffing their rendered frame math is unchanged).
  </action>
  <acceptance_criteria>
    - Islet/Notch/OnboardingViewState.swift exists, imports Foundation only, declares exactly 3 `@Published var ...: Bool?` properties
    - `grep -n "static let onboardingSize = CGSize(width: 360, height: 240)" Islet/Notch/NotchPillView.swift` matches
    - `grep -n "height: CGFloat? = nil" Islet/Notch/NotchPillView.swift` matches inside the blobShape signature
    - The 3 existing blobShape call sites (`expandedIsland`, `mediaExpanded`, `mediaUnavailable`) are textually unchanged (they never pass the new `height:` argument)
    - `cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <done>OnboardingViewState.swift compiles; blobShape accepts an optional height override with zero behavior change for existing callers; onboardingSize constant exists.</done>
</task>

<task type="auto">
  <name>Task 2: onboardingCarousel(step:) view, new init params, Done-screen Launch-at-Login mirror</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift (this session's Task 1 output -- re-read after Task 1 completes to see the exact new blobShape signature and onboardingSize placement)
    .planning/phases/26-onboarding-flow/26-UI-SPEC.md (the FULL design contract -- Screen structure, Bottom nav row, Button/chip style, Permission row layout, Typography, Color, Copywriting Contract tables -- every value below is sourced from this file, read it in full before writing this view)
    Islet/SettingsView.swift lines 67-86 (D-10's exact toggle block to mirror verbatim -- see interfaces block above, do not re-read the whole file)
  </read_first>
  <action>
    Add `case .onboarding(let step): onboardingCarousel(step)` to the `switch presentation` in `body`
    (as a new case, placement does not matter since it is a `switch`, not `if`-chain).

    Implement `private func onboardingCarousel(_ step: OnboardingStep) -> some View` calling
    `blobShape(topCornerRadius: 6, bottomCornerRadius: 32, shelfItems: [], height: Self.onboardingSize.height)`
    (shelfItems is always `[]` -- the shelf never shows during onboarding, per CONTEXT.md's code_context).
    Inside the content closure, build a `VStack(alignment: .leading, spacing: 0)` with:
    - `.padding(.top, 32)` top camera-clearance (matches `mediaExpanded`'s existing convention)
    - `.padding(.horizontal, 16)` screen content padding
    - A `switch step` rendering the 4 screens' heading/body/step-specific-content per the UI-SPEC's
      Typography table (Display 20px semibold for the Welcome heading only; Heading 16px semibold for
      the other 3 step titles; Body 12px regular `.foregroundStyle(.secondary)`; all `.system(design: .rounded)`)
      and Copywriting Contract (transcribe every string EXACTLY as written there, e.g. "Meet Islet",
      "Your notch, upgraded. Now Playing, charging, and a drag-and-drop shelf — always one glance away.",
      "Your 3-day trial has started", "A few permissions", "You're all set", etc. -- do not paraphrase).
    - The Permissions step (`.permissions`) renders 3 rows via a small private helper
      `permissionRow(icon: String, label: String, reason: String, granted: Bool?, onGrant: @escaping () -> Void) -> some View`
      per the UI-SPEC's Permission row layout table: `HStack(spacing: 8)` with a 16px SF Symbol
      (`.foregroundStyle(.white.opacity(0.7))`), a `VStack(alignment: .leading, spacing: 4)` of Label (14px
      semibold) over Reason (12px regular secondary), then either a "Grant" chip Button (calls
      `onOnboardingGrant(permission)`) or the granted-state view: `granted == true` -> green 12px checkmark
      + "Granted" (`.foregroundStyle(.green)`); anything else (`nil` or `false`) -> plain grey "Not granted"
      text, no icon (D-03 -- deliberately NOT the codebase's existing checkmark/xmark pair). Icons exactly:
      Bluetooth `antenna.radiowaves.left.and.right`, Calendar `calendar`, Location `location.fill`. Read
      granted state from `onboardingState.bluetoothGranted` / `.calendarGranted` / `.locationGranted`.
    - The Trial/License/Buy step (`.trialLicenseBuy`) has two secondary Buttons ("Enter License Key",
      "Buy Islet — €7.99") that BOTH call `onOnboardingOpenSettings()` -- do not build a TextField, do not
      call NSWorkspace directly, do not add any license-validation logic here (D-05 LOCKED, zero new
      logic; both buttons exist only to hand off to the real Settings window).
    - The Done step (`.done`) includes a verbatim-mirrored Launch-at-Login toggle: local
      `@State private var onboardingLaunchAtLogin = LaunchAtLogin.isEnabled` declared as a property on
      NotchPillView is WRONG (would leak across all presentation states) -- instead, factor the Done
      step's content into its own small private `@ViewBuilder var onboardingDoneStep: some View` (or a
      private helper struct `OnboardingDoneStep: View`) that owns this `@State` locally, scoped to just
      that step's lifetime, with the exact `onChange` body from the interfaces block above (same
      `LaunchAtLogin.set`/`.requiresApproval`/`.openLoginItemsSettings()` calls, unchanged). Do not add a
      closure for the toggle itself (D-10: same underlying SMAppService state, no new/duplicate flag).
    - A bottom nav `HStack`: Back button (hidden on `.welcome`, calls `onOnboardingBack()` on steps 2-4,
      always enabled per D-09) leading, `Spacer()`, primary CTA button trailing (label per step:
      "Next"/"Next"/"Next"/"Finish" -- Finish calls `onOnboardingFinish()`, the other 3 call
      `onOnboardingNext()`). Both Back and the primary CTA use the shared chip style: `RoundedRectangle(cornerRadius: 8, style: .continuous)` fill `Color.white.opacity(0.12)`, 12px horizontal / 8px vertical padding, 14px semibold white text (Grant chips inside permissionRow use 12px semibold, smaller, per UI-SPEC).

    Add the new init params to the struct (after the existing `shelfViewState` @ObservedObject, before
    `@Environment(\.activityAccent)`): `@ObservedObject var onboardingState: OnboardingViewState`
    (non-defaulted, matching the outfit/shelfViewState convention). After the existing closure block
    (after `onShelfItemDragStarted`), add 5 new defaulted no-op closures:
    `var onOnboardingNext: () -> Void = {}`, `var onOnboardingBack: () -> Void = {}`,
    `var onOnboardingGrant: (OnboardingPermission) -> Void = { _ in }`,
    `var onOnboardingOpenSettings: () -> Void = {}`, `var onOnboardingFinish: () -> Void = {}`.

    Update all 8 existing `#Preview` blocks to add `onboardingState: OnboardingViewState()` to every
    `NotchPillView(...)` construction (they will fail to compile otherwise, since onboardingState is
    non-defaulted).
  </action>
  <acceptance_criteria>
    - `grep -n "case .onboarding(let step):" Islet/Notch/NotchPillView.swift` matches inside the body switch
    - `grep -n "func onboardingCarousel" Islet/Notch/NotchPillView.swift` matches
    - `grep -c "onboardingState: OnboardingViewState()" Islet/Notch/NotchPillView.swift` returns 8 (one per existing #Preview)
    - `grep -n "\"Meet Islet\"" Islet/Notch/NotchPillView.swift` and `grep -n "\"Your 3-day trial has started\"" Islet/Notch/NotchPillView.swift` and `grep -n "\"A few permissions\"" Islet/Notch/NotchPillView.swift` and `grep -n "\"You're all set\"" Islet/Notch/NotchPillView.swift` all match (exact Copywriting Contract strings present)
    - `grep -n "antenna.radiowaves.left.and.right\|\"calendar\"\|location.fill" Islet/Notch/NotchPillView.swift` matches all 3 SF Symbol names
    - `grep -n "onOnboardingGrant: (OnboardingPermission) -> Void = { _ in }" Islet/Notch/NotchPillView.swift` matches
    - No `TextField` or `NSWorkspace` reference was added by this task (`git diff` for this file shows zero new `TextField(` or `NSWorkspace.shared.open` occurrences)
    - `cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <done>NotchPillView renders .onboarding(step) for all 4 steps per the UI-SPEC contract; all 8 previews compile with the new onboardingState param; build gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Onboarding view -> Settings window hand-off | User-tapped "Enter License Key"/"Buy Islet" buttons call `onOnboardingOpenSettings()` only -- no data crosses this boundary, it is a pure navigation signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Elevation of Privilege | `onboardingCarousel`'s Trial/License/Buy step | mitigate | This view NEVER calls `LicenseState.shared` or writes any `license.*`/`trial.*` key directly -- "Enter License Key" and "Buy Islet" both route exclusively through `onOnboardingOpenSettings()` into the existing, unmodified `SettingsView.activate()`/`PolarLicenseService` path (D-05 LOCKED); code-review-depth check: `grep -n "LicenseState" Islet/Notch/NotchPillView.swift` must return zero matches after this plan |
| T-26-03 (carried from 26-01) | Tampering | `OnboardingViewState`'s granted flags | accept | Purely a display-state holder, never persisted, never read by any security/licensing decision -- worst case a stale flag shows the wrong grey/green state for one session, no functional or security impact |
</threat_model>

<verification>
Build gate passes on both tasks. Visual/copy correctness against 26-UI-SPEC.md is confirmed in Plan
26-04's on-device checkpoint (this plan produces the view code; the checkpoint is where a human
actually sees it rendered in the real notch panel).
</verification>

<success_criteria>
- NotchPillView.swift compiles with the new .onboarding case, onboardingCarousel(_:), and all 6 new
  init additions (1 @ObservedObject + 5 closures).
- OnboardingViewState.swift exists and compiles.
- Every Copywriting Contract string, SF Symbol name, and layout value from 26-UI-SPEC.md is present
  verbatim in the new code.
- Zero new license/NSWorkspace/TextField logic introduced (D-05 boundary respected).
</success_criteria>

<output>
Create `.planning/phases/26-onboarding-flow/26-02-SUMMARY.md` when done
</output>
