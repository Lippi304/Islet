---
phase: 26
plan: 04
type: execute
wave: 3
depends_on: ["26-02", "26-03"]
files_modified:
  - Islet/Notch/NotchWindowController.swift
autonomous: false
requirements: [ONBOARD-01, ONBOARD-02, ONBOARD-03]

must_haves:
  truths:
    - "First launch shows Welcome -> Trial/License/Buy -> Permissions -> Done, replacing the old isFirstLaunch -> openSettings() branch entirely"
    - "Each Permissions row's own Grant control fires exactly that permission's real system prompt, independently of the other two rows (D-02)"
    - "Onboarding shows exactly once (D-08/D-09): Finish persists the completed flag, collapses the island back to normal idle, and never re-shows on a later relaunch"
    - "No step offers a whole-flow exit before Done -- a stray tap on the onboarding card background is a no-op, not a collapse (D-09)"
  artifacts:
    - path: Islet/Notch/NotchWindowController.swift
      provides: "onboarding-aware visibleContentZone()/positionAndShow() geometry, isOnboardingActive collapse guards, advanceOnboarding/grantOnboardingPermission/openOnboardingSettings/finishOnboarding handlers, makeRootView(...) wiring"
      contains: "func finishOnboarding()"
  key_links:
    - from: Islet/Notch/NotchWindowController.swift
      to: Islet/Notch/NotchPillView.swift
      via: "makeRootView(accentIndex:) NotchPillView(..., onboardingState: onboardingState, onOnboardingNext: ..., onOnboardingGrant: ..., onOnboardingFinish: ...)"
      pattern: "onboardingState: onboardingState"
    - from: Islet/Notch/NotchWindowController.swift
      to: Islet/ActivitySettings.swift
      via: "finishOnboarding() writes UserDefaults.standard.set(true, forKey: ActivitySettings.onboardingCompletedKey)"
      pattern: "onboardingCompletedKey"
---

<objective>
Wire the full onboarding session's interactive behavior into `NotchWindowController`: keep the
panel forced-open and correctly hit-testable for the onboarding card's actual (taller) bounds
without reintroducing the CR-01 click-through regression, ignore stray background taps so the
forced flow can never be exited early, trigger the real Bluetooth/Location/Calendar system prompts
from each Permissions row independently, hop to the existing Settings window for license entry,
and persist completion on Finish. This is the plan that makes the phase's 4 ROADMAP success
criteria observably true on-device.

Purpose: close the loop between Plan 26-02's view contract and Plan 26-03's launch-time state,
with the one architecturally-necessary deviation from RESEARCH.md this plan makes explicit: (as
`visibleContentZone()` currently derives its size purely from `expandedSize`/shelf state, not
onboarding state) the onboarding card's real 240pt height would leave its own bottom nav row
outside the click-through-interactive rect unless `visibleContentZone()` is made onboarding-aware
-- this plan adds that one small, targeted branch WITHOUT touching `syncClickThrough()`'s
interactive-value OR-logic (the actual CR-01 regression shape stays avoided).
Output: the fully wired `NotchWindowController.swift`; a working, on-device-verified onboarding flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-onboarding-flow/26-CONTEXT.md
@.planning/phases/26-onboarding-flow/26-RESEARCH.md
@.planning/phases/26-onboarding-flow/26-VALIDATION.md
Islet/Notch/OnboardingFlow.swift
Islet/Notch/OnboardingViewState.swift
</context>

<interfaces>
<!-- Exact current code this plan modifies, read verbatim this session. -->

Islet/Notch/NotchWindowController.swift `visibleContentZone()` (lines 842-850, current, unmodified
by Plan 26-03):
```
private func visibleContentZone() -> CGRect? {
    guard let hotZone else { return nil }
    let collapsedFrame = hotZone.insetBy(dx: hotZonePadding, dy: hotZonePadding)
    let shelfHeight = shelfViewState.items.isEmpty ? 0 : NotchPillView.shelfRowHeight
    let visibleFrame = expandedNotchFrame(collapsed: collapsedFrame,
                                          expandedSize: CGSize(width: expandedSize.width,
                                                                height: expandedSize.height + shelfHeight))
    return visibleFrame.insetBy(dx: -hotZonePadding, dy: -hotZonePadding)
}
```

`syncClickThrough()` (lines 903-917, current -- DO NOT MODIFY this function's body; it stays
exactly as-is, its correctness depends only on `visibleContentZone()`'s return value being right):
```
private func syncClickThrough() {
    let interactive: Bool
    if interaction.isExpanded {
        interactive = visibleContentZone()?.contains(lastPointerLocation) ?? false
    } else {
        interactive = pointerInZone
    }
    panel?.ignoresMouseEvents = !interactive
}
```

`handleHoverExit()`'s grace `DispatchWorkItem` (lines 921-946, current):
```
private func handleHoverExit() {
    withAnimation(...) { interaction.phase = nextState(interaction.phase, .pointerExited) }
    let work = DispatchWorkItem { [weak self] in
        guard let self else { return }
        guard !self.isDraggingShelfItem else { return }
        withAnimation(...) { self.interaction.phase = nextState(self.interaction.phase, .graceElapsed); self.renderPresentation() }
        self.updateVisibility()
        self.syncClickThrough()
    }
    graceWorkItem = work
    DispatchQueue.main.asyncAfter(deadline: .now() + graceDelay, execute: work)
    ...
}
```

`handleClick()` (lines 965-1000, current):
```
private func handleClick() {
    let wasExpanded = interaction.isExpanded
    withAnimation(...) {
        interaction.phase = nextState(interaction.phase, .clicked)
        ...
        renderPresentation()
    }
    if !interaction.isExpanded { updateVisibility() }
    syncClickThrough()
}
```

`positionAndShow(on:)`'s panel-union (lines 681-690, current):
```
let expandedFrame = expandedNotchFrame(collapsed: collapsedFrame, expandedSize: CGSize(width: expandedSize.width, height: expandedSize.height + NotchPillView.shelfRowHeight))
let wings = wingsFrame(collapsed: collapsedFrame, wingsSize: wingsSize)
let panelFrame = expandedFrame.union(wings)
```

`makeRootView(accentIndex:)` (lines 1084-1101, current):
```
private func makeRootView(accentIndex: Int) -> some View {
    NotchPillView(interaction: interaction,
                  nowPlaying: nowPlayingState,
                  presentationState: presentationState,
                  outfit: outfitState,
                  shelfViewState: shelfViewState,
                  onClick: { [weak self] in self?.handleClick() },
                  onTogglePlayPause: { [weak self] in self?.nowPlayingMonitor?.togglePlayPause() },
                  onNext: { [weak self] in self?.nowPlayingMonitor?.nextTrack() },
                  onPrevious: { [weak self] in self?.nowPlayingMonitor?.previousTrack() },
                  onShelfItemTap: { [weak self] item in self?.handleShelfItemTap(item) },
                  onShelfItemDelete: { [weak self] id in self?.handleShelfItemDelete(id) },
                  onShelfClearAll: { [weak self] in self?.handleShelfClearAll() },
                  onShelfItemDragStarted: { [weak self] in self?.beginShelfItemDrag() })
        .environment(\.activityAccent, ActivitySettings.accent(for: accentIndex))
}
```

`AppDelegate.openSettings()` (the exact 3-line body to duplicate, since NotchWindowController has
no reference to AppDelegate):
```
NSApp.activate(ignoringOtherApps: true)
NotificationCenter.default.post(name: .openIsletSettings, object: nil)
NSApp.windows.first { $0.identifier?.rawValue == "settings" }?.makeKeyAndOrderFront(nil)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Onboarding-aware click geometry + collapse guards + panel sizing</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift lines 200-260, 650-720, 804-1000 (property declarations,
    positionAndShow, handlePointer/visibleContentZone/handleHoverEnter/syncClickThrough/handleHoverExit/
    handleClick -- see interfaces block above for verbatim quotes, but re-read the live ranges since
    Plan 26-03 already edited nearby lines in this same file)
    Islet/Notch/NotchPillView.swift (Plan 26-02's output -- confirm the exact `onboardingSize` constant name/value)
    .planning/phases/26-onboarding-flow/26-PATTERNS.md "Pattern 3" and "Anti-Patterns to Avoid" sections (the CR-01 discipline this task must not violate)
  </read_first>
  <action>
    1. Add `private let onboardingState = OnboardingViewState()` as a stored property, placed near the
       existing `shelfViewState`/`outfitState` model declarations (same "controller owns it, view
       observes it" convention).
    2. In `visibleContentZone()`, replace the single `let visibleFrame = expandedNotchFrame(collapsed: collapsedFrame, expandedSize: CGSize(width: expandedSize.width, height: expandedSize.height + shelfHeight))`
       line with an onboarding-aware branch computed BEFORE it:
       `let contentSize: CGSize = isOnboardingActive ? NotchPillView.onboardingSize : CGSize(width: expandedSize.width, height: expandedSize.height + shelfHeight)`
       then `let visibleFrame = expandedNotchFrame(collapsed: collapsedFrame, expandedSize: contentSize)`.
       This changes ONLY what geometry `visibleContentZone()` measures -- `syncClickThrough()` itself
       (its `interactive = visibleContentZone()?.contains(lastPointerLocation) ?? false` line) is not
       touched at all, preserving the CR-01 discipline (the interactive VALUE stays purely
       `visibleContentZone()`-derived, never OR'd with a broader flag).
    3. In `handleHoverExit()`'s grace `DispatchWorkItem`, add a guard parallel to the existing
       `guard !self.isDraggingShelfItem else { return }`: `guard !self.isOnboardingActive else { return }`
       (same line, same style, placed directly after the shelf-item guard).
    4. In `handleClick()`, add `guard !isOnboardingActive else { return }` as the very FIRST line of the
       function body (before `let wasExpanded = interaction.isExpanded`). This makes a stray tap on the
       onboarding card's background (which still triggers `blobShape`'s ancestor `.onTapGesture { onClick() }`)
       a complete no-op during onboarding -- the Next/Back/Grant/Finish buttons are real SwiftUI `Button`s
       inside the content closure and already intercept their own taps before this ancestor gesture fires
       (same precedent as `mediaExpanded`'s existing transport-button row), so this one guard is sufficient
       and no `blobShape` signature change is needed for tap suppression.
    5. In `positionAndShow(on:)`, add a third panel-frame union member: after the existing
       `let wings = wingsFrame(collapsed: collapsedFrame, wingsSize: wingsSize)` line, add
       `let onboardingFrame = expandedNotchFrame(collapsed: collapsedFrame, expandedSize: NotchPillView.onboardingSize)`,
       then change `let panelFrame = expandedFrame.union(wings)` to
       `let panelFrame = expandedFrame.union(wings).union(onboardingFrame)` (mirrors exactly how
       `wingsFrame` was added as a second union member in Phase 3 -- the panel is sized once, up front,
       to the union of every possible content size, never resized mid-activity).
  </action>
  <acceptance_criteria>
    - `grep -n "private let onboardingState = OnboardingViewState()" Islet/Notch/NotchWindowController.swift` matches
    - `grep -n "isOnboardingActive ? NotchPillView.onboardingSize" Islet/Notch/NotchWindowController.swift` matches inside `visibleContentZone()`
    - `grep -n "guard !self.isOnboardingActive else { return }" Islet/Notch/NotchWindowController.swift` matches inside `handleHoverExit()`'s DispatchWorkItem
    - `grep -n "guard !isOnboardingActive else { return }" Islet/Notch/NotchWindowController.swift` matches as the first line of `handleClick()`
    - `grep -n "let onboardingFrame = expandedNotchFrame" Islet/Notch/NotchWindowController.swift` and `grep -n ".union(onboardingFrame)" Islet/Notch/NotchWindowController.swift` both match
    - `grep -n "pointerInZone" Islet/Notch/NotchWindowController.swift | grep -v "^.*://"` -- confirm `syncClickThrough()`'s own body (lines ~903-917) is byte-for-byte unchanged from the interfaces block above (no new OR-condition was added to its `interactive` boolean)
    - `cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <done>The panel is sized to include the onboarding card at launch; the click-through interactive rect correctly covers the full 240pt onboarding card (not just the 144pt expandedIsland box); background taps during onboarding are inert; syncClickThrough()'s interactive-value logic is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Step/permission/settings/finish handlers + hosting-view wiring</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift (this session's Task 1 output, plus the current
    `handleClick()`, `startBluetoothMonitor()`, `startLocationOnce()`, `refreshCalendar()`,
    `makeRootView(accentIndex:)` -- see interfaces block above)
    Islet/Notch/OnboardingFlow.swift (nextOnboardingStep, OnboardingEvent, OnboardingPermission)
    Islet/AppDelegate.swift `openSettings()` body (interfaces block above -- the exact 3 lines to duplicate)
  </read_first>
  <action>
    Add `import CoreBluetooth` to the file's import list (needed ONLY for the read-only `CBManager.authorization`
    static property below -- this is a status READ, not a new BLE-central mechanism; do not construct a
    `CBCentralManager`).

    Add these private handler methods (place near `handleClick()`, same `@MainActor` context):
    - `private func advanceOnboarding(_ event: OnboardingEvent)`:
      `guard let step = onboardingStep else { return }`; wrap in the existing
      `withAnimation(.spring(response: springResponse, dampingFraction: springDamping))` and inside set
      `onboardingStep = nextOnboardingStep(step, event)` then call `renderPresentation()`.
    - `private func grantOnboardingPermission(_ permission: OnboardingPermission)`: `switch permission`:
      - `.bluetooth`: call `startBluetoothMonitor()`, then
        `DispatchQueue.main.asyncAfter(deadline: .now() + 1.0) { [weak self] in self?.onboardingState.bluetoothGranted = (CBManager.authorization == .allowedAlways) }`
        (a one-shot delayed status read -- IOBluetoothDevice.register has no completion callback, so this
        is the pragmatic, minimal way to reflect the real TCC outcome without hand-rolling a new
        permission API; document this exact reasoning as a code comment).
      - `.calendar`: call `refreshCalendar()` (already independently callable, unchanged).
      - `.location`: call `startLocationOnce()` (from Plan 26-03, already independently callable, unchanged).
      Inside `refreshCalendar()`'s existing completion closure, add ONE line:
      `self?.onboardingState.calendarGranted = (glance != nil)` (harmless to set on every call, not just
      from onboarding -- the flag is simply unread once onboarding is inactive). Inside
      `startLocationOnce()`'s completion closure (from Plan 26-03), add ONE line:
      `self?.onboardingState.locationGranted = (location != nil)`, same reasoning.
    - `private func openOnboardingSettings()`: duplicate `AppDelegate.openSettings()`'s exact 3-line body
      (`NSApp.activate(ignoringOtherApps: true)`, `NotificationCenter.default.post(name: .openIsletSettings, object: nil)`,
      `NSApp.windows.first { $0.identifier?.rawValue == "settings" }?.makeKeyAndOrderFront(nil)`) --
      intentional small duplication since NotchWindowController has no reference to AppDelegate and every
      API used here is already public.
    - `private func finishOnboarding()`: `UserDefaults.standard.set(true, forKey: ActivitySettings.onboardingCompletedKey)`;
      wrap in `withAnimation(.spring(response: springResponse, dampingFraction: springDamping))`: set
      `isOnboardingActive = false`, `onboardingStep = nil`, `interaction.phase = nextState(interaction.phase, .clicked)`
      (the existing `.expanded -> .collapsed` transition, same one `handleClick()` uses for a toggle-shut
      click), call `renderPresentation()`. After the animation block, call `updateVisibility()` then
      `syncClickThrough()` (mirrors `handleHoverExit()`'s own post-collapse sequence). Then, since a
      genuinely fresh launch deferred them, explicitly start what onboarding withheld:
      `if activityEnabled(ActivitySettings.deviceKey) { startBluetoothMonitor() }` and `startOutfitRefresh()`
      -- both are already idempotent (`guard ... == nil` internally), so calling them here is always safe
      even if a permission was already granted mid-onboarding.
    - NEVER call `LicenseState.shared` or write any `license.*`/`trial.*` key from any of these 4 new
      methods (T-26-04) -- `grep -n "LicenseState" Islet/Notch/NotchWindowController.swift` must show zero
      NEW matches introduced by this task (any pre-existing matches from `licenseState = LicenseState.shared`
      elsewhere in the file are untouched and unrelated).

    Update `makeRootView(accentIndex:)`'s `NotchPillView(...)` call to add 6 new arguments (after the
    existing `onShelfItemDragStarted:` argument):
    `onboardingState: onboardingState`,
    `onOnboardingNext: { [weak self] in self?.advanceOnboarding(.next) }`,
    `onOnboardingBack: { [weak self] in self?.advanceOnboarding(.back) }`,
    `onOnboardingGrant: { [weak self] permission in self?.grantOnboardingPermission(permission) }`,
    `onOnboardingOpenSettings: { [weak self] in self?.openOnboardingSettings() }`,
    `onOnboardingFinish: { [weak self] in self?.finishOnboarding() }`.
  </action>
  <acceptance_criteria>
    - `grep -n "import CoreBluetooth" Islet/Notch/NotchWindowController.swift` matches
    - `grep -n "func advanceOnboarding\|func grantOnboardingPermission\|func openOnboardingSettings\|func finishOnboarding" Islet/Notch/NotchWindowController.swift` returns all 4 method names
    - `grep -n "CBManager.authorization == .allowedAlways" Islet/Notch/NotchWindowController.swift` matches
    - `grep -n "onboardingState.calendarGranted = (glance != nil)" Islet/Notch/NotchWindowController.swift` and `grep -n "onboardingState.locationGranted = (location != nil)" Islet/Notch/NotchWindowController.swift` both match
    - `grep -n "onboardingState: onboardingState" Islet/Notch/NotchWindowController.swift` matches inside `makeRootView(accentIndex:)`'s `NotchPillView(...)` call
    - `grep -n "onboardingCompletedKey" Islet/Notch/NotchWindowController.swift` matches inside `finishOnboarding()`
    - No new `LicenseState`/`license\.`/`trial\.` reference was introduced by this task's diff (T-26-04 code-review gate)
    - `cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"` succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <done>All 4 handler methods exist and are wired into makeRootView(...); build gate passes; no licensing state is touched anywhere in this task's diff.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device UAT -- full onboarding flow, real permission prompts, exactly-once persistence</name>
  <files>Islet/Notch/NotchWindowController.swift, Islet/Notch/NotchPillView.swift, Islet/AppDelegate.swift</files>
  <action>
    No file edits in this task -- reset onboarding/TCC state, build and run the app on-device (Cmd-R in
    Xcode), and perform the full carousel walkthrough described in how-to-verify below. This task's only
    output is the recorded PASSED/FAILED verdict covering this phase's 4 ROADMAP Success Criteria and
    ONBOARD-01/02/03.
  </action>
  <what-built>
    A first-launch onboarding carousel (Welcome -> Trial/License/Buy -> Permissions -> Done) hosted
    inside the real expanded notch panel, replacing the old Settings-auto-open behavior. Each
    Permissions row fires its own real system prompt independently on Grant; "Enter License Key" and
    "Buy Islet" hop to the existing Settings window; Done includes a Launch-at-Login toggle; Finish
    persists a one-time flag and returns the island to normal idle behavior.
  </what-built>
  <how-to-verify>
    1. Reset onboarding + TCC state for a clean fresh-install simulation: in Xcode's DEBUG build,
       quit Islet, run `defaults delete com.lippi304.islet onboarding.completed` (or the whole domain
       via `defaults delete com.lippi304.islet` if you also want to reset activity toggles/accent), and
       `tccutil reset Bluetooth com.lippi304.islet && tccutil reset Calendar com.lippi304.islet && tccutil reset Location com.lippi304.islet`
       in Terminal, then just relaunch from Xcode (Cmd-R) after the `defaults`/`tccutil` resets.
    2. Confirm the island opens EXPANDED immediately at launch showing the Welcome step ("Meet Islet"),
       with no Settings window appearing anywhere.
    3. Tap Next through Welcome -> Trial/License/Buy (confirm "Your 3-day trial has started" copy) ->
       Permissions.
    4. On the Permissions step, confirm all 3 rows render (Bluetooth/Calendar/Location) each with its
       own one-line reason and its own "Grant" chip. Tap each row's Grant independently, in any order:
       confirm each fires ITS OWN real system permission dialog (not all three at once), and after
       responding, the row updates to green "Granted" or quiet grey "Not granted" (try denying at least
       one to confirm the quiet degrade -- no error dialog anywhere).
    5. Confirm the 240pt-tall Permissions card does not clip any row or the bottom Next/Back nav, and
       every button (Back, Next, all 3 Grant chips) is clickable across the whole card, not just a
       narrow hot-zone.
    6. Confirm a stray click on the card's empty background does NOT collapse the island at any step.
    7. Tap Next to Done, confirm "You're all set" + the Launch-at-Login toggle (flip it, confirm it
       matches Settings' own toggle state if you check Settings afterward), tap Finish.
    8. Confirm the island collapses back to the normal idle pill immediately, no Settings window opens.
    9. Quit and relaunch Islet: confirm onboarding does NOT reappear (exactly-once, ONBOARD-03).
    10. On this same relaunch, confirm Bluetooth/Now-Playing/Charging features you granted still work
        normally (the deferred monitors actually started on Finish, not just the flag flip).
    11. Separately, on a build where `onboarding.completed` is left untouched (i.e. skip step 1's
        reset), confirm a normal relaunch behaves EXACTLY as before this phase -- no onboarding, no
        permission-prompt delay, Settings still reachable via the menu-bar item.
  </how-to-verify>
  <resume-signal>Type "approved" once all 11 steps check out, or describe exactly which step failed and what you observed instead.</resume-signal>
  <verify>
    <human-check>All 11 how-to-verify steps confirmed on-device by the user; resume-signal explicitly approved.</human-check>
  </verify>
  <done>User has typed "approved" for the full on-device onboarding walkthrough, confirming ROADMAP.md Phase 26's 4 Success Criteria and ONBOARD-01/02/03 are observably true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Onboarding UI (SwiftUI, click-through panel) -> real OS permission prompts | Grant taps trigger real `IOBluetoothDevice.register`/`CLLocationManager.requestWhenInUseAuthorization`/`EKEventStore.requestFullAccessToEvents` -- all pre-existing, unmodified system-framework call sites, unchanged by this plan |
| Onboarding UI -> Settings window hand-off | `openOnboardingSettings()` only posts the existing `.openIsletSettings` notification + activates the app -- carries no payload, no license data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Elevation of Privilege | `finishOnboarding()`, `advanceOnboarding()`, `grantOnboardingPermission()`, `openOnboardingSettings()` | mitigate | None of these 4 methods write `LicenseState.shared` or any `license.*`/`trial.*` key -- code-review gate enforced in Task 2's acceptance criteria (grep for zero new `LicenseState` references); the ONLY licensing-adjacent action is opening the existing, unmodified Settings window |
| T-26-01 (carried) | Tampering / functional regression | `visibleContentZone()`'s onboarding-aware branch | mitigate | The change is additive and scoped to computing `contentSize` differently when `isOnboardingActive` -- `syncClickThrough()`'s own interactive-value logic (the actual CR-01 regression surface) is verified byte-for-byte unchanged in Task 1's acceptance criteria |
| T-26-06 | Denial of Service (minor) | `grantOnboardingPermission(.bluetooth)`'s 1s delayed `CBManager.authorization` read | accept | Worst case the row briefly shows a stale/neutral state for ~1s after tapping Grant -- no crash path, no retry loop, purely cosmetic and self-correcting on the next render |
</threat_model>

<verification>
Both automated tasks' build gates must pass. Task 3's on-device checkpoint is the authoritative
verification for this phase's 4 ROADMAP success criteria -- none of ONBOARD-01/02/03's real-prompt,
real-window-focus, or real-animation-feel behaviors are XCTest-verifiable per this project's own
`xcodebuild-test-headless-hang` constraint (documented in 26-VALIDATION.md).
</verification>

<success_criteria>
- All 3 tasks complete; Task 3's checkpoint is explicitly approved by the user.
- ROADMAP.md Phase 26 Success Criteria 1-4 are all observably true on-device.
- No regression to any pre-existing launch/hover/click/drag/shelf behavior (confirmed by the existing
  automated test suite + Task 3 step 11's "untouched" relaunch check).
</success_criteria>

<output>
Create `.planning/phases/26-onboarding-flow/26-04-SUMMARY.md` when done
</output>
