---
phase: 53-hover-to-resume-idle-preview
plan: 02
type: execute
wave: 2
depends_on: ["53-01"]
files_modified: []
autonomous: false
requirements: [RESUME-01, RESUME-02]

must_haves:
  truths:
    - "On real notched hardware, hovering the idle island after a track has played this session (then stopped/paused/quit) shows the album-art-left/equalizer-right preview, pixel-identical to the live glance."
    - "Hovering the idle island BEFORE anything has played this session shows nothing new."
    - "Clicking the preview resumes playback wherever the underlying transport supports it (per Plan 53-01 Task 1's spike verdict), and shows 'Wiedergabe nicht möglich' with a clean collapse when it doesn't."
    - "Hovering/clicking across the FULL 290pt preview width (including both edges) works reliably — no dead click-through zones, no premature grace-collapse near the album-art or equalizer edges."
    - "No regression to any other collapsed-state feature (secondary bubble, charging/device/focus/osd wings, ordinary click-to-expand) sharing collapsedInteractiveZone()."
  artifacts:
    - path: ".planning/phases/53-hover-to-resume-idle-preview/53-02-SUMMARY.md"
      provides: "On-device UAT verdict for all 4 ROADMAP success criteria"
  key_links:
    - from: "on-device manual test"
      to: "ROADMAP.md Phase 53 Success Criteria 1-4"
      via: "human checklist below, executed against the real Debug (and Release) build"
      pattern: "SC#1|SC#2|SC#3|SC#4"
---

<objective>
Confirm on real notched hardware that Plan 53-01's hover-to-resume preview satisfies all 4
ROADMAP Phase 53 Success Criteria end-to-end, and that the click-through hot-zone widening and
resume-tap/failure-feedback machinery behave correctly under live MediaRemote conditions — the
one class of behavior this phase cannot verify via `xcodebuild build`/unit tests alone (per
53-RESEARCH.md/53-VALIDATION.md, mirroring every prior MediaRemote-dependent phase in this
project: 4, 47, 48, 49).

Purpose: Close RESUME-01/RESUME-02 and the v1.8 milestone's open technical question with a real,
on-device verdict rather than a build-only gate.

Output: `53-02-SUMMARY.md` recording the full checklist result; REQUIREMENTS.md's RESUME-01/
RESUME-02 rows updated from Pending to Complete on approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/53-hover-to-resume-idle-preview/53-CONTEXT.md
@.planning/phases/53-hover-to-resume-idle-preview/53-UI-SPEC.md
@.planning/phases/53-hover-to-resume-idle-preview/53-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Full on-device UAT — all 4 ROADMAP success criteria</name>
  <files></files>
  <action>
    No file edits — build and run the app on real notched hardware (Debug, then Release) and walk
    through the 7-step checklist below. This task's only output is the recorded PASSED/FAILED
    verdict covering ROADMAP Phase 53's 4 success criteria plus the 2 Manual-Only Verifications
    flagged in 53-VALIDATION.md (hot-zone hit-testing, live resume/failure behavior).
  </action>
  <what-built>
    Plan 53-01's hover-to-resume idle preview: `idleOrResumePreview`/`resumePreviewWings` render
    branch, `handleResumeTap()` + inferred-failure timeout, and the widened
    `collapsedInteractiveZone()` (Islet/Notch/NotchPillView.swift,
    Islet/Notch/NotchWindowController.swift, Islet/Notch/NowPlayingState.swift).
  </what-built>
  <how-to-verify>
    Build and run on real notched hardware (`xcodebuild build -project Islet.xcodeproj -scheme
    Islet -configuration Debug`, then launch; repeat the same checklist once against a Release
    build before final sign-off).

    1. **SC#2 gate (before anything plays):** Relaunch Islet fresh (or confirm no track has
       played yet this session). Hover the idle collapsed island. Confirm NOTHING new appears —
       today's unchanged idle-pill behavior.
    2. **SC#1 preview visual:** Play a track in Spotify, then pause it via Spotify's own UI (not
       quit). Wait for the ambient now-playing wings to settle back to idle (per the existing
       `nowPlayingLaunchGate`/dismiss-timeout precedent). Hover the now-idle collapsed island.
       Confirm the preview shows: album art (left) + bouncing equalizer bars (right), visually
       identical to the live Now Playing collapsed glance — same size/position/animation.
    3. **Full-width hit-testing (Pitfall 1):** While the preview is showing, move the pointer
       slowly across the ENTIRE width — dead-center, toward the album art (left edge), toward the
       equalizer (right edge). Confirm the preview stays showing (no premature grace-collapse)
       and a click anywhere across that width registers (does not silently no-op).
    4. **SC#3/SC#4 resume click — repeat for all 4 combinations from Plan 53-01 Task 1's spike:**
       (a) Spotify paused-then-resume, (b) Spotify quit-then-resume, (c) Apple Music
       paused-then-resume, (d) Apple Music quit-then-resume. For each: hover the preview, click
       it. Where Plan 53-01's spike found resume works: confirm playback audibly resumes and the
       preview morphs into the REAL live nowPlayingWings state (no lingering stale preview).
       Where the spike found resume does NOT work: confirm "Wiedergabe nicht möglich" replaces
       the equalizer bars (album art stays visible), then the preview collapses to idle after
       roughly half a second — never a silent no-op, never a stuck/frozen preview.
    5. **No expansion on click (D-01):** During step 4, confirm the island NEVER expands to the
       full Home transport view on a preview click — it stays exactly the wings-preview shape
       throughout (resuming, failing, or collapsing).
    6. **Regression check:** Confirm the Phase 42 secondary bubble (calendar countdown + now
       playing pairing, if reproducible) and ordinary collapsed hover/click/click-through
       (charging/device wings, plain click-to-expand) still behave exactly as before — no
       regression from the `collapsedInteractiveZone()` restructure.
    7. If the ~1.75s failure-timeout in Plan 53-01 Task 3 feels too slow or triggers a false
       failure on a legitimately-slower resume (e.g. a cold-launched app), report the felt timing
       so the constant can be retuned before closing this plan.
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 steps pass as described, or describe which step(s) failed and how.</resume-signal>
  <verify>
    <human-check>All 7 how-to-verify steps confirmed on-device by the user against both Debug and Release builds; resume-signal explicitly approved.</human-check>
  </verify>
  <done>User has typed "approved" for the full on-device walkthrough, confirming ROADMAP Phase 53's 4 success criteria and the full-width hit-testing/no-expansion-on-click/regression checks are all observably true on real hardware.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (unchanged from Plan 53-01) | This plan performs verification only — no new code, no new trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-07 | Tampering / Repudiation (verification gap) | Plan 53-01's `handleResumeTap`/`collapsedInteractiveZone` code paths | mitigate | This plan's on-device checklist (Task 1) is the mitigation itself — closes the residual risk that build-green code behaves incorrectly against real, non-simulatable MediaRemote/AppKit hit-testing state, mirroring every prior MediaRemote-dependent phase's own UAT-as-mitigation precedent (4, 47, 48, 49). |
</threat_model>

<verification>
On-device UAT checklist (Task 1) is the phase's terminal verification gate — no further
automated command applies beyond the Debug/Release build gates already passed in Plan 53-01.
</verification>

<success_criteria>
- All 7 checklist steps in Task 1 pass ("approved")
- REQUIREMENTS.md RESUME-01/RESUME-02 rows updated to Complete
- ROADMAP.md Phase 53 checkbox marked complete
</success_criteria>

<output>
Create `.planning/phases/53-hover-to-resume-idle-preview/53-02-SUMMARY.md` when done
</output>
