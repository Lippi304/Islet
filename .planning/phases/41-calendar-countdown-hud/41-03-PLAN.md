---
phase: 41-calendar-countdown-hud
plan: 03
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - Islet/Notch/NotchPillView.swift
  - Islet/SettingsView.swift
autonomous: true
requirements: [HUD-08]

must_haves:
  truths:
    - "The collapsed pill shows a calendar icon (left wing) and a live mm:ss countdown (right wing) that visibly ticks every second while the wing is mounted (D-04)"
    - "Icon and text always recolor together (orange >= 60s remaining, red < 60s) — driven from one shared per-tick color value, never two independently-computed colors that could desync (D-05)"
    - "The event title never appears anywhere on the collapsed pill (D-06)"
    - "Clicking the countdown wing expands to Home, and hovering it does nothing extra — both fall out for free from the shared wingsShape() tap gesture with no new code (D-07/D-08)"
    - "The Settings toggle defaults ON and lives in the plain Activities section, not the permission-gated Focus/OSD section (D-03)"
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "countdownWings(for:) view + urgencyColor/formatMMSS helpers + presentationSwitch .calendarCountdown case"
      contains: "func countdownWings"
    - path: "Islet/SettingsView.swift"
      provides: "Calendar Countdown Activities-section toggle"
      contains: "Calendar Countdown"
  key_links:
    - from: "NotchPillView.presentationSwitch"
      to: "countdownWings(for:)"
      via: "case .calendarCountdown(let activity): countdownWings(for: activity)"
      pattern: "case \\.calendarCountdown"
    - from: "SettingsView Toggle(\"Calendar Countdown\")"
      to: "ActivitySettings.calendarCountdownKey"
      via: "@AppStorage"
      pattern: "calendarCountdownKey"
---

<objective>
Render HUD-08's collapsed-pill wing (calendar icon left, live mm:ss right, orange->red urgency
coloring) and give the user a Settings toggle for it — the two purely presentational pieces of
this phase, independent of Plan 02's monitor/controller wiring (both only need Plan 01's
`CalendarCountdownActivity`/`IslandPresentation.calendarCountdown`/`calendarCountdownKey` to
already exist, which they do).

Purpose: this is the only user-visible surface of the feature. Getting the per-second TimelineView
scoping right (Pitfall: the idle-CPU trap) and the icon+text color-desync risk closed here means
Plan 04's on-device UAT is checking a feature that's already structurally correct.
Output: `countdownWings(for:)` wired into `presentationSwitch`, plus the Settings toggle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-calendar-countdown-hud/41-CONTEXT.md
@.planning/phases/41-calendar-countdown-hud/41-UI-SPEC.md
@.planning/phases/41-calendar-countdown-hud/41-PATTERNS.md
</context>

<interfaces>
<!-- From Plan 01 (Islet/Notch/IslandResolver.swift) — already merged, use directly: -->
```swift
struct CalendarCountdownActivity: Equatable {
    let eventStart: Date
}
// IslandPresentation gains: case calendarCountdown(CalendarCountdownActivity)
```

<!-- From Plan 01 (Islet/ActivitySettings.swift) — already merged, use directly: -->
```swift
static let calendarCountdownKey = "activity.calendarCountdown"
```

<!-- Existing NotchPillView.swift helper this task reuses verbatim (do not reinvent): -->
```swift
private func wingsShape<Content: View>(
    leftWidth: CGFloat = Self.wingsSize.width / 2,
    rightWidth: CGFloat = Self.wingsSize.width / 2,
    @ViewBuilder content: () -> Content
) -> some View
// already applies islandFill, matchedGeometryEffect, liquidGlassEffectLayer, and
// .onTapGesture { onClick() } — the shared tap-to-expand every wing (Charging/Device/Focus/OSD)
// already gets for free.
static let wingsSize = CGSize(width: 290, height: 32)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: countdownWings(for:) view + urgency helpers</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift lines 2259-2287 (`focusWings(for:)` — the icon-left,
    `Spacer()`, content-right `HStack` structure and the `wingsShape(leftWidth:rightWidth:)` call
    shape to mirror)
    Islet/Notch/NotchPillView.swift lines 2757-2802 (`EqualizerBars` — the
    `TimelineView(.animation(paused:))` idle-CPU-trap discipline; read the full header comment,
    this phase uses `TimelineView(.periodic(from:by:))` instead, per the UI-SPEC's already-resolved
    mechanism, but the "the clock gate must wrap ONLY what needs to tick" principle is identical)
    Islet/Notch/NotchPillView.swift lines 742-781 (`presentationSwitch` — where the new
    `case .calendarCountdown(let activity): countdownWings(for: activity)` line is added, placed
    near `.nowPlayingWings` since Countdown is ambient, not a transient)
    .planning/phases/41-calendar-countdown-hud/41-UI-SPEC.md (Countdown Wing Contract section —
    exact leftWidth 118pt / rightWidth default 145pt, 14pt leading / 20pt trailing padding, 13pt
    semibold icon+text sizing, `.monospacedDigit()` requirement, and the "Verification Notes for
    Executor" bullet on driving icon+text from ONE shared urgencyColor computed per tick)
  </read_first>
  <action>
    In Islet/Notch/NotchPillView.swift, add two small private pure helpers near
    `countdownWings(for:)`: `private func urgencyColor(for eventStart: Date, at now: Date) ->
    Color { eventStart.timeIntervalSince(now) < 60 ? .red : .orange }` (D-05: instant threshold at
    60s remaining, no gradient/interpolation) and `private func formatMMSS(_ seconds:
    TimeInterval) -> String { let total = max(0, Int(seconds)); return String(format: "%02d:%02d",
    total / 60, total % 60) }`. Add `private func countdownWings(for activity:
    CalendarCountdownActivity) -> some View` calling `wingsShape(leftWidth: 118, rightWidth:
    Self.wingsSize.width / 2) { ... }` per the UI-SPEC's locked dimensions (118pt reused verbatim
    from Focus's on-device-proven icon-only-flank floor; 145pt default rightWidth, no derived math
    needed since `mm:ss` text is short and self-sizing). CRITICAL DEVIATION from
    41-RESEARCH.md's literal code snippet: that snippet places the `Image` OUTSIDE the
    `TimelineView` (evaluated once, would NOT re-render its color every second) and the `Text`
    INSIDE — this desyncs icon/text color exactly as the UI-SPEC's Verification Notes warns
    against. Instead, wrap the ENTIRE `HStack(spacing: 0) { Image(...); Spacer(); Text(...) }` in
    ONE `TimelineView(.periodic(from: .now, by: 1)) { context in ... }`, and inside that closure
    compute `let remaining = max(0, activity.eventStart.timeIntervalSince(context.date))` and `let
    color = urgencyColor(for: activity.eventStart, at: context.date)` ONCE, then apply `color` to
    BOTH the `Image`'s `.foregroundStyle(color)` and the `Text`'s `.foregroundStyle(color)` — this
    is the "one shared urgencyColor computed property" the UI-SPEC requires, and it ticks both
    together every second since both now live inside the same `TimelineView` closure. `Image`:
    `Image(systemName: "calendar")` (icon per UI-SPEC — reused verbatim from the existing calendar
    glyph elsewhere in this file, not a new glyph choice), `.font(.system(size: 13, weight:
    .semibold))`, `.symbolRenderingMode(.hierarchical)`, `.padding(.leading, 14)`. `Text`:
    `Text(formatMMSS(remaining))`, `.font(.system(size: 13, weight: .semibold, design: .rounded))`,
    `.monospacedDigit()` (prevents width jitter as digits tick), `.padding(.trailing, 20)`. In
    `presentationSwitch`, add `case .calendarCountdown(let activity): countdownWings(for:
    activity)  // Phase 41 / HUD-08: ambient, D-01 always wins over nowPlayingWings` immediately
    after the `case .nowPlayingWings(let p): mediaWingsOrToast(p)` line — DO NOT add any tap-
    gesture, hover-state, or click-handling code: `wingsShape`'s existing `.onTapGesture { onClick()
    }` already gives every wing (including this one) tap-to-expand-to-Home for free (D-07), and
    the absence of any hover modifier here is itself D-08's "no hover-reveal" compliance — this is
    a deliberate zero-addition, not an oversight. DO NOT add any `Text`/label carrying the event's
    title anywhere in this view (D-06) — `CalendarCountdownActivity` structurally has no title
    field to even read.
  </action>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func countdownWings" Islet/Notch/NotchPillView.swift` equals 1
    - `grep -c "func urgencyColor\|func formatMMSS" Islet/Notch/NotchPillView.swift` equals 2
    - `countdownWings(for:)`'s body contains exactly ONE `TimelineView(.periodic` call, and BOTH the `Image` and `Text`'s `.foregroundStyle(...)` calls are lexically inside that same `TimelineView`'s trailing closure — verify by source read (not two separately-scoped color computations)
    - `grep -c "case .calendarCountdown" Islet/Notch/NotchPillView.swift` equals 1, and it appears inside `presentationSwitch`
    - `grep -c "\.onTapGesture\|\.onHover" Islet/Notch/NotchPillView.swift` count is unchanged from before this task (no new tap/hover code added — falls out for free via wingsShape)
    - Build succeeds
  </acceptance_criteria>
  <done>countdownWings(for:) renders icon-left/mm:ss-right with synchronized per-second urgency coloring, wired into presentationSwitch; no title ever rendered; no new tap/hover code.</done>
</task>

<task type="auto">
  <name>Task 2: Settings toggle</name>
  <files>Islet/SettingsView.swift</files>
  <read_first>
    Islet/SettingsView.swift lines 20-59 (existing `@AppStorage` property declarations —
    specifically line 33's `@AppStorage(ActivitySettings.deviceKey) private var deviceEnabled =
    true`, the plain default-true shape to mirror; NOT `focusEnabled`'s default-false/popover
    shape at lines 34-38)
    Islet/SettingsView.swift lines 213-241 (`Section("Activities")` — where the new
    `Toggle("Calendar Countdown", isOn: $calendarCountdownEnabled)` line is added, placed
    alongside `Toggle("Devices", isOn: $deviceEnabled)`, NOT inside the Focus/OSD
    permission-explanation block)
  </read_first>
  <action>
    Add `@AppStorage(ActivitySettings.calendarCountdownKey) private var calendarCountdownEnabled =
    true` near the other plain-default-true Activities properties (alongside `deviceEnabled`),
    with a comment "Phase 41 / HUD-08 (D-03) — default ON, matches Charging/Device's opt-out
    convention; no permission popover needed (CalendarService's existing EventKit authorization,
    Phase 14/28, is reused as-is)." Add `Toggle("Calendar Countdown", isOn:
    $calendarCountdownEnabled)` inside `Section("Activities")`, placed directly after
    `Toggle("Devices", isOn: $deviceEnabled)` and BEFORE the Focus Mode HUD toggle block (D-03's
    plain opt-out toggle belongs with Charging/Now Playing/Song-Change Toast/Devices, not with the
    permission-gated Focus/OSD toggles that follow). Do NOT add an `.onChange` handler, a
    `.popover`, or a status-hint `Text` row — unlike Focus/OSD, this feature has no new permission
    to request or explain (matches the UI-SPEC's Copywriting Contract: "no status hint needed
    below the toggle").
  </action>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "calendarCountdownEnabled" Islet/SettingsView.swift` is at least 2 (property declaration + Toggle binding)
    - `grep -c "Toggle(\"Calendar Countdown\"" Islet/SettingsView.swift` equals 1
    - The new Toggle line appears BEFORE `Toggle("Focus Mode HUD"` in file order (source read) — confirms placement in the plain-toggle group, not the permission-gated group
    - `grep -A2 "Calendar Countdown" Islet/SettingsView.swift` shows no `.onChange`/`.popover` attached to this specific Toggle
    - Build succeeds
  </acceptance_criteria>
  <done>A plain default-ON "Calendar Countdown" toggle exists in Settings > Activities, no permission popover, matches D-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `CalendarCountdownActivity` -> `countdownWings(for:)` | Pure `Date` value consumed for display math only — no string/title data crosses into this view at all (D-06 enforced structurally by Plan 01's type shape, reinforced here by never adding a title-rendering `Text`) |
| UserDefaults -> SettingsView Toggle | Local-only preference write, no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-41-07 | Information Disclosure | `countdownWings(for:)` | mitigate | D-06 enforced twice over: `CalendarCountdownActivity` has no title field to read (Plan 01), and Task 1's acceptance criteria confirms no title-rendering `Text` is added in this view — an untrusted calendar-event title (T-14-06, subscribed/shared calendars) can never reach the collapsed pill through this code path |
| T-41-08 | Denial of Service (minor, cosmetic) | `TimelineView(.periodic(from:by: 1))` ticking every second while the wing is mounted | accept | Bounded to at most the ~60 minutes the countdown is actually visible (the wing's own SwiftUI lifecycle IS the gate — mirrors `EqualizerBars`' `TimelineView(.animation(paused:))` precedent's "stops entirely when not needed" property); no separate `Timer`/`DispatchSourceTimer` is added by this view, and the per-second cost is a string-format + color compare, not measurable CPU load |
| T-41-SC | Tampering | npm/pip/cargo installs | not applicable | Zero external dependencies — SF Symbols, system `Color` literals, and SwiftUI `TimelineView` are all Apple-owned |
</threat_model>

<verification>
Build must succeed after each task. No automated unit test exists for SwiftUI view rendering in
this codebase (matches every prior wing view's precedent — `focusWings`/`osdWings` also have no
direct unit test, only their pure data dependencies are tested). The live per-second tick,
color-switch-at-60s, and icon/text sync are manual-only, verified in Plan 04's on-device UAT.
</verification>

<success_criteria>
- `countdownWings(for:)` exists, compiles, is wired into `presentationSwitch`, renders icon+mm:ss
  with synchronized per-tick urgency coloring, and never renders the event title.
- The Settings toggle exists, defaults ON, and has no permission popover.
</success_criteria>

<output>
Create `.planning/phases/41-calendar-countdown-hud/41-03-SUMMARY.md` when done
</output>
