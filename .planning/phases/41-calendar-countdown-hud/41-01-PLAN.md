---
phase: 41-calendar-countdown-hud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Calendar/CalendarGlance.swift
  - Islet/Calendar/CalendarService.swift
  - Islet/Notch/IslandResolver.swift
  - Islet/ActivitySettings.swift
  - IsletTests/CalendarGlanceTests.swift
  - IsletTests/IslandResolverTests.swift
autonomous: true
requirements: [HUD-08]

must_haves:
  truths:
    - "The pure resolver (resolve(...)) returns .calendarCountdown ahead of .nowPlayingWings whenever both a countdown and now-playing input are present, and never while isExpanded or any ActiveTransient is active (D-01)"
    - "nextUpcomingEvent(events:now:lookahead:) selects only NOT-YET-STARTED events (start > now), diverging correctly from nextRelevantEvent's in-progress-inclusive behavior (Pitfall 2)"
    - "The song-change toast gate (songChangeToastGate) requires zero code changes — it has no ambient-tier input and keeps firing unchanged (D-02)"
    - "The countdown's own Settings key defaults ON, matching Charging/Device's opt-out convention, not Focus/OSD's opt-in one (D-03)"
  artifacts:
    - path: "Islet/Notch/IslandResolver.swift"
      provides: "CalendarCountdownActivity struct, IslandPresentation.calendarCountdown case, resolve(...) calendarCountdown: parameter, ambient-branch priority check"
      contains: "struct CalendarCountdownActivity"
    - path: "Islet/Calendar/CalendarGlance.swift"
      provides: "nextUpcomingEvent(events:now:lookahead:) pure selection seam"
      contains: "func nextUpcomingEvent"
    - path: "Islet/Calendar/CalendarService.swift"
      provides: "fetchUpcomingRaw(completion:) raw-EventInput fetch, mirrors fetchUpcoming's 2-day predicate"
      contains: "func fetchUpcomingRaw"
    - path: "Islet/ActivitySettings.swift"
      provides: "calendarCountdownKey default-ON toggle key"
      contains: "calendarCountdownKey"
  key_links:
    - from: "IslandResolver.swift resolve(...)"
      to: "IslandPresentation.calendarCountdown(CalendarCountdownActivity)"
      via: "ambient branch, checked before nowPlayingLaunchGate"
      pattern: "if let countdown = calendarCountdown"
    - from: "CalendarGlance.swift nextUpcomingEvent"
      to: "EventInput.start > now filter"
      via: "pure Foundation-only selection"
      pattern: "\\$0\\.start > now"
---

<objective>
Establish the pure, framework-free contracts this phase's Calendar Countdown HUD (HUD-08) is
built on: the resolver's new ambient-tier priority case (D-01), the event-selection function that
correctly excludes already-started events (Pitfall 2), the raw-event EventKit fetch method the
monitor will call in Plan 02, and the Settings key the toggle (Plan 03) and controller (Plan 02)
both read.

Purpose: every downstream plan (02: monitor + controller wiring, 03: wing view + Settings toggle,
04: on-device UAT) depends on these types/functions already existing and already being correct —
this plan is the "define contracts first" wave so no downstream executor has to guess a signature.
Output: `CalendarCountdownActivity`, `IslandPresentation.calendarCountdown`, `resolve(...)`'s new
parameter, `nextUpcomingEvent(events:now:lookahead:)`, `CalendarService.fetchUpcomingRaw(completion:)`,
and `ActivitySettings.calendarCountdownKey` — all unit-tested, zero AppKit/SwiftUI/EventKit-glue
code yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-calendar-countdown-hud/41-CONTEXT.md
@.planning/phases/41-calendar-countdown-hud/41-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: nextUpcomingEvent pure selection seam</name>
  <files>Islet/Calendar/CalendarGlance.swift, IsletTests/CalendarGlanceTests.swift</files>
  <read_first>
    Islet/Calendar/CalendarGlance.swift (existing nextRelevantEvent(events:now:) at line 37 — the
    exact discipline to mirror: Foundation-only, `now` always an explicit parameter, total/never-
    crashes on empty input)
    IsletTests/CalendarGlanceTests.swift (existing test style for EventInput hand-construction and
    assertion shape, e.g. testEmptyEventsListReturnsNilWithoutCrashing)
  </read_first>
  <behavior>
    - Test: an event whose `start > now` and `start <= now + lookahead` is returned.
    - Test: an event that has ALREADY STARTED (`start <= now`, even if `end > now`) is EXCLUDED —
      this is the exact case `nextRelevantEvent` would include; proves the two functions diverge.
    - Test: an event exactly at the lookahead boundary (`start == now + lookahead`) is INCLUDED
      (inclusive `<=`).
    - Test: an event outside the lookahead window (either too far future or already past) is
      excluded, function returns nil.
    - Test: an empty `events` array returns nil without crashing.
    - Test: multiple qualifying events return the EARLIEST-starting one (sorted by start).
  </behavior>
  <action>
    Add `nextUpcomingEvent(events: [EventInput], now: Date, lookahead: TimeInterval = 3600) ->
    EventInput?` to Islet/Calendar/CalendarGlance.swift as a NEW sibling function placed after
    `nextRelevantEvent(events:now:)` and before `daysInMonth(for:calendar:)` — do NOT modify
    `nextRelevantEvent` itself (it has its own existing caller, `refreshCalendar()` ->
    `outfitState.calendar`, whose in-progress-inclusive behavior must stay unchanged). Filter
    condition is `$0.start > now && $0.start <= now.addingTimeInterval(lookahead)`, sorted by
    `$0.start < $1.start`, `.first`. Add a doc comment referencing Phase 41 / HUD-08 (D-04) and
    Pitfall 2 (the divergence from nextRelevantEvent), mirroring the existing function's comment
    style. Add the 6 test cases from `<behavior>` to IsletTests/CalendarGlanceTests.swift,
    following the existing hand-constructed-EventInput style (see
    testMultipleRelevantEventsTodayReturnsEarliestStarting for the "earliest wins" assertion
    shape, testEndedEventTodayIsSkippedInFavorOfUpcomingOne for the exclusion-style assertion
    shape).
  </action>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func nextUpcomingEvent" Islet/Calendar/CalendarGlance.swift` equals 1
    - `func nextRelevantEvent(events: [EventInput], now: Date) -> CalendarGlance?` in
      Islet/Calendar/CalendarGlance.swift is byte-for-byte unchanged (no modification to its body
      or signature)
    - `grep -c "func testNextUpcomingEvent" IsletTests/CalendarGlanceTests.swift` is at least 4
    - Manual Cmd-U run (per phase Sampling Rate) shows all new CalendarGlanceTests green
  </acceptance_criteria>
  <done>nextUpcomingEvent exists as a new, tested, Foundation-only sibling function; nextRelevantEvent is untouched; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: CalendarService raw-event fetch</name>
  <files>Islet/Calendar/CalendarService.swift</files>
  <read_first>
    Islet/Calendar/CalendarService.swift (existing `fetchUpcoming(completion:)` at line 43 — the
    EXACT predicate shape to mirror: `store.requestFullAccessToEvents()`, D-03 silent-degrade-on-
    denial, `store.calendars(for: .event)`, 2-day `predicateForEvents(withStart:end:calendars:)`
    window, `mapToEventInput` at line 93 — the already-factored WR-04 EKEvent->EventInput mapper
    to reuse verbatim, never re-hand-rolled)
  </read_first>
  <action>
    Add `fetchUpcomingRaw(completion: @escaping ([EventInput]) -> Void)` to the `CalendarService`
    protocol (alongside `fetchUpcoming`/`fetchMonth`) and to its sole conformer `EventKitService`.
    This resolves 41-RESEARCH.md's Open Question 1 as Option (b): a NEW method returning the RAW
    `[EventInput]` list (not a reduced `CalendarGlance`), mirroring `fetchUpcoming`'s exact 2-day
    predicate (`Date()` to `Date().addingTimeInterval(2 * 24 * 3600)`, ALL active calendars via
    `store.calendars(for: .event)`) rather than reusing `fetchMonth`'s calendar-month-boundary
    predicate — this avoids the A2 assumption-log edge case (a late-month event's 1hr lookahead
    crossing into next month being missed). Settle `[]` (never `nil`) on Calendar access denial —
    D-03 silent degrade, never retry/nag, matching `fetchMonth`'s empty-array-on-denial
    convention, not `fetchUpcoming`'s nil-on-denial one. Reuse the existing private
    `mapToEventInput(_:)` helper for the `EKEvent` -> `EventInput` conversion — do not duplicate
    its RGB-extraction/title-passthrough logic. `completion` is always delivered on the main
    thread (file-header contract, unchanged). Add a doc comment on the protocol method explaining
    this is for Phase 41's `CalendarCountdownMonitor` (Plan 02), which needs the raw list to find
    the next not-yet-started event even beyond the immediate 1hr countdown lookahead.
  </action>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func fetchUpcomingRaw" Islet/Calendar/CalendarService.swift` equals 2 (protocol declaration + EventKitService conformance)
    - `EventKitService.fetchUpcomingRaw` calls `mapToEventInput` (not a duplicated RGB-extraction block) — verify by source read
    - `EventKitService.fetchUpcomingRaw`'s denial branch settles `completion([])`, never `completion(nil)`
    - Build succeeds with the new protocol method satisfied by its sole conformer
  </acceptance_criteria>
  <done>CalendarService gains fetchUpcomingRaw(completion:), mirroring fetchUpcoming's predicate shape but returning raw EventInput; EventKitService conforms; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: IslandResolver ambient-pair extension + Settings key</name>
  <files>Islet/Notch/IslandResolver.swift, Islet/ActivitySettings.swift, IsletTests/IslandResolverTests.swift</files>
  <read_first>
    Islet/Notch/IslandResolver.swift (full file — `PendingDrop` struct at line 47, `IslandPresentation`
    enum at line 54, `resolve(...)` signature at line 109 and its ambient branch at lines 157-161,
    `songChangeToastGate` at line 195)
    Islet/ActivitySettings.swift (existing `deviceKey`/`nowPlayingKey` at lines 15-18 — the plain
    default-ON key shape to mirror, NOT `focusKey`'s permission-gated default-OFF shape)
    IsletTests/IslandResolverTests.swift (existing `testDeviceOutranksAmbientMedia` at line 29 and
    `testFocusFallsThroughWhenExpanded` at line 631 — the two test shapes to mirror)
  </read_first>
  <behavior>
    - Test: `resolve(...)` returns `.calendarCountdown` ahead of `.nowPlayingWings` when both a
      `calendarCountdown` and a `.playing` `nowPlaying` input are present, collapsed, no active
      transient (mirrors `testDeviceOutranksAmbientMedia`'s shape).
    - Test: `resolve(...)` never returns `.calendarCountdown` while `isExpanded == true` — falls
      through to whatever the expanded branch would resolve to as if `calendarCountdown` were nil
      (mirrors `testFocusFallsThroughWhenExpanded`'s shape, asserting `.homeEmpty`).
    - Test: a standing `.charging` `activeTransient` outranks a present `calendarCountdown` even
      collapsed (mirrors `testChargingOutranksDeviceAndMedia`'s shape).
  </behavior>
  <action>
    In Islet/Notch/IslandResolver.swift: add `struct CalendarCountdownActivity: Equatable { let
    eventStart: Date }` immediately after the `PendingDrop` struct and before `enum
    IslandPresentation` — Foundation-only, no `title` field at all (D-06: the event title is
    NEVER shown on the collapsed pill, so this pure layer's data path structurally cannot leak
    it). Add `case calendarCountdown(CalendarCountdownActivity)` to `IslandPresentation`
    immediately after `case nowPlayingWings(NowPlayingPresentation)`, with a doc comment "Phase 41
    / HUD-08: ambient tier, D-01 always wins over nowPlayingWings". Add `calendarCountdown:
    CalendarCountdownActivity? = nil` as a new trailing default-nil parameter to `resolve(...)`
    (after `pendingDrop: PendingDrop? = nil`) — additive, every existing call site compiles
    unchanged. In the ambient branch (currently `let ambient = nowPlayingLaunchGate(...); if
    ambient != .none { return .nowPlayingWings(ambient) }; return .idle`), insert `if let
    countdown = calendarCountdown { return .calendarCountdown(countdown) }` as the literal FIRST
    line of that branch, before the `nowPlayingLaunchGate` call — this is the ONLY place D-01's
    priority rule may be expressed (Pitfall 3: never a suppression flag in a monitor or the view
    layer). Confirm via a source read (no code change needed) that `songChangeToastGate(...)` at
    line 195 requires ZERO modification — it has no ambient-tier input at all, so D-02's "toast
    keeps firing unchanged" already holds. Confirm via a source read (no code change needed) that
    `showsSwitcherRow(for:)` requires ZERO modification — `.calendarCountdown` correctly falls
    into its existing `default: return false` branch (Countdown never shows the switcher row,
    matching every other collapsed-only ambient/transient case). In Islet/ActivitySettings.swift:
    add `static let calendarCountdownKey = "activity.calendarCountdown"` alongside
    `deviceKey`/`nowPlayingKey`, with a doc comment "Phase 41 / HUD-08 (D-03) — default ON,
    matches Charging/Device/Now-Playing's opt-out convention, not Focus/OSD's permission-gated
    opt-in one." Add the 3 test cases from `<behavior>` to IsletTests/IslandResolverTests.swift,
    placed in a new `// MARK: Phase 41 / HUD-08 — Calendar Countdown` section mirroring the
    existing Focus/OSD `// MARK:` section style (see line 619, line 670).
  </action>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "struct CalendarCountdownActivity" Islet/Notch/IslandResolver.swift` equals 1, and the struct has exactly one stored property (`eventStart: Date`) — no `title` field
    - `grep -c "case calendarCountdown" Islet/Notch/IslandResolver.swift` is at least 2 (enum case declaration + resolve()'s return statement)
    - `resolve(...)`'s ambient branch has the `if let countdown = calendarCountdown` check textually BEFORE the `nowPlayingLaunchGate` call
    - `songChangeToastGate` function body is byte-for-byte unchanged from before this task
    - `showsSwitcherRow(for:)` function body is byte-for-byte unchanged from before this task
    - `grep -c "calendarCountdownKey" Islet/ActivitySettings.swift` equals 1
    - `grep -c "func testCalendarCountdown\|func testChargingOutranksCalendarCountdown" IsletTests/IslandResolverTests.swift` equals 3
    - Manual Cmd-U run shows all 3 new IslandResolverTests green plus every pre-existing IslandResolverTests case still green (no regression from the new default-nil parameter)
  </acceptance_criteria>
  <done>CalendarCountdownActivity + IslandPresentation.calendarCountdown + resolve()'s ambient priority check exist and are tested; songChangeToastGate/showsSwitcherRow confirmed unchanged; calendarCountdownKey exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| EventKit -> CalendarGlance/IslandResolver | Calendar event data (including untrusted titles from subscribed/shared calendars, T-14-06) crosses from the OS into app code via `EventInput`/`CalendarCountdownActivity` — this phase's own data path (`CalendarCountdownActivity`) carries ONLY `eventStart: Date`, structurally excluding the title field |
| UserDefaults -> ActivitySettings | Local-only user preference (calendarCountdownKey), no injection surface, no network/remote-write path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-41-01 | Information Disclosure | `CalendarCountdownActivity` (IslandResolver.swift) | mitigate | D-06 enforced structurally: the struct has exactly one field (`eventStart: Date`), no `title`/`String` field exists to leak an untrusted calendar-event title into this pure layer at all — verified by Task 3's acceptance criteria (struct has exactly one stored property) |
| T-41-02 | Tampering | `nextRelevantEvent` (existing, CalendarGlance.swift) | accept | Task 1 adds a sibling function without modifying `nextRelevantEvent` — its own existing T-14-06 bounding (event title passed through as plain String only, rendered with `.lineLimit`/`.truncationMode` at the view layer) is unaffected and out of this plan's touch surface |
| T-41-03 | Elevation of Privilege | `resolve(...)` ambient branch | mitigate | The D-01 priority check is added ONLY inside `resolve(...)` itself (Pitfall 3) — never as a flag elsewhere; Task 3's 3 new tests assert the resolver's own precedence (transient > expanded > countdown > now-playing), closing the "feels too simple to need the resolver" bypass class before any monitor/view code exists to bypass it |
| T-41-SC | Tampering | npm/pip/cargo installs | not applicable | Zero external dependencies added — RESEARCH.md's Package Legitimacy Audit confirms this phase uses only Apple system frameworks already linked (EventKit, Foundation, SwiftUI) |
</threat_model>

<verification>
Build must succeed after each task (`xcodebuild ... -configuration Debug build`). After Task 3,
run a manual Cmd-U pass in Xcode covering all of `IsletTests/IslandResolverTests.swift` and
`IsletTests/CalendarGlanceTests.swift` — every pre-existing test must remain green (no regression
from the new default-nil `resolve(...)` parameter) alongside the new Phase 41 test cases.
</verification>

<success_criteria>
- `CalendarCountdownActivity`, `IslandPresentation.calendarCountdown`, and `resolve(...)`'s new
  `calendarCountdown:` parameter exist, compile, and are covered by 3 passing unit tests proving
  D-01's exact priority ordering.
- `nextUpcomingEvent(events:now:lookahead:)` exists, is covered by 6 passing unit tests, and
  `nextRelevantEvent` is provably unmodified.
- `CalendarService.fetchUpcomingRaw(completion:)` exists on the protocol and its sole conformer,
  mirroring `fetchUpcoming`'s predicate shape but returning raw events.
- `ActivitySettings.calendarCountdownKey` exists, default-ON shape (no code change to
  `activityEnabled(_:)` needed — it already defaults true for any key other than `focusKey`).
</success_criteria>

<output>
Create `.planning/phases/41-calendar-countdown-hud/41-01-SUMMARY.md` when done
</output>
