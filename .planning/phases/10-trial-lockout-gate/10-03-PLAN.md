---
phase: 10-trial-lockout-gate
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - Islet/AppDelegate.swift
  - Islet/SettingsView.swift
autonomous: true
requirements: [TRIAL-01, TRIAL-02, LIC-03]

must_haves:
  truths:
    - "On first launch, Settings auto-opens exactly once showing a trial-started notice; on every subsequent launch it does not reopen"
    - "Clicking the menu-bar status item while locked (isLicensed==false) opens Settings directly, skipping the normal Settings…/Quit dropdown"
    - "Clicking the menu-bar status item while licensed shows the normal Settings…/Quit dropdown, unchanged from today"
    - "DEBUG-only stub controls (force-expired / force-licensed / reset-trial) exist in Debug builds and are completely absent from Release builds"
  artifacts:
    - path: "Islet/AppDelegate.swift"
      provides: "first-launch auto-open, D-05 click routing, DEBUG-only stub-flip status item"
      contains: "applyMenuBarClickRouting"
    - path: "Islet/SettingsView.swift"
      provides: "trial-started notice line"
      contains: "Your 3-day trial started"
  key_links:
    - from: "Islet/AppDelegate.swift"
      to: "Islet/Licensing/TrialManager.swift"
      via: "recordFirstLaunchIfNeeded() call before controller.start()"
      pattern: "TrialManager\\.shared\\.recordFirstLaunchIfNeeded"
    - from: "Islet/AppDelegate.swift"
      to: "Islet/Licensing/LicenseState.swift"
      via: "applyMenuBarClickRouting reading LicenseState.shared.isEntitled"
      pattern: "LicenseState\\.shared\\.isEntitled"
    - from: "Islet/SettingsView.swift"
      to: "Islet/Licensing/TrialManager.swift"
      via: "trialStartDate() read for the notice text"
      pattern: "TrialManager\\.shared\\.trialStartDate"
---

<objective>
Wire the first-launch trial notice (TRIAL-02), the locked-state menu-bar click routing (D-05, part of LIC-03's UX), and the DEBUG-only testing seam (D-08/D-09) into `AppDelegate`, and add the trial-started line to `SettingsView`.

Purpose: TRIAL-02 requires an explicit, one-time, never-silent trial-start notice; LIC-03's locked-state behavior additionally specifies that a click on the menu-bar icon while locked has nothing useful to do except jump straight to Settings. Both reuse the app's EXISTING Settings window (`SettingsView.swift` via `IsletApp.swift`'s `Window(id: "settings")`) — no new window/alert/notification type.
Output: `AppDelegate.swift` gains first-launch detection + auto-open, D-05 click routing, and a DEBUG-only secondary status item exposing the 3 stub-flip actions; `SettingsView.swift` gains one notice line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-trial-lockout-gate/10-CONTEXT.md
@.planning/phases/10-trial-lockout-gate/10-RESEARCH.md
@.planning/phases/10-trial-lockout-gate/10-PATTERNS.md
@.planning/phases/10-trial-lockout-gate/10-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01 (10-01), already built — consume directly. -->
```swift
// Islet/Licensing/TrialManager.swift
final class TrialManager {
    static let shared: TrialManager
    static let trialLength: TimeInterval
    func trialStartDate() -> Date?
    @discardableResult func recordFirstLaunchIfNeeded(now: Date = Date()) -> Bool
    #if DEBUG
    func debugResetTrial()
    #endif
}

// Islet/Licensing/LicenseState.swift
final class LicenseState {
    static let shared: LicenseState
    #if DEBUG
    static let debugOverrideKey: String
    enum DebugOverride: String { case forceExpired, forceLicensed }
    #endif
    var isEntitled: Bool { get }
}
```

From `Islet/IsletApp.swift` (existing, unmodified — read for context, not edited by this plan):
```swift
extension Notification.Name { static let openIsletSettings: Notification.Name }
// Window(id: "settings") { SettingsView() } — the window this plan's AppDelegate changes open/hide.
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: First-launch auto-open, D-05 click routing, DEBUG stub menu</name>
  <files>Islet/AppDelegate.swift</files>
  <read_first>
    Islet/AppDelegate.swift (current full file, 93 lines — every change below targets this exact file)
    Islet/IsletApp.swift (the `openIsletSettings` notification + `Window(id: "settings")` scene this file's `openSettings()` already targets)
    Islet/Licensing/TrialManager.swift and Islet/Licensing/LicenseState.swift (from Plan 01 — exact final signatures)
    .planning/phases/10-trial-lockout-gate/10-RESEARCH.md (Architecture Patterns Pattern 4 — NSStatusItem.menu vs button.action; Common Pitfalls Pitfall 2 — the hideSettingsWindowOnLaunch race; Common Pitfalls Pitfall 3 — the mutual-exclusivity gotcha; Common Pitfalls Pitfall 4 — DEBUG gating both sides)
  </read_first>
  <action>
    Make the following concrete changes to `Islet/AppDelegate.swift`:
    (a) Promote the locally-scoped `let menu = NSMenu()` (current line 26) to a stored property `private var menu: NSMenu!`, set inside `applicationDidFinishLaunching` exactly where it is built today.
    (b) At the very top of `applicationDidFinishLaunching`, before building the status item, call `let isFirstLaunch = TrialManager.shared.recordFirstLaunchIfNeeded()` (TRIAL-01/D-10) — this MUST run before `controller.start()` (current line 40) so `LicenseState.shared` already has a valid trial start date the first time `updateVisibility()` runs inside `start()`.
    (c) Replace the existing unconditional `DispatchQueue.main.async { [weak self] in self?.hideSettingsWindowOnLaunch() }` block (current lines 48-50) with: if `isFirstLaunch` is true, set `didHideSettingsAtLaunch = true` (permanently disarms the retry loop with zero flicker risk, per Pitfall 2's recommended fix — "skip the hide entirely on first launch") and call `DispatchQueue.main.async { [weak self] in self?.openSettings() }` (D-02/D-03 — reuses the existing `openSettings()` method verbatim); else (not first launch) keep the existing `hideSettingsWindowOnLaunch()` call exactly as today.
    (d) Add `private func applyMenuBarClickRouting(isLicensed: Bool)` implementing D-05/Pattern 4: if `isLicensed`, set `statusItem.menu = menu` and `statusItem.button?.action = nil`; else set `statusItem.menu = nil`, `statusItem.button?.target = self`, `statusItem.button?.action = #selector(openSettings)` (AppKit shows the assigned `.menu` automatically on click and silently ignores `button.action` while `.menu` is non-nil — Pitfall 3 — so these two states must be mutually exclusive, never both set). Call `applyMenuBarClickRouting(isLicensed: LicenseState.shared.isEntitled)` once, immediately after `statusItem.menu = menu` is first assigned in the existing setup code.
    (e) Add `private var licenseObserver: NSObjectProtocol?` and register it in `applicationDidFinishLaunching`: `NotificationCenter.default.addObserver(forName: UserDefaults.didChangeNotification, object: nil, queue: .main) { [weak self] _ in self?.applyMenuBarClickRouting(isLicensed: LicenseState.shared.isEntitled) }` — mirrors `NotchWindowController`'s existing `defaultsObserver` pattern, so a DEBUG stub-flip (which writes UserDefaults) re-applies click routing live without any new plumbing.
    (f) Add a `#if DEBUG` block (D-08/D-09, Pitfall 4) containing `private var debugStatusItem: NSStatusItem!` — a SEPARATE `NSStatusBar.system.statusItem` distinct from the primary `statusItem`, so debug controls stay reachable even while the primary item is in the D-05 locked-click state (if debug items lived inside `menu`, nil-ing `statusItem.menu` while locked would make them unreachable exactly when a developer most needs to flip the stub back to licensed — the second status item sidesteps this conflict entirely and is itself absent from Release builds). Add a private `setupDebugMenu()` method (called from the end of `applicationDidFinishLaunching`, itself wrapped in `#if DEBUG`) that builds `debugStatusItem` with title/icon "🐞" and a menu of 3 items — "Debug: Force Expired", "Debug: Force Licensed", "Debug: Reset Trial" — targeting `self` and wired to 3 new `@objc private func` actions: `debugForceExpired()` writes `LicenseState.DebugOverride.forceExpired.rawValue` to `UserDefaults.standard` under key `LicenseState.debugOverrideKey`; `debugForceLicensed()` writes `.forceLicensed.rawValue` to the same key; `debugResetTrial()` calls `UserDefaults.standard.removeObject(forKey: LicenseState.debugOverrideKey)` then `TrialManager.shared.debugResetTrial()`. All of (f) — the property, the setup method, and the 3 actions — must be inside `#if DEBUG` ... `#endif`.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -configuration Release && xcodebuild build -scheme Islet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TrialManager.shared.recordFirstLaunchIfNeeded()" Islet/AppDelegate.swift` finds exactly 1 match, positioned before the `controller.start()` line
    - `grep -n "func applyMenuBarClickRouting" Islet/AppDelegate.swift` finds the definition, and `grep -c "applyMenuBarClickRouting(isLicensed:" Islet/AppDelegate.swift` is at least 2 (initial call + observer closure call)
    - `grep -c "#if DEBUG" Islet/AppDelegate.swift` is at least 1, bracketing `debugStatusItem`, `setupDebugMenu()`, and all 3 debug `@objc` actions
    - `xcodebuild build -scheme Islet -configuration Release` succeeds (proves the DEBUG-gated block compiles out cleanly with no leftover references)
    - `xcodebuild build -scheme Islet` (Debug) succeeds
  </acceptance_criteria>
  <done>AppDelegate calls recordFirstLaunchIfNeeded before controller.start(); first-launch skips hideSettingsWindowOnLaunch and opens Settings instead; D-05 click routing toggles statusItem.menu/button.action based on the live isLicensed state and stays in sync via a UserDefaults observer; a DEBUG-only secondary status item exposes the 3 stub-flip actions and is fully absent from Release builds.</done>
</task>

<task type="auto">
  <name>Task 2: Trial-started notice line in SettingsView</name>
  <files>Islet/SettingsView.swift</files>
  <read_first>
    Islet/SettingsView.swift (current full file, 88 lines)
    Islet/Licensing/TrialManager.swift (from Plan 01 — `trialStartDate()` / `trialLength` exact signatures)
  </read_first>
  <action>
    In `Islet/SettingsView.swift`, add a plain `Text` row inside the existing `Form`, placed BEFORE the "Launch Islet at login" `Toggle` (current line 19) and NOT inside a `Section` (per D-02's "short line" framing — mirrors the file's existing single-row `LabeledContent("Version")` precedent rather than a new Section). Render it conditionally: `if let start = TrialManager.shared.trialStartDate() { let expiry = start.addingTimeInterval(TrialManager.trialLength); Text("Your 3-day trial started — ends \(expiry.formatted(date: .abbreviated, time: .omitted)).").font(.footnote).foregroundStyle(.secondary) }`. This line stays visible on every subsequent Settings open during an active trial — the "one-time" requirement of TRIAL-02 is satisfied by the AppDelegate auto-open EVENT (Task 1), not by hiding this line after the first render. Do NOT add any expired-state message, Buy Now button, or license-key entry field here — that content is explicitly Phase 11 (TRIAL-03) scope per CONTEXT.md D-07.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Your 3-day trial started" Islet/SettingsView.swift` finds exactly 1 match
    - `grep -n "TrialManager.shared.trialStartDate" Islet/SettingsView.swift` finds exactly 1 match
    - `grep -n "Buy Now\|license" Islet/SettingsView.swift` finds NO matches (confirms Phase 11 content is not pulled forward)
    - `xcodebuild build -scheme Islet` succeeds
  </acceptance_criteria>
  <done>SettingsView shows the trial-started line whenever a trial start date exists; renders nothing extra when absent (no force-unwrap, no crash); no Phase 11 content added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DEBUG build ↔ Release build | The DEBUG-only secondary status item and its 3 actions must never exist in a shipped Release binary |
| Menu-bar click ↔ AppKit event routing | `NSStatusItem.menu` and `button.action` are mutually exclusive at the AppKit level — a naive implementation of D-05 silently no-ops instead of erroring, so this is a correctness risk, not just a security one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Elevation of Privilege | `AppDelegate` DEBUG-only status item + actions (writer side) | mitigate | `debugStatusItem`, `setupDebugMenu()`, and all 3 debug `@objc` actions are compiled only under `#if DEBUG`; verified via a Release-configuration build in Task 1 (pairs with the reader-side gate already verified in Plan 01/`LicenseState.status`) |
| T-10-05 | (UX-adjacent, not classic STRIDE) | Locked-state menu-bar click routing (D-05) | mitigate | `applyMenuBarClickRouting` deterministically sets ONLY one of `statusItem.menu` / `button.action` at a time (never both), re-applied via a UserDefaults observer whenever license state changes — no dual-firing / stale-menu risk (Pitfall 3) |
</threat_model>

<verification>
`xcodebuild build -scheme Islet -configuration Release` must succeed with zero references to any DEBUG-only symbol; `xcodebuild build -scheme Islet` (Debug) must succeed with the full DEBUG surface present.
</verification>

<success_criteria>
- Fresh launch (no prior Keychain/UserDefaults trial data): Settings auto-opens exactly once with the trial-started line; a second launch does not reopen it.
- Clicking the primary status item while `LicenseState.shared.isEntitled == false` opens Settings directly; while `true`, shows the normal Settings…/Quit dropdown.
- The DEBUG-only secondary status item with 3 stub-flip actions exists in Debug builds and is fully absent from Release builds (confirmed via a Release build).
</success_criteria>

<output>
Create `.planning/phases/10-trial-lockout-gate/10-03-SUMMARY.md` when done
</output>
