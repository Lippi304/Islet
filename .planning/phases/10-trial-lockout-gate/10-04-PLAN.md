---
phase: 10-trial-lockout-gate
plan: 04
type: execute
wave: 3
depends_on: ["10-02", "10-03"]
files_modified: []
autonomous: false
requirements: [TRIAL-01, TRIAL-02, LIC-03]

must_haves:
  truths:
    - "A human has confirmed Keychain trial persistence survives defaults delete + full app reinstall"
    - "A human has confirmed the DEBUG stub controls are absent from a Release-configuration build"
    - "A human has confirmed the first-launch notice fires exactly once and the lockout/unlock transition is non-abrupt"
  artifacts:
    - path: "Islet/Licensing/TrialManager.swift"
      provides: "on-device-verified Keychain persistence (built in Plan 01)"
      contains: "kSecClassGenericPassword"
    - path: "Islet/AppDelegate.swift"
      provides: "on-device-verified DEBUG gating + click routing (built in Plan 03)"
      contains: "applyMenuBarClickRouting"
  key_links:
    - from: "Islet/AppDelegate.swift"
      to: "Islet/Licensing/LicenseState.swift"
      via: "manual on-device confirmation of live isEntitled-driven lockout/unlock"
      pattern: "LicenseState\\.shared"
---

<objective>
Close the 3 manual-only verification gaps flagged in `.planning/phases/10-trial-lockout-gate/10-VALIDATION.md` — behaviors that inherently cannot be asserted by an automated unit test (a real delete/reinstall cycle, visual first-launch confirmation, and interaction-state timing) — plus confirm the DEBUG-stub-inertness security mitigation on a real Release build.

Purpose: Phase 10's ROADMAP success criteria are only genuinely met once these on-device behaviors are confirmed; automated tests in Plans 01-03 cover everything that CAN be automated, but TRIAL-01's tamper-resistance claim, TRIAL-02's first-launch timing, and LIC-03's non-abrupt-unlock claim are observable only on real hardware.
Output: Human-verified confirmation (or a documented failure) of all 3 manual-only rows from `10-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-trial-lockout-gate/10-CONTEXT.md
@.planning/phases/10-trial-lockout-gate/10-VALIDATION.md
@.planning/phases/10-trial-lockout-gate/10-01-SUMMARY.md
@.planning/phases/10-trial-lockout-gate/10-02-SUMMARY.md
@.planning/phases/10-trial-lockout-gate/10-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: On-device verification — Keychain persistence + DEBUG-inertness in Release (per 10-VALIDATION.md)</name>
  <what-built>
    Keychain-backed trial-start persistence (Plan 01: `TrialManager`/`KeychainTrialStore`) that must survive `defaults delete` and a full app reinstall; a DEBUG-only stub-override surface (Plan 01's `LicenseState.status` reader + Plan 03's `AppDelegate` writer) that must be completely absent from a Release-configuration build (Pitfall 4).
  </what-built>
  <how-to-verify>
    1. Build and run the Debug scheme (`xcodebuild build -scheme Islet`, then launch the built `Islet.app`). Confirm Settings auto-opens showing the "Your 3-day trial started — ends [date]" line. Note the exact date shown.
    2. Run `defaults delete com.lippi304.islet` in Terminal, then relaunch Islet. Confirm the trial-started date shown in Settings is UNCHANGED from step 1 (the UserDefaults mirror was just wiped; the Keychain copy alone must reconstitute the same original date — not a new one).
    3. Quit Islet, delete `Islet.app` from wherever it was built/run, rebuild via Xcode, and reinstall/relaunch. Confirm the trial-started date shown is STILL the original date from step 1 (proves reinstall does not reset the Keychain-backed clock).
    4. Build the Release configuration: `xcodebuild build -scheme Islet -configuration Release`. Confirm it succeeds. Then run `nm <path-to-built-Release-Islet-binary> | grep -i debugForce` (or equivalent `strings`/`nm` check) and confirm it returns NO output — proving the DEBUG-only stub-override writer/reader do not exist in the Release binary.
  </how-to-verify>
  <resume-signal>Type "approved" once all 4 steps behave as described, or describe which step failed and what you observed instead.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device verification — first-launch notice, D-05 click routing, non-abrupt lockout transition (per 10-VALIDATION.md)</name>
  <what-built>
    First-launch-only Settings auto-open (Plan 03, TRIAL-02); locked-state menu-bar click routing (Plan 03, D-05); the hard island lockout wired into the single visibility arbiter (Plan 02, LIC-03); DEBUG stub controls that live-update the lockout state at the next natural UI transition, never abruptly mid-interaction (D-13).
  </what-built>
  <how-to-verify>
    1. From a fresh install (or after Task 1's "Debug: Reset Trial" from a prior run), confirm Settings opens automatically on launch showing the trial-started notice; quit and relaunch Islet, and confirm Settings does NOT reopen automatically the second time.
    2. Click "Debug: Force Expired" on the DEBUG-only secondary status item. Confirm the island (pill) disappears entirely within a moment — no pill, no hover response, no expansion possible.
    3. Click the PRIMARY menu-bar status item while in this forced-expired state. Confirm it jumps straight to the Settings window — no Settings…/Quit Islet dropdown appears at all.
    4. Click "Debug: Force Licensed" and confirm the island reappears and behaves exactly as it did before Phase 10 (hover, click-to-expand, all existing activities). Hover to expand the island, and WHILE it is expanded, click "Debug: Force Expired" from the debug item. Confirm the island does NOT abruptly vanish out from under the pointer mid-interaction — it should collapse/hide at the next natural transition (e.g. the pointer leaving the zone, or the next screen/space/app-activate-triggered `updateVisibility()` call), not instantaneously mid-hover.
    5. Click "Debug: Reset Trial". Confirm the island reappears and the Settings trial-started line resets to a fresh "ends [date+3 days from now]".
  </how-to-verify>
  <resume-signal>Type "approved" once all 5 steps behave as described, or describe which step failed and what you observed instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Debug binary ↔ Release binary | Final confirmation point that the compile-time DEBUG/Release boundary from Plans 01 and 03 actually holds in a real built artifact, not just in source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `TrialManager` Keychain persistence | mitigate (verification) | On-device `defaults delete` + reinstall cycle confirms the mitigation built in Plan 01 actually holds outside of unit-test fakes |
| T-10-03 | Elevation of Privilege | DEBUG stub override (both writer and reader) | mitigate (verification) | `nm`/`strings` check against the real Release binary confirms no DEBUG-only symbol survives into the shipped artifact |
| T-10-05 | (UX-adjacent) | Lockout/unlock transition timing | mitigate (verification) | On-device mid-interaction stub flip confirms the transition defers to the next natural UI moment, never an abrupt yank (D-13) |
</threat_model>

<verification>
This plan has no automated verification of its own — it exists specifically to close the manual-only gaps flagged in `10-VALIDATION.md`. All prior automated suites (`xcodebuild test -scheme Islet`) must already be green from Plans 01-03 before this plan begins.
</verification>

<success_criteria>
- Both checkpoint tasks resolved with "approved" (or documented and fixed if a step failed).
- `10-VALIDATION.md`'s 3 manual-only rows are confirmed satisfied on real hardware.
- Phase 10's 5 ROADMAP success criteria are all demonstrably true end-to-end.
</success_criteria>

<output>
Create `.planning/phases/10-trial-lockout-gate/10-04-SUMMARY.md` when done
</output>
