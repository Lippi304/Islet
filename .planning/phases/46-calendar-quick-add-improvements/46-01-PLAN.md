---
phase: 46-calendar-quick-add-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Calendar/CalendarGlance.swift
  - IsletTests/CalendarGlanceTests.swift
  - Islet/Notch/NotchPillView.swift
autonomous: true
requirements: [CALVIEW-05]

must_haves:
  truths:
    - "defaultQuickAddTime(selectedDay:now:) returns the next full hour (minutes/seconds zeroed) when selectedDay is today, and 00:00 of selectedDay when it is not today — total function, Foundation-only, no inline Date()."
    - "QuickAddPopover's popover shows a real date+time picker: two labeled compact DatePickers (Starts/Ends) for Event, one labeled compact DatePicker (Due) for Reminder — no picker for Date itself (D-01), each time field using SwiftUI's .datePickerStyle(.compact) (D-02), and for Events Start/End are two separate compact DatePickers each on its own labeled row rather than a single side-by-side row (D-03)."
    - "Changing Start shifts End to preserve a 1-hour duration until End is edited directly, after which End stops auto-following Start for the rest of that popover session (D-04) — this must survive MULTIPLE sequential Start changes with no manual End edit in between, not just the first one, via an isProgrammaticEndUpdate suppression flag that stops endRow's onChange from misreading startRow's own auto-follow write as a manual edit."
    - "onSubmit carries the picked Date(s) out of QuickAddPopover — (QuickAddKind, String, Date, Date?) — so NotchWindowController (Plan 46-02) can pass real dates to CalendarService instead of today's hardcoded day/day+3600."
  artifacts:
    - path: "Islet/Calendar/CalendarGlance.swift"
      provides: "defaultQuickAddTime(selectedDay:now:) pure helper"
      contains: "func defaultQuickAddTime(selectedDay: Date, now: Date) -> Date"
    - path: "IsletTests/CalendarGlanceTests.swift"
      provides: "Unit coverage for defaultQuickAddTime's today/not-today/hour-rollover branches"
      contains: "defaultQuickAddTime"
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "QuickAddPopover date+time picker rows + widened onSubmit/onQuickAdd signatures"
      contains: "endManuallyEdited"
  key_links:
    - from: "Islet/Notch/NotchPillView.swift QuickAddPopover"
      to: "Islet/Calendar/CalendarGlance.swift defaultQuickAddTime"
      via: "isShowing onChange seeding startTime/endTime"
      pattern: "defaultQuickAddTime\\(selectedDay:"
    - from: "Islet/Notch/NotchPillView.swift QuickAddPopover.onSubmit"
      to: "Islet/Notch/NotchPillView.swift NotchPillView.onQuickAdd"
      via: "widened 4-argument closure signature, kept in lockstep"
      pattern: "\\(QuickAddKind, String, Date, Date\\?\\) -> Void"
    - from: "Islet/Notch/NotchPillView.swift QuickAddPopover.startRow onChange(of: startTime)"
      to: "Islet/Notch/NotchPillView.swift QuickAddPopover.endRow onChange(of: endTime)"
      via: "isProgrammaticEndUpdate suppression flag distinguishing auto-follow writes from user edits"
      pattern: "isProgrammaticEndUpdate"
---

<objective>
Add the real date+time picker to `QuickAddPopover` (CALVIEW-05, D-01 through D-05, D-07) and the
pure default-time helper it seeds from. This plan widens the popover's `onSubmit` contract to carry
real `Date` values but does NOT wire those values into `NotchWindowController`/`CalendarService`
yet — that lockstep-signature wiring, plus the button-placement (CALVIEW-06) and row-padding/island
-growth (CALVIEW-07) changes, is Plan 46-02 (same file, sequenced after this plan by design — see
`files_modified` overlap).

Purpose: `QuickAddPopover` currently has no date/time UI at all — it hardcodes the Event's start to
the selected day and end to start+1hr, and the Reminder's due date to the selected day
(`Islet/Notch/NotchWindowController.swift:1651-1661`). This plan gives the user a real, native
`.compact` time picker per the locked decisions in `46-CONTEXT.md`.

Output: `defaultQuickAddTime(selectedDay:now:)` (pure, tested), `QuickAddPopover` extended with
Starts/Ends/Due rows and auto-follow logic, `onSubmit`/`onQuickAdd` widened to
`(QuickAddKind, String, Date, Date?) -> Void`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-calendar-quick-add-improvements/46-CONTEXT.md
@.planning/phases/46-calendar-quick-add-improvements/46-UI-SPEC.md
@.planning/phases/46-calendar-quick-add-improvements/46-PATTERNS.md
</context>

<interfaces>
<!-- Exact current state, confirmed by direct code read (2026-07-19). Line numbers are as of this
read — they will shift slightly once edits land; use struct/function names as the stable anchor. -->

`Islet/Notch/NotchPillView.swift:219` — the property Plan 46-02's controller wiring depends on:
```swift
var onQuickAdd: (QuickAddKind, String) -> Void = { _, _ in }
```

`Islet/Notch/NotchPillView.swift:3125-3193` — the full current `QuickAddPopover` struct this plan
extends in place (see `46-PATTERNS.md` for the complete current source):
- `@State private var isShowing`, `kind`, `title`
- `let onSubmit: (QuickAddKind, String) -> Void`
- `body`: trigger `Button` + `.popover(isPresented: $isShowing) { quickAddContent }`
- `quickAddContent`: `VStack(alignment: .leading, spacing: 8)` containing the segmented type
  `Picker`, a `TextField`, and the submit `Button` (which calls `onSubmit(kind, trimmedTitle)`,
  clears `title`, sets `isShowing = false`); `.padding(12).frame(width: 220)`

`Islet/Notch/NotchPillView.swift:1165` — the one call site this plan must update to pass the new
`selectedDay` parameter (the D-06 `HStack` child-order flip itself is Plan 46-02's job, NOT this
plan's — only the constructor argument changes here):
```swift
QuickAddPopover(onSubmit: onQuickAdd)
```

`Islet/Calendar/CalendarGlance.swift:37,67` — the two existing pure functions whose header
discipline (Foundation-only, `now:` always an explicit parameter, never `Date()` inline, total
function) `defaultQuickAddTime` must match exactly:
```swift
func nextRelevantEvent(events: [EventInput], now: Date) -> CalendarGlance?
func nextUpcomingEvent(events: [EventInput], now: Date, lookahead: TimeInterval = 3600) -> EventInput?
```

`Islet/Calendar/CalendarViewState.swift:20-23` — `QuickAddKind` (`.event`/`.reminder`), already in
the same module, no new import needed.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add defaultQuickAddTime(selectedDay:now:) pure helper</name>
  <files>Islet/Calendar/CalendarGlance.swift, IsletTests/CalendarGlanceTests.swift</files>
  <read_first>
    Islet/Calendar/CalendarGlance.swift (whole file — `nextRelevantEvent`/`nextUpcomingEvent`'s exact "Foundation-only, `now:` explicit parameter, total function" discipline this new helper must replicate)
    IsletTests/CalendarGlanceTests.swift lines 1-40 (existing Given/When/Then comment style + `XCTAssertEqual` shape to match)
  </read_first>
  <behavior>
    - `defaultQuickAddTime(selectedDay: <today at 14:23:00>, now: <today at 14:23:00>)` returns today at 15:00:00 (next full hour, minutes/seconds zeroed) — `selectedDay` is today.
    - `defaultQuickAddTime(selectedDay: <3 days from now, any time>, now: <today, any time>)` returns `Calendar.current.startOfDay(for: selectedDay)` (00:00:00 on that day) — `selectedDay` is not today.
    - `defaultQuickAddTime(selectedDay: <today at 23:50:00>, now: <today at 23:50:00>)` returns 00:00:00 on the FOLLOWING day (hour-rollover edge case: adding 1 hour to 23:50 crosses midnight, then minutes/seconds zero to that new hour) — confirms no crash/wraparound bug at the day boundary.
    - Empty/degenerate input cannot occur (both parameters are non-optional `Date`) — no nil-handling test needed, matches this file's existing "total function via non-optional inputs" pattern for `daysInMonth`/`events(on:)`.
  </behavior>
  <action>
    In `Islet/Calendar/CalendarGlance.swift`, add a new function `defaultQuickAddTime(selectedDay: Date, now: Date) -> Date`, placed near `nextUpcomingEvent` (after it, before `daysInMonth`), with a doc comment naming CALVIEW-05 and D-... references from `46-CONTEXT.md` the same way this file's existing functions cite their phase/decision IDs. Implementation: use `Calendar.current`; if `!calendar.isDateInToday(selectedDay)`, return `calendar.startOfDay(for: selectedDay)`. Otherwise compute `nextHour = calendar.date(byAdding: .hour, value: 1, to: now) ?? now`, extract `.year, .month, .day, .hour` components from `nextHour` via `calendar.dateComponents(_:from:)`, and return `calendar.date(from: components) ?? nextHour` (this zeroes minutes/seconds and correctly rolls the day/month/year forward when `nextHour` crosses midnight, since the components are read from `nextHour` itself, not from `now`). `now`/`selectedDay` stay explicit parameters — no `Date()`/`Date.now` anywhere in the function body.

    In `IsletTests/CalendarGlanceTests.swift`, add 3 new test methods to the existing
    `CalendarGlanceTests` class covering exactly the 3 `<behavior>` cases above. Construct exact
    `Date`s via `Calendar.current.date(from: DateComponents(year:month:day:hour:minute:second:))`
    (not relative `.addingTimeInterval` offsets from `Date()`, since these tests need a real
    "is this today" comparison against a fixed reference `now` you also construct explicitly) —
    follow this file's existing `// Given/When/Then` comment convention above each test body.
  </action>
  <acceptance_criteria>
    - `grep -c "func defaultQuickAddTime(selectedDay: Date, now: Date) -> Date" Islet/Calendar/CalendarGlance.swift` returns 1
    - `grep -c "Date()" Islet/Calendar/CalendarGlance.swift` returns 0 (no inline `Date()` anywhere in this pure-seam file, including the new function)
    - `grep -c "func test.*[Dd]efaultQuickAddTime\|func test.*QuickAddTime" IsletTests/CalendarGlanceTests.swift` returns 3 or more
    - `xcodebuild -scheme Islet -destination 'platform=macOS' build 2>&1 | tail -20` ends with `** BUILD SUCCEEDED **`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -scheme Islet -destination 'platform=macOS' build 2>&1 | tail -20</automated>
  </verify>
  <done>defaultQuickAddTime(selectedDay:now:) exists as a pure, Foundation-only, total function with no inline Date(); 3 new tests cover today/not-today/hour-rollover; Debug build is green; actual green-run of the new tests confirmed via manual Cmd-U per this project's xcodebuild-test-headless-hang convention, not this task's automated gate.</done>
</task>

<task type="auto">
  <name>Task 2: QuickAddPopover date+time picker rows + widened onSubmit/onQuickAdd signature</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift lines 215-225 (`onQuickAdd` property declaration to widen)
    Islet/Notch/NotchPillView.swift lines 1158-1179 (`dayListColumn` — the one call site to pass `selectedDay:` into, per the `<interfaces>` block above; the D-06 button-flip itself is out of scope for this task)
    Islet/Notch/NotchPillView.swift lines 3120-3193 (`QuickAddPopover` — the full struct to extend, per the `<interfaces>` block above)
    Islet/Notch/NotchPillView.swift lines 3200-3245 (`OnboardingDoneStep`'s Launch-at-Login `@State`/`.onChange` reset pattern — the closest same-file idiom for resetting local `@State` on a lifecycle transition, per `46-PATTERNS.md`)
    Islet/Calendar/CalendarGlance.swift (defaultQuickAddTime, added in Task 1 — the seed function Task 2 calls)
    .planning/phases/46-calendar-quick-add-improvements/46-UI-SPEC.md (Layout Contract "Date+time picker" section — exact copy/labels/sizes)
  </read_first>
  <action>
    In `QuickAddPopover` (`Islet/Notch/NotchPillView.swift`):

    Widen `let onSubmit: (QuickAddKind, String) -> Void` to `let onSubmit: (QuickAddKind, String, Date, Date?) -> Void` — 3rd argument is Start (Event) or Due (Reminder), 4th argument is End (Event only, `nil` for Reminder).

    Add `let selectedDay: Date` as a new stored (non-`@State`) property, and update the one call
    site (`dayListColumn`, line ~1165) to `QuickAddPopover(onSubmit: onQuickAdd, selectedDay: calendarViewState.selectedDay)` — no other change to that `HStack` in this task.

    Add 4 new `@State` properties: `startTime: Date = Date()`, `endTime: Date = Date()`,
    `endManuallyEdited: Bool = false`, `isProgrammaticEndUpdate: Bool = false` (the initial values
    are placeholders only — `@State` defaults cannot call an instance method with `selectedDay`;
    they are overwritten by the `onChange` below the moment the popover actually opens, so the
    placeholder value is never user-visible). `isProgrammaticEndUpdate` distinguishes Task 2's own
    auto-follow write to `endTime` (below) from a genuine user edit — without it, `endRow`'s
    `.onChange(of: endTime)` cannot tell the two apart and incorrectly flips `endManuallyEdited`
    to `true` on the very first Start change, silently breaking D-04's auto-follow after one use.

    Add `.onChange(of: isShowing)` to `body` (alongside the existing `.popover` modifier): when
    `isShowing` becomes `true`, set `let seed = defaultQuickAddTime(selectedDay: selectedDay, now: Date())`, then `startTime = seed`, `endTime = seed.addingTimeInterval(3600)`,
    `endManuallyEdited = false`. This `Date()` call is a live system read at a SwiftUI event-handler
    call site (not inside a pure function), matching `CalendarViewState.swift`'s own documented
    exception for `ObservableObject`/view-level seeding — it is NOT a violation of
    `CalendarGlance.swift`'s "no inline `Date()` in pure logic" rule, which applies only to that
    file's pure functions.

    Change `.popover(isPresented: $isShowing) { quickAddContent }` to
    `.popover(isPresented: $isShowing, arrowEdge: .trailing) { quickAddContent }` (D-07 — forces
    the popover to open toward the day list/trailing, never leftward over the narrow month-grid
    column).

    Inside `quickAddContent`'s `VStack(alignment: .leading, spacing: 8)`, after the existing
    `TextField` and before the submit `Button`, insert `if kind == .event { startRow; endRow } else { dueRow }`. Add 3 new private computed views on `QuickAddPopover`:
    - `startRow`: `HStack { Text("Starts").font(.system(size: 11, weight: .regular, design: .rounded)).foregroundStyle(.white); Spacer(); DatePicker("", selection: $startTime, displayedComponents: .hourAndMinute).datePickerStyle(.compact).labelsHidden() }` with an `.onChange(of: startTime) { _, newValue in ... }` that, when `!endManuallyEdited`, sets `isProgrammaticEndUpdate = true` THEN `endTime = newValue.addingTimeInterval(3600)` — setting the flag before the write is what lets `endRow`'s handler (below) recognize this specific write as auto-follow, not a user edit.
    - `endRow`: same shape, `Text("Ends")`, bound to `$endTime`, with `.onChange(of: endTime) { _, _ in if isProgrammaticEndUpdate { isProgrammaticEndUpdate = false } else { endManuallyEdited = true } }` (D-04 — this suppression-flag check is required: `endRow`'s onChange fires on EVERY write to `endTime`, including `startRow`'s own programmatic auto-follow write, so without the flag the first Start change would incorrectly flip `endManuallyEdited = true` and silently kill auto-follow after one use. The flag consumes itself — `isProgrammaticEndUpdate` resets to `false` immediately after each programmatic write is observed, so the very next direct user edit to End is correctly detected and sets `endManuallyEdited = true`).
    - `dueRow`: same shape, `Text("Due")`, bound to `$startTime` (D-05 — Reminder has no End field at all, not disabled/greyed, just absent).

    Widen the submit `Button`'s existing call from `onSubmit(kind, trimmedTitle)` to
    `onSubmit(kind, trimmedTitle, startTime, kind == .event ? endTime : nil)` — everything else in
    that closure (`title = ""`, `isShowing = false`) stays unchanged.

    Change `.frame(width: 220)` (the last line of `quickAddContent`) to `.frame(width: 240)`
    (UI-SPEC popover-width target).

    Widen `NotchPillView.onQuickAdd` (line ~219) from `(QuickAddKind, String) -> Void = { _, _ in }`
    to `(QuickAddKind, String, Date, Date?) -> Void = { _, _, _, _ in }` — this property is not
    wired to the controller in this plan (that lockstep signature change is Plan 46-02 Task 1); the
    default no-op closure keeps every SwiftUI preview referencing `NotchPillView` compiling
    unmodified in the meantime.
  </action>
  <acceptance_criteria>
    - `grep -c "let onSubmit: (QuickAddKind, String, Date, Date?) -> Void" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "var onQuickAdd: (QuickAddKind, String, Date, Date?) -> Void" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "let selectedDay: Date" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "endManuallyEdited" Islet/Notch/NotchPillView.swift` returns 3 or more (declaration + both onChange sites)
    - `grep -c "isProgrammaticEndUpdate" Islet/Notch/NotchPillView.swift` returns 3 or more (declaration + both onChange sites) — confirms the suppression flag exists and is checked/reset, not just the naive `endManuallyEdited` flip
    - `grep -c "arrowEdge: .trailing" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "\.datePickerStyle(.compact)" Islet/Notch/NotchPillView.swift` returns 3 (Starts/Ends/Due rows)
    - `grep -c "\.frame(width: 240)" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "\.frame(width: 220)" Islet/Notch/NotchPillView.swift` returns 0 (old popover width fully replaced)
    - `xcodebuild -scheme Islet -destination 'platform=macOS' build 2>&1 | tail -20` ends with `** BUILD SUCCEEDED **`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -scheme Islet -destination 'platform=macOS' build 2>&1 | tail -20</automated>
  </verify>
  <done>QuickAddPopover shows Starts/Ends (Event) or Due (Reminder) compact DatePicker rows, seeded from defaultQuickAddTime on open, with Start→End 1-hour auto-follow that stops once End is directly edited; onSubmit/onQuickAdd both carry (QuickAddKind, String, Date, Date?); popover opens trailing at 240pt width; Debug build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| User-selected local Date values (DatePicker) | Values chosen via native macOS `DatePicker` UI — not free-text, not externally sourced. Same trust boundary as the existing title `TextField`, which already has a trim+empty guard (T-28-03, unchanged by this plan). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-46-01 | Tampering | `defaultQuickAddTime(selectedDay:now:)` | accept | Pure derivation from two `Date` inputs the caller already owns (`selectedDay` from `CalendarViewState`, `now` a live system read) — no external/network input, no persistence. |
| T-46-02 | Information Disclosure | `startTime`/`endTime`/`endManuallyEdited` `@State` | accept | Local, transient SwiftUI view state scoped to the popover's own lifetime, never persisted or logged — same trust tier as the existing `title`/`kind` `@State` this plan extends. |
| T-46-03 | Denial of Service | `DatePicker`-driven `Date` values fed into `onSubmit` | accept (deferred to Plan 46-02) | `DatePicker` constrains the value to a valid `Date` by construction (no free-text date parsing) — any downstream EventKit validity constraints (e.g. End before Start) are Plan 46-02's `CalendarService` call-site concern, not this plan's, since this plan does not yet call `CalendarService`. |

No Spoofing / Repudiation / Elevation-of-Privilege entries — this plan adds no new identity, network,
or privilege boundary; it only extends an existing local, same-process SwiftUI popover.
</threat_model>

<verification>
- `xcodebuild -scheme Islet -destination 'platform=macOS' build` succeeds after both tasks.
- Manual Cmd-U in Xcode (recommended, non-blocking): the 3 new `CalendarGlanceTests` methods pass green.
- This plan does not wire real dates into `CalendarService` yet (Plan 46-02's job) — on-device functional verification of the full CALVIEW-05 create-event/create-reminder flow happens in Plan 46-03.
</verification>

<success_criteria>
- `defaultQuickAddTime(selectedDay:now:)` exists, is pure/tested, and correctly branches today-vs-not-today per D-... in `46-CONTEXT.md`.
- `QuickAddPopover` shows a real Starts/Ends (Event) or Due (Reminder) date+time picker, seeded correctly, with D-04's auto-follow behavior.
- `onSubmit`/`onQuickAdd` both carry `(QuickAddKind, String, Date, Date?)` — ready for Plan 46-02 to wire into `NotchWindowController.handleQuickAdd`.
- Debug build is green.
</success_criteria>

<output>
Create `.planning/phases/46-calendar-quick-add-improvements/46-01-SUMMARY.md` when done
</output>
