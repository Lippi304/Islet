---
phase: 46-calendar-quick-add-improvements
plan: 03
type: execute
wave: 3
depends_on: ["46-02"]
files_modified: []
autonomous: false
requirements: [CALVIEW-05, CALVIEW-06, CALVIEW-07]

must_haves:
  truths:
    - "Quick-add's date+time picker (Event Starts/Ends, Reminder Due) works end-to-end on-device: the created Event/Reminder lands in EventKit with the exact picked dates, defaulting correctly to next-full-hour-if-today / 00:00-otherwise, with Start->End 1-hour auto-follow surviving TWO sequential Start changes (not just the first) before any manual End edit (CALVIEW-05, D-04, confirmed on real hardware, not just build-verified)."
    - "The '+ Add' trigger is fully visible with no clipping at its new left-edge position, and its popover opens toward the day list without overlapping the month grid (CALVIEW-06, D-07 re-verified at the new position)."
    - "Day-list event rows are visibly roomier and the island's new width/height accommodate the change without cramping or clipping (CALVIEW-07, D-08/D-10/D-11 on-device tuning pass)."
  artifacts: []
  key_links: []
---

<objective>
On-device confirmation of Plans 46-01/46-02's combined CALVIEW-05/06/07 implementation — the date
picker's real EventKit round-trip, the auto-follow/manual-override time behavior, the button's new
position and popover direction, and the row-padding/island-size bump — plus the directional D-11
sizing values (240pt popover, 472pt calendarWidth, 220pt calendarContentHeight) tuned against the
real rendered layout if needed, per this project's established sizing-iteration precedent (Phases
18/20/21/23/25/26/33/44/45).

Purpose: `xcodebuild build` proves all 3 plans compile and the constants agree with each other; it
cannot prove a `DatePicker`'s live interaction feel, whether the popover's `arrowEdge: .trailing`
genuinely clears the month-grid column at the button's NEW left-edge position (the exact failure
mode CALVIEW-06 exists to fix in a new spot), or whether 220pt/472pt/240pt are the right numbers
once real system fonts and `DatePicker` chrome render. This class of change (popover geometry,
EventKit writes, layout-constant tuning) has this project's own established precedent of requiring
an explicit on-device pass before a phase is considered closed.

Output: Confirmation (or reported regressions/tuning adjustments) for all 3 checks below, run
against a Debug build with Plans 46-01/46-02's changes in place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-calendar-quick-add-improvements/46-CONTEXT.md
@.planning/phases/46-calendar-quick-add-improvements/46-UI-SPEC.md
@.planning/phases/46-calendar-quick-add-improvements/46-01-SUMMARY.md
@.planning/phases/46-calendar-quick-add-improvements/46-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: On-device verification of date picker, button placement, and row/island sizing</name>
  <files></files>
  <action>
    Build and run Islet (Debug, Cmd-R) with Plans 46-01/46-02's changes in place, then execute the
    3 checks below in order. This task is verification-only — no further code changes unless a
    check fails or a D-11 directional value needs on-device tuning, in which case report exactly
    which one and what happened instead (or what value change fixed it).
  </action>
  <what-built>
    Plan 46-01 added a real date+time picker to the calendar quick-add popover: Event shows
    separate "Starts"/"Ends" compact `DatePicker` rows (End auto-follows Start by 1 hour until
    manually edited), Reminder shows a single "Due" row — both seeded from
    `defaultQuickAddTime(selectedDay:now:)` (next full hour if the selected day is today, else
    00:00), with the popover forced to open toward the day list (trailing) at 240pt width. Plan
    46-02 wired those real dates into `NotchWindowController.handleQuickAdd` → `CalendarService` (no
    more hardcoded `day`/`day+3600`), moved the "+ Add" trigger to the day-list column's left edge,
    bumped day-list row padding (12pt horizontal / 8pt vertical / 8pt inter-row spacing, was
    8/5/6), and widened the island for Calendar specifically (`calendarWidth` 460→472,
    `calendarContentHeight` 220 replacing the shared 196pt box for Calendar only).
  </what-built>
  <how-to-verify>
    Build and run Islet in Debug from Xcode (Cmd-R), open the Calendar tab, then on-device:

    1. **Date+time picker end-to-end (CALVIEW-05)** — select today's date in the month grid, tap
       "+ Add", confirm the "Starts" row defaults to the next full hour from right now (e.g. if it's
       2:23pm, Starts should show 3:00pm) and "Ends" defaults to Starts + 1 hour. Change Starts to a
       different time and confirm Ends shifts to preserve the 1-hour gap. THEN, without touching
       Ends, change Starts AGAIN to a second different time and confirm Ends shifts again to preserve
       the 1-hour gap (D-04 regression check: auto-follow must survive more than one Start change per
       popover session — a suppression-flag bug here would make it work once and silently stop).
       Only after confirming two sequential Start changes both auto-follow, manually change Ends to a
       different time and confirm changing Starts again no longer moves Ends. Create the Event
       and confirm it appears in the day list at the exact times you picked (not the old hardcoded
       day/day+1hr). Repeat for a Reminder: confirm only a single "Due" row appears (no Ends field
       at all), create it, and confirm no crash/error. Select a day that is NOT today, tap "+ Add"
       again, and confirm the default time is 00:00 instead of "next full hour".

    2. **Button placement & popover direction (CALVIEW-06, D-07)** — confirm "+ Add" now renders
       fully visible at the LEFT edge of the day-list column, immediately next to the vertical
       divider between the month grid and the day list, with no clipping against either edge of the
       island. Tap it and confirm the popover opens toward the day list (right/trailing side) and
       never visually overlaps or clips against the narrow month-grid column on the left — this is
       the exact failure mode CALVIEW-06 exists to fix in a new spot, so check it carefully.

    3. **Row padding & island sizing (CALVIEW-07, D-08/D-10/D-11 tuning pass)** — with several
       events on the selected day, confirm each day-list row visibly has more breathing room than
       before (roomier padding, not cramped). Confirm the Calendar island itself is slightly wider
       and taller than before, with no content clipping against the island's rounded edges (top
       camera-clearance curve or bottom corner). If the 220pt `calendarContentHeight`, 472pt
       `calendarWidth`, or 240pt popover width look off (too tight, too much empty space, clipped
       content) once real system fonts/DatePicker chrome render, note the specific adjustment needed
       — this is the expected place for D-11's directional numbers to get a final on-device tune.
  </how-to-verify>
  <verify>
    <human-check>All 3 checks above behave as expected on-device.</human-check>
  </verify>
  <done>The date+time picker creates Events/Reminders with the exact picked dates and correct today/not-today defaults; the auto-follow/manual-override behavior works as specified across TWO sequential Start changes (not just the first) before any manual End edit; "+ Add" is fully visible at the day-list column's left edge with its popover opening trailing, never overlapping the month grid; day-list rows are visibly roomier and the island's new size fits the content without clipping.</done>
  <resume-signal>Type "approved" if all 3 checks behave as expected, or describe exactly which check/step failed (or which D-11 value needs tuning) and what happened instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Unchanged from Plans 46-01/46-02 — this plan performs no code changes, only on-device human
verification of those plans' already-assessed date-picker/EventKit-write and layout-geometry
changes.

## STRIDE Threat Register

N/A — no code modified in this plan (`files_modified: []`). See 46-01-PLAN.md/46-02-PLAN.md's
threat models for the full register covering the actual code changes this verification confirms.
</threat_model>

<verification>
All 3 checks confirmed on-device per the checkpoint above.
</verification>

<success_criteria>
- The date+time picker's full CALVIEW-05 create-flow (Event Start/End, Reminder Due, auto-follow,
  today/not-today default) is confirmed correct on real hardware.
- The "+ Add" button's new left-edge position and trailing popover direction show no clipping or
  overlap (CALVIEW-06).
- Day-list row padding and the Calendar island's new width/height are confirmed roomier with no
  clipping (CALVIEW-07), with any needed D-11 tuning adjustment reported.
</success_criteria>

<output>
Create `.planning/phases/46-calendar-quick-add-improvements/46-03-SUMMARY.md` when done
</output>
