---
phase: 63-meeting-hud
plan: 04
type: execute
wave: 4
depends_on: ["63-02", "63-03"]
files_modified:
  - Islet/Notch/NotchWindowController.swift
  - Islet/Notch/IslandResolver.swift
  - Islet/Notch/NotchPillView.swift
autonomous: false

requirements: [MEET-01, MEET-02, MEET-03]

must_haves:
  truths:
    - "Tapping the mute icon toggles the real system-wide mic mute and the icon visibly reflects the new state"
    - "The HUD appears/disappears immediately on real call start/end, no debounce either direction (D-07/D-08)"
    - "Charging/Device still preempt Meeting-HUD and it resumes once they clear (D-05)"
    - "The Meeting HUD Settings toggle defaults OFF (SETTINGS-05) and starting/stopping the monitor live works"
    - "Google Meet in a browser never shows the HUD (MEET-03), and the Settings card copy correctly scopes to Zoom/Teams only"
    - "Tapping the mute icon at its real rendered screen position (the wing's trailing edge, ~100pt outside the raw notch cutout) registers a click — the collapsed click-through hot-zone is widened specifically to reach it (D-09)"
  artifacts:
    - path: "Islet/Notch/NotchWindowController.swift"
      provides: "startMeetingMonitor()/handleMeetingActivityChange(_:)/handleMuteTap()/handleSettingsChanged() Meeting-HUD block/launch-gate"
      contains: "func handleMeetingActivityChange"
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "meetingMuteIconTrailingEdgeOffset — single-source-of-truth static constant for the mute icon's real trailing-edge offset, shared by meetingWings(for:) and collapsedInteractiveZone()"
      contains: "static var meetingMuteIconTrailingEdgeOffset"
  key_links:
    - from: "NotchWindowController.handleMuteTap()"
      to: "toggleSystemInputMute()"
      via: "direct call, mirrors OSDInterceptor's self-drive write pattern"
      pattern: "toggleSystemInputMute\\(\\)"
    - from: "NotchPillView onMuteTap closure"
      to: "NotchWindowController.handleMuteTap()"
      via: "closure forwarding in makeRootView's NotchPillView(...) call site"
      pattern: "onMuteTap:"
    - from: "NotchWindowController.collapsedInteractiveZone()"
      to: "NotchPillView.meetingMuteIconTrailingEdgeOffset"
      via: "meeting-presentation-gated hot-zone widen (D-09)"
      pattern: "meetingMuteIconTrailingEdgeOffset"
---

<objective>
Wire the final controller layer — `MeetingMonitor` lifecycle, the mute-tap handler, live Settings
toggle start/stop, and the launch-time gate — completing the vertical slice from detection through
tap-to-mute; widen the collapsed click-through hot-zone so the mute icon's real screen position is
actually reachable (D-09); then run the phase's full on-device UAT across all 3 requirements
(MEET-01/02/03).

Purpose: this is the LAST wiring plan; every prior plan (63-01 pure models, 63-02 detection +
spike go/no-go, 63-03 resolver + rendering) feeds into this controller layer exactly like
`startCapsLockMonitor()`/`handleCapsLockChange(_:)`/`handleSettingsChanged()`'s existing 1:1
precedent. Without the hot-zone widen, the mute icon renders correctly but never receives clicks —
`collapsedInteractiveZone()` gates whether the OS panel accepts mouse events at all, independent of
what SwiftUI renders.
Output: Meeting-HUD is fully shippable — detectable, rendered, tappable (at its REAL screen
position, not just visually), toggleable, on-device UAT'd.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-meeting-hud/63-CONTEXT.md
@.planning/phases/63-meeting-hud/63-RESEARCH.md
@.planning/phases/63-meeting-hud/63-PATTERNS.md
@.planning/phases/63-meeting-hud/63-VALIDATION.md

<interfaces>
From Islet/Notch/NotchWindowController.swift (current live code — READ THE FULL RELEVANT
SECTIONS before editing; these are the exact 1:1 precedents this plan mirrors):

Idempotent monitor start (mirrors `startCapsLockMonitor()` exactly):
```swift
private func startCapsLockMonitor() {
    guard capsLockMonitor == nil else { return }
    let monitor = CapsLockMonitor { [weak self] isOn in self?.handleCapsLockChange(isOn) }
    capsLockMonitor = monitor
    monitor.start()
}
```

Live settings toggle -> monitor start/stop -> flushTransients (mirrors the Caps Lock /
Download-Progress blocks inside `handleSettingsChanged()` exactly):
```swift
if activityEnabled(ActivitySettings.capsLockKey) {
    startCapsLockMonitor()
} else if capsLockMonitor != nil {
    capsLockMonitor?.stop(); capsLockMonitor = nil
    flushTransients(.capsLock)
}
```

`TransientCategory`/`flushTransients` (the switch has a `default: return false` inside the
`matches` closure, so adding `.meeting` is additive, not exhaustiveness-breaking there — but the
OUTER `switch category { ... }` body IS exhaustive and needs a new `case .meeting: break` arm,
mirroring `.capsLock`/`.updateAvailable`'s existing no-op arms):
```swift
private enum TransientCategory { case charging, device, focus, osd, capsLock, updateAvailable, downloadProgress, timer }
private func flushTransients(_ category: TransientCategory) {
    let matches: (ActiveTransient) -> Bool = { t in
        switch (t, category) {
        case (.charging, .charging), (.device, .device), ..., (.timer, .timer): return true
        default: return false
        }
    }
    ...
    switch category {
    case .charging: chargingState.activity = nil
    case .capsLock: break
    case .updateAvailable: break
    ...
    }
}
```

`TransientQueue.updateHead(_:)` in-place refresh (mirrors the `(.timer, .timer): head = t` case —
add `case (.meeting, .meeting): head = t` alongside it in IslandResolver.swift IF the mute-tap
in-place refresh needs it; confirm this case doesn't already exist from Plan 63-03 before adding):
```swift
mutating func updateHead(_ t: ActiveTransient) {
    guard let h = head else { return }
    switch (h, t) {
    case (.timer, .timer): head = t
    default: break
    }
}
```

Launch-time monitor-start sequence in `start()` (mirrors the Caps Lock launch-gate line exactly):
```swift
if activityEnabled(ActivitySettings.capsLockKey) { startCapsLockMonitor() }
```

`makeRootView()`'s `NotchPillView(...)` call site — closures are forwarded positionally/by-name
alongside the existing Timer closures:
```swift
onTimerPauseResume: { [weak self] in self?.handleTimerPauseResume() },
...
onStartPomodoro: { [weak self] work, brk in self?.handleStartPomodoro(workSeconds: work, breakSeconds: brk) })
```

From Islet/ActivitySettings.swift (already present, confirmed by direct read — NO changes needed
here): `static let meetingHUDKey = "activity.meetingHUD"` already exists and is already included
in `defaultsToFalseKeys` (SETTINGS-05 default-OFF already correct). From Islet/SettingsView.swift
(already present, confirmed by direct read — NO changes needed here): the Meeting HUD
`ActivityCardData` entry at `SettingsView.swift:238-241` already reads Title "Meeting HUD" /
Description "Shows a call timer and mic-mute control during Zoom/Teams calls." / `isOn:
$meetingHUDEnabled` — this description string ALREADY correctly scopes to "Zoom/Teams calls" (not
an unqualified "video calls"), satisfying MEET-03's "documented as a known limitation, not
silently missing" requirement. Confirm this during the on-device checklist below; do not change it
unless it's found to say something broader than Zoom/Teams.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NotchWindowController.swift — MeetingMonitor lifecycle + mute-tap handler + settings wiring</name>
  <files>Islet/Notch/NotchWindowController.swift, Islet/Notch/IslandResolver.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift
    Islet/Notch/IslandResolver.swift
    Islet/Notch/MeetingMonitor.swift
    Islet/Notch/MicMuteController.swift
    Islet/ActivitySettings.swift
  </read_first>
  <action>
    Add `private var meetingMonitor: MeetingMonitor?` to the property block, placed near
    `capsLockMonitor`/`downloadMonitor`'s existing declarations, mirroring their exact style.
    Add `private func startMeetingMonitor()` — idempotent (`guard meetingMonitor == nil else { return }`),
    constructs `MeetingMonitor { [weak self] reading in self?.handleMeetingActivityChange(reading) }`,
    calls `monitor.start()` — mirrors `startCapsLockMonitor()`'s exact shape. Add
    `private func handleMeetingActivityChange(_ reading: MeetingReading?)`: if `reading` is
    non-nil, construct `MeetingActivity(callStart: reading.detectedAt, isMuted: readSystemInputMuted())`
    and call `transientQueue.preempt(.meeting(activity))` UNCONDITIONALLY — per D-05, Meeting-HUD
    outranks every persistent case except Charging/Device, and `preempt()`'s own generalized
    `currentHead.isPersistent` guard already handles displacing WHICHEVER persistent case
    currently holds the head, so no `if case .focus` conditional guard (unlike
    `handleCapsLockChange`) is needed here. Because `MeetingMonitor` (Plan 63-02) only calls its
    `onChange` on an ACTUAL nil<->non-nil transition (dedup discipline), this `preempt` call fires
    exactly once per real call-start — never repeatedly re-preempting an already-standing
    `.meeting` head. If `reading` is nil, call
    `transientQueue.removeAll(where: { if case .meeting = $0 { return true }; return false })`
    (D-08: immediate hide, no debounce, symmetric with D-07's immediate show). Call
    `presentTransientChange()` after either branch if the queue changed. Add
    `private func handleMuteTap()`: calls `toggleSystemInputMute()`; if it returns a non-nil
    `newMuted` AND `transientQueue.head` is currently `case .meeting(let m)`, call
    `transientQueue.updateHead(.meeting(MeetingActivity(callStart: m.callStart, isMuted: newMuted)))`
    (in-place refresh — add a `case (.meeting, .meeting): head = t` arm to
    `IslandResolver.swift`'s `TransientQueue.updateHead(_:)` alongside the existing `.timer` case
    if it isn't already present) — no `presentTransientChange()` re-trigger needed (payload-only
    change, same displayed case as before). Wire `onMuteTap: { [weak self] in self?.handleMuteTap() }`
    into `makeRootView()`'s `NotchPillView(...)` call site, alongside the existing Timer closures.
    Add the Meeting-HUD block to `handleSettingsChanged()`, mirroring the Caps Lock toggle-on/off
    block exactly: `if activityEnabled(ActivitySettings.meetingHUDKey) { startMeetingMonitor() } else if meetingMonitor != nil { meetingMonitor?.stop(); meetingMonitor = nil; flushTransients(.meeting) }`.
    Add `.meeting` to the private `TransientCategory` enum, add
    `case (.meeting, .meeting): return true` to `flushTransients`'s inner `matches` tuple-switch
    (additive — that switch already has a `default: return false`), and add `case .meeting: break`
    to `flushTransients`'s OUTER exhaustive `switch category` (no separate `@Published` model to
    clear, mirrors `.capsLock`/`.updateAvailable`'s existing `break` arms). Add
    `if activityEnabled(ActivitySettings.meetingHUDKey) { startMeetingMonitor() }` to `start()`'s
    existing launch-time monitor-start sequence, mirroring the Caps Lock launch-gate line exactly
    (no extra `isAuthorized`-style condition — CoreAudio/NSWorkspace calls need no special TCC per
    Plan 63-02's spike go/no-go).
  </action>
  <acceptance_criteria>
    <criterion>grep -c "private var meetingMonitor: MeetingMonitor?" Islet/Notch/NotchWindowController.swift == 1</criterion>
    <criterion>grep -c "func startMeetingMonitor" Islet/Notch/NotchWindowController.swift == 1</criterion>
    <criterion>grep -c "func handleMeetingActivityChange" Islet/Notch/NotchWindowController.swift == 1</criterion>
    <criterion>grep -c "func handleMuteTap" Islet/Notch/NotchWindowController.swift == 1</criterion>
    <criterion>grep -c "transientQueue.preempt(.meeting(activity))" Islet/Notch/NotchWindowController.swift == 1</criterion>
    <criterion>grep -c "ActivitySettings.meetingHUDKey" Islet/Notch/NotchWindowController.swift >= 2</criterion>
    <criterion>grep -c "onMuteTap:" Islet/Notch/NotchWindowController.swift >= 1</criterion>
    <criterion>grep -c "case (.meeting, .meeting): head = t" Islet/Notch/IslandResolver.swift == 1</criterion>
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -scheme Islet -configuration Debug 2>&1 | tail -30 &amp;&amp; xcodebuild test -scheme Islet -only-testing:IsletTests/IslandResolverTests 2>&1 | tail -20</automated>
  </verify>
  <done>Debug build succeeds; IslandResolverTests regression suite stays green; Meeting-HUD is fully wired end to end (detection -> resolver -> render -> tap -> mute -> Settings toggle).</done>
</task>

<task type="auto">
  <name>Task 2: NotchWindowController.swift + NotchPillView.swift — widen the collapsed click-through hot-zone to reach the mute icon (D-09)</name>
  <files>Islet/Notch/NotchWindowController.swift, Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift (collapsedInteractiveZone() lines ~1705-1739,
    syncClickThrough() lines ~1898-1911, hotZone/hotZonePadding declarations ~423-451,
    positionAndShow's hotZone assignment ~1398)
    Islet/Notch/NotchPillView.swift (secondaryBubbleDiameter/secondaryBubbleGap/
    secondaryBubbleCenterOffset ~426-437, meetingWings(for:) added by Plan 63-03)
    .planning/phases/63-meeting-hud/63-UI-SPEC.md
  </read_first>
  <action>
    `collapsedInteractiveZone()` (NotchWindowController.swift ~1717-1739) currently widens the raw
    `hotZone` for exactly two existing cases (the NowPlaying secondary bubble, the NowPlaying idle
    hover-preview) and returns `hotZone` UNCHANGED for every other presentation — including
    Meeting-HUD. `meetingWings(for:)`'s mute icon (Plan 63-03) renders past the raw
    notch-cutout-sized `hotZone`, per 63-UI-SPEC.md's locked geometry (margin 20pt beyond the
    camera block's edge, then rightContentWidth 84pt = mm:ss 60pt + 4pt gap + mute icon 20pt) — a
    tap on the mute icon currently never reaches SwiftUI at all, because `syncClickThrough()` only
    flips `ignoresMouseEvents = false` inside whatever rect `collapsedInteractiveZone()` returns.
    First, in NotchPillView.swift, add two new static constants next to the existing
    `secondaryBubbleDiameter`/`secondaryBubbleGap` constants (~line 428):
    `static let meetingWingMargin: CGFloat = 20` and
    `static let meetingWingRightContentWidth: CGFloat = 84` — these mirror 63-UI-SPEC.md's locked
    Spacing Scale values verbatim, do not invent new numbers. Add
    `static var meetingMuteIconTrailingEdgeOffset: CGFloat { meetingWingMargin + meetingWingRightContentWidth }`
    immediately after, mirroring `secondaryBubbleCenterOffset`'s exact single-source-of-truth
    computed-property shape (~line 436-437) — this is the same WR-03 discipline the file's own
    comment documents: never repeat a geometry literal as a bare number at a second call site.
    Then refactor `meetingWings(for:)` (Plan 63-03) so its local `margin`/`rightContentWidth`
    values read FROM these two new static constants (`NotchPillView.meetingWingMargin`,
    `.meetingWingRightContentWidth`) instead of separate local literals, so the geometry has
    exactly one source of truth for both the render and the click-through math.
    Second, in `collapsedInteractiveZone()`, add a third branch — after the existing
    secondary-bubble branch, before the existing NowPlaying-idle-hover-preview branch, neither of
    which changes — gated on `if case .meeting = presentationState.presentation`: compute
    `let muteFarEdge = hotZone.maxX + NotchPillView.meetingMuteIconTrailingEdgeOffset` and return
    `CGRect(x: hotZone.minX, y: hotZone.minY, width: muteFarEdge - hotZone.minX, height: hotZone.height)`.
    This widen is unconditional whenever the case matches (unlike the bubble branch's own
    `if bubbleFarEdge > hotZone.maxX` guard — the mute icon ALWAYS sits outside the raw
    camera-block-sized `hotZone`, since `meetingWingMargin`/`meetingWingRightContentWidth` are both
    positive constants, so no extra comparison is needed here). Add a doc comment directly above
    this new branch (63-04 plan revision, D-09) explaining why the widen is needed, mirroring the
    Phase-42/Phase-53 doc-comment convention already on the two branches above it.
  </action>
  <acceptance_criteria>
    <criterion>grep -c "static let meetingWingMargin: CGFloat = 20" Islet/Notch/NotchPillView.swift == 1</criterion>
    <criterion>grep -c "static let meetingWingRightContentWidth: CGFloat = 84" Islet/Notch/NotchPillView.swift == 1</criterion>
    <criterion>grep -c "static var meetingMuteIconTrailingEdgeOffset" Islet/Notch/NotchPillView.swift == 1</criterion>
    <criterion>grep -c "NotchPillView.meetingMuteIconTrailingEdgeOffset" Islet/Notch/NotchWindowController.swift == 1</criterion>
    <criterion>grep -c "if case .meeting = presentationState.presentation" Islet/Notch/NotchWindowController.swift == 1</criterion>
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -scheme Islet -configuration Debug 2>&1 | tail -30</automated>
  </verify>
  <done>collapsedInteractiveZone() widens the click-through hot-zone by meetingMuteIconTrailingEdgeOffset (104pt) on the trailing edge whenever a Meeting-HUD transient is the collapsed head, reaching the mute icon's real rendered screen position; Debug build succeeds; meetingWings(for:) and the new widen branch read geometry from the same single-source-of-truth static constants.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The complete Meeting-HUD feature: detection (MeetingMonitor, spike-approved in Plan 63-02), the shared mic-mute primitive (MicMuteController), resolver priority (rank 3, D-05/D-06), collapsed-pill rendering with an inline-tappable mute icon (D-09/D-10), the widened click-through hot-zone that makes the mute icon's real screen position actually clickable (Task 2), and the Settings toggle (default OFF, SETTINGS-05).</what-built>
  <how-to-verify>
    1. Open Settings and confirm "Meeting HUD" is OFF by default (SETTINGS-05); turn it ON.
    2. Join a real Zoom or Teams call with mic on — the collapsed pill shows the call icon + a
       live mm:ss elapsed counter; confirm tapping anywhere on the wing EXCEPT the mute icon does
       nothing (no expand, D-10).
    3. Tap the mute icon PRECISELY at its rendered position (the far trailing edge of the wing,
       well outside the raw physical notch cutout — this exercises Task 2's collapsedInteractiveZone()
       widening, not just a click near the center of the pill) — confirm the tap registers and the
       SYSTEM mic actually mutes (check System Settings > Sound > Input level meter goes dead, or
       another mic-using app confirms silence), the icon swaps to `mic.slash.fill` with a red tint;
       tap again to unmute, icon reverts to white `mic.fill`.
    3b. Move the pointer just past the mute icon's far edge (further right, off the rendered wing
        entirely) and click — confirm the click passes through to whatever is underneath (the
        widened hot-zone is bounded to the icon's real footprint, not an open-ended region).
    4. Verify the mm:ss format never switches to h:mm:ss even past the 60-minute mark (D-13) — if
       a 60+ minute call isn't feasible live, cross-check against Plan 63-01's
       `MeetingActivityTests.swift` 60:32-rollover case instead.
    5. While the call HUD is showing, plug in the charger (or connect/disconnect a Bluetooth
       device) — confirm Charging/Device still preempts Meeting-HUD (D-05), and Meeting-HUD
       resumes automatically once the Charging/Device splash clears.
    6. End the call / quit Zoom or Teams — the HUD disappears immediately, no lingering dismiss
       window (D-08).
    7. Join a Google Meet call in a browser with mic on — confirm NO HUD appears (MEET-03).
    8. Confirm the Settings card's description text still reads "Shows a call timer and mic-mute
       control during Zoom/Teams calls" (not a broader "video calls" claim) — this satisfies
       MEET-03's documentation requirement; no change needed if so.
    9. Run the full regression suite: `xcodebuild test -scheme Islet` — confirm no NEW failures
       beyond any pre-existing unrelated ones.
  </how-to-verify>
  <resume-signal>Reply "approved" or describe which step failed and how.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User tap -> system audio state write | The mute icon's tap ultimately calls `AudioObjectSetPropertyData` on the real input device — a genuine (though instantly reversible) system-state write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-08 | Elevation of Privilege | `handleMuteTap()` writes system-wide mic mute state with no confirmation dialog | accept | Instantly reversible (tap again to unmute) — matches every other single-tap wing control in this codebase; no destructive/irreversible action exists anywhere in `NotchPillView.swift`, so no confirmation pattern exists to deviate from |
| T-63-09 | Denial of Service | `preempt(.meeting(activity))` called unconditionally without duplicate-entry protection | mitigate | `MeetingMonitor` (Plan 63-02) dedupes its `onChange` callback to fire ONLY on an actual nil<->non-nil transition, so `preempt`/`removeAll` are each called exactly once per real call start/end — no duplicate-pending-entry risk in `TransientQueue.pending` |
| T-63-10 | Information Disclosure | `ActivitySettings.meetingHUDKey` / launch-gate touch no PII | accept | Plain boolean UserDefaults flag, identical risk profile to every other activity toggle already shipped in this codebase |
| T-63-11 | Tampering (click-swallowing) | `collapsedInteractiveZone()`'s new Meeting-HUD widen branch could over-widen and swallow clicks meant for the desktop/app underneath the notch | mitigate | Widen is bounded to an exact, code-reviewed constant (`meetingMuteIconTrailingEdgeOffset` = 104pt, derived 1:1 from 63-UI-SPEC.md's locked geometry, single source of truth shared with `meetingWings(for:)`'s own render math) and strictly gated on `if case .meeting = presentationState.presentation` — mirrors T-42-07's bounded-widen discipline exactly; UAT step 3b explicitly verifies clicks past the icon's edge still pass through |
</threat_model>

<verification>
- `xcodebuild build -scheme Islet -configuration Debug` succeeds
- `xcodebuild test -scheme Islet -only-testing:IsletTests/IslandResolverTests` green
- On-device UAT checkpoint approved (all checklist steps including 3/3b, MEET-01/02/03 fully confirmed)
- Update REQUIREMENTS.md: mark MEET-01, MEET-02, MEET-03 Complete once UAT is approved
</verification>

<success_criteria>
- Meeting-HUD is fully shippable: detects real Zoom/Teams calls, shows a live call-timer HUD at the correct priority rank, mute icon is actually clickable at its real screen position (collapsed hot-zone widened per D-09) and toggles the real system mic and reflects state, Google Meet stays undetected (documented), Settings toggle defaults OFF
</success_criteria>

<output>
Create `.planning/phases/63-meeting-hud/63-04-SUMMARY.md` when done
</output>
