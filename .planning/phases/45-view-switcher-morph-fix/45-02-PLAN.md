---
phase: 45-view-switcher-morph-fix
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified: []
autonomous: false
requirements: [SWITCH-01, SWITCH-02]

must_haves:
  truths:
    - "All 12 pairwise tab-to-tab transitions (Home↔Tray, Home↔Calendar, Home↔Weather, Tray↔Calendar, Tray↔Weather, Calendar↔Weather, both directions each) morph continuously with no disappear/rebuild flicker (SWITCH-01, D-03 locked)."
    - "No large→small transition (e.g. Calendar→Tray, Weather-Large→Tray) shows the island rendering behind/underneath the switcher pill buttons (SWITCH-02)."
    - "Tapping a new tab while mid-morph toward a previously-tapped tab retargets the spring immediately toward the new tab — never ignored, never queued (D-01)."
  artifacts: []
  key_links: []
---

<objective>
On-device confirmation of Plan 45-01's fix, per D-03 (locked, stricter than a representative
sample): every one of the 12 pairwise tab-to-tab transitions, both directions, must be walked and
confirmed glitch-free.

Purpose: `xcodebuild build` in Plan 45-01 proves the single-call-site refactor compiles and
reproduces today's exact width/height mapping — it cannot prove the actual live
`matchedGeometryEffect` animation now morphs continuously instead of flickering, nor that the
z-order glitch is gone. This project's own established convention (memory
`cr01-clickthrough-or-defeat-gotcha`, and 45-RESEARCH.md Pitfall 1: "a partial fix can look correct
for some of the 12 pairwise transitions and wrong for others") makes this on-device sweep mandatory,
not skippable, even though the code review/build gate already looks clean.

Output: Confirmation (or a reported regression, naming exactly which pair/direction) for all 12
pairwise transitions plus the interrupted-mid-morph retarget check, run against a Debug build with
Plan 45-01's changes in place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-view-switcher-morph-fix/45-CONTEXT.md
@.planning/phases/45-view-switcher-morph-fix/45-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: On-device 12-pairwise-transition sweep + interrupted-tap retarget check</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <action>
    Build and run Islet (Debug, Cmd-R) with Plan 45-01's changes in place, then execute the checks
    below in order. This task is verification-only — no further code changes unless a check fails,
    in which case report exactly which pair/direction/step failed and what happened instead.
  </action>
  <what-built>
    Plan 45-01 collapsed the 6 switcher-row `IslandPresentation` cases' independent `blobShape`
    calls into ONE shared `tabContentView` call site (`tabWidth`/`tabHeight` computed values,
    content-only inner switch). This gives every tab case one continuous view identity, so the
    existing `matchedGeometryEffect` + `handleSwitcherSelect`'s spring
    (`response: 0.6, dampingFraction: 0.62`, unchanged) now morphs directly between tab sizes
    instead of tearing down and rebuilding the subtree.
  </what-built>
  <how-to-verify>
    Build and run Islet in Debug from Xcode (Cmd-R). With something staged in each tab (a track
    played this session for Home/NowPlaying, at least one calendar event, weather permission
    granted, at least one file in Tray) so every tab's real, populated content is visible during
    the sweep (not just empty states):

    1. **All 12 pairwise transitions, both directions (D-03, locked — walk every one, do not
       sample):**
       - Home→Tray, Tray→Home
       - Home→Calendar, Calendar→Home
       - Home→Weather, Weather→Home
       - Tray→Calendar, Calendar→Tray
       - Tray→Weather, Weather→Tray
       - Calendar→Weather, Weather→Calendar

       For each of the 12: tap the target tab's switcher-row icon and watch the transition.
       Expected for every single one: the island shape morphs continuously and directly to the new
       tab's size — no visible disappear/rebuild flicker (SWITCH-01), and the switcher-row icon
       buttons stay visually ON TOP of the island shape throughout the entire morph, never
       behind/underneath it even during a large→small shrink (e.g. Calendar→Tray, Weather→Tray)
       (SWITCH-02).

    2. **Interrupted mid-morph tap (D-01):** tap a tab, and while the island is still visibly
       mid-animation toward it, immediately tap a third, different tab. Expected: the spring
       redirects smoothly toward the new (third) target — reads as one continuous redirected
       motion, never two discrete separate hops, and the tap is never ignored or queued for after
       the first morph finishes.

    3. **Populated-Home sanity (45-RESEARCH.md Pitfall 2):** repeat at least one Home-involving
       pairwise transition (e.g. Tray→Home) while a track is ACTIVELY PLAYING (not paused, not
       empty) — confirm it is equally glitch-free. (`.nowPlayingExpanded` is one of the 6 unified
       cases; this check confirms the "3 sub-states of Home" are not accidentally left on the old,
       unfixed code path.)
  </how-to-verify>
  <verify>
    <human-check>All 12 pairwise transitions (both directions) plus the interrupted-tap retarget and populated-Home sanity check behave as expected on-device.</human-check>
  </verify>
  <done>All 12 pairwise tab transitions morph continuously with no flicker and no behind-buttons z-order glitch in either direction; an interrupted mid-morph tap redirects smoothly to the new target; the populated (actively-playing) Home state is confirmed equally glitch-free.</done>
  <resume-signal>Type "approved" if all checks behave as expected, or describe exactly which pair/direction/step failed and what happened instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Unchanged from Plan 45-01 — this plan performs no code changes, only on-device human verification
of Plan 45-01's already-assessed refactor.

## STRIDE Threat Register

N/A — no code modified in this plan (`files_modified: []`). See 45-01-PLAN.md's threat model for
the full register covering the actual code change this verification confirms.
</threat_model>

<verification>
All 12 pairwise transitions (both directions), the interrupted-tap retarget, and the
populated-Home sanity check confirmed on-device per the checkpoint above.
</verification>

<success_criteria>
- All 12 pairwise tab-to-tab transitions, both directions, morph continuously with no
  disappear/rebuild flicker (SWITCH-01).
- No large→small transition renders the island behind the switcher pill buttons (SWITCH-02).
- An interrupted mid-morph tap redirects the spring smoothly toward the new target (D-01).
- The populated/actively-playing Home state is equally glitch-free, not just the empty state.
</success_criteria>

<output>
Create `.planning/phases/45-view-switcher-morph-fix/45-02-SUMMARY.md` when done
</output>
