---
phase: 65-quick-actions-bar
plan: 08
type: execute
wave: 4
depends_on: [65-07]
files_modified:
  - Islet/Notch/QuickActionsBar/QuickActionsBarManualSpike.swift
autonomous: false
requirements: [QACTION-01, QACTION-02, QACTION-03]

must_haves:
  truths:
    - "All 8 configured Quick Actions have been exercised on real hardware at least once and confirmed to perform their real system effect"
    - "Screen Lock (private API, RESEARCH.md Pitfall 3/A2) is confirmed working on the current macOS version, or its failure is explicitly documented if it is not"
    - "DND/Focus failure-visible behavior (QACTION-03) is confirmed both with and without the required 'Islet Toggle Focus' Shortcut configured"
    - "Mic mute toggled from the Quick Actions bar and from Meeting-HUD (if reachable) both reflect the identical live system state"
  artifacts:
    - path: "Islet/Notch/QuickActionsBar/QuickActionsBarManualSpike.swift"
      provides: "#if DEBUG scaffold exercising all 8 real system calls for fast on-device iteration"
      contains: "#if DEBUG"
  key_links: []
---

<objective>
Close out Phase 65 with the manual/on-device verification VALIDATION.md requires for every
system-call side effect XCTest cannot meaningfully assert (display sleep, dark mode, screen lock,
DND, caffeinate, empty trash, launch, mic mute) — plus the one genuinely uncertain claim
(RESEARCH.md A2: does `SACLockScreenImmediate` still work on this Mac's current macOS version)
that must be confirmed before the phase can be considered done, per this codebase's own
"on-device verification over research assumption" convention (Phase 38 Path A/Path B precedent).

Purpose: Every other plan in this phase is code-complete but system-call side effects are
unverifiable by any automated test; this plan is the phase-closing gate.
Output: A `#if DEBUG`-gated manual spike file for fast iteration, and a completed on-device
checkpoint covering all 8 actions plus the two hardest cases (Screen Lock, DND).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-quick-actions-bar/65-VALIDATION.md
@.planning/phases/65-quick-actions-bar/65-RESEARCH.md

<interfaces>
From Islet/Notch/NowPlayingMonitor.swift (the existing `#if DEBUG` spike convention to mirror —
NSLog-marked, throwaway, gated out of Release builds):
```swift
#if DEBUG
func spikeLikeCurrentTrack() {
    NSLog("SPIKE likeTrack() sending kMRLikeTrack")
    controller.likeTrack()
}
#endif
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: QuickActionsBarManualSpike.swift</name>
  <files>Islet/Notch/QuickActionsBar/QuickActionsBarManualSpike.swift</files>
  <read_first>
    Islet/Notch/NowPlayingMonitor.swift
  </read_first>
  <action>
    Create `Islet/Notch/QuickActionsBar/QuickActionsBarManualSpike.swift` wrapped entirely in
    `#if DEBUG ... #endif` (mirrors `NowPlayingMonitor.swift`'s `spikeLikeCurrentTrack`/
    `spikeTriggerAutomationPrompt` convention — never compiled into Release). Add one
    `NSLog`-marked static function per catalog action, each directly calling that action's real
    Plan 65-02/65-03/65-04 helper (`DisplaySleepAction.sleepNow()`,
    `ScreenLockAction.lockNow()`, `DarkModeToggleAction.toggle { NSLog("SPIKE darkMode result=\($0)") }`,
    `CaffeinateToggleAction.toggle()` + `NSLog("SPIKE caffeinate isActive=\(CaffeinateToggleAction.isActive)")`,
    `FocusToggleAction.toggle { NSLog("SPIKE focus result=\($0)") }`, `EmptyTrashAction.empty {
    NSLog("SPIKE emptyTrash result=\($0)") }`, `LaunchAction.launch(target: "https://apple.com")`,
    `_ = toggleSystemInputMute()`), grouped under one `enum QuickActionsBarManualSpike { ... }`
    namespace so they are easy to trigger individually from a debugger/breakpoint or a temporary
    call site during Task 2's on-device pass — no UI hookup required (this file is a development
    aid, not shipped functionality).
    This file is brand new. After creating it, run `xcodegen generate` from the project root
    BEFORE building — this project's `project.yml` uses XcodeGen glob sources (`sources: - path:
    Islet`) and `Islet.xcodeproj`'s explicit `PBXFileReference`/`PBXBuildFile` entries do NOT
    auto-discover new files on disk; skipping this step silently excludes the file from the build
    target and both the Debug and Release `xcodebuild build` commands below would report a
    false-positive green with the file simply absent from either target (masking the very
    Debug-vs-Release exclusion check this task exists to perform). This exact gap caused blocking
    mid-execution fixes at Phase 59-02 and Phase 61-01, per STATE.md.
  </action>
  <acceptance_criteria>
    - The entire file is inside a single `#if DEBUG ... #endif` block
    - `grep -c "#if DEBUG" Islet/Notch/QuickActionsBar/QuickActionsBarManualSpike.swift` returns 1 and `grep -c "#endif"` returns 1
    - Each of the 8 catalog actions has exactly one corresponding static spike function
  </acceptance_criteria>
  <verify>
    <automated>xcodegen generate && xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build 2>&1 | tail -30 && xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Release build 2>&1 | tail -10</automated>
  </verify>
  <done>File compiles in Debug; Release build is unaffected (confirms the #if DEBUG gate actually excludes it, closing T-65-14 below).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All 8 Quick Actions catalog mechanisms (mic mute reuse, display sleep, dark/light mode toggle,
    screen lock, DND/Focus best-effort with visible failure state, keep-awake/caffeinate, empty
    Trash, launch app/URL), the 8-slot Settings configuration UI, and the switcher-tab bar itself
    are fully wired end-to-end.
  </what-built>
  <how-to-verify>
    1. Build and run Islet (Debug). Open Settings → Produktivität → Quick Actions, tap the options
       chevron, and assign all 8 slots (fill Slot 1 with Mute Microphone, Slot 2 Sleep Display,
       Slot 3 Dark/Light Mode, Slot 4 Lock Screen, Slot 5 Do Not Disturb, Slot 6 Keep Awake, Slot 7
       Empty Trash, Slot 8 Open App/URL — pick any installed app via "Choose App…"). Enable the
       Quick Actions toggle.
    2. In Settings → Switcher (Icon Placement), assign any one slot to "Quick Actions". Close
       Settings.
    3. Hover/click the island to expand it, tap the switcher icon you just assigned — confirm the
       bar shows exactly 8 tiles in a 2×4 grid, no empty placeholders.
    4. Tap each tile once, in order, confirming: (a) EVERY tap shows the same brief scale+flash
       pulse regardless of action; (b) Mute Microphone actually mutes the system mic (check
       System Settings → Sound → Input, or another app's mic level) and the tile's glyph swaps to
       the muted icon; (c) Sleep Display puts the display to sleep; (d) Dark/Light Mode actually
       flips System Settings → Appearance; (e) Lock Screen actually locks the screen — THIS IS THE
       ONE UNCONFIRMED-ON-CURRENT-MACOS CLAIM (RESEARCH.md A2) — if it does NOT lock the screen,
       stop and report this back rather than treating the phase as done; (f) Do Not Disturb — test
       WITHOUT a "Islet Toggle Focus" Shortcut configured first: confirm the tile shows the
       failure flash (red exclamation icon, ~1.2s) and Focus does NOT actually turn on; then
       create a Shortcut named exactly "Islet Toggle Focus" containing a single "Set Focus"
       action, tap the tile again, and confirm Focus DOES turn on (check Control Center) and the
       tile shows the green confirmed-on state; (g) Keep Awake actually prevents display sleep
       (verify via `pmset -g assertions` in Terminal showing an assertion held by Islet, or simply
       wait past the normal sleep timeout) and the tile shows the active/green state; (h) Empty
       Trash actually empties the Trash with NO confirmation dialog appearing; (i) Open App/URL
       actually opens the configured app.
    5. If a Meeting-HUD call is reachable to test (Zoom/Teams), confirm toggling mic mute from the
       Quick Actions bar is reflected live on the Meeting-HUD's own mute icon, and vice versa.
    6. Turn the Quick Actions Settings toggle OFF while the switcher slot is still assigned to
       Quick Actions — confirm tapping that switcher slot now shows Home instead of the bar.
  </how-to-verify>
  <resume-signal>Type "approved" once all 8 actions are confirmed working (or explicitly document which ones did not, e.g. "Screen Lock failed on macOS X.Y" — this is a legitimate phase-closing finding, not a blocker to report back for re-planning), or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Debug-only diagnostic code → shipped Release binary | The manual spike must never reach a distributed build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-14 | Tampering (unintended code reaching production) | `QuickActionsBarManualSpike.swift` | mitigate | Entire file gated behind `#if DEBUG`; Task 1's acceptance criteria confirms both a Debug build (compiles) and a Release build (compiles and excludes it) succeed |
</threat_model>

<verification>
Full IsletTests suite green: `xcodebuild test -project Islet.xcodeproj -scheme Islet`.
On-device checkpoint (Task 2) approved by the user, covering all 8 actions.
</verification>

<success_criteria>
- All 8 Quick Actions confirmed working on real hardware (or a specific, documented exception recorded for Screen Lock/DND if either doesn't work as expected on this macOS version)
- Mic-mute state parity between Quick Actions bar and Meeting-HUD confirmed
- Quick Actions Settings toggle OFF correctly hides the bar even with a stale switcher-slot assignment
- QACTION-01, QACTION-02, QACTION-03 all marked Complete in REQUIREMENTS.md after this checkpoint
</success_criteria>

<output>
Create `.planning/phases/65-quick-actions-bar/65-08-SUMMARY.md` when done
</output>
</output>
