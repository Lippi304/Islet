---
phase: 00-foundations-notarization-dry-run
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - docs/GATEKEEPER-DEMO.md
autonomous: false
requirements: [APP-04]
must_haves:
  truths:
    - "Running scripts/release.sh against the finished app produces a real dist/Islet.dmg (ad-hoc signed, notarize/staple skipped)"
    - "An ad-hoc-signed, quarantined build is shown to be REJECTED by Gatekeeper locally (spctl --assess returns rejected)"
    - "The contrast is documented: what notarization + stapling will change on a clean Mac (the deferred Phase-6 carry-over)"
  artifacts:
    - path: "dist/Islet.dmg"
      provides: "The Phase-0 distributable artifact (ad-hoc, not notarized) — git-ignored, produced at runtime"
      note: "git-ignored output; existence verified at runtime, not committed"
    - path: "docs/GATEKEEPER-DEMO.md"
      provides: "Recorded commands + actual spctl/xattr output proving the local Gatekeeper block, plus what notarization changes"
      contains: "spctl --assess"
  key_links:
    - from: "scripts/release.sh"
      to: "dist/Islet.dmg"
      via: "hdiutil DMG build run end-to-end"
      pattern: "Islet\\.dmg"
    - from: "com.apple.quarantine attribute"
      to: "spctl --assess verdict"
      via: "local Gatekeeper assessment of the un-notarized build"
      pattern: "spctl --assess"
---

<objective>
Prove the release pipeline and Gatekeeper behavior END-TO-END on the finished hello-world app, without an Apple Developer account or a second Mac. (1) Run `scripts/release.sh` to actually produce `dist/Islet.dmg` (ad-hoc signed; notarize/staple cleanly skipped). (2) Demonstrate LOCALLY that the un-notarized, quarantined build is rejected by Gatekeeper using `xattr` + `spctl --assess`. (3) Record the commands and actual output in `docs/GATEKEEPER-DEMO.md`, documenting what real notarization will change. (Completes APP-04's Phase-0 portion; the real notarized clean-Mac open is the documented Phase-6 carry-over per D-01/D-02/D-04.)

Purpose: D-02 requires the artifact built and the Gatekeeper block demonstrated locally NOW (D-04 — no second Mac). This closes the loop: the script runs, the DMG exists, and the user can SEE that an un-notarized build is blocked and understand exactly what notarization fixes.
Output: a produced `dist/Islet.dmg` (git-ignored, runtime artifact) and `docs/GATEKEEPER-DEMO.md` capturing the demonstration.

This is the LAST plan in the phase — it depends on the finished app (Plan 02) and the script (Plan 03).

CRITICAL constraints (from RESEARCH + CONTEXT):
- Do NOT run real notarization (no Developer ID — D-01). The script's notarize/staple steps MUST skip; verify that skip message appears.
- No second Mac (D-04): demonstrate the BLOCK on THIS Mac via `com.apple.quarantine` + `spctl --assess` (read-only verdict; `spctl --master-disable` is removed and not used).
- Expected verdict for the ad-hoc/un-notarized build: `rejected` (source like "no usable signature" / "Unnotarized Developer ID" / "unidentified developer"). Capture the ACTUAL wording from this machine (RESEARCH flags exact strings as MEDIUM confidence — confirm on the real artifact).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-foundations-notarization-dry-run/00-CONTEXT.md
@.planning/phases/00-foundations-notarization-dry-run/00-RESEARCH.md
@scripts/release.sh
@docs/RELEASE.md

<interfaces>
<!-- From Plan 03: the script produces these paths. From Plan 01/02: the Islet scheme builds the finished agent app. -->
scripts/release.sh  → produces  dist/Islet.dmg  and  build/export/Islet.app
Expected skip message (placeholders unfilled): "SKIPPING notarize + staple — placeholders not filled (Phase 6 step)."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the release script end-to-end and confirm it produces an ad-hoc-signed dist/Islet.dmg with notarize/staple skipped</name>
  <read_first>
    - scripts/release.sh (the pipeline being run — confirm the ad-hoc + skip branches before running)
    - .planning/phases/00-foundations-notarization-dry-run/00-RESEARCH.md (§ "Pattern 4", § "Code Examples" hdiutil/ad-hoc, § "Pitfall 4")
    - .planning/phases/00-foundations-notarization-dry-run/00-CONTEXT.md (D-01 deferred, D-02 done-definition, D-03 ad-hoc, D-05 dmg)
  </read_first>
  <files>(no source files modified — this task RUNS the script and verifies its output)</files>
  <action>
Run the release script from the repo root and verify it completes the Phase-0 path:
```
bash scripts/release.sh
```
Expected behavior (the placeholders DEVELOPER_ID / NOTARY_PROFILE are still at their `__...__` defaults):
1. It archives a Release build (`xcodebuild ... archive`).
2. It exports `build/export/Islet.app` (via ditto from the archive).
3. It AD-HOC signs the app (`codesign --force --deep --sign -`) and prints the "No Developer ID set: AD-HOC signing" message.
4. It builds `dist/Islet.dmg` via `hdiutil create`.
5. It SKIPS notarize + staple and prints "SKIPPING notarize + staple — placeholders not filled (Phase 6 step)." then exits 0.

Then verify the artifact and its ad-hoc signature:
```
test -f dist/Islet.dmg && echo "DMG exists"
codesign --verify --verbose build/export/Islet.app   # ad-hoc signature valid
codesign -dvv build/export/Islet.app 2>&1 | grep -i "Signature=adhoc"   # confirm ad-hoc
```
If `xcodebuild archive` fails because the scheme isn't shared/visible to xcodebuild, mark the Islet scheme as "Shared" in Xcode (Product → Scheme → Manage Schemes → check "Shared") and re-run. Document any such fix in the SUMMARY.

Do NOT fill in the placeholders. Do NOT attempt real notarization (no account — D-01).
  </action>
  <verify>
    <automated>bash scripts/release.sh 2>&1 | tee /tmp/islet_release.log | grep -q "SKIPPING notarize" && test -f dist/Islet.dmg && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `dist/Islet.dmg` exists after running the script (`test -f dist/Islet.dmg`)
    - The script output contains "SKIPPING notarize + staple" (notarization correctly deferred)
    - `build/export/Islet.app` exists and `codesign --verify --verbose build/export/Islet.app` exits 0 (valid ad-hoc signature)
    - `codesign -dvv build/export/Islet.app` output indicates an ad-hoc signature (e.g. `Signature=adhoc`)
    - The script did NOT call real `notarytool submit` (no notary network submission occurred)
  </acceptance_criteria>
  <done>Running scripts/release.sh produces a real ad-hoc-signed dist/Islet.dmg and cleanly skips the deferred notarize/staple steps — the Phase-0 release dry-run works end to end.</done>
</task>

<task type="auto">
  <name>Task 2: Demonstrate the local Gatekeeper block (quarantine + spctl) and record it in docs/GATEKEEPER-DEMO.md</name>
  <read_first>
    - dist/Islet.dmg / build/export/Islet.app (the artifacts produced in Task 1 — assess these)
    - .planning/phases/00-foundations-notarization-dry-run/00-RESEARCH.md (§ "Pattern 5: Local Gatekeeper demonstration", § "State of the Art" spctl notes, § "Metadata" — exact spctl strings are MEDIUM confidence)
    - .planning/phases/00-foundations-notarization-dry-run/00-CONTEXT.md (D-04 no second Mac, D-02(d) Gatekeeper block demonstrated locally)
    - docs/RELEASE.md (cross-reference — RELEASE.md points here for the demo)
  </read_first>
  <files>docs/GATEKEEPER-DEMO.md</files>
  <action>
Run the local Gatekeeper demonstration from RESEARCH Pattern 5 against the artifacts from Task 1, capture the ACTUAL output, and write it up in `docs/GATEKEEPER-DEMO.md`.

Run these commands and capture their real output (the exact verdict strings on this macOS 26 machine — RESEARCH flags wording as MEDIUM confidence, so record what actually prints):
```
# 1. Simulate "downloaded from the internet" by adding the quarantine attribute to the DMG:
xattr -w com.apple.quarantine \
  "0081;$(printf '%x' $(date +%s));Islet;00000000-0000-0000-0000-000000000000" \
  dist/Islet.dmg

# 2. Confirm the attribute is present:
xattr -p com.apple.quarantine dist/Islet.dmg

# 3. Ask Gatekeeper for its verdict on the DMG (install assessment):
spctl --assess --type install -vvv dist/Islet.dmg ; echo "exit=$?"

# 4. And on the .app (execute assessment):
spctl --assess --type execute -vvv build/export/Islet.app ; echo "exit=$?"
```
Expected: both `spctl --assess` calls REJECT the ad-hoc / un-notarized build (non-zero exit, message like "rejected", "no usable signature", "Unnotarized Developer ID", or "unidentified developer"). Capture the literal output.

Then create `docs/GATEKEEPER-DEMO.md` containing:
1. **Why local** — no second Mac (D-04); we demonstrate the block on this machine with quarantine + `spctl --assess` (read-only verdict).
2. **The commands run** (the four above) — copy them in so the user can re-run them.
3. **The ACTUAL captured output** — paste the real `xattr -p` result and the real `spctl --assess` verdict + exit codes from THIS machine (do not paraphrase — use what actually printed).
4. **Interpretation** — explain that `rejected` is EXPECTED and CORRECT for an ad-hoc, un-notarized build: an ad-hoc signature is not a Gatekeeper-acceptable signature.
5. **What notarization changes (deferred — Phase 6)** — after a real Developer-ID sign + `notarytool` + `stapler staple`, `spctl --assess` returns `accepted, source=Notarized Developer ID`, and a quarantined download opens with NO Gatekeeper warning, even offline (the stapled ticket). This is the Phase-0 success-criterion #3 carry-over to Phase 6 (D-02).
6. **Beginner caveat** — Gatekeeper's quarantine check fires on first GUI launch; double-clicking the quarantined `.app`/`.dmg` shows the block dialog, while `spctl --assess` gives the headless verdict.

Keep it factual and skimmable.
  </action>
  <verify>
    <automated>test -f docs/GATEKEEPER-DEMO.md && grep -q "spctl --assess" docs/GATEKEEPER-DEMO.md && grep -qi "quarantine" docs/GATEKEEPER-DEMO.md && grep -qi "rejected\|reject" docs/GATEKEEPER-DEMO.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/GATEKEEPER-DEMO.md` exists
    - Contains the four demo commands (`xattr -w com.apple.quarantine`, `xattr -p`, two `spctl --assess` lines)
    - Contains the ACTUAL captured `spctl --assess` verdict from this machine (shows a rejection for the un-notarized build)
    - Contains an explanation of what real notarization + stapling will change (the Phase-6 carry-over)
    - References D-04 (no second Mac → local demo)
  </acceptance_criteria>
  <done>The local Gatekeeper block is demonstrated against the real ad-hoc DMG/app and recorded with actual command output in docs/GATEKEEPER-DEMO.md, including what notarization will change at Phase 6.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the full Phase-0 release dry-run + Gatekeeper demonstration</name>
  <what-built>An end-to-end Phase-0 release dry run: scripts/release.sh produces an ad-hoc-signed dist/Islet.dmg (notarize/staple skipped), and a local Gatekeeper demonstration (docs/GATEKEEPER-DEMO.md) shows the un-notarized build is blocked, with the Phase-6 notarization carry-over documented.</what-built>
  <how-to-verify>
1. Confirm `dist/Islet.dmg` exists (`ls -lh dist/Islet.dmg`).
2. Open `docs/GATEKEEPER-DEMO.md` — confirm it shows the actual `spctl --assess` REJECTION output captured from this Mac and explains what notarization will change.
3. (Optional, the most faithful local repro) Double-click the quarantined `dist/Islet.dmg` (or the .app inside) in Finder — you should get the macOS "unidentified developer / cannot be opened" Gatekeeper dialog. This is the EXPECTED block for an un-notarized build.
4. Confirm the script did NOT attempt real notarization (no Apple account was used — D-01) and printed the "SKIPPING notarize + staple" message.
5. Sanity-check that this matches the Phase-0 definition of done (D-02): (a) the agent runs (verified in Plans 01/02), (b) a .dmg artifact is built, (c) the sign→notarize→staple flow is a re-runnable script with placeholders, (d) Gatekeeper block demonstrated locally. The REAL notarization + clean-second-Mac open is intentionally carried to Phase 6.
  </how-to-verify>
  <resume-signal>Type "approved" if the DMG built, the script skipped notarization, and the Gatekeeper block is demonstrated + documented — or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → user's Mac (quarantine) | A downloaded artifact carries `com.apple.quarantine`; Gatekeeper evaluates it on first launch. We simulate this boundary locally with `xattr`. |
| build artifact → Gatekeeper verdict | Gatekeeper decides accept/reject based on the signature/notarization state of the artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-09 | Tampering / Spoofing | An un-notarized build reaching a user could be tampered or impersonated | mitigate (demonstrated, real-run deferred) | This plan PROVES Gatekeeper rejects the ad-hoc/un-notarized artifact locally (`spctl --assess` → rejected), confirming the protective control works; the full Developer-ID notarize+staple that makes a legitimate build ACCEPTED is scripted (Plan 03) and runs at Phase 6 (D-01). No high-severity gap: an un-notarized build is correctly blocked, not silently trusted. |
| T-00-10 | Repudiation | Phase-0 DMG mistaken for a shippable release | mitigate | The artifact is ad-hoc signed and documented as NOT notarized (RELEASE.md + GATEKEEPER-DEMO.md); the script prints a loud skip message; `dist/` is git-ignored so it is not committed/distributed. Low severity. |
</threat_model>

<verification>
- `bash scripts/release.sh` produces `dist/Islet.dmg` and prints "SKIPPING notarize + staple"; no real notarization runs.
- `codesign --verify` passes on the ad-hoc-signed app.
- `spctl --assess` against the quarantined artifact returns a rejection (captured in docs/GATEKEEPER-DEMO.md).
- Manual (Task 3 checkpoint): artifact exists, demo documented, matches D-02 done-definition.
- The real notarized clean-Mac open is the documented Phase-6 carry-over — NOT a Phase-0 gate (per CONTEXT scope note + success criterion #3 deferral).
</verification>

<success_criteria>
- A `.dmg` artifact is built by the repeatable script (D-02(b), APP-04).
- Gatekeeper block-behavior is demonstrated locally and documented (D-02(d), D-04).
- The deferred real notarization is clearly captured as a Phase-6 carry-over, not left ambiguous.
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundations-notarization-dry-run/00-04-SUMMARY.md` documenting: the actual script run output (ad-hoc sign + skip), the produced DMG path/size, the EXACT `spctl --assess` verdict strings observed on this macOS 26 machine (so Phase 6 knows the baseline), and an explicit note that the real notarization + clean-Mac open carries to Phase 6.
</output>
