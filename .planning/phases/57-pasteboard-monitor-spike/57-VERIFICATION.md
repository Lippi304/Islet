---
phase: 57-pasteboard-monitor-spike
verified: 2026-07-22T21:59:16Z
status: passed
score: 9/9 must-haves verified
overrides_applied: 0
---

# Phase 57: Pasteboard Monitor — Spike Verification Report

**Phase Goal:** The one genuinely new, only-verifiable-on-real-hardware subsystem — live `NSPasteboard`
polling, sensitive-content filtering, and the self-capture guard — is proven in isolation before it's
wired into the menu, so it can't destabilize the already-proven pure model/persistence work. Mirrors
this project's own Phase 22/38/47 spike-first precedent.
**Verified:** 2026-07-22T21:59:16Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `ClipboardMonitor` detects a genuine copy via a `changeCount` diff within ~500ms and correctly classifies text/image (ROADMAP SC#1) | VERIFIED | `poll()` in `Islet/Clipboard/ClipboardMonitor.swift:48-65` reads `pb.changeCount != lastChangeCount` gate first, 0.5s timer (`repeating: 0.5`, line 42); on-device checkpoint (57-02-PLAN.md Task 2, steps 3-4) documented as approved in `57-02-SUMMARY.md` and independently corroborated in `.planning/STATE.md:232-233` with idiosyncratic detail (`kind=image(5246006 bytes)`, recursive-copy stress test) |
| 2 | Concealed/Transient/AutoGenerated-type copies are never captured (ROADMAP SC#2 / PRIV-01) | VERIFIED | `isConcealedOrTransient(_:)` (`ClipboardMonitor.swift:98-103`) checked before any content read; 4 unit tests cover all 3 type strings + false-negative case (`IsletTests/ClipboardMonitorTests.swift:17-47`); on-device `debugSpikeWriteConcealedTestItem` checkpoint approved (STATE.md:233) |
| 3 | Self-capture guard (`restoreMarkerType`) prevents re-ingesting a simulated restore write (ROADMAP SC#3) | VERIFIED | `isSelfCaptureMarker(_:markerType:)` (`ClipboardMonitor.swift:107-109`), 2 unit tests (`ClipboardMonitorTests.swift:49-65`); on-device `debugSpikeSimulateSelfCaptureWrite` checkpoint approved (STATE.md:233) |
| 4 | Pasteboard-access privacy prompt handled gracefully with a one-time in-app explanation (ROADMAP SC#4) | VERIFIED | `needsAccessExplanation` (`ClipboardMonitor.swift:71-76`) gates on real `NSPasteboard.AccessBehavior.alwaysAllow` (confirmed against actual SDK header, not the speculative `.always`); `debugSpikeCheckPasteboardAccessBehavior` session-gate flag proven correct on 2nd click on-device (STATE.md:233) |
| 5 | `poll()` only reads pasteboard content when `changeCount` has actually changed — no unconditional content read every tick | VERIFIED | `guard pb.changeCount != lastChangeCount else { return }` is the literal first line of real logic in `poll()` (`ClipboardMonitor.swift:52`) |
| 6 | `lastChangeCount` seeded from `NSPasteboard.general.changeCount` at init, not 0 | VERIFIED | `ClipboardMonitor.swift:33` — `self.lastChangeCount = NSPasteboard.general.changeCount` inside `init` |
| 7 | `classifyPasteboardContent` prefers `.text` over `.image` when both are present (text-priority rule) | VERIFIED | `ClipboardMonitor.swift:113-120`; unit test `testClassifyPasteboardContentPrefersTextWhenBothPresent` asserts `.text("hello")` when both string+image data passed |
| 8 | `ClipboardMonitor.swift` contains zero references to `IslandResolver`/`TransientQueue`/`NotchWindowController` (isolation boundary) | VERIFIED | `grep -c "IslandResolver\|TransientQueue\|NotchWindowController" Islet/Clipboard/ClipboardMonitor.swift` → `0` |
| 9 | All 4 new spike menu actions + their backing state (`debugClipboardMonitor`, `debugHasShownPasteboardAccessExplanation`) are DEBUG-only, absent from Release builds | VERIFIED | Confirmed both properties (lines 36-37) and all 4 `@objc` handlers (lines 313-358) sit inside the single `#if DEBUG ... #endif` block spanning `Islet/AppDelegate.swift:226-359` |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `Islet/Clipboard/ClipboardMonitor.swift` | `@MainActor` timer-poll class + 3 pure helper functions + `needsAccessExplanation` | VERIFIED | 121 lines; all required members present (`restoreMarkerType`, `init`, `start`, `poll`, `needsAccessExplanation`, `stop`, `deinit`, 3 top-level pure functions) |
| `IsletTests/ClipboardMonitorTests.swift` | Unit coverage for 3 pure helpers via fresh (never `.general`) pasteboard fixtures | VERIFIED | 83 lines, 9 `func test...` methods, `freshPasteboard()` helper, 0 references to `NSPasteboard.general` |
| `IsletTests/ClipboardMonitorManualSpike.swift` | Manual on-device spike scaffold mirroring `AudioOutputMonitorManualSpike.swift` | VERIFIED | 29 lines, `MANUAL SPIKE` header, constructs real `ClipboardMonitor`, 45s `RunLoop` window, always-green `XCTAssertTrue(true, ...)` |
| `Islet/AppDelegate.swift` | 4 new DEBUG-only spike menu items + `@objc` handlers wired to `ClipboardMonitor` | VERIFIED | 4 menu items (lines 249-256), 4 handlers (lines 313-358), all inside existing `#if DEBUG` block |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `ClipboardMonitor.swift` | `poll()`'s changeCount gate | `guard pb.changeCount != lastChangeCount else { return }` as first line of `poll()` | WIRED | `ClipboardMonitor.swift:52`, confirmed literal match |
| `ClipboardMonitor.swift` | `isConcealedOrTransient`/`isSelfCaptureMarker` guard | `poll()`'s combined guard | WIRED | `ClipboardMonitor.swift:56-57` |
| `AppDelegate.swift` | `ClipboardMonitor.swift` | `debugSpikeStartClipboardMonitor()` constructing `ClipboardMonitor(onChange:)` + `.start()` | WIRED | `AppDelegate.swift:313-322` |
| `AppDelegate.swift` | `ClipboardMonitor.restoreMarkerType` | `debugSpikeSimulateSelfCaptureWrite()` tagging a test item with the marker before writing to `NSPasteboard.general` | WIRED | `AppDelegate.swift:334-341` |

### Build Verification (independently re-run, not trusted from SUMMARY)

| Command | Result | Status |
|---------|--------|--------|
| `xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug` | `** BUILD SUCCEEDED **` | PASS |
| `xcodebuild build-for-testing -scheme Islet -destination 'platform=macOS' -configuration Debug` | `** TEST BUILD SUCCEEDED **` | PASS |
| `git log --oneline --all \| grep <5 claimed hashes>` | All 5 commits found (`001f0e0`, `133dbb5`, `6b25068`, `151be39`, `89035cb`) | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|--------------|------------|-------------|--------|----------|
| PRIV-01 | 57-01, 57-02 | Copies marked sensitive/concealed (`org.nspasteboard.ConcealedType`/`TransientType`) are never captured | SATISFIED | `isConcealedOrTransient` + unit tests + on-device checkpoint (Truth #2); REQUIREMENTS.md already marks PRIV-01 `[x]` Complete, mapped to Phase 57, no orphaned Phase-57 requirement IDs found |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | No TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER markers found in any of the 4 phase-57 files | — | Clean |
| `Islet/Clipboard/ClipboardMonitor.swift` (via 57-REVIEW.md WR-01) | 80-89 | `stop()`'s documented "owner calls stop() on teardown" contract is never actually invoked by `AppDelegate` (no paired "Stop" action, no `applicationWillTerminate` hook) | INFO (non-blocking) | Latent only — DEBUG-only, process death cleans up the GCD timer; explicitly flagged by code review as a Phase 58 follow-up concern, not a Phase 57 goal blocker |
| `Islet/Clipboard/ClipboardMonitor.swift` (via 57-REVIEW.md WR-02) | 20-23 | `nonisolated(unsafe) timer/running` — dormant data race once `stop()` is ever wired to a background-thread teardown path | INFO (non-blocking) | Mirrors existing `FocusModeMonitor` pattern, not new; currently unreachable since `stop()` is never called (see WR-01) |
| `Islet/AppDelegate.swift` (via 57-REVIEW.md IN-02) | 325-341 | DEBUG spike hooks destructively overwrite the real system clipboard with placeholder secrets, no restore | INFO (non-blocking) | Acceptable for a `#if DEBUG`-gated manual dev tool per the plan's own explicit threat-model disposition (T-57-06: accept) |

None of these rise to blocker level — they were already surfaced by this phase's own code review (`57-REVIEW.md`) as non-blocking, Phase-58-relevant follow-ups, and do not affect any of the 9 observable truths above.

### Human Verification Required

None outstanding. The phase's one `checkpoint:human-verify` gate (57-02-PLAN.md Task 2, all 9 on-device steps covering ROADMAP SC#1-4) was already reached and approved by the actual user during phase execution — corroborated independently in `.planning/STATE.md:232-233` with specific, non-generic hardware detail (exact byte count for an image capture, this-Mac's real `accessBehavior` value, a recursive-copy stress test) that would be implausible to fabricate. This matches the project's own established precedent (Phase 45-02, 48-03, 52-04, 53-02, 56-02) where an approved on-device UAT closes out the phase's success criteria without requiring a separate re-ask at verify-work time.

### Gaps Summary

No gaps. All 4 ROADMAP Phase 57 success criteria and all 5 plan-level `must_haves` truths (57-01 + 57-02) are verified at the code level (build succeeds, unit tests cover every pure-function behavior specified, isolation boundary confirmed via grep, DEBUG-only wiring confirmed) and the on-device checkpoint proving all 4 success criteria on real hardware was already run and approved during execution, independently corroborated in STATE.md. PRIV-01 is satisfied. The phase achieves its stated goal: the pasteboard-monitoring subsystem is proven in isolation via DEBUG-only spike hooks, with zero coupling to `IslandResolver`/`TransientQueue`/`NotchWindowController`, before Phase 58 wires it into the real menu.

---

_Verified: 2026-07-22T21:59:16Z_
_Verifier: Claude (gsd-verifier)_
