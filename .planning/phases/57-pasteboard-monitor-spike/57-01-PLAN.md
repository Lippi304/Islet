---
phase: 57-pasteboard-monitor-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Clipboard/ClipboardMonitor.swift
  - IsletTests/ClipboardMonitorTests.swift
  - IsletTests/ClipboardMonitorManualSpike.swift
autonomous: true
requirements: [PRIV-01]

must_haves:
  truths:
    - "ClipboardMonitor.poll() only reads pasteboard content (.string/.data(forType:)) when NSPasteboard.general.changeCount has actually changed since the last tick — no unconditional content read every tick (Pitfall 2, cheap-check-first discipline)"
    - "isConcealedOrTransient(_:) returns true when the pasteboard's types include org.nspasteboard.ConcealedType, org.nspasteboard.TransientType, or org.nspasteboard.AutoGeneratedType, proven by unit test against a fresh (never .general) NSPasteboard fixture (PRIV-01)"
    - "isSelfCaptureMarker(_:markerType:) returns true only when the pasteboard's types include the exact marker type passed in, proven by unit test (Pitfall 1 self-capture guard mechanism)"
    - "classifyPasteboardContent(string:imageData:) returns .text when a non-empty string is present regardless of whether image data is also present (text-priority rule), returns .image when only image data is present, and returns nil when neither is present"
    - "ClipboardMonitor's lastChangeCount is seeded from NSPasteboard.general.changeCount at init time, not from 0 — so content already on the pasteboard before start() is never captured as a false-positive first tick"
    - "Islet/Clipboard/ClipboardMonitor.swift contains zero references to IslandResolver, TransientQueue, or NotchWindowController by name (mirrors Phase 55/56's SC-4-style independence boundary — this is a pure isolation-seam class, owned by AppDelegate in Plan 57-02, never by NotchWindowController)"
  artifacts:
    - path: "Islet/Clipboard/ClipboardMonitor.swift"
      provides: "ClipboardMonitor (@MainActor timer-poll class, init/start/stop) + 3 pure top-level helper functions (isConcealedOrTransient, isSelfCaptureMarker, classifyPasteboardContent) + the restoreMarkerType constant + the needsAccessExplanation accessBehavior check"
      exports: ["ClipboardMonitor", "isConcealedOrTransient", "isSelfCaptureMarker", "classifyPasteboardContent"]
      min_lines: 45
    - path: "IsletTests/ClipboardMonitorTests.swift"
      provides: "Unit coverage for the 3 pure helper functions using fresh, uniquely-named NSPasteboard fixtures (never .general)"
    - path: "IsletTests/ClipboardMonitorManualSpike.swift"
      provides: "Manual on-device spike scaffold (mirrors AudioOutputMonitorManualSpike.swift) — run via Xcode Cmd-U only, feeds Plan 57-02's on-device checkpoint"
  key_links:
    - from: "Islet/Clipboard/ClipboardMonitor.swift"
      to: "poll()'s changeCount gate"
      via: "guard pb.changeCount != lastChangeCount else { return } as the very first line of poll()"
      pattern: "changeCount != lastChangeCount"
    - from: "Islet/Clipboard/ClipboardMonitor.swift"
      to: "isConcealedOrTransient / isSelfCaptureMarker guard"
      via: "poll()'s guard !isConcealedOrTransient(types), !isSelfCaptureMarker(types, markerType: ...) else { return }"
      pattern: "isConcealedOrTransient\\(types\\)"
---

<objective>
Build `ClipboardMonitor` — the `@MainActor`, timer-polled class that reads `NSPasteboard.general`'s
`changeCount`, filters `org.nspasteboard.ConcealedType`/`TransientType`/`AutoGeneratedType` copies,
classifies text vs. image content, and guards against re-ingesting its own future click-to-restore
write (Pitfall 1) via a private marker pasteboard type — plus the `NSPasteboard.accessBehavior`
check `ClipboardMonitor.needsAccessExplanation` that Plan 57-02's D-07 spike hook will drive.

Purpose: This is the one genuinely new, only-fully-verifiable-on-real-hardware subsystem in v1.9
(ROADMAP Phase 57 goal) — isolate it as its own class first, with its filtering/classification/
self-capture logic split into pure, directly unit-testable top-level functions (mirroring
`DragDropSupport.swift`'s `isGenuineFileDrag`/`fileURLs(from:)` convention), before Plan 57-02 wires
DEBUG spike hooks and runs the on-device checkpoint against it.

Output: `Islet/Clipboard/ClipboardMonitor.swift` (the class + 3 pure helper functions),
`IsletTests/ClipboardMonitorTests.swift` (automated unit coverage for the pure functions),
`IsletTests/ClipboardMonitorManualSpike.swift` (a manual, Cmd-U-only on-device spike scaffold —
mirrors `AudioOutputMonitorManualSpike.swift` exactly in shape). No `AppDelegate` wiring, no DEBUG
menu items, no on-device checkpoint — that is Plan 57-02, which depends on this plan's output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-pasteboard-monitor-spike/57-CONTEXT.md
@.planning/phases/57-pasteboard-monitor-spike/57-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md

# Direct analogs to mirror exactly (read before writing any code)
@Islet/Notch/FocusModeMonitor.swift
@Islet/Notch/DragDropSupport.swift
@IsletTests/DragDropSupportTests.swift
@IsletTests/AudioOutputMonitorManualSpike.swift
@Islet/Clipboard/ClipboardItem.swift
</context>

<interfaces>
<!-- Extracted from the codebase's own current files and PATTERNS.md's already-completed pattern
analysis. Executor should use these shapes directly — no further codebase exploration needed. -->

Full file to mirror for the class shape (`Islet/Notch/FocusModeMonitor.swift:29-97`, timer/
idempotent-start/nonisolated-stop shape — swap Focus's 2.5s poll for this phase's 0.5s, and swap
the single `INFocusStatusCenter` read for a `changeCount` diff + filter + classify):
```swift
@MainActor
final class FocusModeMonitor {
    private nonisolated(unsafe) var timer: DispatchSourceTimer?
    private nonisolated(unsafe) var running = false
    private let onChange: (Bool) -> Void

    init(onChange: @escaping (Bool) -> Void) { self.onChange = onChange }

    func start() {
        guard !running else { return }
        running = true
        let t = DispatchSource.makeTimerSource(queue: .main)
        t.schedule(deadline: .now(), repeating: 2.5, leeway: .milliseconds(500))
        t.setEventHandler { [weak self] in self?.poll() }
        t.resume()
        timer = t
    }

    nonisolated func stop() {
        timer?.cancel()
        timer = nil
        running = false
    }

    deinit { /* owner (AppDelegate) calls stop() explicitly on teardown */ }
}
```

`DragDropSupport.swift`'s pure-top-level-function convention (`isGenuineFileDrag`, full signature —
mirror this shape exactly for the 3 new pure functions this plan adds):
```swift
func isGenuineFileDrag(currentChangeCount: Int, gestureBaselineChangeCount: Int, urls: [URL]) -> Bool {
    currentChangeCount != gestureBaselineChangeCount && !urls.isEmpty
}
```

`DragDropSupportTests.swift`'s fresh-pasteboard test-fixture convention (full shape — NEVER use
`NSPasteboard.general` in a test, it would pollute the real system clipboard):
```swift
private func freshPasteboard() -> NSPasteboard {
    NSPasteboard(name: NSPasteboard.Name("DragDropSupportTests-\(UUID())"))
}

func testFileURLsReturnsEmptyForNonFilePayload() {
    let pasteboard = freshPasteboard()
    pasteboard.clearContents()
    pasteboard.setString("hello", forType: .string)
    XCTAssertTrue(fileURLs(from: pasteboard).isEmpty)
}
```

`AudioOutputMonitorManualSpike.swift` (full file, 42 lines — the exact shape
`ClipboardMonitorManualSpike.swift` must mirror):
```swift
import XCTest
@testable import Islet

final class AudioOutputMonitorManualSpike: XCTestCase {
    @MainActor
    func testManualDeviceEnumerationAndSwitch() {
        var lastDevices: [AudioOutputDevice] = []
        var monitor: AudioOutputMonitor!
        monitor = AudioOutputMonitor(onDevicesChanged: { devices in
            lastDevices = devices
            devices.forEach { device in print("[AudioOutputSpike] ...") }
        })
        monitor.start()
        RunLoop.current.run(until: Date().addingTimeInterval(15))
        // ... window for developer manual interaction ...
        monitor.stop()
        XCTAssertTrue(true, "manual spike — see console output ... for the real pass/fail criteria")
    }
}
```

`ClipboardItem`'s exact current shape (`Islet/Clipboard/ClipboardItem.swift`, full file, unchanged
by this plan — `ClipboardMonitor` constructs values of this type):
```swift
import Foundation

struct ClipboardItem: Equatable, Codable {
    let id: UUID
    var kind: Kind
    var timestamp: Date

    enum Kind: Equatable, Codable {
        case text(String)
        case image(Data)
    }
}
```

ARCHITECTURE.md's own `poll()` sketch (Pattern 1) — the exact concealed-type raw-string check to
mirror (note: this plan additionally includes `AutoGeneratedType`, per STACK.md's explicit 3-type
convention list, one line beyond ARCHITECTURE.md's 2-type sketch):
```swift
guard !pb.types.map(\.rawValue).contains(where: {
    $0 == "org.nspasteboard.ConcealedType" || $0 == "org.nspasteboard.TransientType"
}) else { return }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ClipboardMonitor — changeCount-gated poll, concealed/transient filter, self-capture guard, classification</name>
  <files>Islet/Clipboard/ClipboardMonitor.swift</files>
  <read_first>
    Islet/Notch/FocusModeMonitor.swift (full file — the @MainActor/timer/idempotent-start/
    nonisolated-stop class shape to mirror exactly, see interfaces section above)
    Islet/Notch/DragDropSupport.swift (the pure-top-level-function convention — isGenuineFileDrag,
    fileURLs(from:) — this task's 3 new pure functions must follow this exact shape: plain value
    types in, plain value/Bool out, no side effects)
    Islet/Clipboard/ClipboardItem.swift (the exact struct/Kind enum this class constructs — id/kind/
    timestamp fields, Kind.text(String)/Kind.image(Data) cases)
    .planning/phases/57-pasteboard-monitor-spike/57-CONTEXT.md (D-07: accessBehavior check is
    Claude's-Discretion-implemented here, one-time-gate/NSAlert placeholder itself is Plan 57-02's
    job; D-08: concealed-type simulation is a Plan-57-02 DEBUG hook, not this plan; self-capture
    guard mechanism = marker pasteboard type per Claude's Discretion, matching Maccy's proven
    pattern)
    .planning/research/PITFALLS.md (Pitfall 1 — self-capture loop, marker-type fix; Pitfall 2 —
    changeCount-gate-first discipline, never read pasteboard content unconditionally; Pitfall 4 —
    the marker check covers the required baseline convention, encryption elsewhere covers the
    residual gap, not this file's job)
    .planning/research/STACK.md (exact org.nspasteboard.ConcealedType/TransientType/
    AutoGeneratedType raw strings; .string/.tiff/.png pasteboard type read order — text checked
    before image, since a single copy can carry both representations simultaneously)
  </read_first>
  <behavior>
    - (Covered by Task 2's unit tests, not inline XCTest assertions in this file) isConcealedOrTransient(_:) true for ConcealedType/TransientType/AutoGeneratedType, false otherwise
    - isSelfCaptureMarker(_:markerType:) true only when the marker type is present in types
    - classifyPasteboardContent(string:imageData:) prefers .text when a non-empty string is present (even alongside image data), returns .image when only image data is present, returns nil when neither is present
  </behavior>
  <action>
    Create Islet/Clipboard/ClipboardMonitor.swift with `import AppKit` (needed for NSPasteboard;
    Foundation is implied). Header comment in FocusModeMonitor.swift's style — cite "Phase 57 /
    PRIV-01", state this is the ONE file touching NSPasteboard.general directly (mirrors every
    other Monitor's "isolate the fragile system surface behind one file" discipline), note there is
    no notification API for pasteboard changes so this one polls (like FocusModeMonitor), and note
    the owner is AppDelegate (Plan 57-02), NOT NotchWindowController — the one deliberate ownership
    deviation ARCHITECTURE.md flags, same class shape, new owner.

    Define `@MainActor final class ClipboardMonitor` with exactly these members, in this order:

    `static let restoreMarkerType = NSPasteboard.PasteboardType("com.islet.clipboardhistory.restored")`
    — the private marker type Phase 58's real click-to-restore write will tag itself with; this
    plan only defines and consumes the constant, it never writes it (Plan 57-02's DEBUG hook
    simulates a write of it for on-device proof).

    `private nonisolated(unsafe) var timer: DispatchSourceTimer?` and `private nonisolated(unsafe)
    var running = false` — identical to FocusModeMonitor's fields, same rationale (owner's
    nonisolated deinit needs to call stop()).

    `private var lastChangeCount: Int` and `private let onChange: (ClipboardItem) -> Void`.

    `init(onChange: @escaping (ClipboardItem) -> Void)`: sets `self.onChange = onChange`, then sets
    `self.lastChangeCount = NSPasteboard.general.changeCount` — seed the baseline from the CURRENT
    count at construction time, NOT 0, so anything already on the pasteboard before start() is never
    captured as a false first-tick "new" copy (ARCHITECTURE.md's Launch Flow note).

    `func start()`: identical idempotent-guard/timer-construction shape to FocusModeMonitor.start()
    (see interfaces above), but schedules with `repeating: 0.5, leeway: .milliseconds(100)` — 500ms
    per Maccy's proven default (PITFALLS.md Pitfall 2, STACK.md), NOT Focus's 2.5s.

    `private func poll()`: reads `let pb = NSPasteboard.general`. FIRST line of real logic is the
    cheap gate: `guard pb.changeCount != lastChangeCount else { return }` — nothing below this line
    runs unless the count actually changed. Immediately update `lastChangeCount = pb.changeCount`.
    Read `let types = pb.types ?? []`. Then `guard !isConcealedOrTransient(types),
    !isSelfCaptureMarker(types, markerType: ClipboardMonitor.restoreMarkerType) else { return }` —
    both checks run only after the changeCount gate already passed. Then read `let string =
    pb.string(forType: .string)` and `let imageData = pb.data(forType: .tiff) ?? pb.data(forType:
    .png)` (check .tiff first, .png fallback — STACK.md's documented type-priority order for image
    reads). Then `guard let kind = classifyPasteboardContent(string: string, imageData: imageData)
    else { return }`. Finally call `onChange(ClipboardItem(id: UUID(), kind: kind, timestamp:
    Date()))`.

    `static var needsAccessExplanation: Bool`: D-07's accessBehavior check. Guard with
    `if #available(macOS 15.4, *)` since the project's deployment target is 15.0 (project.yml), one
    minor version below where this API shipped — inside the availability check, compare
    NSPasteboard.general.accessBehavior against its "always allowed" case. PITFALLS.md flags this
    exact API's Swift symbol names as not fully documented outside Apple's own headers yet — confirm
    the real case name via Xcode's autocomplete/Quick Help on NSPasteboard.AccessBehavior while
    implementing (expected to be `.always`, per Michael Tsai's "always allowed / never allowed /
    ask" description); if the case name differs from `.always`, use the real one — semantics
    (return true only when access is NOT always-allowed) are what matters, not the literal
    spelling. Return false from the #available else-branch (macOS below 15.4 has no such gate,
    nothing to explain).

    `nonisolated func stop()`: identical to FocusModeMonitor.stop() — cancels and nils the timer,
    resets running to false.

    `deinit {}`: empty, with the same "owner calls stop() explicitly, deinit can't be @MainActor in
    Swift 5 mode" comment as FocusModeMonitor.deinit.

    Below the class, define 3 top-level (module-level, NOT nested in the class) pure functions,
    mirroring DragDropSupport.swift's convention exactly:

    `func isConcealedOrTransient(_ types: [NSPasteboard.PasteboardType]) -> Bool`: builds a Set of
    the raw string values, then returns true if that set contains "org.nspasteboard.ConcealedType",
    "org.nspasteboard.TransientType", OR "org.nspasteboard.AutoGeneratedType" — all 3 conventions
    per STACK.md's Core Technologies table, one more than ARCHITECTURE.md's 2-type sketch.

    `func isSelfCaptureMarker(_ types: [NSPasteboard.PasteboardType], markerType:
    NSPasteboard.PasteboardType) -> Bool`: returns `types.contains(markerType)`. Takes markerType as
    a parameter (not reading ClipboardMonitor.restoreMarkerType internally) so it stays a pure,
    directly-testable function with no dependency on the class.

    `func classifyPasteboardContent(string: String?, imageData: Data?) -> ClipboardItem.Kind?`:
    returns `.text(string)` when string is non-nil and non-empty (checked first — text takes
    priority over image when both are present, per STACK.md's documented tie-break rule); otherwise
    returns `.image(imageData)` when imageData is non-nil; otherwise returns nil.

    Run `xcodegen generate` from the repo root so Islet.xcodeproj picks up the new file.
  </action>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "@MainActor" Islet/Clipboard/ClipboardMonitor.swift` matches
    - `grep -q "final class ClipboardMonitor" Islet/Clipboard/ClipboardMonitor.swift` matches
    - `grep -q "static let restoreMarkerType = NSPasteboard.PasteboardType" Islet/Clipboard/ClipboardMonitor.swift` matches
    - `grep -q "com.islet.clipboardhistory.restored" Islet/Clipboard/ClipboardMonitor.swift` matches
    - `grep -q "repeating: 0.5" Islet/Clipboard/ClipboardMonitor.swift` matches (500ms interval, not Focus's 2.5s)
    - `grep -q "pb.changeCount != lastChangeCount" Islet/Clipboard/ClipboardMonitor.swift` matches (cheap gate first)
    - `grep -q "self.lastChangeCount = NSPasteboard.general.changeCount" Islet/Clipboard/ClipboardMonitor.swift` matches (seeded at init, not 0)
    - `grep -q "func isConcealedOrTransient" Islet/Clipboard/ClipboardMonitor.swift` matches
    - `grep -q "func isSelfCaptureMarker" Islet/Clipboard/ClipboardMonitor.swift` matches
    - `grep -q "func classifyPasteboardContent" Islet/Clipboard/ClipboardMonitor.swift` matches
    - `grep -q "static var needsAccessExplanation: Bool" Islet/Clipboard/ClipboardMonitor.swift` matches
    - `grep -c "IslandResolver\|TransientQueue\|NotchWindowController" Islet/Clipboard/ClipboardMonitor.swift` returns 0
    - `xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug` reports BUILD SUCCEEDED
  </acceptance_criteria>
  <done>ClipboardMonitor.swift exists: changeCount-gated 0.5s poll seeded from the real baseline at init, concealed/transient/auto-generated filtering, self-capture marker guard, text-priority classification, and a D-07 accessBehavior check — zero references to IslandResolver/TransientQueue/NotchWindowController; BUILD SUCCEEDED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ClipboardMonitorTests — unit coverage for the 3 pure helper functions</name>
  <files>IsletTests/ClipboardMonitorTests.swift</files>
  <read_first>
    Islet/Clipboard/ClipboardMonitor.swift (Task 1's output — exact signatures of
    isConcealedOrTransient/isSelfCaptureMarker/classifyPasteboardContent)
    IsletTests/DragDropSupportTests.swift (full file — the freshPasteboard() fixture convention:
    NEVER touch NSPasteboard.general in a test; one func test...() per behavior, no setUp/
    tearDown, no shared fixture)
  </read_first>
  <behavior>
    - Test: isConcealedOrTransient returns true for a pasteboard whose types include
      org.nspasteboard.ConcealedType
    - Test: isConcealedOrTransient returns true for a pasteboard whose types include
      org.nspasteboard.TransientType
    - Test: isConcealedOrTransient returns true for a pasteboard whose types include
      org.nspasteboard.AutoGeneratedType
    - Test: isConcealedOrTransient returns false for a pasteboard holding only plain .string
      content
    - Test: isSelfCaptureMarker returns true when types includes the exact marker type passed in
    - Test: isSelfCaptureMarker returns false when types does not include the marker type (even
      if other custom types are present)
    - Test: classifyPasteboardContent(string:imageData:) returns .text when both a non-empty
      string AND image data are passed (text-priority rule)
    - Test: classifyPasteboardContent returns .image when only image data is passed (string is
      nil)
    - Test: classifyPasteboardContent returns nil when both arguments are nil
  </behavior>
  <action>
    Create IsletTests/ClipboardMonitorTests.swift: `import XCTest`, `import AppKit`, `@testable
    import Islet`. Header comment mirroring DragDropSupportTests.swift's opening lines — cite
    "Phase 57 / PRIV-01", state this covers ClipboardMonitor.swift's 3 pure top-level functions,
    one test per behavior, no setUp/tearDown, no mocking framework, and explicitly note (matching
    DragDropSupportTests.swift's own comment) that a fresh uniquely-named NSPasteboard is used
    everywhere a real pasteboard fixture is needed — NEVER NSPasteboard.general, which would
    pollute the real system clipboard.

    `final class ClipboardMonitorTests: XCTestCase` with a private `freshPasteboard() ->
    NSPasteboard` helper identical in shape to DragDropSupportTests.swift's own
    (`NSPasteboard(name: NSPasteboard.Name("ClipboardMonitorTests-\(UUID())"))`).

    Implement the 9 behaviors above as 9 separate func test...() methods. For the concealed/
    transient/auto-generated tests: construct a fresh pasteboard, call clearContents(), then
    setData(Data(), forType:) with the relevant org.nspasteboard.* type string, then call
    isConcealedOrTransient(pasteboard.types ?? []) and assert true/false as appropriate. For the
    self-capture marker tests: define a local marker type constant (either
    NSPasteboard.PasteboardType("com.islet.clipboardhistory.restored") or a direct reference to
    ClipboardMonitor.restoreMarkerType — either is fine since the function takes markerType as a
    parameter), write it (or a DIFFERENT custom type, for the false case) onto a fresh pasteboard,
    then call isSelfCaptureMarker(pasteboard.types ?? [], markerType: markerType). For the
    classification tests: call classifyPasteboardContent(string:imageData:) directly with plain
    literal String?/Data? arguments — no pasteboard involvement needed for these 3 tests since the
    function's inputs are already plain values.

    Run xcodegen generate from the repo root.
  </action>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build-for-testing -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "TEST BUILD SUCCEEDED" && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "final class ClipboardMonitorTests: XCTestCase" IsletTests/ClipboardMonitorTests.swift` matches
    - `grep -q "func freshPasteboard() -> NSPasteboard" IsletTests/ClipboardMonitorTests.swift` matches
    - `grep -c "NSPasteboard.general" IsletTests/ClipboardMonitorTests.swift` returns 0 (never touches the real system clipboard)
    - `grep -q "org.nspasteboard.ConcealedType" IsletTests/ClipboardMonitorTests.swift` matches
    - `grep -q "org.nspasteboard.TransientType" IsletTests/ClipboardMonitorTests.swift` matches
    - `grep -q "org.nspasteboard.AutoGeneratedType" IsletTests/ClipboardMonitorTests.swift` matches
    - `grep -c "func test" IsletTests/ClipboardMonitorTests.swift` returns 9 or more
    - `xcodebuild build-for-testing -scheme Islet -destination 'platform=macOS' -configuration Debug` reports `** TEST BUILD SUCCEEDED **`
    - `xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug` reports BUILD SUCCEEDED
    - Manual Cmd-U run in Xcode (headless xcodebuild test hangs in this repo — PROJECT.md-documented
      Bluetooth TCC wait; the build-for-testing gate above is the automated proof, test EXECUTION is
      verified via Xcode's Test Navigator) shows all 9 ClipboardMonitorTests passing
  </acceptance_criteria>
  <done>ClipboardMonitorTests.swift exists, covers all 9 behaviors for isConcealedOrTransient/isSelfCaptureMarker/classifyPasteboardContent using fresh (never .general) pasteboard fixtures; TEST BUILD SUCCEEDED, BUILD SUCCEEDED.</done>
</task>

<task type="auto">
  <name>Task 3: ClipboardMonitorManualSpike — on-device manual spike scaffold</name>
  <files>IsletTests/ClipboardMonitorManualSpike.swift</files>
  <read_first>
    IsletTests/AudioOutputMonitorManualSpike.swift (full file, 42 lines — the exact shape to mirror,
    see interfaces section above)
    Islet/Clipboard/ClipboardMonitor.swift (Task 1's output — the init(onChange:)/start()/stop()
    signatures this spike constructs and drives)
  </read_first>
  <action>
    Create IsletTests/ClipboardMonitorManualSpike.swift: `import XCTest`, `@testable import
    Islet`. Header comment mirroring AudioOutputMonitorManualSpike.swift's exact wording pattern —
    "MANUAL SPIKE — DO NOT RUN VIA `xcodebuild test`" (the full Islet.app test host hangs headless,
    this project's established xcodebuild-test-headless-hang precedent, PROJECT.md-documented). Run
    via Xcode Cmd-U for THIS single test method only, then read the Xcode console and follow the
    on-device verification steps in 57-02-PLAN.md Task 2.

    `final class ClipboardMonitorManualSpike: XCTestCase` with one `@MainActor func
    testManualPollingAndClassification()`: declare `var monitor: ClipboardMonitor!`, construct
    `monitor = ClipboardMonitor(onChange: { item in print("[ClipboardMonitorSpike] captured
    kind=\(item.kind) timestamp=\(item.timestamp)") })` (a throwaway console-print sink — never
    ClipboardStore/ClipboardFileStore, per D-09), call `monitor.start()`, then `RunLoop.current.run(
    until: Date().addingTimeInterval(45))` — a 45-second window for the developer to manually copy
    text and image content while watching the console (mirrors AudioOutputMonitorManualSpike's
    45-second manual-interaction window). Call `monitor.stop()` afterward. End with `XCTAssertTrue(
    true, "manual spike — see console output and 57-02-PLAN.md Task 2 for the real pass/fail
    criteria")` — always green, matching the analog's own documented convention (the real pass/fail
    is the human-read console output plus Plan 57-02's on-device checkpoint, never this trivial
    assertion).

    Run xcodegen generate from the repo root.
  </action>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build-for-testing -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "TEST BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "MANUAL SPIKE" IsletTests/ClipboardMonitorManualSpike.swift` matches
    - `grep -q "final class ClipboardMonitorManualSpike: XCTestCase" IsletTests/ClipboardMonitorManualSpike.swift` matches
    - `grep -q "ClipboardMonitor(onChange:" IsletTests/ClipboardMonitorManualSpike.swift` matches
    - `grep -q "monitor.start()" IsletTests/ClipboardMonitorManualSpike.swift` matches
    - `grep -q "monitor.stop()" IsletTests/ClipboardMonitorManualSpike.swift` matches
    - `grep -q "XCTAssertTrue(true" IsletTests/ClipboardMonitorManualSpike.swift` matches (always-green, real verdict is console output + Plan 57-02's checkpoint)
    - `xcodebuild build-for-testing -scheme Islet -destination 'platform=macOS' -configuration Debug` reports `** TEST BUILD SUCCEEDED **`
  </acceptance_criteria>
  <done>ClipboardMonitorManualSpike.swift exists, mirrors AudioOutputMonitorManualSpike.swift's shape exactly, drives a real ClipboardMonitor against a console-print throwaway sink with a 45s manual-interaction window; TEST BUILD SUCCEEDED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `NSPasteboard.general` → `ClipboardMonitor.poll()` | Untrusted-content boundary — ANY process on the system (including a password manager) can write to the general pasteboard at any time; `poll()` treats every tick's content as unvalidated input |
| `ClipboardMonitor.onChange` → its caller | This plan's own callers (the manual spike, and later Plan 57-02's DEBUG hooks) only print to console — no persistence, no real `ClipboardStore`/`ClipboardFileStore` write occurs from this plan's code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Information Disclosure | `ClipboardMonitor.poll()` capturing concealed/transient content | mitigate | `isConcealedOrTransient(_:)` checked before any content read, covering `ConcealedType`/`TransientType`/`AutoGeneratedType` (PRIV-01's required baseline). PITFALLS.md Pitfall 4 explicitly documents this convention as necessary-but-not-sufficient (e.g. Bitwarden's browser extension sets no marker) — the residual gap is covered by Phase 56's already-shipped at-rest encryption (PRIV-02) once Phase 58 wires real persistence, not by this file |
| T-57-02 | Spoofing (self-capture loop) | `ClipboardMonitor.poll()` re-ingesting its own future restore write | mitigate | `isSelfCaptureMarker(_:markerType:)` checked in the same guard as the concealed/transient filter, using a private marker pasteboard type (`ClipboardMonitor.restoreMarkerType`) that Phase 58's real click-to-restore write will tag itself with — Maccy's proven pattern (PITFALLS.md Pitfall 1) |
| T-57-03 | Denial of Service (self-inflicted, low severity) | `ClipboardMonitor.start()`'s poll interval | mitigate | Fixed at 0.5s (Maccy's proven default), gated behind a cheap `changeCount` integer comparison before any real work — never sub-100ms, never reads pasteboard content unconditionally (PITFALLS.md Pitfall 2) |
| T-57-04 | Information Disclosure | `NSPasteboard.accessBehavior`'s macOS 15.4+ privacy gate | accept (partial, this phase) | `ClipboardMonitor.needsAccessExplanation` exposes the check; the actual one-time in-app explanation UX is Plan 57-02's D-07 placeholder (spike-quality), with Phase 58 building the final polished copy — this plan only proves the underlying check compiles and is callable |

No package-manager installs occur in this plan (`AppKit`/`Foundation` only, zero new dependencies)
— the Package Legitimacy Gate does not apply; `T-{phase}-SC` is omitted accordingly, matching
Phase 55/56's own precedent.

</threat_model>

<verification>
1. `xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug` reports BUILD SUCCEEDED after all 3 tasks.
2. `xcodebuild build-for-testing -scheme Islet -destination 'platform=macOS' -configuration Debug` reports `** TEST BUILD SUCCEEDED **`.
3. Manual Cmd-U in Xcode (headless `xcodebuild test` hangs in this repo — PROJECT.md-documented Bluetooth TCC wait) runs `ClipboardMonitorTests`, all 9 green.
4. `grep -c "IslandResolver\|TransientQueue\|NotchWindowController" Islet/Clipboard/ClipboardMonitor.swift` returns 0 — confirms the monitor is its own independent axis (mirrors Phase 55/56's SC-4 style boundary).
5. `grep -q "repeating: 0.5" Islet/Clipboard/ClipboardMonitor.swift` confirms the 500ms poll interval, not Focus's 2.5s.
</verification>

<success_criteria>
- `ClipboardMonitor` polls `NSPasteboard.general.changeCount` on a gated 0.5s timer, classifying genuine copies as text or image (ROADMAP Phase 57 SC#1's underlying mechanism — on-device proof itself is Plan 57-02).
- `isConcealedOrTransient(_:)` correctly filters `org.nspasteboard.ConcealedType`/`TransientType`/`AutoGeneratedType`, proven by unit test (PRIV-01's code-level implementation; on-device proof is Plan 57-02 SC#2).
- `isSelfCaptureMarker(_:markerType:)` correctly detects the private restore-marker type, proven by unit test (Pitfall 1's guard mechanism; on-device proof is Plan 57-02 SC#3).
- `ClipboardMonitor.needsAccessExplanation` exposes the `NSPasteboard.accessBehavior` check Plan 57-02's D-07 spike hook will drive (SC#4's underlying mechanism).
- `xcodebuild build -scheme Islet` reports BUILD SUCCEEDED; `xcodebuild build-for-testing -scheme Islet` reports TEST BUILD SUCCEEDED.
- Manual Cmd-U confirms all 9 `ClipboardMonitorTests` pass.
</success_criteria>

<output>
Create `.planning/phases/57-pasteboard-monitor-spike/57-01-SUMMARY.md` when done
</output>
