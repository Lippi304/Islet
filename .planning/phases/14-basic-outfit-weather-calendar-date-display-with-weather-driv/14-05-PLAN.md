---
phase: 14
plan: 05
type: execute
wave: 4
depends_on: ["14-04"]
files_modified: []
autonomous: false
requirements: [WEATHER-01, CAL-01, OUTFIT-01]

must_haves:
  truths:
    - "A real on-device WeatherKit fetch succeeds (no silent entitlement failure) after the Wave-1 signing/capability setup"
    - "Denying Location permission omits the weather column with no error UI (D-01)"
    - "Denying Calendar permission omits the calendar column with no error UI (D-03)"
    - "The shown calendar event live-advances through the day and falls to tomorrow's first event when today's are exhausted (D-04)"
    - "No animation-driven CPU activity survives collapsing the island back to the idle pill (D-04/Pitfall 5 precedent)"
  artifacts: []
  key_links: []
---

<objective>
Prove, on-device, everything 14-01 through 14-04 could not prove headlessly: that the real
WeatherKit entitlement/signing setup (14-02) actually works at runtime, that permission denial
degrades silently (D-01/D-03) rather than erroring, that the next-event selection live-advances
through the day (D-04), and that the weather icon's animation carries zero idle-CPU cost once
the island collapses (D-04/Pitfall 5's established precedent for `EqualizerBars`/`ProgressBar`).
This mirrors the on-device verification plans already used in this project (10-04, 12-04,
13-01's Task 3) — permission prompts and Energy sampling cannot be automated headlessly
(VALIDATION.md).

Purpose: this phase's D-01/D-03 silent-degradation contract is invisible to any build gate —
a broken entitlement and "permission correctly denied" both produce the exact same "column
absent" UI, so only a human watching the actual permission dialogs and columns can tell them
apart.
Output: a confirmed, working Phase 14 feature — or a clearly reported gap for follow-up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-basic-outfit-weather-calendar-date-display-with-weather-driv/14-CONTEXT.md
@.planning/phases/14-basic-outfit-weather-calendar-date-display-with-weather-driv/14-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: WeatherKit end-to-end + silent permission-denial verification (D-01/D-03)</name>
  <what-built>A real, entitled WeatherKit fetch (14-02's signing/capability setup + 14-03's
  WeatherKitService/EventKitService + 14-04's controller wiring), and the D-01/D-03 silent
  "column absent" degradation for both weather and calendar when permission is denied.</what-built>
  <action>
    1. In Xcode, build and run Islet (Cmd-R). Hover/click the notch to expand it into the idle
       glance with no media playing.
    2. Expected on first run: macOS shows the Location permission prompt — click "Allow" (or
       "Allow Once"). Shortly after, the LEFT column should show a weather icon (animating) and
       a temperature. If macOS also prompts for Calendar access, click "Allow Full Access" — the
       RIGHT column should show "Today"/"Tomorrow" + your next real calendar event (if any exist
       in the next 2 days across any of your calendars), or stay blank if you have none.
    3. Quit Islet. In System Settings > Privacy & Security > Location Services, find Islet and
       toggle it OFF (deny). Relaunch Islet, expand the island: confirm the weather column is
       completely ABSENT (no icon, no "--°", no error text) — not just empty, gone.
    4. In System Settings > Privacy & Security > Calendars, find Islet and set it to "Don't
       Allow" (deny). Relaunch Islet, expand the island: confirm the calendar column is
       completely ABSENT — no "No events" text, no error, no link to System Settings.
    5. Re-enable both Location and Calendar for Islet in System Settings, relaunch once more, and
       confirm both columns return.
  </action>
  <how-to-verify>Same steps as above.</how-to-verify>
  <resume-signal>Reply "approved" if all 5 steps matched expectations, or describe exactly which
  step diverged (e.g. "weather column never appeared even with Location allowed" — this would
  point back at 14-02's signing/capability setup, not this plan).</resume-signal>
  <done>A real weather fetch succeeds and renders after the entitlement setup; denying either
  permission produces a silently absent column with no error UI; re-granting restores both.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Next-event live advancement (D-04) + idle-CPU check (D-04/Pitfall 5 precedent)</name>
  <what-built>The `nextRelevantEvent` live-advancing selection (14-01/14-03) and the weather
  icon's construction-based idle-CPU gating (14-04) — the same on-device Energy-sampling
  discipline already applied to `EqualizerBars`/`ProgressBar` in earlier phases.</what-built>
  <action>
    1. In Calendar.app, create 2-3 test events today: one that started a few minutes ago and
       ends soon (in-progress), and one starting later today. Confirm the notch's calendar
       column shows the currently-relevant one (in-progress or the next upcoming), labeled
       "Today". Wait for the in-progress event to end (or edit its end time to be in the past)
       and re-expand the island — confirm the column now shows the NEXT event, still "Today".
    2. Delete/move all of today's test events so none remain, add one event tomorrow, and
       confirm the column now shows it labeled "Tomorrow". Delete that one too and confirm the
       calendar column goes fully absent (D-04's "nothing" case).
    3. Idle-CPU check: expand the island so the weather icon is visibly animating. In Terminal,
       run `sample Islet 3 -file /tmp/islet-expanded.sample` while the island stays expanded and
       animating, then collapse the island back to the idle pill and run
       `sample Islet 3 -file /tmp/islet-collapsed.sample`. Compare: the collapsed sample should
       show no `symbolEffect`/animation-driver-related stack frames actively consuming CPU
       (mirrors the existing `EqualizerBars`/`ProgressBar` on-device precedent). Alternatively,
       open Activity Monitor's Energy tab, watch Islet's Energy Impact while expanded-and-
       animating vs. collapsed-and-idle, and confirm it drops back to near-zero after collapse.
  </action>
  <how-to-verify>Same steps as above.</how-to-verify>
  <resume-signal>Reply "approved" if the event advanced correctly through both cases and idle
  CPU returned to baseline after collapse, or describe the exact divergence (e.g. "calendar
  column kept showing the ended event" or "sample showed ongoing symbolEffect CPU after
  collapse").</resume-signal>
  <done>The calendar column live-advances exactly per D-04 (in-progress/next-today ->
  tomorrow's-first -> nothing), and no animation-driven CPU activity survives the island
  collapsing back to the idle pill.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| OS permission dialogs -> human observer | This plan's entire purpose is to have a human confirm the app honors these boundaries; no code changes occur here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-11 | Elevation of Privilege (silent permission bypass going undetected) | Whole Phase 14 feature | mitigate | This plan is itself the mitigation: a human explicitly toggles both permissions off/on and confirms the app never nags, retries, or shows an error — the only verification method for an access-control boundary that can't be automated headlessly (ASVS V4) |

No package-manager installs, no code changes in this plan — the Package Legitimacy Gate does
not apply.
</threat_model>

<verification>
Both checkpoint tasks approved by the human operator on-device, per VALIDATION.md's
Manual-Only Verifications table.
</verification>

<success_criteria>
- WeatherKit fetch succeeds on-device (14-02's setup verified functionally, not just structurally).
- Weather/calendar columns are silently absent on permission denial, restored on re-grant.
- The shown calendar event live-advances per D-04's exact rule (in-progress/next-today -> tomorrow's-first -> nothing).
- No animation-driven CPU activity survives island collapse.
</success_criteria>

<output>
Create `.planning/phases/14-basic-outfit-weather-calendar-date-display-with-weather-driv/14-05-SUMMARY.md` when done.
</output>
