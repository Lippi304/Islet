---
phase: 14-basic-outfit-weather-calendar-date-display-with-weather-driv
plan: 02
subsystem: infra
tags: [xcodegen, code-signing, weatherkit, entitlements, apple-developer-portal]

# Dependency graph
requires: []
provides:
  - Debug builds now sign with the real Apple Developer Team (R7AGU84UX7), entitled for WeatherKit
  - com.apple.developer.weatherkit entitlement wired into Islet.entitlements
  - Location/Calendars/CalendarsFullAccess Info.plist usage-description keys, ready for OS permission prompts
affects: [14-03, 14-04, 14-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "WeatherKit auth via code-signing identity + entitlement, not an API key — real DEVELOPMENT_TEAM is a prerequisite, not optional"

key-files:
  created: []
  modified:
    - project.yml
    - Islet/Islet.entitlements
    - Islet.xcodeproj/project.pbxproj

key-decisions:
  - "DEVELOPMENT_TEAM=R7AGU84UX7 committed as plaintext (not a secret, same disposition as scripts/release.sh's DEVELOPER_ID, T-13-01/T-14-03)"
  - "Both NSCalendarsUsageDescription and NSCalendarsFullAccessUsageDescription keys added defensively for macOS 14+/26 granular access scopes (Pitfall 3 precedent)"

patterns-established: []

requirements-completed: [WEATHER-01]

# Metrics
duration: 20min
completed: 2026-07-08
---

# Phase 14 Plan 02: WeatherKit signing/entitlement setup Summary

**Real Developer Team signing (R7AGU84UX7) + WeatherKit entitlement + Location/Calendar usage-description keys wired into project.yml/entitlements; BUILD SUCCEEDED.**

## Performance

- **Duration:** ~20 min (5 min investigation + checkpoint wait, 15 min Task 2 execution/build)
- **Started:** 2026-07-08
- **Tasks:** 2/2 completed
- **Files modified:** 3 (project.yml, Islet/Islet.entitlements, Islet.xcodeproj/project.pbxproj)

## Accomplishments
- Task 1 (human-action checkpoint): user confirmed WeatherKit capability enabled on the `com.lippi304.islet` App ID in the Apple Developer portal, and reported the real 10-character Team ID (`R7AGU84UX7`).
- Task 2: replaced ad-hoc `CODE_SIGN_IDENTITY: "-"` with `DEVELOPMENT_TEAM: "R7AGU84UX7"` in `project.yml`; added `com.apple.developer.weatherkit` to `Islet/Islet.entitlements`; added three `INFOPLIST_KEY_*` usage-description keys (Location, Calendars, CalendarsFullAccess) mirroring the existing Bluetooth-key convention; regenerated the Xcode project via `xcodegen generate`; confirmed `xcodebuild build -scheme Islet` succeeds with the new signing configuration.

## Task Commits

Each task was committed atomically:

1. **Task 1: Enable WeatherKit on the App ID and obtain the real Team ID** - `5a4e8f8` (docs — checkpoint record, no code change; this was the initial halt commit, later resolved by user confirmation with no further commit needed for Task 1 itself)
2. **Task 2: Wire the real Team ID + WeatherKit entitlement + usage-description keys, regenerate, build** - `77c0828` (feat)

**Plan metadata:** (final SUMMARY commit follows this file)

## Files Created/Modified
- `project.yml` - `DEVELOPMENT_TEAM: "R7AGU84UX7"` replaces ad-hoc `CODE_SIGN_IDENTITY: "-"`; three new `INFOPLIST_KEY_*` usage-description keys added
- `Islet/Islet.entitlements` - added `com.apple.developer.weatherkit` sibling key to the existing `disable-library-validation` entry
- `Islet.xcodeproj/project.pbxproj` - regenerated by `xcodegen generate` to reflect the above

## Decisions Made
- Committed the real Team ID as plaintext in `project.yml` per the plan's own accepted threat disposition (T-14-03): a Team ID is a public identifier, not a secret, same precedent as the committed `DEVELOPER_ID` in `scripts/release.sh`.
- Kept both `NSCalendarsUsageDescription` and `NSCalendarsFullAccessUsageDescription` (same German-locale string) per the plan's defensive dual-key posture for macOS 14+/26's split calendar access scopes.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added `-allowProvisioningUpdates -allowProvisioningDeviceRegistration` flags to the verification build**
- **Found during:** Task 2 (`xcodebuild build -scheme Islet` verification step)
- **Issue:** The plan's literal verify command (`xcodegen generate && xcodebuild build -scheme Islet`) failed twice: first with "No profiles for 'com.lippi304.islet' were found... Automatic signing is disabled and unable to generate a profile" (needs `-allowProvisioningUpdates`), then with "Device ... isn't registered in your developer account" (needs `-allowProvisioningDeviceRegistration` for the one-time local-Mac device registration that Xcode's GUI does silently but `xcodebuild` requires an explicit flag for).
- **Fix:** Ran `xcodebuild build -scheme Islet -allowProvisioningUpdates -allowProvisioningDeviceRegistration`. This is a one-time bootstrap of the local automatic-signing provisioning profile/device registration — the flags are safe, standard Apple-documented xcodebuild options for first-time automatic signing setup, not a workaround of anything security-relevant.
- **Files modified:** None (build invocation only, no source change)
- **Verification:** `** BUILD SUCCEEDED **`
- **Committed in:** `77c0828` (documented here; the build command itself isn't a file change)

---

**Total deviations:** 1 auto-fixed (1 blocking — CLI flag addition, no architectural change)
**Impact on plan:** Necessary one-time provisioning bootstrap for automatic signing under a real Team via CLI; no scope creep. Future `xcodebuild build -scheme Islet` invocations (e.g. in 14-05) should now succeed without these flags since the profile/device are now registered.

## Issues Encountered

None beyond the provisioning-profile bootstrap documented above.

## User Setup Required

None further — the one external-service step (WeatherKit capability enrollment + Team ID lookup) was completed in Task 1 during this plan's execution.

## Next Phase Readiness

- `project.yml` and `Islet/Islet.entitlements` are ready for 14-03/14-04 to build WeatherKit-calling code against — the entitlement and signing identity are in place.
- Full on-device runtime verification of an actual WeatherKit fetch (does the entitlement actually authorize a live network call) is explicitly deferred to 14-05 per this plan's own scope — this plan only proves the setup is syntactically/structurally correct and the app builds/signs.
- No blockers carried forward.

---
*Phase: 14-basic-outfit-weather-calendar-date-display-with-weather-driv*
*Completed: 2026-07-08*

## Self-Check: PASSED

- FOUND: project.yml DEVELOPMENT_TEAM: "R7AGU84UX7"
- FOUND: Islet/Islet.entitlements com.apple.developer.weatherkit
- FOUND: commit 5a4e8f8
- FOUND: commit 77c0828
