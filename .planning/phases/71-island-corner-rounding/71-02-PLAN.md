---
phase: 71-island-corner-rounding
plan: 02
type: execute
wave: 2
depends_on: ["71-01"]
files_modified:
  - Islet/ActivitySettings.swift
  - Islet/Notch/NotchPillView.swift
  - Islet/AppDelegate.swift
  - IsletTests/ActivitySettingsTests.swift
autonomous: false
requirements: [SHAPE-03]

must_haves:
  truths:
    - "A DEBUG-only 'Corner Radius' Wing Tuner axis exists with ±1/±5 step buttons (D-05) that live-shift both wingsShape()'s topCornerRadius and bottomCornerRadius by the same delta (D-03)"
    - "Reset Wing Tuner zeroes the new axis alongside the existing 4; Print Wing Tuner Values includes the new axis's value in its printed output"
    - "Release builds compile with zero corner-radius tuning UI present — the same #if DEBUG compile-time exclusion already proven by the existing 4 axes"
    - "mediaWingsOrToast()/resumePreviewWings() do NOT get their own tuner axis — only wingsShape()'s call site is nudge-reachable, per D-06"
  artifacts:
    - path: "Islet/ActivitySettings.swift"
      provides: "debugWingCornerRadiusNudgeKey constant, DEBUG-gated"
      contains: "static let debugWingCornerRadiusNudgeKey = \"debug.wingTuner.cornerRadiusNudge\""
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "debugWingCornerRadiusNudge @AppStorage + wingCornerRadiusNudge always-compiled read point, wired into wingsShape()"
      contains: "private var wingCornerRadiusNudge: CGFloat"
    - path: "Islet/AppDelegate.swift"
      provides: "4 new NSMenuItems + @objc actions + updated Reset/Print"
      contains: "Corner Radius +1"
    - path: "IsletTests/ActivitySettingsTests.swift"
      provides: "key-name regression test for the new axis"
      contains: "func testWingCornerRadiusNudgeKeyName"
  key_links:
    - from: "AppDelegate NSMenuItem action"
      to: "adjustWingNudge(ActivitySettings.debugWingCornerRadiusNudgeKey, by:)"
      via: "@objc selector"
      pattern: "adjustWingNudge\\(ActivitySettings\\.debugWingCornerRadiusNudgeKey"
    - from: "wingCornerRadiusNudge"
      to: "wingsShape()'s NotchShape(...) call"
      via: "computed property read at render time"
      pattern: "Self\\.wingBaseTopCornerRadius \\+ wingCornerRadiusNudge"
---

<objective>
Add a 5th DEBUG-only Wing Tuner axis — "Corner Radius" — mirroring the existing Leading/Trailing/Margin/Gap mechanism exactly: one `@AppStorage` key (`ActivitySettings.swift`), one always-compiled read point that nudges both of `wingsShape()`'s radii together (`NotchPillView.swift`), and `NSMenu` buttons + Reset/Print wiring (`AppDelegate.swift`). Per D-03 this is a single combined axis (not independent top/bottom axes); per D-05 it uses ±1/±5 steps only (no ±10/±20 tier); per D-06 `mediaWingsOrToast()`/`resumePreviewWings()` do not get this axis.

Purpose: Deliver SHAPE-03 — a live on-device tuning tool for refining Plan 71-01's 16/8 starting values, using this codebase's own established Wing Tuner convention.
Output: A 5th working Wing Tuner axis, plus an on-device UAT checkpoint closing out both SHAPE-02 and SHAPE-03's manual-only verifications.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-island-corner-rounding/71-CONTEXT.md
@.planning/phases/71-island-corner-rounding/71-RESEARCH.md
@.planning/phases/71-island-corner-rounding/71-VALIDATION.md

<interfaces>
The existing 4-axis Wing Tuner 3-file wiring this plan mechanically extends to a 5th axis:

From Islet/ActivitySettings.swift (lines 112-117):
#if DEBUG
static let debugWingLeadingNudgeKey = "debug.wingTuner.leadingNudge"
static let debugWingTrailingNudgeKey = "debug.wingTuner.trailingNudge"
static let debugWingMarginNudgeKey = "debug.wingTuner.marginNudge"
static let debugWingGapNudgeKey = "debug.wingTuner.gapNudge"
#endif

From Islet/Notch/NotchPillView.swift (lines 181-218):
#if DEBUG
@AppStorage(ActivitySettings.debugWingMarginNudgeKey) private var debugWingMarginNudge: Double = 0
#endif
private var wingMarginNudge: CGFloat {
    #if DEBUG
    return CGFloat(debugWingMarginNudge)
    #else
    return 0
    #endif
}

From Islet/AppDelegate.swift (lines 593-596, the shared helper every axis's actions call):
private func adjustWingNudge(_ key: String, by delta: Double) {
    let current = UserDefaults.standard.double(forKey: key)
    UserDefaults.standard.set(current + delta, forKey: key)
}

Plan 71-01 already added (and this plan builds on):
static let wingBaseTopCornerRadius: CGFloat = 16
static let wingBaseBottomCornerRadius: CGFloat = 8
// wingsShape() line 3041 currently reads:
let shape = NotchShape(topCornerRadius: Self.wingBaseTopCornerRadius, bottomCornerRadius: Self.wingBaseBottomCornerRadius)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: New @AppStorage key + read point, wire nudge into wingsShape() (D-03/D-04)</name>
  <files>Islet/ActivitySettings.swift, Islet/Notch/NotchPillView.swift, IsletTests/ActivitySettingsTests.swift</files>
  <read_first>
    Islet/ActivitySettings.swift (lines 109-118)
    Islet/Notch/NotchPillView.swift (lines 181-219 for the storage/read-point block; line 3041, as left by Plan 71-01, for `wingsShape()`'s call site)
    IsletTests/ActivitySettingsTests.swift (lines 111-129, `testNewV110KeyNames`'s pattern)
  </read_first>
  <behavior>
    - Test: `ActivitySettings.debugWingCornerRadiusNudgeKey` equals the literal string `"debug.wingTuner.cornerRadiusNudge"`
  </behavior>
  <action>
    In `Islet/ActivitySettings.swift`, inside the existing `#if DEBUG ... #endif` block (lines 112-117) that declares `debugWingLeadingNudgeKey`/`debugWingTrailingNudgeKey`/`debugWingMarginNudgeKey`/`debugWingGapNudgeKey`, add a 5th line: `static let debugWingCornerRadiusNudgeKey = "debug.wingTuner.cornerRadiusNudge"` — matching the existing `debug.wingTuner.<axis>Nudge` naming convention exactly.
    In `Islet/Notch/NotchPillView.swift`, inside the existing `#if DEBUG ... #endif` `@AppStorage` block (lines 181-186), add a 5th line: `@AppStorage(ActivitySettings.debugWingCornerRadiusNudgeKey) private var debugWingCornerRadiusNudge: Double = 0`. Immediately after the existing `wingGapNudge` computed property (ends line 218), add a matching computed property: `private var wingCornerRadiusNudge: CGFloat { #if DEBUG return CGFloat(debugWingCornerRadiusNudge) #else return 0 #endif }`.
    In `wingsShape()` (the call site Plan 71-01 left as `NotchShape(topCornerRadius: Self.wingBaseTopCornerRadius, bottomCornerRadius: Self.wingBaseBottomCornerRadius)`), change it to `NotchShape(topCornerRadius: Self.wingBaseTopCornerRadius + wingCornerRadiusNudge, bottomCornerRadius: Self.wingBaseBottomCornerRadius + wingCornerRadiusNudge)` — the SAME nudge value shifts both radii per D-03 (one combined axis, not independent top/bottom axes), preserving whatever base ratio Plan 71-01 baked in.
    Do NOT touch `mediaWingsOrToast()` or `resumePreviewWings()` in this task — per D-06 those two track only the baked-in base values from Plan 71-01, with no DEBUG tuner axis of their own.
    In `IsletTests/ActivitySettingsTests.swift`, add `func testWingCornerRadiusNudgeKeyName() { XCTAssertEqual(ActivitySettings.debugWingCornerRadiusNudgeKey, "debug.wingTuner.cornerRadiusNudge") }` immediately after `testNewV110KeyNames()` (line 129), following that method's exact one-assertion style.
  </action>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodebuild test -scheme Islet -destination 'platform=macOS' -only-testing:IsletTests/ActivitySettingsTests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'static let debugWingCornerRadiusNudgeKey = "debug.wingTuner.cornerRadiusNudge"' Islet/ActivitySettings.swift` returns 1
    - `grep -c "debugWingCornerRadiusNudge: Double = 0" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "private var wingCornerRadiusNudge: CGFloat" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "Self.wingBaseTopCornerRadius + wingCornerRadiusNudge" Islet/Notch/NotchPillView.swift` returns 1 and `grep -c "Self.wingBaseBottomCornerRadius + wingCornerRadiusNudge" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "wingCornerRadiusNudge" Islet/Notch/NotchPillView.swift` returns 2 (the property declaration line + the single `wingsShape()` call-site line, which uses the identifier twice but `grep -c` counts matching lines, not occurrences) — confirming `mediaWingsOrToast()`/`resumePreviewWings()` reference it zero times
    - `xcodebuild test -scheme Islet -destination 'platform=macOS' -only-testing:IsletTests/ActivitySettingsTests` passes, including the new `testWingCornerRadiusNudgeKeyName`
  </acceptance_criteria>
  <done>New key, storage, read point, and wingsShape() wiring all exist; new key-name test passes; mediaWingsOrToast/resumePreviewWings remain untouched.</done>
</task>

<task type="auto">
  <name>Task 2: AppDelegate menu items, actions, Reset/Print wiring (D-04/D-05)</name>
  <files>Islet/AppDelegate.swift</files>
  <read_first>
    Islet/AppDelegate.swift (lines 499-541 for `wingTunerMenu` construction; lines 590-625 for the shared `adjustWingNudge` helper, per-axis `@objc` actions, `debugWingTunerReset()`, `debugWingTunerPrint()`)
  </read_first>
  <action>
    In `setupDebugMenu()`'s `wingTunerMenu` construction, immediately before the existing `wingTunerMenu.addItem(.separator())` line, insert 4 new `NSMenuItem`s in this order: `wingTunerMenu.addItem(withTitle: "Corner Radius -5", action: #selector(debugWingCornerRadiusMinus5), keyEquivalent: "")`, `wingTunerMenu.addItem(withTitle: "Corner Radius -1", action: #selector(debugWingCornerRadiusMinus1), keyEquivalent: "")`, `wingTunerMenu.addItem(withTitle: "Corner Radius +1", action: #selector(debugWingCornerRadiusPlus1), keyEquivalent: "")`, `wingTunerMenu.addItem(withTitle: "Corner Radius +5", action: #selector(debugWingCornerRadiusPlus5), keyEquivalent: "")` — matching D-05's ±1/±5-only convention (no ±10/±20 tier, unlike Margin's 6-button layout).
    Immediately after the existing `debugWingGapPlus()` action (line 609), add 4 new one-line `@objc private func` actions, each calling the existing shared `adjustWingNudge(_:by:)` helper (no new helper needed): `debugWingCornerRadiusMinus5() { adjustWingNudge(ActivitySettings.debugWingCornerRadiusNudgeKey, by: -5) }`, `debugWingCornerRadiusMinus1() { adjustWingNudge(ActivitySettings.debugWingCornerRadiusNudgeKey, by: -1) }`, `debugWingCornerRadiusPlus1() { adjustWingNudge(ActivitySettings.debugWingCornerRadiusNudgeKey, by: 1) }`, `debugWingCornerRadiusPlus5() { adjustWingNudge(ActivitySettings.debugWingCornerRadiusNudgeKey, by: 5) }`.
    Extend `debugWingTunerReset()` (lines 611-617) with a 5th line — `UserDefaults.standard.set(0.0, forKey: ActivitySettings.debugWingCornerRadiusNudgeKey)` — inserted before the existing `notchController?.debugClearAllPreviews()` call.
    Extend `debugWingTunerPrint()` (lines 619-625) with a 5th `let cornerRadius = UserDefaults.standard.double(forKey: ActivitySettings.debugWingCornerRadiusNudgeKey)` line, and append `cornerRadiusNudge=\(cornerRadius)` to the interpolated print string immediately after the existing `gapNudge=\(gap)` segment.
  </action>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodebuild build -scheme Islet -configuration Release -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'withTitle: "Corner Radius -5"' Islet/AppDelegate.swift`, `grep -c 'withTitle: "Corner Radius -1"' Islet/AppDelegate.swift`, `grep -c 'withTitle: "Corner Radius +1"' Islet/AppDelegate.swift`, `grep -c 'withTitle: "Corner Radius +5"' Islet/AppDelegate.swift` each return 1
    - `grep -c 'withTitle: "Corner Radius -10"' Islet/AppDelegate.swift` and `grep -c 'withTitle: "Corner Radius +10"' Islet/AppDelegate.swift` both return 0 (D-05: no coarse tier)
    - `grep -c "adjustWingNudge(ActivitySettings.debugWingCornerRadiusNudgeKey, by:" Islet/AppDelegate.swift` returns 4 (the 4 new `@objc` action bodies)
    - `grep -c "ActivitySettings.debugWingCornerRadiusNudgeKey" Islet/AppDelegate.swift` returns 6 (4 action bodies + 1 in `debugWingTunerReset()` + 1 in `debugWingTunerPrint()`)
    - `grep -c "cornerRadiusNudge=" Islet/AppDelegate.swift` returns 1 (the new field appended to `debugWingTunerPrint()`'s interpolated string)
    - `xcodebuild build -scheme Islet -configuration Release -destination 'platform=macOS'` succeeds (SC#4: Release build has zero corner-radius tuning UI — compiled out via `#if DEBUG`, same mechanism as the existing 4 axes)
  </acceptance_criteria>
  <done>4 new menu items, 4 new actions, and updated Reset/Print all exist and compile cleanly in both Debug and Release configurations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device UAT — corner rounding visual check + Corner Radius tuner live-tune/persist/reset/print</name>
  <files></files>
  <action>
    No file edits — build and run the app on-device (Cmd-R in Xcode) and walk through the steps below. This task's only output is the recorded PASSED/FAILED verdict covering SHAPE-02's visual rounding and SHAPE-03's live-tune/persist/reset/print behavior.
  </action>
  <what-built>
    Plan 71-01 bumped every wing-state HUD's corner radius from 12/6 to 16/8 (Charging, Device, Focus, Countdown, OSD, CapsLock, Update, Download, Timer, Meeting via `wingsShape()`, plus Now Playing's `mediaWingsOrToast()`/`resumePreviewWings()` per D-06). Plan 71-02 added a DEBUG-only "Corner Radius" Wing Tuner axis (±1/±5 steps) that live-shifts `wingsShape()`'s radii, wired into the existing Reset/Print actions.
  </what-built>
  <how-to-verify>
    1. Launch the Debug build. Trigger each wing HUD in turn (play music for Now Playing, plug in charger for Charging, connect a device, start a Timer, join/leave a meeting, press a volume/brightness key for OSD, toggle Caps Lock, trigger an Update/Download notification) — or use the existing "Preview Wing" DEBUG menu to fake-trigger each one without the real event.
    2. For each, confirm the pill silhouette reads as noticeably more rounded at both ends than before this phase, closer to a full pill, while still visibly asymmetric (not a uniform pill) — compare against the reference screenshot described in `.planning/seeds/island-corner-rounding.md`.
    3. Confirm Now Playing's live wing and the idle-hover "resume last track" preview both show the same rounder corners as the other wings.
    4. Confirm the idle/collapsed pill (no activity active) is visually unchanged from before this phase.
    5. Open the 🐞 debug menu → Wing Tuner submenu. Click "Corner Radius +5" a few times while a wing HUD is showing — confirm the corners visibly get rounder live, with no shape glitch or self-intersecting corner (this would appear as a flat-clipped or crossed-over corner). Click "Corner Radius -5" back down.
    6. Quit and relaunch the app. Confirm any non-zero Corner Radius nudge value persisted across relaunch.
    7. Click "Reset Wing Tuner" — confirm the Corner Radius nudge visibly zeroes (corners return to the 16/8 baseline). Click "Print Wing Tuner Values" — confirm the printed console line includes a `cornerRadiusNudge=` field.
  </how-to-verify>
  <resume-signal>Type "approved" once all 7 steps pass, or describe exactly which step/behavior failed.</resume-signal>
  <verify>
    <human-check>All 7 how-to-verify steps confirmed on-device by the user; resume-signal explicitly approved.</human-check>
  </verify>
  <done>User has typed "approved" for the full on-device walkthrough, confirming SHAPE-02 (visual rounding across every wing HUD including Now Playing, idle pill unchanged) and SHAPE-03 (live-tune, persistence across relaunch, Reset/Print correctness) are both observably true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | The DEBUG menu is a fixed, hardcoded set of `NSMenuItem`s with no free-text entry — identical in kind to the 4 already-shipped Wing Tuner axes. No network, no IPC, no new persisted data type. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-03 | Information Disclosure (DEBUG-only surface reaching Release) | `debugWingCornerRadiusNudgeKey`, `wingCornerRadiusNudge`, the 4 new `NSMenuItem`s/`@objc` actions | mitigate | Every new symbol is `#if DEBUG`-gated identically to the 4 existing axes (compiler-excluded, not a runtime check) — verified by Task 2's Release-configuration `xcodebuild build` acceptance criterion, the same proof already used for the existing 4 axes |
| T-71-04 | Tampering (unbounded nudge value) | `UserDefaults.standard` double for the new key | accept | No clamp is applied, matching this project's own established precedent — none of the existing 4 Wing Tuner axes clamp either ("no clamp, trust the dev using the dev tool"); the tool is DEBUG-only and single-user (the developer's own machine), so an out-of-range value only produces a visibly broken dev-tool shape, never a Release-facing issue |
| T-71-05 | All other STRIDE categories | — | accept | No network calls, no free-text/user-supplied input, no privilege/access-control surface — same class of change as the 4 already-shipped Wing Tuner axes |

</threat_model>

<verification>
Run the full XCTest suite after Task 1/2 land: `xcodebuild test -scheme Islet -destination 'platform=macOS'` — confirm zero *new* failures against the documented 569-tests/6-known-failures baseline (STATE.md). Confirm a clean Release build (Task 2's own acceptance criterion). Then run Task 3's on-device UAT checkpoint.
</verification>

<success_criteria>
- `ActivitySettings.debugWingCornerRadiusNudgeKey` exists, DEBUG-gated, matching the `debug.wingTuner.<axis>Nudge` convention
- `wingCornerRadiusNudge` nudges both of `wingsShape()`'s radii by the same delta at render time
- 4 new menu items (±1/±5 only) + 4 new actions + updated Reset/Print exist in `AppDelegate.swift`
- Release build compiles clean with the new axis fully excluded
- On-device UAT (Task 3) approved for both SHAPE-02's visual rounding and SHAPE-03's live-tune/persist/reset/print behavior
</success_criteria>

<output>
Create `.planning/phases/71-island-corner-rounding/71-02-SUMMARY.md` when done
</output>
