---
phase: 71-island-corner-rounding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Notch/NotchPillView.swift
  - IsletTests/NotchShapeTests.swift
autonomous: true
requirements: [SHAPE-02]

must_haves:
  truths:
    - "Every wingsShape()-routed HUD (Charging, Device, Focus, Countdown, OSD, CapsLock, Update, Download, Timer, Meeting) renders with a noticeably larger, still-asymmetric corner radius than today's 12/6 pair"
    - "The Now Playing glance (mediaWingsOrToast) and idle-hover resume preview (resumePreviewWings) also render with the new baked-in radius, per D-06 — even though neither calls wingsShape()"
    - "The idle/collapsed pill (collapsedIsland) and every expanded blob (blobShape() callers) remain pixel-identical to today — zero diff"
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "wingBaseTopCornerRadius/wingBaseBottomCornerRadius constants + updated wingsShape()/mediaWingsOrToast()/resumePreviewWings() NotchShape call sites"
      contains: "static let wingBaseTopCornerRadius: CGFloat = 16"
    - path: "IsletTests/NotchShapeTests.swift"
      provides: "regression tests proving the new base radii produce a valid closed path at both nominal and depth-scale-floor wings rect sizes"
      contains: "func testWingBaseRadiiProduceAClosedNonEmptyPathAtDepthScaleFloor"
  key_links:
    - from: "wingsShape()"
      to: "NotchShape(topCornerRadius:bottomCornerRadius:)"
      via: "direct call-site argument"
      pattern: "NotchShape\\(topCornerRadius: Self\\.wingBaseTopCornerRadius"
---

<objective>
Bump the wing-state HUD silhouette's corner radii from today's 12/6 to a noticeably rounder, still-asymmetric 16/8 pair (D-01/D-02), applied at `wingsShape()`'s single call site plus the two Now-Playing-related call sites that bypass it (`mediaWingsOrToast()`/`resumePreviewWings()`, D-06). Add regression tests proving the new radii still produce a valid, non-self-intersecting `NotchShape` path at the worst-case depth-scaled wing height.

Purpose: Deliver SHAPE-02 — the collapsed-wide (wings) island state reads as noticeably more rounded, closer to a full pill, across every wing HUD including Now Playing.
Output: Two new named radius constants, three updated `NotchShape` call sites, two new XCTest regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-island-corner-rounding/71-CONTEXT.md
@.planning/phases/71-island-corner-rounding/71-RESEARCH.md

<interfaces>
NotchShape.swift — the shape being tuned. No code changes needed here; only the literal
arguments passed at each call site change. The critical invariant every new value must
respect: topCornerRadius + bottomCornerRadius <= rect.height at the SMALLEST rect this
shape is ever asked to fill, or the path's addLine between the two quad-curve corners
inverts/self-intersects.

From Islet/Notch/NotchShape.swift:
struct NotchShape: Shape {
    var topCornerRadius: CGFloat = 6
    var bottomCornerRadius: CGFloat = 14
    func path(in rect: CGRect) -> Path { ... }  // addLine from (minX+topCornerRadius, minY+topCornerRadius) to (minX+topCornerRadius, maxY-bottomCornerRadius)
}

resolvedWingDepthScale (NotchPillView.swift ~line 2905) clamps to 0.8...1.5 (NotchGeometry.swift:120).
wingsShape()'s frame height is Self.wingsSize.height * depthScale = 32 * 0.8 = 25.6pt at the floor —
this is the smallest rect wingsShape()'s NotchShape is ever asked to fill.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add wing base radius constants, update wingsShape() call site (D-01/D-02, SHAPE-02)</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift (lines 465-475 for `wingsSize`; lines 3034-3091 for `wingsShape()`)
    Islet/Notch/NotchShape.swift (full file — the invariant `topCornerRadius + bottomCornerRadius <= rect.height`)
  </read_first>
  <action>
    Immediately after `static let wingsSize = CGSize(width: 290, height: 32)` (line 470), add two new constants: `static let wingBaseTopCornerRadius: CGFloat = 16` and `static let wingBaseBottomCornerRadius: CGFloat = 8`. These scale today's 12/6 pair up by the same 4/3 factor (16 = 12 * 4/3, 8 = 6 * 4/3), preserving the exact 2:1 top:bottom ratio D-01 requires to stay unchanged, while keeping the sum (24) under the 25.6pt depth-scale-floor height with 1.6pt of headroom (Pitfall 1). In `wingsShape()` (line 3041), replace `NotchShape(topCornerRadius: 12, bottomCornerRadius: 6)` with `NotchShape(topCornerRadius: Self.wingBaseTopCornerRadius, bottomCornerRadius: Self.wingBaseBottomCornerRadius)`. Update the trailing inline comment on that line to reference the new 16/8 values instead of 12/6 (keep the rest of the comment's reasoning about `blobShape`'s 24 intact).
  </action>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && grep -c "static let wingBaseTopCornerRadius: CGFloat = 16" Islet/Notch/NotchPillView.swift && grep -c "static let wingBaseBottomCornerRadius: CGFloat = 8" Islet/Notch/NotchPillView.swift && grep -c "NotchShape(topCornerRadius: 12, bottomCornerRadius: 6)" Islet/Notch/NotchPillView.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "static let wingBaseTopCornerRadius: CGFloat = 16"` returns 1
    - `grep -c "static let wingBaseBottomCornerRadius: CGFloat = 8"` returns 1
    - `grep -c "NotchShape(topCornerRadius: 12, bottomCornerRadius: 6)"` returns 0 (old literal fully replaced)
    - `grep -c "NotchShape(topCornerRadius: Self.wingBaseTopCornerRadius, bottomCornerRadius: Self.wingBaseBottomCornerRadius)"` returns 1
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with no new errors
  </acceptance_criteria>
  <done>wingsShape()'s call site uses the two new named constants; old 12/6 literal is gone; build is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Bump mediaWingsOrToast() and resumePreviewWings() literals to match (D-06)</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift (lines 3205-3247 for `mediaWingsOrToast()`; lines 3280-3308 for `resumePreviewWings()`)
    .planning/phases/71-island-corner-rounding/71-CONTEXT.md (D-06)
  </read_first>
  <action>
    In `mediaWingsOrToast()` (line 3215), replace `NotchShape(topCornerRadius: 6, bottomCornerRadius: toast != nil ? 16 : 6)` with `NotchShape(topCornerRadius: Self.wingBaseTopCornerRadius, bottomCornerRadius: toast != nil ? 16 : Self.wingBaseBottomCornerRadius)`. The top radius always uses the new base (16) regardless of toast state. The non-toast bottom radius uses the new base (8). The toast-active bottom literal (16) is deliberately left unchanged — it already exceeds the new base and is the pre-existing "grows blob-like once the toast row appears" behavior, unrelated to SHAPE-02's baseline bump; this branch's total frame height (`Self.wingsSize.height * resolvedWingDepthScale + Self.toastExtraHeight`, where `toastExtraHeight = 32`) comfortably exceeds `topCornerRadius + bottomCornerRadius = 32` at any depth scale, so Pitfall 1's invariant still holds. In `resumePreviewWings()` (line 3281), replace `NotchShape(topCornerRadius: 6, bottomCornerRadius: 6)` with `NotchShape(topCornerRadius: Self.wingBaseTopCornerRadius, bottomCornerRadius: Self.wingBaseBottomCornerRadius)` — this call site's frame is never depth-scaled (`Self.wingsSize.height` directly, no `resolvedWingDepthScale` multiplier), so 24 <= 32 always holds.
  </action>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && grep -c "toast != nil ? 16 : Self.wingBaseBottomCornerRadius" Islet/Notch/NotchPillView.swift && grep -c "NotchShape(topCornerRadius: Self.wingBaseTopCornerRadius, bottomCornerRadius: Self.wingBaseBottomCornerRadius)" Islet/Notch/NotchPillView.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "NotchShape(topCornerRadius: 6, bottomCornerRadius: toast != nil ? 16 : 6)"` returns 0 (old mediaWingsOrToast literal fully replaced)
    - `grep -c "toast != nil ? 16 : Self.wingBaseBottomCornerRadius"` returns 1
    - `grep -c "NotchShape(topCornerRadius: Self.wingBaseTopCornerRadius, bottomCornerRadius: Self.wingBaseBottomCornerRadius)"` returns 2 (wingsShape() from Task 1 + resumePreviewWings() from this task)
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with no new errors
  </acceptance_criteria>
  <done>mediaWingsOrToast()'s and resumePreviewWings()'s NotchShape literals both track the new base radii per D-06; build is clean.</done>
</task>

<task type="auto">
  <name>Task 3: Regression tests for the new radii + no-touch guard for collapsedIsland/blobShape</name>
  <files>IsletTests/NotchShapeTests.swift</files>
  <read_first>
    IsletTests/NotchShapeTests.swift (full file — mirror `testLargerTopCornerRadiusProducesAClosedNonEmptyPath`, lines 47-53)
    Islet/Notch/NotchPillView.swift (lines 1304-1313 for `collapsedIsland`; the 4 `blobShape(topCornerRadius: 24, bottomCornerRadius: 32` call sites at lines 1145, 2062, 2114, 2400)
  </read_first>
  <behavior>
    - Test: `NotchShape(topCornerRadius: 16, bottomCornerRadius: 8).path(in: CGRect(x: 0, y: 0, width: 290, height: 32))` (nominal wings rect) produces a non-empty, closed path with a positive-width and positive-height bounding box
    - Test: the same radii against `CGRect(x: 0, y: 0, width: 290, height: 25.6)` (the resolvedWingDepthScale 0.8x floor rect, Pitfall 1's worst case) also produce a non-empty, closed path with a positive-width and positive-height bounding box
  </behavior>
  <action>
    In `IsletTests/NotchShapeTests.swift`, add two new test methods immediately after `testLargerTopCornerRadiusProducesAClosedNonEmptyPath` (line 53), following that method's exact structure: `testWingBaseRadiiProduceAClosedNonEmptyPathAtNominalSize()` using `NotchShape(topCornerRadius: 16, bottomCornerRadius: 8).path(in: CGRect(x: 0, y: 0, width: 290, height: 32))`, and `testWingBaseRadiiProduceAClosedNonEmptyPathAtDepthScaleFloor()` using the same radii against `CGRect(x: 0, y: 0, width: 290, height: 25.6)`. Each test asserts `XCTAssertFalse(path.cgPath.isEmpty, ...)`, `XCTAssertGreaterThan(cgBounds.width, 0, ...)`, and `XCTAssertGreaterThan(cgBounds.height, 0, ...)`, exactly mirroring the existing test's assertion style and message wording pattern.
  </action>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodebuild test -scheme Islet -destination 'platform=macOS' -only-testing:IsletTests/NotchShapeTests</automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild test -scheme Islet -destination 'platform=macOS' -only-testing:IsletTests/NotchShapeTests` passes, including both new test methods
    - `sed -n '1304,1313p' Islet/Notch/NotchPillView.swift | grep -c "let shape = NotchShape()"` returns 1 (collapsedIsland's bare, no-explicit-radii call site is untouched — SC#2 regression guard)
    - `grep -c "topCornerRadius: 24, bottomCornerRadius: 32" Islet/Notch/NotchPillView.swift` returns 4 (all 4 blobShape() call sites unchanged)
  </acceptance_criteria>
  <done>Both new NotchShapeTests pass; collapsedIsland and blobShape() call sites are confirmed untouched by grep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | Pure in-process SwiftUI geometry constant change; no network, no user-supplied input, no IPC, no file I/O beyond the existing `NotchShape` rendering path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-01 | Denial of Service (degenerate geometry) | `NotchShape.path(in:)` via `wingsShape()`/`mediaWingsOrToast()`/`resumePreviewWings()` | mitigate | The new base radii (16/8, sum 24) are chosen to stay under the 25.6pt depth-scale-floor rect height (Pitfall 1) at every call site touched by this plan, verified by Task 3's two new XCTest regression tests plus the existing `testPathStaysWithinItsRect`/`testCustomRadiiProduceAClosedNonEmptyPath` — a self-intersecting/degenerate path would show up as a visibly broken silhouette, not a crash, so the automated test is the primary guard |
| T-71-02 | All other STRIDE categories | — | accept | No network calls, no free-text/user-supplied input, no new persisted data, no privilege/access-control surface touched by this plan — same class of pure rendering-constant change as the already-shipped Phase 29 (SHAPE-01) flare and Phase 25 material/animation phases |

</threat_model>

<verification>
Run the full XCTest suite after both tasks land: `xcodebuild test -scheme Islet -destination 'platform=macOS'` — confirm zero *new* failures against the documented 569-tests/6-known-failures baseline (STATE.md). Confirm a clean Debug build (`xcodebuild build -scheme Islet -destination 'platform=macOS'`).
</verification>

<success_criteria>
- `wingsShape()`, `mediaWingsOrToast()`, and `resumePreviewWings()` all render through the new 16/8 base radii (directly or via the toast-conditional bottom exception documented in Task 2)
- `collapsedIsland` and every `blobShape()` call site are unchanged (grep-confirmed)
- `IsletTests/NotchShapeTests.swift`'s two new tests pass, proving the new radii stay valid at the depth-scale floor
- Debug build succeeds with no new errors
</success_criteria>

<output>
Create `.planning/phases/71-island-corner-rounding/71-01-SUMMARY.md` when done
</output>
