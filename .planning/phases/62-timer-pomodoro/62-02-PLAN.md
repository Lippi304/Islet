---
phase: 62-timer-pomodoro
plan: 02
type: tdd
wave: 2
depends_on: ["62-01"]
files_modified:
  - Islet/Notch/TimerActivityState.swift
  - Islet/Notch/TimerMonitor.swift
  - IsletTests/TimerActivityStateTests.swift
autonomous: true
requirements: [TIMER-02, TIMER-04]

must_haves:
  truths:
    - "Pausing a running timer captures the exact remaining interval and stops advancing; resuming recomputes a fresh deadline from that captured remaining interval — no drift."
    - "Add-time (+1 min) works identically whether the session is running (extends the live deadline) or paused (extends the captured remaining interval) — D-09 + A5."
    - "Resetting restarts the CURRENT segment (work or break) at its own full configured duration — never the other segment's duration, never touching cycle count."
    - "When a Pomodoro segment's deadline is reached, the state holder atomically both (a) reports a completion splash for the segment that just ended and (b) has ALREADY advanced its own internal state to the next segment's live countdown — D-11's 'begins right away', not a two-step process."
  artifacts:
    - path: "Islet/Notch/TimerActivityState.swift"
      provides: "session mutation state holder: start/pause/resume/reset/addMinute/stop/handleDeadlineReached, testable now: overloads"
      contains: "final class TimerActivityState: ObservableObject"
    - path: "Islet/Notch/TimerMonitor.swift"
      provides: "one-shot DispatchSourceTimer deadline scheduler, mirrors CalendarCountdownMonitor's cancel-then-reschedule discipline"
      contains: "final class TimerMonitor"
  key_links:
    - from: "Islet/Notch/TimerActivityState.swift (handleDeadlineReached)"
      to: "Islet/Notch/TimerActivity.swift (TimerActivity.segmentDone / nextPhase)"
      via: "return (.segmentDone(finishedPhase:, cycle:), next)"
      pattern: "handleDeadlineReached"
---

<objective>
Build the STATEFUL session layer on top of Plan 62-01's pure `TimerActivity` contract: `TimerActivityState` (the `@Published` holder owning pause/resume/reset/add-time/stop mutation logic per D-08/D-09/D-10, plus the Pomodoro auto-advance-on-completion logic per D-11/D-06) and `TimerMonitor` (the one-shot deadline scheduler that fires a callback at the right instant, mirroring `CalendarCountdownMonitor`'s proven cancel-then-reschedule pattern — never a repeating poll).

Purpose: Give Plan 62-04's controller wiring a fully-tested, deterministic mutation API to drive — every method with a `now:`-parameterized testable overload, no live `Date()` reads inside the mutation logic itself.
Output: `TimerActivityState.swift` + `TimerActivityStateTests.swift`, `TimerMonitor.swift`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-timer-pomodoro/62-CONTEXT.md
@.planning/phases/62-timer-pomodoro/62-RESEARCH.md
@.planning/phases/62-timer-pomodoro/62-PATTERNS.md
</context>

<interfaces>
<!-- TimerActivity/TimerMode/TimerPhase/TimerContext from Plan 62-01 — the contract this plan builds against. -->

From Islet/Notch/TimerActivity.swift (created by Plan 62-01):
```swift
enum TimerMode: Equatable { case countdown, pomodoro }
enum TimerPhase: Equatable { case work, breakTime }
struct TimerContext: Equatable { let mode: TimerMode; let phase: TimerPhase?; let cycle: Int? }
enum TimerActivity: Equatable {
    case running(deadline: Date, context: TimerContext)
    case paused(remaining: TimeInterval, context: TimerContext)
    case completed
    case segmentDone(finishedPhase: TimerPhase, cycle: Int)
}
extension TimerActivity { var isRunningOrPaused: Bool { ... } }
func nextPhase(after phase: TimerPhase) -> TimerPhase
```

From Islet/Notch/CalendarCountdownMonitor.swift (the one-shot deadline scheduling analog — cite verbatim for the arm/cancel discipline):
```swift
private func armTimer(at date: Date) {
    let t = DispatchSource.makeTimerSource(queue: .main)
    t.schedule(deadline: .now() + max(0, date.timeIntervalSinceNow))
    t.setEventHandler { [weak self] in self?.recheck() }
    t.resume()
    timer = t
}
nonisolated func stop() {
    timer?.cancel()
    timer = nil
    ...
}
```

From Islet/Notch/DownloadCoordinator.swift (the testable-overload discipline to mirror on every mutation method):
```swift
func handle(_ reading: DownloadReading) { handle(reading, now: Date()) }
func handle(_ reading: DownloadReading, now: Date) { /* zero Date() calls in this body */ }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TimerActivityState.swift + TimerActivityStateTests.swift</name>
  <files>Islet/Notch/TimerActivityState.swift, IsletTests/TimerActivityStateTests.swift</files>
  <read_first>Islet/Notch/DownloadCoordinator.swift, IsletTests/DownloadCoordinatorTests.swift, Islet/Notch/ChargingActivityState.swift, Islet/Notch/TimerActivity.swift</read_first>
  <behavior>
    `@MainActor final class TimerActivityState: ObservableObject` with `@Published var activity: TimerActivity?` plus private session fields: `mode: TimerMode = .countdown`, `phase: TimerPhase = .work`, `cycle: Int = 1`, `workDuration: TimeInterval = 25*60`, `breakDuration: TimeInterval = 5*60`, `deadline: Date?`, `pausedRemaining: TimeInterval?`. A private `currentContext() -> TimerContext` helper returns `TimerContext(mode: .countdown, phase: nil, cycle: nil)` for countdown mode, `TimerContext(mode: .pomodoro, phase: phase, cycle: cycle)` for pomodoro.

    Public API, each with a `now:`-parameterized testable overload (public no-arg wrapper calls `Date()`, forwards to the overload; the overload itself never calls `Date()`):
    - `startCountdown(minutes: Int, now: Date)` — sets `mode = .countdown`, `cycle = 1`, `phase = .work`, `workDuration = TimeInterval(minutes * 60)`, `pausedRemaining = nil`, computes `deadline = now.addingTimeInterval(workDuration)`, sets `activity = .running(deadline:, context: currentContext())`.
    - `startPomodoro(workMinutes: Int, breakMinutes: Int, now: Date)` — same shape, `mode = .pomodoro`, sets both `workDuration`/`breakDuration`, starts at `phase = .work`, `cycle = 1`.
    - `pause(now: Date)` — no-op (`guard let d = deadline else { return }`) if not currently running; else captures `remaining = max(0, d.timeIntervalSince(now))`, sets `pausedRemaining = remaining`, `deadline = nil`, `activity = .paused(remaining:, context: currentContext())`.
    - `resume(now: Date)` — no-op if not currently paused (`guard let remaining = pausedRemaining else { return }`); else computes `deadline = now.addingTimeInterval(remaining)`, clears `pausedRemaining`, sets `activity = .running(deadline:, context: currentContext())`.
    - `reset(now: Date)` — no-op if `activity == nil`; else restarts the CURRENT segment (`phase == .work ? workDuration : breakDuration`) at full duration: `pausedRemaining = nil`, `deadline = now.addingTimeInterval(duration)`, `activity = .running(deadline:, context: currentContext())`. Does NOT touch `cycle`/`phase`/`mode`.
    - `addMinute(now: Date)` — D-09 (+60s per tap) + A5 (works while paused too): if `deadline` is non-nil, extend it by 60s and update `activity` to `.running` with the new deadline; else if `pausedRemaining` is non-nil, extend it by 60s and update `activity` to `.paused` with the new remaining value; else no-op.
    - `stop()` — D-10: full reset. `activity = nil`, `deadline = nil`, `pausedRemaining = nil`, `cycle = 1`, `phase = .work`. No `now:` needed (no Date read).
    - `handleDeadlineReached(now: Date) -> (splash: TimerActivity, next: TimerActivity?)` — called when the armed deadline fires. For `.countdown`: `deadline = nil`, `activity = .completed`, returns `(.completed, nil)`. For `.pomodoro`: capture `finishedPhase = phase` and `finishedCycle = cycle` BEFORE mutating; set `phase = nextPhase(after: finishedPhase)`; if `finishedPhase == .breakTime`, increment `cycle` (a new cycle begins once work resumes after a break — D-06/D-07); compute `nextDuration = phase == .work ? workDuration : breakDuration`, `nextDeadline = now.addingTimeInterval(nextDuration)`; set `deadline = nextDeadline`, `pausedRemaining = nil`, build `next = TimerActivity.running(deadline: nextDeadline, context: currentContext())`, set `activity = next`; return `(.segmentDone(finishedPhase: finishedPhase, cycle: finishedCycle), next)`. The caller (Plan 62-04's controller) is responsible for presenting `splash` as the immediate transient and enqueueing `next` behind it — this method's own job is ONLY to compute both values and leave `self.activity` as the authoritative "what's actually running now" state.

    Tests (mirrors `DownloadCoordinatorTests.swift`'s "no fakes, plain XCTAssert, testable `now:` overload" convention, all against a fixed base `let t0 = Date(timeIntervalSince1970: 1_700_000_000)`):
    - `testStartCountdownSetsRunningWithDeadlineTenMinutesOut` — `startCountdown(minutes: 10, now: t0)`, assert `activity == .running(deadline: t0.addingTimeInterval(600), context: TimerContext(mode: .countdown, phase: nil, cycle: nil))`.
    - `testStartPomodoroDefaultsToWorkPhaseCycleOne` — `startPomodoro(workMinutes: 25, breakMinutes: 5, now: t0)`, assert context is `.pomodoro`/`.work`/cycle 1 and deadline is `t0 + 1500`.
    - `testPauseCapturesRemainingAndClearsRunning` — start a 10-min countdown at `t0`, `pause(now: t0.addingTimeInterval(60))`, assert `activity == .paused(remaining: 540, context: ...)`.
    - `testPauseIsNoOpWhenNotRunning` — call `pause(now: t0)` with no active session, assert `activity == nil` still.
    - `testResumeRecomputesDeadlineFromRemaining` — start, pause at +60s (remaining 540), `resume(now: t0.addingTimeInterval(90))`, assert new deadline is `t0.addingTimeInterval(90 + 540)`.
    - `testResetRestartsCurrentSegmentAtFullDuration` — start a 10-min countdown, `reset(now: t0.addingTimeInterval(400))`, assert new deadline is `t0.addingTimeInterval(400 + 600)` (full duration from reset instant) and `activity` is `.running` (not paused, even if reset was called while paused — test both a running-then-reset and a paused-then-reset case).
    - `testAddMinuteExtendsDeadlineWhileRunning` — start 10-min countdown, `addMinute(now: t0)`, assert deadline moved from `t0+600` to `t0+660`.
    - `testAddMinuteExtendsRemainingWhilePaused` — start, pause (remaining 540), `addMinute(now: ...)`, assert `activity == .paused(remaining: 600, ...)`.
    - `testStopClearsActivityAndResetsSessionFields` — start a pomodoro session at cycle 2/`.breakTime`, `stop()`, assert `activity == nil`, then `startCountdown(minutes: 5, now: t0)` and confirm a FRESH session starts at cycle-independent state (countdown context has no cycle anyway, but assert no leaked pomodoro phase by checking a subsequent `startPomodoro` begins at `.work`/cycle 1).
    - `testHandleDeadlineReachedCountdownReturnsCompletedWithNoNext` — start 5-min countdown, `handleDeadlineReached(now: t0.addingTimeInterval(300))`, assert returned tuple is `(.completed, nil)` and `activity == .completed`.
    - `testHandleDeadlineReachedPomodoroWorkToBreakReturnsSegmentDoneAndAdvances` — start pomodoro (25/5) at `t0`, `handleDeadlineReached(now: t0.addingTimeInterval(1500))`, assert returned `splash == .segmentDone(finishedPhase: .work, cycle: 1)` and returned `next` is `.running(deadline: t0.addingTimeInterval(1500+300), context: TimerContext(mode: .pomodoro, phase: .breakTime, cycle: 1))`, and `activity == next`.
    - `testHandleDeadlineReachedPomodoroBreakToWorkIncrementsCycle` — continue from the prior state (now at `.breakTime`, cycle 1), `handleDeadlineReached(now: ...)` again, assert `splash == .segmentDone(finishedPhase: .breakTime, cycle: 1)` and `next`'s context is `.work`/cycle 2.
  </behavior>
  <action>
    RED: write `IsletTests/TimerActivityStateTests.swift` (`@MainActor final class TimerActivityStateTests: XCTestCase`, `import XCTest` + `@testable import Islet`) with the 12 test methods above, referencing `TimerActivityState` before it exists — confirm compile failure.
    GREEN: create `Islet/Notch/TimerActivityState.swift` with exactly the API in Behavior. File header comment states the testable-overload discipline explicitly (mirrors `DownloadCoordinator.swift`'s own header framing) and notes this class does NOT reach into `TransientQueue` itself — the controller (Plan 62-04) reads `.activity`/return values and pushes into the queue, mirroring `ChargingActivityState`'s "controller reads `@Published`, controller enqueues" convention (simpler than `DownloadCoordinator`'s closure-reach-back shape, since Timer's session state has no per-instance identity to track the way per-tempPath downloads do). Run tests, confirm green.
    REFACTOR: none expected.
  </action>
  <verify>
    <automated>Cmd-U in Xcode targeting IsletTests/TimerActivityStateTests.swift</automated>
  </verify>
  <acceptance_criteria>
    - `Islet/Notch/TimerActivityState.swift` exists, `@MainActor final class TimerActivityState: ObservableObject`.
    - `grep -c "func .*(now: Date)" Islet/Notch/TimerActivityState.swift` reports at least 7 (start x2, pause, resume, reset, addMinute, handleDeadlineReached).
    - `grep -n "Date()" Islet/Notch/TimerActivityState.swift` returns matches ONLY inside the no-arg public wrappers (e.g. `func pause() { pause(now: Date()) }`), never inside a `now:`-parameterized overload's body.
    - All 12 `TimerActivityStateTests.swift` methods pass via Cmd-U.
  </acceptance_criteria>
  <done>TimerActivityState.swift implements every D-08/D-09/D-10/D-11 mutation deterministically, all 12 tests pass, zero live Date() reads inside testable overload bodies.</done>
</task>

<task type="auto">
  <name>Task 2: TimerMonitor.swift — one-shot deadline scheduler</name>
  <files>Islet/Notch/TimerMonitor.swift</files>
  <read_first>Islet/Notch/CalendarCountdownMonitor.swift</read_first>
  <action>
    Create `Islet/Notch/TimerMonitor.swift`: `import Foundation`, `@MainActor final class TimerMonitor`. Holds `private let onFire: () -> Void` and `private nonisolated(unsafe) var timer: DispatchSourceTimer?`. `init(onFire: @escaping () -> Void)` stores the closure. Public method `func arm(at date: Date?)` — cancel-then-reschedule discipline mirroring `CalendarCountdownMonitor.scheduleNext`'s exact shape: `timer?.cancel(); timer = nil`; `guard let date else { return }` (a nil date, e.g. pause/stop, just cancels with no replacement — mirrors `CalendarCountdownMonitor`'s "no event -> no timer armed" branch); then build a `DispatchSource.makeTimerSource(queue: .main)`, `.schedule(deadline: .now() + max(0, date.timeIntervalSinceNow))`, `.setEventHandler { [weak self] in self?.onFire() }`, `.resume()`, assign to `timer`. `nonisolated func stop()` cancels and clears `timer` (mirrors `CalendarCountdownMonitor.stop()`, called from the owner's deinit). Empty `deinit` with the same "owner-driven teardown" comment `CalendarCountdownMonitor.deinit` carries. Unlike `CalendarCountdownMonitor`, this class has NO `start()`/`running` guard, no EventKit query, no notification observer — `arm(at:)` is idempotent by construction (every call cancels first), called by the controller (Plan 62-04) every time `TimerActivityState`'s deadline changes (start/resume/reset/add-time/segment-advance) or clears (pause/stop).
  </action>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `Islet/Notch/TimerMonitor.swift` exists, `@MainActor final class TimerMonitor`.
    - `grep -n "func arm(at date: Date?)" Islet/Notch/TimerMonitor.swift` returns 1 match.
    - `grep -n "nonisolated(unsafe) var timer" Islet/Notch/TimerMonitor.swift` returns 1 match.
    - Debug build succeeds with the new file present (no unresolved references).
    - No dedicated test file is created for this class — matches the existing project convention that `CalendarCountdownMonitor.swift` (the class this mirrors) also has no test file; its scheduling correctness is exercised indirectly via Plan 62-04's on-device UAT (TIMER-03).
  </acceptance_criteria>
  <done>TimerMonitor.swift compiles, arm(at:)/stop() follow the exact cancel-then-reschedule discipline CalendarCountdownMonitor already establishes in this codebase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan is pure state/timer-scheduling logic with no external input — durations arrive pre-validated from Plan 62-01's `validateCustomDurationMinutes` (consumed by Plan 62-03's UI) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-03 | Denial of Service (self) | `TimerMonitor.arm(at:)` | accept | `max(0, date.timeIntervalSinceNow)` guards against a negative deadline ever being passed to `DispatchSourceTimer.schedule` (which would otherwise fire immediately in a tight loop if misused) — no external input reaches this path, durations are bounded 1-180 min upstream |
</threat_model>

<verification>
Cmd-U pass on `TimerActivityStateTests.swift` (12 tests, all green). Debug build succeeds with `TimerMonitor.swift` present. Zero regressions in `IsletTests` overall.
</verification>

<success_criteria>
- `TimerActivityState` correctly implements every D-08/D-09/D-10/D-11 mutation, fully unit-tested with deterministic `now:` overloads.
- `TimerMonitor` correctly arms/cancels a one-shot deadline, mirroring the codebase's own proven scheduling pattern.
- Plan 62-04 can construct both classes and wire them to the controller with zero further changes to either file.
</success_criteria>

<output>
Create `.planning/phases/62-timer-pomodoro/62-02-SUMMARY.md` when done
</output>
