---
phase: 62-timer-pomodoro
plan: 04
type: execute
wave: 3
depends_on: ["62-02", "62-03"]
files_modified:
  - Islet/Notch/NotchWindowController.swift
autonomous: false
requirements: [TIMER-01, TIMER-02, TIMER-03, TIMER-04]

must_haves:
  truths:
    - "Tapping 'Start Timer' with a chosen duration makes the collapsed island show a live mm:ss countdown immediately, with zero further user action."
    - "Pause/Reset/Add-Time/Stop each take effect immediately and are correctly reflected in the collapsed pill the moment the island re-collapses."
    - "When the timer reaches zero, a completion splash + NSSound.beep() (D-12: standard system notification sound, not a custom chime) fire even if Islet is not the frontmost app; for Pomodoro, the SAME sound+splash treatment fires at every work<->break segment transition (D-13), not just on final stop, and the next segment's live countdown begins immediately with no second tap."
    - "A running timer, interrupted by a Charging/Device transient, resumes its live countdown afterward — proving TransientQueue.preempt()/ActiveTransient.isPersistent now generalizes beyond the original Focus-only case (SC5)."
  artifacts:
    - path: "Islet/Notch/NotchWindowController.swift"
      provides: "TimerActivityState/TimerMonitor construction, deadline-fire orchestration (NSSound.beep + dual-enqueue), Start/Pause/Reset/AddTime/Stop handlers, settings toggle gating, geometry Site 3 (.timerExpanded)"
      contains: "private func handleTimerDeadlineReached"
  key_links:
    - from: "Islet/Notch/NotchWindowController.swift (makeRootView)"
      to: "Islet/Notch/NotchPillView.swift (onStartCountdown/onStartPomodoro/onTimerPauseResume/onTimerReset/onTimerAddTime/onTimerStop)"
      via: "closure forwarding, mirrors onOnboardingNext/onQuickAdd's exact pattern"
      pattern: "onTimerPauseResume: \\{ \\[weak self\\]"
    - from: "Islet/Notch/NotchWindowController.swift (handleTimerDeadlineReached)"
      to: "Islet/Notch/IslandResolver.swift (TransientQueue.updateHead + preempt)"
      via: "updateHead(.timer(splash)) then preempt(.timer(next)) -- Pattern 4 dual-enqueue"
      pattern: "transientQueue\\.updateHead\\(\\.timer\\(splash\\)\\)"
---

<objective>
Wire `TimerActivityState`/`TimerMonitor` (Plan 62-02) into `NotchWindowController.swift`, forward `NotchPillView`'s (Plan 62-03) new closures to real handlers, complete the SC5 cleanup at the two EXISTING coordinator call sites (deviceCoordinator/downloadCoordinator's `enqueue` closures still re-derive the old `.focus`-only guard even though `preempt()` itself was already generalized in Plan 62-01), add the `.timerExpanded` geometry branch (CR-01 three-site rule, Site 3), and gate Timer via the existing `ActivitySettings.timerKey`/`handleSettingsChanged()` toggle-reconciliation pattern. Close with the phase's on-device UAT checkpoint covering all 5 ROADMAP success criteria plus 62-VALIDATION.md's 3 manual-only verifications.

Purpose: This is the plan that actually makes Timer/Pomodoro function end-to-end on real hardware — every prior plan produced tested-in-isolation pieces; this plan proves they compose correctly, including the one behavior no unit test can observe (a real `NSSound.beep()` firing while the app isn't focused).
Output: A fully working Timer/Pomodoro feature, on-device UAT-approved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-timer-pomodoro/62-CONTEXT.md
@.planning/phases/62-timer-pomodoro/62-RESEARCH.md
@.planning/phases/62-timer-pomodoro/62-PATTERNS.md
@.planning/phases/62-timer-pomodoro/62-VALIDATION.md
</context>

<interfaces>
<!-- TimerActivityState/TimerMonitor from Plan 62-02. -->
From Islet/Notch/TimerActivityState.swift (Plan 62-02):
```swift
@MainActor final class TimerActivityState: ObservableObject {
    @Published var activity: TimerActivity?
    private(set) var deadline: Date?   // nil while paused/idle, non-nil while running
    func startCountdown(minutes: Int)
    func startPomodoro(workMinutes: Int, breakMinutes: Int)
    func pause(); func resume(); func reset(); func addMinute(); func stop()
    func handleDeadlineReached() -> (splash: TimerActivity, next: TimerActivity?)
}
```
From Islet/Notch/TimerMonitor.swift (Plan 62-02):
```swift
@MainActor final class TimerMonitor {
    init(onFire: @escaping () -> Void)
    func arm(at date: Date?)   // nil cancels with no replacement
    nonisolated func stop()
}
```
<!-- New NotchPillView closures from Plan 62-03 this plan must forward. -->
From Islet/Notch/NotchPillView.swift (Plan 62-03):
```swift
var onStartCountdown: (Int) -> Void = { _ in }
var onStartPomodoro: (Int, Int) -> Void = { _, _ in }
var onTimerPauseResume: () -> Void = {}
var onTimerReset: () -> Void = {}
var onTimerAddTime: () -> Void = {}
var onTimerStop: () -> Void = {}
```

<!-- Existing call sites this plan modifies (current state, verbatim). -->
From Islet/Notch/NotchWindowController.swift (deviceCoordinator's enqueue closure, ~line 498-503):
```swift
enqueue: { [weak self] t in
    guard let self else { return false }
    if case .focus = self.transientQueue.head { return self.transientQueue.preempt(t) }
    return self.transientQueue.enqueue(t)
},
```
From Islet/Notch/NotchWindowController.swift (downloadCoordinator's enqueue closure, ~line 525-529): byte-identical shape to the above.

From Islet/Notch/NotchWindowController.swift (flushTransients' category machinery, ~line 2613-2646):
```swift
private enum TransientCategory { case charging, device, focus, osd, capsLock, updateAvailable, downloadProgress }
private func flushTransients(_ category: TransientCategory) {
    let oldHead = transientQueue.head
    let matches: (ActiveTransient) -> Bool = { t in
        switch (t, category) {
        case (.charging, .charging), (.device, .device), (.focus, .focus), (.osd, .osd),
             (.capsLock, .capsLock), (.updateAvailable, .updateAvailable),
             (.downloadProgress, .downloadProgress): return true
        default: return false
        }
    }
    transientQueue.removeAll(where: matches)
    switch category {
    case .charging: chargingState.activity = nil
    case .device: deviceCoordinator.clearPendingBatteryPolls()
    case .focus: break
    case .osd: break
    case .capsLock: break
    case .updateAvailable: break
    case .downloadProgress: break
    }
    guard transientQueue.head != oldHead else { return }
    dismissWorkItem?.cancel()
    if transientQueue.head != nil {
        deviceCoordinator.activityPromoted()
        scheduleActivityDismiss()
    }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Construction, closure wiring, settings gating, SC5 call-site cleanup</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>Islet/Notch/NotchWindowController.swift (lines 85-95 chargingState declaration, lines 170-180 downloadCoordinator declaration, lines 495-560 deviceCoordinator/downloadCoordinator construction, lines 2434-2486 makeRootView, lines 2492-2560 handleSettingsChanged, lines 2612-2653 flushTransients/TransientCategory)</read_first>
  <action>
    Declare `private let timerActivityState = TimerActivityState()` immediately after `private let chargingState = ChargingActivityState()` (~line 90). Declare `private var timerMonitor: TimerMonitor!` immediately after `private var downloadCoordinator: DownloadCoordinator!` (~line 177).
    Construct `timerMonitor` in the SAME init block as `downloadCoordinator` (immediately after its closing `)`, ~line 557): `timerMonitor = TimerMonitor { [weak self] in self?.handleTimerDeadlineReached() }` (the `handleTimerDeadlineReached()` method itself is built in Task 2 of this plan — this line references it forward, which compiles fine since it's a same-type instance method).
    SC5 cleanup — replace deviceCoordinator's `enqueue` closure body (`if case .focus = self.transientQueue.head { return self.transientQueue.preempt(t) } return self.transientQueue.enqueue(t)`) with the single line `return self.transientQueue.preempt(t)`. Apply the IDENTICAL replacement to downloadCoordinator's `enqueue` closure. This is safe and behavior-preserving for every existing category (Charging/Device/Focus/OSD/CapsLock/UpdateAvailable/Download all still resolve identically, since `preempt()`'s own internal `isPersistent` guard now covers exactly the same ground the old `.focus`-only check did, PLUS correctly displaces a `.downloadProgress(.inProgress)` head too — Pitfall 2's fix, now applied at the real call sites, not just inside `preempt()`'s own body).
    Add 6 new closures to `makeRootView`'s `NotchPillView(...)` construction, appended immediately after the existing `onQuickAdd:` argument (matching declared parameter order): `onStartCountdown: { [weak self] minutes in self?.handleStartCountdown(minutes: minutes) }`, `onStartPomodoro: { [weak self] work, brk in self?.handleStartPomodoro(workMinutes: work, breakMinutes: brk) }`, `onTimerPauseResume: { [weak self] in self?.handleTimerPauseResume() }`, `onTimerReset: { [weak self] in self?.handleTimerReset() }`, `onTimerAddTime: { [weak self] in self?.handleTimerAddTime() }`, `onTimerStop: { [weak self] in self?.handleTimerStop() }`.
    Add `.timer` to the `TransientCategory` enum (`case charging, device, focus, osd, capsLock, updateAvailable, downloadProgress, timer`). Add `(.timer, .timer): return true` to `flushTransients`'s `matches` switch (grouped with the other single-line cases). Add `case .timer: break` to `flushTransients`'s trailing switch (mirrors capsLock/updateAvailable's "no separate @Published model to clear here" — Timer's own session state lives in `timerActivityState`, which `handleTimerStop()` in Task 2 clears directly, not via this generic flush path).
    Add a Timer toggle-off block to `handleSettingsChanged()`, placed near the Update HUD block (~line 2540, both share the "no monitor to start/stop, but must force-stop a live session on toggle-off" shape): `if !activityEnabled(ActivitySettings.timerKey) { timerActivityState.stop(); timerMonitor.arm(at: nil); flushTransients(.timer) }`.
    Add `syncActivityModels()`'s new arm: `case .timer: chargingState.activity = nil` (mirrors every other transient case's "not charging -- no standing charging splash" comment).
  </action>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "private let timerActivityState = TimerActivityState()" Islet/Notch/NotchWindowController.swift` returns 1 match.
    - `grep -n "private var timerMonitor: TimerMonitor!" Islet/Notch/NotchWindowController.swift` returns 1 match.
    - `grep -n "if case .focus = self.transientQueue.head" Islet/Notch/NotchWindowController.swift` returns 0 matches (both old guards removed).
    - `grep -c "return self.transientQueue.preempt(t)" Islet/Notch/NotchWindowController.swift` returns at least 2 (deviceCoordinator + downloadCoordinator enqueue closures).
    - `grep -n "onTimerStop: { \[weak self\]" Islet/Notch/NotchWindowController.swift` returns 1 match inside `makeRootView`.
    - `grep -n "case .timer: break" Islet/Notch/NotchWindowController.swift` and `grep -n "case .timer, .timer" Islet/Notch/NotchWindowController.swift` (or equivalent tuple form) both present.
    - Debug build succeeds.
  </acceptance_criteria>
  <done>TimerActivityState/TimerMonitor are constructed and wired to every NotchPillView closure; the two pre-existing enqueue closures are simplified to the generalized preempt(t) call; Timer participates correctly in flushTransients/settings-toggle-off/syncActivityModels.</done>
</task>

<task type="auto">
  <name>Task 2: Handler bodies, deadline-fire orchestration, geometry Site 3</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>Islet/Notch/NotchWindowController.swift (lines 1607-1693 visibleContentZone, lines 2359-2395 scheduleActivityDismiss)</read_first>
  <action>
    Add `import AppKit` usage note: `NSSound` is already available since `Islet/Notch/NotchWindowController.swift` already `import AppKit` at the top of the file — no new import needed.
    Add handler methods (placed near `startDownloadMonitor()`/`focusPermissionGranted()`, ~line 850):
    `private func handleStartCountdown(minutes: Int)` — `guard activityEnabled(ActivitySettings.timerKey) else { return }`; `timerActivityState.startCountdown(minutes: minutes)`; call the shared `presentNewTimerSession()` helper below.
    `private func handleStartPomodoro(workMinutes: Int, breakMinutes: Int)` — same guard; `timerActivityState.startPomodoro(workMinutes: workMinutes, breakMinutes: breakMinutes)`; call `presentNewTimerSession()`.
    `private func presentNewTimerSession()` — `guard let activity = timerActivityState.activity else { return }`; `_ = transientQueue.preempt(.timer(activity))`; `timerMonitor.arm(at: timerActivityState.deadline)`; `presentTransientChange()` (the existing reach-back method — already wraps `renderPresentation()`/`updateVisibility()`/`scheduleActivityDismiss()`, mirrors exactly how `deviceCoordinator`/`downloadCoordinator`'s own `presentTransientChange` closure is used after their `enqueue` calls).
    `private func handleTimerPauseResume()` — `if case .paused = timerActivityState.activity { timerActivityState.resume() } else { timerActivityState.pause() }`; call `refreshTimerHeadInPlace()`.
    `private func handleTimerReset()` — `timerActivityState.reset()`; call `refreshTimerHeadInPlace()`.
    `private func handleTimerAddTime()` — `timerActivityState.addMinute()`; call `refreshTimerHeadInPlace()`.
    `private func refreshTimerHeadInPlace()` — `guard let activity = timerActivityState.activity else { return }`; `transientQueue.updateHead(.timer(activity))` (in-place refresh, mirrors the `(.osd, .osd)`/`(.downloadProgress, .downloadProgress)` precedent — a no-op unless the current head is already `.timer`, which it always is while these 3 buttons are reachable); `timerMonitor.arm(at: timerActivityState.deadline)` (re-arms for the new deadline after resume/reset/add-time, cancels after pause since `deadline` is nil while paused); `withAnimation(.spring(response: springResponse, dampingFraction: springDamping)) { renderPresentation() }`.
    `private func handleTimerStop()` — `timerActivityState.stop()`; `timerMonitor.arm(at: nil)`; `flushTransients(.timer)` (the generic category-flush path, already extended in Task 1 — handles the render/dismiss-rearm dance the same way every other Stop-equivalent action does).
    `private func handleTimerDeadlineReached()` — called by `timerMonitor`'s `onFire` closure (wired in Task 1). Body: `let (splash, next) = timerActivityState.handleDeadlineReached()`; `NSSound.beep()` (D-12/D-13 — plays the user's configured System Settings alert sound, zero permission/entitlement); `transientQueue.updateHead(.timer(splash))` (in-place: the running head is replaced by the non-persistent splash); `if let next { _ = transientQueue.preempt(.timer(next)) }` (Pattern 4's dual-enqueue: `preempt()`'s own `isPersistent` guard sees the just-set `.timer(splash)` head is NOT persistent, so this correctly falls through to plain `enqueue`, appending `next` to `pending` — `TransientQueue.advance()`'s existing generic mechanism promotes it automatically once the splash's shared ~3s dismiss timer elapses, giving D-11's "next segment begins right away" with ZERO new queue mechanics); `withAnimation(.spring(response: springResponse, dampingFraction: springDamping)) { renderPresentation() }`; `scheduleActivityDismiss()` (arms the shared ~3s dismiss for the non-persistent splash head — the existing `guard let head = transientQueue.head, !head.isPersistent else { return }` inside it already does the right thing unmodified); `timerMonitor.arm(at: timerActivityState.deadline)` (re-arms `timerMonitor`'s OWN deadline for the segment now running "underneath" the splash — `nil` for a completed plain countdown, correctly cancelling with no replacement).
    Geometry Site 3 — add a new `else if` branch to `visibleContentZone()`, inserted after the existing `.calendarExpanded` branch and before the final `else`: `} else if case .timerExpanded = presentationState.presentation { contentSize = CGSize(width: expandedSize.width, height: expandedSize.height) }` — mirrors `NotchPillView.timerExpandedContent`'s own `blobShape` call from Plan 62-03 Task 2 (no height override, no switcher row, so the base `expandedSize` applies exactly, same as the final `else` branch's own non-output-panel case). Do NOT add a new union member to `positionAndShow()` (Site 2) — the existing `expandedFrame` union already reserves `NotchPillView.switcherContentHeight` (196pt), taller than `expandedSize.height` (144pt), so `.timerExpanded`'s footprint is already covered without any new reservation.
  </action>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "private func handleTimerDeadlineReached" Islet/Notch/NotchWindowController.swift` returns 1 match.
    - `grep -n "NSSound.beep()" Islet/Notch/NotchWindowController.swift` returns exactly 1 match.
    - `grep -n "transientQueue.updateHead(.timer(splash))" Islet/Notch/NotchWindowController.swift` returns 1 match.
    - `grep -n "transientQueue.preempt(.timer(next))" Islet/Notch/NotchWindowController.swift` returns 1 match.
    - `grep -n "case .timerExpanded = presentationState.presentation" Islet/Notch/NotchWindowController.swift` returns 1 match inside `visibleContentZone()`.
    - Debug build succeeds with 0 warnings referencing unused `timerActivityState`/`timerMonitor`.
  </acceptance_criteria>
  <done>Every NotchPillView closure has a real handler; a Pomodoro segment transition correctly self-elapses its splash and auto-promotes the next running segment with zero extra taps; visibleContentZone correctly sizes the click-through region for .timerExpanded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device UAT — all 5 ROADMAP success criteria</name>
  <files></files>
  <read_first>.planning/ROADMAP.md "Phase 62: Timer/Pomodoro" Success Criteria (5 items), .planning/phases/62-timer-pomodoro/62-VALIDATION.md Manual-Only Verifications table</read_first>
  <action>
No file edits in this task — build and run Islet on-device (Cmd-R in Xcode) and perform the 10-step
on-device checklist described in how-to-verify below. This task's only output is the recorded
approved/failed verdict covering all 5 ROADMAP Phase 62 Success Criteria plus 62-VALIDATION.md's 3
manual-only verifications (completion splash+sound while unfocused, expanded-priority-over-Calendar,
SC5's Charging/Device interruption resume).
  </action>
  <what-built>
    A complete Timer/Pomodoro feature: "Start Timer" button in Home (all 3 sub-states), a Countdown/Pomodoro duration picker with preset+custom chips, a live collapsed-pill countdown (+ Pomodoro cycle label), expanded pause/reset/add-time/stop controls, a completion splash with system sound at every countdown-end and every Pomodoro segment transition, and the generalized `TransientQueue.preempt()`/`ActiveTransient.isPersistent` (SC5) proven against a real Charging/Device interruption.
  </what-built>
  <how-to-verify>
    Build and run the Debug configuration on-device. Confirm Settings > Activities has "Timer" toggled ON (default OFF per SETTINGS-05 — turn it on for this checklist).
    1. Hover the collapsed island, click to expand Home, tap "Start Timer". Confirm the picker shows a Countdown/Pomodoro segmented control, 5/10/20/30 min chips, and a "Custom…" option.
    2. Select "Custom…", type "200", tap the filled checkmark Start button — confirm it does NOT start and shows "Enter a number between 1 and 180." inline (V5 validation).
    3. Correct to "1" minute, tap Start — confirm the island collapses to show a live mm:ss countdown ticking down from 01:00 with no further interaction (TIMER-01).
    4. Expand the island — confirm 4 circular buttons appear (Pause/Reset/Add-Time/Stop), no chip-style buttons, no switcher row. Tap Pause — countdown freezes; the pause button's glyph flips to a play icon. Tap Resume — countdown continues from where it froze (not reset). Tap Add Time twice — confirm the displayed remaining time jumps up by 2 minutes total. Tap Reset — confirm it returns to the FULL original duration, not the pre-reset remaining value (TIMER-02).
    5. Let a fresh 10-second custom countdown reach zero while Islet is NOT the frontmost app (click into another app first) — confirm a completion splash briefly appears in the collapsed pill AND the system alert sound plays (TIMER-03).
    6. Start a Pomodoro session with Work=1 min / Break=1 min (via Custom). Confirm the collapsed pill shows "Work · Cycle 1" + live mm:ss with a small green dot. Let it reach zero — confirm a "Work Done" splash + sound fires, then WITHOUT any tap the pill automatically shows "Break · Cycle 1" ticking down. Let that reach zero — confirm "Break Done" splash + sound, then the pill automatically shows "Work · Cycle 2" (TIMER-04, D-06/D-07/D-11/D-13).
    7. Start a fresh countdown, then plug in or unplug the charger (or connect/disconnect a Bluetooth device) to trigger a Charging/Device splash — confirm it correctly interrupts the Timer display, and once the Charging/Device splash's own ~3s dismiss elapses, the Timer's live countdown resumes showing correctly (not stuck, not reset) — this is the literal SC5 proof (ROADMAP Success Criterion 5).
    8. Tap Stop mid-session — confirm the island returns cleanly to idle/Home, with no lingering Timer state on a subsequent "Start Timer" (fresh session starts at Cycle 1).
    9. Toggle Settings > Activities > Timer OFF while a session is running — confirm the session is force-stopped and the "Start Timer" button disappears from Home; toggle back ON — confirm it reappears and a fresh session can start.
    10. Start a fresh countdown, then switch to the Calendar tab (still running underneath), then expand the island — confirm the Timer's expanded pause/reset/add-time/stop controls are shown, NOT the Calendar month grid (Resolved Open Question 1: Timer's expanded controls take priority over an active Calendar/Weather/Tray tab selection).
  </how-to-verify>
  <resume-signal>Type "approved" once all 10 steps pass, or describe which step failed and what was observed instead</resume-signal>
  <done>All 10 on-device checklist steps confirmed; all 5 ROADMAP Phase 62 Success Criteria and TIMER-01..04 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan only wires already-validated values (Plan 62-01's `validateCustomDurationMinutes`, Plan 62-02's deterministic state mutations) into the controller — no new external input crosses a boundary here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-05 | Tampering | `TransientQueue.preempt()`/`enqueue()` call-site simplification (deviceCoordinator/downloadCoordinator) | accept | Pure in-memory queue mutation, no external input; behavior-preserving for every pre-existing category, verified by Plan 62-01's regression test plus this plan's own on-device Charging/Device interruption check (Task 3, step 7) |
</threat_model>

<verification>
Debug build succeeds. Full manual Cmd-U pass across `IsletTests` (zero regressions from Plans 62-01/62-02's new/extended tests). On-device UAT (Task 3) approved covering all 5 ROADMAP success criteria and all 3 62-VALIDATION.md manual-only verifications.
</verification>

<success_criteria>
- All 4 phase requirements (TIMER-01..04) and ROADMAP Success Criterion 5 (SC5) are demonstrated working on real hardware.
- Zero regressions in any pre-existing transient category (Charging/Device/Focus/OSD/CapsLock/UpdateAvailable/Download) from the `preempt()`/`enqueue()` call-site simplification.
- REQUIREMENTS.md's Traceability table can mark TIMER-01..04 Complete after this plan's on-device approval.
</success_criteria>

<output>
Create `.planning/phases/62-timer-pomodoro/62-04-SUMMARY.md` when done
</output>
