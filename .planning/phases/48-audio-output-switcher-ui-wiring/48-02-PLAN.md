---
phase: 48-audio-output-switcher-ui-wiring
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - Islet/Notch/NotchPillView.swift
autonomous: true
requirements: [OUTPUT-01, OUTPUT-02, OUTPUT-03]

must_haves:
  truths:
    - "The right reserved 28x28 slot in mediaContent's control row (line 2872) is a real speaker-icon TransportButton that calls onToggleOutputPanel — tapping it reveals the panel, re-tapping it closes the panel (D-08, symmetric toggle)."
    - "The panel shows a thick draggable Capsule-track volume slider (OutputVolumeSlider, D-03's visual language copied from OSDLevelBar) with no live numeric/percentage readout (D-04) — dragging computes a 0...1 fraction from the drag position and forwards it via onVolumeChange."
    - "When presentationState.outputHasVolumeControl is false, the slider stays visible but is dimmed/non-interactive (D-06) rather than hidden."
    - "The panel shows a vertical list of presentationState.outputDevices (already sorted, default-first) with an accent-tinted checkmark next to the current device (D-05) and no other row highlighting; tapping a non-current row calls onSelectOutputDevice(device) and does NOT close the panel (D-07)."
    - "The device list container animates row-order changes (a spring, mirroring OSDLevelBar's response:0.15/dampingFraction:0.86 starting point) so the newly-selected device visibly slides to the top (D-02/OUTPUT-03) purely as a result of presentationState.outputDevices changing — no separate reorder-animation code path."
    - "tabHeight's default case (Home/NowPlaying group) grows by a new outputPanelExtraHeight constant when presentationState.outputPanelOpen is true, and is unchanged (170, the existing regression-locked value) when false — NotchPillViewTests.testTabWidthHeightMatchesKnownPerCaseValues must still pass unmodified."
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "Speaker-icon button, draggable OutputVolumeSlider, outputPanel(devices:) list subview, 3 new closures, tabHeight height bump"
      contains: "onToggleOutputPanel"
  key_links:
    - from: "Islet/Notch/NotchPillView.swift speaker TransportButton"
      to: "onToggleOutputPanel closure"
      via: "Button(action:) forwarding, mirrors every other TransportButton call site"
      pattern: "TransportButton\\(systemName: \"speaker"
    - from: "Islet/Notch/NotchPillView.swift outputPanel(devices:) row .onTapGesture"
      to: "onSelectOutputDevice(device) closure"
      via: "direct closure call with the tapped AudioOutputDevice value (UID-identified, never AudioDeviceID)"
      pattern: "onSelectOutputDevice\\(device\\)"
    - from: "Islet/Notch/NotchPillView.swift OutputVolumeSlider drag gesture"
      to: "onVolumeChange(Float) closure"
      via: "DragGesture(minimumDistance: 0).onChanged, clamped fraction from value.location.x / geo.size.width"
      pattern: "onVolumeChange\\("
    - from: "Islet/Notch/NotchPillView.swift tabHeight default case"
      to: "presentationState.outputPanelOpen"
      via: "ternary height addition, CR-01 geometry three-site Site 1 (Plan 48-03 mirrors this exact boolean at Site 2/Site 3)"
      pattern: "outputPanelExtraHeight"
---

<objective>
Wire the real speaker-icon button, a genuinely draggable volume slider, and the tap-to-select
device list into `NotchPillView.mediaContent`'s reserved right slot — the SwiftUI-only, AppKit-free
half of this phase's UI. Declares the 3 new closures (`onToggleOutputPanel`,
`onSelectOutputDevice`, `onVolumeChange`) Plan 48-03's controller will forward, reads the 4
`presentationState.output*` fields Plan 48-01 just added, and bumps `tabHeight` (Site 1 of the
geometry three-site rule) — Plan 48-03 mirrors this exact height math at Site 2
(`positionAndShow`) and Site 3 (`visibleContentZone()`).

Purpose: This is the entire visible, interactive surface OUTPUT-01/02/03 describe. No CoreAudio
code lives in this file (AppKit-free convention preserved) — every tap/drag only reports intent via
a closure, matching this file's existing `onClick`/`onTogglePlayPause`/`onSwitcherSelect` pattern.
Output: A real speaker button, `OutputVolumeSlider`, `outputPanel(devices:)`, and the height bump —
build-verified; Plan 48-03 wires the closures to real CoreAudio behavior next.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-audio-output-switcher-ui-wiring/48-CONTEXT.md
@.planning/phases/48-audio-output-switcher-ui-wiring/48-PATTERNS.md
@.planning/phases/48-audio-output-switcher-ui-wiring/48-01-SUMMARY.md
</context>

<interfaces>
<!-- Exact analog code this plan's new View code must mirror. Already read in full at planning time. -->

From `Islet/Notch/NotchPillView.swift:2904-2924` (`TransportButton` — reuse directly, do not
reimplement):
```swift
private struct TransportButton: View {
    let systemName: String
    let action: () -> Void
    @State private var isHovering = false
    var body: some View {
        Button(action: action) {
            Image(systemName: systemName)
                .font(.system(size: 16, weight: .semibold))
                .foregroundStyle(.white)
                .frame(width: 28, height: 28)
                .background(RoundedRectangle(cornerRadius: 8, style: .continuous)
                    .fill(isHovering ? Color.white.opacity(0.40) : Color.clear))
        }
        .frame(width: 32, height: 32)
        .buttonStyle(.plain)
        .onHover { isHovering = $0 }
    }
}
```

From `Islet/Notch/NotchPillView.swift:2862-2873` (`mediaContent`'s control row — the RIGHT
`Color.clear.frame(width: 28, height: 28)` at line 2872 becomes the speaker button; LEFT slot at
line 2864 is untouched, out of scope):
```swift
HStack(spacing: 0) {
    Color.clear.frame(width: 28, height: 28)   // reserved Shuffle slot (D-09, not built)
    Spacer()
    TransportButton(systemName: "backward.fill", action: onPrevious)
    Spacer()
    TransportButton(systemName: "playpause.fill", action: onTogglePlayPause)
    Spacer()
    TransportButton(systemName: "forward.fill", action: onNext)
    Spacer()
    Color.clear.frame(width: 28, height: 28)   // becomes: TransportButton(systemName: "speaker.wave.2.fill", action: onToggleOutputPanel)
}
```

From `Islet/Notch/NotchPillView.swift:3055-3068` (`OSDLevelBar` — the visual language
`OutputVolumeSlider` copies, D-03; `private` to this file, display-only, has no gesture today):
```swift
private struct OSDLevelBar: View {
    let fraction: CGFloat
    let tint: Color
    var body: some View {
        GeometryReader { geo in
            ZStack(alignment: .leading) {
                Capsule().fill(Color.white.opacity(0.15))
                Capsule().fill(tint).frame(width: geo.size.width * fraction)
                    .animation(.spring(response: 0.15, dampingFraction: 0.86), value: fraction)
            }
        }
    }
}
```

From `Islet/Notch/NotchPillView.swift:184-186` (closure declaration style to extend, immediately
after `onPrevious`):
```swift
var onTogglePlayPause: () -> Void = {}
var onNext: () -> Void = {}
var onPrevious: () -> Void = {}
```

From `Islet/Notch/NotchPillView.swift:94-109` (`tabWidth`/`tabHeight` — the SINGLE site controlling
`blobShape`'s per-tab height; `default` covers `.nowPlayingExpanded`/`.homeLastPlayed`/`.homeEmpty`,
the ONLY cases this plan's height bump touches):
```swift
var tabHeight: CGFloat {
    switch presentation {
    case .calendarExpanded: return Self.calendarContentHeight
    case .trayExpanded: return Self.trayContentHeight
    case .weatherExpanded: return weatherStyle == .large ? Self.weatherLargeContentHeight : Self.weatherMediumContentHeight
    default: return Self.homeContentHeight
    }
}
```

From `Islet/Notch/NotchPillView.swift:656` (`homeContentHeight` constant, and its neighboring
`switcherContentHeight`/`onboardingSize`/`traySize` constants at lines 642-680 — the "size once, a
'for now' number, tune on-device" convention `outputPanelExtraHeight` follows):
```swift
static let homeContentHeight: CGFloat = 170
```

From `Islet/Notch/IslandPresentationState.swift` (Plan 48-01's output — read directly, do not
re-derive):
```swift
@Published var outputPanelOpen: Bool = false
@Published var outputDevices: [AudioOutputDevice] = []
@Published var outputCurrentVolumeFraction: CGFloat = 0
@Published var outputHasVolumeControl: Bool = true
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: New closures + speaker-icon button</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift lines 163-230 (the full closure-declaration block — onClick through onQuickAdd — the exact style/placement convention to extend)
    Islet/Notch/NotchPillView.swift lines 2826-2896 (mediaContent — the control-row HStack at 2862-2873 where the speaker button replaces the right reserved slot)
    Islet/Notch/AudioOutputPresentation.swift (whole file — AudioOutputDevice, the payload type onSelectOutputDevice carries)
  </read_first>
  <action>
    In the closure-declaration block, immediately after `var onPrevious: () -> Void = {}` (line
    186), add 3 new closures mirroring the exact declaration style (doc comment naming Phase
    48/OUTPUT-01..04, "the view stays AppKit-free, only REPORTS intent" framing, default no-op so
    #Preview blocks keep compiling): `var onToggleOutputPanel: () -> Void = {}`, `var
    onSelectOutputDevice: (AudioOutputDevice) -> Void = { _ in }`, `var onVolumeChange: (Float) ->
    Void = { _ in }`.

    In `mediaContent`'s control-row `HStack` (lines 2862-2873), replace the RIGHT reserved slot
    (`Color.clear.frame(width: 28, height: 28)` at line 2872, "reserved Repeat slot") with
    `TransportButton(systemName: "speaker.wave.2.fill", action: onToggleOutputPanel)`. Update its
    trailing comment to note the slot is now real (Phase 48/D-08) and the Repeat placeholder that
    used to live there is dropped (never built, matches the Star/favorite precedent already noted
    in this function's own header comment). Leave the LEFT reserved slot (line 2864, future
    Shuffle) completely untouched.
  </action>
  <acceptance_criteria>
    - `grep -c "var onToggleOutputPanel: () -> Void = {}" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "var onSelectOutputDevice: (AudioOutputDevice) -> Void = { _ in }" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "var onVolumeChange: (Float) -> Void = { _ in }" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "TransportButton(systemName: \"speaker.wave.2.fill\", action: onToggleOutputPanel)" Islet/Notch/NotchPillView.swift` returns 1
    - `cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5` ends with `** BUILD SUCCEEDED **`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <done>3 new closures declared with default no-ops; the right reserved slot is a real speaker-icon TransportButton wired to onToggleOutputPanel; left reserved slot untouched; Debug build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: OutputVolumeSlider + outputPanel(devices:) + tabHeight height bump</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift lines 3020-3068 (OSDLevelBar's whole body — the exact Capsule/GeometryReader visual to copy for OutputVolumeSlider, plus its neighboring EqualizerBars for general View-struct-placement convention in this file)
    Islet/Notch/NotchPillView.swift lines 94-109 (tabHeight — the default case this task's height bump modifies)
    Islet/Notch/NotchPillView.swift lines 640-680 (switcherContentHeight/homeContentHeight/onboardingSize/traySize constants block — where the new outputPanelExtraHeight constant is added, matching this block's "size once, tune on-device" doc-comment convention)
    Islet/Notch/NotchPillView.swift lines 2826-2896 (mediaContent — where the conditional outputPanel(devices:) reveal is inserted into the VStack, after the control-row HStack)
    .planning/phases/48-audio-output-switcher-ui-wiring/48-CONTEXT.md decisions D-02 through D-07 (every visual/interaction rule this task implements)
  </read_first>
  <action>
    Add a new private struct `OutputVolumeSlider: View` near `OSDLevelBar` (same file region),
    copying its `GeometryReader { ZStack(alignment: .leading) { Capsule()... } }` body verbatim
    (D-03: reuse the visual style, not the component — `OSDLevelBar` itself stays untouched, used
    only by the OSD wing). Give it properties `let fraction: CGFloat`, `let tint: Color`, `let
    enabled: Bool`, `let onChange: (Float) -> Void`. Thicken it relative to `OSDLevelBar`'s 5pt wing
    usage — this is a standalone control, not a thin wing bar (D-03 "scaled up/thicker"); pick a
    concrete height (e.g. in the 10-14pt range) and document it as a starting value, tune on-device,
    matching this codebase's own established convention for first-pass UI constants (see
    `onboardingSize`'s doc comment: "still a 'for now' number, not a final one"). When `enabled` is
    `false`, apply `.opacity(0.35)` (or similar dimming) to the whole view and skip attaching any
    gesture (D-06 — no numeric readout exists to disable, fill-only per D-04, so dimming +
    non-interactivity is the entire disabled treatment). When `enabled` is `true`, attach `.gesture(
    DragGesture(minimumDistance: 0).onChanged { value in let clamped = max(0, min(1,
    Float(value.location.x / geo.size.width))); onChange(clamped) })` inside the `GeometryReader`
    closure (needs the geo proxy in scope) — no separate onEnded handling required, every
    intermediate drag position forwards live (D-04's "no live numeric readout" governs the TEXT
    label, not the live-updating fill itself, which must track the drag in real time per OUTPUT-01
    "real time").

    Add a new private method `outputPanel(devices: [AudioOutputDevice]) -> some View` (or
    `@ViewBuilder private var outputPanel: some View` reading `presentationState.outputDevices`
    directly — either is acceptable, prefer reading `presentationState` directly since every other
    new piece in this file already does). Body: a `VStack` containing (a) the `OutputVolumeSlider`
    fed `fraction: presentationState.outputCurrentVolumeFraction`, `tint: nowPlayingAccent` (the
    existing `@Environment(\.nowPlayingAccent)` this file already reads for other Now-Playing
    elements), `enabled: presentationState.outputHasVolumeControl`, `onChange: onVolumeChange`; (b)
    a device list built via `ForEach(presentationState.outputDevices) { device in ... }` (works
    directly — `AudioOutputDevice` is already `Identifiable` via `uid`), each row an `HStack`
    showing the device name (`Text(device.name)`, white, matching this view's existing text
    styling conventions) plus, ONLY when `device.isDefault`, an accent-tinted `Image(systemName:
    "checkmark")` (D-05 — single-accent-signal, no full-row background highlight), with
    `.onTapGesture { onSelectOutputDevice(device) }` on the whole row (D-07: this closure call is
    the row's ENTIRE tap behavior — do not also mutate `outputPanelOpen` here, the panel must stay
    open). Apply `.animation(.spring(response: 0.15, dampingFraction: 0.86), value:
    presentationState.outputDevices)` to the `ForEach`'s containing `VStack` (D-02/OUTPUT-03 — this
    single modifier is what makes the re-sorted list visibly animate the selected device to the top
    when `presentationState.outputDevices` changes; no separate reorder-animation code is needed
    beyond this).

    In `mediaContent`'s body `VStack` (after the control-row `HStack`, lines 2862-2873), add `if
    presentationState.outputPanelOpen { outputPanel(devices: presentationState.outputDevices) }` (or
    the equivalent if you chose the `@ViewBuilder private var` shape above) so the panel only
    renders when open.

    In the geometry-constants block (near `homeContentHeight`, line 656), add `static let
    outputPanelExtraHeight: CGFloat = 140` (or your own reasoned estimate — device-list rows at
    ~24-28pt each for a realistic small device count, plus the slider's own height, plus VStack
    spacing; document it exactly like `homeContentHeight`'s own comment: a reasoned first-pass
    number, NOT final, to be confirmed on-device). Change `tabHeight`'s `default` case (line 107)
    to `default: return Self.homeContentHeight + (presentationState.outputPanelOpen ?
    Self.outputPanelExtraHeight : 0)` — this is Site 1 of the CR-01 geometry three-site rule; Plan
    48-03 mirrors this exact `presentationState.outputPanelOpen` read at Site 2
    (`positionAndShow`'s panel-frame union) and Site 3 (`visibleContentZone()`'s matching branch).
  </action>
  <acceptance_criteria>
    - `grep -c "private struct OutputVolumeSlider: View" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "DragGesture(minimumDistance: 0)" Islet/Notch/NotchPillView.swift` returns 1 (the new gesture; OSDLevelBar itself must stay gesture-free)
    - `grep -c "func outputPanel(devices:\|var outputPanel:" Islet/Notch/NotchPillView.swift` returns 1 or more
    - `grep -c "onTapGesture { onSelectOutputDevice(device) }" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "static let outputPanelExtraHeight: CGFloat" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "default: return Self.homeContentHeight + (presentationState.outputPanelOpen ? Self.outputPanelExtraHeight : 0)" Islet/Notch/NotchPillView.swift` returns 1
    - `cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5` ends with `** BUILD SUCCEEDED **`
    - `xcodebuild test -scheme Islet -destination 'platform=macOS' -only-testing:IsletTests/NotchPillViewTests/testTabWidthHeightMatchesKnownPerCaseValues 2>&1 | tail -20` — run manually per this project's documented `xcodebuild test` headless-hang precedent (Cmd-U in Xcode is the reliable path); must still pass since `presentationState.outputPanelOpen` defaults `false` in every existing test construction.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <done>OutputVolumeSlider is genuinely draggable and reuses OSDLevelBar's visual language, dims/disables when outputHasVolumeControl is false, outputPanel(devices:) renders the sorted list with a D-05 checkmark and D-07 stay-open tap behavior plus a D-02/OUTPUT-03 reorder animation, tabHeight's Site 1 height bump is in place, Debug build green, existing tabHeight regression test unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| User tap/drag input -> SwiftUI closures | The only inputs this file's new code accepts: a button tap (no payload), a device-row tap (an already-vetted `AudioOutputDevice` value sourced from `presentationState.outputDevices`, itself sourced from local CoreAudio, not user-typed), and a drag gesture's `CGFloat` location (converted to a clamped `Float` fraction). No network, no file I/O, no shell invocation anywhere in this file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-48-04 | Tampering | OutputVolumeSlider's DragGesture handler | mitigate | The drag fraction is computed as `max(0, min(1, ...))` before ever leaving this view via `onChange` — a drag past the slider's bounds (fast mouse movement, multi-monitor edge cases) can never forward an out-of-range value; Plan 48-01's `VolumeReader.setSystemVolume(_:)` re-clamps defensively again on the receiving end (defense in depth). |
| T-48-05 | Denial of Service (UI availability) | tabHeight's Site-1 height bump | mitigate | This plan's `outputPanelExtraHeight` addition is the FIRST of 3 required identical reads of `presentationState.outputPanelOpen` (CR-01 geometry three-site rule) — Plan 48-03 must mirror this exact boolean at Sites 2/3 or the panel renders visually while becoming unclickable past the old, un-bumped bounds (the exact regression class Weather/Tray already hit and fixed). Flagged explicitly in this plan's task action and must-haves for the next plan to close. |

No Spoofing / Information Disclosure / Repudiation / Elevation-of-Privilege entries — this file
stays entirely AppKit/CoreAudio-free (the established convention every other `NotchPillView`
element already follows); it only renders already-vetted local data and reports intent via
closures.
</threat_model>

<verification>
- `xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug` succeeds after both tasks.
- `NotchPillViewTests.testTabWidthHeightMatchesKnownPerCaseValues` still passes (manual Cmd-U per this project's `xcodebuild test` headless-hang precedent) — confirms the tabHeight height bump does not regress the existing Home/NowPlaying 170pt assertion.
- Full interactive behavior (real drag-to-volume, real tap-to-select, real live device-list updates) is exercised end-to-end once Plan 48-03 wires the closures to real CoreAudio calls — this plan's own gate is build-green + the grep-based structural checks above.
</verification>

<success_criteria>
- The speaker-icon button and draggable slider exist and are visually correct (ROADMAP Phase 48 Success Criterion 1 — UI half).
- The device list panel renders sorted/checkmarked devices (Success Criterion 2).
- Tapping a row re-sorts with a visible animation (Success Criterion 3 — UI half; the actual system-output switch is Plan 48-03).
- `tabHeight` Site 1 is ready for Plan 48-03's Site 2/Site 3 geometry mirror.
</success_criteria>

<output>
Create `.planning/phases/48-audio-output-switcher-ui-wiring/48-02-SUMMARY.md` when done
</output>
