---
phase: 70-file-tray-convert-button
plan: 03
type: execute
wave: 2
depends_on: ["70-01"]
files_modified:
  - Islet/Notch/NotchWindowController.swift
autonomous: true
requirements: [D-01, D-03, D-04, D-05, D-06]

must_haves:
  truths:
    - "Releasing on a DIMMED/disabled button (AirDrop/Mail when unavailable, or Convert when the batch has a non-image) never dispatches that button's handler — fixed for ALL 4 main-row buttons, not just Convert (D-06)."
    - "Releasing on the enabled Convert chip opens the format-tile stage WITHOUT converting or dismissing anything yet (D-01 — not a commit)."
    - "Releasing on a format tile converts every item in the pending drop's batch to that ONE chosen format and lands the converted copies in Tray via shelfCoordinator.append, the same call Drop already uses (D-03/D-04)."
    - "Releasing inside the card but not on a format tile, during the format stage, discards the pending drop and force-collapses — identical to the existing main-row off-target-release behavior (D-13 precedent)."
  artifacts:
    - path: "Islet/Notch/NotchWindowController.swift"
      provides: "handleQuickActionConvert(to:), isConvertEnabled, D-06-gated handleDragApproachEnd() dispatch, stage-flag resets"
  key_links:
    - from: "handleDragApproachEnd()"
      to: "presentationState.isShowingConvertFormats"
      via: "stage-first hit-test branch"
      pattern: "isShowingConvertFormats"
    - from: "handleQuickActionConvert(to:)"
      to: "ImageConversionService.convert"
      via: "per-item conversion before landing"
      pattern: "ImageConversionService\\.convert\\("
    - from: "handleQuickActionConvert(to:)"
      to: "shelfCoordinator.append"
      via: "same landing call handleQuickActionDrop() already uses"
      pattern: "shelfCoordinator\\.append\\(convertedItem\\)"
---

<objective>
Wire the controller side of Convert: fix the dormant D-06 bug (a release on a dimmed button
still fires its handler today, for AirDrop/Mail — confirmed via grep, zero controller-side gate
exists) for all 4 main-row buttons, add the Convert tap → format-tile-stage transition, add the
format-tile release → real conversion-and-landing flow, and make sure the new
`isShowingConvertFormats` stage flag resets correctly on every dismiss/discard path so a stale
stage never leaks into the next drop.

Purpose: this is the ONLY place a release point is hit-tested and dispatched — every other piece
(the pure conversion function, the dimmed-chip rendering) already exists from Plans 70-01/70-02;
this plan makes the release actually DO the right thing, including the first-ever real gate on a
sometimes-false `enabled` flag this component has had (RESEARCH.md Pitfall 3).
Output: `handleDragApproachEnd()` branches on stage before index, `handleQuickActionConvert(to:)`
converts-then-lands the whole batch, `computeQuickActionButtonFrames`'s call site is updated to
the Plan 70-01 signature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/70-file-tray-convert-button/70-CONTEXT.md
@.planning/phases/70-file-tray-convert-button/70-RESEARCH.md

<interfaces>
<!-- From Plan 70-01, already merged before this plan runs: -->
<!-- func computeQuickActionButtonFrames(card: CGRect, count: Int) -> [CGRect] -->
<!-- enum ImageFormat: CaseIterable { case jpg, png, heic, tiff; var fileExtension: String { ... } } -->
<!-- ImageConversionService.convert(_ sourceURL: URL, to: ImageFormat, destinationURL: URL) throws -->
<!-- ImageConversionService.isImageFile(_ url: URL) -> Bool -->
<!-- IslandPresentationState.isShowingConvertFormats: Bool (default false) -->

<!-- IslandResolver.swift (unchanged, read-only context): -->
<!-- struct PendingDrop: Equatable { let items: [ShelfItem] } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-06 gate fix + Convert stage-entry dispatch + format-tile hit-test branch</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift lines 133-149 (pendingDrop/quickActionButtonFrames
    declarations — add the new isConvertEnabled computed property and airDropAvailable/
    mailAvailable constants near here)
    Islet/Notch/NotchWindowController.swift lines 1441-1446 (quickActionPickerFrame +
    quickActionButtonFrames = computeQuickActionButtonFrames(card: quickActionPickerFrame) —
    the stale 1-arg call site Plan 70-01 deliberately left untouched)
    Islet/Notch/NotchWindowController.swift lines 1583-1629 (handleDragApproachEnd() — the
    FULL current release hit-test dispatch: switch hit { case 0: handleQuickActionDrop(); case
    1: handleQuickActionAirDrop(); case 2: handleQuickActionMail(); default: break }, with zero
    enabled-gate today — confirmed via grep, this IS the D-06 bug)
  </read_first>
  <action>
    Near pendingDrop's declaration (~line 139), add two private constants mirroring
    NotchPillView's own defaults exactly (34-RESEARCH.md's D-09 fallback rationale — both are
    HIGH-confidence "no fallback needed", never dynamically toggled today): `private let
    airDropAvailable = true` and `private let mailAvailable = true`. Add a computed property
    `private var isConvertEnabled: Bool { pendingDrop?.items.allSatisfy {
    ImageConversionService.isImageFile($0.originalURL) } ?? false }` (D-05 — mirrors the same
    expression Plan 70-02 uses in the view, computed independently on this side since the
    controller needs its own gate at hit-test time, never a value threaded from the view).
    At line 1446, change `quickActionButtonFrames = computeQuickActionButtonFrames(card:
    quickActionPickerFrame)` to `quickActionButtonFrames = computeQuickActionButtonFrames(card:
    quickActionPickerFrame, count: 4)` — a single call suffices for both stages (main row and
    format-tile row are both exactly 4 equal-width items in the identical card), so no second
    computeQuickActionButtonFrames call site is needed anywhere, simplifying RESEARCH.md
    Pitfall 4's original two-call assumption. In handleDragApproachEnd()'s hit-test block
    (currently `if let hit = quickActionButtonFrames.firstIndex(where: { $0.contains(point) })
    { switch hit { case 0: handleQuickActionDrop() ... } }`), restructure so the stage is
    checked FIRST: if presentationState.isShowingConvertFormats is true, a hit index within
    ImageFormat.allCases.indices calls handleQuickActionConvert(to:
    ImageFormat.allCases[hit]) (implemented in Task 2 of this plan); otherwise (main-row
    stage), keep the existing switch hit but gate cases 1/2/3 behind their enabled flag:
    `case 0: handleQuickActionDrop()`, `case 1: if airDropAvailable {
    handleQuickActionAirDrop() }`, `case 2: if mailAvailable { handleQuickActionMail() }`,
    `case 3: if isConvertEnabled { presentationState.isShowingConvertFormats = true }`, `default:
    break`. Entering the format stage (case 3) must NOT call discardPendingDrop() or
    dismissExpandedImmediately() — D-01 is explicit that choosing Convert is not a commit.
    In the existing off-target-release else branch (release inside the card but not on any
    recognized frame), add `presentationState.isShowingConvertFormats = false` alongside the
    existing discardPendingDrop()/dismissExpandedImmediately() calls, so a release off-target
    during either stage resets cleanly (D-13 precedent, now applies to both stages identically).
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -configuration Debug -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "computeQuickActionButtonFrames(card: quickActionPickerFrame, count: 4)" Islet/Notch/NotchWindowController.swift returns 1.
    - grep -c "private let airDropAvailable = true" Islet/Notch/NotchWindowController.swift returns 1; same check for mailAvailable.
    - grep -c "private var isConvertEnabled: Bool" Islet/Notch/NotchWindowController.swift returns 1.
    - grep -c "if airDropAvailable { handleQuickActionAirDrop() }" Islet/Notch/NotchWindowController.swift returns 1 (D-06 gate present for AirDrop).
    - grep -c "if mailAvailable { handleQuickActionMail() }" Islet/Notch/NotchWindowController.swift returns 1 (D-06 gate present for Mail).
    - grep -c "if isConvertEnabled { presentationState.isShowingConvertFormats = true }" Islet/Notch/NotchWindowController.swift returns 1.
    - grep -c "presentationState.isShowingConvertFormats" Islet/Notch/NotchWindowController.swift returns at least 3 (stage-entry set, stage-check branch, off-target reset — Task 2 adds more).
  </acceptance_criteria>
  <done>D-06 fixed for all 4 main-row buttons, Convert tap enters the format stage without committing, computeQuickActionButtonFrames call site updated to count:4, off-target release resets the stage flag.</done>
</task>

<task type="auto">
  <name>Task 2: handleQuickActionConvert(to:) — real conversion-and-landing flow + stage-flag resets on every dismiss path</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift lines 1662-1715 (handleQuickActionDrop(),
    finishQuickActionSharing(), discardPendingDrop() — the exact shapes to mirror: for-loop
    over pendingDrop?.items, shelfCoordinator.append/ShelfFileStore.deleteSessionCopy,
    resyncShelfViewState(), viewSwitcherState.selectedView = .tray, pendingDrop = nil,
    dismissExpandedImmediately())
    Islet/Shelf/ShelfFileStore.swift lines 20-29 (the .-..-empty filename-safety guard AND
    the IsletShelf/<uuid>/ directory-staging convention — reuse the SAME staging convention
    for the converted file's destination, never call makeSessionCopy itself for the converted
    output since it only does a byte copy, RESEARCH.md Pitfall 2)
    Islet/Shelf/ShelfItem.swift (the exact ShelfItem initializer shape: id, originalURL,
    localURL, filename, addedAt)
    Islet/Notch/NotchWindowController.swift lines 1532-1559 (recheckDragAcceptRegion's arm
    branch — where a fresh pendingDrop gets populated; add a defensive stage reset here too)
  </read_first>
  <action>
    Add `private func handleQuickActionConvert(to format: ImageFormat)`: guard let pendingDrop
    else return. For each item in pendingDrop.items: construct a fresh UUID, build itemDir at
    NSTemporaryDirectory()/IsletShelf/&lt;uuid&gt;/ (the same staging convention
    ShelfFileStore.makeSessionCopy uses, not a call to that function itself — it cannot
    transcode), try? FileManager.default.createDirectory(at: itemDir,
    withIntermediateDirectories: true), skip this item (continue) on failure. Derive baseName
    from (item.filename as NSString).deletingPathExtension; guard !baseName.isEmpty &&
    baseName != "." && baseName != ".." else continue (T-70-02's filename-safety guard,
    mirrors ShelfFileStore's exact .-..-empty check). Build newFilename = baseName + "." +
    format.fileExtension and destinationURL = itemDir.appendingPathComponent(newFilename).
    Call try? ImageConversionService.convert(item.originalURL, to: format, destinationURL:
    destinationURL); on failure, continue (skip-and-continue per 70-UI-SPEC.md's resolved
    Open Question 2 — a failed item is silently dropped from the batch, successes still land).
    On success, construct ShelfItem(id: id, originalURL: item.originalURL, localURL:
    destinationURL, filename: newFilename, addedAt: Date()) and call
    shelfCoordinator.append(convertedItem) — the same landing call handleQuickActionDrop()
    already uses (D-04). After the loop, clean up the ORIGINAL (unconverted) session-temp copies
    via ShelfFileStore.deleteSessionCopy(at: item.localURL) for every item in
    pendingDrop.items (mirrors finishQuickActionSharing()'s cleanup shape — these originals
    were never landed). Then: resyncShelfViewState(), viewSwitcherState.selectedView =
    .tray, self.pendingDrop = nil, presentationState.isShowingConvertFormats = false,
    dismissExpandedImmediately(). Separately, add presentationState.isShowingConvertFormats =
    false to discardPendingDrop() (alongside its existing pendingDrop = nil, so every path
    that discards also resets the stage) AND to recheckDragAcceptRegion()'s arm branch, right
    where a fresh pendingDrop gets populated (defensive reset — a brand-new drop must always
    start on the main row, never inherit a stale stage from a previous interrupted drop).
  </action>
  <verify>
    <automated>xcodebuild test -scheme Islet -destination 'platform=macOS' -only-testing:IsletTests/ImageConversionServiceTests -only-testing:IsletTests/DragDropSupportTests</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "private func handleQuickActionConvert(to format: ImageFormat)" Islet/Notch/NotchWindowController.swift returns 1.
    - grep -c "ImageConversionService.convert(item.originalURL, to: format, destinationURL: destinationURL)" Islet/Notch/NotchWindowController.swift returns 1.
    - grep -c "shelfCoordinator.append(convertedItem)" Islet/Notch/NotchWindowController.swift returns 1.
    - grep -c "ShelfFileStore.deleteSessionCopy(at: item.localURL)" Islet/Notch/NotchWindowController.swift returns at least 2 (existing finishQuickActionSharing/discardPendingDrop call plus the new one in handleQuickActionConvert).
    - grep -c "presentationState.isShowingConvertFormats = false" Islet/Notch/NotchWindowController.swift returns at least 2 (handleQuickActionConvert's own reset, discardPendingDrop's reset — recheckDragAcceptRegion's defensive reset is a 3rd).
    - Full project build succeeds: xcodebuild build -scheme Islet -configuration Debug -destination 'platform=macOS' exits 0, zero "error:" lines.
    - Full test suite green: xcodebuild test -scheme Islet -destination 'platform=macOS' reports zero NEW failures beyond this project's already-documented pre-existing 6 (LicenseStateTests x4, SettingsViewTests x2).
  </acceptance_criteria>
  <done>handleQuickActionConvert converts every item in the batch via ImageConversionService, lands successes in the Tray via shelfCoordinator.append, cleans up original session copies, and every dismiss/discard path resets isShowingConvertFormats so no stage state leaks across drops.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Release-point hit-test → handler dispatch | The D-06 boundary: a dimmed/disabled chip must never be a valid dispatch target regardless of its screen-space frame still existing |
| item.filename → converted destination path | Locally-dropped filename, still defensively validated before path construction (V5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-04 | Tampering | handleDragApproachEnd()'s hit-test dispatch (the D-06 bug itself) | mitigate | ALL 4 main-row cases (not just Convert) gated behind their `enabled` boolean at hit-test time — a release on a dimmed chip is now a no-op, matching the view's own dim-never-hide rendering |
| T-70-05 | Tampering | Converted filename path construction in handleQuickActionConvert | mitigate | `.`/`..`/empty guard applied to the derived `baseName` before any path component use, mirroring ShelfFileStore.makeSessionCopy's exact guard |
| T-70-06 | Denial of Service | Per-item conversion failure mid-batch | mitigate | `try?` + `continue` (skip-and-continue) — one corrupt/unreadable image in a multi-file batch never aborts or crashes the whole conversion |
</threat_model>

<verification>
Full Debug build green. `xcodebuild test -scheme Islet -destination 'platform=macOS' -only-testing:IsletTests/ImageConversionServiceTests -only-testing:IsletTests/DragDropSupportTests` green after each task. Full suite (`xcodebuild test -scheme Islet -destination 'platform=macOS'`) green (excluding this project's 6 pre-existing documented failures) before Plan 70-04's on-device checkpoint.
</verification>

<success_criteria>
D-06 fixed for all 4 main-row buttons; Convert tap opens the format stage without committing; a format-tile release converts and lands the whole batch via the exact Drop-landing mechanism; every dismiss/discard path leaves isShowingConvertFormats correctly reset for the next drop.
</success_criteria>

<output>
Create `.planning/phases/70-file-tray-convert-button/70-03-SUMMARY.md` when done
</output>
