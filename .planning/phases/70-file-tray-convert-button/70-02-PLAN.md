---
phase: 70-file-tray-convert-button
plan: 02
type: execute
wave: 2
depends_on: ["70-01"]
files_modified:
  - Islet/Notch/NotchPillView.swift
autonomous: true
requirements: [D-01, D-02, D-05]

must_haves:
  truths:
    - "A 4th 'Convert' chip renders in the main Quick Action row, using the same quickActionButton component as Drop/AirDrop/Mail (D-01)."
    - "Convert renders dimmed (enabled: false) whenever the pending drop contains ANY non-image item, computed fresh every render from PendingDrop.items — never a stored/threaded Bool (D-05, Pattern 3)."
    - "When the controller-owned isShowingConvertFormats flag is true, the SAME card shows 4 format tiles (JPG/PNG/HEIC/TIFF) instead of the main row, with no card resize (D-01)."
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "quickActionButtonRow(_:) 4th Convert chip, formatTileRow(), quickActionPickerView(pendingDrop:) stage branch"
  key_links:
    - from: "quickActionButtonRow(_ pendingDrop:)"
      to: "ImageConversionService.isImageFile"
      via: "pendingDrop.items.allSatisfy"
      pattern: "ImageConversionService\\.isImageFile"
    - from: "quickActionPickerView(pendingDrop:)"
      to: "presentationState.isShowingConvertFormats"
      via: "Group { if / else }"
      pattern: "isShowingConvertFormats"
    - from: "presentationSwitch's .quickActionPicker case"
      to: "quickActionPickerView(pendingDrop:)"
      via: "case .quickActionPicker(let pendingDrop):"
      pattern: "case \\.quickActionPicker\\(let pendingDrop\\)"
---

<objective>
Add the Convert button and its 2-step format-tile row to the Quick Action Destination Picker's
view layer — purely rendering, zero controller/dispatch logic (that is Plan 70-03's job, running
in parallel this same wave since it touches a different file with no overlap).

Purpose: gives the user the visible 4th Convert chip (D-01) and the 4-tile JPG/PNG/HEIC/TIFF
format step (D-02), with the dim-never-hide enable rule (D-05) computed inline from the already-
available PendingDrop payload — never a new stored Bool, which is exactly the sync-gap bug class
D-06 (Plan 70-03) is separately fixing.
Output: `NotchPillView.swift`'s Quick Action Picker renders 4 main-row buttons and, on stage
change, 4 format tiles — both reusing the existing `quickActionButton` component verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/70-file-tray-convert-button/70-CONTEXT.md
@.planning/phases/70-file-tray-convert-button/70-UI-SPEC.md

<interfaces>
<!-- From Plan 70-01, already merged before this plan runs: -->
<!-- enum ImageFormat: CaseIterable { case jpg, png, heic, tiff; var label: String { ... } } -->
<!-- ImageConversionService.isImageFile(_ url: URL) -> Bool -->
<!-- IslandPresentationState.isShowingConvertFormats: Bool (default false) -->

<!-- IslandResolver.swift (unchanged by this plan, read-only context): -->
<!-- struct PendingDrop: Equatable { let items: [ShelfItem] } -->
<!-- case quickActionPicker(PendingDrop) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bind PendingDrop at the render switch, add the Convert chip to the main row</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift lines 1113-1116 (the `presentationSwitch`'s `case
    .quickActionPicker:` arm — currently discards the associated `PendingDrop` value)
    Islet/Notch/NotchPillView.swift lines 2320-2357 (`quickActionButtonRow()` and
    `quickActionButton(icon:label:enabled:isHovered:)` — the exact current 3-button row and the
    reusable chip component, verbatim, zero changes needed to `quickActionButton` itself)
    Islet/Notch/NotchPillView.swift lines 419-424 (`airDropAvailable`/`mailAvailable` — confirmed
    dead/never-wired from the controller; do NOT add a matching `var convertAvailable: Bool`
    stored property — this is the exact D-06 bug class, Pitfall 3 of 70-RESEARCH.md)
  </read_first>
  <action>
    At `presentationSwitch`'s `case .quickActionPicker:` (currently discards the payload), bind
    the associated value: `case .quickActionPicker(let pendingDrop): quickActionPickerView(pendingDrop: pendingDrop)`.
    Change `quickActionPickerView()`'s signature to `quickActionPickerView(pendingDrop:
    PendingDrop) -> some View` (body changes land in Task 2 of this plan). Change
    `quickActionButtonRow()`'s signature to `quickActionButtonRow(_ pendingDrop: PendingDrop) ->
    some View`, keep the existing Drop/AirDrop/Mail `quickActionButton` calls at indices 0/1/2
    completely unchanged, and add a 4th call: `quickActionButton(icon:
    "arrow.triangle.2.circlepath", label: "Convert", enabled: pendingDrop.items.allSatisfy {
    ImageConversionService.isImageFile($0.originalURL) }, isHovered:
    presentationState.hoveredQuickActionButtonIndex == 3)` — same `HStack(spacing: 16)`, same
    fixed `quickActionButtonWidth` (130pt), no new geometry. The `enabled:` expression MUST be
    computed inline exactly as shown (D-05, Pattern 3) — never assigned to a new stored `var` on
    this struct.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -configuration Debug -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "case .quickActionPicker(let pendingDrop):" Islet/Notch/NotchPillView.swift` returns 1.
    - `grep -c "func quickActionButtonRow(_ pendingDrop: PendingDrop)" Islet/Notch/NotchPillView.swift` returns 1.
    - `grep -c "label: \"Convert\"" Islet/Notch/NotchPillView.swift` returns 1.
    - `grep -c "var convertAvailable" Islet/Notch/NotchPillView.swift` returns 0 (must not reproduce the dead-flag pattern).
    - `grep -c "pendingDrop.items.allSatisfy { ImageConversionService.isImageFile" Islet/Notch/NotchPillView.swift` returns 1 (enable state computed inline, not stored).
    - Debug build compiles cleanly (this task alone will NOT yet compile standalone since `quickActionPickerView`'s body still calls the old no-arg `quickActionButtonRow()` — Task 2 in this SAME plan completes the signature threading; verify the FULL plan's build at Task 2, not after Task 1 alone).
  </acceptance_criteria>
  <done>Render switch binds PendingDrop, quickActionButtonRow accepts it and renders 4 chips with Convert's enabled state computed fresh from the payload every render.</done>
</task>

<task type="auto">
  <name>Task 2: Format-tile row + 2-stage branch inside quickActionPickerView</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift lines 2050-2075 (`quickActionPickerView()`'s current body —
    one `blobShape` call wrapping `quickActionButtonRow()` with `.padding(.top,
    Self.cameraClearance).padding(.horizontal, 24)`)
    Islet/Notch/NotchPillView.swift line ~1988 comment referencing the "Group{if/else}
    .padding(.top, cameraClearance)" pattern already proven elsewhere in this file — reuse that
    exact shape here, do not invent a different conditional-rendering structure
  </read_first>
  <action>
    Add a new private view function `formatTileRow() -> some View`: an `HStack(spacing: 16)`
    that iterates `ImageFormat.allCases` (via `Array(ImageFormat.allCases.enumerated())`,
    `id: \.offset`) and calls `quickActionButton(icon: "photo", label: format.label, enabled:
    true, isHovered: presentationState.hoveredQuickActionButtonIndex == index)` for each — same
    shared icon (`photo`) for all 4 tiles per 70-UI-SPEC.md's resolved Open Question 1, label
    text is the only differentiator, all 4 tiles always enabled (the gate already happened at
    the main row's Convert chip). Change `quickActionPickerView(pendingDrop:)`'s body to wrap a
    `Group { if presentationState.isShowingConvertFormats { formatTileRow() } else {
    quickActionButtonRow(pendingDrop) } }` INSIDE the existing `blobShape` call, with the SAME
    `.padding(.top, Self.cameraClearance).padding(.horizontal, 24)` modifiers applied to the
    `Group`, not per-branch. `Self.traySize.width` and `Self.quickActionPickerContentHeight`
    stay completely unchanged (D-01: no new card geometry for either stage).
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -configuration Debug -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func formatTileRow" Islet/Notch/NotchPillView.swift` returns 1.
    - `grep -c "ImageFormat.allCases" Islet/Notch/NotchPillView.swift` returns 1.
    - `grep -c "presentationState.isShowingConvertFormats" Islet/Notch/NotchPillView.swift` returns 1 (the Group's if-condition).
    - `grep -c "icon: \"photo\"" Islet/Notch/NotchPillView.swift` returns 1 (one shared icon reference inside formatTileRow's loop, not 4 separate calls).
    - Full Debug build succeeds: `xcodebuild build -scheme Islet -configuration Debug -destination 'platform=macOS'` exits 0.
    - `xcodebuild build -scheme Islet -configuration Debug -destination 'platform=macOS' 2>&1 | grep -c "error:"` returns 0.
  </acceptance_criteria>
  <done>quickActionPickerView renders quickActionButtonRow or formatTileRow based on isShowingConvertFormats, both inside the identical unchanged card geometry, full project builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| PendingDrop.items → Convert enable state | Render-only computation, no new trust boundary — reuses Plan 70-01's already-guarded isImageFile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-03 | Tampering | Convert's `enabled:` sync-gap risk (D-06 bug class, view-side) | mitigate | Computed inline every render directly from `pendingDrop.items` — no stored/threaded `Bool` that could ever drift out of sync with the controller (this is the exact pattern that made `airDropAvailable`/`mailAvailable` silently dead) |
</threat_model>

<verification>
Full Debug build green with the new Convert chip and format-tile row present. On-device visual/interaction confirmation happens in Plan 70-04 once Plan 70-03's controller wiring lands alongside this plan.
</verification>

<success_criteria>
NotchPillView renders a 4th Convert chip (dimmed per-drop-content) and, on stage flip, 4 format tiles — reusing quickActionButton verbatim, zero new stored Bool, zero card geometry change.
</success_criteria>

<output>
Create `.planning/phases/70-file-tray-convert-button/70-02-SUMMARY.md` when done
</output>
