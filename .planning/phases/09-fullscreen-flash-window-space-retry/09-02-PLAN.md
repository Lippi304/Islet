---
phase: 09-fullscreen-flash-window-space-retry
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
# Wave-2 branch note: this plan executes ONLY if 09-01-SUMMARY.md recorded option-continue.
# Standard wave/depends_on frontmatter cannot express "run this only if a prior plan's on-device
# decision matches X" — the actual enforcement is Task 0 below, which reads 09-01-SUMMARY.md and
# hard-fails this plan as a no-op (zero file changes) if the recorded decision was option-accept.
# If 09-01 recorded option-accept, FS-01 is already resolved and 09-02/09-03/09-04/09-05 never run.
files_modified:
  - Islet/Notch/NotchPanel.swift
  - IsletTests/NotchPanelTests.swift
autonomous: false
requirements: [FS-01]
user_setup: []

must_haves:
  truths:
    - "This plan executes ONLY when 09-01-SUMMARY.md recorded option-continue — a Task-0 guard hard-fails this plan as a no-op otherwise, writing its own SUMMARY.md recording the halt"
    - "The .canJoinAllSpaces flag is removed from NotchPanel.collectionBehavior as a DISTINCT, separately-tested follow-up — the CGSSpace membership from 09-01 becomes the only remaining all-Spaces mechanism, never combining this removal with any other Candidate C change in the same trial (RESEARCH.md Anti-Patterns / Open Question 2)"
    - "testPanelJoinsAllSpacesAboveFullscreenAux is rewritten to reflect the new invariant (fullScreenAuxiliary still present; canJoinAllSpaces intentionally absent) rather than left silently failing"
    - "A single option-accept/option-proceed-to-b decision is recorded on-device, backed by the same full D-03/D-07/Pitfall-3 suite as 09-01"
  artifacts:
    - path: "Islet/Notch/NotchPanel.swift"
      provides: "collectionBehavior with .canJoinAllSpaces removed, .fullScreenAuxiliary/.stationary retained"
      contains: "collectionBehavior = [.fullScreenAuxiliary, .stationary]"
    - path: "IsletTests/NotchPanelTests.swift"
      provides: "testPanelJoinsAllSpacesAboveFullscreenAux rewritten for the no-canJoinAllSpaces invariant"
      contains: "func testPanelJoinsAllSpacesAboveFullscreenAux"
  key_links:
    - from: "09-02-PLAN.md Task 0"
      to: "09-01-SUMMARY.md"
      via: "precondition guard reads the recorded option-accept/option-continue decision before any file is touched"
      pattern: "option-(accept|continue)"
---

<!--
PRECONDITION — READ BEFORE EXECUTING:
This plan executes ONLY if 09-01-PLAN.md's recorded decision was option-continue. If 09-01's
SUMMARY.md recorded option-accept, FS-01 is already resolved — DO NOT execute this plan or any of
09-03/09-04/09-05. Task 0 below is the machine-checkable enforcement, not just this prose warning.
-->

<objective>
Wave 2 of Phase 9 (executes ONLY if 09-01 recorded option-continue): attempt the second and final
configuration of Candidate C — remove `.canJoinAllSpaces` from `NotchPanel.collectionBehavior`,
relying solely on the dedicated max-level CGSSpace (added in 09-01, left untouched here) for
all-Spaces visibility. Per RESEARCH.md's Anti-Patterns guidance and Open Question 2, this is a
DISTINCT, separately on-device-tested experiment — it changes exactly one variable relative to
09-01's additive attempt, never two at once, so the regression suite can correctly attribute cause.

No reference implementation (`Ebullioscopic/Atoll`, `TheBoredTeam/boring.notch`) tests this
specific combination — it is untested territory beyond this project's own on-device verification.

Purpose: exhaust both configurations of the Candidate C architecture direction before falling back
to Candidate B (per D-06 — Candidate C's failure, once both variants are tried, is itself the
trigger to proceed to Candidate B automatically, no separate human checkpoint required).

Output: `NotchPanel.collectionBehavior` with `.canJoinAllSpaces` removed, `NotchPanelTests.swift`
updated to match, and a recorded option-accept/option-proceed-to-b decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-fullscreen-flash-window-space-retry/09-CONTEXT.md
@.planning/phases/09-fullscreen-flash-window-space-retry/09-RESEARCH.md
@.planning/phases/09-fullscreen-flash-window-space-retry/09-01-SUMMARY.md

<interfaces>
<!-- Current state (as left by 09-01) of the two files this plan touches. -->

From `Islet/Notch/NotchPanel.swift` line 32 (unchanged by 09-01):
```swift
collectionBehavior = [.canJoinAllSpaces, .fullScreenAuxiliary, .stationary] // ISL-02: all Spaces, above fullscreen-aux
```
Target state for this plan:
```swift
collectionBehavior = [.fullScreenAuxiliary, .stationary] // FS-01/Phase 9 (D-XX): all-Spaces visibility now comes from the dedicated CGSSpace (NotchWindowController.notchSpace), not this flag — see CGSSpace.swift
```

From `IsletTests/NotchPanelTests.swift` lines 38-44 (unchanged by 09-01):
```swift
func testPanelJoinsAllSpacesAboveFullscreenAux() {
    let panel = makePanel()
    XCTAssertTrue(panel.collectionBehavior.contains(.canJoinAllSpaces),
                  "ISL-02: the island must be visible across all Spaces.")
    XCTAssertTrue(panel.collectionBehavior.contains(.fullScreenAuxiliary),
                  "ISL-02: the island must sit above fullscreen-auxiliary content.")
}
```
Target state: remove the `.canJoinAllSpaces` assertion (all-Spaces visibility is now a CGSSpace
responsibility, verified on-device in Task 2's checklist, not a `collectionBehavior` unit-test
concern); keep the `.fullScreenAuxiliary` assertion; add a comment explaining the invariant shift.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Precondition guard — verify 09-01 recorded option-continue</name>
  <files>.planning/phases/09-fullscreen-flash-window-space-retry/09-01-SUMMARY.md</files>
  <read_first>
    .planning/phases/09-fullscreen-flash-window-space-retry/09-01-SUMMARY.md (the recorded
    option-accept/option-continue decision this entire plan is gated on — MUST be read before
    touching either file in this plan's `files_modified`)
  </read_first>
  <action>
    Read `09-01-SUMMARY.md` and extract the recorded decision (search for the literal string
    `option-accept` or `option-continue`). This plan executes ONLY when the recorded decision is
    `option-continue`.

    If the recorded decision is `option-accept`: HALT IMMEDIATELY. Do not proceed to Task 1 or
    Task 2. Do not modify `NotchPanel.swift` or `NotchPanelTests.swift`. Write
    `.planning/phases/09-fullscreen-flash-window-space-retry/09-02-SUMMARY.md` stating verbatim:
    "09-02-PLAN.md precondition failed: 09-01-SUMMARY.md recorded option-accept (Candidate C
    additive already resolved FS-01). This plan is a no-op — FS-01 is closed, no further plans in
    this phase execute." Then stop execution of this plan entirely.

    If the recorded decision is `option-continue`: proceed to Task 1.
  </action>
  <acceptance_criteria>
    - `09-01-SUMMARY.md` has been read and its recorded decision identified before any other file in this plan is touched
    - If `option-accept` was recorded: this plan halts with zero modifications to `Islet/Notch/NotchPanel.swift` or `IsletTests/NotchPanelTests.swift`, and `09-02-SUMMARY.md` records the halt verbatim as specified above
    - If `option-continue` was recorded: execution proceeds to Task 1
  </acceptance_criteria>
  <verify>
    <automated>grep -oE "option-(accept|continue)" .planning/phases/09-fullscreen-flash-window-space-retry/09-01-SUMMARY.md | head -1</automated>
  </verify>
  <done>Either the plan has halted with the exact option-accept mismatch message and no code changed, or the option-continue decision is confirmed and Task 1 begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Remove .canJoinAllSpaces as the distinct follow-up experiment</name>
  <files>Islet/Notch/NotchPanel.swift, IsletTests/NotchPanelTests.swift</files>
  <read_first>
    Islet/Notch/NotchPanel.swift (full file, 37 lines)
    IsletTests/NotchPanelTests.swift (full file, 58 lines — every existing test, especially
    `testPanelJoinsAllSpacesAboveFullscreenAux` at lines 38-44)
    .planning/phases/09-fullscreen-flash-window-space-retry/09-RESEARCH.md ("Anti-Patterns to
    Avoid" and "Open Question 2" — why this is a distinct, single-variable experiment)
  </read_first>
  <behavior>
    - Test: `panel.collectionBehavior.contains(.fullScreenAuxiliary)` -> `true` (unchanged invariant)
    - Test: `panel.collectionBehavior.contains(.stationary)` -> `true` (unchanged invariant)
    - Test: `panel.collectionBehavior.contains(.canJoinAllSpaces)` -> `false` (the removed invariant — all-Spaces visibility is now the CGSSpace's job)
  </behavior>
  <action>
    In `NotchPanel.swift` line 32, change `collectionBehavior` from
    `[.canJoinAllSpaces, .fullScreenAuxiliary, .stationary]` to
    `[.fullScreenAuxiliary, .stationary]` — update the trailing comment to explain that all-Spaces
    visibility now comes from `NotchWindowController.notchSpace` (the dedicated CGSSpace from
    09-01), not this flag. In `NotchPanelTests.swift`, rewrite
    `testPanelJoinsAllSpacesAboveFullscreenAux` per the `<behavior>` block above: replace the
    `XCTAssertTrue(...canJoinAllSpaces...)` line with `XCTAssertFalse(...canJoinAllSpaces...,
    "FS-01/Phase 9: all-Spaces visibility now comes from the dedicated CGSSpace, not this flag —
    see NotchWindowController.notchSpace.")`, keep the `.fullScreenAuxiliary` assertion unchanged.
  </action>
  <acceptance_criteria>
    - `Islet/Notch/NotchPanel.swift` line 32 reads `collectionBehavior = [.fullScreenAuxiliary, .stationary]` (no `.canJoinAllSpaces`)
    - `IsletTests/NotchPanelTests.swift`'s `testPanelJoinsAllSpacesAboveFullscreenAux` asserts `XCTAssertFalse` for `.canJoinAllSpaces` and `XCTAssertTrue` for `.fullScreenAuxiliary`
    - `xcodebuild test -scheme Islet -only-testing:IsletTests/NotchPanelTests` passes with zero failures
    - `xcodebuild build -scheme Islet` succeeds with zero errors
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -scheme Islet -only-testing:IsletTests/NotchPanelTests</automated>
  </verify>
  <done>collectionBehavior no longer includes .canJoinAllSpaces; the updated unit test documents and enforces the new invariant.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: On-device Candidate C (replace variant) verification — final regression suite for Candidate C</name>
  <files>Islet/Notch/NotchPanel.swift</files>
  <action>
    No further code changes, UNLESS a regression is found — per D-04, fix any regression before
    finalizing a decision. Build and run Islet on-device and repeat the EXACT same 8-item checklist
    from 09-01 Task 3 (D-03 core-behavior suite, D-07 trigger matrix including the ordinary
    Space-switch check, and the Pitfall-3 lock-screen/sleep-wake check), paying particular
    attention to item 4 (visibility across ALL Spaces) and item 7 (ordinary Space switch) — these
    are the highest-risk items for THIS specific variant, since `.canJoinAllSpaces` no longer
    provides any all-Spaces guarantee and the panel now depends entirely on the CGSSpace mechanism
    for that property.
  </action>
  <decision>
    Does removing `.canJoinAllSpaces` (relying solely on the CGSSpace from 09-01) eliminate the
    fullscreen-enter flash with zero regressions, or does Candidate C (both configurations now
    exhausted) fail to close the gap — requiring automatic fallback to Candidate B?
  </decision>
  <context>
    Re-run the identical 8-item on-device checklist from 09-01-PLAN.md Task 3:
    1. Flash check (3 trigger methods, ≥3 trials each).
    2. Hover/click-expand without focus steal.
    3. Click-through outside the pill.
    4. Visibility across ALL Spaces (≥2 ordinary desktop Spaces) — HIGH RISK for this variant.
    5. Positioning through clamshell/external-display changes.
    6. Fullscreen hide-during/restore-on-exit (all 3 trigger methods).
    7. Ordinary (non-fullscreen) Space switch — HIGH RISK for this variant.
    8. Lock-screen / sleep-wake.
  </context>
  <options>
    <option id="option-accept">
      <name>Candidate C (replace variant) accepted — FS-01 resolved</name>
      <pros>Flash eliminated across all 3 D-07 trigger methods AND all 8 checklist items pass with zero regressions, including the high-risk all-Spaces and ordinary-Space-switch checks. Closes FS-01.</pros>
      <cons>None. Plans 09-03 through 09-05 do not execute; the phase is done.</cons>
    </option>
    <option id="option-proceed-to-b">
      <name>Candidate C exhausted (both configurations) — proceed automatically to Candidate B (09-03/09-04)</name>
      <pros>Confirms neither Candidate C configuration closes the gap or both introduce an unfixable regression (per D-04). Both variants are now conclusively ruled out — a real, evidence-backed negative result. 09-03 (revert Candidate C + prep Candidate B) is already scoped and ready.</pros>
      <cons>Requires two further on-device rounds (09-03 revert/prep is code-only; 09-04 is the real Candidate B attempt). Only select this once all 8 checklist items have been fixed or confirmed as non-regressions elsewhere — a regression must be fixed here first (D-04), not carried forward as an excuse to bail early.</cons>
    </option>
  </options>
  <verify>
    <human-check>Full 8-item checklist re-executed on-device for the replace variant; raw pass/fail findings recorded for every item, with particular attention to all-Spaces visibility and the ordinary Space-switch check.</human-check>
  </verify>
  <done>A single option-accept/option-proceed-to-b decision is recorded in the SUMMARY, backed by the full on-device checklist evidence.</done>
  <resume-signal>Select option-accept or option-proceed-to-b, and include the full on-device checklist results (all 8 items) plus the flash-check outcome for every trigger method in this plan's SUMMARY.md so 09-03 (if triggered) can cite the evidence.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WindowServer (private CGS Space API) → app process | Unchanged from 09-01 — this plan removes an AppKit-level flag (`.canJoinAllSpaces`), it does not add any new private symbol surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 (carried forward) | Denial of Service (local resource exhaustion, low severity) | `CGSSpace` (unchanged from 09-01) | mitigate | Unchanged — `CGSHideSpaces`/`CGSSpaceDestroy` teardown already in place from 09-01 |
| T-09-02 (carried forward) | Tampering (ABI type mismatch) | connection-ID binding (unchanged from 09-01) | mitigate | Unchanged — `CGSSpace.swift`'s self-contained binding remains untouched by this plan |
| T-09-04 | Denial of Service (product-availability regression, not a security boundary) | `NotchPanel.collectionBehavior` — the panel's all-Spaces visibility now depends ENTIRELY on the CGSSpace mechanism, an untested-by-any-reference-implementation combination | mitigate | The full on-device D-03 checklist (items 4 and 7 specifically) must pass with zero regressions before `option-accept` is recorded (D-04) — the checkpoint itself is the mitigation; any failure here is treated as Candidate C's overall failure, not a partial ship |

</threat_model>

<verification>
`xcodebuild test -scheme Islet -only-testing:IsletTests/NotchPanelTests` passes. On-device: the
Task 2 checklist (8 items, re-run for the replace variant) produces a clear, evidence-backed
option-accept/option-proceed-to-b decision recorded in this plan's SUMMARY.md.
</verification>

<success_criteria>
Either FS-01 closes via the replace-variant Candidate C with zero regressions, or Candidate C is
conclusively exhausted (both configurations tried, both failing) and 09-03 is authorized to
proceed to Candidate B automatically, per D-06.
</success_criteria>

<output>
Create `.planning/phases/09-fullscreen-flash-window-space-retry/09-02-SUMMARY.md` when done (or,
if Task 0 halted, the halt-record SUMMARY as specified in Task 0).
</output>
