---
phase: 09-fullscreen-flash-window-space-retry
plan: 05
type: execute
wave: 5
depends_on: ["09-04"]
# Wave-5 branch note: this plan executes ONLY if 09-04-SUMMARY.md recorded option-escalate. This
# is the terminal plan of the phase's conditional chain — Task 0 below is the machine-checkable
# guard, not just this prose warning.
files_modified:
  - project.yml
  - Islet/Notch/DisplayAnimationProbe.swift
  - Islet/Notch/FullscreenSpaceProbe.swift
  - Islet/Notch/NotchWindowController.swift
  - Islet/Notch/FullscreenDetector.swift
  - IsletTests/VisibilityDecisionTests.swift
  - .planning/phases/09-fullscreen-flash-window-space-retry/09-ESCALATION.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [FS-01]
user_setup: []

must_haves:
  truths:
    - "This plan executes ONLY when 09-04-SUMMARY.md recorded option-escalate — a Task-0 guard hard-fails this plan as a no-op otherwise"
    - "Candidate B is fully reverted (project.yml linker settings, DisplayAnimationProbe.swift, all NotchWindowController/FullscreenDetector/VisibilityDecisionTests changes) before the escalation report is written — no code change ships for FS-01 from this phase if it terminates here, mirroring 08-03's precedent"
    - "The escalation report offers EXACTLY two options — accept as permanent technical debt, or formally descope FS-01 from REQUIREMENTS.md — with no 'investigate a new candidate' language, per D-05"
    - "REQUIREMENTS.md (and ROADMAP.md if applicable) is updated to reflect whichever of the two options the user selects"
  artifacts:
    - path: ".planning/phases/09-fullscreen-flash-window-space-retry/09-ESCALATION.md"
      provides: "the final FS-01 escalation report spanning Phase 2/6/8/9, limited to the 2 D-05-permitted options"
      contains: "option-descope"
  key_links:
    - from: "09-05-PLAN.md Task 2"
      to: ".planning/REQUIREMENTS.md"
      via: "the user's selected option (accept-debt / descope) is applied to REQUIREMENTS.md's FS-01 row"
      pattern: "FS-01"
---

<!--
PRECONDITION — READ BEFORE EXECUTING:
This plan executes ONLY if 09-04-PLAN.md's recorded decision was option-escalate. If 09-04's
SUMMARY.md recorded option-accept, FS-01 is already resolved — DO NOT execute this plan. Task 0
below is the machine-checkable enforcement.
-->

<objective>
Wave 5 of Phase 9 (executes ONLY if 09-04 recorded option-escalate): this is the terminal plan of
FS-01's investigation. Per D-05, FS-01 has now been root-caused across Phase 2, Phase 6, Phase 8,
and Phase 9, with four distinct candidate approaches attempted (Phase 2's original diagnosis,
Phase 8's CGS 106/107 push-signal candidate, Phase 9's Candidate C in both configurations, Phase
9's Candidate B poll). This plan (a) fully reverts all Candidate B code — mirroring the same
no-partial-abandoned-private-API-surface discipline used at every prior revert point in this
phase and in Phase 8's `08-03-PLAN.md` — and (b) writes the final escalation report, limited to
EXACTLY two options per D-05: accept as permanent technical debt, or formally descope FS-01 from
REQUIREMENTS.md. No "investigate a new candidate" option is offered.

Purpose: close out the phase with a clean, evidence-backed, traceable decision — not a silent
"good enough" ship (REQUIREMENTS.md's "Out of Scope: best-effort/partial reduction" exclusion
applies to silent partial mitigation, not to a fully-documented, user-adjudicated escalation).

Output: the codebase fully reverted to the pre-Phase-9 baseline (Candidate C was already reverted
in 09-03; this plan reverts Candidate B), `09-ESCALATION.md`, and — once the user selects an
option — the corresponding update to `REQUIREMENTS.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-fullscreen-flash-window-space-retry/09-CONTEXT.md
@.planning/phases/09-fullscreen-flash-window-space-retry/09-RESEARCH.md
@.planning/phases/09-fullscreen-flash-window-space-retry/09-01-SUMMARY.md
@.planning/phases/09-fullscreen-flash-window-space-retry/09-02-SUMMARY.md
@.planning/phases/09-fullscreen-flash-window-space-retry/09-04-SUMMARY.md
@.planning/phases/08-fullscreen-enter-flash-elimination/08-ESCALATION.md
@.planning/phases/08-fullscreen-enter-flash-elimination/08-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Precondition guard — verify 09-04 recorded option-escalate</name>
  <files>.planning/phases/09-fullscreen-flash-window-space-retry/09-04-SUMMARY.md</files>
  <read_first>
    .planning/phases/09-fullscreen-flash-window-space-retry/09-04-SUMMARY.md (the recorded
    option-accept/option-escalate decision this entire plan is gated on — MUST be read before
    touching any file in this plan's `files_modified`)
  </read_first>
  <action>
    Read `09-04-SUMMARY.md` and extract the recorded decision (search for the literal string
    `option-accept` or `option-escalate`; also treat a Task-0 halt record from 09-04 as equivalent
    to `option-accept`/upstream-resolved for this guard's purposes). This plan executes ONLY when
    the recorded decision is `option-escalate`.

    If the decision is anything other than `option-escalate`: HALT IMMEDIATELY. Do not proceed to
    Task 1 or Task 2. Do not modify any file in this plan's `files_modified`. Write
    `.planning/phases/09-fullscreen-flash-window-space-retry/09-05-SUMMARY.md` stating verbatim:
    "09-05-PLAN.md precondition failed: 09-04-SUMMARY.md did not record option-escalate (FS-01 was
    already resolved upstream, or this branch was never reached). This plan is a no-op." Then stop
    execution of this plan entirely.

    If the decision is `option-escalate`: proceed to Task 1.
  </action>
  <acceptance_criteria>
    - `09-04-SUMMARY.md` has been read and its recorded (or cascaded) decision identified before any other file in this plan is touched
    - If the decision is not `option-escalate`: this plan halts with zero modifications to any file in `files_modified`, and `09-05-SUMMARY.md` records the halt verbatim as specified above
    - If the decision is `option-escalate`: execution proceeds to Task 1
  </acceptance_criteria>
  <verify>
    <automated>grep -oE "option-(accept|escalate)" .planning/phases/09-fullscreen-flash-window-space-retry/09-04-SUMMARY.md | head -1</automated>
  </verify>
  <done>Either the plan has halted with the exact mismatch message and no code changed, or the option-escalate decision is confirmed and Task 1 begins.</done>
</task>

<task type="auto">
  <name>Task 1: Revert Candidate B to the pre-Phase-9 baseline</name>
  <files>project.yml, Islet/Notch/DisplayAnimationProbe.swift, Islet/Notch/FullscreenSpaceProbe.swift, Islet/Notch/NotchWindowController.swift, Islet/Notch/FullscreenDetector.swift, IsletTests/VisibilityDecisionTests.swift</files>
  <read_first>
    Islet/Notch/DisplayAnimationProbe.swift (as created in 09-04 — delete this file entirely)
    Islet/Notch/FullscreenSpaceProbe.swift (as left by 09-04 — remove the added
    `builtinDisplayHasFullscreenSpace` function, restoring the file to its pre-Phase-9 shape)
    Islet/Notch/NotchWindowController.swift (as left by 09-04 — remove `displayAnimationMonitor`,
    `pendingFullscreenTransition`, `pendingFullscreenTimeout`, `handleFullscreenTransitionDetected`,
    the `start()`/`deinit` wiring, and the `updateVisibility()` call-site change back to its
    3-argument `shouldShow(...)` call)
    Islet/Notch/FullscreenDetector.swift (as left by 09-04 — remove the 4th
    `pendingFullscreenTransition` parameter, restoring the original 3-parameter signature)
    IsletTests/VisibilityDecisionTests.swift (as left by 09-04 — remove the 3 new
    `pendingFullscreenTransition`-specific tests and the 4th argument from the 6 original tests)
    project.yml (as left by 09-03 — remove the `FRAMEWORK_SEARCH_PATHS`/`OTHER_LDFLAGS` additions)
    .planning/phases/08-fullscreen-enter-flash-elimination/08-03-PLAN.md (Task 1 — the exact
    byte-for-byte revert discipline this task mirrors)
  </read_first>
  <action>
    Delete `Islet/Notch/DisplayAnimationProbe.swift` entirely. In `FullscreenSpaceProbe.swift`,
    remove the `builtinDisplayHasFullscreenSpace` function added in 09-04, restoring the file to
    its pre-Phase-9 85-line shape. In `NotchWindowController.swift`, remove every 09-04 addition
    (`displayAnimationMonitor`, `pendingFullscreenTransition`, `pendingFullscreenTimeout`,
    `handleFullscreenTransitionDetected`, its `start()` construction/registration, and its `deinit`
    teardown), restoring `updateVisibility()`'s `shouldShow(...)` call to its original 3-argument
    form. In `FullscreenDetector.swift`, restore `shouldShow(...)` to its original 3-parameter
    signature (`hasTarget`, `hideInFullscreen`, `isFullscreen` only). In
    `VisibilityDecisionTests.swift`, remove the 3 `pendingFullscreenTransition`-specific tests and
    the 4th argument from the 6 original test call sites, restoring the file to its original
    40-line shape. In `project.yml`'s top-level `settings: base:` block, remove the
    `FRAMEWORK_SEARCH_PATHS`/`OTHER_LDFLAGS` entries added in 09-03. Run `xcodegen generate` to
    regenerate `Islet.xcodeproj`.
  </action>
  <acceptance_criteria>
    - `Islet/Notch/DisplayAnimationProbe.swift` no longer exists
    - `grep -c 'builtinDisplayHasFullscreenSpace' Islet/Notch/FullscreenSpaceProbe.swift` is 0
    - `grep -c 'pendingFullscreenTransition' Islet/Notch/NotchWindowController.swift Islet/Notch/FullscreenDetector.swift IsletTests/VisibilityDecisionTests.swift` is 0 across all three files
    - `Islet/Notch/FullscreenDetector.swift`'s `shouldShow` has exactly 3 parameters
    - `project.yml` no longer contains `-framework SkyLight` or `/System/Library/PrivateFrameworks`
    - `xcodebuild test -scheme Islet` (full suite) passes with zero failures, reproducing the exact pre-Phase-9 baseline test count and behavior
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -scheme Islet</automated>
  </verify>
  <done>Candidate B is fully reverted; the codebase (having already had Candidate C reverted in 09-03) is back to its exact pre-Phase-9 baseline. No FS-01 code change ships from this phase.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Write the final escalation report and apply the user's scope decision</name>
  <files>.planning/phases/09-fullscreen-flash-window-space-retry/09-ESCALATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/phases/08-fullscreen-enter-flash-elimination/08-ESCALATION.md (the exact report
    structure and tone to mirror: Root Cause, What Was Tried This Phase, Requested Decision)
    .planning/phases/09-fullscreen-flash-window-space-retry/09-01-SUMMARY.md (Candidate C additive
    on-device evidence to cite)
    .planning/phases/09-fullscreen-flash-window-space-retry/09-02-SUMMARY.md (Candidate C replace
    variant on-device evidence to cite)
    .planning/phases/09-fullscreen-flash-window-space-retry/09-04-SUMMARY.md (Candidate B on-device
    evidence to cite)
    .planning/REQUIREMENTS.md (the FS-01 row and Traceability table this task edits if
    option-descope is selected)
  </read_first>
  <action>
    Write `.planning/phases/09-fullscreen-flash-window-space-retry/09-ESCALATION.md` mirroring
    `08-ESCALATION.md`'s structure: a "Root Cause" section recapping the now-four-investigation
    history (Phase 2's original window-server-compositor-timing diagnosis, Phase 8's CGS 106/107
    push-signal candidate — disproven, never fires cross-process, Phase 9's Candidate C in both
    additive and replace configurations — cite the exact on-device findings from
    09-01-SUMMARY.md/09-02-SUMMARY.md, and Phase 9's Candidate B poll+disambiguator — cite the
    exact on-device findings from 09-04-SUMMARY.md); a "What Was Tried This Phase" section with the
    raw evidence citations; and a "Requested Decision" section offering EXACTLY two options per
    D-05 — `option-accept-debt` (accept as permanent technical debt, no further investigation
    spend) and `option-descope` (formally descope FS-01 from REQUIREMENTS.md, editing the
    requirement's status/Out-of-Scope table) — explicitly stating that no third
    "investigate-a-new-candidate" option is offered, since D-05 forecloses it after four
    exhausted candidates. Present this report to the user and await their selection. Once
    selected: if `option-descope`, edit `.planning/REQUIREMENTS.md`'s FS-01 row (move it to a
    descoped/out-of-scope state with a dated rationale citing this escalation report) and update
    its Traceability table entry accordingly; if `option-accept-debt`, add a dated note to
    REQUIREMENTS.md's FS-01 row recording the explicit accept-as-debt decision (status stays
    open/accepted, not descoped) with a citation to this escalation report.
  </action>
  <decision>
    Final FS-01 scope decision, per D-05: accept the fullscreen-enter flash as permanent technical
    debt, or formally descope FS-01 from REQUIREMENTS.md.
  </decision>
  <context>
    Per D-05, this is the LAST investigation round — no further candidate investigation is
    permitted regardless of which option is chosen. The report must cite the specific on-device
    evidence from all three Phase-9 SUMMARY.md files (09-01, 09-02, 09-04) plus Phase 8's
    08-ESCALATION.md, so the decision is fully evidence-backed and traceable.
  </context>
  <options>
    <option id="option-accept-debt">
      <name>Accept as permanent technical debt</name>
      <pros>Closes the phase now, no further investigation spend. FS-01 stays open/known in REQUIREMENTS.md with an explicit, dated, evidence-backed acceptance rather than a silent carry-over.</pros>
      <cons>The ~1-frame flash remains in shipped builds indefinitely.</cons>
    </option>
    <option id="option-descope">
      <name>Formally descope FS-01</name>
      <pros>Makes the decision explicit and traceable by editing REQUIREMENTS.md to reflect the descope, rather than leaving it as an implicit carry-over across milestones.</pros>
      <cons>FS-01 is no longer a tracked requirement — any future revisit would need to be reintroduced as a new requirement with fresh scope.</cons>
    </option>
  </options>
  <verify>
    <human-check>09-ESCALATION.md is written with the complete 4-investigation evidence chain (Phase 2, 8, 9-Candidate-C-additive, 9-Candidate-C-replace, 9-Candidate-B) and exactly 2 options; the user has selected one; REQUIREMENTS.md has been updated to match the selection.</human-check>
  </verify>
  <done>09-ESCALATION.md exists with the full evidence chain and exactly 2 options; REQUIREMENTS.md reflects the user's selected option; no FS-01 code change ships from this phase.</done>
  <resume-signal>Select option-accept-debt or option-descope.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new — this plan is revert + documentation only | No new code surface is introduced; the only risk is an incomplete revert leaving stale private-API references in the shipped codebase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Tampering (incomplete revert leaves orphaned private-API surface with no corresponding benefit, across the ENTIRE phase's private-API additions — CGSSpace.swift already reverted in 09-03, DisplayAnimationProbe.swift reverted here) | `DisplayAnimationProbe.swift` / `FullscreenSpaceProbe.swift` / `NotchWindowController.swift` / `FullscreenDetector.swift` / `VisibilityDecisionTests.swift` / `project.yml` | mitigate | Every acceptance criterion in Task 1 is a grep/diff-verifiable assertion (file absence, zero-occurrence greps across all affected files, exact restored parameter count); the full test suite passing confirms the revert reproduces the pre-Phase-9 baseline exactly, matching 08-03's established discipline |
| T-09-12 | Repudiation (an undocumented or silently-decided scope change) | FS-01's final disposition | mitigate | The escalation report is written BEFORE any REQUIREMENTS.md edit, cites the complete 4-investigation evidence chain, and the REQUIREMENTS.md edit only happens after the user's explicit, recorded selection — no silent scope change |

</threat_model>

<verification>
`xcodebuild test -scheme Islet` (full suite) passes, reproducing the exact pre-Phase-9 baseline.
`09-ESCALATION.md` exists with the complete evidence chain and exactly 2 options. `REQUIREMENTS.md`
reflects the user's selected option.
</verification>

<success_criteria>
FS-01's investigation concludes with a clean, evidence-backed, traceable decision — either accepted
as permanent technical debt or formally descoped — with zero FS-01 code shipped from this phase
and the codebase fully restored to its pre-Phase-9 baseline.
</success_criteria>

<output>
Create `.planning/phases/09-fullscreen-flash-window-space-retry/09-05-SUMMARY.md` when done (or,
if Task 0 halted, the halt-record SUMMARY as specified in Task 0).
</output>
