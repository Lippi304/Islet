---
phase: 09-fullscreen-flash-window-space-retry
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
# Wave-3 branch note: this plan executes ONLY if 09-02-SUMMARY.md recorded option-proceed-to-b
# (which itself only happens if 09-01 recorded option-continue AND 09-02 also failed to close the
# flash). Task 0 below is the machine-checkable guard, not just this prose warning.
files_modified:
  - Islet/Notch/CGSSpace.swift
  - Islet/Notch/NotchPanel.swift
  - Islet/Notch/NotchWindowController.swift
  - IsletTests/NotchPanelTests.swift
  - project.yml
autonomous: true
requirements: [FS-01]
user_setup: []

must_haves:
  truths:
    - "This plan executes ONLY when 09-02-SUMMARY.md recorded option-proceed-to-b — a Task-0 guard hard-fails this plan as a no-op otherwise, writing its own SUMMARY.md recording the halt"
    - "Candidate C is fully reverted — CGSSpace.swift removed, NotchWindowController's notchSpace wiring removed, NotchPanel.collectionBehavior and NotchPanelTests restored to the pre-Phase-9 baseline — before any Candidate B code is written, per the same no-partial-abandoned-private-API-surface discipline Phase 8's 08-03 established"
    - "project.yml gains the SkyLight.framework linker settings Candidate B requires, with xcodegen regenerated, so 09-04 can build against SLSManagedDisplayIsAnimating without a separate setup step"
  artifacts:
    - path: "project.yml"
      provides: "FRAMEWORK_SEARCH_PATHS/OTHER_LDFLAGS additions enabling SkyLight.framework linkage for Candidate B"
      contains: "-framework SkyLight"
  key_links:
    - from: "09-03-PLAN.md Task 0"
      to: "09-02-SUMMARY.md"
      via: "precondition guard reads the recorded option-accept/option-proceed-to-b decision"
      pattern: "option-(accept|proceed-to-b)"
---

<!--
PRECONDITION — READ BEFORE EXECUTING:
This plan executes ONLY if 09-02-PLAN.md's recorded decision was option-proceed-to-b. If 09-02's
SUMMARY.md recorded option-accept (or if 09-02 itself halted because 09-01 recorded option-accept),
FS-01 is already resolved — DO NOT execute this plan or 09-04/09-05. Task 0 below is the
machine-checkable enforcement.
-->

<objective>
Wave 3 of Phase 9 (executes ONLY if 09-02 recorded option-proceed-to-b): Candidate C has now been
tried in both of its viable configurations (09-01 additive, 09-02 replace) and neither closed the
flash without an unfixable regression. This plan (a) fully reverts all Candidate C code back to the
pre-Phase-9 baseline — mirroring the discipline Phase 8's `08-03-PLAN.md` established (no
partially-abandoned private-API surface ships when a candidate is exhausted) — and (b) prepares the
groundwork Candidate B needs: the `SkyLight.framework` linker settings in `project.yml`, documented
as a clean addition in `09-RESEARCH.md`'s Environment Availability section.

This is purely mechanical revert-and-prep work; no new behavior is attempted in this plan. Per
D-06, this automatic continuation (no separate human checkpoint) is itself the authorized
transition from "Candidate C exhausted" to "attempting Candidate B."

Output: the codebase returned to its pre-Phase-9 baseline for every Candidate C file, plus
`project.yml` ready to link `SkyLight.framework` for 09-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-fullscreen-flash-window-space-retry/09-CONTEXT.md
@.planning/phases/09-fullscreen-flash-window-space-retry/09-RESEARCH.md
@.planning/phases/09-fullscreen-flash-window-space-retry/09-02-SUMMARY.md
@.planning/phases/08-fullscreen-enter-flash-elimination/08-03-PLAN.md

<interfaces>
<!-- Target restored state for each reverted file. -->

`Islet/Notch/NotchPanel.swift` line 32 — restore to the pre-Phase-9 baseline:
```swift
collectionBehavior = [.canJoinAllSpaces, .fullScreenAuxiliary, .stationary] // ISL-02: all Spaces, above fullscreen-aux
```

`IsletTests/NotchPanelTests.swift`'s `testPanelJoinsAllSpacesAboveFullscreenAux` — restore to the
pre-Phase-9 baseline:
```swift
func testPanelJoinsAllSpacesAboveFullscreenAux() {
    let panel = makePanel()
    XCTAssertTrue(panel.collectionBehavior.contains(.canJoinAllSpaces),
                  "ISL-02: the island must be visible across all Spaces.")
    XCTAssertTrue(panel.collectionBehavior.contains(.fullScreenAuxiliary),
                  "ISL-02: the island must sit above fullscreen-auxiliary content.")
}
```

`Islet/Notch/NotchWindowController.swift` — remove entirely: the `private let notchSpace =
CGSSpace(level: 2147483647)` property, the `notchSpace.windows.insert(panel)` line inside
`positionAndShow()`'s `if self.panel == nil` branch, and the `notchSpace.windows.remove(panel)`
line inside `deinit`.

`project.yml`'s top-level `settings.base` block (lines 23-30, currently no
`FRAMEWORK_SEARCH_PATHS`/`OTHER_LDFLAGS` entries) — target addition, per `08-ESCALATION.md`'s
"Untried Fallback" section:
```yaml
settings:
  base:
    SWIFT_VERSION: "5.0"
    MACOSX_DEPLOYMENT_TARGET: "14.0"
    ENABLE_HARDENED_RUNTIME: YES
    CODE_SIGN_STYLE: Automatic
    CODE_SIGN_IDENTITY: "-"
    DEAD_CODE_STRIPPING: YES
    FRAMEWORK_SEARCH_PATHS: "$(inherited) /System/Library/PrivateFrameworks"
    OTHER_LDFLAGS: "$(inherited) -framework SkyLight"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Precondition guard — verify 09-02 recorded option-proceed-to-b</name>
  <files>.planning/phases/09-fullscreen-flash-window-space-retry/09-02-SUMMARY.md</files>
  <read_first>
    .planning/phases/09-fullscreen-flash-window-space-retry/09-02-SUMMARY.md (the recorded
    option-accept/option-proceed-to-b decision this entire plan is gated on — MUST be read before
    touching any file in this plan's `files_modified`)
  </read_first>
  <action>
    Read `09-02-SUMMARY.md` and extract the recorded decision (search for the literal string
    `option-accept` or `option-proceed-to-b`). Note: `09-02-SUMMARY.md` may itself be a Task-0 halt
    record from 09-02 (if 09-01 recorded `option-accept`) — treat that identically to
    `option-accept` for this guard's purposes. This plan executes ONLY when the recorded (or
    cascaded) decision is `option-proceed-to-b`.

    If the decision is anything other than `option-proceed-to-b`: HALT IMMEDIATELY. Do not proceed
    to Task 1. Do not modify any file in this plan's `files_modified`. Write
    `.planning/phases/09-fullscreen-flash-window-space-retry/09-03-SUMMARY.md` stating verbatim:
    "09-03-PLAN.md precondition failed: 09-02-SUMMARY.md did not record option-proceed-to-b
    (FS-01 was already resolved upstream, or this branch was never reached). This plan is a
    no-op — no further plans in this phase execute." Then stop execution of this plan entirely.

    If the decision is `option-proceed-to-b`: proceed to Task 1.
  </action>
  <acceptance_criteria>
    - `09-02-SUMMARY.md` has been read and its recorded (or cascaded-halt) decision identified before any other file in this plan is touched
    - If the decision is not `option-proceed-to-b`: this plan halts with zero modifications to any of `CGSSpace.swift`, `NotchPanel.swift`, `NotchWindowController.swift`, `NotchPanelTests.swift`, or `project.yml`, and `09-03-SUMMARY.md` records the halt verbatim as specified above
    - If the decision is `option-proceed-to-b`: execution proceeds to Task 1
  </acceptance_criteria>
  <verify>
    <automated>grep -oE "option-(accept|proceed-to-b)" .planning/phases/09-fullscreen-flash-window-space-retry/09-02-SUMMARY.md | head -1</automated>
  </verify>
  <done>Either the plan has halted with the exact mismatch message and no code changed, or the option-proceed-to-b decision is confirmed and Task 1 begins.</done>
</task>

<task type="auto">
  <name>Task 1: Revert Candidate C to the pre-Phase-9 baseline and prepare Candidate B's linker settings</name>
  <files>Islet/Notch/CGSSpace.swift, Islet/Notch/NotchPanel.swift, Islet/Notch/NotchWindowController.swift, IsletTests/NotchPanelTests.swift, project.yml</files>
  <read_first>
    Islet/Notch/CGSSpace.swift (as created in 09-01 — delete this file entirely)
    Islet/Notch/NotchPanel.swift (as left by 09-02 — collectionBehavior currently has
    `.canJoinAllSpaces` removed; restore it)
    Islet/Notch/NotchWindowController.swift (as left by 09-01 — the `notchSpace` property,
    `positionAndShow()`'s `notchSpace.windows.insert(panel)` line, and `deinit`'s
    `notchSpace.windows.remove(panel)` line; remove all three)
    IsletTests/NotchPanelTests.swift (as left by 09-02 — `testPanelJoinsAllSpacesAboveFullscreenAux`
    currently asserts `XCTAssertFalse` for `.canJoinAllSpaces`; restore the original
    `XCTAssertTrue`)
    project.yml (full file, 102 lines — the top-level `settings.base` block at lines 23-30 that
    gains the new linker entries)
    .planning/phases/08-fullscreen-enter-flash-elimination/08-03-PLAN.md (Task 1 — the exact
    byte-for-byte revert discipline this task mirrors)
  </read_first>
  <action>
    Delete `Islet/Notch/CGSSpace.swift` entirely. In `NotchWindowController.swift`, remove the
    `private let notchSpace = CGSSpace(level: 2147483647)` property declaration, the
    `notchSpace.windows.insert(panel)` line from `positionAndShow()`'s `if self.panel == nil`
    branch, and the `notchSpace.windows.remove(panel)` line from `deinit` — restoring those two
    functions to their exact pre-Phase-9 shape. In `NotchPanel.swift` line 32, restore
    `collectionBehavior` to `[.canJoinAllSpaces, .fullScreenAuxiliary, .stationary]` exactly as it
    was before 09-02. In `NotchPanelTests.swift`, restore
    `testPanelJoinsAllSpacesAboveFullscreenAux` to its original two `XCTAssertTrue` assertions
    (both `.canJoinAllSpaces` and `.fullScreenAuxiliary`), exactly as it was before 09-02. Then, in
    `project.yml`'s top-level `settings: base:` block (lines 23-30), add the two new keys shown in
    the `<interfaces>` block above: `FRAMEWORK_SEARCH_PATHS: "$(inherited)
    /System/Library/PrivateFrameworks"` and `OTHER_LDFLAGS: "$(inherited) -framework SkyLight"`.
    Run `xcodegen generate` to regenerate `Islet.xcodeproj` from the updated `project.yml`.
  </action>
  <acceptance_criteria>
    - `Islet/Notch/CGSSpace.swift` no longer exists
    - `grep -c 'notchSpace' Islet/Notch/NotchWindowController.swift` is 0
    - `Islet/Notch/NotchPanel.swift` line 32 reads `collectionBehavior = [.canJoinAllSpaces, .fullScreenAuxiliary, .stationary]`
    - `IsletTests/NotchPanelTests.swift`'s `testPanelJoinsAllSpacesAboveFullscreenAux` contains two `XCTAssertTrue` calls (`.canJoinAllSpaces` and `.fullScreenAuxiliary`), zero `XCTAssertFalse` calls
    - `project.yml` contains `-framework SkyLight` and `/System/Library/PrivateFrameworks`
    - `Islet.xcodeproj` has been regenerated after the `project.yml` change (`xcodegen generate` run, confirmed via a fresh `git status`/mtime check on the `.xcodeproj` bundle)
    - `xcodebuild test -scheme Islet` (full suite) passes with zero failures
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -scheme Islet</automated>
  </verify>
  <done>Candidate C is fully reverted to the pre-Phase-9 baseline (verified by the passing full suite matching pre-Phase-9 behavior) and project.yml/Islet.xcodeproj are ready for Candidate B's SkyLight.framework binding in 09-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SkyLight.framework (Apple system private framework) → linker | `project.yml`'s new `OTHER_LDFLAGS`/`FRAMEWORK_SEARCH_PATHS` entries link against an Apple-provided private framework already present on every macOS install — not a third-party dependency, so the Package Legitimacy Gate does not apply (per 09-RESEARCH.md's Package Legitimacy Audit section) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Tampering (incomplete revert leaves orphaned private-API surface in a build with no corresponding benefit) | `CGSSpace.swift` / `NotchWindowController.swift` / `NotchPanel.swift` / `NotchPanelTests.swift` | mitigate | Every acceptance criterion in Task 1 is a grep/diff-verifiable assertion (file absence, zero `notchSpace` occurrences, exact restored `collectionBehavior` line, exact restored test assertions) — the full test suite passing is the final confirmation the revert reproduced the pre-Phase-9 baseline exactly |
| T-09-06 | Denial of Service (accidental supply-chain risk from a new linker setting) | `project.yml`'s new `OTHER_LDFLAGS`/`FRAMEWORK_SEARCH_PATHS` | accept | Not applicable — `SkyLight.framework` is an Apple system framework already present on the OS, not a downloaded/versioned third-party artifact; no new supply-chain surface is introduced |

</threat_model>

<verification>
`xcodebuild test -scheme Islet` (full suite) passes, reproducing pre-Phase-9 baseline behavior
exactly. `project.yml` contains the new `SkyLight.framework` linker settings and `Islet.xcodeproj`
has been regenerated.
</verification>

<success_criteria>
The codebase is back to its pre-Phase-9 state for every Candidate C file, and `project.yml`/
`Islet.xcodeproj` are ready for 09-04 to bind `SLSManagedDisplayIsAnimating` without any further
build-configuration work.
</success_criteria>

<output>
Create `.planning/phases/09-fullscreen-flash-window-space-retry/09-03-SUMMARY.md` when done (or,
if Task 0 halted, the halt-record SUMMARY as specified in Task 0).
</output>
