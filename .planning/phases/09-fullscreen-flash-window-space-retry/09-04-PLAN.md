---
phase: 09-fullscreen-flash-window-space-retry
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
# Wave-4 branch note: this plan executes ONLY if 09-03 actually performed the revert+prep (i.e.
# 09-03 was not itself a Task-0 no-op). Task 0 below is the machine-checkable guard.
files_modified:
  - Islet/Notch/FullscreenDetector.swift
  - IsletTests/VisibilityDecisionTests.swift
  - Islet/Notch/FullscreenSpaceProbe.swift
  - Islet/Notch/DisplayAnimationProbe.swift
  - Islet/Notch/NotchWindowController.swift
autonomous: false
requirements: [FS-01]
user_setup: []

must_haves:
  truths:
    - "This plan executes ONLY when 09-03-SUMMARY.md confirms the revert+prep completed (i.e. 09-03 itself was not a guard no-op) — a Task-0 guard hard-fails this plan as a no-op otherwise"
    - "SLSManagedDisplayIsAnimating is polled via a CVDisplayLink-driven clock, paired with a 'did a new fullscreen-type Space just appear' disambiguator, so an ordinary Space switch never spuriously hides the island (08-ESCALATION.md's Untried Fallback design)"
    - "The new signal feeds the existing single updateVisibility() arbiter (Pattern 7) via a bounded, self-healing pendingFullscreenTransition flag with a ~1s safety timeout — never a parallel show/hide path"
    - "A single option-accept/option-escalate decision is recorded on-device, backed by the same full D-03/D-07/Pitfall-3 suite used for both Candidate C attempts, and this is the FINAL investigation round (D-05) — no further candidate is investigated regardless of outcome"
  artifacts:
    - path: "Islet/Notch/DisplayAnimationProbe.swift"
      provides: "SLSManagedDisplayIsAnimating binding + CVDisplayLink poll + fullscreen-space-appearing disambiguator"
      contains: "SLSManagedDisplayIsAnimating"
    - path: "Islet/Notch/FullscreenDetector.swift"
      provides: "shouldShow(...) extended with the pendingFullscreenTransition input"
      contains: "pendingFullscreenTransition"
  key_links:
    - from: "Islet/Notch/DisplayAnimationProbe.swift"
      to: "Islet/Notch/NotchWindowController.swift (updateVisibility)"
      via: "onFullscreenTransitionDetected callback sets pendingFullscreenTransition and calls updateVisibility() — never orderOut/positionAndShow directly"
      pattern: "onFullscreenTransitionDetected"
    - from: "Islet/Notch/NotchWindowController.swift (updateVisibility)"
      to: "Islet/Notch/FullscreenDetector.swift (shouldShow)"
      via: "pendingFullscreenTransition threaded as the 4th argument"
      pattern: "shouldShow\\(hasTarget:"
---

<!--
PRECONDITION — READ BEFORE EXECUTING:
This plan executes ONLY if 09-03-PLAN.md actually performed its revert+prep (i.e. 09-03 was not a
Task-0 no-op cascading from an earlier option-accept). Task 0 below is the machine-checkable
enforcement, not just this prose warning.
-->

<objective>
Wave 4 of Phase 9 (executes ONLY if Candidate C — both configurations — was exhausted in 09-01/
09-02, and 09-03 completed the revert+prep): implement Candidate B, the documented fallback from
`08-ESCALATION.md`'s "Untried Fallback" section — a `CVDisplayLink`-driven poll of the private
`SLSManagedDisplayIsAnimating` signal, paired with a disambiguator (a NEW fullscreen-type Space
appearing) so an ordinary Space switch is never mistaken for a fullscreen transition, feeding the
existing single `updateVisibility()` arbiter via a bounded, self-healing
`pendingFullscreenTransition` flag — the exact same design shape Phase 8's `08-02-PLAN.md`
specified for its own "option-b" branch (never executed there, since Phase 8 recorded option-c),
reused here for Candidate B's genuinely new polling signal.

Per D-05, this is the FINAL investigation round for FS-01: if this candidate also fails to close
the flash without an unfixable regression, no further candidate is investigated — 09-05 (the final
escalation report, limited to exactly two options) executes next.

Output: `Islet/Notch/DisplayAnimationProbe.swift` (new — the poll + disambiguator),
`FullscreenSpaceProbe.swift` extended with the "does the built-in have a fullscreen-type Space
right now" helper, `FullscreenDetector.shouldShow(...)` extended with `pendingFullscreenTransition`,
`NotchWindowController` wired to feed it, and a recorded option-accept/option-escalate decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-fullscreen-flash-window-space-retry/09-CONTEXT.md
@.planning/phases/09-fullscreen-flash-window-space-retry/09-RESEARCH.md
@.planning/phases/09-fullscreen-flash-window-space-retry/09-03-SUMMARY.md
@.planning/phases/08-fullscreen-enter-flash-elimination/08-ESCALATION.md
@.planning/phases/08-fullscreen-enter-flash-elimination/08-02-PLAN.md

<interfaces>
<!-- Target contracts this plan builds. Interface-first: Task 1 defines the pure predicate
     extension, Task 2 implements the real signal and wires it in. -->

Target extension to `Islet/Notch/FullscreenDetector.swift` (currently the 3-parameter form shown
in `<read_first>` below — this is the ONE permitted change to this pure predicate file, mirroring
`08-02-PLAN.md`'s own "option-b" design verbatim):
```swift
func shouldShow(hasTarget: Bool, hideInFullscreen: Bool, isFullscreen: Bool, pendingFullscreenTransition: Bool) -> Bool {
    hasTarget && !(hideInFullscreen && (isFullscreen || pendingFullscreenTransition))
}
```

Target addition to `Islet/Notch/FullscreenSpaceProbe.swift` (mirrors the existing
`isBuiltinDisplayInFullscreenSpace(builtinUUID:)` — same fail-safe-to-false philosophy, same
display-dict lookup, but reads the display's FULL `"Spaces"` array instead of just `"Current
Space"`, since a fullscreen Space is created and appears in that list before "Current Space" flips
to it):
```swift
func builtinDisplayHasFullscreenSpace(builtinUUID: String?) -> Bool
```

Target new file `Islet/Notch/DisplayAnimationProbe.swift`:
```swift
@_silgen_name("SLSManagedDisplayIsAnimating")
func SLSManagedDisplayIsAnimating(_ cid: Int32) -> Bool
// Reuses FullscreenSpaceProbe.swift's existing CGSMainConnectionID() -> Int32 binding as the
// `cid` argument — SAME Int32 type, no ABI-mismatch risk (unlike CGSSpace.swift's deliberately
// independent UInt binding in the now-reverted Candidate C). 08-ESCALATION.md's own wording
// ("returns true for ANY display animation") confirms this is connection-scoped, not
// display-specific — a single Int32 connection argument, no display-ID parameter.

final class DisplayAnimationMonitor {
    init(builtinUUIDProvider: @escaping () -> String?,
         onFullscreenTransitionDetected: @escaping () -> Void)
    func start()
    func stop()
}
```

Target additions to `Islet/Notch/NotchWindowController.swift`:
```swift
private var displayAnimationMonitor: DisplayAnimationMonitor?
private var pendingFullscreenTransition = false
private var pendingFullscreenTimeout: DispatchWorkItem?
```

From `Islet/Notch/NotchWindowController.swift`'s existing `scheduleDeviceBatteryRefresh` (lines
791-819) — the cancel-and-replace one-shot `DispatchWorkItem` idiom to mirror for the ~1.0s
`pendingFullscreenTimeout` safety timeout.

From `Islet/Notch/NotchWindowController.swift`'s `updateVisibility()` (lines 414-441) — the SOLE
show/hide arbiter this new signal must feed, never bypass:
```swift
private func updateVisibility() {
    let descriptors = NSScreen.screens.map { $0.descriptor }
    let target = selectTargetScreen(from: descriptors)
    let fullscreen = isBuiltinDisplayInFullscreenSpace(builtinUUID: currentBuiltin()?.uuid)
    if shouldShow(hasTarget: target != nil, hideInFullscreen: hideInFullscreen, isFullscreen: fullscreen), let target {
        positionAndShow(on: target)
    } else {
        panel?.orderOut(nil)
        hotZone = nil; expandedZone = nil; pointerInZone = false
    }
}
```
Target: pass `pendingFullscreenTransition: pendingFullscreenTransition` as the 4th argument to
`shouldShow(...)`, and immediately after computing `fullscreen`, if `fullscreen == true` clear
`pendingFullscreenTransition = false` and cancel `pendingFullscreenTimeout` (the real reactive
signal has caught up — steady state takes over, mirroring 08-02's 3-way clear-path design: timeout
/ steady-state catch-up / explicit stop-of-animation clear).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Precondition guard — verify 09-03 completed the revert+prep</name>
  <files>.planning/phases/09-fullscreen-flash-window-space-retry/09-03-SUMMARY.md</files>
  <read_first>
    .planning/phases/09-fullscreen-flash-window-space-retry/09-03-SUMMARY.md (records either the
    completed revert+prep, or a cascaded halt — MUST be read before touching any file in this
    plan's `files_modified`)
  </read_first>
  <action>
    Read `09-03-SUMMARY.md`. If it records a halt (contains the literal string "precondition
    failed" or "no-op" per 09-03-PLAN.md Task 0's halt-record format): HALT IMMEDIATELY. Do not
    modify any file in this plan's `files_modified`. Write
    `.planning/phases/09-fullscreen-flash-window-space-retry/09-04-SUMMARY.md` stating verbatim:
    "09-04-PLAN.md precondition failed: 09-03-SUMMARY.md recorded a halt (Candidate C was already
    resolved upstream, or this branch was never reached). This plan is a no-op — no further plans
    in this phase execute." Then stop execution of this plan entirely.

    If `09-03-SUMMARY.md` confirms the revert+prep completed (no halt language present): also
    verify directly against the filesystem that `Islet/Notch/CGSSpace.swift` does not exist and
    `project.yml` contains `-framework SkyLight` — if either check fails, treat this as a halt
    condition too (09-03 may have partially failed) and report the discrepancy instead of
    proceeding. Otherwise, proceed to Task 1.
  </action>
  <acceptance_criteria>
    - `09-03-SUMMARY.md` has been read and its outcome (completed vs. halted) identified before any other file in this plan is touched
    - If halted (or the filesystem checks fail): this plan halts with zero modifications to any file in `files_modified`, and `09-04-SUMMARY.md` records the halt verbatim as specified above
    - If completed: `Islet/Notch/CGSSpace.swift` confirmed absent and `project.yml` confirmed to contain `-framework SkyLight` before Task 1 begins
  </acceptance_criteria>
  <verify>
    <automated>test -f Islet/Notch/CGSSpace.swift && echo "HALT: CGSSpace.swift still present" || grep -c "framework SkyLight" project.yml</automated>
  </verify>
  <done>Either the plan has halted with the exact mismatch message and no code changed, or the precondition is confirmed and Task 1 begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Extend the pure predicate layer with pendingFullscreenTransition</name>
  <files>Islet/Notch/FullscreenDetector.swift, IsletTests/VisibilityDecisionTests.swift</files>
  <read_first>
    Islet/Notch/FullscreenDetector.swift (full file, 31 lines)
    IsletTests/VisibilityDecisionTests.swift (full file, 40 lines — every existing call site, all 6 tests)
    .planning/phases/08-fullscreen-enter-flash-elimination/08-02-PLAN.md (Task 1 — the identical
    `pendingFullscreenTransition` extension pattern this task reuses verbatim)
  </read_first>
  <behavior>
    - Test: `shouldShow(hasTarget: true, hideInFullscreen: true, isFullscreen: false, pendingFullscreenTransition: true)` -> `false` (a pending transition alone hides, same as isFullscreen)
    - Test: `shouldShow(hasTarget: true, hideInFullscreen: false, isFullscreen: false, pendingFullscreenTransition: true)` -> `true` (hideInFullscreen off neutralizes pendingFullscreenTransition exactly like it neutralizes isFullscreen)
    - Test: `shouldShow(hasTarget: false, hideInFullscreen: true, isFullscreen: false, pendingFullscreenTransition: true)` -> `false` (no target still dominates)
  </behavior>
  <action>
    Add the 4th parameter `pendingFullscreenTransition: Bool` to `shouldShow(...)` in
    `FullscreenDetector.swift` exactly as shown in the `<interfaces>` block above (OR the condition
    into the existing `hideInFullscreen &&` clause), updating the doc comment above it to describe
    the new input as "Phase 9 (FS-01, Candidate B): a bounded, self-healing override set the
    instant the CVDisplayLink poll detects a likely fullscreen-transition start, cleared on
    steady-state catch-up / animation-stop / timeout — see NotchWindowController /
    DisplayAnimationProbe". In `VisibilityDecisionTests.swift`, add `pendingFullscreenTransition:
    false` as an explicit 4th argument to ALL 6 existing test call sites (keeps their original
    intent unchanged and unbroken under the new signature) and append 3 new test functions from
    the `<behavior>` block above, named `testPendingTransitionAloneHidesLikeFullscreen`,
    `testPendingTransitionIgnoredWhenHideFlagOff`, `testNoTargetHidesEvenWithPendingTransition`,
    mirroring the file's existing one-assertion-per-test style exactly.
  </action>
  <acceptance_criteria>
    - `shouldShow` has 4 parameters including `pendingFullscreenTransition: Bool`
    - `IsletTests/VisibilityDecisionTests.swift` contains `testPendingTransitionAloneHidesLikeFullscreen`, `testPendingTransitionIgnoredWhenHideFlagOff`, and `testNoTargetHidesEvenWithPendingTransition`
    - all 6 pre-existing test functions still compile and pass with the new 4th argument explicitly set to `false`
    - `xcodebuild test -scheme Islet -only-testing:IsletTests/VisibilityDecisionTests` passes with zero failures
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -scheme Islet -only-testing:IsletTests/VisibilityDecisionTests</automated>
  </verify>
  <done>The pure predicate layer accepts the new bounded-override input, fully covered by unit tests, matching Phase 8's already-specified option-b shape.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the CVDisplayLink poll + disambiguator, wire into updateVisibility()</name>
  <files>Islet/Notch/FullscreenSpaceProbe.swift, Islet/Notch/DisplayAnimationProbe.swift, Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/FullscreenSpaceProbe.swift (full file, 85 lines — the exact display-dict lookup
    logic in `isBuiltinDisplayInFullscreenSpace` to mirror for the new `"Spaces"`-array read, and
    the existing `CGSMainConnectionID`/`CGSCopyManagedDisplaySpaces` bindings to reuse, NOT
    reinvent)
    Islet/Notch/NotchWindowController.swift (full file — `start()` lines 234-303 for the
    monitor-construction idiom to mirror, `scheduleDeviceBatteryRefresh` lines 791-819 for the
    cancel-and-replace `DispatchWorkItem` idiom, `updateVisibility()` lines 414-441 which this
    signal must feed and never bypass, `deinit` lines 1039-1069)
    .planning/phases/08-fullscreen-enter-flash-elimination/08-ESCALATION.md ("Untried Fallback"
    section — the full Candidate B design this task implements: poll + disambiguator pairing)
    .planning/phases/08-fullscreen-enter-flash-elimination/08-02-PLAN.md (Task 2 — the
    `pendingFullscreenTransition` timeout/clear-path wiring this task mirrors for a different raw
    signal)
  </read_first>
  <action>
    In `FullscreenSpaceProbe.swift`, add `builtinDisplayHasFullscreenSpace(builtinUUID: String?) ->
    Bool` immediately after the existing `isBuiltinDisplayInFullscreenSpace` function, reusing the
    SAME `CGSMainConnectionID()`/`CGSCopyManagedDisplaySpaces` bindings and the SAME
    display-dict-selection logic (exact/`"Main"`/single-display fallback, fail-safe-to-false on any
    ambiguity) — but instead of reading `display["Current Space"]`, read `display["Spaces"] as?
    [[String: Any]]` and return `true` if ANY entry's `"type"` equals `kCGSSpaceFullscreen` (4).
    Add a `#if DEBUG` print confirming the observed `"Spaces"` array shape on first read (mirrors
    the existing `kCGSSpaceFullscreen` on-device-confirmation convention at line 81) since this
    specific key's shape is inferred, not verified in RESEARCH.md.

    Create `Islet/Notch/DisplayAnimationProbe.swift` per the `<interfaces>` block above: bind
    `SLSManagedDisplayIsAnimating(_ cid: Int32) -> Bool` via `@_silgen_name`, reusing
    `CGSMainConnectionID()` from `FullscreenSpaceProbe.swift` (same file, same Int32 type — do NOT
    create a second, independent connection-ID binding here). Implement
    `final class DisplayAnimationMonitor` owning a `CVDisplayLink` (created via
    `CVDisplayLinkCreateWithActiveCGDisplays`, started via `CVDisplayLinkStart`, stopped via
    `CVDisplayLinkStop` in `stop()`/`deinit`). The display-link output callback fires on a
    dedicated, non-main thread — it must ONLY read `SLSManagedDisplayIsAnimating(...)` and then
    `DispatchQueue.main.async` before touching any other state (mirrors the CGS-callback
    main-thread-hop discipline from Phase 8's `fullscreenProbeCallback`). On the main-thread hop,
    track `wasAnimating: Bool` and `wasFullscreenSpacePresent: Bool` (both start `false`); on each
    tick compute `animating = SLSManagedDisplayIsAnimating(...)` and `fullscreenSpacePresent =
    builtinDisplayHasFullscreenSpace(builtinUUID: builtinUUIDProvider())`; fire
    `onFullscreenTransitionDetected()` exactly on the edge `animating && !wasAnimating &&
    fullscreenSpacePresent && !wasFullscreenSpacePresent` (a NEW fullscreen-type Space appearing at
    the exact moment an animation starts) — this is the disambiguator that prevents an ordinary
    Space switch (which has no new fullscreen-type Space) from spuriously triggering.

    In `NotchWindowController.swift`: add the three properties from `<interfaces>` above. In
    `start()`, alongside the existing `powerMonitor`/`bluetoothMonitor`/`nowPlayingMonitor`
    construction: `displayAnimationMonitor = DisplayAnimationMonitor(builtinUUIDProvider: { [weak
    self] in self?.currentBuiltin()?.uuid }, onFullscreenTransitionDetected: { [weak self] in
    self?.handleFullscreenTransitionDetected() })`; `displayAnimationMonitor?.start()`. Implement
    `handleFullscreenTransitionDetected()`: set `pendingFullscreenTransition = true`, cancel any
    existing `pendingFullscreenTimeout`, schedule a fresh one-shot `DispatchWorkItem` (mirroring
    `scheduleDeviceBatteryRefresh`'s cancel-and-replace shape) that clears
    `pendingFullscreenTransition = false` and calls `updateVisibility()` after a ~1.0s bounded
    safety timeout (fail-safe-to-visible — an unconfirmed pending flag must self-clear, never hide
    permanently), then call `updateVisibility()` immediately. This function must ONLY mutate state
    and call `updateVisibility()` — never `panel?.orderOut`/`positionAndShow` directly (Pattern 7).
    Update the `updateVisibility()` call site to pass `pendingFullscreenTransition:
    pendingFullscreenTransition` as the 4th argument to `shouldShow(...)`, and — immediately after
    computing `fullscreen` — if `fullscreen == true`, clear `pendingFullscreenTransition = false`
    and cancel `pendingFullscreenTimeout` (steady-state catch-up clear). In `deinit`, add
    `displayAnimationMonitor?.stop()` and `pendingFullscreenTimeout?.cancel()`, mirroring the
    existing `powerMonitor?.stop()` / `dismissWorkItem?.cancel()` teardown lines.
  </action>
  <acceptance_criteria>
    - `Islet/Notch/FullscreenSpaceProbe.swift` contains `func builtinDisplayHasFullscreenSpace(builtinUUID: String?) -> Bool`
    - `Islet/Notch/DisplayAnimationProbe.swift` exists, contains `@_silgen_name("SLSManagedDisplayIsAnimating")`, and does NOT declare a new `CGSMainConnectionID`-equivalent binding (reuses the existing one)
    - `final class DisplayAnimationMonitor` exposes `start()` and `stop()`
    - the display-link output callback hops via `DispatchQueue.main.async` before any state mutation
    - `Islet/Notch/NotchWindowController.swift` contains `displayAnimationMonitor`, `pendingFullscreenTransition`, and `pendingFullscreenTimeout`
    - `handleFullscreenTransitionDetected` never calls `panel?.orderOut` or `positionAndShow` directly (only `updateVisibility()`)
    - `updateVisibility()` passes `pendingFullscreenTransition: pendingFullscreenTransition` to `shouldShow(...)` and clears the flag + cancels the timeout when `fullscreen == true`
    - `deinit` calls `displayAnimationMonitor?.stop()` and `pendingFullscreenTimeout?.cancel()`
    - `xcodebuild build -scheme Islet` succeeds with zero errors
    - `xcodebuild test -scheme Islet` (full suite) passes
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -scheme Islet</automated>
  </verify>
  <done>The CVDisplayLink-driven poll + disambiguator feeds the single updateVisibility() arbiter via a bounded, self-healing flag; the full test suite is green.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: On-device Candidate B verification — final investigation round (D-05)</name>
  <files>Islet/Notch/NotchWindowController.swift, Islet/Notch/DisplayAnimationProbe.swift</files>
  <action>
    No further code changes, UNLESS a regression is found — per D-04, fix any regression before
    finalizing a decision. Build and run Islet on-device and repeat the EXACT same 8-item
    checklist used in 09-01/09-02, with TWO additional high-priority checks specific to
    Candidate B's poll-based nature: (a) does the ordinary Space-switch check (item 7) ever
    spuriously trigger a hide — this is the disambiguator's core risk, since
    `SLSManagedDisplayIsAnimating` returns true for ANY display animation; (b) does the flag ever
    get stuck (island fails to reappear within ~1s of a false trigger) — this is the bounded-flag
    safety-timeout's core risk.
  </action>
  <decision>
    Does the CVDisplayLink-driven SLSManagedDisplayIsAnimating poll + disambiguator eliminate the
    fullscreen-enter flash with zero regressions, or does Candidate B (the final investigation
    round per D-05) also fail — requiring the final escalation report in 09-05?
  </decision>
  <context>
    Re-run the identical 8-item on-device checklist from 09-01-PLAN.md Task 3, plus the two
    Candidate-B-specific checks above:
    1. Flash check (3 trigger methods, ≥3 trials each).
    2. Hover/click-expand without focus steal.
    3. Click-through outside the pill.
    4. Visibility across ALL Spaces.
    5. Positioning through clamshell/external-display changes.
    6. Fullscreen hide-during/restore-on-exit (all 3 trigger methods).
    7. Ordinary (non-fullscreen) Space switch — PLUS explicitly confirm no spurious hide occurs.
    8. Lock-screen / sleep-wake.
    9. (Candidate-B-specific) Repeat an ordinary Space switch ≥5 times in quick succession; confirm zero spurious hides.
    10. (Candidate-B-specific) If any spurious hide is ever observed, confirm the island self-recovers within ~1s (the safety-timeout clearing pendingFullscreenTransition).
  </context>
  <options>
    <option id="option-accept">
      <name>Candidate B accepted — FS-01 resolved</name>
      <pros>Flash eliminated across all 3 D-07 trigger methods AND all 10 checklist items pass with zero regressions, including zero spurious hides on ordinary Space switches. Closes FS-01 via the poll-based fallback.</pros>
      <cons>None. Plan 09-05 does not execute; the phase is done.</cons>
    </option>
    <option id="option-escalate">
      <name>Candidate B fails — FINAL escalation (D-05, no further candidates)</name>
      <pros>Per D-05, this closes out the investigation definitively: FS-01 has now been root-caused across Phase 2, Phase 6, Phase 8, and Phase 9, with four distinct candidate approaches attempted (Phase 2's diagnosis, Phase 8's CGS 106/107 push signal, Phase 9's Candidate C in two configurations, Phase 9's Candidate B poll). 09-05 is already scoped to produce the final, two-option-only escalation report.</pros>
      <cons>The flash remains unresolved pending the user's explicit scope decision in 09-05 (accept as permanent tech debt, or formally descope FS-01). No further "investigate a new candidate" loop is permitted — do not attempt the `Lakr233/SkyLightWindow`-style third approach mentioned informationally in RESEARCH.md's Alternatives Considered; D-05 forecloses it.</cons>
    </option>
  </options>
  <verify>
    <human-check>Full 10-item checklist (8 shared + 2 Candidate-B-specific) executed on-device; raw pass/fail findings recorded for every item, with particular attention to spurious-hide behavior during repeated ordinary Space switches.</human-check>
  </verify>
  <done>A single option-accept/option-escalate decision is recorded in the SUMMARY, backed by the full on-device checklist evidence.</done>
  <resume-signal>Select option-accept or option-escalate, and include the full on-device checklist results (all 10 items) plus the flash-check outcome for every trigger method in this plan's SUMMARY.md so 09-05 (if triggered) can cite the complete evidence chain for the final escalation report.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SkyLight.framework (private, linked via project.yml) → app process | `SLSManagedDisplayIsAnimating`, a private symbol whose exact argument shape is not independently verified beyond "confirmed callable, returns false at rest" (RESEARCH.md Assumption A4) |
| CVDisplayLink (public CoreVideo API) → app process | The display-link output callback fires on a dedicated, high-priority, non-main thread — no Apple-documented guarantee about what app state it is safe to touch directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Denial of Service (crash) | `DisplayAnimationMonitor`'s `CVDisplayLink` output callback | mitigate | The callback ONLY calls `SLSManagedDisplayIsAnimating(...)` (a plain C function call, thread-safe) and then hops via `DispatchQueue.main.async` before touching any `self`/AppKit state — mirrors Phase 8's `fullscreenProbeCallback` main-thread-hop discipline (Pitfall 1 precedent) |
| T-09-08 | Tampering (ABI/behavior uncertainty on an under-verified private symbol) | `SLSManagedDisplayIsAnimating` binding | mitigate | Reuses the already-verified `CGSMainConnectionID() -> Int32` binding (same connection-ID family already proven working in `FullscreenSpaceProbe.swift`) rather than inventing a new, independently-typed connection binding; the on-device checkpoint (Task 3) is the empirical validation step for this under-verified symbol's actual behavior, mirroring the existing `kCGSSpaceFullscreen` on-device-confirmation convention |
| T-09-09 | Denial of Service (self-inflicted — permanent hide from a stuck flag) | `pendingFullscreenTransition` | mitigate | Bounded ~1.0s `DispatchWorkItem` safety-timeout (cancel-and-replace, mirroring `scheduleDeviceBatteryRefresh`) PLUS the steady-state catch-up clear inside `updateVisibility()` — two independent clear paths, never an unbounded override, per the fail-safe-to-visible philosophy already established in `FullscreenSpaceProbe.swift` |
| T-09-10 | Denial of Service (idle-CPU regression — an accepted design tradeoff, not a vulnerability) | `CVDisplayLink` running continuously at vsync frequency (~60Hz) for the app's entire runtime | accept | Inherent to Candidate B's poll-based design (08-ESCALATION.md) — a proactive signal fundamentally requires continuous polling; `stop()` is called in `deinit` so the link does not outlive the controller, but it cannot be paused while running without defeating the proactive-detection purpose it exists for |

</threat_model>

<verification>
`xcodebuild test -scheme Islet` (full suite) passes. On-device: the Task 3 checklist (10 items)
produces a clear, evidence-backed option-accept/option-escalate decision recorded in this plan's
SUMMARY.md.
</verification>

<success_criteria>
Either FS-01 closes via Candidate B with zero regressions, or the final investigation round (D-05)
concludes with a clear escalate decision, unblocking 09-05 to produce the two-option-only
escalation report.
</success_criteria>

<output>
Create `.planning/phases/09-fullscreen-flash-window-space-retry/09-04-SUMMARY.md` when done (or,
if Task 0 halted, the halt-record SUMMARY as specified in Task 0).
</output>
