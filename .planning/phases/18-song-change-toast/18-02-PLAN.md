---
phase: 18-song-change-toast
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - Islet/Notch/NotchWindowController.swift
  - Islet/Notch/NotchPillView.swift
autonomous: false
requirements: [NOW-05, NOW-06]

must_haves:
  truths:
    - "When playback switches to a genuinely different song (not the first track after launch, not a pause/resume/scrub of the same track), the island briefly expands downward showing the new title+artist as text, then collapses back to the compact glance after ~3s — ROADMAP Success Criterion 1"
    - "The toast does not fire for the first track detected after launch, or for pause/resume/scrub of the same track — ROADMAP Success Criterion 2"
    - "If a charging or device splash is active when a genuine change happens, the toast is skipped entirely, never shown afterward — D-02"
    - "If the notch is already manually expanded when a genuine change happens, the toast is suppressed — D-04"
    - "Rapid track skips: each new genuine change replaces the toast's content and restarts the ~3s timer — only the final settled track gets a full display, no pile-up — D-03"
    - "Settings' Activities tab toggle turns the toast on/off; turning it off while a toast is showing clears it live, not just gates future triggers — ROADMAP Success Criterion 3/4, Pitfall 4"
    - "A toast interrupted mid-display by a transient starting (charging/device head going non-nil) or by a manual expand click clears immediately and does not reappear once the interrupting state ends/collapses — checker gap fix, RESEARCH.md Pitfall 5"
  artifacts:
    - path: "Islet/Notch/NotchWindowController.swift"
      provides: "handleNowPlaying detection wiring (songChangeToastContent + songChangeToastGate), scheduleToastDismiss() one-shot timer, toggle-off live-clear in handleSettingsChanged, interruption live-clear in presentTransientChange()/handleClick()"
      contains: "func scheduleToastDismiss"
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "songChangeToastView(_:) render + mediaWingsOrToast(_:) branch consumed by the .nowPlayingWings switch case"
      contains: "func songChangeToastView"
  key_links:
    - from: "NotchWindowController.handleNowPlaying"
      to: "songChangeToastContent(previous:current:hasPlayedSinceLaunch:) + songChangeToastGate(activeTransient:isExpanded:toastEnabled:)"
      via: "both gates evaluated BEFORE nowPlayingState.songChangeToast is set or scheduleToastDismiss() is called (Pitfall 3 — never schedule-then-suppress)"
      pattern: "songChangeToastGate\\(activeTransient"
    - from: "NotchWindowController.scheduleToastDismiss"
      to: "NowPlayingState.songChangeToast"
      via: "one-shot DispatchWorkItem, cancel-then-reschedule on every genuine change, byte-for-byte mirroring scheduleMediaDismiss"
      pattern: "toastDismissWorkItem"
    - from: "NotchPillView.mediaWingsOrToast"
      to: "NowPlayingState.songChangeToast"
      via: "nowPlaying.songChangeToast read directly by the view, no resolve()/IslandPresentation involvement"
      pattern: "nowPlaying\\.songChangeToast"
    - from: "NotchWindowController.handleSettingsChanged"
      to: "toastDismissWorkItem / NowPlayingState.songChangeToast"
      via: "when activityEnabled(ActivitySettings.songChangeToastKey) is false, cancel the work item and clear the field immediately, not just gate future triggers"
      pattern: "toastDismissWorkItem\\?\\.cancel\\(\\)"
    - from: "NotchWindowController.presentTransientChange / handleClick"
      to: "NowPlayingState.songChangeToast"
      via: "live-clear (cancel toastDismissWorkItem + nil the field) the instant transientQueue.head transitions nil->non-nil or interaction.isExpanded transitions false->true, so an already-showing toast interrupted mid-display never reappears once the interrupting state ends"
      pattern: "songChangeToast = nil"
---

<objective>
Wire Plan 01's pure seam end-to-end: detect a genuine song change in `handleNowPlaying`, gate it
through `songChangeToastGate(...)`, drive its ~3s auto-dismiss via a dedicated one-shot timer
mirroring `scheduleMediaDismiss` exactly, render it as an expanded downward blob per
18-UI-SPEC.md, make the Settings toggle clear an in-flight toast live when turned off, and clear
an in-flight toast live the instant it is interrupted by a NEW transient or a manual expand
(not just when the toggle flips off).

Purpose: Completes NOW-05/NOW-06 — the toast becomes a real, observable feature on top of the
Plan 01 foundation.
Output: Controller wiring in `NotchWindowController.swift`, the toast's SwiftUI render in
`NotchPillView.swift`, and an on-device verification pass covering every ROADMAP success
criterion and CONTEXT.md decision (D-02/D-03/D-04).

### Architecture note (see 18-01-PLAN.md's "Deviation from RESEARCH.md" for the full argument)

This plan's Task 1 gates the toast entirely inside the controller
(`songChangeToastGate(...)` called from `handleNowPlaying`), never inside `resolve(...)`. This
diverges from 18-RESEARCH.md's Architectural Responsibility Map, which assigns toast
suppression to the resolver. It is a permitted CONTEXT.md-discretion choice (Plan 01 already
built the gate this way for test-surface-isolation reasons), not an oversight — this plan just
consumes Plan 01's contract as given.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-song-change-toast/18-CONTEXT.md
@.planning/phases/18-song-change-toast/18-RESEARCH.md
@.planning/phases/18-song-change-toast/18-PATTERNS.md
@.planning/phases/18-song-change-toast/18-UI-SPEC.md
@.planning/phases/18-song-change-toast/18-01-SUMMARY.md
</context>

<interfaces>
<!-- Exact current-state code this plan wires against. Verified by direct file reads this
session. Plan 01 adds songChangeToastContent(...)/songChangeToastGate(...)/TrackToast/
NowPlayingState.songChangeToast — this plan is the first to CALL them. -->

From Islet/Notch/NotchWindowController.swift (current, lines 155-165 — the work-item/timing
properties this plan adds a sibling to):
```
private var mediaDismissWorkItem: DispatchWorkItem?
private let pausedTimeout: TimeInterval = 15.0
...
private let activityDuration: TimeInterval = 3.0
```

From Islet/Notch/NotchWindowController.swift (current, lines 945-1017 — `handleNowPlaying`, the
exact integration point; `previous` and the pre-callback `hasPlayedSinceLaunch` value are
ALREADY captured here by Phase 17, reuse them, do not re-read):
```
private func handleNowPlaying(_ snapshot: TrackSnapshot?, _ art: NSImage?) {
    let p = nowPlayingPresentation(from: snapshot)
    let previous = nowPlayingState.presentation
    let previousPosition = nowPlayingState.position
    nowPlayingState.isHealthy = true
    if case .playing = p { nowPlayingState.hasPlayedSinceLaunch = true }

    withAnimation(.spring(response: springResponse, dampingFraction: springDamping)) {
        nowPlayingState.presentation = p
        nowPlayingState.position = resolvePublishedPosition(previous: previous, previousPosition: previousPosition,
                                                              incoming: p, incomingPosition: playbackPosition(from: snapshot),
                                                              now: Date().timeIntervalSince1970)
        if let art {
            nowPlayingState.artwork = art
        } else if p == .none || !isSameTrack(previous, p) {
            nowPlayingState.artwork = nil
        }
        renderPresentation()
    }
    updateVisibility()

    switch p {
    case .playing: mediaDismissWorkItem?.cancel()
    case .paused:  if previous != p { scheduleMediaDismiss(after: pausedTimeout) }
    case .none:    mediaDismissWorkItem?.cancel()
    }
}
```
IMPORTANT: `nowPlayingState.hasPlayedSinceLaunch = true` (line 966) mutates the flag BEFORE the
`withAnimation` block runs. The toast's genuine-change check needs the PRE-mutation value
(Pitfall 2) — capture it into a local BEFORE that line, e.g. `let hadPlayedSinceLaunch =
nowPlayingState.hasPlayedSinceLaunch` placed directly above the `if case .playing = p { ... }`
line, mirroring how `previous`/`previousPosition` are already captured before their own
overwrites.

From Islet/Notch/NotchWindowController.swift (current, lines 1023-1037 — `scheduleMediaDismiss`,
the exact template `scheduleToastDismiss()` mirrors):
```
private func scheduleMediaDismiss(after seconds: TimeInterval) {
    mediaDismissWorkItem?.cancel()
    let work = DispatchWorkItem { [weak self] in
        guard let self else { return }
        withAnimation(.spring(response: self.springResponse, dampingFraction: self.springDamping)) {
            self.nowPlayingState.presentation = .none
            self.nowPlayingState.artwork = nil
            self.nowPlayingState.position = nil
            self.renderPresentation()
        }
        self.updateVisibility()
    }
    mediaDismissWorkItem = work
    DispatchQueue.main.asyncAfter(deadline: .now() + seconds, execute: work)
}
```

From Islet/Notch/NotchWindowController.swift (current, lines 869-880 — the `nowPlayingKey`
live-disable branch inside `handleSettingsChanged()`, the exact precedent for this plan's
toggle-off live-clear):
```
if activityEnabled(ActivitySettings.nowPlayingKey) {
    startNowPlayingMonitor()
} else if nowPlayingMonitor != nil {
    nowPlayingMonitor?.stop(); nowPlayingMonitor = nil
    mediaDismissWorkItem?.cancel()
    nowPlayingState.presentation = .none
    nowPlayingState.artwork = nil
    nowPlayingState.position = nil
}
```

From Islet/Notch/NotchWindowController.swift (current, line 355-357 — the existing toggle-read
helper this plan reuses verbatim, no new helper):
```
private func activityEnabled(_ key: String) -> Bool {
    UserDefaults.standard.object(forKey: key) as? Bool ?? true
}
```

From Islet/Notch/NotchWindowController.swift (current, lines 480-486 — `presentTransientChange`,
the SHARED render/visibility/dismiss triplet used by both `handlePower`'s and the device
coordinator's "a new transient just became head" path — this is where this plan's interruption
live-clear #1 is inserted):
```
private func presentTransientChange() {
    withAnimation(.spring(response: springResponse, dampingFraction: springDamping)) {
        renderPresentation()
    }
    updateVisibility()
    scheduleActivityDismiss()
}
```
Per `TransientQueue.enqueue(_:)`'s own doc comment ("Returns true iff `t` becomes the head NOW
(show immediately)"), `presentTransientChange()` is called ONLY at the exact moment
`transientQueue.head` transitions from `nil` to non-nil (both the charging path at
`handlePower`'s `if changed { presentTransientChange() }` call and the device coordinator's
equivalent closure, injected at line 254, funnel through this single function on that same
transition). This makes it the single correct choke point for "a transient just started
interrupting the ambient toast" — no separate hook is needed in `handlePower`/the device
coordinator individually.

From Islet/Notch/NotchWindowController.swift (current, lines 726-748 — `handleClick`, the ONLY
path to `.expanded`; this plan's interruption live-clear #2 is inserted here):
```
private func handleClick() {
    withAnimation(.spring(response: springResponse, dampingFraction: springDamping)) {
        interaction.phase = nextState(interaction.phase, .clicked)
        // Phase 6: expand/collapse flips `isExpanded`, a resolver input — re-resolve inside
        // the SAME spring so the island morphs between the wings/expanded presentation cases.
        renderPresentation()
    }
    // Phase 10 / D-13: this is the OTHER natural-transition recheck the idle-state guard
    // depends on — a toggle-shut click (.expanded → .collapsed) applies any previously
    // deferred pendingLockoutHide right away instead of waiting for a hover-exit.
    if !interaction.isExpanded {
        updateVisibility()
    }
    // WR-02: a toggle-shut click (.expanded → .collapsed) schedules NO grace timer, so
    // without this the window would keep swallowing clicks until the next hover cycle.
    // Re-deriving from pointerInZone + phase keeps click-through correct on every click
    // (expand → interactive, toggle-shut while still in zone → still interactive until
    // exit, toggle-shut already out → pass-through).
    syncClickThrough()
}
```
`handleClick()` is the ONLY function that can flip `interaction.isExpanded` from `false` to
`true` (the click-to-expand transition) — no other call site sets it true, so this is the
single correct choke point for "the user just manually expanded, interrupting the ambient
toast".

From Islet/Notch/NotchPillView.swift (current, lines 133-148 — the switch this plan's ONE case
body changes; every other case is untouched):
```
switch presentation {
case .charging(let a):
    wings(for: a)
case .device(let d):
    deviceWings(for: d)
case .nowPlayingWings(let p):
    mediaWings(p, art: nowPlaying.artwork)
case .nowPlayingExpanded(let p, true):
    mediaExpanded(p, art: nowPlaying.artwork)
case .nowPlayingExpanded(_, false):
    mediaUnavailable
case .expandedIdle:
    expandedIsland
case .idle:
    collapsedIsland
}
```

From Islet/Notch/NotchPillView.swift (current, lines 220-230 — `blobShape`, reused verbatim by
the new toast view; and lines 332-345 — `deviceTrailing`, the exact `@ViewBuilder` if/else
precedent this plan's new `mediaWingsOrToast` helper mirrors):
```
private func blobShape<Content: View>(topCornerRadius: CGFloat,
                                       bottomCornerRadius: CGFloat,
                                       alignment: Alignment = .center,
                                       @ViewBuilder content: () -> Content) -> some View {
    NotchShape(topCornerRadius: topCornerRadius, bottomCornerRadius: bottomCornerRadius)
        .fill(Color.black)
        .matchedGeometryEffect(id: "island", in: ns)
        .frame(width: Self.expandedSize.width, height: Self.expandedSize.height)
        .overlay(alignment: alignment) { content() }
        .onTapGesture { onClick() }
}
```
```
@ViewBuilder
private func deviceTrailing(isConnected: Bool, battery: Int?) -> some View {
    if isConnected, let battery {
        BatteryIndicator(level: battery)
    } else {
        Image(systemName: isConnected ? "checkmark" : "xmark")
            .font(.system(size: 13, weight: .bold))
            .foregroundStyle(isConnected ? accent : Color.white.opacity(0.5))
    }
}
```
NOTE on panel geometry: the panel window frame is a fixed UNION of `expandedNotchFrame` and
`wingsFrame`, computed once in `NotchWindowController` (`panelFrame = expandedFrame.union(wings)`,
~line 584) and independent of the current `IslandPresentation` case. Rendering the toast's full
`Self.expandedSize` (360x144) blob inside the `.nowPlayingWings` case — which normally renders
the smaller 290x32 `wingsShape` strip — requires NO panel/window frame changes; the content is
always free to draw up to the union size.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Controller wiring — detection, ~3s dismiss timer, toggle-off + interruption live-clear</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift, Islet/Notch/IslandResolver.swift, Islet/Notch/NowPlayingPresentation.swift, Islet/Notch/NowPlayingState.swift, Islet/ActivitySettings.swift
  </read_first>
  <action>
    Add `private var toastDismissWorkItem: DispatchWorkItem?` directly below the existing
    `private var mediaDismissWorkItem: DispatchWorkItem?` (line 158).

    In `handleNowPlaying(_:_:)`: insert `let hadPlayedSinceLaunch = nowPlayingState.hasPlayedSinceLaunch`
    directly above the existing `if case .playing = p { nowPlayingState.hasPlayedSinceLaunch = true }`
    line (~966), capturing the PRE-mutation value (Pitfall 2 — mirrors how `previous`/
    `previousPosition` are captured before their own overwrites just above). Then, inside the
    existing `withAnimation { ... }` block, immediately AFTER the `renderPresentation()` call
    (still inside the closure), add the toast trigger: compute `songChangeToastGate(activeTransient:
    transientQueue.head, isExpanded: interaction.isExpanded, toastEnabled:
    activityEnabled(ActivitySettings.songChangeToastKey))` and `songChangeToastContent(previous:
    previous, current: p, hasPlayedSinceLaunch: hadPlayedSinceLaunch)` — if the gate returns true
    AND the content function returns a non-nil `TrackToast`, assign it to
    `nowPlayingState.songChangeToast` and call `scheduleToastDismiss()`. Per Pitfall 3, both pure
    checks MUST be evaluated before `nowPlayingState.songChangeToast` is mutated or
    `scheduleToastDismiss()` is called — never schedule then suppress. Add a comment referencing
    Phase 18 / NOW-05 / D-02 / D-03 / D-04 explaining why this sits inside the same spring block
    (the toast's appearance animates together with the rest of this callback's mutation) and why
    it never touches `resolve(...)`/`IslandPresentation` (Plan 01's design decision, see 18-01's
    `<objective>` "Deviation from RESEARCH.md" note).

    Add a new `scheduleToastDismiss()` private function directly below `scheduleMediaDismiss(after:)`
    (after its closing brace, before the D-13 mid-session-death section). Mirror
    `scheduleMediaDismiss` exactly: cancel `toastDismissWorkItem` first, build a single
    `DispatchWorkItem` that — inside `withAnimation(.spring(response: self.springResponse,
    dampingFraction: self.springDamping))` — sets `self.nowPlayingState.songChangeToast = nil` (and
    ONLY that; do not touch `presentation`/`artwork`/`position`/`renderPresentation()`/
    `updateVisibility()` — the toast is orthogonal to the resolver's presentation, so clearing it
    needs no re-resolve), assign it to `toastDismissWorkItem`, and schedule it via
    `DispatchQueue.main.asyncAfter(deadline: .now() + activityDuration, execute: work)` — reuse the
    existing `activityDuration` constant (3.0s), do NOT add a new duplicate literal or reuse
    `pausedTimeout` (15.0s, unrelated).

    In `handleSettingsChanged()`, directly after the existing "Now Playing" block (after line 880,
    before `applyAccentIfChanged()`), add the toast toggle's live-clear branch (Pitfall 4, mirroring
    the `nowPlayingKey` disable branch's discipline exactly): if
    `!activityEnabled(ActivitySettings.songChangeToastKey)` AND
    `nowPlayingState.songChangeToast != nil`, cancel `toastDismissWorkItem` and, inside
    `withAnimation(.spring(response: springResponse, dampingFraction: springDamping))`, set
    `nowPlayingState.songChangeToast = nil`. This must fire immediately when the toggle flips off
    mid-toast, not just gate future triggers.

    **Interruption live-clear (RESEARCH.md Pitfall 5 — checker gap fix):** a toast already showing
    must clear the instant it is interrupted by a NEW transient or a manual expand, not just when
    the toggle flips off — otherwise it can reappear once the interrupting state ends. Add exactly
    two insertions, both idempotent no-ops when no toast is currently showing (guarded by
    `!= nil`), mirroring the toggle-off branch's cancel+clear shape:

    1. In `presentTransientChange()` (see `<interfaces>` above for its exact current 4-line body):
       add, as the FIRST statement inside its `withAnimation { ... }` block (before the existing
       `renderPresentation()` call), `if nowPlayingState.songChangeToast != nil {
       toastDismissWorkItem?.cancel(); nowPlayingState.songChangeToast = nil }`. Because
       `presentTransientChange()` is called ONLY at the exact moment `transientQueue.head`
       transitions from `nil` to non-nil (per `TransientQueue.enqueue(_:)`'s own "returns true iff
       `t` becomes the head NOW" contract — see `<interfaces>`), this single insertion covers BOTH
       the charging and device interruption paths with no duplicate logic in `handlePower`/the
       device coordinator.

    2. In `handleClick()` (see `<interfaces>` above for its exact current body): capture
       `let wasExpanded = interaction.isExpanded` directly above the existing `withAnimation { ... }`
       block. Inside that block, directly after `interaction.phase = nextState(interaction.phase,
       .clicked)` and BEFORE the existing `renderPresentation()` call, add: `if !wasExpanded &&
       interaction.isExpanded && nowPlayingState.songChangeToast != nil { toastDismissWorkItem?
       .cancel(); nowPlayingState.songChangeToast = nil }` — this fires only on the expand
       transition (never on a toggle-shut click, since `wasExpanded` would already be `true` there).

    Add a short comment at each of these two insertion points referencing Phase 18 / RESEARCH.md
    Pitfall 5 / D-02 / D-04, and noting neither insertion changes the pre-existing behavior of
    `presentTransientChange()`/`handleClick()` for any caller that isn't currently showing a toast.
  </action>
  <acceptance_criteria>
    - `grep -c "toastDismissWorkItem" Islet/Notch/NotchWindowController.swift` returns at least 6 (property declaration, cancel-before-schedule in the trigger path, cancel+assign inside scheduleToastDismiss, cancel inside the toggle-off branch, cancel inside presentTransientChange's live-clear, cancel inside handleClick's live-clear)
    - `grep -c "func scheduleToastDismiss" Islet/Notch/NotchWindowController.swift` returns 1
    - `songChangeToastGate(` and `songChangeToastContent(` both appear inside `handleNowPlaying`'s body, and both are evaluated (as part of the same `if`/guard condition or sequential `let` bindings feeding one `if`) BEFORE any assignment to `nowPlayingState.songChangeToast` in that function
    - `handleSettingsChanged()` contains a branch keyed on `!activityEnabled(ActivitySettings.songChangeToastKey)` that cancels `toastDismissWorkItem` and clears `nowPlayingState.songChangeToast`
    - `presentTransientChange()`'s body contains a `nowPlayingState.songChangeToast != nil` guard that cancels `toastDismissWorkItem` and clears `nowPlayingState.songChangeToast`, placed before its existing `renderPresentation()` call
    - `handleClick()`'s body contains a `wasExpanded` local captured before its `withAnimation` block, and a guard on `!wasExpanded && interaction.isExpanded` that clears `nowPlayingState.songChangeToast`
    - `scheduleToastDismiss()`'s body never assigns to `nowPlayingState.presentation`, `.artwork`, or `.position`, and never calls `renderPresentation()` or `updateVisibility()`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -20 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <done>handleNowPlaying detects a genuine change via songChangeToastContent(...), suppresses it per songChangeToastGate(...) computed before any mutation, and schedules the toast's own ~3s dismiss via the new scheduleToastDismiss(). handleSettingsChanged clears an in-flight toast live when the toggle turns off. presentTransientChange() and handleClick() each clear an in-flight toast live the instant an interrupting transient/expand begins, so an interrupted toast never reappears once the interrupting state ends. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Toast render — songChangeToastView + mediaWingsOrToast branch</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift, .planning/phases/18-song-change-toast/18-UI-SPEC.md
  </read_first>
  <action>
    In the `body` switch (lines 133-148), change the `.nowPlayingWings(let p):` case body from
    `mediaWings(p, art: nowPlaying.artwork)` to `mediaWingsOrToast(p)`. Every other case is
    untouched.

    Add a new `@ViewBuilder private func mediaWingsOrToast(_ p: NowPlayingPresentation) -> some View`
    directly above `mediaWings(_:art:)` (before line 284), following the exact `@ViewBuilder`
    if/else shape of the existing `deviceTrailing(isConnected:battery:)` helper (lines 336-345):
    `if let toast = nowPlaying.songChangeToast { songChangeToastView(toast) } else { mediaWings(p,
    art: nowPlaying.artwork) }`.

    Add a new `private func songChangeToastView(_ toast: TrackToast) -> some View` directly below
    `mediaWingsOrToast`, before `mediaWings(_:art:)`. Per 18-UI-SPEC.md's Motion & Interaction
    Contract and Typography sections: reuse `blobShape(topCornerRadius: 6, bottomCornerRadius: 20)`
    with the DEFAULT `.center` alignment (do not pass `.top` — that is `mediaExpanded`'s
    camera-clearance need, not this content's). Inside the overlay content, render a
    `VStack(spacing: 2)` (the UI-SPEC's documented 2px tight-kerning exception, NOT the 1px
    `mediaExpanded` value or the base 4pt scale) containing: `Text(toast.title)` styled
    `.font(.system(size: 15, weight: .bold, design: .rounded))`, `.foregroundStyle(.white)`,
    `.lineLimit(1)`, `.truncationMode(.tail)`; then `Text(toast.artist)` styled
    `.font(.system(size: 12, design: .rounded))`, `.foregroundStyle(.secondary)`, `.lineLimit(1)`,
    `.truncationMode(.tail)`. Apply `.padding(.horizontal, 16)` to the VStack (UI-SPEC's documented
    16px horizontal inset, matching `expandedIsland`'s pattern). Do NOT add a new tap gesture — it
    is inherited automatically from `blobShape`'s `.onTapGesture { onClick() }`. Do NOT introduce
    any new size constant — the frame comes entirely from `blobShape`'s `Self.expandedSize`.
  </action>
  <acceptance_criteria>
    - `grep -c "func mediaWingsOrToast" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "func songChangeToastView" Islet/Notch/NotchPillView.swift` returns 1
    - The `.nowPlayingWings(let p):` case body reads `mediaWingsOrToast(p)`, not `mediaWings(p, art: nowPlaying.artwork)` directly
    - `songChangeToastView`'s body calls `blobShape(topCornerRadius: 6, bottomCornerRadius: 20)` with no `alignment:` argument (i.e. uses the default `.center`)
    - `songChangeToastView`'s VStack uses `spacing: 2`
    - Every other `case` in the `body` switch is byte-for-byte unchanged from before this task
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -20 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <done>The .nowPlayingWings case renders the toast (center-aligned expanded blob, title+artist, UI-SPEC tokens) when nowPlaying.songChangeToast is non-nil, and falls back to the unchanged collapsed mediaWings strip otherwise. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device verification — toast trigger, suppression, interruption, rapid-skip, toggle</name>
  <what-built>
    NOW-05: a genuine song change (while ambient/collapsed, no charging/device splash active, not
    manually expanded) briefly expands the island downward to show the new track's title+artist
    as text, then collapses back after ~3s. The first track detected after launch never toasts;
    pausing/resuming/scrubbing the same track never toasts (isSameTrack semantics preserved).
    Rapid skips replace the toast's content and restart the timer in place — no pile-up. A toast
    interrupted mid-display by a NEW transient or a manual expand clears immediately and does not
    reappear once the interrupting state ends (RESEARCH.md Pitfall 5 fix).
    NOW-06: a new "Song-Change Toast" toggle in Settings' Activities tab (default on) turns this
    off; turning it off while a toast is on-screen clears it immediately. Automated build gate
    passed (`xcodebuild build` — BUILD SUCCEEDED) for both plans; `xcodebuild test` is not used
    per project memory (hangs headless in this app) — run the 10 new Plan 01 unit tests manually
    via Cmd-U first, then do the on-device checks below.
  </what-built>
  <how-to-verify>
    In Xcode, open Islet.xcodeproj, select the Islet scheme, and run the test suite with Cmd-U —
    confirm the 5 new NowPlayingPresentationTests.swift tests (testSongChangeToastContent*) and
    the 5 new IslandResolverTests.swift tests (testSongChangeToastGate*) pass alongside the
    existing suite.

    Then, on-device (build + run the Islet target from Xcode, Cmd-R), using Spotify or Apple Music:
    1. Launch Islet, start playback of any track (lifts the Phase 17 launch gate), let the glance
       settle. Expected: NO toast appears for this first track — only Phase 17's normal ambient
       glance shows.
    2. Skip to a genuinely different track. Expected: the island briefly expands downward showing
       the new title + artist as centered white/grey text, then collapses back to the compact
       glance after ~3s.
    3. Pause, then resume the SAME track (no track change). Expected: no toast — the same-track
       play/pause transition does not re-trigger it.
    4. Skip through 3-4 tracks quickly (within a few seconds of each other). Expected: the toast
       does not flicker/collapse-then-reexpand between skips — it stays expanded and the text swaps
       in place, and only the FINAL settled track gets a full ~3s display from that point.
    5. Plug in the charger (or connect/disconnect a Bluetooth device) so a charging/device splash is
       showing, then skip to a new track while it's still showing. Expected: no toast appears at
       all, during or after the splash (D-02 — skipped entirely, not queued).
    6. Manually expand the notch (click/hover to show the full Now Playing card), then skip to a
       new track while expanded. Expected: no toast — the expanded card already shows the new
       title/artist live, no redundant toast on top (D-04).
    7. Open Settings (Activities tab) and confirm the "Song-Change Toast" toggle appears directly
       after "Now Playing", default ON.
    8. Skip to a new track to trigger a toast, and WHILE it is still on-screen, turn the toggle OFF
       in Settings. Expected: the toast disappears immediately, not after its natural ~3s timeout.
    9. With the toggle OFF, skip to another new track. Expected: no toast appears. Toggle it back ON
       and confirm skipping to yet another new track shows the toast again.
    10. Skip to a new track to trigger the toast, and WHILE it is still on-screen (before the ~3s
        timer completes), plug in the charger (or click to manually expand the notch). Expected: the
        toast disappears immediately as the charging splash/expanded card takes over. Let the splash
        finish (or click again to collapse the expanded card) and confirm the toast does NOT
        reappear afterward — the interrupted toast is gone for good, not just hidden behind the
        interrupting state.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MediaRemote adapter -> `handleNowPlaying` | The existing Phase 4 trust boundary (already-classified `NowPlayingPresentation`, not raw adapter output) — this plan adds no NEW boundary, it consumes the same classified data the ambient glance already renders. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Tampering | `songChangeToastView` — untrusted track-title/artist text rendered as SwiftUI `Text` | accept | SwiftUI `Text` does not interpret its string content as markup/HTML (unlike a WKWebView render path) — no injection risk. Both lines are `.lineLimit(1)` + `.truncationMode(.tail)`, bounding an over-long adversarial title/artist from breaking layout, mirroring the identical mitigation already applied to `mediaExpanded`'s own title/artist. |
| T-18-05 | Denial of Service (local) | `toastDismissWorkItem` timer-race vs `mediaDismissWorkItem`/`dismissWorkItem` | mitigate | The toast's dismiss timer is a fully independent `DispatchWorkItem` property, never sharing state with `mediaDismissWorkItem` — cancel-then-reschedule on every genuine change (D-03), plus the new interruption live-clear (presentTransientChange()/handleClick()), bounds it to at most one pending toast timer at any moment, identical to the existing `scheduleMediaDismiss`/`scheduleActivityDismiss` precedent already proven safe in this codebase. |
| T-18-06 | Tampering | `ActivitySettings.songChangeToastKey` UserDefaults toggle | accept | Already covered in 18-01's threat model (T-18-03) — `activityEnabled(_:)`'s existing `as? Bool ?? true` fallback degrades safely; unchanged by this plan. |

</threat_model>

<verification>
1. `xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug` reports BUILD SUCCEEDED after Task 2.
2. Cmd-U in Xcode: all Plan 01's 10 new unit tests pass alongside the full existing suite.
3. On-device checkpoint (Task 3) confirms all 4 ROADMAP Phase 18 success criteria plus D-02/D-03/D-04 nuances, Pitfall 4's live-clear behavior, and Pitfall 5's interruption live-clear behavior.
</verification>

<success_criteria>
- A genuine song change triggers the toast (title+artist, ~3s, expand-then-collapse) exactly once per settled track.
- The first track after launch and same-track play/pause/scrub never trigger it.
- Charging/device splash and manual-expand both silently suppress it with zero queueing.
- Rapid skips coalesce into a single in-place content swap, only the final track gets a full display.
- The Settings toggle exists, defaults on, and clears an in-flight toast live when turned off.
- An in-flight toast interrupted mid-display by a new transient or a manual expand clears immediately and never reappears once the interrupting state ends.
</success_criteria>

<output>
Create `.planning/phases/18-song-change-toast/18-02-SUMMARY.md` when done
</output>
