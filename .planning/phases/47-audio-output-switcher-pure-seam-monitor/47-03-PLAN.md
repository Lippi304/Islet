---
phase: 47-audio-output-switcher-pure-seam-monitor
plan: 03
type: execute
wave: 3
depends_on: ["47-02"]
files_modified:
  - IsletTests/AudioOutputMonitorManualSpike.swift
autonomous: false
requirements: [D-03]

must_haves:
  truths:
    - "A manual (non-CI) Cmd-U test harness exercises AudioOutputMonitor against real hardware and prints each device's uid/name/isDefault/hasVolumeControl to the Xcode console, without any NotchWindowController/NotchPillView wiring (Phase 48 scope, per 47-CONTEXT.md's explicit phase boundary)."
    - "On-device verification confirms: device UID stability across a Bluetooth disconnect/reconnect cycle (no duplicate/stale entries — Pitfall 4), a live default-output switch via setDefaultOutput that is confirmed-after-set (Pitfall 8), and per-device volume-property support checked against built-in speakers, 2 distinct Bluetooth devices, and a USB/wired output device (D-03's widened verification scope)."
  artifacts:
    - path: "IsletTests/AudioOutputMonitorManualSpike.swift"
      provides: "Manual on-device verification harness for AudioOutputMonitor"
      contains: "MANUAL SPIKE"
  key_links:
    - from: "IsletTests/AudioOutputMonitorManualSpike.swift"
      to: "Islet/Notch/AudioOutputMonitor.swift start()/setDefaultOutput()/hasVolumeControl()/stop()"
      via: "direct instantiation and method calls, exercised via a single manually-run XCTest"
      pattern: "AudioOutputMonitor\\("
---

<objective>
Verify `AudioOutputMonitor` (Plan 47-02) against real hardware — the one part of this phase that
cannot be unit-tested — via a throwaway manual Cmd-U harness plus an on-device checkpoint covering
D-03's widened verification scope (built-in speakers, 2 distinct Bluetooth devices, 1 USB/wired
device), closing out ROADMAP Phase 47 Success Criteria 2 and 4.

Purpose: Confirms the monitor is genuinely correct on this machine's real audio hardware — not just
plausible from reading CoreAudio documentation — before Phase 48 builds a UI on top of it with
confidence, mirroring this project's own "verify on real hardware, not just API surface" lesson from
Phase 38/39.

Output: `IsletTests/AudioOutputMonitorManualSpike.swift` (a manual-only test harness, never part of
the automated `xcodebuild test` gate) + a completed on-device checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-audio-output-switcher-pure-seam-monitor/47-CONTEXT.md
@.planning/research/PITFALLS.md
</context>

<interfaces>
<!-- Plan 47-02's public surface this harness calls directly. -->
```swift
@MainActor
final class AudioOutputMonitor {
    init(onDevicesChanged: @escaping ([AudioOutputDevice]) -> Void)
    func start()
    nonisolated func stop()
    func setDefaultOutput(_ device: AudioOutputDevice, completion: @escaping (Bool) -> Void)
    func hasVolumeControl(deviceUID: String) -> Bool
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Manual on-device spike harness</name>
  <files>IsletTests/AudioOutputMonitorManualSpike.swift</files>
  <read_first>
    Islet/Notch/AudioOutputMonitor.swift (Plan 47-02's output — the exact start()/stop()/setDefaultOutput()/hasVolumeControl() signatures this harness calls)
    Islet/Notch/AudioOutputPresentation.swift (AudioOutputDevice's fields to print)
    IsletTests/OSDActivityTests.swift (header/test-class style to loosely match — this file's own header will be distinctly different, see action)
  </read_first>
  <action>
    Create `IsletTests/AudioOutputMonitorManualSpike.swift` (NEW FILE). Header comment, in caps,
    states: "MANUAL SPIKE — DO NOT RUN VIA `xcodebuild test` (the full Islet.app test host hangs
    headless — this project's established xcodebuild-test-headless-hang precedent). Run via Xcode
    Cmd-U for THIS single test method only, then read the Xcode console and follow the D-03
    on-device verification steps in 47-03-PLAN.md Task 2."

    Define `final class AudioOutputMonitorManualSpike: XCTestCase` with one method
    `func testManualDeviceEnumerationAndSwitch()`. Body: declare `var lastDevices: [AudioOutputDevice] = []`
    captured by reference; instantiate
    `let monitor = AudioOutputMonitor(onDevicesChanged: { devices in lastDevices = devices; devices.forEach { device in print("[AudioOutputSpike] uid=\(device.uid) name=\(device.name) isDefault=\(device.isDefault) hasVolumeControl=\(monitor.hasVolumeControl(deviceUID: device.uid))") } })`
    (note: `monitor` is captured inside its own closure — declare it with `var` and assign after
    construction, or restructure as a local function, whichever compiles cleanly under Swift's
    definite-initialization rules); call `monitor.start()`; run
    `RunLoop.current.run(until: Date().addingTimeInterval(15))` so the initial `onDevicesChanged`
    print settles; then, if `lastDevices.first(where: { !$0.isDefault })` exists, call
    `monitor.setDefaultOutput(that device) { success in print("[AudioOutputSpike] switch result: \(success)") }`;
    then `RunLoop.current.run(until: Date().addingTimeInterval(45))` for a further ~45s window so the
    developer can manually connect/disconnect the 2 Bluetooth devices and the USB/wired device per
    D-03 while console output continues to update; finally call `monitor.stop()`. End with
    `XCTAssertTrue(true, "manual spike — see console output and 47-03-PLAN.md Task 2 for the real pass/fail criteria")`
    so the test always reports green in Xcode's test navigator — the actual verification is the
    human-read console output plus Task 2's checkpoint, never this trivial assertion.
  </action>
  <acceptance_criteria>
    - `grep -c "MANUAL SPIKE" IsletTests/AudioOutputMonitorManualSpike.swift` returns 1
    - `grep -c "class AudioOutputMonitorManualSpike: XCTestCase" IsletTests/AudioOutputMonitorManualSpike.swift` returns 1
    - `grep -c "monitor.start()" IsletTests/AudioOutputMonitorManualSpike.swift` returns 1
    - `grep -c "monitor.setDefaultOutput(" IsletTests/AudioOutputMonitorManualSpike.swift` returns 1
    - `grep -c "monitor.stop()" IsletTests/AudioOutputMonitorManualSpike.swift` returns 1
    - `cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5` ends with `** BUILD SUCCEEDED **` (build only — do NOT run `xcodebuild test`, per this file's own header)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <done>Manual spike harness compiles, is clearly marked manual-only, exercises start/enumerate/switch/stop against AudioOutputMonitor; ready for Task 2's on-device Cmd-U run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device D-03 verification</name>
  <files></files>
  <action>
    Run `IsletTests/AudioOutputMonitorManualSpike.swift`'s `testManualDeviceEnumerationAndSwitch()`
    via Xcode Cmd-U (single-test run, not the whole suite) with Plan 47-02's `AudioOutputMonitor` in
    place, then execute the 8 steps below in order. This task is verification-only — no further code
    changes unless a check fails, in which case report exactly which step and what happened instead.
  </action>
  <what-built>
    AudioOutputMonitor (event-driven CoreAudio device monitor, Plan 47-02) + AudioOutputPresentation
    (pure device/sort seam, Plan 47-01) + a manual Cmd-U spike harness (Task 1) that enumerates real
    audio-output devices, prints their uid/name/default-flag/volume-control-support, and exercises
    one live default-output switch with confirm-after-set.
  </what-built>
  <how-to-verify>
    1. In Xcode, open `IsletTests/AudioOutputMonitorManualSpike.swift`. Click the diamond next to
       `testManualDeviceEnumerationAndSwitch()` to run ONLY this test — do not run the whole
       `IsletTests` suite (Cmd-U on the whole suite may hit this project's known
       xcodebuild-test-headless-hang issue for unrelated tests).
    2. Before running, have ready: 2 distinct Bluetooth output devices (per D-03, to catch
       codec/implementation differences between them, not just Bluetooth-vs-built-in) and 1
       USB/wired output device. Leave built-in speakers as the initial system default.
    3. Run the test. Open the Xcode console (View -> Debug Area -> Activate Console). During the
       first ~15s, confirm at least the built-in speakers device is printed with a real (non-empty)
       uid, a real name, `isDefault=true`, and a `hasVolumeControl` result.
    4. During the following ~45s window: connect and disconnect each of the 2 Bluetooth devices in
       turn (at least one full disconnect+reconnect cycle for each), then connect the USB/wired
       device. Note the printed uid/name/hasVolumeControl line for each.
    5. Confirm the Bluetooth reconnect case specifically: the SAME uid string reappears after a
       disconnect+reconnect (not a new/different value) — this is the Pitfall 4 regression check.
    6. Confirm the automatic default-output switch (triggered ~15s in, printed as
       `[AudioOutputSpike] switch result: ...`) reports `true`, and confirm via System Settings ->
       Sound (or the menu-bar Sound icon) that the output actually changed to the device the monitor
       selected.
    7. Confirm no crash, no Xcode purple runtime warning (main-thread-checker violation), and no
       hang for the full ~60s test run.
    8. Note which of the 4 tested devices (built-in, Bluetooth #1, Bluetooth #2, USB/wired) report
       `hasVolumeControl=true` vs `false` — this becomes Phase 48's authoritative input for which
       devices get a real slider vs a disabled one.
  </how-to-verify>
  <verify>
    <human-check>All 8 steps above behave as expected on-device.</human-check>
  </verify>
  <done>Device UID stability across a Bluetooth reconnect is confirmed (Pitfall 4); the automatic default-output switch is confirmed-after-set and audibly/visually verified (Pitfall 8); hasVolumeControl results are recorded for all 4 tested devices (built-in, 2x Bluetooth, USB/wired — D-03); no crash, hang, or main-thread-checker warning occurred during the ~60s run.</done>
  <resume-signal>Reply "approved" with the 4 devices' hasVolumeControl results, or describe any issue (missing device, duplicate UID after reconnect, switch never confirmed, crash, hang).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Manual test-only code path | `AudioOutputMonitorManualSpike.swift` runs solely inside a developer-triggered Xcode test session on the dev machine — never shipped in the Islet.app target, never part of automated CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-47-06 | Tampering | Manual spike harness's automatic setDefaultOutput call | accept | Test-only code, run solely by the developer on their own dev machine during a single manually-triggered Cmd-U on-device UAT session; never part of the shipped Islet.app target or automated `xcodebuild test` CI gate, clearly marked MANUAL SPIKE in its own file header. |

No Spoofing / Repudiation / Information Disclosure / Denial-of-Service / Elevation-of-Privilege
entries — this plan adds no shipped code, only a developer-only diagnostic test harness.
</threat_model>

<verification>
- `xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug` succeeds after Task 1.
- Task 2's on-device checkpoint is the real verification gate for ROADMAP Phase 47 Success Criteria 2 and 4 — no further automated check exists for real-hardware CoreAudio behavior.
</verification>

<success_criteria>
- Device UID stability across a Bluetooth reconnect confirmed on real hardware (Pitfall 4 regression check passes).
- A live default-output switch is confirmed-after-set (Pitfall 8), matching ROADMAP Phase 47's implicit "verify the switch actually stuck" expectation.
- Per-device volume-property support is verified against built-in speakers, 2 distinct Bluetooth devices, and a USB/wired device (ROADMAP Phase 47 Success Criterion 4 / D-03's widened scope).
- Phase 47 is complete: the pure seam (47-01), the monitor (47-02), and its on-device proof (47-03) are all done — Phase 48 can build the UI with confidence.
</success_criteria>

<output>
Create `.planning/phases/47-audio-output-switcher-pure-seam-monitor/47-03-SUMMARY.md` when done
</output>
