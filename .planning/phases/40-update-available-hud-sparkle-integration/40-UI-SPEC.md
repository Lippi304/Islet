---
phase: 40
slug: update-available-hud-sparkle-integration
status: draft
shadcn_initialized: false
preset: not applicable — native SwiftUI/AppKit app, no component-registry tooling
created: 2026-07-18
---

# Phase 40 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**Scope note:** This phase's ONLY new visual surface is a small persistent corner badge on the collapsed pill (HUD-06) plus one new Settings toggle row (D-11) and one new menu item ("Check for Updates…"). There is NO new wing, NO new `IslandPresentation`/`ActiveTransient` case, NO new material — per `40-CONTEXT.md`'s domain statement and `ARCHITECTURE.md` Integration Point 5, the badge is an orthogonal `@Published` flag rendered as a corner overlay, never a `TransientQueue` participant. Tapping the badge (or the menu item) opens Sparkle's own standard alert dialog — that dialog's UI is NOT designed here; it is out of scope per REQUIREMENTS.md's "no full custom Sparkle install/progress flow."

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — native macOS Swift/SwiftUI app, no component-registry tooling (shadcn/etc. do not apply to this stack) |
| Component library | none — hand-built `NotchShape`/`NotchPillView`, no third-party UI kit. Sparkle's own `SPUStandardUserDriver` renders its dialog with its own native `NSAlert`/`NSWindow` chrome — not styled by this app |
| Icon library | SF Symbols (`Image(systemName:)`) — established convention, unchanged. Badge: `arrow.up.circle.fill` (the standard macOS "update available" glyph family, matches the existing `capsule.fill` menu-bar icon's monochrome SF Symbol convention) |
| Font | San Francisco system font, `.system(size:weight:design:)` — no new font |

---

## Spacing Scale

This phase uses the project's standard 8-point scale: **4, 8, 16, 24, 32, 48, 64**. The badge's corner inset (below) uses **4pt**, the smallest step on the scale — no exceptions needed for this phase.

---

## Update-Available Badge Contract (HUD-06)

**Structural constraint (D-05/D-06, locked):** a small, fixed corner overlay on the collapsed pill only — NOT a wing, NOT a `wingsShape()` call, NOT routed through `resolve()`/`IslandPresentation`. It renders regardless of whichever `IslandPresentation` case is currently active on the collapsed pill (idle, Now Playing wings, Charging/Device/Focus/OSD splash) and disappears entirely once the island expands (D-06). Implementation-wise this means the badge overlay is attached at the OUTERMOST collapsed-pill container (the one wrapping the `switch presentation` in `NotchPillView`), not inside any individual case body — the same "must render above/beside whatever else is showing" requirement the badge's own D-05 describes as "conceptually similar to an app-icon badge dot."

**No idle/absent visual variant to design:** the badge is either in the view tree (update pending) or it is not (D-13 — pure reflection of live `SPUUpdaterDelegate` state, no separate "checking…"/"up to date" visual state exists).

| Property | Value |
|----------|-------|
| Shape | `Image(systemName: "arrow.up.circle.fill")` — a filled glyph badge, not a bare `Circle()` dot. D-05 says "dot/icon"; D-08 ("icon-only, no hover tooltip") confirms the glyph-badge reading, not a plain color dot — the glyph itself communicates "update" without needing a tooltip |
| Size | `.font(.system(size: 12, weight: .semibold))` — one step up from the 8pt plain-dot precedent (Focus's `Circle().fill(Color.green).frame(width: 8, height: 8)`, `NotchPillView.swift` ~L2209) because this badge carries a recognizable glyph, not just a color signal; stays inside the same visual weight class as every other wing icon (13pt) without competing with it |
| Rendering mode | `.symbolRenderingMode(.hierarchical)` — matches every other SF Symbol glyph in this codebase (wing icons, moon, bolt) |
| Placement | `.overlay(alignment: .topTrailing)` on the collapsed pill's outer container, inset **4pt** from the pill's top-trailing corner (`.offset(x: -4, y: 4)` or equivalent) — the smallest step on the project's 8-point spacing scale, close enough to read as flush-adjacent to the corner while still mirroring the "app-icon badge dot" reference in D-05 (top-right corner, small inset, not flush with the edge) |
| Interaction | Tappable — same `.onTapGesture` convention as other interactive glyphs in this codebase (e.g. shelf trash icons). Tap calls `updaterController.checkForUpdates(nil)` (or the equivalent action wired to the badge's own tap handler), surfacing Sparkle's standard dialog. No hover state, no tooltip (D-08). Set `.accessibilityLabel("Update available")` on the tappable glyph — VoiceOver still needs text even though D-08 locks out a visible hover tooltip |
| Color (D-07, resolved) | Reuses `env.nowPlayingAccent` (the existing per-element accent `EnvironmentKey` from Phase 27) — **not** a new 4th accent picker row, satisfying D-07's "no new standalone color parameter." Rationale: the badge sits on the collapsed pill regardless of content, and the collapsed pill's default/idle/most-common state is the Now Playing surface — reusing that accent avoids inventing a new Settings control while still following "the existing per-element accent-theming convention" as D-07 requires (this differs from Focus/OSD's `Color.white`/`Color.green`/`Color.orange`, which are deliberately fixed "system-level state ignores theme" colors — D-07 explicitly opts OUT of that pattern for this badge) |
| Visibility across states | Renders identically regardless of the active `IslandPresentation` case underneath (idle pill, Now Playing wings, Charging/Device/Focus/OSD splash) — same corner position, same size, unaffected by whatever else is on screen (D-05) |
| Expanded-state behavior | Does not render at all once `interaction.isExpanded` — no badge anywhere in Home/Calendar/Weather/Tray/switcher row (D-06) |

**Reference precedent, not a new visual language:** this is the first "always-present regardless-of-content corner overlay" in the codebase — closest existing shapes are Focus's fixed-color status dot (color/size class) and the app-icon-badge-dot mental model named directly in D-05 (placement/behavior class). Do not build a pill-takeover splash (Charging/Device/Focus/OSD's shape) — that shape is explicitly rejected by D-05's own comparison.

---

## Settings Toggle Contract (D-11, D-12)

### Toggle row

| Property | Value |
|----------|-------|
| Location | Existing Settings sidebar "Activities" section, alongside the Focus/OSD-suppression toggles (`SettingsView.swift` ~L202-228) — or its own adjacent row; exact placement is executor discretion, same open-question precedent as Phase 38/39's toggle placement |
| Label | `"Automatically Check for Updates"` |
| Control | Standard `Toggle`, **on by default** (D-12 — deliberately the opposite default from Focus/OSD's off-by-default permission toggles, since this one gates no system permission, just a background network check) — `@AppStorage`-backed, same pattern as every other toggle in `ActivitySettings.swift`/`SettingsView.swift` |
| Status hint | None — unlike Focus/OSD's permission-gated toggles, there is no grant/deny state to surface. Wires directly to Sparkle's `SUEnableAutomaticChecks`, no intermediate explanation popover, no deep link |
| Behavior | Toggling off stops Sparkle's background scheduled checks (D-09/D-10's ~24h interval); does not affect the "Check for Updates…" menu item, which always works regardless of this toggle's state (manual check is always available) |

**No permission-explanation popover for this phase** — unlike Focus (Communication Notifications) and OSD (Accessibility), automatic update checks require no macOS permission grant, so there is nothing analogous to Phase 38/39's popover pattern to build here.

---

## Menu Item Contract

| Property | Value |
|----------|-------|
| Location | `AppDelegate.swift`'s existing `NSMenu` (`menu.addItem(...)`), inserted between "Settings…" and the separator/"Quit Islet" — same `target = self` wiring pattern as the existing two items |
| Label | `"Check for Updates…"` — Sparkle's own conventional label (matches the ellipsis convention already used by "Settings…" in this same menu, since it opens a dialog rather than performing an instant action) |
| Action | `updaterController.checkForUpdates(nil)` — surfaces Sparkle's own standard alert. This is an explicit user-initiated click, so Sparkle's dialog activating/stealing focus here is acceptable (mirrors the existing `openOnboardingSettings()` `NSApp.activate` precedent already in this codebase, per `ARCHITECTURE.md` Integration Point 5) |

---

## Typography

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Badge glyph | 12pt | semibold | n/a (glyph) | `arrow.up.circle.fill` corner badge |
| Settings toggle label | 13px | regular | default (system) | `"Automatically Check for Updates"` — matches existing Settings row label convention |
| Menu item label | system menu font (not app-controlled) | regular | n/a | `"Check for Updates…"` — rendered by `NSMenu`, not a custom SwiftUI text style |

Font family: San Francisco system font throughout, no exceptions in this phase.

**Declared weight: semibold (badge glyph) + regular (Settings/menu text) — 2, per contract.**

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | Shared Liquid Glass material (`islandFill`, Phase 35, unchanged) | Pill/island chrome background — zero new material, this phase renders inside the existing material only |
| Secondary (30%) | `.primary`/`.secondary` (Settings row/menu text) | Settings toggle label, menu item label |
| Accent (10%) | `env.nowPlayingAccent` (existing Phase 27 per-element accent, reused — not a new key) | **Reserved exclusively for:** the update-available badge glyph. No other element in this phase uses this accent binding beyond its existing Now Playing usage |
| Destructive | n/a | No destructive actions in this phase |

Accent reserved for: `env.nowPlayingAccent` → update-available badge glyph fill only, in this phase's own new UI. This is a deliberate departure from Phase 38/39's "fixed color, ignore theme" convention for system-level HUDs — D-07 explicitly requires this badge to follow the theme-accent convention instead.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Menu item | `"Check for Updates…"` |
| Settings toggle label | `"Automatically Check for Updates"` |
| Badge | *(icon-only, no visible text, no tooltip — D-08 locked; VoiceOver label "Update available")* |
| Empty state | Not applicable — no list/collection UI in this phase |
| Error state | Not applicable — Sparkle's own standard dialog owns all success/failure/no-update-available messaging; this phase builds no custom error copy |
| Destructive confirmation | Not applicable — no destructive actions in this phase (Sparkle's own Install/Remind Me Later/Skip This Version dialog owns any such decisions, D-14) |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| shadcn official | none — native app, no registry tooling | not applicable |
| Swift Package Manager — Sparkle (`sparkle-project/Sparkle`, 2.9.4 pin per `STACK.md`) | `SPUStandardUpdaterController`, embedded/signed `Sparkle.framework` | Not a shadcn-style registry; standard SPM dependency from the project's own established `MediaRemoteAdapter` embed/codeSign/disable-library-validation precedent (`project.yml`). No UI code is imported from Sparkle beyond its own standard alert dialog, which this spec does not style |

---

## Verification Notes for Executor

- **The badge is NOT a wing.** Do not call `wingsShape(leftWidth:rightWidth:)` for this — it is a plain `.overlay(alignment: .topTrailing)` on the collapsed pill's own container, sized/positioned independently of the camera-bridge-avoidance geometry every wing (Charging/Device/Focus/OSD) has to solve. There is no camera-occlusion concern here since the badge sits at the pill's outer corner, not near the notch center.
- **Attach the overlay outside the `switch presentation` body**, so it renders identically whichever case is active underneath — attaching it inside one specific case (e.g. only `.idle`) would violate D-05's "stays visible regardless of whatever else is currently showing" requirement.
- **Gate the overlay's presence, not its opacity** — no badge in the view tree at all when no update is pending (mirrors the absence-based pattern OSD/Charging already use for their own wings when inactive), not a `.opacity(0)` element that still occupies layout.
- **`env.nowPlayingAccent` reuse is this document's own resolution of D-07's ambiguity** — flag during plan-checker review if a stronger candidate emerges (e.g. a dedicated accent) during planning; this spec's choice is the lazy/no-new-Settings-row reading of "no new standalone color parameter," not an explicit locked decision from `40-CONTEXT.md` itself.
- **Sparkle's own dialog is unstyled by this app** — do not attempt to reskin `SPUStandardUserDriver`'s alert; REQUIREMENTS.md and `ARCHITECTURE.md` both explicitly scope this phase away from a custom `SPUUserDriver`.
- **Settings toggle has no permission-popover pattern to reuse** (unlike Phase 38/39) — do not build one; automatic-check scheduling requires no macOS privacy grant.
- **Badge glyph needs `.accessibilityLabel("Update available")`** — icon-only tappable elements without a hover tooltip must still expose a VoiceOver label.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
</content>
