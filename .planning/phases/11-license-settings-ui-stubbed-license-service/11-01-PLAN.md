---
phase: 11-license-settings-ui-stubbed-license-service
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Licensing/LicenseService.swift
  - Islet/Licensing/LicenseState.swift
  - IsletTests/LicenseServiceTests.swift
autonomous: true
requirements: [TRIAL-03]
must_haves:
  truths:
    - "D-05: StubLicenseService returns .success for the magic key ISLET-DEMO-OK"
    - "D-05: StubLicenseService returns .failure(.invalidKey) for any other non-empty key; empty input is inert (never attempts)"
    - "D-06: Completion is delivered asynchronously (~1s simulated round-trip) on the main thread"
    - "LicenseState reports .licensed when the in-memory sessionActivated flag is true (Claude's Discretion — session-only, not persisted)"
  artifacts:
    - path: "Islet/Licensing/LicenseService.swift"
      provides: "LicenseService protocol + LicenseActivationError enum + StubLicenseService conformer"
      contains: "protocol LicenseService"
      min_lines: 30
    - path: "Islet/Licensing/LicenseState.swift"
      provides: "In-memory sessionActivated flag + .licensed short-circuit in status"
      contains: "sessionActivated"
    - path: "IsletTests/LicenseServiceTests.swift"
      provides: "Async key→Result mapping + main-thread completion tests"
      contains: "XCTestExpectation"
      min_lines: 25
  key_links:
    - from: "StubLicenseService.activate"
      to: "DispatchQueue.main.asyncAfter"
      via: "~1s simulated round-trip, completion on main"
      pattern: "asyncAfter\\(deadline: .now\\(\\) \\+ 1"
    - from: "LicenseState.status"
      to: ".licensed"
      via: "sessionActivated short-circuit after the DEBUG block"
      pattern: "if sessionActivated"
---

<objective>
Create the protocol-isolated stub license service seam and the in-memory session
entitlement flag that the Phase 11 Settings UI (Plan 02) consumes, plus the async
unit tests that lock the stub's key→verdict contract.

Purpose: This is the one genuinely new subsystem in Phase 11. The stub is the exact
swap seam Phase 12's real `PolarLicenseService` drops into (mirrors the existing
`NowPlayingService` protocol-isolation pattern), so its shape — closure-based
`Result` completion with a documented main-thread contract — must be right now, when
it is trivial, rather than retrofitted later. Keeping the stub PURE (no singleton
mutation inside it) makes it deterministically unit-testable.

Output: `Islet/Licensing/LicenseService.swift` (new), a two-line addition to
`Islet/Licensing/LicenseState.swift`, and `IsletTests/LicenseServiceTests.swift` (new,
the Nyquist Wave 0 async test harness).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/11-license-settings-ui-stubbed-license-service/11-RESEARCH.md
@.planning/phases/11-license-settings-ui-stubbed-license-service/11-PATTERNS.md
@.planning/phases/11-license-settings-ui-stubbed-license-service/11-VALIDATION.md

<interfaces>
<!-- The exact target contract this plan implements. Read the analog files in each
     task's <read_first> for the surrounding idiom; do NOT explore the codebase. -->

Analog protocol shape to mirror (Islet/Notch/NowPlayingMonitor.swift:40-47):
```swift
protocol NowPlayingService: AnyObject {
    func start()
    func runHealthCheck(then setHealthy: @escaping (Bool) -> Void)   // closure completion idiom
}
```

Analog one-shot delay + main-thread completion (Islet/Notch/NowPlayingMonitor.swift:107-111):
```swift
DispatchQueue.main.asyncAfter(deadline: .now() + 3.0) {
    if settled { return }
    settled = true
    setHealthy(false)
}
```

Existing LicenseState.status to extend (Islet/Licensing/LicenseState.swift:33-56):
the `#if DEBUG` override block stays FIRST; the new `.licensed` short-circuit goes
directly after it, before the `TrialManager.shared.trialStartDate()` computation.
`LicenseStatus` cases are `.trial(daysRemaining: Int)`, `.trialExpired`, `.licensed`.
`isEntitled` already maps `.licensed → true` and needs no change.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create LicenseService seam + in-memory session entitlement flag</name>
  <files>Islet/Licensing/LicenseService.swift, Islet/Licensing/LicenseState.swift</files>
  <read_first>
    - Islet/Notch/NowPlayingMonitor.swift (lines 35-112) — the `NowPlayingService: AnyObject` protocol-isolation precedent, the class-bound protocol + one `final class` conformer, closure completion idiom, and the `DispatchQueue.main.asyncAfter` one-shot that guarantees main-thread completion. This is the exact shape to mirror.
    - Islet/Licensing/LicenseState.swift (whole file) — the singleton being extended; note the DEBUG override discipline (both constants AND read-site are `#if DEBUG`) and that `status` is a plain computed property.
    - .planning/phases/11-license-settings-ui-stubbed-license-service/11-PATTERNS.md (§LicenseService.swift and §LicenseState.swift) — the target shape and the "keep the stub pure" rule.
    - .planning/phases/11-license-settings-ui-stubbed-license-service/11-RESEARCH.md (§Pattern 1, §LicenseState change, §Security Domain) — main-thread contract, magic-key scaffold note, in-memory-only constraint.
  </read_first>
  <behavior>
    - `StubLicenseService().activate(key: "ISLET-DEMO-OK", ...)` → completion with `.success(())`.
    - Any other non-empty key (e.g. "WRONG-KEY") → completion with `.failure(.invalidKey)`.
    - Leading/trailing whitespace is trimmed before compare: "  ISLET-DEMO-OK \n" → `.success`.
    - Completion fires asynchronously after ~1s (NOT synchronously) and on the main thread.
    - `LicenseState.shared.sessionActivated = true` makes `LicenseState.shared.status == .licensed` and `isEntitled == true`.
  </behavior>
  <action>
    Create `Islet/Licensing/LicenseService.swift` mirroring the `NowPlayingService` precedent. Define `enum LicenseActivationError: Error, Equatable` with cases `invalidKey` and `unreachable(String)` — the stub NEVER emits `unreachable`, but the case exists now so Phase 12's real service needs zero protocol change (per D-05 / RESEARCH Pattern 1). Define `protocol LicenseService: AnyObject` with the single method `func activate(key: String, completion: @escaping (Result<Void, LicenseActivationError>) -> Void)`, and a doc comment stating the completion is ALWAYS delivered on the MAIN thread (contract — Phase 12's URLSession impl must hop; this stub already runs on main). Define `final class StubLicenseService: LicenseService` with `static let validKey = "ISLET-DEMO-OK"` (D-05 magic key). In `activate`, trim the key via `trimmingCharacters(in: .whitespacesAndNewlines)`, then `DispatchQueue.main.asyncAfter(deadline: .now() + 1.0)` (D-06 observable ~1s round-trip) calling `completion(trimmed == Self.validKey ? .success(()) : .failure(.invalidKey))`. Keep the stub PURE — do NOT mutate `LicenseState.shared` inside `activate` (that is the caller's job in Plan 02). Add a file header comment documenting: the main-thread completion contract, that the magic key is a DEBUG-scaffold not a shipped credential, and that Phase 12 deletes this whole file before Phase 13 distribution (T-11-01). Optionally `#if DEBUG`-gate the magic-key comparison as belt-and-suspenders.

    Then modify `Islet/Licensing/LicenseState.swift`: add a stored `var sessionActivated = false` property (in-memory ONLY — never persisted to UserDefaults/Keychain this phase, per T-11-02 / Pitfall 1). In the computed `status`, immediately AFTER the closing `#endif` of the existing `#if DEBUG` override block and BEFORE the `guard let start = TrialManager.shared.trialStartDate()` line, insert `if sessionActivated { return .licensed }`. Leave `isEntitled` and `trialExpiryDate` unchanged.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS' 2>&1 | tail -3 | grep -q 'BUILD SUCCEEDED'</automated>
    Note: the behavioral proof is Task 2's test target (created in this same plan). This task's automated gate is compilation of the new types; do not defer it to a manual step.
  </verify>
  <acceptance_criteria>
    - `Islet/Licensing/LicenseService.swift` exists and contains `protocol LicenseService: AnyObject`, `enum LicenseActivationError: Error, Equatable` with `case invalidKey` and `case unreachable(String)`, and `func activate(key: String, completion:`.
    - `StubLicenseService` contains `static let validKey = "ISLET-DEMO-OK"` and calls `DispatchQueue.main.asyncAfter(deadline: .now() + 1.0)`.
    - `StubLicenseService.activate` does NOT reference `LicenseState` (grep confirms the stub is pure).
    - `Islet/Licensing/LicenseState.swift` contains `var sessionActivated = false` and, inside `status`, `if sessionActivated { return .licensed }` positioned after the `#endif` of the DEBUG block.
    - `sessionActivated` is never written to `UserDefaults` or Keychain anywhere in `LicenseState.swift` (grep: no `UserDefaults`/`Keychain` write keyed to session activation).
    - `xcodebuild build -scheme Islet` reports BUILD SUCCEEDED.
  </acceptance_criteria>
  <done>The stub service seam and in-memory session flag compile; the stub is pure and the LicenseState short-circuit sits correctly after the DEBUG block.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 — async LicenseService unit tests (D-05, D-06)</name>
  <files>IsletTests/LicenseServiceTests.swift</files>
  <read_first>
    - IsletTests/PowerActivityTests.swift (whole file) — the XCTest skeleton to copy: `import XCTest` + `@testable import Islet`, `final class ...: XCTestCase`, header comment naming the pure seam under test, one focused `func testX()` per case, `// MARK:` dividers.
    - IsletTests/TrialManagerTests.swift (lines 1-32) — style reference only; its fake-injection is NOT needed here (the stub has no dependencies).
    - Islet/Licensing/LicenseService.swift (from Task 1) — the types under test.
    - .planning/phases/11-license-settings-ui-stubbed-license-service/11-VALIDATION.md (§Wave 0 Requirements, §Per-Task Verification Map) — the exact cases D-05/D-06 require and the run command.
    - .planning/phases/11-license-settings-ui-stubbed-license-service/11-PATTERNS.md (§LicenseServiceTests.swift) — the async `XCTestExpectation` harness (the one new technique vs the synchronous existing tests).
  </read_first>
  <action>
    Create `IsletTests/LicenseServiceTests.swift`: `import XCTest` + `@testable import Islet`, a `final class LicenseServiceTests: XCTestCase`, and a header comment explaining this verifies the pure `StubLicenseService` key→`Result` seam (the state-flip + live-unlock behavior is verified on-device per VALIDATION, not here — `LicenseState` is a `private init()` singleton). Because the stub completes after ~1s via `asyncAfter`, each test uses `let exp = expectation(description:)`, calls `StubLicenseService().activate(key:) { result in ... exp.fulfill() }`, then `wait(for: [exp], timeout: 3.0)` (comfortably above the 1s delay). Cover exactly: (1) `"ISLET-DEMO-OK"` → `.success` AND the completion asserts `XCTAssertTrue(Thread.isMainThread)` (D-06 main-thread contract); (2) an unknown non-empty key (e.g. `"NOPE-1234"`) → `.failure(.invalidKey)`; (3) whitespace-padded magic key `"  ISLET-DEMO-OK \n"` → `.success` (trimming); (4) asynchronicity — set a `var completed = false`, assert it is still `false` on the line immediately after the `activate(...)` call returns (proving the completion did not run synchronously), then `exp.fulfill()` inside the closure. Do NOT assert empty-input inertness here — that is a view-layer concern (Activate button disabled), not the stub's responsibility.
  </action>
  <verify>
    <automated>xcodebuild test -scheme Islet -only-testing:IsletTests/LicenseServiceTests -destination 'platform=macOS' 2>&1 | tail -5 | grep -q 'TEST SUCCEEDED\|Test Suite.*passed'</automated>
  </verify>
  <acceptance_criteria>
    - `IsletTests/LicenseServiceTests.swift` exists, contains `XCTestExpectation` usage (`expectation(description:` + `wait(for:`) and `Thread.isMainThread`.
    - A test asserts `.success` for `"ISLET-DEMO-OK"`, a test asserts `.failure(.invalidKey)` for a non-magic non-empty key, and a test proves whitespace trimming.
    - A test proves the completion is asynchronous (a flag set inside the closure is still false immediately after the `activate` call returns).
    - `xcodebuild test -scheme Islet -only-testing:IsletTests/LicenseServiceTests` exits 0 with all tests passing.
  </acceptance_criteria>
  <done>The Wave 0 async test harness passes, locking the D-05 key→verdict mapping and the D-06 async main-thread completion contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| license key string → StubLicenseService | Untrusted user-supplied string crosses into the (fake) validator |
| entitlement truth → app runtime | The in-memory `sessionActivated` flag gates `isEntitled` via the single arbiter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Elevation of Privilege | `StubLicenseService.validKey` magic key | mitigate | Magic key is DEBUG-scaffold only; Phase 12 replaces the entire `StubLicenseService` file with `PolarLicenseService` before Phase 13 distribution. File header documents this. Optional `#if DEBUG` gate on the compare. Phase 11 is not a public release, so residual is accepted for this phase. |
| T-11-02 | Elevation of Privilege | `LicenseState.sessionActivated` | mitigate | Keep entitlement an in-memory `var` that resets to `false` on every launch; never persist to UserDefaults/Keychain this phase (dodges the flippable-bool bypass, Pitfall 1/3). |
| T-11-03 | Tampering | `StubLicenseService.activate(key:)` input | mitigate | Treat the key as an opaque untrusted string: `trimmingCharacters` then `==` compare only. No interpolation into any shell/URL/logging sink. Empty input is a view-layer inert case (Plan 02), not the stub's concern. |
| T-11-SC | Tampering | package installs | accept | N/A — this phase installs no npm/pip/cargo/SPM packages; no `project.yml` dependency change. Nothing to slopcheck. |
</threat_model>

<verification>
Per-task automated gates above. Wave merge gate: `xcodebuild test -scheme Islet` (full
suite) green before Plan 02 begins.

No manual verification in this plan — all behavior here is deterministically unit-tested.
</verification>

<success_criteria>
- `LicenseService.swift`, the `LicenseState` field, and `LicenseServiceTests.swift` all exist and compile.
- `xcodebuild test -scheme Islet -only-testing:IsletTests/LicenseServiceTests` passes.
- The stub is pure (no `LicenseState` mutation inside it); entitlement stays in-memory only.
- The `LicenseService` protocol carries a documented main-thread completion contract, ready for Phase 12's drop-in swap.
</success_criteria>

<output>
Create `.planning/phases/11-license-settings-ui-stubbed-license-service/11-01-SUMMARY.md` when done.
</output>
