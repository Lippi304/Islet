---
phase: 29-notchshape-flare
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Notch/NotchShape.swift
  - IsletTests/NotchShapeTests.swift
  - Islet/Notch/NotchPillView.swift
  - Islet/Notch/NotchWindowController.swift
autonomous: false
requirements: [SHAPE-01]

must_haves:
  truths:
    - "Every expanded presentation (Home, Tray, Calendar, Weather, Charging/Device wings) shows a new outward-flaring top edge instead of today's flush vertical edge"
    - "The collapsed/idle pill renders pixel-identical to today — no shape, size, or position regression (D-03, Success Criterion #2)"
    - "The Now-Playing media wings / song-change toast (mediaWingsOrToast) stays flush like the collapsed pill — no flare (D-03/D-04, locked)"
    - "The flare animates smoothly as part of the existing collapse↔expand spring morph, with no dropped frames, artifacts, or clipping at the panel edge (Success Criterion #3)"
  artifacts:
    - path: "Islet/Notch/NotchShape.swift"
      provides: "topFlareWidth stored property (default 0) + path math that widens the top edge outward before merging into the existing topCornerRadius transition"
      contains: "var topFlareWidth: CGFloat = 0"
    - path: "IsletTests/NotchShapeTests.swift"
      provides: "Regression coverage proving topFlareWidth: 0 is pixel-identical to today's path, plus new coverage proving a non-zero value widens the path beyond its rect"
      contains: "topFlareWidth"
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "Self.topFlareWidth single-source-of-truth constant + blobShape()/wingsShape() threading it into their NotchShape(...) construction"
      contains: "static let topFlareWidth: CGFloat = 10"
  key_links:
    - from: "NotchPillView.swift blobShape()/wingsShape()"
      to: "NotchShape(..., topFlareWidth: Self.topFlareWidth)"
      via: "construction argument, one shared constant"
      pattern: "topFlareWidth: Self\\.topFlareWidth"
    - from: "NotchWindowController.swift positionAndShow(on:)"
      to: "NotchPillView.topFlareWidth"
      via: "panel-frame width reservation (contingency fix, applied only if on-device UAT observes flare clipping)"
      pattern: "NotchPillView\\.topFlareWidth"
---

<objective>
Give every expanded presentation (Home, Tray, Calendar, Weather, Charging/Device wings) a subtle outward-flaring top edge — threaded through the single shared `NotchShape` struct and its two call-site helpers (`blobShape()`, `wingsShape()`) so it applies automatically wherever those are used — while the collapsed/idle pill and the Now-Playing media wings/toast stay pixel-identical to today (SHAPE-01, D-01 through D-05).

Purpose: Phase 29 is the first phase of the v1.5 milestone and is fully independent of the other 5 phases — it only touches `NotchShape`'s path geometry and 2 of its 4 call sites, the same "pure rendering-value change" shape as Phase 25.
Output: `NotchShape.swift` gains one new animatable geometry parameter; `NotchPillView.swift`'s `blobShape()`/`wingsShape()` pass it a shared, tunable constant; `NotchWindowController.swift` reserves extra panel width for the flare bulge ONLY if on-device testing shows it would otherwise be clipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-notchshape-flare/29-CONTEXT.md
@.planning/phases/29-notchshape-flare/29-PATTERNS.md
@.planning/phases/29-notchshape-flare/29-UI-SPEC.md

<interfaces>
<!-- Exact current state of every edit target and its analogs, read in full this session.
     Executor should edit these directly — no further codebase exploration needed. -->

From Islet/Notch/NotchShape.swift (full file, 33 lines, unchanged since Phase 1):
```
struct NotchShape: Shape {
    var topCornerRadius: CGFloat = 6
    var bottomCornerRadius: CGFloat = 14
    func path(in rect: CGRect) -> Path {
        var p = Path()
        p.move(to: CGPoint(x: rect.minX, y: rect.minY))
        p.addQuadCurve(to: CGPoint(x: rect.minX + topCornerRadius, y: rect.minY + topCornerRadius),
                       control: CGPoint(x: rect.minX + topCornerRadius, y: rect.minY))
        p.addLine(to: CGPoint(x: rect.minX + topCornerRadius, y: rect.maxY - bottomCornerRadius))
        p.addQuadCurve(to: CGPoint(x: rect.minX + topCornerRadius + bottomCornerRadius, y: rect.maxY),
                       control: CGPoint(x: rect.minX + topCornerRadius, y: rect.maxY))
        p.addLine(to: CGPoint(x: rect.maxX - topCornerRadius - bottomCornerRadius, y: rect.maxY))
        p.addQuadCurve(to: CGPoint(x: rect.maxX - topCornerRadius, y: rect.maxY - bottomCornerRadius),
                       control: CGPoint(x: rect.maxX - topCornerRadius, y: rect.maxY))
        p.addLine(to: CGPoint(x: rect.maxX - topCornerRadius, y: rect.minY + topCornerRadius))
        p.addQuadCurve(to: CGPoint(x: rect.maxX, y: rect.minY),
                       control: CGPoint(x: rect.maxX - topCornerRadius, y: rect.minY))
        p.addLine(to: CGPoint(x: rect.minX, y: rect.minY))
        return p
    }
}
```
The top edge is the flat run between the two top quad-curves; the flat run's endpoints are literally `(rect.minX, rect.minY)` (path start, via `move(to:)`) and `(rect.maxX, rect.minY)` (end of the last `addQuadCurve`, closed by the final `addLine`). Widening the flare means pushing those two endpoints outward (beyond `rect.minX`/`rect.maxX`) and bridging back in to meet the UNCHANGED `topCornerRadius` corner-curves that already exist.

From Islet/Notch/NotchPillView.swift (4 NotchShape construction sites — confirmed via grep, no others exist):
```
// line 413 — collapsedIsland, EXCLUDED, must stay zero-diff:
return NotchShape()

// line 1089 — blobShape<Content>(), gets the flare:
return NotchShape(topCornerRadius: topCornerRadius, bottomCornerRadius: bottomCornerRadius)

// line 1174 — wingsShape<Content>(), gets the flare:
NotchShape(topCornerRadius: 6, bottomCornerRadius: 6)   // flatter than the downward blob

// line 1237 — mediaWingsOrToast(_:), EXCLUDED (D-03/D-04), must stay zero-diff:
NotchShape(topCornerRadius: 6, bottomCornerRadius: toast != nil ? 16 : 6)
```
`wingsSize` is declared `static let wingsSize = CGSize(width: 290, height: 32)` at line 206 — `topFlareWidth` should be added as a new sibling `static let` directly beneath it, following the same single-source-of-truth doc-comment convention already used for `expandedSize`/`wingsSize`/the Phase-25 `gradientMaterial`.

The 7 `blobShape(...)` call sites (lines 440, 474, 658, 724, 772, 1492, 1561) all pass `topCornerRadius`/`bottomCornerRadius` as literal arguments into the helper — none construct `NotchShape` directly, so none need editing; the flare is injected exactly once, inside `blobShape()`'s own body.

From Islet/Notch/NotchWindowController.swift (`positionAndShow(on:)`, the panel-frame sizing — lines 750-830, `expandedSize`/`wingsSize` properties at lines 227/305):
```
private let wingsSize = NotchPillView.wingsSize          // line 227
private let expandedSize = NotchPillView.expandedSize    // line 305
...
let expandedFrame = expandedNotchFrame(collapsed: collapsedFrame,
                                       expandedSize: CGSize(width: expandedSize.width,
                                                             height: NotchPillView.switcherContentHeight + NotchPillView.shelfRowHeight + NotchPillView.switcherRowHeight))   // line 790-792
...
let wings = wingsFrame(collapsed: collapsedFrame, wingsSize: wingsSize)   // line 798
let onboardingFrame = expandedNotchFrame(collapsed: collapsedFrame, expandedSize: NotchPillView.onboardingSize)   // line 802 — NOT flared, onboarding is out of scope
let panelFrame = expandedFrame.union(wings).union(onboardingFrame)        // line 803
```
`expandedNotchFrame`/`wingsFrame` (Islet/Notch/NotchGeometry.swift, unmodified this phase) both center the returned frame on `collapsed.midX` and size it to EXACTLY the `size:` argument passed in — no built-in margin for content that paints outside its own rect. If on-device testing (Task 3) shows the flare bulge clipped by the panel's own bounds, the fix is to widen the `CGSize` passed into these 2 calls (not `NotchGeometry.swift` itself) by `2 * NotchPillView.topFlareWidth` — see Task 3's contingency.

`syncClickThrough()`/`visibleContentZone()` (lines 955-987) are NOT touched by this phase — they size the click-through hit-test box from `expandedSize.width` directly (unflared), which is correct: the flare is a thin decorative bulge only at the very top few pixels, the same category of shape/hit-test mismatch the existing 6pt corner curves already have, not a new CR-01-style regression (see project memory `cr01-clickthrough-or-defeat-gotcha`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NotchShape topFlareWidth property + path geometry (SHAPE-01, D-01/D-02)</name>
  <files>Islet/Notch/NotchShape.swift, IsletTests/NotchShapeTests.swift</files>
  <read_first>
    Islet/Notch/NotchShape.swift (full file, already in context above — small file, no re-read needed)
    IsletTests/NotchShapeTests.swift (existing 3 tests — pattern to extend, already read this session)
    .planning/phases/29-notchshape-flare/29-PATTERNS.md (geometry guidance: quad-curve-only constraint, degeneracy-at-zero requirement)
  </read_first>
  <behavior>
    - Test: `NotchShape()`'s default `topFlareWidth` is `0` (matches `topCornerRadius`/`bottomCornerRadius`'s existing default-value pattern).
    - Test (regression): `NotchShape(topCornerRadius: 6, bottomCornerRadius: 14, topFlareWidth: 0).path(in: rect).cgPath.boundingBox` is IDENTICAL to `NotchShape(topCornerRadius: 6, bottomCornerRadius: 14).path(in: rect).cgPath.boundingBox` — explicit zero equals omitted-default equals today's exact shape. This is the unit-test-level proof of D-03/Success-Criterion-#2 (collapsed pill and mediaWingsOrToast, which never pass `topFlareWidth`, must stay byte-identical).
    - Test: with `topFlareWidth: 10` (topCornerRadius: 6, bottomCornerRadius: 32), the resulting path's bounding box extends beyond the input rect horizontally: `boundingRect.minX < rect.minX` and `boundingRect.maxX > rect.maxX`.
    - Test: the flared path (topFlareWidth: 10) stays closed and non-empty, mirroring `testCustomRadiiProduceAClosedNonEmptyPath` — `path.cgPath.isEmpty == false`, `cgBounds.width > 0`, `cgBounds.height > 0`.
    - Existing 3 tests (`testPathIsNonEmpty`, `testPathStaysWithinItsRect`, `testCustomRadiiProduceAClosedNonEmptyPath`) must keep passing UNMODIFIED — they exercise the no-flare-argument code paths.
  </behavior>
  <action>
Add `var topFlareWidth: CGFloat = 0` as a third plain stored property, directly beneath `bottomCornerRadius` (line 15) — same declaration style as the other two (no `private`, no `didSet`), so SwiftUI's `Shape` per-property interpolation covers it automatically, exactly like `topCornerRadius`/`bottomCornerRadius` already do across the collapse↔expand morph. Add one short doc-comment above it (matching this file's existing comment density) noting it is SHAPE-01's fixed, identical-across-all-presentations widen amount (D-05) and defaults to `0` (today's flush edge) for `collapsedIsland`/`mediaWingsOrToast`, which never pass it.

Modify `path(in:)` so the top edge widens outward by `topFlareWidth` on both sides before merging into the EXISTING (unmodified) `topCornerRadius` quad-curves — using ONLY `move`/`addLine`/`addQuadCurve` (no `addArc`, no cubic-Bezier `addCurve`), per 29-PATTERNS.md's explicit curve-type constraint (mixing curve types would visually clash with the rest of the shape's quad-curve corner language). Concretely: the `move(to:)` target and the closing `addLine(to:)` target currently sit at `rect.minX`/`rect.maxX` (at `rect.minY`) — shift these outward to `rect.minX - topFlareWidth`/`rect.maxX + topFlareWidth`, and bridge each flared endpoint back in to the point where the existing `topCornerRadius` quad-curve already begins, with one new `addQuadCurve` per side. When `topFlareWidth == 0` the new math MUST degenerate to exactly today's path (verified by the regression test above) — this is the hard invariant, not the exact on-device look of the non-zero case. Exact control-point placement for the non-zero case is on-device-tuned discretion (D-02) — final visual verification happens in Task 3, not here.

Do not add `animatableData` conformance pre-emptively (Phase 25 D-09 precedent, 29-UI-SPEC.md "Animation behavior" section) — only add it later if Task 3's on-device check shows a visible morph "snap," per that section's documented contingency.
  </action>
  <acceptance_criteria>
    - `grep -c "var topFlareWidth: CGFloat = 0" Islet/Notch/NotchShape.swift` equals 1
    - `grep -c "func path(in rect: CGRect)" Islet/Notch/NotchShape.swift` equals 1 (single method, extended in place, not duplicated)
    - `grep -c "addArc\|addCurve" Islet/Notch/NotchShape.swift` equals 0 (quad-curve-only constraint honored)
    - All 4 new tests plus the 3 existing tests in `IsletTests/NotchShapeTests.swift` compile and pass via manual Cmd-U (automated build gate below only proves compilation; per project memory `xcodebuild-test-headless-hang`, `xcodebuild test` hangs booting the full `Islet.app` — route the actual test RUN to Xcode's Cmd-U, covered by Task 3's checkpoint)
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` succeeds with zero errors
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <done>`NotchShape` has a new `topFlareWidth: CGFloat = 0` property whose path math widens the top edge outward when non-zero and is byte-identical to today's path at the default `0`; `NotchShapeTests.swift` has 4 new tests covering both cases; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the flare into blobShape()/wingsShape() (SHAPE-01, D-03/D-04/D-05)</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift lines 190-220 (expandedSize/wingsSize static-let convention) and lines 1074-1186 (blobShape/wingsShape bodies) — already read this session
    .planning/phases/29-notchshape-flare/29-CONTEXT.md (D-03/D-04/D-05 — exclusion list and fixed-value-across-presentations rule)
  </read_first>
  <action>
Add `static let topFlareWidth: CGFloat = 10` directly beneath `wingsSize` (line 206), following the exact same doc-comment convention as `expandedSize`/`wingsSize` (a short comment noting: this is the ONE shared, fixed flare value every covered presentation uses identically per D-05 — both the wide 360pt Home/Tray/Calendar/Weather blob and the narrower 290pt Charging/Device wings get the SAME absolute value, not a proportionally-scaled one; `NotchWindowController.swift` also reads this constant for its panel-frame reservation, mirroring how it already reads `wingsSize`/`onboardingSize`/`switcherContentHeight` from this file).

Thread it into exactly 2 `NotchShape(...)` construction sites:
1. `blobShape<Content>()` (line 1089): add `topFlareWidth: Self.topFlareWidth` as a third argument to the existing `NotchShape(topCornerRadius: topCornerRadius, bottomCornerRadius: bottomCornerRadius)` call.
2. `wingsShape<Content>()` (line 1174): add `topFlareWidth: Self.topFlareWidth` as a third argument to the existing `NotchShape(topCornerRadius: 6, bottomCornerRadius: 6)` call.

Every other line in both functions (`.fill`, `.matchedGeometryEffect`, `.frame`, `.overlay`, `.onTapGesture`) stays UNCHANGED.

Do NOT touch `collapsedIsland` (line 413, plain `NotchShape()`) — its `topFlareWidth` falls back to the default `0`, so it is already correct as-is; editing this line at all (even to add `topFlareWidth: 0` explicitly) would violate the zero-diff requirement (Success Criterion #2). Do NOT touch `mediaWingsOrToast` (line 1237, its own standalone `NotchShape(topCornerRadius: 6, bottomCornerRadius: toast != nil ? 16 : 6)`) — per D-03/D-04 it stays flush, omit the parameter entirely (defaults to `0`). Do NOT touch any of the 7 `blobShape(...)` call sites (lines 440, 474, 658, 724, 772, 1492, 1561) — they call the helper and never construct `NotchShape` directly.
  </action>
  <acceptance_criteria>
    - `grep -c "static let topFlareWidth: CGFloat = 10" Islet/Notch/NotchPillView.swift` equals 1
    - `grep -n "NotchShape(topCornerRadius: topCornerRadius, bottomCornerRadius: bottomCornerRadius, topFlareWidth: Self.topFlareWidth)" Islet/Notch/NotchPillView.swift` matches exactly 1 line (blobShape)
    - `grep -n "NotchShape(topCornerRadius: 6, bottomCornerRadius: 6, topFlareWidth: Self.topFlareWidth)" Islet/Notch/NotchPillView.swift` matches exactly 1 line (wingsShape)
    - `grep -c "NotchShape()" Islet/Notch/NotchPillView.swift` equals 1 (collapsedIsland's plain call, untouched)
    - `grep -n "NotchShape(topCornerRadius: 6, bottomCornerRadius: toast != nil ? 16 : 6)" Islet/Notch/NotchPillView.swift` matches exactly 1 line, with no `topFlareWidth` argument added (mediaWingsOrToast, untouched)
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` succeeds with zero errors
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <done>`blobShape()` and `wingsShape()` both pass `topFlareWidth: Self.topFlareWidth`; `collapsedIsland` and `mediaWingsOrToast` are byte-identical to before this task; build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device UAT — flare look, panel-edge clipping, and zero-diff exclusion sites</name>
  <read_first>
    Islet/Notch/NotchShape.swift (Task 1's edited path math)
    Islet/Notch/NotchPillView.swift (Task 2's edited blobShape/wingsShape + new topFlareWidth constant)
    Islet/Notch/NotchWindowController.swift lines 750-830 (positionAndShow — the panel-frame union this task may need to widen)
    .planning/phases/29-notchshape-flare/29-UI-SPEC.md ("Open geometry question — panel-frame reservation" and "Verification Notes for Executor" sections)
  </read_first>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <action>
    No file edits by default — build and run the app on-device (Cmd-R in Xcode) and perform the
    checklist in how-to-verify. The ONLY in-scope edit is the documented contingency in check 2:
    if the flare bulge is visibly clipped at the panel's edge, widen the `CGSize` passed into the
    2 `expandedNotchFrame`/`wingsFrame` calls inside `positionAndShow(on:)` (NotchWindowController.swift):
    at line ~791, change `expandedSize: CGSize(width: expandedSize.width, height: ...)` to
    `CGSize(width: expandedSize.width + 2 * NotchPillView.topFlareWidth, height: ...)`; at line
    ~798, change `wingsFrame(collapsed: collapsedFrame, wingsSize: wingsSize)` to
    `wingsFrame(collapsed: collapsedFrame, wingsSize: CGSize(width: wingsSize.width + 2 * NotchPillView.topFlareWidth, height: wingsSize.height))`.
    Both `expandedNotchFrame`/`wingsFrame` center their result on `collapsed.midX` (NotchGeometry.swift,
    unmodified), so widening the size symmetrically keeps the frame centered and reserves exactly
    `topFlareWidth` extra pixels on each side. Do NOT touch `onboardingFrame` (line 802, out of scope)
    or `visibleContentZone()`/`syncClickThrough()` (lines 955-1049, unrelated to this fix and a known
    CR-01 regression risk if touched carelessly — see project memory `cr01-clickthrough-or-defeat-gotcha`).
  </action>
  <what-built>
    Every expanded presentation (Home, Tray, Calendar, Weather, Charging/Device wings) now renders an outward-flaring top edge via the shared `NotchShape.topFlareWidth` parameter (Tasks 1-2); the collapsed pill and Now-Playing media wings/toast are unchanged.
  </what-built>
  <how-to-verify>
    Open `Islet.xcodeproj` in Xcode (Finder path: `/Users/lippi304/conductor/workspaces/notch/algiers/Islet.xcodeproj`), select the `Islet` scheme, and Cmd-R to run on the real MacBook with the physical notch (a simulator/preview cannot validate the real never-focused panel's frame edges).

    1. **Flare look (D-01):** Hover then click to expand Home. Confirm the top edge reads as a SUBTLE widen — "a touch more pronounced" than today's existing 6pt corner curve — NOT a dramatic trumpet/bell flourish. If too pronounced, lower `NotchPillView.topFlareWidth` (Task 2, currently `10`) toward the 8pt floor and re-run; if barely visible, raise toward the 12pt ceiling — do not exceed ~12pt without re-checking against D-01's "subtle" requirement.
    2. **No panel-edge clipping:** While Home is expanded, look closely at the very top-left and top-right corners of the flare. Confirm the flared bulge renders FULLY — not cut off in a hard vertical line at some fixed width. If clipped, apply this task's documented contingency fix (widen `expandedFrame`/`wings` in `positionAndShow(on:)`) and re-run.
    3. **Coverage — all 5 flared presentations:** Cycle through Home, Tray, Calendar, Weather (via the switcher pill) and trigger a Charging splash and a Device splash (plug in charger / connect Bluetooth headphones, or use the DEBUG hand-seed if available). Confirm ALL 5 show the same flare.
    4. **Exclusion — collapsed pill zero-diff (D-03, Success Criterion #2):** With nothing expanded, compare the idle pill against memory of its pre-phase appearance. Confirm it is pixel-identical — flush top edge, no flare, no size/position change.
    5. **Exclusion — media wings/toast (D-03/D-04):** Play music so the collapsed Now-Playing glance (or a song-change toast) appears. Confirm it stays flush like the collapsed pill — no flare.
    6. **Morph smoothness (Success Criterion #3):** Repeatedly hover/click-expand/collapse. Confirm no dropped frames, no hard "snap" at the flare boundary, no visual artifact mid-morph. If a snap IS visible specifically at the flare (not expected per Phase 25's animatableData precedent), add `NotchShape`'s 3-property `animatableData` conformance (documented in 29-UI-SPEC.md's "Animation behavior" section) and re-verify — this is the only condition under which that conformance is added.
    7. **Click-through regression check (CR-01 discipline):** With Home expanded, hover→expand→move the pointer down past the visible content into the transparent reserved band below it. Confirm the pointer correctly falls through to whatever is behind the notch (no phantom click-swallowing) — this re-confirms `visibleContentZone()`/`syncClickThrough()` were NOT affected by this phase's panel-width changes (if the contingency in check 2 was applied).
  </how-to-verify>
  <resume-signal>Type "approved" once all 7 checks pass, or describe which check failed and its specific symptom (e.g. "flare clipped at panel edge on Tray", "flare too pronounced on wings", "snap visible on collapse").</resume-signal>
  <verify>
    <human-check>All 7 checklist items pass on-device on the real notch hardware; if check 2's or check 6's contingency was applied, `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` succeeds afterward too.</human-check>
  </verify>
  <done>User replies "approved"; flare look, panel-edge clipping, all-5-presentation coverage, both zero-diff exclusions, morph smoothness, and click-through regression are all on-device confirmed correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | This phase introduces no new trust boundary. It adds one `CGFloat` geometry parameter to a `Shape` struct and threads a compile-time constant into 2 existing call sites; the only conditional follow-on edit (Task 3's contingency) widens a locally-computed `CGSize` passed to an already-existing pure geometry function. No new user input, network call, file I/O, persisted state, or external data source is introduced anywhere in this phase's scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|------------------|
| T-29-01 | Tampering | `NotchShape.swift` `topFlareWidth` / `NotchPillView.swift` `Self.topFlareWidth` / `NotchWindowController.swift` panel-frame CGSize math | accept | No externally-influenced input reaches any of these values — all are compile-time literals or values pure-computed from other compile-time literals (screen frame geometry), consumed only by the local rendering pipeline. Risk is inherently zero for this change's scope. |
</threat_model>

<verification>
- All 3 tasks' automated build gates (`xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug`) pass with zero errors.
- Task 3's on-device UAT checklist (7 items) passes and the human returns "approved."
- `IsletTests/NotchShapeTests.swift`'s 4 new + 3 existing tests stay green (run via manual Cmd-U in Xcode per project memory `xcodebuild-test-headless-hang`).
- No diff appears anywhere outside `Islet/Notch/NotchShape.swift`, `IsletTests/NotchShapeTests.swift`, `Islet/Notch/NotchPillView.swift`, and — only if Task 3's contingency triggers — `Islet/Notch/NotchWindowController.swift` (and, only if Task 3's animatableData contingency triggers, a small addition back in `Islet/Notch/NotchShape.swift`).
</verification>

<success_criteria>
- Every expanded presentation (Home, Tray, Calendar, Weather, Charging/Device wings) shows the new outward-flaring top edge instead of today's flush vertical edge (ROADMAP Phase 29 Success Criterion #1).
- The collapsed/idle pill renders pixel-identical to today — no shape, size, or position regression (Success Criterion #2).
- The flare animates smoothly as part of the existing collapse↔expand spring morph, with no visual glitches, artifacts, or dropped frames (Success Criterion #3).
</success_criteria>

<output>
Create `.planning/phases/29-notchshape-flare/29-01-SUMMARY.md` when done
</output>
