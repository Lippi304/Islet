---
phase: 25-visual-material-theming-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Notch/NotchPillView.swift
  - Islet/Notch/NotchWindowController.swift
autonomous: false
requirements: [VISUAL-01, VISUAL-02]

must_haves:
  truths:
    - "The collapsed pill, expanded island, and all activity wings render one shared black-to-transparent vertical gradient (opaque near the top, fading to ~50% opacity only at the bottom edge) instead of flat black"
    - "The expand/collapse and hover-widen animations feel deliberately slower than before, with one visible overshoot-and-settle bounce, not a flat ease-in and not multiple bounces"
    - "The expanded blob's bottom corners read noticeably rounder than before (32pt vs. the old 20pt)"
    - "Now Playing, Charging, and idle-glance activity content render completely unchanged inside the new chrome — no layout, font, or color changes"
    - "No visual artifacts (hard edges, banding, corner 'snap') appear mid-morph between collapsed, wings, and expanded states"
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "Shared `islandMaterial` LinearGradient definition, all 4 fill sites swapped from Color.black, bottomCornerRadius raised 20→32 at the 3 expanded-blob call sites"
      contains: "static let islandMaterial"
    - path: "Islet/Notch/NotchWindowController.swift"
      provides: "Retuned springResponse/springDamping constants (only these 2 declarations touched, all 13 call sites unchanged)"
      contains: "private let springResponse: Double = 0.6"
  key_links:
    - from: "NotchPillView.swift collapsedIsland/blobShape/wingsShape/mediaWingsOrToast"
      to: "Self.islandMaterial"
      via: ".fill(Self.islandMaterial) replacing .fill(Color.black)"
      pattern: "fill\\(Self\\.islandMaterial\\)"
    - from: "NotchWindowController.swift's 13 withAnimation(.spring(...)) call sites"
      to: "springResponse / springDamping declarations"
      via: "shared private let constants, read not duplicated"
      pattern: "springResponse: Double = 0\\.6"
---

<objective>
Give the shared notch shell chrome (collapsed pill, expanded island, activity wings) a black-to-transparent vertical gradient material and a slower, gently-bouncy spring feel, matching the iPhone Dynamic Island's characteristic look — per VISUAL-01 (D-01/D-02/D-08) and VISUAL-02 (D-05/D-06/D-07). This is a pure rendering-value change to two already-existing files: no new files, no new types, no architecture change. Individual activity content (Now Playing, Charging, idle glance, Shelf) is explicitly untouched (D-03/D-11).

Purpose: Phase 25 replaces the current flat `Color.black` fill and the "too fast, not enough overshoot" 0.35/0.65 spring with the Droppy-inspired material/feel the user locked in during `/gsd:discuss-phase 25`.
Output: `NotchPillView.swift` renders every collapsed/wings/expanded state through one shared gradient with a rounder expanded bottom corner; `NotchWindowController.swift`'s spring constants drive a deliberately slower, single-overshoot morph everywhere.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-visual-material-theming-redesign/25-CONTEXT.md
@.planning/phases/25-visual-material-theming-redesign/25-RESEARCH.md
@.planning/phases/25-visual-material-theming-redesign/25-PATTERNS.md
@.planning/phases/25-visual-material-theming-redesign/25-UI-SPEC.md
@.planning/phases/25-visual-material-theming-redesign/25-VALIDATION.md

<interfaces>
<!-- Exact current state of both edit targets, read in full this session. Executor should
     edit these directly — no further codebase exploration needed. -->

From Islet/Notch/NotchPillView.swift (current state, all 4 fill sites + corner-radius call sites):
```
// collapsedIsland (line 216): .fill(collapsedFill)
// collapsedFill computed var (lines 728-734):
private var collapsedFill: Color {
    #if DEBUG
    return Color.red.opacity(0.6)
    #else
    return Color.black
    #endif
}

// blobShape<Content> helper (line 277-278) — routes expandedIsland (line 236),
// mediaExpanded (line 651), mediaUnavailable (line 716), all passing
// topCornerRadius: 6, bottomCornerRadius: 20:
return NotchShape(topCornerRadius: topCornerRadius, bottomCornerRadius: bottomCornerRadius)
    .fill(Color.black)

// wingsShape<Content> helper (lines 333-334) — routes wings(for:) and deviceWings(for:):
NotchShape(topCornerRadius: 6, bottomCornerRadius: 6)
    .fill(Color.black)

// mediaWingsOrToast(_:) (lines 396-397) — builds its own NotchShape directly:
NotchShape(topCornerRadius: 6, bottomCornerRadius: toast != nil ? 16 : 6)
    .fill(Color.black)
```

From Islet/Notch/NotchWindowController.swift (lines 260-265, the single tuning point for 13 call sites):
```
private let springResponse: Double = 0.35
private let springDamping: Double = 0.65
```

From Islet/Notch/NotchShape.swift (unchanged this phase per D-09; contingency reference only):
```
struct NotchShape: Shape {
    var topCornerRadius: CGFloat = 6
    var bottomCornerRadius: CGFloat = 14
    func path(in rect: CGRect) -> Path { /* quad-curve path math, unchanged */ }
}
```
No `animatableData` conformance exists. Contingency-only addition if Task 3's on-device check
finds a visible corner "snap":
```
var animatableData: AnimatablePair<CGFloat, CGFloat> {
    get { AnimatablePair(topCornerRadius, bottomCornerRadius) }
    set { topCornerRadius = newValue.first; bottomCornerRadius = newValue.second }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared gradient material + expanded-blob corner radius (VISUAL-01, D-01/D-02/D-08)</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift (full file — already read this session; contains all 4 fill sites and the 3 bottomCornerRadius:20 call sites)
    .planning/phases/25-visual-material-theming-redesign/25-UI-SPEC.md (Material Contract section — exact gradient stop values, corner radius target)
    .planning/phases/25-visual-material-theming-redesign/25-PATTERNS.md (exact line numbers and the collapsedFill opaque-return-type technique)
  </read_first>
  <action>
Add one new `private static let islandMaterial: LinearGradient` near the existing size seeds (`collapsedSize`/`expandedSize`/`wingsSize`, lines 107-134), following that file's own static-let-single-source-of-truth convention. Define it with exactly 3 stops: `.init(color: .black, location: 0.0)`, `.init(color: .black, location: 0.65)`, `.init(color: .black.opacity(0.5), location: 1.0)`, `startPoint: .top`, `endPoint: .bottom` (per D-01: pure black, no grey mixed in; D-02: long opaque stretch, ~50% floor only at the very bottom, never `.clear`).

Swap all 4 `.fill(Color.black)` sites to `.fill(Self.islandMaterial)`:
1. `blobShape<Content>` helper (line 278) — this single edit covers `expandedIsland`, `mediaExpanded`, and `mediaUnavailable` (all 3 route through this helper).
2. `wingsShape<Content>` helper (line 334) — covers `wings(for:)` and `deviceWings(for:)`.
3. `mediaWingsOrToast(_:)` (line 397) — builds its own `NotchShape` directly, edit this site's `.fill(Color.black)` individually.
4. `collapsedFill` computed var (lines 728-734): widen the declared return type from `Color` to `some ShapeStyle` (both `Color` and `LinearGradient` conform to `ShapeStyle`; this compiles because `#if DEBUG`/`#else` is a compile-time branch — each build configuration only ever sees one concrete return type). Keep the `#if DEBUG` branch returning `Color.red.opacity(0.6)` unchanged; change ONLY the `#else` branch from `return Color.black` to `return Self.islandMaterial`.

Raise `bottomCornerRadius` from `20` to `32` at all 3 expanded-blob call sites that pass it as a literal argument to `blobShape(...)`: `expandedIsland` (line 236), `mediaExpanded` (line 651), `mediaUnavailable` (line 716). Per D-08 this is a starting point for on-device tuning (Task 3), not a final value — all 3 sites must stay identical to each other. Do NOT change the `topCornerRadius: 6` argument (D-09: unchanged) or the `wingsShape`/`mediaWingsOrToast` corner radii (6/6, 6/16 — D-09: unchanged).

Do not touch any typography, spacing, padding, or activity-content code in this file (D-03/D-11, `expandedIdle`'s 3-column weather/time/calendar layout, `mediaExpanded`'s title/artist/transport row, `EqualizerBars`, `ProgressBar`, the shelf row) — those are out of scope and must render byte-identical to before this phase.
  </action>
  <acceptance_criteria>
    - `grep -c "static let islandMaterial" Islet/Notch/NotchPillView.swift` equals 1
    - `grep -c "fill(Color.black)" Islet/Notch/NotchPillView.swift` equals 0 (all 3 direct sites swapped)
    - `grep -c "fill(Self.islandMaterial)" Islet/Notch/NotchPillView.swift` equals 3 (blobShape, wingsShape, mediaWingsOrToast)
    - `collapsedFill`'s `#else` branch returns `Self.islandMaterial`, `#if DEBUG` branch still returns `Color.red.opacity(0.6)` unchanged
    - `grep -c "bottomCornerRadius: 32" Islet/Notch/NotchPillView.swift` equals 3 (expandedIsland, mediaExpanded, mediaUnavailable)
    - `grep -c "bottomCornerRadius: 20" Islet/Notch/NotchPillView.swift` equals 0
    - `wingsShape`'s `NotchShape(topCornerRadius: 6, bottomCornerRadius: 6)` and `mediaWingsOrToast`'s `toast != nil ? 16 : 6` are unchanged
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` succeeds with zero errors
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <done>All 4 fill sites render through the one shared `islandMaterial` gradient; the 3 expanded-blob call sites pass `bottomCornerRadius: 32`; build succeeds; no activity-content code touched.</done>
</task>

<task type="auto">
  <name>Task 2: Spring animation retune (VISUAL-02, D-05/D-06/D-07)</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift lines 250-290 (the springResponse/springDamping declarations and their surrounding comments — already read this session)
    .planning/phases/25-visual-material-theming-redesign/25-UI-SPEC.md (Spring animation table — starting values and rationale)
  </read_first>
  <action>
Change ONLY the 2 constant declarations at lines 264-265: `springResponse` from `0.35` to `0.6` (D-05: deliberately slower — larger `response` lengthens the settle time), `springDamping` from `0.65` to `0.62` (D-06: single visible overshoot-and-settle bounce — stay within the 0.55-0.7 band per RESEARCH.md Pitfall 3; going below ~0.5 risks a second, disallowed bounce per Success Criterion #2).

Do NOT touch any of the 13 `withAnimation(.spring(response: springResponse, dampingFraction: springDamping))` / `withAnimation(.spring(response: self.springResponse, dampingFraction: self.springDamping))` call sites (lines 519, 747, 789, 799, 834, 921, 1010, 1019, 1107, 1179, 1199, 1212, 1242) — they already read the same 2 properties, per this file's established single-source-of-truth pattern. Do NOT touch `graceDelay` (line 258, `0.4`) — it is a separate, independent constant, explicitly out of this phase's scope per RESEARCH.md Open Question 1 (flag any interaction issue found during Task 3's on-device check as a follow-up, do not silently fix it here).
  </action>
  <acceptance_criteria>
    - `grep -n "private let springResponse: Double = 0.6" Islet/Notch/NotchWindowController.swift` matches exactly 1 line
    - `grep -n "private let springDamping: Double = 0.62" Islet/Notch/NotchWindowController.swift` matches exactly 1 line
    - `grep -c "withAnimation(.spring(response:" Islet/Notch/NotchWindowController.swift` equals 13 (unchanged call-site count)
    - `grep -n "private let graceDelay: TimeInterval = 0.4" Islet/Notch/NotchWindowController.swift` matches exactly 1 line (unchanged)
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` succeeds with zero errors
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <done>springResponse=0.6 and springDamping=0.62 are the only 2 lines changed in this file; all 13 call sites and graceDelay are untouched; build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device UAT — gradient depth, corner roundness, spring feel, activity-content regression</name>
  <read_first>
    Islet/Notch/NotchPillView.swift (Task 1's edited islandMaterial + bottomCornerRadius values)
    Islet/Notch/NotchWindowController.swift (Task 2's edited springResponse/springDamping values)
    .planning/phases/25-visual-material-theming-redesign/25-VALIDATION.md "Manual-Only Verifications" table
  </read_first>
  <files>Islet/Notch/NotchPillView.swift, Islet/Notch/NotchWindowController.swift</files>
  <action>
    No file edits in this task by default — build and run the app on-device (Cmd-R in Xcode) and
    perform the 7-item on-device UAT described in how-to-verify below. The only in-scope edit is
    the documented contingency in check 5: adding NotchShape.swift's 3-line animatableData
    conformance IF AND ONLY IF a visible corner snap appears during the morph.
  </action>
  <what-built>
    A shared black-to-~50%-transparent gradient material across the collapsed pill, expanded island, and all activity wings (Task 1); a noticeably rounder expanded-blob bottom corner (20→32pt, Task 1); a slower spring with one visible overshoot-and-settle bounce on every hover-widen and click-expand transition (Task 2).
  </what-built>
  <how-to-verify>
    Open `Islet.xcodeproj` in Xcode (Finder path: `/Users/lippi304/conductor/workspaces/notch/algiers/Islet.xcodeproj`), select the `Islet` scheme, and Cmd-R to run on the real MacBook with the physical notch (a simulator/preview cannot validate real-panel frame drops or the never-focused panel — Success Criterion #4).

    1. **Gradient depth (D-02):** Hover the collapsed pill, then click to expand. Confirm the shape reads as solid/opaque black for most of its height, with only a mild, ~50%-opacity fade near the very bottom edge — NOT a "frosted/see-through" look. If it reads too transparent or the fade starts too early, adjust the `location: 0.65` stop in `islandMaterial` (Task 1) toward `0.7-0.75` and re-run.
    2. **Pure black, no grey (D-01):** Confirm no grey tint is visible anywhere in the gradient — pure black only.
    3. **Corner roundness (D-08):** Compare the expanded blob's bottom corners against memory of the old (pre-phase) shape — confirm they read distinctly "rounder"/"prall." If not rounded enough or too rounded, adjust the `32` value (Task 1, all 3 call sites in lock-step) and re-run.
    4. **Spring feel (D-05/D-06):** Trigger hover-widen and click-to-expand multiple times. Confirm the motion feels deliberately slower than a snappy default, with exactly ONE visible overshoot (grows slightly past target size, then settles back) — not a flat ease-in, and not 2+ oscillations ("jiggly"). If too flat, lower `springDamping` slightly (stay ≥0.5); if multi-bouncing, raise it back toward 0.7.
    5. **No dropped frames / no morph artifacts:** Watch specifically for banding, a hard edge, or a visible "kink"/"snap" in the bottom corner partway through the collapse↔expand morph (RESEARCH.md Pitfall 1 — `NotchShape` has no `animatableData` conformance, so the corner radius itself does not tween, only frame size does). If a visible snap appears, add the 3-line `animatableData` conformance to `Islet/Notch/NotchShape.swift` (shown in this plan's `<interfaces>` context block) and re-verify — this is the ONLY condition under which `NotchShape.swift` changes in this phase (D-09).
    6. **Rapid hover-enter/exit/re-enter (RESEARCH.md Pitfall 4):** Move the pointer in and out of the hot-zone rapidly several times. Confirm no "double-morph" flicker or intermediate-size snap. If found, do NOT fix `graceDelay` here — note it as a follow-up for a future phase.
    7. **Activity-content regression (D-03/D-11 scope boundary):** Cycle through Now Playing (playing + paused), Charging wings, Device wings, and the idle 3-column weather/time/calendar glance. Confirm every activity's layout, text, icons, and existing accent tinting (equalizer bars, charging glyph, device battery icon) render EXACTLY as before this phase — only the black chrome behind them changed.
  </how-to-verify>
  <resume-signal>Type "approved" once all 7 checks pass, or describe which check failed and its specific symptom (e.g. "gradient too transparent", "corner snap visible on expand", "spring feels flat, no overshoot").</resume-signal>
  <verify>
    <human-check>All 7 checklist items in how-to-verify pass on-device on the real notch hardware; if check 5's contingency was applied, xcodebuild build succeeds afterward too.</human-check>
  </verify>
  <done>User replies "approved"; gradient depth, corner roundness, spring feel, and activity-content parity are all on-device confirmed correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | This phase introduces no new trust boundary. It changes a `ShapeStyle` fill (`Color.black` → `LinearGradient`) and two `Double` animation-timing constants inside code that already renders only locally-computed, trusted values (shape geometry, spring timing). No new user input, network call, file I/O, persisted state, or external data source is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|--------------|------------------|
| T-25-01 | Tampering | `NotchPillView.swift` fill sites / `NotchWindowController.swift` spring constants | accept | No externally-influenced input reaches these values — both are compile-time literals read by the local rendering pipeline only. No mitigation needed; risk is inherently zero for this change's scope. |
</threat_model>

<verification>
- All 3 tasks' automated gates (`xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug`) pass with zero errors.
- Task 3's on-device UAT checklist (7 items) passes and the human returns "approved."
- Existing `NotchShapeTests.swift` / `InteractionStateTests.swift` / `EqualizerBarsTests.swift` stay green (run via manual Cmd-U in Xcode per project memory `xcodebuild-test-headless-hang` — this phase's changes do not touch the logic those tests cover).
- No diff appears anywhere outside `Islet/Notch/NotchPillView.swift` and `Islet/Notch/NotchWindowController.swift` (plus, only if Task 3's contingency triggers, `Islet/Notch/NotchShape.swift`).
</verification>

<success_criteria>
- The collapsed pill, expanded island, and activity wings all render the one shared vertical alpha-gradient material (VISUAL-01, ROADMAP Phase 25 Success Criterion #1).
- The expand/collapse animation uses a fluid, deliberately-paced spring with a subtle bounce-in on open, no dropped frames, no overshoot beyond one subtle in-bounce (VISUAL-02, Success Criterion #2).
- The material and animation changes apply only to shared shell chrome; existing activity content renders unchanged inside the new chrome (Success Criterion #3).
- The material composites without visual artifacts mid-morph, verified on-device in the real never-focused panel (Success Criterion #4).
</success_criteria>

<output>
Create `.planning/phases/25-visual-material-theming-redesign/25-01-SUMMARY.md` when done
</output>
