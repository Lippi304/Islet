---
phase: 67.1-notch-size-scaling-resolution-aware-manual-override
plan: 07
type: execute
wave: 5
depends_on: ["67.1-02", "67.1-03", "67.1-04"]
files_modified: [Islet/Notch/NotchPillView.swift, Islet/Notch/NotchWindowController.swift, Islet/SettingsView.swift]
autonomous: true
requirements: [D-13, D-12]

must_haves:
  truths:
    - "Dragging the Width/Depth Settings sliders away from their auto default no longer changes the size of Home/Tray/Weather/Calendar/Quick Actions Bar/Switcher (expanded content) at all — only the auto resolution-driven factor affects them"
    - "The Settings 'Island Size' section's helper caption no longer claims to control expanded views; it accurately describes the new collapsed-state Live Activity wing target"
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "resolvedWidthScale/resolvedDepthScale hardcoded to manualOffset: 0 (auto-only, D-13)"
      contains: "resolvedIslandScale(auto: autoIslandScale, manualOffset: 0)"
    - path: "Islet/Notch/NotchWindowController.swift"
      provides: "currentWidthScale/currentDepthScale hardcoded to manualOffset: 0 (auto-only, D-13)"
      contains: "resolvedIslandScale(auto: autoScale, manualOffset: 0)"
  key_links:
    - from: "NotchPillView.tabWidth/tabHeight/totalHeight"
      to: "resolvedWidthScale/resolvedDepthScale"
      via: "direct property read, now auto-only"
      pattern: "resolvedIslandScale\\(auto: autoIslandScale, manualOffset: 0\\)"
    - from: "NotchWindowController.visibleContentZone()"
      to: "currentWidthScale/currentDepthScale"
      via: "direct property read, now auto-only"
      pattern: "resolvedIslandScale\\(auto: autoScale, manualOffset: 0\\)"
---

<objective>
Retarget the phase's manual Width/Depth control away from the EXPANDED state per D-13: `resolvedWidthScale`/`resolvedDepthScale` (NotchPillView.swift, Site 1 render) and `currentWidthScale`/`currentDepthScale` (NotchWindowController.swift, Site 3 click-through) stop reading the `islandWidthScaleOffset`/`islandDepthScaleOffset` manual offsets and become auto-only (D-05/D-06 resolution-driven factor only) — the two sliders no longer affect Home/Tray/Weather/Calendar/Quick Actions Bar/Switcher at all. Site 2 (`positionAndShow`'s panel-frame reservation) already uses the fixed 150% ceiling unconditionally (Plan 03, Pitfall 3) and needs no change. Also updates the Settings UI copy (D-12) so the "Island Size" section's helper caption no longer describes the (now stale) expanded-view target — Plans 08-10 retarget the SAME two `@AppStorage` offsets to drive collapsed-state Live Activity wing content instead.

Purpose: Reversal step 1 of the second CONTEXT.md amendment — the expanded state must keep ONLY the D-05/D-06 auto factor going forward, with zero manual-slider influence, before Plan 08 repurposes the freed-up manual offset for wing content. Doing this FIRST (before Plan 08 adds a new consumer of the same offsets) keeps the "one offset, one active target at a time" invariant clean — there is never a moment where the offset simultaneously drives two things.
Output: Expanded content (Home/Tray/Weather/Calendar/Quick Actions Bar/Switcher) scales with resolution only, never with the Width/Depth sliders. Settings copy no longer describes the old target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-CONTEXT.md
@.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-02-SUMMARY.md
@.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-03-SUMMARY.md
@.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-04-SUMMARY.md
</context>

<interfaces>
<!-- Existing auto+manual combination this plan strips the manual half from. No new types. -->

From Islet/Notch/NotchGeometry.swift (unchanged, read-only — Plan 01):
```swift
func islandAutoScaleFactor(screenWidthPoints: CGFloat, baseline: CGFloat = 1470) -> CGFloat
func resolvedIslandScale(auto: CGFloat, manualOffset: CGFloat, range: ClosedRange<CGFloat> = 0.8...1.5) -> CGFloat
```

Current state, NotchPillView.swift:2801-2814 (to be changed by Task 1):
```swift
private var autoIslandScale: CGFloat { ... }               // UNCHANGED — still the live screen read
private var resolvedWidthScale: CGFloat {
    resolvedIslandScale(auto: autoIslandScale, manualOffset: CGFloat(islandWidthScaleOffset))   // -> manualOffset: 0
}
private var resolvedDepthScale: CGFloat {
    resolvedIslandScale(auto: autoIslandScale, manualOffset: CGFloat(islandDepthScaleOffset))   // -> manualOffset: 0
}
```

Current state, NotchWindowController.swift:1343-1347 (to be changed by Task 1):
```swift
let widthOffset = UserDefaults.standard.double(forKey: ActivitySettings.islandWidthScaleOffsetKey)
let depthOffset = UserDefaults.standard.double(forKey: ActivitySettings.islandDepthScaleOffsetKey)
let autoScale = islandAutoScaleFactor(screenWidthPoints: target.frame.width)
currentWidthScale = resolvedIslandScale(auto: autoScale, manualOffset: widthOffset)   // -> manualOffset: 0, delete widthOffset local
currentDepthScale = resolvedIslandScale(auto: autoScale, manualOffset: depthOffset)   // -> manualOffset: 0, delete depthOffset local
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: D-13 — expanded state becomes auto-only (Site 1 + Site 3)</name>
  <files>Islet/Notch/NotchPillView.swift, Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift:2801-2814 (autoIslandScale/resolvedWidthScale/resolvedDepthScale)
    Islet/Notch/NotchPillView.swift:175-176 (islandWidthScaleOffset/islandDepthScaleOffset @AppStorage declarations — NOT removed by this task, Plan 08 repurposes them)
    Islet/Notch/NotchWindowController.swift:1340-1347 (currentWidthScale/currentDepthScale computation inside positionAndShow)
    Islet/Notch/NotchWindowController.swift:1855-1953 (visibleContentZone(), Site 3 — the sole consumer of currentWidthScale/currentDepthScale; confirm it only READS the two stored properties, no change needed there)
    .planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-CONTEXT.md lines 55-63 (D-11..D-14, especially D-13's exact wording)
  </read_first>
  <action>
    In NotchPillView.swift, change `resolvedWidthScale`'s body from `resolvedIslandScale(auto: autoIslandScale, manualOffset: CGFloat(islandWidthScaleOffset))` to `resolvedIslandScale(auto: autoIslandScale, manualOffset: 0)`. Change `resolvedDepthScale` identically (`islandDepthScaleOffset` argument becomes literal `0`). Add a short comment directly above both properties noting: 67.1-07 (D-13) — expanded state is auto-only going forward; the manual Width/Depth sliders now drive collapsed-state wing content instead via `resolvedWingWidthScale`/`resolvedWingDepthScale` (Plan 08). Leave the `islandWidthScaleOffset`/`islandDepthScaleOffset` `@AppStorage` property declarations at lines 175-176 completely untouched — they become temporarily unread until Plan 08 adds new consumers (a Swift "variable was never used" warning on these two properties between this plan landing and Plan 08 landing is expected and harmless; it does not fail the build).

    In NotchWindowController.swift, inside `positionAndShow(on:)`, change the two computation lines currently reading `currentWidthScale = resolvedIslandScale(auto: autoScale, manualOffset: widthOffset)` and the matching depth line to pass `manualOffset: 0` instead of `widthOffset`/`depthOffset`. Since `widthOffset`/`depthOffset` become dead locals once inlined out, delete the two `let widthOffset = UserDefaults.standard.double(forKey: ActivitySettings.islandWidthScaleOffsetKey)` / `let depthOffset = ...` lines entirely (do not leave unused locals) and inline `manualOffset: 0` directly at both `resolvedIslandScale(...)` call sites. Add the same short D-13 comment above.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "resolvedIslandScale(auto: autoIslandScale, manualOffset: 0)" Islet/Notch/NotchPillView.swift` returns 2
    - `grep -c "resolvedIslandScale(auto: autoScale, manualOffset: 0)" Islet/Notch/NotchWindowController.swift` returns 2
    - `grep -c "islandWidthScaleOffsetKey\|islandDepthScaleOffsetKey" Islet/Notch/NotchWindowController.swift` returns 0 (the dead UserDefaults reads are fully removed, not just unused)
    - `grep -c "@AppStorage(ActivitySettings.islandWidthScaleOffsetKey)" Islet/Notch/NotchPillView.swift` still returns 1 (declaration untouched, only its consumer changed)
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` exits 0
  </acceptance_criteria>
  <done>Expanded content (Home/Tray/Weather/Calendar/Quick Actions Bar/Switcher) at both Site 1 (render) and Site 3 (click-through) reads a scale that is a pure function of the live screen width only — zero manual-offset influence, D-13 satisfied by construction.</done>
</task>

<task type="auto">
  <name>Task 2: D-12 — retarget Settings "Island Size" helper copy</name>
  <files>Islet/SettingsView.swift</files>
  <read_first>
    Islet/SettingsView.swift:1045-1073 (the "Island Size" Section block: Width/Depth sliders, helper caption, Reset to Auto)
    .planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-UI-SPEC.md lines 120-131 (locked copy this partially supersedes — the caption line is now stale per D-12; everything else in that table stays valid)
    .planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-CONTEXT.md lines 60-63 ("Claude's Discretion (second amendment)" — exact wording is Claude's call)
  </read_first>
  <action>
    Change ONLY the helper caption text at line 1064, currently `Text("Adjusts the size of expanded views — Home, Tray, Calendar, Weather, Quick Actions. The collapsed notch is unaffected.")`, to `Text("Adjusts the size of collapsed Live Activities — Music, OSD volume/brightness, Caps Lock, Charging, Focus, Device, Update, Download, Timer, Meeting, and Countdown. The plain idle notch is unaffected.")`. Leave the section header ("Island Size"), row labels ("Width"/"Depth"), the `Int(...Binding.wrappedValue * 100)` percentage readout, and the "Reset to Auto" button copy exactly as they are today — this is a copy-only edit, no binding/computation changes in this file.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Adjusts the size of expanded views" Islet/SettingsView.swift` returns 0
    - `grep -c "Adjusts the size of collapsed Live Activities" Islet/SettingsView.swift` returns 1
    - `grep -c 'Section("Island Size")' Islet/SettingsView.swift` still returns 1 (unchanged)
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` exits 0
  </acceptance_criteria>
  <done>Settings copy accurately describes what the two sliders control after D-12/D-13's retarget — no leftover reference to the now-stale expanded-view target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted UserDefaults offset -> expanded-content geometry | The two manual offset keys are no longer read for expanded-content sizing at all after this plan — removes, not adds, an input to Site 1/Site 3's geometry math |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67.1-10 | Repudiation of the geometry three-site rule (Site 1/Site 3 desync) | NotchPillView.swift resolvedWidthScale/resolvedDepthScale vs. NotchWindowController.swift currentWidthScale/currentDepthScale | mitigate | Both sites change to `manualOffset: 0` in the SAME task/commit, verified by the same two grep patterns in the same acceptance_criteria block — cannot land one without the other |
</threat_model>

<verification>
Full build green (Task 1 + Task 2). Grep-verified: expanded-content scale computation is auto-only at both Site 1 and Site 3; the dead UserDefaults offset reads are removed from NotchWindowController.swift; Settings copy no longer references the expanded-view target.
</verification>

<success_criteria>
- resolvedWidthScale/resolvedDepthScale (Site 1) and currentWidthScale/currentDepthScale (Site 3) are provably auto-only (manualOffset literal 0, dead UserDefaults reads removed).
- Settings "Island Size" helper caption accurately describes the collapsed-state wing target.
- Build green.
</success_criteria>

<output>
Create `.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-07-SUMMARY.md` when done
</output>
