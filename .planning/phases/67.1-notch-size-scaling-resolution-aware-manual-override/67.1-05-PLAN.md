---
phase: 67.1
plan: 05
type: execute
wave: 3
depends_on: ["67.1-02", "67.1-03", "67.1-04"]
files_modified: []
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08]

must_haves:
  truths:
    - "On the user's real reported resolution (1710×1112), the expanded island renders visibly larger than at 1470×956, with zero manual action, matching the ~1.1633 auto-scale factor"
    - "Dragging the Width/Depth sliders in Settings live-resizes the real notch island immediately, with no relaunch and no visible lag/flicker"
    - "Clicking any of Home/Tray/Weather/Calendar/Quick-Actions-Bar/Switcher-row remains fully functional (no dead zones, no premature collapse) at both the 0.8 and 1.5 scale extremes"
    - "The collapsed/idle pill and the physical-camera clearance band are pixel-identical to before this phase at every scale value"
    - "Reset to Auto correctly restores both sliders to the current display's computed default"
  artifacts: []
  key_links: []
---

<objective>
Close out Phase 67.1: run the full automated gate (build + full XCTest suite) across all 3 sites' changes together, then run the on-device UAT VALIDATION.md flags as the ONLY way to confirm (rendered pixel size, live slider drag, click-through correctness across the 3 geometry sites — none of which XCTest can verify).

Purpose: This is the single point where Plan 02 (Site 1), Plan 03 (Site 2/3), and Plan 04 (Settings UI) are exercised together for the first time — the "geometry three-site rule" parity established by construction in Plans 01-03 gets its real-world confirmation here, per this project's own established convention for every geometry-affecting phase (Phase 29/39/48/62/63/65 UAT history).
Output: Full test suite green + on-device UAT approval, closing Phase 67.1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-CONTEXT.md
@.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-VALIDATION.md
@.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-02-SUMMARY.md
@.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-03-SUMMARY.md
@.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build + full test suite gate</name>
  <files></files>
  <read_first>Islet/Notch/NotchGeometry.swift, Islet/Notch/NotchPillView.swift, Islet/Notch/NotchWindowController.swift, Islet/ActivitySettings.swift, Islet/SettingsView.swift</read_first>
  <action>
    Build the app in Release configuration (not just Debug) to catch any `#available`-branch or
    optimization-level compile issue the earlier plans' Debug-only builds might have missed
    (mirrors this project's own Phase 24-03 "including a Release-configuration pass" precedent).
    Run the FULL XCTest suite (not just the `NotchGeometryTests` filter used per-task) to confirm
    zero cross-file regressions between Plan 02's NotchPillView.swift changes and Plan 03's
    NotchWindowController.swift changes now that both are merged.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -configuration Release -destination 'platform=macOS' && xcodebuild test -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - Release build exits 0
    - Full XCTest suite exits 0, including all 5 new NotchGeometryTests cases from Plan 01 and the full existing NotchPillViewTests/NotchWindowControllerTests/NotchPanelTests suites with zero regressions
  </acceptance_criteria>
  <done>Release build and full test suite both green with all 3 plans' changes merged together.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Automatic resolution-aware island scaling (D-05/D-06) + two manual Settings sliders
    (Width/Depth, D-03/D-04) that combine additively with the auto default (D-07/D-08), applied
    uniformly across Home, Tray, Weather, Calendar, Quick Actions Bar, and the switcher row
    (D-01), with the collapsed pill and camera clearance explicitly untouched (D-02).
  </what-built>
  <how-to-verify>
    1. On your actual 1710×1112-point display (the reported problem resolution): open the island
       to Home, Tray, Weather, Calendar, and the Quick Actions Bar in turn. Each should read
       visibly larger than it did before this phase (roughly +16% linear, matching the
       1710/1470 ≈ 1.1633 ratio) — confirm none looks clipped, cropped, or overflowing past the
       island's rounded edge.
    2. Open Settings → Appearance → "Island Size". Confirm both sliders start near 116% (the
       auto-computed default for this resolution), not 100%.
    3. Drag the Width slider to its 150% ceiling while the island is open on Home — confirm the
       real notch island resizes live, with no relaunch, no visible lag, no flicker, and no
       window clipping. Repeat for Depth.
    4. Drag both sliders down to their 80% floor — confirm the island shrinks live and every tab
       (Home/Tray/Weather/Calendar/Quick Actions Bar/switcher row) is STILL fully clickable at
       this smaller size — no dead zones, no clicks that fall through to the desktop, no
       premature collapse when moving the pointer within the visibly-shrunk shape.
    5. Open the Timer setup tab and Home with the Output sub-panel expanded, at both the 150%
       ceiling and 80% floor — these also scale via Phase 45's shared `tabContentView` choke
       point (Plans 02/03) but aren't among the 6 named views above, so confirm neither is
       clipped, overflowing, or has dead click zones at either extreme.
    6. Click "Reset to Auto" — confirm both sliders snap back to the ~116% auto default (not 100%,
       not 0%).
    7. Confirm the collapsed/idle pill (before hovering/clicking) is pixel-identical to before
       this phase at every slider position tried above — it must never grow or shrink.
    8. If you have access to a second Mac or an external display set to exactly 1470×956 points,
       repeat step 1 there and confirm the island renders at its pre-phase baseline size (100%,
       no visible change from before this phase).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (be specific about which tab/scale combination and what looked wrong)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Full merged codebase → real user hardware | The first point where all 3 geometry sites' changes run together against live NSScreen data and real click-through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67.1-07 | Repudiation of the "geometry three-site rule" (silent desync) | Full merged app | mitigate | This plan's on-device checkpoint is the ONLY verification step covering all 3 sites together — it exists specifically because Plans 01-03 each verified their own site in isolation, and this codebase's own history (4+ prior CR-01/CR-02 regressions) shows that isolation alone is insufficient; step 4 of the checkpoint explicitly exercises click-through at both scale extremes for exactly this reason |
</threat_model>

<verification>
Full Release build + full XCTest suite green (Task 1). On-device UAT checkpoint approved covering auto-scale at the real reported resolution, live slider drag at both extremes, click-through correctness at both extremes, Reset to Auto, and collapsed-pill/camera-clearance non-regression (Task 2).
</verification>

<success_criteria>
- Full test suite green in Release configuration.
- On-device UAT approved: auto-scale visible at the real resolution, manual sliders live-update with no lag, click-through correct at 0.8x and 1.5x extremes across all 6 named views, Reset to Auto works, collapsed pill/camera clearance unchanged.
- Phase 67.1 ready to close.
</success_criteria>

<output>
Create `.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-05-SUMMARY.md` when done
</output>
