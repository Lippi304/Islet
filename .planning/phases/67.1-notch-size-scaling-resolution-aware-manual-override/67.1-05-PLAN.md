---
phase: 67.1
plan: 05
type: execute
wave: 9
depends_on: ["67.1-02", "67.1-03", "67.1-04", "67.1-06", "67.1-07", "67.1-08", "67.1-09", "67.1-10"]
files_modified: []
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14]

must_haves:
  truths:
    - "On the user's real reported resolution (1710x1112), the expanded island (Home/Tray/Weather/Calendar/Quick Actions Bar/Switcher) renders visibly larger than at 1470x956, matching the ~1.1633 auto-scale factor, with ZERO influence from the Width/Depth sliders at any slider position (D-13)"
    - "Dragging the Width/Depth sliders in Settings live-resizes the COLLAPSED-state Live Activity wings (Music, OSD, Caps Lock, Charging, Focus, Device, Update, Download, Timer, Meeting, Countdown) immediately, with no relaunch and no visible lag/flicker (D-12/D-14)"
    - "At the slider's 80% floor, every wing's real camera-cutout clearance is unchanged from its pre-phase (Plan 06) value — no wing ever visually overlaps or clips against the real camera at any slider position (D-14 camera-safety floor)"
    - "The Music-Play wing (mediaWingsRow) no longer clips the album art or the 5th equalizer bar at the user's originally-reported 1710x1112 resolution — the specific bug that triggered this amendment (D-14)"
    - "The plain idle collapsed pill (no active Live Activity, collapsedIsland) is pixel-identical to its Plan 06-confirmed size at every Width/Depth slider position and every tested resolution — completely unaffected by any slider (D-02/D-09/D-11)"
    - "Reset to Auto correctly restores both sliders to the current display's computed default, and that default now visibly affects wing content, not expanded views (D-07/D-12)"
    - "Clicking Home/Tray/Weather/Calendar/Quick-Actions-Bar/Switcher-row remains fully functional (no dead zones) at the expanded state's 150% auto-ceiling reservation, and the Meeting wing's mute icon remains clickable at both the 80% and 150% wing-scale extremes"
  artifacts: []
  key_links: []
---

<objective>
Close out Phase 67.1 (superseding the stale, never-executed original 67.1-05-PLAN.md, which predates D-09 through D-14): run the full automated build+test gate across ALL of Plans 01-04 and 06-10's changes together, then run the on-device UAT this project's own established convention requires for every geometry-affecting phase (Phase 29/39/48/62/63/65 UAT history) — rendered pixel size, live slider drag, click-through correctness, and the camera-safety floor are things XCTest cannot verify.

This is the FIRST point where the full, twice-amended scope of this phase is exercised together: D-01/D-02 (expanded auto-scale, collapsed pill out of scope), D-03/D-04 (Width/Depth sliders, 80-150% range), D-05/D-06 (resolution-driven auto default), D-07/D-08 (auto+manual combination, single global offset), D-09/D-10 (collapsed-pill live re-measurement fix), and — the second amendment — D-11 (idle pill reaffirmed untouched), D-12 (sliders retargeted from expanded to wing content), D-13 (expanded state is now auto-only), D-14 (wing content gets the new auto+manual combination, camera-safety floor non-negotiable).

Purpose: Single comprehensive UAT gate closing every decision in this phase, including the original Music-Play wing clipping bug that triggered the second amendment.
Output: Full test suite green + on-device UAT approval, closing Phase 67.1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-CONTEXT.md
@.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-VALIDATION.md
@.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-07-SUMMARY.md
@.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-08-SUMMARY.md
@.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-09-SUMMARY.md
@.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-10-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build + full test suite gate across all 10 plans' changes</name>
  <files></files>
  <read_first>
    Islet/Notch/NotchGeometry.swift, Islet/Notch/NotchPillView.swift, Islet/Notch/NotchWindowController.swift, Islet/ActivitySettings.swift, Islet/SettingsView.swift
    .planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/deferred-items.md (the pre-existing, unrelated SettingsViewTests + LicenseStateTests failures every prior plan in this phase has logged — confirm the count hasn't grown)
  </read_first>
  <action>
    Build the app in Release configuration (not just Debug) to catch any `#available`-branch or
    optimization-level compile issue Plans 07-10's Debug-only builds might have missed (mirrors this
    project's own Phase 24-03 "including a Release-configuration pass" precedent, and this phase's
    own original Plan 05 design). Run the FULL XCTest suite to confirm zero NEW cross-file
    regressions between Plan 07 (expanded-state auto-only retarget), Plan 08 (wing-scale contract +
    4 wings), Plan 09 (6 more wings), and Plan 10 (Music-Play wing rebuild) now that all are merged.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -configuration Release -destination 'platform=macOS' && xcodebuild test -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - Release build exits 0
    - Full XCTest suite exits 0 modulo ONLY the 2 pre-existing SettingsViewTests + 4 pre-existing LicenseStateTests failures already documented in deferred-items.md (no new failures introduced by Plans 07-10)
    - `grep -c "resolvedIslandScale(auto: autoIslandScale, manualOffset: 0)" Islet/Notch/NotchPillView.swift` returns 2 (D-13 Site 1 still auto-only)
    - `grep -c "resolvedIslandScale(auto: autoScale, manualOffset: 0)" Islet/Notch/NotchWindowController.swift` returns 2 (D-13 Site 3 still auto-only)
    - `grep -c "private var resolvedWingWidthScale\|private var resolvedWingDepthScale" Islet/Notch/NotchPillView.swift` returns 2 (D-14 contract present)
  </acceptance_criteria>
  <done>Release build and full test suite both green with all 10 plans' changes merged together; D-13/D-14's key structural invariants confirmed present by grep before proceeding to on-device UAT.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device UAT — full D-01..D-14 verification</name>
  <files></files>
  <read_first>
    .planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-CONTEXT.md (all decisions D-01..D-14)
    67.1-07-SUMMARY.md, 67.1-08-SUMMARY.md, 67.1-09-SUMMARY.md, 67.1-10-SUMMARY.md (exactly what Plans 07-10 shipped, so the on-device steps below test the REAL implementation, not an assumption)
  </read_first>
  <what-built>
    The complete, twice-amended Phase 67.1: (1) automatic resolution-aware scaling for the EXPANDED
    state (Home/Tray/Weather/Calendar/Quick Actions Bar/Switcher), now auto-only per D-13 with zero
    manual-slider influence; (2) the SAME two Width/Depth Settings sliders, retargeted per D-12 to
    grow/shrink COLLAPSED-state Live Activity wing content instead (Music, OSD, Caps Lock, Charging,
    Focus, Device, Update, Download, Timer, Meeting, Countdown), combining an auto resolution-driven
    default with the manual offset (D-14), with the real camera cutout's clearance guaranteed
    untouched at every slider position; (3) the plain idle collapsed pill, unaffected by any of the
    above (D-02/D-09/D-11); (4) a fix for the Music-Play wing's original clipping bug at non-baseline
    resolutions, the specific trigger for this whole amendment.
  </what-built>
  <how-to-verify>
    1. On your actual 1710x1112-point display (the reported problem resolution): open the island to
       Home, Tray, Weather, Calendar, and the Quick Actions Bar in turn. Each should read visibly
       larger than the pre-Phase-67.1 baseline (roughly +16% linear, matching 1710/1470 ~= 1.1633) —
       confirm none looks clipped, cropped, or overflowing.
    2. Open Settings -> Appearance -> "Island Size". Confirm the helper caption now reads
       "Adjusts the size of collapsed Live Activities..." (not "expanded views").
    3. With Home open, drag BOTH sliders from their auto default down to 80% and up to 150% — confirm
       Home/Tray/Weather/Calendar/Quick Actions Bar/Switcher do NOT change size at all while dragging
       (D-13: expanded state is now auto-only). This is the key regression check for the second
       amendment's reversal.
    4. Play a song (or trigger any Now Playing state) so the Music-Play wing (collapsed) is visible.
       Drag both sliders through their full 80%-150% range — confirm the wing visibly grows/shrinks
       (more/less padding around the album art and equalizer bars), and confirm the album art and
       ALL 5 equalizer bars stay fully visible and un-clipped against the real camera cutout at every
       slider position, including the 80% floor. This directly re-tests the original bug trigger.
    5. Trigger each of the other collapsed wings in turn (OSD volume/brightness via a hardware key,
       Caps Lock toggle, plug/unplug for Charging, a Focus mode toggle, a Bluetooth device
       connect/disconnect, a Sparkle update check if available, a file download, a Timer/Pomodoro
       session, a Meeting/call, a Calendar countdown) and drag both sliders through 80%/auto/150% for
       each — confirm each wing visibly grows/shrinks and never clips against the real camera cutout
       at the 80% floor.
    6. During the Meeting wing check specifically: confirm the mute icon is still clickable (tap it,
       confirm mute toggles) at both the 80% and 150% wing-scale extremes.
    7. With NO Live Activity active (plain idle pill only), drag both sliders through their full
       range — confirm the idle pill's size never changes at any position (D-02/D-09/D-11
       non-regression, already confirmed in Plan 06 — re-confirm it still holds after Plans 07-10).
    8. Click "Reset to Auto" — confirm both sliders snap back to the auto-computed default for this
       resolution, and confirm wing content (not expanded views) visibly reflects that default.
    9. If you have access to a second Mac or an external display set to exactly 1470x956 points,
       repeat steps 1 and 4-5 there and confirm expanded views render at their pre-phase baseline
       (100%, auto-only) and wing content still correctly auto+manual-scales relative to that
       baseline.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (be specific about which wing/tab/slider-position combination and what looked wrong)</resume-signal>
  <acceptance_criteria>
    User replies "approved" after completing all 9 numbered steps in how-to-verify above — a reply describing any issue is treated as NOT approved and requires a follow-up fix before this task is done.
  </acceptance_criteria>
  <action>No code changes — perform the on-device verification steps above and relay the result.</action>
  <verify>
    <manual>User confirms "approved" after performing all 9 steps above on real hardware</manual>
  </verify>
  <done>On-device UAT approved: every truth in this plan's must_haves block is confirmed on real hardware, closing Phase 67.1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Full merged codebase (Plans 01-04, 06-10) -> real user hardware | The first point where the complete, twice-amended scope of this phase runs together against live NSScreen data and real click-through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67.1-15 | Repudiation of the "geometry three-site rule" / D-13 reversal (silent desync between Site 1 and Site 3, or a wing accidentally still reading the old expanded-scale properties) | Full merged app | mitigate | This plan's on-device checkpoint step 3 explicitly re-tests the D-13 reversal (expanded state must NOT move with the sliders) as its own dedicated check, not folded into a generic "looks fine" pass — this project's own history (4+ prior CR-01/CR-02 regressions, plus this exact phase's own D-09/D-10 gap) shows isolated per-plan verification is insufficient for cross-cutting geometry changes |
| T-67.1-16 | Denial of Service (usability) — a wing's click-through zone silently doesn't cover its full visually-grown footprint at the 150% wing-scale ceiling | collapsedInteractiveZone()/meetingClickThroughZones() vs. the now width-scalable wings | accept | Out of this phase's scope to retrofit (these click-through zones were not widened for ANY wing's rendered footprint before this phase either, per direct code read of collapsedInteractiveZone() — this is pre-existing behavior, not a regression this phase introduces); step 6 of the on-device checkpoint specifically verifies the ONE wing with a documented click-through mechanism (Meeting's mute icon) still works at both extremes; any newly-discovered dead zone on another wing is a candidate for a follow-up gap-closure phase, not a blind fix here |
</threat_model>

<verification>
Full Release build + full XCTest suite green (Task 1), with D-13/D-14's key structural invariants grep-confirmed present. On-device UAT checkpoint approved covering: expanded-state auto-only non-regression (D-13), wing-content auto+manual scaling with camera-safety floor at extremes (D-14) across all 10 named wings plus the Music-Play bug fix, idle-pill non-regression (D-02/D-09/D-11), Reset to Auto, and Meeting mute-icon click-through at both scale extremes.
</verification>

<success_criteria>
- Full test suite green in Release configuration, D-13/D-14 structural invariants grep-confirmed.
- On-device UAT approved: expanded views are provably unaffected by the sliders; every named collapsed wing scales with the sliders and never clips the real camera at the 80% floor; the Music-Play wing's original clipping bug is confirmed fixed; the idle pill is unaffected; Reset to Auto works; the Meeting mute icon stays clickable at both scale extremes.
- Phase 67.1 (both amendments) ready to close.
</success_criteria>

<output>
Create `.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-05-SUMMARY.md` when done
</output>
