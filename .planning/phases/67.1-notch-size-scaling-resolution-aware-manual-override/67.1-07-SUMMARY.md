---
phase: 67.1-notch-size-scaling-resolution-aware-manual-override
plan: 07
subsystem: ui
tags: [swiftui, notch-geometry, settings, scaling]

# Dependency graph
requires:
  - phase: 67.1-02
    provides: resolvedIslandScale/islandAutoScaleFactor pure functions + the two manual @AppStorage offset keys
  - phase: 67.1-03
    provides: geometry three-site rule (Site 1/Site 3 parity for currentWidthScale/currentDepthScale)
  - phase: 67.1-04
    provides: the Settings "Island Size" section UI (sliders, percentage readout, Reset to Auto)
provides:
  - Expanded content (Home/Tray/Weather/Calendar/Quick Actions Bar/Switcher) now scales with the D-05/D-06 auto resolution factor only — zero manual-slider influence at Site 1 (render) and Site 3 (click-through)
  - Settings "Island Size" helper caption rewritten to describe the collapsed-state Live Activity wing target instead of the stale expanded-view target
affects: [67.1-08, 67.1-09, 67.1-10]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - Islet/Notch/NotchPillView.swift
    - Islet/Notch/NotchWindowController.swift
    - Islet/SettingsView.swift

key-decisions:
  - "D-13 implemented literally: manualOffset hardcoded to 0 at both Site 1 and Site 3 in the same commit, verified by the same grep patterns in one acceptance_criteria block (threat T-67.1-10 mitigation)"
  - "islandWidthScaleOffset/islandDepthScaleOffset @AppStorage declarations left untouched per plan — they become temporarily unread until Plan 08 adds new consumers"

patterns-established: []

requirements-completed: [D-13, D-12]

# Metrics
duration: 8min
completed: 2026-07-26
---

# Phase 67.1 Plan 07: Expanded-State Auto-Only Retarget (D-13/D-12) Summary

**Expanded content (Home/Tray/Weather/Calendar/Quick Actions Bar/Switcher) now scales purely with live screen resolution — the Width/Depth Settings sliders no longer touch it at all — and the Settings caption was rewritten to stop claiming otherwise, clearing the ground for Plan 08 to repurpose the same two sliders for collapsed-state wing content.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-07-26T18:09:00Z
- **Completed:** 2026-07-26T18:17:55Z
- **Tasks:** 2 completed
- **Files modified:** 3

## Accomplishments
- `resolvedWidthScale`/`resolvedDepthScale` (NotchPillView.swift, Site 1) and `currentWidthScale`/`currentDepthScale` (NotchWindowController.swift, Site 3) are now provably auto-only — `manualOffset: 0` literal at both sites, dead `UserDefaults` offset reads removed from the controller.
- Settings "Island Size" helper caption accurately describes the new collapsed-state Live Activity wing target.

## Task Commits

Each task was committed atomically:

1. **Task 1: D-13 — expanded state becomes auto-only (Site 1 + Site 3)** - `1569cba` (feat)
2. **Task 2: D-12 — retarget Settings "Island Size" helper copy** - `b741c43` (docs)

_Note: no TDD tasks in this plan._

## Files Created/Modified
- `Islet/Notch/NotchPillView.swift` - `resolvedWidthScale`/`resolvedDepthScale` now pass `manualOffset: 0` instead of the `islandWidthScaleOffset`/`islandDepthScaleOffset` AppStorage values; `autoIslandScale` and the AppStorage declarations themselves unchanged.
- `Islet/Notch/NotchWindowController.swift` - `positionAndShow(on:)` no longer reads `islandWidthScaleOffsetKey`/`islandDepthScaleOffsetKey` from `UserDefaults`; `currentWidthScale`/`currentDepthScale` computed with `manualOffset: 0`.
- `Islet/SettingsView.swift` - Helper caption text under the Width/Depth sliders updated to describe the collapsed-state Live Activity wing target; section header, row labels, percentage readout, and Reset to Auto button untouched.

## Decisions Made
- Both Site 1 and Site 3 changed in a single commit (Task 1) rather than split across two commits, per the plan's explicit threat-model mitigation for T-67.1-10 (Site 1/Site 3 desync) — a partial land was never possible.
- Left the `islandWidthScaleOffset`/`islandDepthScaleOffset` `@AppStorage` properties in `NotchPillView.swift` fully untouched (per plan instruction) even though they are now unread until Plan 08 lands — expected and harmless per the plan's own acceptance criteria.

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None.

## Threat Flags

None - this plan removes an input to expanded-content geometry (manual offset reads), it does not add new surface.

## Self-Check: PASSED
