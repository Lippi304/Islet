---
phase: 67.1-notch-size-scaling-resolution-aware-manual-override
plan: 03
subsystem: ui
tags: [swift, appkit, notch-geometry, click-through, resolution-scaling]

# Dependency graph
requires:
  - phase: 67.1-01
    provides: "islandAutoScaleFactor/resolvedIslandScale pure functions (NotchGeometry.swift) and ActivitySettings.islandWidthScaleOffsetKey/islandDepthScaleOffsetKey UserDefaults keys, unit-tested"
  - phase: 67.1-02
    provides: "Site 1 (NotchPillView) scale-aware rendering using the same 2 pure functions and Constant Scale Inventory"
provides:
  - "Site 2 (positionAndShow) reserves the AppKit panel frame at the fixed 150% (D-04) ceiling on every in-scope axis"
  - "Site 3 (visibleContentZone) click-through hit-test mirrors Site 1's LIVE resolved scale exactly"
  - "currentWidthScale/currentDepthScale stored properties on NotchWindowController, resolved once per positionAndShow call"
affects: [67.1-04, 67.1-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Geometry three-site rule: Site 1 (SwiftUI render) and Site 3 (click-through hit-test) both read the SAME live-resolved scale; Site 2 (AppKit panel reservation) always reserves the fixed upper-bound ceiling instead of the live value, so a live slider drag never needs a mid-interaction panel resize (Pitfall 3)"

key-files:
  created: []
  modified:
    - "Islet/Notch/NotchWindowController.swift"

key-decisions:
  - "currentWidthScale/currentDepthScale stored as private CGFloat properties (default 1.0), resolved once per positionAndShow call and read back by visibleContentZone() — parity by construction rather than a duplicated computation"
  - "switcherHeight (visibleContentZone's local let) is scaled once at its own declaration site rather than at each of its 7 usage sites"
  - "quickActionPickerFrame/onboardingFrame/wings (Site 2) and their Site 3 equivalents (.quickActionPicker/isOnboardingActive branches) deliberately left unscaled, matching the plan's Constant Scale Inventory out-of-scope list"

patterns-established: []

requirements-completed: [D-01, D-02, D-04, D-05, D-06, D-07, D-08]

# Metrics
duration: 25min
completed: 2026-07-26
---

# Phase 67.1 Plan 03: Site 2/3 Scale-Aware Panel Reservation & Click-Through Summary

**NotchWindowController's AppKit panel-frame reservation (positionAndShow) now reserves the fixed 150% ceiling while its click-through hit-test (visibleContentZone) mirrors Site 1's live resolved scale exactly, closing the three-site parity gap by construction.**

## Performance

- **Duration:** 25 min
- **Started:** 2026-07-26T10:18:00Z
- **Completed:** 2026-07-26T10:43:20Z
- **Tasks:** 2 completed
- **Files modified:** 1

## Accomplishments
- `positionAndShow()` resolves `currentWidthScale`/`currentDepthScale` once per call using the exact same 2 pure functions (`islandAutoScaleFactor`/`resolvedIslandScale`) Plan 01 unit-tested, reading the same `UserDefaults` offset keys and the same screen-width input Site 1 already uses.
- `positionAndShow()`'s panel-frame union (`expandedFrame`/`trayFrame`/`weatherExpandedFrame`/`outputPanelExpandedFrame`/`timerSetupFrame`) reserves every in-scope constant at a fixed `maxAxisScale = 1.5` (D-04 ceiling) — never the live scale — so a live Settings slider drag never needs a mid-interaction window resize.
- `visibleContentZone()`'s every in-scope branch (`.trayExpanded`, `.quickActionsBarExpanded`, `.weatherExpanded`, `.calendarExpanded`, `.timerSetup`, `.timerExpanded`, final `else`) multiplies by the live `currentWidthScale`/`currentDepthScale`, matching what Site 1 is actually rendering.
- `wings`, `onboardingFrame`, `quickActionPickerFrame` (Site 2) and their Site 3 equivalents (`isOnboardingActive`, `.quickActionPicker`) stay completely unscaled, per the plan's Constant Scale Inventory.

## Task Commits

Each task was committed atomically:

1. **Task 1: Site 2 — positionAndShow's panel-frame reservation at the fixed 150% ceiling** - `e0a1e57` (feat)
2. **Task 2: Site 3 — visibleContentZone mirrors Site 1's live scale exactly** - `92ffed2` (feat)

**Plan metadata:** (this commit, follows)

## Files Created/Modified
- `Islet/Notch/NotchWindowController.swift` - added `currentWidthScale`/`currentDepthScale` stored properties; scaled `positionAndShow`'s panel-frame union to the fixed 150% ceiling; scaled `visibleContentZone`'s live click-through hit-test to the current live scale

## Decisions Made
- `currentWidthScale`/`currentDepthScale` declared right after the existing `hotZone` stored property (grouped with the controller's other geometry-adjacent state), not beside `debugLastCollapsedFrame` — kept the `#if DEBUG` block undisturbed.
- `switcherHeight` scaled once at its declaration site inside `visibleContentZone()`, avoiding 7 repeated multiplications at each usage.

## Deviations from Plan

None - plan executed exactly as written. Both tasks' acceptance criteria (grep counts, wings/onboarding-untouched checks, picker-height-never-scaled check) verified via automated grep and passed on first attempt.

## Issues Encountered

Full `xcodebuild test -scheme Islet` run surfaced 2 pre-existing test failures (`SettingsViewTests.testProductivityCardsAllNew`, `SettingsViewTests.testSystemHUDCardsExistingBeforeNew`) — already documented in `deferred-items.md` from Plan 02 as unrelated to notch geometry (they assert on `SettingsView`'s card metadata from the Phase 59 Settings-Redesign feature, no relationship to `NotchWindowController.swift`/`NotchGeometry.swift`/`ActivitySettings.swift`'s scale keys). Not auto-fixed per the scope-boundary rule; no new deferred-items.md entry needed since the same 2 failures were already logged. 567/569 tests pass, 0 regressions caused by this plan.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Site 2 and Site 3 are now scale-aware and match Plan 02's Site 1 exactly, closing VALIDATION.md's Wave 0 three-site-parity gap by construction. Plan 04/05 (manual-offset Settings UI and on-device UAT) can proceed — the visual/click-through consequence of this parity is confirmed on-device in Plan 05 per this plan's own threat-model note (XCTest cannot render pixels or simulate real click-through).

---
*Phase: 67.1-notch-size-scaling-resolution-aware-manual-override*
*Completed: 2026-07-26*
