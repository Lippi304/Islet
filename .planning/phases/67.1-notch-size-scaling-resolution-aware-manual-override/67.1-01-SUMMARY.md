---
phase: 67.1-notch-size-scaling-resolution-aware-manual-override
plan: 01
subsystem: ui
tags: [swift, geometry, appstorage, tdd, notch-scaling]

# Dependency graph
requires: []
provides:
  - "islandAutoScaleFactor(screenWidthPoints:baseline:) — pure D-06 resolution-aware auto-scale function"
  - "resolvedIslandScale(auto:manualOffset:range:) — pure D-04/D-07/D-08 additive clamp-resolver function"
  - "islandWidthScaleOffsetKey / islandDepthScaleOffsetKey — @AppStorage key constants for the manual offset sliders"
affects: [67.1-02, 67.1-03, 67.1-04]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pure, unit-tested geometry functions in NotchGeometry.swift (no live NSScreen), following the existing hasNotch/notchSize/topEdgeCutoutGap contract"
    - "Offset-not-absolute @AppStorage key convention: manual scale keys store a delta from the live auto value, never an absolute scale"

key-files:
  created: []
  modified:
    - "Islet/Notch/NotchGeometry.swift"
    - "IsletTests/NotchGeometryTests.swift"
    - "Islet/ActivitySettings.swift"

key-decisions:
  - "islandAutoScaleFactor guards non-positive baseline/screenWidthPoints and returns identity 1.0 rather than dividing by zero, mirroring the file's existing guard-return-safe-default convention"
  - "resolvedIslandScale is additive (auto + manualOffset) not multiplicative, per D-07/D-08, with a single shared 0.8...1.5 clamp (D-04) that every downstream consumer (Plans 02-04) routes through"

patterns-established:
  - "Manual-offset AppStorage keys store deltas from a computed auto value, documented explicitly in the doc comment to prevent a future call site treating them as absolute scales"

requirements-completed: [D-04, D-06, D-07, D-08]

# Metrics
duration: 5min
completed: 2026-07-26
---

# Phase 67.1 Plan 01: Pure Scale-Factor Functions & Manual-Offset Keys Summary

**Two pure, fully unit-tested geometry functions (resolution-aware auto-scale + additive manual-offset resolver) plus two new AppStorage key constants — the shared contract all three geometry call sites in Plans 02-04 will use for three-site parity.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-07-26T10:04:06Z
- **Completed:** 2026-07-26T10:06:53Z
- **Tasks:** 2 completed
- **Files modified:** 3

## Accomplishments
- `islandAutoScaleFactor(screenWidthPoints:baseline:)` added to NotchGeometry.swift — 1470pt baseline reads as 1.0 (D-06), driven by scaled-resolution points, not physical diagonal (D-05)
- `resolvedIslandScale(auto:manualOffset:range:)` added — additive (not multiplicative) combination clamped to 0.8...1.5 (D-04/D-07/D-08), the single shared defense point against out-of-range values
- 5 new unit tests added and green, zero regressions across the file's 19 pre-existing tests (24/24 total)
- `islandWidthScaleOffsetKey` / `islandDepthScaleOffsetKey` added to ActivitySettings.swift, correctly excluded from the Bool-only `defaultsToFalseKeys` set

## Task Commits

Each task was committed atomically (TDD RED/GREEN for Task 1):

1. **Task 1 RED: failing tests for islandAutoScaleFactor/resolvedIslandScale** - `c25a876` (test)
2. **Task 1 GREEN: implement islandAutoScaleFactor and resolvedIslandScale** - `e10dcc9` (feat)
3. **Task 2: manual-offset AppStorage key constants** - `0c443b0` (feat)

## Files Created/Modified
- `Islet/Notch/NotchGeometry.swift` - added `islandAutoScaleFactor` and `resolvedIslandScale` pure functions
- `IsletTests/NotchGeometryTests.swift` - added 5 unit tests covering baseline, reported resolution, both clamp extremes, and additive-not-multiplicative combination
- `Islet/ActivitySettings.swift` - added `islandWidthScaleOffsetKey`/`islandDepthScaleOffsetKey` constants

## Decisions Made
None beyond what the plan specified — plan executed exactly as written.

## Deviations from Plan

None - plan executed exactly as written.

## TDD Gate Compliance

Task 1 (`tdd="true"`) followed the full RED/GREEN cycle:
- RED gate: `test(67.1-01): add failing tests...` (`c25a876`) — confirmed compile failure before implementation
- GREEN gate: `feat(67.1-01): implement islandAutoScaleFactor...` (`e10dcc9`) — confirmed all 5 new tests pass, no regressions
- No REFACTOR needed (functions already minimal, per plan)

## Verification

- `xcodebuild test -scheme Islet -only-testing:IsletTests/NotchGeometryTests -destination 'platform=macOS'` — exit 0, 24/24 tests green
- `xcodebuild build -scheme Islet -destination 'platform=macOS'` — BUILD SUCCEEDED
- `grep -c "func islandAutoScaleFactor" Islet/Notch/NotchGeometry.swift` == 1
- `grep -c "func resolvedIslandScale" Islet/Notch/NotchGeometry.swift` == 1
- `grep -c 'islandWidthScaleOffsetKey = "island.widthScaleOffset"' Islet/ActivitySettings.swift` == 1
- `grep -c 'islandDepthScaleOffsetKey = "island.depthScaleOffset"' Islet/ActivitySettings.swift` == 1
- Confirmed by direct file read (not the plan's ambiguous grep pattern) that the actual `defaultsToFalseKeys` set definition (lines 64-67) does NOT include either new key — the plan's own acceptance-criteria grep pattern (`grep -A2 "defaultsToFalseKeys"`) produces a false-positive match against this plan's own doc-comment text mentioning the set's name; verified against the real set literal instead.

## Known Stubs

None — both functions are fully implemented and unit-tested; the AppStorage keys are intentionally unconsumed until Plans 02-04 wire them (per plan's own success criteria).

## Threat Flags

None — this plan's only new surface (the two AppStorage keys) is explicitly covered by the plan's own threat model (T-67.1-01), mitigated by `resolvedIslandScale`'s shared clamp.

## Self-Check: PASSED

All created/modified files found on disk, all 3 task commit hashes found in git log.
