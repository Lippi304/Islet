---
phase: 67.1-notch-size-scaling-resolution-aware-manual-override
plan: 10
type: execute
wave: 8
depends_on: ["67.1-08", "67.1-09"]
files_modified: [Islet/Notch/NotchPillView.swift]
autonomous: true
requirements: [D-14]

must_haves:
  truths:
    - "At the user's originally-reported problem resolution (1710x1112), the Music-Play wing (mediaWingsRow) no longer clips the album art or the 5th equalizer bar against the real camera cutout — the original trigger for this entire amendment"
    - "The Music-Play wing's camera clearance is computed from the live interaction.collapsedNotchSize (like osdWings/capsLockWings), never from a flexible Spacer() alone"
    - "The Width/Depth sliders visibly grow/shrink the Music-Play wing's leading/trailing padding, at both the 80% floor and 150% ceiling, without ever shrinking the camera-block clearance itself"
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "mediaWingsOrToast/mediaWingsRow adopt an explicit interaction.collapsedNotchSize-derived camera-block spacer plus D-14 wing-scale padding, replacing the old fixed-Self.wingsSize.width + flexible-Spacer() approach"
      contains: "mediaWingContentWidth"
  key_links:
    - from: "mediaWingsOrToast"
      to: "mediaWingContentWidth()"
      via: "direct call replacing the fixed Self.wingsSize.width frame arguments"
      pattern: "mediaWingContentWidth\\(\\)"
---

<objective>
Fix the ORIGINAL bug that triggered this entire amendment (67.1-CONTEXT.md's second amendment, paragraph 2): the Music-Play wing (`mediaWingsRow`, wrapped by `mediaWingsOrToast`) never adopted the live-hardware camera-clearance mechanism the other 9 wings use — it still sizes itself from a fixed `Self.wingsSize.width` (290pt) constant with a plain, non-measuring `Spacer()` for camera clearance. At the user's non-baseline resolution (1710x1112) the real camera cutout exceeds what that fixed 290pt/Spacer() combination naturally reserves, clipping the album art and the 5th equalizer bar.

Unlike Plans 08/09's wings, `mediaWingsOrToast` does NOT route through the shared `wingsShape` helper (its bottom corner radius varies with the song-change toast, per its own header comment) — it builds its own `NotchShape` and frame directly, so this plan gives it its OWN small camera-block calculation (`mediaWingContentWidth()`, mirroring osdWings' proven `rawNotchHalfWidth + margin` pattern) rather than reusing `wingsShape`. This single fix simultaneously resolves the original clipping bug (camera clearance is now hardware-derived, not `Spacer()`-guessed) AND wires in the D-14 auto+manual wing scale (leading/trailing padding grows/shrinks with the Width/Depth sliders, exactly like Plan 09's six wings).

Purpose: Close the specific gap CONTEXT.md's second amendment names as its trigger, using the same camera-safety-first mechanism the rest of this phase now establishes.
Output: mediaWingsRow never clips at any tested resolution; the Music-Play wing responds to the Width/Depth sliders like every other named wing in this phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-CONTEXT.md
@.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-08-SUMMARY.md
@.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-09-SUMMARY.md
</context>

<interfaces>
<!-- resolvedWingWidthScale/resolvedWingDepthScale already exist (Plan 08). This plan adds one
     new private helper, mirroring osdWings' proven camera-block pattern but NOT going through
     wingsShape (mediaWingsOrToast builds its own shape/frame directly). -->

Current state, NotchPillView.swift:3068-3112 (to be replaced):
```swift
@ViewBuilder
private func mediaWingsOrToast(_ p: NowPlayingPresentation) -> some View {
    let toast = nowPlaying.songChangeToast
    let height = Self.wingsSize.height + (toast != nil ? Self.toastExtraHeight : 0)
    let shape = NotchShape(topCornerRadius: 6, bottomCornerRadius: toast != nil ? 16 : 6)
    shape
        .fill(islandFill)
        .matchedGeometryEffect(id: "island", in: ns)
        .frame(width: Self.wingsSize.width, height: height)
        .overlay(liquidGlassEffectLayer(shape: shape, size: CGSize(width: Self.wingsSize.width, height: height), parameters: .expanded))
        .overlay(alignment: .top) {
            VStack(spacing: 0) {
                mediaWingsRow(p, art: nowPlaying.artwork)
                if let toast { toastTextRow(toast).transition(.opacity) }
            }
        }
        .onTapGesture { onClick() }
}

private func mediaWingsRow(_ presentation: NowPlayingPresentation, art: NSImage?) -> some View {
    let isPlaying = isPlayingFor(presentation)
    return HStack(spacing: 0) {
        artThumbnail(art, side: Self.wingsSize.height - 8, corner: 6).padding(.leading, 22)
        Spacer()
        EqualizerBars(isPlaying: isPlaying).padding(.trailing, 24)
    }
    .frame(width: Self.wingsSize.width, height: Self.wingsSize.height)
}
```

EqualizerBars' real intrinsic width (NotchPillView.swift:4489,4523 — read-only reference, do not change): 5 bars * 1pt width + 4 gaps * 4pt spacing = 21pt.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Give mediaWingsOrToast/mediaWingsRow a live-hardware camera block + D-14 wing scale</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift:3068-3112 (mediaWingsOrToast + mediaWingsRow, current implementation — shown in full in this plan's `<interfaces>` block above)
    Islet/Notch/NotchPillView.swift:3429-3455 (osdWings' cameraBlockWidth/leftWidth/rightWidth derivation — the pattern to mirror, NOT the wingsShape call itself since mediaWingsOrToast doesn't use wingsShape)
    Islet/Notch/NotchPillView.swift:4486-4525 (EqualizerBars — confirms its fixed 21pt intrinsic width: 5 * barWidth(1) + 4 * spacing(4))
    .planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-CONTEXT.md lines 49-58, 97 (the original bug report and D-14's camera-safety requirement)
  </read_first>
  <action>
    Add a new private helper `mediaWingContentWidth() -> (left: CGFloat, right: CGFloat)` placed directly above `mediaWingsOrToast`. It computes, mirroring osdWings' proven pattern: `rawNotchHalfWidth` from `(interaction.collapsedNotchSize?.width ?? Self.collapsedSize.width) / 2`; a fixed `margin: CGFloat = 55` (osdWings' own on-device-confirmed value — the closest existing content class: album art + equalizer bars, not long text); `cameraBlockWidth = (rawNotchHalfWidth + margin) * 2` (never scaled by anything); `leadingPad = 22 * resolvedWingWidthScale` and `trailingPad = 24 * resolvedWingWidthScale` (the wing's existing 22/24 paddings, now scaled per D-14); `artSide = (Self.wingsSize.height - 8) * resolvedWingDepthScale` (the album art's square side, scaled with depth since — unlike the width axis — depth has no camera-safety floor to protect, so scaling content directly here is both simpler and safer against vertical overflow); a fixed `eqBarsWidth: CGFloat = 21` (EqualizerBars' own intrinsic width, per this task's read_first — content stays fixed size on the width axis, matching the rest of this phase's convention). Return `left = leadingPad + artSide + cameraBlockWidth / 2` and `right = (leadingPad + artSide + cameraBlockWidth + eqBarsWidth + trailingPad) - left`.

    Rewrite `mediaWingsOrToast` to call this helper once (`let (leftWidth, rightWidth) = mediaWingContentWidth()`), compute `width = leftWidth + rightWidth` (replacing the old fixed `Self.wingsSize.width` in both the `.frame(...)` call and the `liquidGlassEffectLayer` size argument), and compute `height = Self.wingsSize.height * resolvedWingDepthScale + (toast != nil ? Self.toastExtraHeight : 0)` (replacing the old unscaled height). Add `.alignmentGuide(HorizontalAlignment.center) { _ in leftWidth }` to the modifier chain (mirroring `wingsShape`'s own alignment mechanism, lines 2991) so the wing's true center — not its geometric width/2 — pins to the notch's real center now that `leftWidth`/`rightWidth` can be asymmetric. Keep the existing `.animation(nil, value: size)`-equivalent guard: add `.animation(nil, value: CGSize(width: width, height: height))` immediately after the `.frame(...)` call, mirroring D-10/Plan 06's fix for `collapsedIsland`/`wingsShape` — this camera-derived size must never visibly lag on a live resolution switch either.

    Update `mediaWingsRow` to accept the computed `leftWidth`/`rightWidth` as new parameters (from `mediaWingsOrToast`, which already has them) instead of recomputing anything, and rebuild its `HStack` as a SEQUENTIAL layout with an EXPLICIT `Color.clear.frame(width: cameraBlockWidth)` element between the art and the equalizer bars — replacing the old flexible `Spacer()` — mirroring osdWings'/capsLockWings' own "not a flexible Spacer()" convention (their own code comments explain why: a `Spacer()` only correlates with where the camera roughly is, an explicit fixed-width element mechanically cannot be skipped or shrunk). Since `mediaWingContentWidth()` is now the single source of `cameraBlockWidth`, either pass it through as a third parameter alongside `leftWidth`/`rightWidth`, or recompute it by calling `mediaWingContentWidth()` a second time inside `mediaWingsRow` (same live inputs, same result) — planner's/executor's call, whichever keeps the diff smaller; do not hardcode the value in two places.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "private func mediaWingContentWidth" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "Self.wingsSize.width" Islet/Notch/NotchPillView.swift | true; grep -n "private func mediaWingsOrToast" -A30 Islet/Notch/NotchPillView.swift | grep -c "Self.wingsSize.width"` returns 0 (the old fixed-width frame arguments are gone from mediaWingsOrToast)
    - `sed -n '/private func mediaWingsRow/,/^    }/p' Islet/Notch/NotchPillView.swift | grep -c "Spacer()"` returns 0 (the flexible Spacer() is fully replaced)
    - `sed -n '/private func mediaWingsRow/,/^    }/p' Islet/Notch/NotchPillView.swift | grep -c "Color.clear.frame(width: cameraBlockWidth)"` returns 1
    - `sed -n '/private func mediaWingContentWidth/,/^    }/p' Islet/Notch/NotchPillView.swift | grep -c "cameraBlockWidth \* \|margin \* "` returns 0 (camera block/margin never scaled)
    - `xcodebuild build -scheme Islet -destination 'platform=macOS' && xcodebuild test -scheme Islet -destination 'platform=macOS'` exits 0, full suite green modulo the 2 pre-existing SettingsViewTests + 4 pre-existing LicenseStateTests failures already logged in deferred-items.md
  </acceptance_criteria>
  <done>mediaWingsOrToast/mediaWingsRow compute their camera clearance from live hardware (never a flexible Spacer()), scale their leading/trailing padding and height with the D-14 wing scale, and never shrink the camera-block clearance itself at any slider position.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manual @AppStorage offset -> Music-Play wing padding (real hardware camera floor) | Mirrors T-67.1-12 (Plan 09) for the one wing that previously had NO hardware-derived floor at all — this plan adds one where none existed before |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67.1-14 | Tampering (manual offset shrinking the Music-Play wing's real camera clearance) | mediaWingContentWidth() | mitigate | cameraBlockWidth/margin are never multiplied by any wing-scale term, grep-verified zero occurrences; this is a strictly stronger guarantee than the pre-phase Spacer()-based approach, which had no hardware-derived floor at all |
</threat_model>

<verification>
Full build + full test suite green. Grep-verified: mediaWingsRow's flexible Spacer() is fully replaced by an explicit hardware-derived camera-block element; camera-block/margin are never scaled.
</verification>

<success_criteria>
- mediaWingsOrToast/mediaWingsRow derive camera clearance from interaction.collapsedNotchSize, not a flexible Spacer().
- Leading/trailing padding and height scale with the Width/Depth sliders (D-14).
- Camera-block width is never multiplied by the wing scale, at any slider position.
- Full build + test suite green.
</success_criteria>

<output>
Create `.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-10-SUMMARY.md` when done
</output>
