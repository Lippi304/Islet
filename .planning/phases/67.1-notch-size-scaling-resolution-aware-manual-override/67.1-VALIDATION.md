---
phase: 67.1
slug: notch-size-scaling-resolution-aware-manual-override
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-26
---

# Phase 67.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | XCTest (existing `IsletTests` target, confirmed via `xcodebuild -list`) |
| **Config file** | Xcode project scheme "Islet" — no separate test-plan file found |
| **Quick run command** | `xcodebuild test -scheme Islet -only-testing:IsletTests/NotchGeometryTests -destination 'platform=macOS'` |
| **Full suite command** | `xcodebuild test -scheme Islet -destination 'platform=macOS'` |
| **Estimated runtime** | ~30-60 seconds (quick), ~3-5 minutes (full) |

---

## Sampling Rate

- **After every task commit:** Run `xcodebuild test -scheme Islet -only-testing:IsletTests/NotchGeometryTests -destination 'platform=macOS'`
- **After every plan wave:** Run `xcodebuild test -scheme Islet -destination 'platform=macOS'`
- **Before `/gsd:verify-work`:** Full suite must be green, PLUS an on-device UAT pass (established project convention for every geometry-affecting phase — see Phase 29/39/48/62/63/65 UAT history in STATE.md) since XCTest cannot visually confirm rendered pixel size or click-through correctness.
- **Max feedback latency:** ~60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Decision | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|----------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | D-06 | `islandAutoScaleFactor(screenWidthPoints: 1710)` ≈ 1.1633 | unit | `xcodebuild test -scheme Islet -only-testing:IsletTests/NotchGeometryTests -destination 'platform=macOS'` | ❌ Wave 0 | ⬜ pending |
| TBD | TBD | TBD | D-06 | `islandAutoScaleFactor(screenWidthPoints: 1470)` == 1.0 (baseline identity) | unit | same as above | ❌ Wave 0 | ⬜ pending |
| TBD | TBD | TBD | D-04 | `resolvedIslandScale(auto: 2.0, manualOffset: 0)` clamps to 1.5, not 2.0 | unit | same as above | ❌ Wave 0 | ⬜ pending |
| TBD | TBD | TBD | D-04 | `resolvedIslandScale(auto: 0.1, manualOffset: 0)` clamps to 0.8, not 0.1 | unit | same as above | ❌ Wave 0 | ⬜ pending |
| TBD | TBD | TBD | D-07/D-08 | `resolvedIslandScale(auto: 1.1633, manualOffset: 0.05)` == 1.2133 (additive, not multiplicative) | unit | same as above | ❌ Wave 0 | ⬜ pending |
| TBD | TBD | TBD | D-01/D-02 | Site 1 (content), Site 2 (panel-frame), Site 3 (click-through rect) all compute the SAME scaled dimension for a given (screen, offsets) input | unit or manual UAT | no existing automated cross-site-parity test — see Wave 0 gap | ❌ Wave 0 | ⬜ pending |

*Task/Plan/Wave IDs are TBD until the planner assigns them — this table anchors to CONTEXT.md decisions (D-0x) rather than REQ-IDs since none are mapped to this phase yet.*

---

## Wave 0 Requirements

- [ ] `IsletTests/NotchGeometryTests.swift` — add cases for `islandAutoScaleFactor`/`resolvedIslandScale` (framework and file already exist, only new test functions needed)
- [ ] Decide whether a new three-site parity unit test is worth adding, or whether on-device UAT is the sole check for that cross-cutting property (the geometry math itself is otherwise fully unit-testable in isolation) — planner's call, per RESEARCH.md Wave 0 Gaps

---

## Manual-Only Verifications

| Behavior | Decision | Why Manual | Test Instructions |
|----------|----------|------------|-------------------|
| Rendered pixel size and click-through hit-test correctness across the three geometry sites | D-01/D-02 | XCTest cannot visually confirm rendered pixel size or click-through correctness — this project's established convention for every geometry-affecting phase | On-device UAT: verify Home/Tray/Weather/Calendar/Quick Actions/Switcher render at the expected scaled size and remain clickable at both the 0.8 and 1.5 clamp extremes, on at least one auto-scaled (e.g. MacBook Pro 16") and one baseline (e.g. MacBook Air 13") display |
| Settings sliders live-update the island without app restart | D-03 (Settings surface) | Requires observing the actual rendered panel resize in real time | On-device UAT: drag width/depth sliders in Settings, confirm the notch island resizes immediately with no debounce lag or flicker |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
