---
phase: 67.1-notch-size-scaling-resolution-aware-manual-override
plan: 08
subsystem: ui
tags: [swiftui, notch-scaling, live-activities, appstorage]

requires:
  - phase: 67.1-07
    provides: "Freed islandWidthScaleOffset/islandDepthScaleOffset @AppStorage offsets (expanded content is now auto-only; sliders were unread until this plan)"
provides:
  - "resolvedWingWidthScale/resolvedWingDepthScale computed properties (D-12/D-14 auto+manual mechanism, retargeted to wing content)"
  - "wingsShape(depthScale:) parameter, default 1.0, applied to the shape's own frame height"
  - "Charging/Focus/Device/Countdown collapsed wings grow with the Width/Depth sliders, floored at their pre-phase fixed size (never shrink)"
affects: ["67.1-09 (six hardware-camera-block wings)", "67.1-10 (bespoke media wing)"]

tech-stack:
  added: []
  patterns: ["wing-scale floor: max(1.0, resolvedWingWidthScale) applied at the call site, never inside wingsShape itself, so width-shrink protection is per-caller and explicit"]

key-files:
  created: []
  modified:
    - "Islet/Notch/NotchPillView.swift"

key-decisions:
  - "resolvedWingWidthScale/resolvedWingDepthScale are byte-identical in shape to the pre-Plan-07 resolvedWidthScale/resolvedDepthScale (same resolvedIslandScale call), only the @AppStorage key target and now the consuming call sites changed — no new math introduced."
  - "Width floor applied via max(1.0, resolvedWingWidthScale) at each of the 4 call sites (not clamped inside wingsShape) per the plan's explicit instruction — depth has no floor since height doesn't affect camera clearance."

requirements-completed: [D-12, D-14]

duration: 73min
completed: 2026-07-26
---

# Phase 67.1 Plan 08: Wing Scale Contract + First Batch (Charging/Focus/Device/Countdown) Summary

**Retargeted the D-14 auto+manual Width/Depth scale mechanism from expanded content onto four Spacer()-based collapsed wings, with a hard width-growth-only floor for camera safety.**

## Performance

- **Duration:** 73 min
- **Started:** 2026-07-26T20:20:19+02:00
- **Completed:** 2026-07-26T21:33:52+02:00
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- `resolvedWingWidthScale`/`resolvedWingDepthScale` computed properties added, reading the same `islandWidthScaleOffset`/`islandDepthScaleOffset` `@AppStorage` keys Plan 07 freed from expanded-content duty
- `wingsShape` threads a new `depthScale: CGFloat = 1.0` parameter through to its own frame height, backward-compatible for all 10 existing call sites via the default
- `wings(for:)` (Charging), `focusWings`, `deviceWings`, `countdownWings` now grow their `leftWidth`/`rightWidth` by `max(1.0, resolvedWingWidthScale)` (never below their pre-phase fixed size) and pass `depthScale: resolvedWingDepthScale`

## Task Commits

Each task was committed atomically:

1. **Task 1: Define resolvedWingWidthScale/resolvedWingDepthScale + thread depthScale through wingsShape** - `e7a18fc` (feat)
2. **Task 2: Apply wing scale to Charging/Focus/Device/Countdown (Spacer()-based wings)** - `d5d4388` (feat)

**Plan metadata:** (this commit, see below)

## Files Created/Modified
- `Islet/Notch/NotchPillView.swift` - Added `resolvedWingWidthScale`/`resolvedWingDepthScale`; `wingsShape` gained `depthScale:`; `wings(for:)`/`focusWings`/`deviceWings`/`countdownWings` each grow via `widthGrowth` and pass `depthScale`

## Decisions Made
None beyond what's captured in `key-decisions` above — plan executed as specified.

## Deviations from Plan

None - plan executed exactly as written. The two `let widthGrowth = ...` insertions in `wings(for:)` and `deviceWings` required an explicit `return` in front of `wingsShape(...)` (Swift's implicit-return single-expression-body rule no longer applies once a preceding `let` statement is added) — this is a direct, uneventful consequence of the plan's own instructed edit, not a deviation.

## Issues Encountered

**Task 2's acceptance-criteria grep pattern (`grep -A3 "private func wings..." | grep -c widthGrowth`) returned 0 for all four functions**, even though `widthGrowth` is correctly present in each. Root cause: the grep-A3 window is too narrow — `wings(for:)`/`deviceWings` have a `switch` block between the function signature and the `let widthGrowth` line, pushing it past the 3-line window. Verified correctness instead with a wider context (`grep -A20`), which found `widthGrowth` in all 4 functions (3/3/3/2 lines respectively). This is an imprecision in the plan's own acceptance-criteria grep, not a code defect — build and full test suite both confirm the wiring is correct and complete.

**Full test suite showed 6 failures** (`LicenseStateTests` x4, `SettingsViewTests` x2). Investigated per coordinator instruction: these are the exact same 6 pre-existing failures already documented in this phase's `deferred-items.md` (confirmed unrelated to `NotchPillView.swift` via a `git stash` + re-run comparison during Plan 06's Task 3, and explicitly listed as the allowed exception in this plan's own Task 2 acceptance criteria: "full regression suite green modulo the 2 pre-existing SettingsViewTests + 4 pre-existing LicenseStateTests failures already logged in deferred-items.md"). No fix needed or applied — confirmed pre-existing/unrelated, not a regression from this plan's changes.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- The `resolvedWingWidthScale`/`resolvedWingDepthScale` contract and `wingsShape(depthScale:)` parameter are established and proven on 4 of ~10 wing functions — Plan 09 can now apply the same pattern to the six hardware-camera-block wings (osdWings/capsLockWings/etc.), and Plan 10 to the bespoke media wing.
- No blockers.

---
*Phase: 67.1-notch-size-scaling-resolution-aware-manual-override*
*Completed: 2026-07-26*

## Self-Check: PASSED
