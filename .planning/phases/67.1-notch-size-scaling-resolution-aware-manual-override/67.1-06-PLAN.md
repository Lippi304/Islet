---
phase: 67.1
plan: 06
type: execute
wave: 3
depends_on: ["67.1-02", "67.1-03"]
files_modified: [Islet/Notch/NotchWindowController.swift, Islet/Notch/NotchPillView.swift]
autonomous: false
gap_closure: true
requirements: [D-09, D-10]

must_haves:
  truths:
    - "Immediately after a live scaled-resolution switch via System Settings -> Displays (no app relaunch), the collapsed pill's depth (height) visually matches the real physical camera cutout on the very first glance"
    - "The fix holds symmetrically in both directions: baseline (1470x956) -> problem resolution (1710x1112) AND back again"
    - "The collapsed pill's size is byte-identical regardless of the D-01 auto-scale value or the D-03/D-04 manual Width/Depth slider positions (D-09 reaffirmed, non-regression)"
    - "No blind/unverified timing fix was added — whatever fix ships is justified by an actual on-device trace of the real NSScreen values across a live switch, not a guess (D-10, mirrors the disproven Phase 36 'settle re-poll' precedent)"
  artifacts:
    - path: "Islet/Notch/NotchWindowController.swift"
      provides: "Evidence-based fix for the collapsed-pill live re-measurement bug (exact mechanism determined by Task 2's real trace, not assumed)"
      contains: "positionAndShow"
  key_links:
    - from: "NSApplication.didChangeScreenParametersNotification observer"
      to: "interaction.collapsedNotchSize"
      via: "updateVisibility() -> positionAndShow(on:) -> notchSize(...)"
      pattern: "collapsedNotchSize = notchSize"
---

<objective>
Fix the collapsed-pill live re-measurement bug found during Plan 05's on-device UAT (D-09/D-10): immediately after a live scaled-resolution switch (System Settings -> Displays, no relaunch), the collapsed pill's depth no longer matches the real physical camera cutout. D-02 was and remains correct that the collapsed pill needs no D-01 scale factor — this is a pure correctness fix to the EXISTING live-measurement path (notification -> updateVisibility -> positionAndShow -> notchSize), not new scaling scope.

Per D-10's explicit framing and this codebase's own Phase-36 precedent (a guessed "settle re-poll" fix was later proven unnecessary and removed — see NotchWindowController.swift:2419-2426), this plan diagnoses with a real on-device trace BEFORE proposing a fix, rather than assuming which layer is stale.

Purpose: Close the one open gap from Plan 05's UAT so Phase 67.1 can resume and finish its on-device checkpoint.
Output: Root-cause-diagnosed fix, temporary diagnostics removed, full test suite green, on-device confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-CONTEXT.md
@.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-03-SUMMARY.md
</context>

<interfaces>
<!-- Existing live-measurement path this plan diagnoses and fixes. No new types. -->

From Islet/Notch/NSScreen+Notch.swift (unchanged, read-only reference):
```swift
extension NSScreen {
    var displayID: CGDirectDisplayID? { ... }
    var isBuiltinDisplay: Bool { ... }
    var displayUUID: String? { ... }
    var descriptor: ScreenDescriptor {
        ScreenDescriptor(
            uuid: displayUUID,
            frame: frame,
            safeAreaTop: safeAreaInsets.top,
            auxLeftWidth: auxiliaryTopLeftArea?.width,
            auxRightWidth: auxiliaryTopRightArea?.width,
            isBuiltin: isBuiltinDisplay
        )
    }
}
```

From Islet/Notch/NotchGeometry.swift (unchanged, read-only reference):
```swift
func notchSize(screenWidth: CGFloat, safeAreaTop: CGFloat,
               auxLeftWidth: CGFloat?, auxRightWidth: CGFloat?,
               widthFudge: CGFloat = 4) -> CGSize?
```

From Islet/Notch/NotchInteractionState.swift (unchanged, read-only reference):
```swift
final class NotchInteractionState: ObservableObject {
    @Published var collapsedNotchSize: CGSize?   // nil = fall back to static 200x38 seed
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add temporary diagnostic trace instrumentation</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift:595-611 (the didChangeScreenParametersNotification observer)
    Islet/Notch/NotchWindowController.swift:1269-1325 (updateVisibility())
    Islet/Notch/NotchWindowController.swift:1327-1360 (positionAndShow(on:))
    Islet/Notch/NSScreen+Notch.swift (the ScreenDescriptor bridge — safeAreaTop/auxLeftWidth/auxRightWidth sources)
    .planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-CONTEXT.md lines 70-99 (D-09/D-10 amendment, exact file/line pointers, and the explicit "trace before fix" instruction)
  </read_first>
  <action>
    Add temporary `#if DEBUG` trace logging (tag "[D10-TRACE]") at exactly 3 points, per CONTEXT.md's
    explicit instruction to log `target.safeAreaTop`/`auxLeftWidth`/`auxRightWidth` immediately in the
    notification handler vs. a few run-loop ticks later, before proposing any fix:

    1. In the `didChangeScreenParametersNotification` observer closure (NotchWindowController.swift:603-611):
       immediately when the notification fires, BEFORE the existing `DispatchQueue.main.async` hop, build
       `NSScreen.screens.map { $0.descriptor }` and print each descriptor's `uuid`, `isBuiltin`, `safeAreaTop`,
       `auxLeftWidth`, `auxRightWidth` with tag "[D10-TRACE][notify-immediate]" plus `Date()`. Then, inside the
       existing `DispatchQueue.main.async { ... }` block, immediately before the call to `self?.updateVisibility()`,
       repeat the same descriptor dump with tag "[D10-TRACE][notify-afterHop]".
    2. In `positionAndShow(on:)`, immediately after the `notchSize(...)` call that sets
       `interaction.collapsedNotchSize` (NotchWindowController.swift:1355-1359), print `target.safeAreaTop`,
       `target.auxLeftWidth`, `target.auxRightWidth`, and the resulting `interaction.collapsedNotchSize` with
       tag "[D10-TRACE][positionAndShow]" plus `Date()`.

    Wrap all three additions in `#if DEBUG ... #endif` (mirrors the existing `debugLastCollapsedFrame` pattern
    at NotchWindowController.swift:1336-1338). These are TEMPORARY — Task 3 removes them once the fix is
    confirmed, leaving only a short root-cause comment (mirrors PowerActivity.swift's settle-repoll postmortem
    comment convention at NotchWindowController.swift:2419-2426).
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -configuration Debug -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\[D10-TRACE\]' Islet/Notch/NotchWindowController.swift` returns 3 (notify-immediate, notify-afterHop, positionAndShow)
    - Every new print statement sits inside an `#if DEBUG` guard (grep confirms `#if DEBUG` appears within 5 lines above each new print)
    - Debug build exits 0
  </acceptance_criteria>
  <done>Debug build launches with the 3 new "[D10-TRACE]" log points wired at the exact call sites CONTEXT.md flagged, ready to capture a real live-switch trace in Task 2.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Capture a live-switch diagnostic trace (no CLI/API can trigger a live scaled-resolution switch)</name>
  <files></files>
  <read_first>Task 1's 3 new "[D10-TRACE]" print sites in Islet/Notch/NotchWindowController.swift</read_first>
  <action>No code changes — wait for the user to perform a live scaled-resolution switch on real hardware (no CLI/API can drive this) and relay the captured "[D10-TRACE]" console output back verbatim, per the steps below.</action>
  <what-built>
    Task 1's "[D10-TRACE]" debug logging is wired and ready to capture the real NSScreen values across a
    live resolution change, at the exact 3 points where the bug could plausibly live.
  </what-built>
  <how-to-verify>
    1. Run the app from Xcode in Debug configuration (so the debug console is visible), or launch the Debug
       build and open Console.app filtered to process "Islet".
    2. With the display currently at your baseline resolution (1470x956, or whatever it is right now), open
       System Settings -> Displays -> your built-in display, and switch LIVE to the problem resolution
       (1710x1112) — do NOT relaunch the app.
    3. Immediately after the switch completes, look at the collapsed pill and note whether its depth
       mismatches the camera cutout right away (reproducing the bug).
    4. Copy every console/debug-log line containing "[D10-TRACE]" printed from roughly 2 seconds before to
       3 seconds after the switch — this should include at least one notify-immediate / notify-afterHop pair
       and one positionAndShow line, possibly several of each if the notification fires more than once (it
       often does during a live resolution change).
    5. Paste all captured "[D10-TRACE]" lines back verbatim, in order, with their timestamps.
  </how-to-verify>
  <acceptance_criteria>
    At least one full notify-immediate -> notify-afterHop -> positionAndShow trace triple captured spanning
    the live resolution switch, OR an explicit "no output appeared" report if logging didn't fire at all
    (itself diagnostic information for Task 3).
  </acceptance_criteria>
  <resume-signal>Paste the captured "[D10-TRACE]" log lines verbatim (with timestamps), or report that no output appeared.</resume-signal>
  <verify>
    <manual>User-pasted "[D10-TRACE]" trace lines (or explicit "no output" report) reviewed before Task 3 proceeds</manual>
  </verify>
  <done>Real on-device trace of the live resolution switch captured and relayed back, ready for Task 3's root-cause analysis.</done>
</task>

<task type="auto">
  <name>Task 3: Diagnose root cause from the trace and implement the evidence-based fix</name>
  <files>Islet/Notch/NotchWindowController.swift, Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Task 2's pasted "[D10-TRACE]" trace output — read this FIRST, before opening any source file; the fix
    branch below is entirely determined by what it shows, not by assumption
    Islet/Notch/NotchWindowController.swift:2405-2426 (Phase 36's DISPROVEN "settle re-poll" postmortem —
    a directly on-point cautionary precedent: do not add a blind settle-retry without trace evidence backing it)
    Islet/Notch/NowPlayingMonitor.swift:129-141 (this codebase's established "settled flag + bounded async
    fallback" idiom — reusable IF the trace supports a genuine timing race)
    Islet/Notch/NotchPillView.swift:1253-1263 (collapsedIsland — reads `interaction.collapsedNotchSize` as a
    plain `@Published` value directly in `body`, so SwiftUI already re-renders on every publish; a render-layer
    bug here is unlikely but check anyway per Case A below)
  </read_first>
  <action>
    Compare "[D10-TRACE][notify-immediate]" against "[D10-TRACE][notify-afterHop]" and
    "[D10-TRACE][positionAndShow]" across every distinct notification firing captured in Task 2's trace, then
    implement exactly ONE of the following two evidence-justified branches (do not implement both, do not
    implement a fix the trace does not support):

    - **Case A — values are already correct on every firing, including the very first one the user sees**:
      the safeAreaTop/auxLeftWidth/auxRightWidth reaching `positionAndShow` already reflect the NEW
      resolution by the time the user notices the mismatch. This rules out NSScreen-property staleness
      entirely — do NOT add a settle-retry (Phase 36 precedent: a guessed settle-poll for a non-existent
      staleness problem was later found unnecessary and removed). Instead, the bug is in the render path
      despite the direct `@Published` read: check whether `NotchShape()` or the `.fill`/rim-mask overlay in
      `collapsedIsland` (NotchPillView.swift:1253+) applies an implicit animation/transition that visually
      delays the size change, and remove or explicitly disable that animation for `collapsedNotchSize` changes
      only (leaving other animations in the same view untouched).

    - **Case B — the FIRST firing's values reflect the OLD resolution, and only a LATER firing (or a longer
      delay) reflects the new one** (the expected AppKit-lag outcome): implement a bounded settle-until-stable
      retry in `positionAndShow`'s caller path. Add a private method
      `retryPositioningUntilStable(previousSize: CGSize?, attempt: Int = 0)` on `NotchWindowController`,
      called from inside the notification's `DispatchQueue.main.async` block immediately AFTER the existing
      `updateVisibility()` call: capture `interaction.collapsedNotchSize` right after that call as
      `previousSize`. If `attempt < 4`, schedule
      `DispatchQueue.main.asyncAfter(deadline: .now() + [0.15, 0.3, 0.6, 1.0][attempt])` (reusing this
      codebase's own established asyncAfter magnitudes from AudioOutputMonitor.swift:125 and
      DeviceCoordinator.swift:260 — no new magic numbers) to call `updateVisibility()` again and compare the
      NEW `interaction.collapsedNotchSize` against `previousSize`: if unchanged, stop (already stable); if
      `attempt == 4`, stop (give up, keep the latest value); otherwise recurse with `attempt + 1` and the new
      size as `previousSize`. Guard the whole helper so the steady-state path (no resolution change in
      flight, `previousSize` already stable on the very first post-hop measurement) schedules ZERO extra
      work — this must cost nothing when no bug is occurring.

    Whichever case the trace supports, add a SHORT code comment at the fix site documenting the actual
    diagnosed root cause (mirrors NotchWindowController.swift:2419-2426's postmortem-comment convention) so a
    future reader does not re-guess the same dead end. Then remove all 3 "[D10-TRACE]" print statements added
    in Task 1 — the diagnosis is now encoded as a comment + a working fix, not raw print spam.

    D-09 non-regression (mandatory regardless of which case applies): do NOT let this fix read
    `currentWidthScale`, `currentDepthScale`, or `resolvedIslandScale` anywhere in the
    `interaction.collapsedNotchSize` computation — the collapsed pill must never receive the D-01 auto-scale
    factor or the D-03/D-04 manual sliders.
  </action>
  <verify>
    <automated>xcodebuild test -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - Full `xcodebuild test` run passes with zero NEW failures beyond the 2 pre-existing, unrelated
      SettingsViewTests failures already documented in 67.1-03-SUMMARY.md's "Issues Encountered"
      (`testProductivityCardsAllNew`, `testSystemHUDCardsExistingBeforeNew`)
    - `grep -c '\[D10-TRACE\]' Islet/Notch/NotchWindowController.swift` returns 0 (Task 1's temporary
      diagnostics fully removed)
    - A short root-cause comment exists at the fix site
    - `grep -n 'collapsedNotchSize = notchSize' -A5 Islet/Notch/NotchWindowController.swift` shows zero
      reference to `currentWidthScale`/`currentDepthScale`/`resolvedIslandScale` in that computation (D-09
      non-regression)
  </acceptance_criteria>
  <done>Root-cause-diagnosed fix implemented and committed; diagnostic print statements removed; full test suite green modulo the 2 known pre-existing unrelated failures; D-09 non-regression grep passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: On-device confirmation — collapsed pill stays synced through a live resolution switch</name>
  <files></files>
  <read_first>Task 3's implemented fix and its root-cause comment</read_first>
  <action>No code changes — confirm Task 3's fix on real hardware by performing a live resolution switch in both directions, per the steps below.</action>
  <what-built>
    An evidence-based fix (diagnosed via Task 2's real on-device trace, not assumed) so the collapsed pill's
    live-measured size stays correctly synced to the real camera notch immediately after any live scaled-
    resolution change, with D-09 (no D-01/D-03/D-04 scaling ever reaches the collapsed pill) reaffirmed as a
    non-regression.
  </what-built>
  <how-to-verify>
    1. With the app running (fix already built in, no relaunch needed for this step), confirm the collapsed
       pill at your CURRENT resolution still matches the real camera cutout exactly (baseline regression check).
    2. Live-switch to the other resolution (1470x956 <-> 1710x1112, whichever you are not currently on) via
       System Settings -> Displays -> your built-in display. Do NOT relaunch the app.
    3. Immediately (within ~1 second of the switch completing) look at the collapsed pill — confirm its depth
       now visually matches the real camera cutout, with zero visible cropping or gap, on the FIRST glance
       (not "after waiting a bit").
    4. Confirm the pill's horizontal centering is unchanged throughout.
    5. Switch back to the original resolution live and re-confirm correctness in the other direction too.
    6. Open Settings -> Appearance -> "Island Size" and drag the Width/Depth sliders through 80%, the auto
       default, and 150% while the pill is collapsed (not hovering) — confirm the collapsed pill's size never
       changes at any slider position (D-09 non-regression).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (be specific about which direction of switch and what looked wrong)</resume-signal>
  <verify>
    <manual>User confirms "approved" after performing steps 1-6 on real hardware</manual>
  </verify>
  <done>On-device UAT approved: collapsed pill matches the real camera notch immediately after a live resolution switch in both directions, D-09 non-regression confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS display-reconfiguration events -> app geometry state | `NSApplication.didChangeScreenParametersNotification` delivers OS-controlled timing/data this app does not control; a stale or malformed read must fail safe, never crash or draw garbage geometry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67.1-08 | Denial of Service (self-inflicted) | `retryPositioningUntilStable` (Case B, if implemented) | mitigate | Bounded to 4 retries with a hard stop at `attempt == 4`; guarded to schedule zero extra work when the first post-hop measurement is already stable — cannot runaway-loop or accumulate `asyncAfter` work under repeated resolution changes (each notification firing starts its own independently-bounded chain, and `updateVisibility()` itself is idempotent) |
| T-67.1-09 | Tampering (malformed/transitional screen data) | `notchSize(...)` fed from live `NSScreen` mid-reconfiguration | accept | Already fails safe to `nil` on a non-positive computed width (NotchGeometry.swift:35) — pre-existing, unchanged by this plan, no new surface introduced |
</threat_model>

<verification>
Task 1: Debug build green with exactly 3 "[D10-TRACE]" trace points wired. Task 2: real on-device trace captured (human-action, no automation possible for a live resolution switch). Task 3: evidence-based fix implemented per the trace, temporary diagnostics removed, full XCTest suite green (modulo 2 known pre-existing unrelated failures), D-09 non-regression grep passes. Task 4: on-device UAT confirms the collapsed pill stays synced to the real camera notch immediately after a live resolution switch in both directions, with zero change from Width/Depth sliders.
</verification>

<success_criteria>
- Root cause of the collapsed-pill live re-measurement bug is diagnosed from a real on-device trace, not assumed.
- Fix implemented, temporary diagnostics removed, full test suite green (modulo 2 known pre-existing unrelated failures).
- On-device UAT approved: collapsed pill matches the real camera notch immediately after a live resolution switch in both directions, with zero relaunch, and D-09 (no D-01/D-03/D-04 scaling ever touches the collapsed pill) reconfirmed.
- Phase 67.1's Plan 05 UAT checkpoint can resume and complete.
</success_criteria>

<output>
Create `.planning/phases/67.1-notch-size-scaling-resolution-aware-manual-override/67.1-06-SUMMARY.md` when done
</output>
