---
phase: 67.1-notch-size-scaling-resolution-aware-manual-override
plan: 09
subsystem: ui
tags: [swiftui, notch-scaling, live-activities, camera-safety]

requires:
  - phase: 67.1-08
    provides: "resolvedWingWidthScale/resolvedWingDepthScale computed properties and wingsShape(depthScale:) parameter"
provides:
  - "osdWings/capsLockWings/updateWings/downloadWings/timerWings/meetingWings each scale their outer leadingPad/trailingPad by resolvedWingWidthScale and their inner content height by resolvedWingDepthScale"
  - "All 10 of the codebase's wingsShape-routed wings now respond to the Width/Depth sliders (4 from Plan 08, 6 from this plan)"
affects: ["67.1-10 (bespoke media wing, the one remaining wingsShape-adjacent surface)"]

tech-stack:
  added: []
  patterns: ["hardware-derived camera-block wings scale ONLY leadingPad/trailingPad (the footprint padding) — margin/cameraBlockWidth (built from live interaction.collapsedNotchSize) and every content width (icon/bar/text/pill) are left completely untouched, unlike Plan 08's Spacer()-based wings which scale leftWidth/rightWidth directly with a width-growth-only floor"]

key-files:
  created: []
  modified:
    - "Islet/Notch/NotchPillView.swift"

key-decisions:
  - "wScale/dScale are applied unclamped (plain resolvedWingWidthScale/resolvedWingDepthScale, no max(1.0, ...) floor) — unlike Plan 08's Spacer()-based wings, these 6 wings' leftWidth/rightWidth are derived arithmetically from the scaled leadingPad/trailingPad plus the untouched cameraBlockWidth/content widths, so shrinking below 1.0 only shrinks the padding, never the hardware-verified camera clearance."

requirements-completed: [D-14]

duration: 12min
completed: 2026-07-26
---

# Phase 67.1 Plan 09: Wing Scale — OSD/CapsLock/Update/Download/Timer/Meeting Wings Summary

**Applied Plan 08's D-14 wing-scale contract to the 6 remaining wingsShape-routed wings that derive camera clearance from live hardware (`cameraBlockWidth`), scaling only their outer leading/trailing padding and never the camera-block width, margin, or any content.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-26T21:36:27+02:00
- **Completed:** 2026-07-26T21:46:00+02:00
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- `osdWings`, `capsLockWings`, `updateWings` each gained a local `wScale`/`dScale` pair; their `iconLeadingPad`/`leadingPad` and `trailingPad` constants now multiply by `wScale`, and every `.frame(height: Self.wingsSize.height, ...)` reference inside their content closures multiplies by `dScale`; `wingsShape(...)` calls now pass `depthScale: dScale`.
- `downloadWings`, `timerWings`, `meetingWings` received the identical treatment for their own `leadingPad`/`trailingPad` and height references.
- `meetingWings`' locked `elapsedWidth`/`iconGap`/`muteIconWidth` triple (summing to the static `Self.meetingWingRightContentWidth = 84`, read by `NotchWindowController.collapsedInteractiveZone()`/`meetingClickThroughZones()`) was left completely unscaled — grep-verified zero occurrences of `elapsedWidth * `, `iconGap * `, `muteIconWidth * ` in the function body.
- All 6 functions' `cameraBlockWidth`/`margin` locals remain unscaled — grep-verified zero occurrences of `cameraBlockWidth * ` / `margin * ` across all 6 function bodies.
- All 10 of the codebase's `wingsShape`-routed wings (4 from Plan 08 + 6 from this plan) now respond to the Width/Depth sliders.

## Task Commits

Each task was committed atomically:

1. **Task 1: Apply wing scale to OSD, Caps Lock, and Update wings** - `ccaece5` (feat)
2. **Task 2: Apply wing scale to Download, Timer, and Meeting wings** - `15f0a6d` (feat)

**Plan metadata:** (this commit, see below)

## Files Created/Modified
- `Islet/Notch/NotchPillView.swift` - `osdWings`/`capsLockWings`/`updateWings`/`downloadWings`/`timerWings`/`meetingWings` each gained `wScale`/`dScale` locals, scaled outer padding, scaled inner content height, and `depthScale:` passed to their `wingsShape(...)` call

## Decisions Made
None beyond what's captured in `key-decisions` above — plan executed as specified.

## Deviations from Plan

None - plan executed exactly as written. All acceptance-criteria greps (per-function `wScale`/`dScale` occurrence counts, zero-count camera-block/margin/locked-triple scaling checks) passed on the first attempt, matching the plan's specified counts.

## Issues Encountered

**Full test suite showed 6 failures** (`LicenseStateTests` x4, `SettingsViewTests` x2) — the exact same 6 pre-existing failures already documented in this phase's `deferred-items.md` and explicitly called out as the allowed exception in this plan's own note and Task 2 acceptance criteria. No fix needed or applied — confirmed pre-existing/unrelated, not a regression from this plan's changes.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- All 10 named `wingsShape`-routed wings from `67.1-CONTEXT.md`'s canonical_refs list now respond to the D-14 Width/Depth sliders with camera-safety guaranteed by construction at every slider position.
- Plan 10 can proceed to the one remaining bespoke media wing (not `wingsShape`-routed, its own distinct rendering path).
- No blockers.

---
*Phase: 67.1-notch-size-scaling-resolution-aware-manual-override*
*Completed: 2026-07-26*

## Self-Check: PASSED
