---
phase: 42-dual-activity-display
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [DUAL-01]

must_haves:
  truths:
    - "The click-through coverage gap for wing-tier content (42-RESEARCH.md Pitfall 2 / Open Question 1) is confirmed or ruled out on real hardware, on TODAY's shipped build, before any secondary-bubble code is written"
  artifacts: []
  key_links: []
---

<objective>
Resolve 42-RESEARCH.md's single highest-risk open item (Pitfall 2 / Open Question 1) BEFORE any
secondary-bubble code exists: does tapping the far outer edge of an EXISTING wing — content that
already renders wider than `NotchWindowController.hotZone`'s small fixed rect — currently register
a click at all, or does it silently pass through to whatever is behind the notch?

Purpose: This is a pure on-device spike against the CURRENT build (no code changes). Its outcome
is a hard input to Plan 42-04 Task 2: if today's `hotZone` already fails to cover wing-tier
content, the collapsed-tier click-through geometry must be widened before the secondary bubble's
tap target (D-12) can possibly work — this project already shipped one real regression from this
exact mechanism (Phase 40-03's badge-tap bug, confirmed root-caused to `NotchWindowController`'s
`hotZone` not covering the badge's real screen position). Running this spike first, in isolation,
means Plan 42-04 either does the widening work with certainty it's needed, or skips it with
certainty it's not — never a guess baked into the bubble's own implementation.
Output: A confirmed answer ("registers" or "passes through"), recorded in this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/42-dual-activity-display/42-RESEARCH.md
@.planning/phases/40-update-available-hud-sparkle-integration/40-03-SUMMARY.md

<!-- 40-03-SUMMARY.md is the directly relevant precedent: the badge-tap bug there was root-caused
     to this EXACT mechanism (NotchWindowController.hotZone not covering a wing-tier-adjacent
     overlay's real position). This spike determines whether the same gap already affects TODAY's
     existing wings (Charging/Device/Countdown), independent of any new Phase 42 code. -->
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: On-device spike — does tapping the far outer edge of an existing wing register a click?</name>
  <files>Islet/Notch/NotchWindowController.swift, Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift lines 960-1005 (hotZone/expandedZone computation in
    `positionAndShow()` — `hotZone = collapsedFrame.insetBy(dx: -hotZonePadding, dy:
    -hotZonePadding)`, fixed to the small measured-notch rect, current line 995)
    Islet/Notch/NotchWindowController.swift lines 1207-1240 (`handlePointer(at:)` — while
    collapsed, `activeZone` is ALWAYS the bare `hotZone`, current line 1217)
    Islet/Notch/NotchPillView.swift lines 2000-2050 (`wings(for:)` — the Charging wing's
    right-flank BatteryIndicator, one of the two content elements to test)
    Islet/Notch/NotchPillView.swift lines 2240-2266 (`countdownWings(for:)` — the mm:ss text at
    `wingsLabelWidth/2` = 200pt from center, the other content element to test)
  </read_first>
  <action>
    No code changes in this task — verification-only. Build and run today's Islet.app (Debug) and
    manually confirm, on real hardware, whether AppKit's click-through `hotZone` (fixed to the
    small ~179×32pt collapsed-pill rect, `NotchWindowController.swift:995`) already fails to cover
    taps on wing-tier content that renders wider than that rect — the exact mechanism confirmed to
    have caused the Phase 40-03 badge-tap regression.
  </action>
  <what-built>
    Nothing new this task — verifies EXISTING click-through geometry
    (`NotchWindowController.hotZone`, fixed to the small ~179×32pt collapsed-pill rect) against
    EXISTING wing content that already renders wider than that rect (Charging's battery
    percentage at the far right flank, or Countdown's mm:ss text at ~200pt from center).
  </what-built>
  <how-to-verify>
    1. Build and run Islet.app from Xcode (Debug configuration).
    2. Trigger a live wing that renders content wider than the small collapsed-pill hot-zone: EITHER
       plug in/unplug the charger to show the Charging wing, OR wait for/create a calendar event
       starting within the next hour to show the Countdown wing.
    3. With the wing visibly showing, click directly on its FAR outer edge content — for Charging:
       the battery percentage text/glyph on the right flank; for Countdown: the mm:ss text on the
       right flank (~200pt from the notch's physical center, well outside the small hot-zone).
    4. Observe: does the island toggle expand/collapse (the click registered) or does the click
       pass straight through to whatever app/desktop is behind the notch at that screen position
       (the click was swallowed by AppKit's `ignoresMouseEvents` before ever reaching SwiftUI)?
    5. Repeat once on the LEFT flank's outer edge (Charging's "Charging" label / Countdown's
       calendar icon, both also outside the small hot-zone) for symmetry.
  </how-to-verify>
  <resume-signal>
    Report exactly one of: "registers" (today's hotZone already covers wing-tier content — no
    widening needed in Plan 42-04) or "passes through" (hotZone must be widened for the
    collapsed/wing tier in Plan 42-04 Task 2, mirroring `visibleContentZone()`'s existing
    per-presentation-aware pattern already used for the expanded tier). Include which of the two
    flanks (left/right) and which wing (Charging/Countdown) was tested.
  </resume-signal>
  <acceptance_criteria>
    - A definitive "registers" or "passes through" outcome is recorded, with the specific wing and
      flank tested named explicitly (not a vague "seems fine")
    - No source file is modified by this task (files_modified stays empty)
  </acceptance_criteria>
  <done>User has reported one of the two outcomes above, recorded in this plan's SUMMARY.md for Plan 42-04 to consume as a hard input to its Task 2 branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | This plan makes zero code changes — it is a manual on-device observation of already-shipped behavior. No new trust boundary is introduced or crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04 | N/A | N/A | accept | No code changes in this plan — no new attack surface to assess |
</threat_model>

<verification>
Human-verify checkpoint outcome recorded in this plan's SUMMARY.md; no automated check applies (no code produced).
</verification>

<success_criteria>
- A definitive, on-device-confirmed answer to "does today's hotZone cover wing-tier tap targets" exists before Plan 42-04 begins its Task 2 (secondary-bubble tap wiring)
</success_criteria>

<output>
Create `.planning/phases/42-dual-activity-display/42-02-SUMMARY.md` when done, explicitly recording the "registers" / "passes through" outcome for Plan 42-04 to read.
</output>
