---
phase: 42-dual-activity-display
plan: 03
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - Islet/Notch/NotchPillView.swift
autonomous: true
requirements: [DUAL-01]

must_haves:
  truths:
    - "A SwiftUI Preview with presentationState.secondary hand-seeded to .nowPlaying(...) shows a round bubble to the right of the primary pill, distinct from and not overlapping it"
    - "presentationSwitch's own switch statement is byte-for-byte unchanged — the bubble is composed as a sibling in body's ZStack, never a new case inside presentationSwitch"
    - "The bubble carries no .onHover modifier anywhere (D-13 — no hover-reveal)"
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "secondaryBubble(_:) view + artThumbnailCircular(_:diameter:) + composition in body's ZStack"
      contains: "matchedGeometryEffect(id: \"secondaryBubble\""
  key_links:
    - from: "NotchPillView.body's ZStack"
      to: "IslandPresentationState.secondary"
      via: "if let secondary = presentationState.secondary"
      pattern: "presentationState\\.secondary"
    - from: "NotchPillView.secondaryBubble's onTapGesture"
      to: "onSecondaryTap closure property"
      via: ".onTapGesture { onSecondaryTap() }"
      pattern: "onSecondaryTap\\(\\)"
---

<objective>
Build the secondary bubble's SwiftUI view — a circular artwork-cropped shape with its own
`matchedGeometryEffect` id, and its own tap target — as a pure consumer of
`IslandPresentationState.secondary` (42-01's output). Composed alongside, never inside,
`presentationSwitch`, per ROADMAP's additive-only constraint (success criterion 4).

Purpose: This is the first time this view hierarchy renders two simultaneous
`matchedGeometryEffect` shapes at once (D-09) — every other case in this file assumes exactly one
visible shape per frame. The view stays a pure render-only consumer (never decides
primary/secondary precedence itself, per the "one pure arbiter" discipline PITFALLS.md
establishes) and never touches the controller-owned tap-handling logic (that's Plan 42-04).
Output: `secondaryBubble(_:)`, `artThumbnailCircular(_:diameter:)`, `onSecondaryTap` property, composed into `body`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/42-dual-activity-display/42-CONTEXT.md
@.planning/phases/42-dual-activity-display/42-RESEARCH.md
@.planning/phases/42-dual-activity-display/42-PATTERNS.md
@.planning/phases/42-dual-activity-display/42-UI-SPEC.md

<interfaces>
<!-- Consumed from 42-01 (Islet/Notch/IslandResolver.swift, Islet/Notch/IslandPresentationState.swift),
     already merged before this plan runs (wave 2 depends_on 42-01): -->
```
enum SecondaryActivity: Equatable {
    case nowPlaying(NowPlayingPresentation)
}
```
`IslandPresentationState.secondary: SecondaryActivity?` — the `@Published` field this view reads via
`presentationState.secondary` (the view's existing `@ObservedObject var presentationState:
IslandPresentationState`, NotchPillView.swift:43).

<!-- Existing NotchPillView.swift conventions this plan must match exactly (already read this
     session — see 42-PATTERNS.md for the full excerpts): -->
- `@Namespace private var ns` (line 198) — reuse this SAME namespace with a NEW distinct
  `id: "secondaryBubble"`, do not add a second `@Namespace`.
- `var onClick: () -> Void = {}` (line 145) — the exact declaration style every `onXxx` closure
  property in this file follows; `onSecondaryTap` must match it.
- `artThumbnail(_ art: NSImage?, side: CGFloat, corner: CGFloat)` (line 2519) — the nil→placeholder
  structure `artThumbnailCircular` must mirror, swapping `RoundedRectangle` for `Circle()`.
- `@ObservedObject var nowPlaying: NowPlayingState` (line 36) — artwork for the bubble comes from
  `nowPlaying.artwork: NSImage?`, the SAME property `mediaExpanded(p, art: nowPlaying.artwork)`
  already uses — NOT from `SecondaryActivity.nowPlaying`'s own associated value (which carries
  only title/artist, not the image).
- `.matchedGeometryEffect(...)` MUST precede `.frame(...)` on any shape — documented, 3×-fixed bug
  in this exact file (lines 850-858, 1803-1808, 1956-1958).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: artThumbnailCircular + secondaryBubble view functions + onSecondaryTap property</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift lines 190-270 (namespace + size constants —
    `wingsLabelWidth` at line 263 is the anchor point for the two new constants below)
    Islet/Notch/NotchPillView.swift lines 1947-1993 (`wingsShape` — the
    `.fill → .matchedGeometryEffect → .frame → .overlay → .onTapGesture` skeleton to mirror)
    Islet/Notch/NotchPillView.swift lines 2518-2536 (`artThumbnail(_:side:corner:)` — the direct
    nil-fallback precedent for `artThumbnailCircular`)
    .planning/phases/42-dual-activity-display/42-UI-SPEC.md "Bubble geometry" and "Bubble content"
    tables (source of truth for the exact 24pt/8pt starting values and fill/fallback treatment)
  </read_first>
  <action>
    Add two new static constants directly below `wingsLabelWidth` (current line 263):
    `static let secondaryBubbleDiameter: CGFloat = 24` and `static let secondaryBubbleGap: CGFloat
    = 8` (UI-SPEC's locked starting values — bottom of the 24-28pt range, the `sm` 8-point-scale
    gap token — tunable on-device in Plan 42-04's checkpoint per this file's own established
    "small geometry constants get tuned on real hardware" precedent, citing Phase 41/39 in the
    comment). Add `var onSecondaryTap: () -> Void = {}` to the property list directly below
    `onClick: () -> Void = {}` (current line 145), matching its exact declaration style. Add
    `@ViewBuilder private func artThumbnailCircular(_ art: NSImage?, diameter: CGFloat) -> some
    View` directly below `artThumbnail(_:side:corner:)` (after current line 2536): same
    nil-fallback structure as `artThumbnail` — non-nil branch: `Image(nsImage:
    art).resizable().aspectRatio(contentMode: .fill).frame(width: diameter, height:
    diameter).clipShape(Circle())`; nil branch: `Circle().fill(Color.white.opacity(0.12))
    .frame(width: diameter, height: diameter).overlay(Image(systemName:
    "music.note").font(.system(size: diameter * 0.45)).foregroundStyle(.white.opacity(0.7)))`.
    Add `private func secondaryBubble(_ activity: SecondaryActivity) -> some View` directly below
    that: switch on `activity`, currently exactly one case `.nowPlaying` (the associated
    `NowPlayingPresentation` value is unused for rendering — artwork comes from
    `nowPlaying.artwork`, the view's own existing property, matching how `mediaExpanded(p, art:
    nowPlaying.artwork)` already sources artwork separately from the presentation enum). Build:
    `Circle().fill(islandFill)` then `.matchedGeometryEffect(id: "secondaryBubble", in: ns)`
    (MUST precede `.frame`) then `.frame(width: Self.secondaryBubbleDiameter, height:
    Self.secondaryBubbleDiameter)` then `.overlay(liquidGlassEffectLayer(shape: Circle(), size:
    CGSize(width: Self.secondaryBubbleDiameter, height: Self.secondaryBubbleDiameter),
    parameters: .expanded))` (Phase 35 material, reused verbatim — UI-SPEC's "Dominant" color
    role) then `.overlay(artThumbnailCircular(nowPlaying.artwork, diameter:
    Self.secondaryBubbleDiameter))` then `.onTapGesture { onSecondaryTap() }` (D-12). Add NO
    `.onHover` modifier anywhere on this shape (D-13 — no hover-reveal, mirrors Phase 41 D-08's
    Countdown-pill precedent).
  </action>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "func secondaryBubble" Islet/Notch/NotchPillView.swift` returns exactly 1 match
    - `grep -n "func artThumbnailCircular" Islet/Notch/NotchPillView.swift` returns exactly 1 match
    - Reading the `secondaryBubble` function body confirms `.matchedGeometryEffect(id:
      "secondaryBubble"` appears on the line immediately BEFORE the `.frame(width:
      Self.secondaryBubbleDiameter` line (ordering check, not just presence)
    - `grep -c "\.onHover" Islet/Notch/NotchPillView.swift` count is unchanged from this task's
      start (zero new `.onHover` added)
    - `xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build` exits 0
  </acceptance_criteria>
  <done>secondaryBubble(_:) and artThumbnailCircular(_:diameter:) compile; onSecondaryTap property exists with a default no-op; nothing composed into body yet (Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Compose secondaryBubble into body's ZStack (D-05/D-08/D-09)</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift lines 759-770 (`body`'s `ZStack(alignment: .top)` —
    `presentationSwitch` is currently the ZStack's only child)
    Islet/Notch/NotchPillView.swift lines 1947-1993 (`wingsShape`'s
    `.alignmentGuide(HorizontalAlignment.center)` technique — the notch-center-pinning mechanism
    every existing shape in this view relies on)
    Islet/Notch/NotchPillView.swift lines 2240-2266 (`countdownWings(for:)` — confirms
    `rightWidth: Self.wingsLabelWidth / 2` = 200pt is the FIXED value whenever `.calendarCountdown`
    is the primary presentation, which is the ONLY presentation the bubble ever renders alongside)
    .planning/phases/42-dual-activity-display/42-UI-SPEC.md "Positioning mechanism" row (the
    project's own documented preference and the Phase 39-07 `.offset()` history it cites)
  </read_first>
  <action>
    In `body`'s `ZStack(alignment: .top)` (current line 763), add the secondary bubble as a
    SIBLING to `presentationSwitch` — NOT a case inside it; `presentationSwitch`'s own switch
    statement (current lines 716-757) must stay byte-for-byte unchanged. Condition it on `if let
    secondary = presentationState.secondary`. Because `resolveSecondary` (42-01) guarantees the
    bubble only ever mounts when `presentation == .calendarCountdown`, and `countdownWings(for:)`
    always renders with a fixed `rightWidth: Self.wingsLabelWidth / 2` (200pt), the bubble's center
    must land at `200 + Self.secondaryBubbleGap + Self.secondaryBubbleDiameter / 2` = 220pt to the
    right of the shared notch center (x=0 in this ZStack's local space, where every other shape's
    own `.alignmentGuide(HorizontalAlignment.center)` already pins the notch). Apply `.offset(x:
    220)` to the bubble view as the FIRST attempt. This exact `.offset` usage is flagged
    UNVERIFIED by 42-RESEARCH.md — Phase 39-07's `.offset()` bug occurred inside `wingsShape`'s OWN
    content `ZStack` (a different, nested context from this top-level `body` ZStack), so it may
    not reproduce here, but has never been confirmed working in THIS specific `ZStack` either.
    Verify via an Xcode `#Preview` with a hand-constructed `presentationState.secondary =
    .nowPlaying(.playing(title: "Test", artist: "Test"))` whether the bubble visually renders 220pt
    right of the notch center, to the right of the countdown wing, with a visible gap and no
    overlap. If the Preview shows the bubble NOT at its intended offset position (the Phase 39-07
    symptom — the shape renders at its pre-offset origin), fall back to UI-SPEC's stated
    preference: express the primary content and the bubble as siblings inside an
    `HStack(spacing: Self.secondaryBubbleGap)`, scoped ONLY to the `.calendarCountdown` case (the
    bubble never appears alongside any other case, so this fallback never touches
    `presentationSwitch`'s other 15 cases). Add `.transition(.scale.combined(with: .opacity))` to
    the bubble's conditional view — the SwiftUI-side complement to the `matchedGeometryEffect`
    spring (D-09).
  </action>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "if let secondary = presentationState.secondary" Islet/Notch/NotchPillView.swift`
      returns exactly 1 match, located inside `body`'s `ZStack`, not inside `presentationSwitch`
    - Diff review confirms `presentationSwitch`'s switch statement (current lines 716-757) is
      byte-identical to its pre-task-2 state
    - Xcode `#Preview` with hand-seeded `presentationState.secondary` visually shows the bubble to
      the right of the countdown pill, with a visible non-overlapping gap
    - `xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build` exits 0
  </acceptance_criteria>
  <done>Secondary bubble renders alongside the countdown wing in an Xcode Preview with hand-seeded state, positioned to the right with a visible gap, never inside presentationSwitch's cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Renders already-trusted local data (album artwork already sourced by the existing `NowPlayingState.artwork` pipeline, Phase 4) through a new circular clip shape. No new data source, no new external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05 | Information Disclosure | `artThumbnailCircular` / `secondaryBubble` | accept | Renders the SAME already-local, already-trusted artwork the app already shows elsewhere (`mediaExpanded`) — a new render surface for existing trusted data, not a new data source |
| T-42-06 | Tampering | `secondaryBubble`'s `.onTapGesture` | accept | The SwiftUI gesture itself has no privileged effect (calls a closure property, default no-op until Plan 42-04 wires it) — any real risk lives in Plan 42-04's AppKit click-through geometry, tracked there |
</threat_model>

<verification>
- `xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build` exits 0 after both tasks
- Xcode Preview with hand-seeded `presentationState.secondary` visually confirms bubble placement, gap, and no overlap with the primary pill
</verification>

<success_criteria>
- `secondaryBubble(_:)` and `artThumbnailCircular(_:diameter:)` exist and compile
- `presentationSwitch`'s existing switch statement is unchanged (ROADMAP success criterion 4)
- The bubble carries a distinct `matchedGeometryEffect(id: "secondaryBubble")` on the shared `ns` namespace, never colliding with `"island"`
- No `.onHover` on the bubble (D-13)
</success_criteria>

<output>
Create `.planning/phases/42-dual-activity-display/42-03-SUMMARY.md` when done
</output>
