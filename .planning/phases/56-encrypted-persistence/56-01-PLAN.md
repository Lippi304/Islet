---
phase: 56-encrypted-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Clipboard/KeychainClipboardKeyStore.swift
  - Islet/Clipboard/ClipboardFileStore.swift
  - IsletTests/ClipboardFileStoreTests.swift
autonomous: true
requirements: [CLIP-04, PRIV-02]

must_haves:
  truths:
    - "ClipboardFileStore.save(_:root:key:) followed by ClipboardFileStore.load(root:key:) against the SAME root/key reproduces the original items in the same order, for both text and image items (SC#1)"
    - "The raw bytes of index.json.enc and every images/*.enc file contain no readable plaintext substring of the original clipboard content when inspected directly (SC#2, PRIV-02)"
    - "ClipboardFileStore.load(root:key:) returns an empty array — never throws, never crashes — when the index file is corrupted, missing, or decrypted with the wrong key (D-04)"
    - "Saving a smaller items array than a previous save deletes the now-orphaned image .enc file(s) from disk as part of the same save call, and never touches the still-referenced files (D-06)"
    - "deleteOrphanedImageFile(at:root:) never removes a file whose standardized path does not live under the standardized storage root — mirrors ShelfFileStore.deleteSessionCopy's guard exactly (SC#3)"
    - "The AES-256 SymmetricKey used to encrypt/decrypt is stored in Keychain with kSecAttrSynchronizable explicitly set to false, never iCloud-syncable (D-05)"
    - "No explicit nonce: argument is ever passed to AES.GCM.seal — every encryption call relies on CryptoKit's own fresh-random-nonce default, so no two writes under the same key ever reuse a nonce"
  artifacts:
    - path: "Islet/Clipboard/KeychainClipboardKeyStore.swift"
      provides: "Keychain-backed AES-256 SymmetricKey storage: read/write/readOrCreateKey, device-only (D-05)"
      exports: ["KeychainClipboardKeyStore"]
      min_lines: 20
    - path: "Islet/Clipboard/ClipboardFileStore.swift"
      provides: "Encrypted JSON-index + per-image-file persistence (storageRoot/load/save/deleteOrphanedImageFile) + the on-disk ClipboardItemRecord shape"
      exports: ["ClipboardFileStore", "ClipboardItemRecord", "ClipboardFileStoreError"]
      min_lines: 50
    - path: "IsletTests/ClipboardFileStoreTests.swift"
      provides: "Round-trip (SC#1), plaintext-absence (SC#2), D-04 graceful-degradation, D-06 orphan-cleanup, and SC#3 delete-hardening coverage"
  key_links:
    - from: "Islet/Clipboard/ClipboardFileStore.swift"
      to: "Islet/Clipboard/KeychainClipboardKeyStore.swift"
      via: "load/save accept an already-resolved SymmetricKey parameter, obtained by the caller via KeychainClipboardKeyStore().readOrCreateKey() — ClipboardFileStore itself never touches Keychain"
      pattern: "key: SymmetricKey"
    - from: "Islet/Clipboard/ClipboardFileStore.swift"
      to: "hardened delete guard"
      via: "deleteOrphanedImageFile's hasPrefix(root.path + \"/\") check before any removeItem call"
      pattern: "hasPrefix\\(root\\.path \\+ \"/\"\\)"
---

<objective>
Build `ClipboardFileStore` (encrypted JSON-index + separate image-file persistence, rooted under
Application Support) and its sibling `KeychainClipboardKeyStore` (Keychain-backed AES-256 key,
device-only per D-05) — the store↔disk round trip that satisfies CLIP-04 (persists across
relaunch) and PRIV-02 (encrypted at rest via CryptoKit AES-GCM) at the file-store layer. No live
`NSPasteboard` monitoring (Phase 57) and no menu UI (Phase 58) exist yet or are touched here.

Purpose: Establish encryption-at-rest from day one, exactly as the ROADMAP's Phase 56 goal states
("not retrofitted later") — mirroring this codebase's two already-proven precedents exactly:
`ShelfFileStore`'s standalone-enum-with-hardened-delete shape, and `KeychainLicenseStore`'s
SecItem upsert shape.

Output: `Islet/Clipboard/ClipboardFileStore.swift`, `Islet/Clipboard/KeychainClipboardKeyStore.swift`,
`IsletTests/ClipboardFileStoreTests.swift`. `ClipboardItem.swift`/`ClipboardStore.swift` (Phase 55)
are read but NOT modified — the pure model stays pure (SC-4 from Phase 55 carries forward).
Plan 56-02 wires a DEBUG-only on-device kill-and-restart check (ROADMAP SC#4) against this plan's
output; not built here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-encrypted-persistence/56-CONTEXT.md
@.planning/phases/56-encrypted-persistence/56-RESEARCH.md
@.planning/phases/56-encrypted-persistence/56-PATTERNS.md

# Direct analogs to mirror exactly (read before writing any code)
@Islet/Shelf/ShelfFileStore.swift
@Islet/Licensing/KeychainLicenseStore.swift
@Islet/Clipboard/ClipboardItem.swift
@Islet/Clipboard/ClipboardStore.swift
@IsletTests/ShelfFileStoreTests.swift
</context>

<interfaces>
<!-- Extracted from the codebase's own current files and RESEARCH.md's already-completed pattern
analysis. Executor should use these shapes directly — no further codebase exploration needed. -->

The exact current shape of the value type being persisted (`Islet/Clipboard/ClipboardItem.swift`,
full file, unchanged by this plan):
```swift
import Foundation

struct ClipboardItem: Equatable, Codable {
    let id: UUID
    var kind: Kind
    var timestamp: Date

    enum Kind: Equatable, Codable {
        case text(String)
        case image(Data)
    }
}
```

`ShelfFileStore`'s hardened delete-under-root guard (`Islet/Shelf/ShelfFileStore.swift:49-56`) —
D-06/SC#3's exact shape to mirror, adapted from a whole-directory delete to a single-file delete:
```swift
static func deleteSessionCopy(at localURL: URL) {
    let itemDir = localURL.deletingLastPathComponent().standardizedFileURL
    let shelfRoot = URL(fileURLWithPath: NSTemporaryDirectory())
        .appendingPathComponent("IsletShelf", isDirectory: true)
        .standardizedFileURL
    guard itemDir.path.hasPrefix(shelfRoot.path + "/") else { return }
    try? FileManager.default.removeItem(at: itemDir)
}
```

`KeychainLicenseStore`'s SecItem read/write shape (`Islet/Licensing/KeychainLicenseStore.swift:32-67`)
— the exact query/upsert dictionaries to mirror, swapping the `Codable` payload for raw key bytes:
```swift
func read() -> LicenseRecord? {
    let query: [String: Any] = [
        kSecClass as String: kSecClassGenericPassword,
        kSecAttrService as String: service,
        kSecAttrAccount as String: account,
        kSecReturnData as String: true,
        kSecMatchLimit as String: kSecMatchLimitOne
    ]
    var result: AnyObject?
    let status = SecItemCopyMatching(query as CFDictionary, &result)
    guard status == errSecSuccess,
          let data = result as? Data,
          let record = try? JSONDecoder().decode(LicenseRecord.self, from: data)
    else { return nil }
    return record
}

@discardableResult
func write(_ record: LicenseRecord) -> Bool {
    guard let payload = try? JSONEncoder().encode(record) else { return false }
    let query: [String: Any] = [
        kSecClass as String: kSecClassGenericPassword,
        kSecAttrService as String: service,
        kSecAttrAccount as String: account
    ]
    SecItemDelete(query as CFDictionary)
    var attributes = query
    attributes[kSecValueData as String] = payload
    attributes[kSecAttrAccessible as String] = kSecAttrAccessibleAfterFirstUnlock
    return SecItemAdd(attributes as CFDictionary, nil) == errSecSuccess
}
```

CryptoKit AES-GCM seal/open round trip (RESEARCH.md Pattern 2, official API shape):
```swift
let sealedBox = try AES.GCM.seal(plaintext, using: key)   // combined = nonce||ciphertext||tag
let combined = sealedBox.combined  // Data?, nil only if a non-default nonce size were used
let reopened = try AES.GCM.SealedBox(combined: combinedData)
let plaintext = try AES.GCM.open(reopened, using: key)     // throws CryptoKitError.authenticationFailure on tamper/wrong key
```

`ShelfFileStoreTests`'s fixturesDir setUp/tearDown convention (`IsletTests/ShelfFileStoreTests.swift:10-24`,
full shape to mirror — rename the fixture folder name only):
```swift
final class ShelfFileStoreTests: XCTestCase {
    private var fixturesDir: URL!

    override func setUp() {
        super.setUp()
        fixturesDir = URL(fileURLWithPath: NSTemporaryDirectory())
            .appendingPathComponent("ShelfFileStoreTestsFixtures-\(UUID())", isDirectory: true)
        try? FileManager.default.createDirectory(at: fixturesDir, withIntermediateDirectories: true)
    }

    override func tearDown() {
        try? FileManager.default.removeItem(at: fixturesDir)
        fixturesDir = nil
        super.tearDown()
    }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: KeychainClipboardKeyStore — device-only AES-256 key storage (D-05)</name>
  <files>Islet/Clipboard/KeychainClipboardKeyStore.swift</files>
  <read_first>
    Islet/Licensing/KeychainLicenseStore.swift (exact SecItem query/upsert shape to mirror —
    read()/write() at lines 32-67, kSecAttrAccessibleAfterFirstUnlock precedent at line 65)
    .planning/phases/56-encrypted-persistence/56-CONTEXT.md (D-05: kSecAttrSynchronizable=false,
    device-only key — clipboard content is personal, unlike the syncable license key)
    .planning/phases/56-encrypted-persistence/56-RESEARCH.md (Pattern 3 — the exact
    KeychainClipboardKeyStore shape already drafted; Pitfall 2 — resolve the key once per call
    site, never re-query Keychain on a hot path)
  </read_first>
  <action>
    Create `Islet/Clipboard/KeychainClipboardKeyStore.swift`: `import Foundation`, `import
    Security`, `import CryptoKit`. Header comment in `KeychainLicenseStore.swift`'s style — cite
    "Phase 56 / D-05", state this is the Keychain persistence glue for the clipboard's AES-256
    `SymmetricKey`, and explicitly call out the one deliberate divergence from
    `KeychainLicenseStore`: this key is device-only (`kSecAttrSynchronizable = false`) since
    clipboard content is personal and this milestone has no cross-Mac sync mechanism, whereas the
    license key is fine to sync (tied to a purchase, not private data).

    Define `struct KeychainClipboardKeyStore` with `private let service =
    "com.lippi304.islet.clipboard"` and `private let account = "encryptionKey"` (mirrors
    `KeychainLicenseStore`'s `service`/`account` field shape exactly, distinct string values).

    Implement `func read() -> SymmetricKey?`: identical `SecItemCopyMatching` query dictionary
    shape to `KeychainLicenseStore.read()` (kSecClassGenericPassword / kSecAttrService /
    kSecAttrAccount / kSecReturnData: true / kSecMatchLimit: kSecMatchLimitOne). `guard status ==
    errSecSuccess, let data = result as? Data else { return nil }` then `return
    SymmetricKey(data: data)` — raw bytes only, NO `JSONDecoder` step and NO wrapping `Codable`
    struct (RESEARCH.md's explicit anti-pattern: `SymmetricKey` has no `Codable` conformance,
    store its bytes directly).

    Implement `@discardableResult func write(_ key: SymmetricKey) -> Bool`: `let payload =
    key.withUnsafeBytes { Data($0) }`. Same delete-then-add upsert query shape as
    `KeychainLicenseStore.write(_:)` (`SecItemDelete` on the bare query first, then build
    `attributes` with `kSecValueData: payload`, `kSecAttrAccessible:
    kSecAttrAccessibleAfterFirstUnlock` — mirrors `KeychainLicenseStore.swift:65`, no reason to
    diverge per CONTEXT.md's Claude's Discretion). ADD one line beyond `KeychainLicenseStore`'s
    shape: `attributes[kSecAttrSynchronizable as String] = false` — this is D-05's explicit,
    non-negotiable requirement and the ONE deliberate divergence from the license-key precedent.
    Return `SecItemAdd(attributes as CFDictionary, nil) == errSecSuccess`.

    Implement `func readOrCreateKey() -> SymmetricKey`: `if let existing = read() { return
    existing }`; otherwise `let newKey = SymmetricKey(size: .bits256)`, call `write(newKey)`
    (result intentionally discarded — a write failure here degrades to "next read also misses and
    a fresh key is generated again," an acceptable D-04-adjacent fallback with no UI to surface it
    to yet), `return newKey`.

    No `delete()` method — unlike `KeychainLicenseStore.delete()` (which has a real caller,
    `LicenseManager.debugResetLicense()`), nothing in this phase or its DEBUG scope needs a
    "reset clipboard encryption key" action; do not add unused scaffolding.

    Run `xcodegen generate` from the repo root so `Islet.xcodeproj` picks up the new file.
  </action>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "struct KeychainClipboardKeyStore" Islet/Clipboard/KeychainClipboardKeyStore.swift` matches
    - `grep -q "func readOrCreateKey() -> SymmetricKey" Islet/Clipboard/KeychainClipboardKeyStore.swift` matches
    - `grep -q "SymmetricKey(size: .bits256)" Islet/Clipboard/KeychainClipboardKeyStore.swift` matches
    - `grep -q "SymmetricKey(data: data)" Islet/Clipboard/KeychainClipboardKeyStore.swift` matches
    - `grep -q "kSecAttrAccessible as String\] = kSecAttrAccessibleAfterFirstUnlock" Islet/Clipboard/KeychainClipboardKeyStore.swift` matches
    - `grep -q "kSecAttrSynchronizable as String\] = false" Islet/Clipboard/KeychainClipboardKeyStore.swift` matches (D-05)
    - `grep -c "func delete()" Islet/Clipboard/KeychainClipboardKeyStore.swift` returns 0 (no unused scaffolding)
    - `xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug` reports `BUILD SUCCEEDED`
  </acceptance_criteria>
  <done>KeychainClipboardKeyStore.swift exists, mirrors KeychainLicenseStore's SecItem read/write shape, readOrCreateKey() generates-and-persists a 256-bit key if absent, write() sets kSecAttrSynchronizable=false (D-05); BUILD SUCCEEDED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ClipboardFileStore — encrypted JSON-index + image files, D-04 graceful degradation, D-06 orphan cleanup</name>
  <files>Islet/Clipboard/ClipboardFileStore.swift, IsletTests/ClipboardFileStoreTests.swift</files>
  <read_first>
    Islet/Shelf/ShelfFileStore.swift (standalone enum shape + hardened delete-under-root guard,
    lines 49-56 — D-06/SC#3 must mirror this exactly, adapted from a directory delete to a
    single-file delete)
    Islet/Clipboard/ClipboardItem.swift (the exact struct/Kind enum being persisted — see
    <interfaces> above)
    Islet/Clipboard/KeychainClipboardKeyStore.swift (Task 1's output — load/save accept an
    already-resolved SymmetricKey parameter obtained via readOrCreateKey(); ClipboardFileStore
    itself never calls Keychain)
    IsletTests/ShelfFileStoreTests.swift (fixturesDir setUp/tearDown convention, lines 10-24; the
    delete-path-hardening test shape, lines 75-86)
    .planning/phases/56-encrypted-persistence/56-CONTEXT.md (D-04: any decrypt/read failure =
    empty history, never a crash, no special handling; D-06: orphaned image files deleted as part
    of the SAME save that evicts/dedupes them, diffed against the incoming items array)
    .planning/phases/56-encrypted-persistence/56-RESEARCH.md (System Architecture Diagram; Pattern
    1 standalone-enum-with-injectable-root; Pattern 2 AES-GCM seal/open; Pitfall 1 decrypt-failure
    handling; Pitfall 3 write-new-files-before-deleting-orphans ordering; Pitfall 4 keep image
    bytes out of the JSON index; Anti-Patterns — never pass an explicit nonce)
  </read_first>
  <behavior>
    - Test: `save([textItem, imageItem], root: fixturesDir, key: testKey)` then `load(root:
      fixturesDir, key: testKey)` returns an array equal to the original 2 items, same order,
      same `id`/`kind`/`timestamp` values (SC#1 round trip, both Kind cases)
    - Test: after the save above, the raw `Data(contentsOf:)` bytes of `index.json.enc` do NOT
      contain the UTF8 bytes of the text item's original plaintext string anywhere in the file,
      and the raw bytes of the image item's `images/<uuid>.enc` file do NOT contain the original
      image `Data` as a contiguous byte range (SC#2, PRIV-02 — use `Data.range(of:)` to search,
      assert `nil`)
    - Test: `load(root:key:)` against a directory where `index.json.enc` contains hand-written
      garbage bytes (not valid AES-GCM ciphertext, e.g. `Data("not encrypted".utf8)`) returns `[]`
      rather than throwing or crashing (D-04)
    - Test: `load(root:key:)` using a DIFFERENT freshly-generated `SymmetricKey` than the one used
      to `save` returns `[]` — a wrong key is indistinguishable from corruption (D-04)
    - Test: saving `[imageItemA, imageItemB]` then saving again with only `[imageItemA]`
      (simulating an eviction/dedupe) leaves `imageItemA`'s on-disk `.enc` file intact and BYTE-
      IDENTICAL to before, and deletes `imageItemB`'s on-disk `.enc` file entirely (D-06
      diff-and-delete, computed against the new incoming array, never against stale on-disk state)
    - Test: calling `ClipboardFileStore.deleteOrphanedImageFile(at:root:)` directly with a file
      path OUTSIDE the storage root leaves that file completely untouched on disk (SC#3, mirrors
      `ShelfFileStoreTests.testDeleteSessionCopyOutsideShelfRootIsSafeNoOp` exactly)
  </behavior>
  <action>
    Create `Islet/Clipboard/ClipboardFileStore.swift`: `import Foundation`, `import CryptoKit`.
    Header comment in `ShelfFileStore.swift`'s style — cite "Phase 56 / CLIP-04 / PRIV-02 (D-04 /
    D-06)", state this is the ONE place performing real FileManager I/O + AES-GCM encryption for
    the clipboard, kept as a standalone enum (not a method on `ClipboardStore`) so `ClipboardStore`
    stays a pure, side-effect-free reducer — mirrors `ShelfFileStore`'s stated rationale relative
    to `ShelfLogic`.

    Define `enum ClipboardFileStoreError: Error, Equatable { case sealFailed }` — defensive only;
    `AES.GCM.seal(_:using:).combined` is `Optional` but is only ever `nil` for a non-default nonce
    size, which this file never produces (no explicit `nonce:` argument is ever passed).

    Define `struct ClipboardItemRecord: Codable` with exactly 5 fields, in this order: `let id:
    UUID`, `let kind: String` (literal `"text"` or `"image"`), `let text: String?`, `let
    imageFilename: String?`, `let timestamp: Date`. For a `.text` item: `kind = "text"`, `text`
    holds the string, `imageFilename` is `nil`. For a `.image` item: `kind = "image"`, `text` is
    `nil`, `imageFilename` is `id.uuidString + ".enc"` (RESEARCH.md Open Question 1's recommended
    convention — reuses `ShelfFileStore`'s existing `id.uuidString`-as-filename pattern). This
    keeps image bytes entirely out of the JSON index (Pitfall 4) — the index only ever stores a
    filename reference for images, never inline `Data`.

    Define `enum ClipboardFileStore` with these static members:

    `static func storageRoot() -> URL` — `FileManager.default.urls(for:
    .applicationSupportDirectory, in: .userDomainMask).first!.appendingPathComponent(
    "IsletClipboard", isDirectory: true)`. Production call sites use this; tests pass their own
    `fixturesDir` instead (this is what makes SC#1's injectable-root test possible).

    `static func load(root: URL, key: SymmetricKey) -> [ClipboardItem]` — chain every step
    (`Data(contentsOf:)`, `AES.GCM.SealedBox(combined:)`, `AES.GCM.open`, `JSONDecoder().decode`)
    with `try?`/optional-binding inside ONE `guard ... else { return [] }`. Any single step
    failing returns `[]` immediately — never throw, never crash out of `load` (D-04). On success,
    `compactMap` each `ClipboardItemRecord` back to a `ClipboardItem`: for `kind == "text"`,
    construct directly (`ClipboardItem(id: record.id, kind: .text(record.text ?? ""), timestamp:
    record.timestamp)`) — no further decryption needed, the text was already inside the encrypted
    index blob. For `kind == "image"`, read+decrypt `root.appendingPathComponent(
    "images").appendingPathComponent(imageFilename)` the same guarded way; if THAT specific image
    file is missing or fails to decrypt, `compactMap` drops just that one record (return `nil` from
    the closure) rather than blanking the entire load — one bad image must not destroy the rest of
    the history.

    `static func save(_ items: [ClipboardItem], root: URL, key: SymmetricKey) throws` — first
    `try FileManager.default.createDirectory(at: root.appendingPathComponent("images",
    isDirectory: true), withIntermediateDirectories: true)`. Then, for every `.image` item in
    `items`, encrypt its `Data` and write it to `images/<id.uuidString>.enc` — do this BEFORE
    writing the index (Pitfall 3: a mid-save crash must never leave a referenced-but-missing
    file). Then build `[ClipboardItemRecord]` from `items` per the field rules above, `JSONEncoder
    ().encode(records)`, encrypt the WHOLE encoded blob as one unit (never per-field — Pitfall 4's
    sibling anti-pattern), write the result to `root.appendingPathComponent("index.json.enc")`.
    FINALLY, run the D-06 orphan sweep: list `root.appendingPathComponent("images")`'s current
    directory contents via `FileManager.default.contentsOfDirectory(at:includingPropertiesForKeys:)`,
    compute the set of filenames that SHOULD exist from the just-saved `items` array (never from
    what was previously on disk — Pitfall 3's exact warning), and for every on-disk filename NOT in
    that set, call `deleteOrphanedImageFile(at:root:)` below.

    `static func deleteOrphanedImageFile(at fileURL: URL, root: URL)` — internal, NOT `private`
    (mirrors `ShelfFileStore.deleteSessionCopy`'s access level exactly so the test file can call it
    directly). Standardize both `fileURL` and `root` via `.standardizedFileURL`, `guard
    fileURL.path.hasPrefix(root.path + "/") else { return }` (exact copy of `ShelfFileStore
    .deleteSessionCopy`'s guard shape — see <interfaces> above), then `try?
    FileManager.default.removeItem(at: fileURL)` — non-throwing, idempotent, silent no-op outside
    the root or on an already-gone file.

    Two `private static func` helpers implementing the seal/open round trip exactly as shown in
    <interfaces> above: `encrypt(_ plaintext: Data, using key: SymmetricKey) throws -> Data` (seals,
    unwraps `.combined`, throws `ClipboardFileStoreError.sealFailed` if nil — defensive only) and
    `decrypt(_ combined: Data, using key: SymmetricKey) throws -> Data` (`SealedBox(combined:)` then
    `AES.GCM.open`). NEVER pass an explicit `nonce:` argument to `seal` anywhere in this file — let
    CryptoKit generate a fresh random nonce every call.

    Create `IsletTests/ClipboardFileStoreTests.swift`: `import XCTest`, `@testable import Islet`,
    `import CryptoKit`. Header comment in `ShelfFileStoreTests.swift`'s style — cite "Phase 56",
    state this covers CLIP-04 (SC#1 round-trip) and PRIV-02 (SC#2 plaintext-absence, SC#3
    delete-path hardening) plus D-04/D-06, and uses the same fixturesDir setUp/tearDown deviation
    as `ShelfFileStoreTests` (real disk I/O needs a throwaway root). `final class
    ClipboardFileStoreTests: XCTestCase` with a `fixturesDir: URL!` property set up/torn down
    exactly like `ShelfFileStoreTests` (rename the fixture folder to
    `ClipboardFileStoreTestsFixtures-\(UUID())`). Add `let testKey = SymmetricKey(size: .bits256)`
    constructed fresh in each test that needs one (no Keychain involvement in this test file — the
    key is passed directly as a parameter, proving `ClipboardFileStore` needs no Keychain access to
    be testable). Implement the 6 behaviors listed above as 6 separate `func test...()` methods, one
    behavior per test, no shared fixture beyond `fixturesDir`.

    After creating both files, run `xcodegen generate` from the repo root.
  </action>
  <verify>
    <automated>cd /Users/lippi304/conductor/workspaces/notch/algiers && xcodegen generate && xcodebuild build-for-testing -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "TEST BUILD SUCCEEDED" && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "enum ClipboardFileStore" Islet/Clipboard/ClipboardFileStore.swift` matches
    - `grep -q "struct ClipboardItemRecord: Codable" Islet/Clipboard/ClipboardFileStore.swift` matches
    - `grep -q "static func storageRoot() -> URL" Islet/Clipboard/ClipboardFileStore.swift` matches
    - `grep -q "static func load(root: URL, key: SymmetricKey) -> \[ClipboardItem\]" Islet/Clipboard/ClipboardFileStore.swift` matches
    - `grep -q "static func save(_ items: \[ClipboardItem\], root: URL, key: SymmetricKey) throws" Islet/Clipboard/ClipboardFileStore.swift` matches
    - `grep -q "static func deleteOrphanedImageFile" Islet/Clipboard/ClipboardFileStore.swift` matches, and `grep -q "private static func deleteOrphanedImageFile" Islet/Clipboard/ClipboardFileStore.swift` does NOT match (must be internal, not private, for test access)
    - `grep -q "hasPrefix(root.path + \"/\")" Islet/Clipboard/ClipboardFileStore.swift` matches (D-06/SC#3 guard)
    - `grep -c "nonce:" Islet/Clipboard/ClipboardFileStore.swift` returns 0 (never pass an explicit nonce)
    - `grep -c "func test" IsletTests/ClipboardFileStoreTests.swift` returns 6 or more
    - `xcodebuild build-for-testing -scheme Islet -destination 'platform=macOS' -configuration Debug` reports `** TEST BUILD SUCCEEDED **`
    - `xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug` reports `BUILD SUCCEEDED`
    - Manual Cmd-U run in Xcode (headless `xcodebuild test` hangs in this repo — pre-existing
      Bluetooth TCC-authorization wait during the full-app test-host boot, PROJECT.md-documented;
      the build-for-testing gate above is the automated proof, test EXECUTION is verified via
      Xcode's Test Navigator) shows all 6 `ClipboardFileStoreTests` passing
  </acceptance_criteria>
  <done>ClipboardFileStore.swift + ClipboardItemRecord exist; load/save round-trip via injectable root+key; D-04 graceful degradation returns [] on any decrypt/decode failure (corrupted file, missing file, or wrong key); D-06 orphan-image diff-and-delete runs on every save, computed against the incoming items array; deleteOrphanedImageFile hardened under storageRoot (mirrors ShelfFileStore); ClipboardFileStoreTests.swift covers all 6 behaviors; TEST BUILD SUCCEEDED, BUILD SUCCEEDED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Disk (`<AppSupport>/IsletClipboard/`) → `ClipboardFileStore.load` | Untrusted-at-rest boundary — a corrupted, hand-edited, or tampered `index.json.enc`/`images/*.enc` file is treated as attacker-or-corruption-controlled input on every launch |
| Keychain → `KeychainClipboardKeyStore.read` | Local machine's Keychain, gated by the OS's own ACL/entitlement model; this phase treats a missing/reset key identically to a corrupted file (D-04), never a special-cased crash |
| `ClipboardStore.items` → `ClipboardFileStore.save` | App-internal only in this phase — every `ClipboardItem` originates from the app's own in-memory Phase-55 store, never an externally-supplied file path or user-controlled filename (RESEARCH.md's V5 Input Validation note) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | Tampering | `ClipboardFileStore.deleteOrphanedImageFile` | mitigate | `hasPrefix(root.path + "/")` guard on both `.standardizedFileURL`-normalized paths before any `removeItem` call — exact mirror of `ShelfFileStore.deleteSessionCopy` (SC#3); a URL outside the storage root is a silent no-op, never an error |
| T-56-02 | Information Disclosure | `KeychainClipboardKeyStore.write` | mitigate | `kSecAttrSynchronizable = false` set explicitly on every Keychain write — the AES key never syncs via iCloud Keychain to another device (D-05, explicit user decision) |
| T-56-03 | Tampering / Information Disclosure | `ClipboardFileStore`'s `encrypt`/`decrypt` helpers | mitigate | No explicit `nonce:` argument is ever passed to `AES.GCM.seal` — CryptoKit generates a fresh random nonce every call, preventing nonce reuse under the same key from breaking GCM's authentication guarantee; enforced by an acceptance-criteria grep (`nonce:` count == 0) |
| T-56-04 | Tampering | `ClipboardFileStore.load` | mitigate | GCM's built-in authentication tag causes `AES.GCM.open` to throw on any tamper; `load`'s full `try?`-chained guard treats ANY thrown error as "empty history," never a partial/corrupted read (D-04) |
| T-56-05 | Denial of Service | `ClipboardFileStore.load` | mitigate | A single corrupted file (index or one image) never crashes the app — the index-level failure returns `[]` entirely (D-04), and a single bad image record is dropped individually via `compactMap` rather than blanking the whole load |
| T-56-06 | Information Disclosure | On-disk `index.json.enc` / `images/*.enc` | mitigate | Whole-blob AES-GCM encryption of both the JSON index and every image file (PRIV-02) — verified directly by SC#2's raw-byte plaintext-absence unit test |

No package-manager installs occur in this phase (`CryptoKit`/`Security`/`Foundation` are all
first-party system frameworks, zero new dependencies) — the Package Legitimacy Gate does not
apply; `T-{phase}-SC` is omitted accordingly, matching Phase 55's own precedent.

</threat_model>

<verification>
1. `xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS' -configuration Debug` reports `BUILD SUCCEEDED` after both tasks.
2. `xcodebuild build-for-testing -scheme Islet -destination 'platform=macOS' -configuration Debug` reports `** TEST BUILD SUCCEEDED **`.
3. Manual Cmd-U in Xcode (headless `xcodebuild test` hangs in this repo — PROJECT.md-documented Bluetooth TCC wait) runs `ClipboardFileStoreTests`, all 6 green.
4. `grep -rn "nonce:" Islet/Clipboard/ClipboardFileStore.swift` returns no matches — confirms no manual nonce handling anywhere.
5. `grep -q "kSecAttrSynchronizable" Islet/Clipboard/KeychainClipboardKeyStore.swift` confirms D-05's guard is present.
6. `grep -rn "AppKit\|SwiftUI\|Cocoa\|NSPasteboard" Islet/Clipboard/ClipboardFileStore.swift Islet/Clipboard/KeychainClipboardKeyStore.swift` returns no matches beyond documentation comments (both files are FileManager/Keychain/CryptoKit only).
</verification>

<success_criteria>
- Saving then reloading via `ClipboardFileStore` against the same injectable root reproduces the same items in the same order (ROADMAP Phase 56 SC#1, CLIP-04).
- The on-disk JSON index and image files are encrypted via `CryptoKit.AES.GCM` with a Keychain-stored key — inspecting the raw file shows no readable plaintext content (ROADMAP SC#2, PRIV-02).
- Deleting an item's on-disk data validates the delete target lives under the storage root before removing anything, mirroring `ShelfFileStore`'s hardened delete pattern (ROADMAP SC#3, PRIV-02).
- `xcodebuild build -scheme Islet` reports `BUILD SUCCEEDED`; `xcodebuild build-for-testing -scheme Islet` reports `TEST BUILD SUCCEEDED`.
- Manual Cmd-U confirms all 6 `ClipboardFileStoreTests` pass.
- ROADMAP SC#4 (real kill-and-restart) is explicitly OUT of scope for this plan — closed by Plan 56-02.
</success_criteria>

<output>
Create `.planning/phases/56-encrypted-persistence/56-01-SUMMARY.md` when done
</output>
