---
phase: 41-calendar-countdown-hud
plan: 04
type: execute
wave: 3
depends_on: ["41-02", "41-03"]
files_modified: []
autonomous: false
requirements: [HUD-08]

must_haves:
  truths:
    - "Within 1 hour of a real calendar event's start time, the collapsed pill shows a calendar icon (left) and a live minutes-remaining countdown (right) that updates continuously without user interaction (ROADMAP Success Criterion 1)"
    - "The countdown HUD dismisses at (or shortly after) the event's start time, using its own scheduling — never the shared 3s activityDuration auto-dismiss (ROADMAP Success Criterion 2)"
    - "The countdown timer is scheduled to actual relevant instants rather than a tight poll loop, verified via Activity Monitor's Idle Wake Ups column showing no regression (ROADMAP Success Criterion 3, PITFALLS.md Pitfall 7)"
    - "The countdown participates in IslandResolver as an ambient single-winner activity, correctly yielding to any higher-priority Charging/Device transient, and re-arms immediately for a back-to-back event (ROADMAP Success Criterion 4, D-09)"
  artifacts: []
  key_links: []
---

<objective>
Confirm on real hardware, with a real calendar and real wall-clock time, that Plans 01-03's
code-complete implementation actually satisfies all 4 of Phase 41's ROADMAP success criteria —
none of which are automatable (real EventKit event, real timing, Activity Monitor).

Purpose: this is the phase's hard merge gate. Everything up to this point is unit-tested/build-
verified in isolation; this plan is where the whole pipeline (EventKit -> CalendarCountdownMonitor
-> IslandResolver -> NotchPillView) is proven to actually work end-to-end, with no idle-wakeup
regression, on the developer's own machine.
Output: an approved on-device UAT checkpoint, closing HUD-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-calendar-countdown-hud/41-CONTEXT.md
@.planning/phases/41-calendar-countdown-hud/41-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Debug build + full Cmd-U gate</name>
  <files></files>
  <read_first>
    .planning/phases/41-calendar-countdown-hud/41-VALIDATION.md (Test Infrastructure + Sampling
    Rate sections — confirms `xcodebuild ... build`, never `test`, plus the manual Cmd-U full-suite
    requirement before any on-device checkpoint)
  </read_first>
  <action>
    Run the Debug build gate, then open the project in Xcode and run the full `IsletTests` suite
    via Cmd-U (never `xcodebuild test` — project memory: it hangs headless because `IsletTests` is
    hosted inside the full `Islet.app`, which boots the `NSPanel`/`MediaRemote`/`IOBluetooth`
    stack). Confirm every test from Plans 01-03 is green (the 3 new `CalendarGlanceTests`
    functions, the 6 new `nextUpcomingEvent` cases, and the 3 new `IslandResolverTests` functions)
    alongside zero regressions in the full pre-existing suite.
  </action>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build</automated>
  </verify>
  <acceptance_criteria>
    - Debug build exits 0
    - Manual Cmd-U run in Xcode reports 0 failures across the full `IsletTests` target
  </acceptance_criteria>
  <done>Debug build is green and the full test suite (including every Phase 41 addition) passes via Cmd-U.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Consolidated on-device UAT — all 4 ROADMAP Success Criteria</name>
  <files>Islet/Notch/CalendarCountdownMonitor.swift, Islet/Notch/IslandResolver.swift, Islet/Notch/NotchPillView.swift, Islet/Notch/NotchWindowController.swift, Islet/Calendar/CalendarGlance.swift, Islet/Calendar/CalendarService.swift, Islet/ActivitySettings.swift, Islet/SettingsView.swift</files>
  <action>No code changes in this task — verification-only, running the complete Calendar Countdown HUD end-to-end on real hardware with a real calendar event.</action>
  <what-built>
    The Calendar Countdown HUD (HUD-08): starting 1 hour before a real calendar event, the
    collapsed pill shows a calendar icon (left) and a live mm:ss countdown (right), orange until
    the final minute then red, driven by `CalendarCountdownMonitor`'s deadline-scheduled (never
    polling) timer — dismissing at the event's start and immediately re-arming for a back-to-back
    event (D-09). The countdown always wins the ambient slot over Now-Playing (D-01) and always
    yields to a standing Charging/Device/Focus/OSD transient. A default-ON "Calendar Countdown"
    toggle in Settings > Activities turns it off live.
  </what-built>
  <how-to-verify>
    1. Open System Settings (or the macOS Calendar app) and create a real calendar event starting
       ~3-5 minutes from now, in a calendar Islet has access to.
    2. Watch the notch: within the hour-before window the countdown should already be armed, but
       for a fast check, confirm the pill switches to the countdown wing once the event enters the
       1-hour window (or wait for a fresh 3-5 minute-out event to trigger it immediately if the
       monitor's arm-instant math is being spot-checked). Confirm the calendar icon sits on the
       LEFT, the `mm:ss` countdown sits on the RIGHT, and the countdown visibly ticks down every
       second without any interaction (Success Criterion 1).
    3. Confirm the countdown text and icon are ORANGE while more than 60 seconds remain, and both
       switch to RED together (not one before the other) once fewer than 60 seconds remain (D-05).
    4. Confirm the event's TITLE is never shown anywhere on the collapsed pill (D-06) — only the
       icon and the time.
    5. Let the event reach its start time. Confirm the countdown wing dismisses at/shortly after
       that instant — NOT after a flat 3 seconds the way Charging/Device splashes do (Success
       Criterion 2).
    6. Schedule two back-to-back events (event B starting within 1 hour of event A's start) ahead
       of time. Let event A's countdown dismiss at its start time and confirm the pill immediately
       re-arms showing event B's countdown, with no idle gap in between (D-09).
    7. While a countdown is showing, plug in the charger (or connect/disconnect a Bluetooth
       device). Confirm the Charging/Device splash takes over immediately, and the countdown
       resumes correctly once the splash dismisses (Success Criterion 4, D-01's yield behavior).
    8. Click the countdown wing — confirm it expands to Home (D-07), matching Now-Playing's
       existing ambient click behavior. Hover the wing — confirm nothing extra appears (D-08).
    9. Open Activity Monitor, select Islet, and watch the Idle Wake Ups column for ~2-3 minutes
       WITH NO event imminent (delete/let all test events pass, or check before creating any).
       Confirm no measurable elevation compared to Islet's normal idle baseline — proving the
       monitor is deadline-scheduled, not polling (Success Criterion 3, PITFALLS.md Pitfall 7).
    10. In Settings > Activities, toggle "Calendar Countdown" off while a countdown is showing —
        confirm it disappears immediately (live toggle-off). Toggle it back on and confirm it
        resumes correctly for any still-qualifying event.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (per step number above)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Real EventKit calendar data -> full pipeline | The end-to-end path (EventKit -> CalendarCountdownMonitor -> IslandResolver -> NotchPillView) is exercised with real, potentially attacker-influenced (subscribed-calendar) event data for the first time this phase — this checkpoint is the final confirmation that no title/string data leaks onto the collapsed pill anywhere along that path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-41-09 | Information Disclosure | Full pipeline, real calendar data | mitigate | Checkpoint step 4 is an explicit on-device confirmation that no event title ever renders on the collapsed pill — the last verification layer on top of Plan 01's structural guarantee (no title field) and Plan 03's view-layer guarantee (no title-rendering Text) |
| T-41-10 | Denial of Service (minor) | `CalendarCountdownMonitor`'s real-world scheduling | mitigate | Checkpoint step 9 is the authoritative Nyquist verification PITFALLS.md Pitfall 7 requires — Activity Monitor Idle Wake Ups with no imminent event, closing the loop Plans 01/02's static code-review-level mitigations could only partially prove |
| T-41-SC | Tampering | npm/pip/cargo installs | not applicable | Zero external dependencies added across the whole phase |
</threat_model>

<verification>
Debug build + full Cmd-U suite green (Task 1), then the 10-step on-device checkpoint (Task 2)
covering all 4 ROADMAP Success Criteria plus every locked CONTEXT.md decision (D-01, D-03, D-05
through D-09) that has no automated test coverage. This is the phase's hard merge gate — mirrors
the established precedent of Phase 29/35/38/39's own consolidated on-device UAT plans.
</verification>

<success_criteria>
- All 4 ROADMAP Success Criteria for Phase 41 are confirmed true on real hardware.
- No Activity Monitor Idle Wake Ups regression with no imminent event.
- HUD-08 is fully shipped; no known gaps remain.
</success_criteria>

<output>
Create `.planning/phases/41-calendar-countdown-hud/41-04-SUMMARY.md` when done
</output>
