---
phase: 38-focus-mode-hud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/FocusDetectionSpike.swift
  - Islet/AppDelegate.swift
autonomous: false
requirements: [HUD-05]

must_haves:
  truths:
    - "An on-device spike confirms and records which detection path is used (INFocusStatusCenter vs. Assertions.json+FDA polling) before any FocusModeMonitor implementation proceeds (ROADMAP Success Criterion #1)"
    - "If neither detection path is viable, this is recorded as a D-12 descope decision, not silently assumed"
  artifacts:
    - path: "Islet/FocusDetectionSpike.swift"
      provides: "DEBUG-only throwaway spike that probes both detection paths and prints go/no-go signal to console"
      exports: ["runFocusDetectionSpike"]
  key_links:
    - from: "Islet/AppDelegate.swift"
      to: "Islet/FocusDetectionSpike.swift"
      via: "applicationDidFinishLaunching DEBUG-gated call to runFocusDetectionSpike()"
      pattern: "runFocusDetectionSpike\\(\\)"
---

<objective>
Run the phase's mandatory go/no-go spike (ROADMAP Success Criterion #1, D-12) BEFORE any real
implementation: confirm on THIS dev machine (macOS 26/Tahoe) whether `INFocusStatusCenter`
("Path A") or `~/Library/DoNotDisturb/DB/Assertions.json` + Full Disk Access ("Path B") is the
viable Focus/DND detection mechanism, or whether neither is viable (D-12 clean descope, same
precedent as Phase 37's abandonment).

Purpose: 38-RESEARCH.md's Architecture Patterns §1 rates Path A as a near-certain structural
dead end (gated behind the Communication Notifications capability, which Islet has no
legitimate basis to add) and Path B as the likely winner — but this is a prediction, not a
confirmed fact, and every downstream plan (Monitor, Controller wiring, Settings UI) depends on
knowing which path actually won on this machine.
Output: `Islet/FocusDetectionSpike.swift` (DEBUG-only, throwaway — deleted in 38-07 once the
path is locked into `FocusModeMonitor.swift`), a recorded go/no-go decision in this plan's
SUMMARY.md that Plan 38-03 reads before writing `FocusModeMonitor.swift`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/38-focus-mode-hud/38-CONTEXT.md
@.planning/phases/38-focus-mode-hud/38-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the throwaway detection-path spike</name>
  <files>Islet/FocusDetectionSpike.swift, Islet/AppDelegate.swift</files>
  <read_first>
    - Islet/Notch/PowerSourceMonitor.swift (file header comment, lines 1-22) — this project's
      convention for isolating a single system-glue probe in its own file with a clear "THIS is
      the risky part" header comment
    - Islet/Notch/NotchWindowController.swift lines ~1994-2009 — the existing `#if DEBUG`
      hand-seed block convention (DEBUG-only code that never compiles into Release) to mirror
    - Islet/AppDelegate.swift — read in full to find `applicationDidFinishLaunching` and confirm
      exact insertion point for a one-line DEBUG-gated spike call
    - 38-RESEARCH.md "Architecture Patterns §1" — the two exact code blocks (Path A ~15 lines,
      Path B file-read snippet) this task must implement essentially verbatim
  </read_first>
  <action>
    Create `Islet/FocusDetectionSpike.swift`, its ENTIRE contents wrapped in `#if DEBUG` /
    `#endif` (never compiles into Release — mirrors the NotchWindowController DEBUG hand-seed
    block's discipline). Define `func runFocusDetectionSpike()` containing two independent
    probes, each printing a clearly labeled line so console output is unambiguous:
    Probe A (Path A, per RESEARCH §1's exact snippet): `import Intents` at the top of the file;
    print `INFocusStatusCenter.default.authorizationStatus` BEFORE requesting, then call
    `INFocusStatusCenter.default.requestAuthorization { status in ... }` and print the
    resulting `status`; if `status == .authorized`, also print
    `INFocusStatusCenter.default.focusStatus.isFocused`. Label every print with
    `"[FocusSpike][PathA]"` prefix.
    Probe B (Path B): attempt `FileManager.default.contents(atPath:
    NSString(string: "~/Library/DoNotDisturb/DB/Assertions.json").expandingTildeInPath)`; print
    whether the read returned `nil` (not granted / file absent) or `Data` (granted — also
    attempt a `JSONSerialization.jsonObject` parse and print whether `data[0].storeAssertionRecords`
    parses and whether it is empty or non-empty). Label every print with
    `"[FocusSpike][PathB]"` prefix. Never force-unwrap any JSON access (RESEARCH's defensive-
    parsing requirement applies even to this throwaway spike — a malformed read must print
    a "parse failed" line, not crash).
    In `Islet/AppDelegate.swift`, inside `applicationDidFinishLaunching(_:)`, add ONE line
    wrapped in `#if DEBUG` / `#endif` calling `runFocusDetectionSpike()` — do not call it from
    anywhere else, and do not gate it behind any toggle (this is a one-time throwaway probe, not
    a shipped feature).
  </action>
  <acceptance_criteria>
    - `Islet/FocusDetectionSpike.swift` exists, its entire contents (including the `import
      Intents`) are between a single top-level `#if DEBUG` and `#endif`
    - `grep -c "#if DEBUG" Islet/FocusDetectionSpike.swift` returns exactly `1` and the file has
      a matching single `#endif`
    - `runFocusDetectionSpike()` is called exactly once, from `Islet/AppDelegate.swift`'s
      `applicationDidFinishLaunching(_:)`, wrapped in its own `#if DEBUG`/`#endif`
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Release 2>&1 | grep -i "runFocusDetectionSpike"` returns NO matches (confirms the DEBUG guard strips it from Release)
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` succeeds
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <done>Debug build compiles clean with the spike wired to launch; Release build does not contain the spike symbol.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device detection-path go/no-go</name>
  <files>Islet/FocusDetectionSpike.swift, Islet/AppDelegate.swift</files>
  <action>No code changes in this task — verification-only, running Task 1's spike on real hardware and recording which detection path (if any) is viable.</action>
  <what-built>
    A DEBUG-only spike that, on launch, checks (A) whether `INFocusStatusCenter` can ever reach
    `.authorized` for this app, and (B) whether Islet's own process can read
    `~/Library/DoNotDisturb/DB/Assertions.json` (i.e. whether Full Disk Access has already been
    granted to Islet, or what the unauthorized-read behavior looks like).
  </what-built>
  <how-to-verify>
    1. Open the project in Xcode and Build and Run the Islet scheme (Cmd-R) in Debug configuration.
    2. Open Xcode's Debug Console (View -> Debug Area -> Activate Console) and find the lines
       prefixed `[FocusSpike][PathA]` and `[FocusSpike][PathB]`.
    3. Toggle macOS Focus/Do Not Disturb ON via Control Center, then check the console again —
       Path A's `requestAuthorization` may trigger a system permission dialog; note whether one
       appears at all.
    4. Report back exactly:
       - Path A: what `authorizationStatus`/`requestAuthorization` result printed (e.g.
         `.denied`, `.authorized`, or no dialog ever appeared).
       - Path B: whether the file read returned `nil` or real `Data`, and if `Data`, whether
         `storeAssertionRecords` was present/empty/non-empty.
       - Your overall call: `path-a` (Path A is `.authorized` and usable), `path-b` (Path A is
         not authorized, but Path B's read either already works or is a well-understood "grant
         Full Disk Access manually" case), or `descope` (neither path shows any viable signal —
         D-12 applies).
  </how-to-verify>
  <resume-signal>Reply with one of: "path-a", "path-b", or "descope", plus the console output you saw for both probes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Islet process <-> `~/Library/DoNotDisturb/DB/Assertions.json` | An Apple-internal, undocumented file whose schema is not a public contract — untrusted-shape data crossing into this process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01-01 | Denial of Service | `FocusDetectionSpike.swift` Probe B JSON parse | mitigate | No force-unwraps anywhere in the probe; a malformed/absent file prints a "parse failed" line instead of crashing (Task 1 acceptance criteria) |
| T-38-01-02 | Elevation of Privilege | `INFocusStatusCenter.requestAuthorization` call | accept | This spike is DEBUG-only, never ships in Release (verified by Task 1's Release-build grep check), and requests the SAME authorization the real feature would eventually request — no broader ask than the shipped feature will make |

This plan ships zero production code (everything is `#if DEBUG`-gated and deleted in 38-07) — the
threat surface is limited to "does the throwaway probe crash," which the mitigation above covers.
</threat_model>

<verification>
Debug build succeeds with the spike wired; Release build excludes it; the human-verify checkpoint
records a definitive path decision (`path-a` / `path-b` / `descope`) that Plan 38-03 reads before
proceeding.
</verification>

<success_criteria>
The spike runs on-device and produces an unambiguous go/no-go signal for both detection paths,
recorded in this plan's SUMMARY.md, satisfying ROADMAP Phase 38 Success Criterion #1.
</success_criteria>

<output>
Create `.planning/phases/38-focus-mode-hud/38-01-SUMMARY.md` when done. MUST record the exact
console output for both probes and the final `path-a` / `path-b` / `descope` decision — Plan
38-03 depends on reading this file before writing `FocusModeMonitor.swift`.
</output>
