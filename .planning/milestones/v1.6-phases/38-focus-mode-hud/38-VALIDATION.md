---
phase: 38
slug: focus-mode-hud
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-17
---

# Phase 38 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | XCTest (existing `IsletTests` target) |
| **Config file** | `Islet.xcodeproj` (generated by `project.yml` via XcodeGen) — no separate test config |
| **Quick run command** | `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` |
| **Full suite command** | Manual Cmd-U in Xcode (per project memory `xcodebuild-test-headless-hang` — `xcodebuild test` hangs because tests host the full `Islet.app`, which boots `NSPanel`/MediaRemote/IOBluetooth) |
| **Estimated runtime** | ~30s (build gate); manual Cmd-U untimed |

---

## Sampling Rate

- **After every task commit:** Run `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug`
- **After every plan wave:** Manual Cmd-U in Xcode for the full `IsletTests` suite
- **Before `/gsd:verify-work`:** Full suite green (manual Cmd-U) + on-device spike go/no-go recorded + fresh-install-no-FDA UAT checkpoint passed
- **Max feedback latency:** ~30s (build gate); manual suite run is developer-paced

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 38-01-0X | 01 | 0 | HUD-05 | — | `resolve()` returns `.focus` when Focus transient active and NOT expanded | unit | Cmd-U → `IslandResolverTests` (new methods) | ❌ Wave 0 | ⬜ pending |
| 38-01-0X | 01 | 0 | HUD-05 | — | `resolve()` falls through normally to Tray/Calendar/Weather/Home when expanded | unit | Cmd-U → `IslandResolverTests` | ❌ Wave 0 | ⬜ pending |
| 38-0X-0X | TBD | 0 | HUD-05 | — | Focus transient survives past the uniform 3s `activityDuration` with no self-dismiss (D-06) | unit/integration | Cmd-U → `TransientQueue`/`NotchWindowController`-adjacent test — single most important new test this phase adds | ❌ Wave 0 | ⬜ pending |
| 38-0X-0X | TBD | 0 | HUD-05 | — | Charging/Device preempts a standing Focus head immediately; Focus resumes once cleared (D-08) | unit | Cmd-U → new `TransientQueue`/`IslandResolverTests` test | ❌ Wave 0 | ⬜ pending |
| 38-0X-0X | TBD | 0 | HUD-05 | — | Focus Off removes the pill silently, no separate HUD moment (D-09) | unit | Cmd-U → exercise `removeAll(where:)` with a `.focus` predicate | ❌ Wave 0 | ⬜ pending |
| 38-0X-0X | TBD | 0 | HUD-05 | — | Settings toggle status hint maps state correctly ("Permission needed" vs. "Active", D-05) | unit (pure mapping fn) + manual (visual) | Cmd-U → new test in `ActivitySettingsTests.swift`, if mapping is factored into a pure function | ❌ Wave 0 | ⬜ pending |
| — | — | — | HUD-05 | — | Fresh install, FDA never granted / `INFocusStatusCenter` denied → no crash, no spin, silently inert | manual-only | — | On-device UAT checkpoint | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `IsletTests/IslandResolverTests.swift` — extend with new methods covering the `.focus` `where !isExpanded` guard, non-self-dismissal timing, and Charging/Device preemption behavior (no new test file — mirrors how Charging/Device tests already live there)
- [ ] `IsletTests/ActivitySettingsTests.swift` — extend with new methods for the Focus toggle key + permission-status mapping function, if introduced
- [ ] No new test framework/config needed — `IsletTests` target already exists and is wired

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| On-device detection-path spike (Path A `INFocusStatusCenter` quick-fail check → Path B FDA/`Assertions.json` check → D-12 go/no-go) | HUD-05 | Requires real macOS Focus/DND toggling and TCC/FDA state on physical hardware — not fabricatable in XCTest | Toggle Focus/DND on the build machine (macOS 26/Tahoe); run the spike's ~15-line `INFocusStatusCenter` test first, then the `Assertions.json` fallback if Path A fails; record which path wins in SUMMARY.md |
| Fresh install with FDA never granted (or `INFocusStatusCenter` denied) → no crash, no spin, no stuck state | HUD-05 | Requires a real un-granted TCC/permission state, not simulatable in unit tests | Fresh install (or reset TCC db entry) on the build machine; toggle Focus Mode HUD Settings on WITHOUT granting the requested permission; confirm app doesn't crash, spin, or silently poll forever; confirm toggle shows "Permission needed" hint |
| Visual/interaction check of the new Focus wing (Droppy-pill layout, moon-crescent icon, fixed color, collapsed-only takeover) | HUD-05 | Visual polish and hover/expand interaction feel cannot be asserted by XCTest | Toggle Focus on-device; verify pill matches Phase 36 Droppy-pill wing language; hover to expand island while Focus active — confirm Tray/Calendar/Weather/Now-Playing remain accessible and Focus state isn't shown expanded |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s (build gate)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
