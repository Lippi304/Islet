---
phase: 38-focus-mode-hud
plan: 04
type: execute
wave: 2
depends_on: ["38-01", "38-02"]
files_modified:
  - Islet/Notch/NotchPillView.swift
autonomous: true
requirements: [HUD-05]

must_haves:
  truths:
    - "The Focus wing visually matches Phase 36's Droppy-pill wing language: icon+label LEFT, status indicator RIGHT, reusing wingsShape() — no new shape/visual language invented (D-10)"
    - "The wing's icon and label render in a FIXED white color, never accent-tinted (D-11)"
    - "There is no 'Focus Off' render branch anywhere in this view — the wing either renders (Focus on) or is absent from the view tree entirely (D-09), matching 38-UI-SPEC.md's explicit warning against an if/else on/off branch inside the wing view"
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "focusWings(for:) private func + presentationSwitch .focus dispatch arm"
      contains: "private func focusWings(for activity: FocusActivity)"
  key_links:
    - from: "Islet/Notch/NotchPillView.swift presentationSwitch"
      to: "focusWings(for:)"
      via: "case .focus(let activity): focusWings(for: activity)"
      pattern: "case \\.focus\\(let activity\\): focusWings\\(for: activity\\)"
---

<objective>
Render the new `.focus(FocusActivity)` presentation as a wing view, mechanically reapplying
Phase 36's already-approved Droppy-pill wing language (per 38-UI-SPEC.md: "not a new design
pass"). This plan depends on Plan 38-02's pure types (`FocusActivity`,
`IslandPresentation.focus`) for its code, and on Plan 38-01's spike decision as a GATE only (see
prerequisite check below) — it has no dependency on `FocusModeMonitor`/`NotchWindowController`
wiring (Plan 38-05 feeds the real live state in later; this plan only needs the presentation
case to exist).

**Prerequisite check:** read `.planning/phases/38-focus-mode-hud/38-01-SUMMARY.md` before
starting. If it recorded a `descope` decision (D-12), STOP — do not execute this plan. Per
CONTEXT.md D-12, a full descope means "no half-built UI" ships; this wing view is user-facing
and must not land if the feature as a whole was abandoned, even though Plan 38-02's underlying
resolver plumbing may still stand as the phase's pipeline-proving evidence. Report to the
orchestrator that this plan (and 38-05/38-06/38-07) must not run.

Purpose: closes ROADMAP Success Criterion #2's visual half ("Toggling Focus/DND on shows the
new HUD in a generic Focus On state").
Output: `focusWings(for:)` wired into `NotchPillView.swift`'s existing `presentationSwitch`,
plus a SwiftUI preview entry mirroring the file's existing preview-per-case convention.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-focus-mode-hud/38-01-SUMMARY.md
@.planning/phases/38-focus-mode-hud/38-UI-SPEC.md
@.planning/phases/38-focus-mode-hud/38-PATTERNS.md
</context>

<interfaces>
`wingsShape(leftWidth:rightWidth:content:)` (NotchPillView.swift:1928-1951, already read in full
during planning) — the shared helper `focusWings(for:)` MUST call, verbatim signature:
```swift
private func wingsShape<Content: View>(
    leftWidth: CGFloat = Self.wingsSize.width / 2,
    rightWidth: CGFloat = Self.wingsSize.width / 2,
    @ViewBuilder content: () -> Content
) -> some View
```

Direct sibling to mirror structurally, `wings(for:)` (NotchPillView.swift:1958-1998, charging):
icon+label LEFT inside `HStack(spacing: 4)` with `.padding(.leading, 12)`, `Spacer()`, indicator
RIGHT with `.padding(.trailing, 14)`, whole content wrapped by `wingsShape(leftWidth:rightWidth:)`.

Constants to reuse verbatim (NotchPillView.swift:240, 263) — do not invent new sizing:
`Self.wingsSize = CGSize(width: 290, height: 32)`, `Self.wingsLabelWidth: CGFloat = 400`.

`presentationSwitch`'s existing dispatch (NotchPillView.swift:702-737, already read in full
during planning) — add the new arm between `.device` and `.nowPlayingWings`:
```swift
case .device(let d):
    deviceWings(for: d)                                              // D-02 rank 2 transient
case .nowPlayingWings(let p):
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: focusWings(for:) + presentationSwitch dispatch</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    - Islet/Notch/NotchPillView.swift lines 1928-2130 (wingsShape, wings(for:), deviceWings(for:)) — read this exact range before editing, confirms current byte-for-byte state
    - .planning/phases/38-focus-mode-hud/38-UI-SPEC.md "Focus Wing Contract" section — the full locked visual spec (icon `moon.fill`, label `"Focus"`, fixed white, 8x8pt green dot RIGHT, 12pt/14pt paddings)
  </read_first>
  <action>
    In `Islet/Notch/NotchPillView.swift`, add a new private function `focusWings(for activity:
    FocusActivity) -> some View` directly below `deviceWings(for:)` (mirrors the file's existing
    charging-then-device-then-next-wing ordering). Implementation: call `wingsShape(leftWidth:
    Self.wingsLabelWidth / 2, rightWidth: Self.wingsSize.width / 2)` (the wing's label is ALWAYS
    shown per D-10, unlike Charging/Device's conditional-width ternary — there is no negative
    state to size for, per D-09) with content: an `HStack(spacing: 0)` containing (a) an
    `HStack(spacing: 4)` with `Image(systemName: "moon.fill")` at `.font(.system(size: 13,
    weight: .semibold))` + `.symbolRenderingMode(.hierarchical)` + `.foregroundStyle(.white)`
    (D-11: literal `.white`, never `deviceAccent`/`chargingAccent`/any Environment accent), then
    `Text("Focus")` at `.font(.system(size: 12, weight: .semibold, design: .rounded))` +
    `.foregroundStyle(.white)`, wrapped with `.padding(.leading, 12)`; (b) `Spacer()`; (c)
    `Circle().fill(Color.green).frame(width: 8, height: 8)` with `.padding(.trailing, 14)` (a
    literal fixed green dot — NOT `BatteryIndicator`, which carries a percentage this HUD has no
    equivalent for). Do NOT add any `if`/`else`/ternary branching on the activity's state inside
    this function — `FocusActivity` has exactly one case (`.on`) per Plan 38-02, so the
    parameter exists only to match the `presentationSwitch` dispatch shape, not to drive a
    branch.
    In `presentationSwitch` (the `@ViewBuilder private var presentationSwitch` computed
    property), add `case .focus(let activity): focusWings(for: activity)` immediately after the
    existing `case .device(let d): deviceWings(for: d)` line and before `case .nowPlayingWings`.
  </action>
  <acceptance_criteria>
    - `grep -n "private func focusWings" Islet/Notch/NotchPillView.swift` returns exactly 1 match
    - `grep -n "case .focus(let activity): focusWings(for: activity)" Islet/Notch/NotchPillView.swift` returns exactly 1 match
    - `grep -n "moon.fill" Islet/Notch/NotchPillView.swift` returns exactly 1 match
    - `grep -A 20 "private func focusWings" Islet/Notch/NotchPillView.swift | grep -c "deviceAccent\|chargingAccent\|nowPlayingAccent"` returns `0` (confirms no accent-tinting leaked in, D-11)
    - `grep -A 20 "private func focusWings" Islet/Notch/NotchPillView.swift | grep -c "if \|? .*:.*"` returns `0` (confirms no on/off branch inside the wing view, per 38-UI-SPEC.md's explicit warning)
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <done>focusWings(for:) exists, is wired into presentationSwitch, matches Phase 36's wing language exactly, contains no accent-tinting and no on/off branch.</done>
</task>

<task type="auto">
  <name>Task 2: SwiftUI preview entry for .focus(.on)</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    - Islet/Notch/NotchPillView.swift lines 2660-2850 (the existing `#Preview` blocks for
      `.idle`, `.homeLastPlayed`, `.charging`, `.device`, `.nowPlayingWings`, etc.) — read this
      exact range to confirm the current preview-block boilerplate shape before adding a new one
  </read_first>
  <action>
    Add one new `#Preview` block mirroring the existing `.charging`/`.device` preview blocks'
    exact structure (same `NotchPillView(...)` constructor arguments, same
    `presentationState: IslandPresentationState(...)` pattern), passing
    `IslandPresentationState(.focus(.on))` as the presentation state. Insert it directly after
    the existing `.device(...)` preview block (mirrors this file's existing ordering: previews
    follow the same case order as `IslandPresentation`'s own declaration).
  </action>
  <acceptance_criteria>
    - `grep -c "IslandPresentationState(.focus(.on))" Islet/Notch/NotchPillView.swift` returns exactly 1
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` succeeds (confirms the preview block compiles)
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <done>A new SwiftUI preview renders the Focus wing in isolation, mirroring the file's existing per-case preview convention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — this plan renders an already-validated `FocusActivity` value (Plan 38-02's pure seam);
no external input, no file I/O, no network calls anywhere in this view code.

## STRIDE Threat Register

No threats apply to this plan. `focusWings(for:)` is a pure SwiftUI render function over an
already-typed, already-validated in-process value — there is no trust boundary for STRIDE to
model. (Explicitly stated per the phase's threat_model_requirement: "if no threats apply to a
given plan, state that explicitly with rationale.")
</threat_model>

<verification>
Debug build succeeds; the new preview renders without a runtime crash (spot-checked via Xcode
Canvas, not part of the automated gate); no accent-tinting or on/off branching present in the
new function.
</verification>

<success_criteria>
The Focus wing view exists, is wired into the resolver's presentation switch, and is visually
indistinguishable in construction from Phase 36's Charging/Bluetooth wing restyles — ready for
Plan 38-07's on-device visual UAT.
</success_criteria>

<output>
Create `.planning/phases/38-focus-mode-hud/38-04-SUMMARY.md` when done.
</output>
