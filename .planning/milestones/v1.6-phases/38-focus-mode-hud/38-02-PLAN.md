---
phase: 38-focus-mode-hud
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - Islet/Notch/FocusActivity.swift
  - IsletTests/FocusActivityTests.swift
  - Islet/Notch/IslandResolver.swift
  - IsletTests/IslandResolverTests.swift
autonomous: true
requirements: [HUD-05]

must_haves:
  truths:
    - "resolve() returns .focus(f) when a Focus transient is active and the island is NOT expanded (D-07)"
    - "resolve() does NOT return .focus when the island IS expanded — it falls through to whatever Tray/Calendar/Weather/Home would resolve to as if no transient were active (D-07)"
    - "A Charging or Device transient immediately preempts an already-standing Focus head instead of queuing behind it (D-08)"
    - "ActiveTransient.focus is marked persistent (isPersistent == true) while every other case is not — the seam the controller uses to skip the uniform 3s auto-dismiss (D-06)"
  artifacts:
    - path: "Islet/Notch/FocusActivity.swift"
      provides: "Pure FocusActivity enum + focusActivity(from:) TOTAL mapping function"
      exports: ["FocusActivity", "focusActivity(from:)"]
    - path: "Islet/Notch/IslandResolver.swift"
      provides: "ActiveTransient.focus case, IslandPresentation.focus case, ActiveTransient.isPersistent, TransientQueue.preempt(_:), the where-guarded resolve() switch case"
      contains: "case focus(FocusActivity)"
  key_links:
    - from: "Islet/Notch/IslandResolver.swift resolve(...)"
      to: "IslandPresentation.focus(FocusActivity)"
      via: "case .focus(let f) where !isExpanded: return .focus(f)"
      pattern: "case \\.focus\\(let f\\) where !isExpanded"
---

<objective>
Build the phase's actual pipeline-proving core (per 38-CONTEXT.md D-06's own framing: "giving
the new ActiveTransient case real state to arbitrate is the actual point of this phase") as
pure, Foundation-only, unit-tested logic — zero AppKit, zero system frameworks, zero detection-
path dependency. This plan is independent of Plan 38-01's spike outcome: the resolver/queue
mechanics for "collapsed-only" (D-07), "non-self-dismissing" (D-06), and "preemptible by
Charging/Device" (D-08) are the same regardless of which detection path eventually feeds
`FocusActivity` in.

Purpose: 38-RESEARCH.md's Architecture §4/§5 identifies two real, code-verified gaps in the
existing `TransientQueue`/`scheduleActivityDismiss()` machinery that a naive "just add a case"
implementation would silently break (Focus auto-dismissing after 3s; Charging/Device queuing
behind an indefinitely-standing Focus pill instead of preempting it). This plan closes both
gaps at the pure-logic layer, verified with dedicated failing-first tests.
Output: `FocusActivity.swift` (new pure model), `IslandResolver.swift` extended with the new
transient case, presentation case, persistence flag, and preemption method — all covered by
new XCTest methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-focus-mode-hud/38-CONTEXT.md
@.planning/phases/38-focus-mode-hud/38-RESEARCH.md
@.planning/phases/38-focus-mode-hud/38-PATTERNS.md
</context>

<interfaces>
<!-- Current IslandResolver.swift state (read in full during planning) — the exact lines this
plan extends. Executor should edit these in place, not re-derive them. -->

Current `ActiveTransient` (IslandResolver.swift:71-74):
```swift
enum ActiveTransient: Equatable {
    case charging(ChargingActivity)
    case device(DeviceActivity)
}
```

Current `IslandPresentation` transient cases (IslandResolver.swift:57-58):
```swift
case charging(ChargingActivity)                        // D-02 rank 1 transient
case device(DeviceActivity)                             // D-02 rank 2 transient
```

Current `resolve(...)`'s transient switch (IslandResolver.swift:103-107):
```swift
switch activeTransient {                              // D-04: transient wins even over expanded
case .charging(let a): return .charging(a)           // D-02 rank 1
case .device(let d):   return .device(d)             // D-02 rank 2
case nil: break
}
```

Current `TransientQueue.enqueue(_:)` (IslandResolver.swift:225-231) — the sibling method
`preempt(_:)` must be additive next to, mirroring its de-dup/bound discipline:
```swift
mutating func enqueue(_ t: ActiveTransient) -> Bool {
    if head == nil { head = t; return true }
    if head == t || pending.contains(t) { return false }   // D-03 dedup (head + pending)
    pending.append(t)
    if pending.count > maxDepth { pending.removeFirst() }   // D-03 bound (drop oldest pending)
    return false
}
```

Analog for the pure model file, `Islet/Notch/PowerActivity.swift` (header + shape, already
read during planning — file imports ONLY Foundation, defines a Reading struct, an Activity
enum, and a TOTAL `xActivity(from:)` mapping function with the same doc-comment discipline as
`Islet/Notch/DeviceActivity.swift`).
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: FocusActivity.swift pure seam</name>
  <files>Islet/Notch/FocusActivity.swift, IsletTests/FocusActivityTests.swift</files>
  <read_first>
    - Islet/Notch/PowerActivity.swift (full file) — the file-header comment convention ("imports
      ONLY Foundation... no system frameworks") and the TOTAL mapping-function shape
      (`powerActivity(from:)`) to mirror exactly
    - Islet/Notch/DeviceActivity.swift (full file) — a second sibling example, confirms the
      "plain values + total functions" discipline is consistent across this codebase
    - IsletTests/PowerActivityTests.swift — the existing plain-XCTest style for this exact
      class of pure-seam file, to mirror in FocusActivityTests.swift
  </read_first>
  <behavior>
    - `focusActivity(from: true)` returns `.on`
    - `focusActivity(from: false)` returns `nil` (mirrors `powerActivity(from:)`'s "nil is a
      legitimate no-op result" convention — there is no `.off` case, per D-09: Focus Off has no
      distinct rendered state, so there is nothing to represent)
  </behavior>
  <action>
    Create `Islet/Notch/FocusActivity.swift` with `import Foundation` only (no `Intents`, no
    `FileManager` — those belong in `FocusModeMonitor.swift`, Plan 38-03). Define
    `enum FocusActivity: Equatable { case on }` — deliberately the minimal single-case shape (no
    named-mode payload, per REQUIREMENTS.md's Out of Scope table; no `.off` case, per D-09).
    Define the TOTAL pure mapping function `func focusActivity(from isFocused: Bool) ->
    FocusActivity?` returning `.on` when `isFocused` is `true`, `nil` otherwise. Add a matching
    file-header doc comment in this codebase's established style (see PowerActivity.swift lines
    1-21) noting this is Phase 38 / HUD-05's pure seam and that `FocusModeMonitor.swift` (system
    glue) is the ONLY caller.
    Create `IsletTests/FocusActivityTests.swift` with a `final class FocusActivityTests:
    XCTestCase` containing the two RED-then-GREEN test methods from the `<behavior>` block above.
  </action>
  <acceptance_criteria>
    - `Islet/Notch/FocusActivity.swift` contains exactly `import Foundation` (no other imports)
    - `enum FocusActivity: Equatable` has exactly one case, `on`
    - `func focusActivity(from isFocused: Bool) -> FocusActivity?` is a total function (no
      `fatalError`/force-unwrap in its body)
    - `xcodebuild test -project Islet.xcodeproj -scheme Islet -destination 'platform=macOS' -only-testing:IsletTests/FocusActivityTests` is NOT run (project convention: `xcodebuild test` hangs — see Task verify below); the build gate + manual Cmd-U per the phase's own VALIDATION.md substitutes for it
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build-for-testing -project Islet.xcodeproj -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <done>FocusActivity.swift and FocusActivityTests.swift exist, both test methods pass under manual Cmd-U, build-for-testing succeeds.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: IslandResolver.swift — new transient case, persistence flag, preemption, collapsed-only guard</name>
  <files>Islet/Notch/IslandResolver.swift, IsletTests/IslandResolverTests.swift</files>
  <read_first>
    - Islet/Notch/IslandResolver.swift (already read in full during planning — re-read is
      unnecessary if this session already has it in context; otherwise read in full before
      editing, per this project's own "no re-reads but read before every edit" discipline)
    - IsletTests/IslandResolverTests.swift — read the existing `testChargingOutranksDeviceAndMedia`,
      `testEnqueueWhileShowingEnqueuesBehind`, `testAdvancePromotesPending`,
      `testRemoveAllMatchingHeadPromotesAndChangesHead` methods (the exact call/assert shape
      every new test below must mirror) and confirm the current line count before appending
  </read_first>
  <behavior>
    - `resolve(activeTransient: .focus(.on), ..., isExpanded: false)` returns `.focus(.on)`
    - `resolve(activeTransient: .focus(.on), ..., isExpanded: true, selectedView: .home,
      nowPlaying: .none, hasPlayedSinceLaunch: false)` returns `.homeEmpty` — i.e. IDENTICAL to
      what `resolve(activeTransient: nil, ...)` would return with the same other arguments
      (proves the `where` guard falls through cleanly, not to some new "focus expanded" branch)
    - `ActiveTransient.charging(...).isPersistent == false`, `.device(...).isPersistent ==
      false`, `.focus(.on).isPersistent == true`
    - Given a `TransientQueue` with `head == .focus(.on)`, calling
      `preempt(.charging(.charging(percent: 50)))` makes `.charging(...)` the new `head`
      immediately (return value `true`), and a subsequent `advance()` call promotes `.focus(.on)`
      back to `head` (proving it was pushed to the FRONT of `pending`, not the back)
  </behavior>
  <action>
    In `Islet/Notch/IslandResolver.swift`: add `case focus(FocusActivity)` to `ActiveTransient`
    (after `.device`). Add `case focus(FocusActivity)` to `IslandPresentation` (after
    `.device(DeviceActivity)`, before `.nowPlayingWings`), with a doc comment mirroring the
    existing "D-02 rank N transient" comments: `// Phase 38 / HUD-05: rank 3 transient,
    collapsed-only (D-07)`. In `resolve(...)`'s transient switch (currently lines 103-107),
    replace the 3-case switch with: `.charging`/`.device` unchanged, then add
    `case .focus(let f) where !isExpanded: return .focus(f)` followed by `case .focus: break`
    (falls through to the existing `isExpanded` branch below, completely unmodified) — this is
    the ONE load-bearing edit for D-07, expressed as a `where`-guarded case addition to the
    existing exhaustive switch, per 38-CONTEXT.md's Claude's Discretion note ("without
    duplicating the existing switch-statement structure"). Add a new computed property directly
    below the `ActiveTransient` enum: `var isPersistent: Bool { if case .focus = self { return
    true }; return false }` — the seam Plan 38-05's controller wiring reads to decide whether to
    arm the uniform 3s dismiss timer (D-06). Add a new method to `TransientQueue`, directly below
    `enqueue(_:)`: `mutating func preempt(_ t: ActiveTransient) -> Bool { guard case .focus =
    head else { return enqueue(t) }; let displaced = head!; head = t; pending.insert(displaced,
    at: 0); return true }` — additive, does not modify `enqueue(_:)` itself (D-08). Add a doc
    comment above `preempt(_:)` explaining it exists because Focus (per the new `isPersistent`
    flag) never self-elapses to naturally yield the head, so Charging/Device must force their
    way in rather than queue behind it.
    In `IsletTests/IslandResolverTests.swift`: append 4 new test methods under a new `// MARK:
    Phase 38 / HUD-05 — Focus transient (collapsed-only, persistent, preemptible)` section,
    covering the 4 `<behavior>` bullets above exactly (`testFocusWinsWhenCollapsed`,
    `testFocusFallsThroughWhenExpanded`, `testActiveTransientIsPersistentFlags`,
    `testPreemptPushesFocusToFrontOfPending`).
  </action>
  <acceptance_criteria>
    - `grep -n "case focus(FocusActivity)" Islet/Notch/IslandResolver.swift` returns 2 matches (one in `ActiveTransient`, one in `IslandPresentation`)
    - `grep -n "case .focus(let f) where !isExpanded" Islet/Notch/IslandResolver.swift` returns exactly 1 match
    - `grep -n "func preempt" Islet/Notch/IslandResolver.swift` returns exactly 1 match, and `grep -n "mutating func enqueue" Islet/Notch/IslandResolver.swift` still returns exactly 1 match with its body byte-identical to before this task (verify via reading the diff — `enqueue(_:)` must be untouched)
    - `grep -n "var isPersistent" Islet/Notch/IslandResolver.swift` returns exactly 1 match
    - All 4 new test methods exist in `IsletTests/IslandResolverTests.swift` and pass under manual Cmd-U
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` succeeds (confirms every other exhaustive `switch activeTransient`/`ActiveTransient` site in the codebase was NOT broken by the new case — if the build fails with a non-exhaustive-switch error anywhere else, that is expected and must be fixed as part of this task per RESEARCH.md Pitfall D, e.g. `showsSwitcherRow(for:)` should NOT need a change since Focus never reaches its `isExpanded`-only cases, but CONFIRM this by letting the compiler tell you, not by assuming)
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <done>IslandResolver.swift compiles with the new case/property/method; all new and pre-existing IslandResolverTests pass under manual Cmd-U; no other file needed a compiler-forced fix (or if one did, it was made and documented in SUMMARY.md).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None crossed by this plan — `FocusActivity.swift` and the `IslandResolver.swift` extensions are
pure Foundation-only value types and total functions with no external input, no file I/O, no
network, no system framework calls (Pitfall 6's own "pure arbiter" discipline is what this plan
IS).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-02-01 | Tampering | none | accept | No trust boundary exists in this plan's scope — `resolve(...)` and `TransientQueue` operate only on already-validated in-process Swift values, never on raw external/untrusted bytes (that boundary is Plan 38-03's `FocusModeMonitor`, covered in that plan's own threat model) |

No further threats apply — this plan is pure logic with no I/O.
</threat_model>

<verification>
Manual Cmd-U full suite green (existing + 6 new tests: 2 in FocusActivityTests, 4 in
IslandResolverTests); `xcodebuild build` Debug configuration succeeds with no non-exhaustive-
switch errors remaining anywhere in the codebase.
</verification>

<success_criteria>
The resolver/queue pipeline correctly expresses all three of Focus's genuinely new behaviors
(collapsed-only win, non-self-dismissal readiness via `isPersistent`, preemptibility via
`preempt(_:)`) as pure, deterministically-tested logic — satisfying ROADMAP Success Criterion #4
("routes through IslandResolver/TransientQueue like every other transient — no resolver
bypass") before any system-glue or view code exists.
</success_criteria>

<output>
Create `.planning/phases/38-focus-mode-hud/38-02-SUMMARY.md` when done.
</output>
