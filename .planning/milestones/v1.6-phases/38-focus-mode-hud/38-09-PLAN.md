---
phase: 38-focus-mode-hud
plan: 09
type: execute
wave: 1
depends_on: []
files_modified: [Islet/Notch/NotchWindowController.swift]
autonomous: false
requirements: [HUD-05]
gap_closure: true

must_haves:
  truths:
    - "With real macOS Focus/DND toggled ON then OFF, and Focus the sole standing HUD head with nothing else queued, the Focus pill disappears within one poll cycle (~2.5s) of turning off — with no unrelated hover/click/Now-Playing/Charging/Device event required to incidentally trigger the redraw"
    - "Toggle-OFF + prior authorization + relaunch does NOT show the Focus HUD (38-08's Scenario A, never yet run on-device)"
    - "First-grant flow (toggle ON -> Continue -> grant, no re-toggle/relaunch) starts the monitor and shows the Focus HUD on the next Focus/DND toggle (38-08's Scenario B, never yet run on-device)"
  artifacts:
    - path: "Islet/Notch/NotchWindowController.swift"
      provides: "handleFocusChange's off-branch calls renderPresentation()/updateVisibility() after flushTransients(.focus), mirroring handleSettingsChanged's tail"
  key_links:
    - from: "Islet/Notch/NotchWindowController.swift handleFocusChange(false)"
      to: "Islet/Notch/NotchWindowController.swift renderPresentation()/updateVisibility()"
      via: "direct call in the else branch, after flushTransients(.focus)"
      pattern: "flushTransients\\(\\.focus\\)[\\s\\S]{0,120}renderPresentation\\(\\)"
---

<objective>
Close the ONE new BLOCKER found by 38-VERIFICATION.md (re-verification after 38-08): `handleFocusChange(false)` flushes the Focus transient from the queue but never re-renders, so `presentationState.presentation` can stay frozen at `.focus(.on)` indefinitely after real Focus/DND turns off — violating ROADMAP Success Criterion #2 ("toggling off dismisses it") and CONTEXT.md's D-06/D-09.

Purpose: This is the last defect blocking HUD-05. The pure resolver/queue/view layer is already verified correct (38-VERIFICATION.md truths #1, #3, #4); this plan fixes exactly the missing render tail on one call site and nothing else.
Output: `handleFocusChange`'s off-branch mirrors `handleSettingsChanged`'s existing render tail; a Debug build gate passes; a single batched on-device UAT confirms this fix plus 38-08's two still-outstanding, never-yet-run scenarios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/38-focus-mode-hud/38-VERIFICATION.md
@.planning/phases/38-focus-mode-hud/38-REVIEW.md
@.planning/phases/38-focus-mode-hud/38-CONTEXT.md
@.planning/phases/38-focus-mode-hud/38-08-SUMMARY.md

Do NOT touch `IslandResolver.swift`, `TransientQueue`, or `NotchPillView.swift` — WR-01 (the `preempt(_:)` maxDepth bypass) is explicitly out of scope, carried forward as a non-blocking warning. Do NOT re-touch `activityEnabled(_:)` or `focusPermissionGranted()` — both already fixed and confirmed live by 38-08/38-VERIFICATION.md. Do NOT touch `SettingsView.swift` — no defect remains there.
</context>

<interfaces>
<!-- Exact current state of the two functions involved, and the sibling tail this plan mirrors. All read directly from live source at the line numbers below. -->

From Islet/Notch/NotchWindowController.swift:1593-1603 (function to fix):
```swift
private func handleFocusChange(_ isFocused: Bool) {
    if isFocused {
        guard let activity = focusActivity(from: true) else { return }
        let changed = transientQueue.enqueue(.focus(activity))
        if changed {
            presentTransientChange()
        }
    } else {
        flushTransients(.focus)
    }
}
```

From Islet/Notch/NotchWindowController.swift:1760-1766 (the sibling tail to mirror — end of `handleSettingsChanged()`, unconditionally run after every branch including its own `flushTransients(.focus)` call):
```swift
        // Re-render the resolver verdict (a forced-.none Now-Playing or a flushed transient may
        // have changed it) and route through the SOLE show/hide site.
        withAnimation(.spring(response: springResponse, dampingFraction: springDamping)) {
            renderPresentation()
        }
        updateVisibility()
```

From Islet/Notch/NotchWindowController.swift:350-351 (spring constants already in scope, used verbatim, do not redefine):
```swift
private let springResponse: Double = 0.6
private let springDamping: Double = 0.62
```

From Islet/Notch/NotchWindowController.swift:732-734 and :766+ (the two functions being called — read-only, unchanged by this plan):
```swift
private func renderPresentation() {
    presentationState.presentation = currentPresentation()
}
```
`updateVisibility()` is the sole show/hide site (comment at :1760-1761 documents this); no signature changes needed, called with no arguments exactly like every other call site.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the missing render tail to handleFocusChange's off-branch</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    - Islet/Notch/NotchWindowController.swift lines 1593-1603 (handleFocusChange, the function to fix)
    - Islet/Notch/NotchWindowController.swift lines 1740-1766 (handleSettingsChanged's full tail, the pattern being mirrored)
    - Islet/Notch/NotchWindowController.swift lines 1784-1805 (flushTransients — confirm it truly never calls renderPresentation()/updateVisibility() itself, so the caller must)
    - Islet/Notch/NotchWindowController.swift lines 732-734 (renderPresentation) and lines 766-800 (updateVisibility, to confirm no-argument signature and idempotency when called redundantly)
    - .planning/phases/38-focus-mode-hud/38-REVIEW.md "CR-01" section (this review's numbering — the exact suggested fix diff)
  </read_first>
  <action>
    In `Islet/Notch/NotchWindowController.swift`, modify `handleFocusChange(_:)` (lines 1593-1603) so its `else` branch (currently the single line `flushTransients(.focus)` at line 1601) becomes three statements: `flushTransients(.focus)`, then `withAnimation(.spring(response: springResponse, dampingFraction: springDamping)) { renderPresentation() }`, then `updateVisibility()` — mirroring `handleSettingsChanged`'s tail (lines 1762-1765) exactly, both in call order and in wrapping `renderPresentation()` (not `updateVisibility()`) inside the `withAnimation` block. Do not add a doc comment longer than one line; a short inline comment referencing "38-09 gap closure — mirrors handleSettingsChanged's render tail, flushTransients never renders itself" is sufficient context for a future reader. Do not touch the `if isFocused` branch (lines 1594-1599) — it already calls `presentTransientChange()` correctly. Do not touch `flushTransients` itself (lines 1784-1805) — the fix belongs at this one caller, matching the review's diagnosis that `handleFocusChange` is the sole leaf-function call site lacking a render tail.
  </action>
  <acceptance_criteria>
    - `grep -A6 "private func handleFocusChange" Islet/Notch/NotchWindowController.swift` shows the `else` branch containing, in order: `flushTransients(.focus)`, a `withAnimation(.spring(response: springResponse, dampingFraction: springDamping))` block whose body is exactly `renderPresentation()`, then a standalone `updateVisibility()` call
    - `grep -c "func flushTransients" Islet/Notch/NotchWindowController.swift` still returns `1` and its body (lines ~1784-1805) is byte-identical to before this task (verify via `git diff Islet/Notch/NotchWindowController.swift` showing no hunk touching `flushTransients`'s body)
    - `git diff Islet/Notch/NotchWindowController.swift` shows changes scoped ONLY to `handleFocusChange`'s `else` branch — no other function touched
    - `IslandResolver.swift`, `NotchPillView.swift`, `SettingsView.swift` show zero diff (verify via `git status`)
  </acceptance_criteria>
  <verify>
    <automated>grep -A8 "private func handleFocusChange" Islet/Notch/NotchWindowController.swift</automated>
  </verify>
  <done>handleFocusChange's off-branch re-renders and re-runs visibility exactly like handleSettingsChanged's tail; no other function or file modified.</done>
</task>

<task type="auto">
  <name>Task 2: Debug build gate</name>
  <files>(none — verification only)</files>
  <read_first>
    - Islet/Notch/NotchWindowController.swift (post-Task-1 state, to confirm no syntax errors before invoking the build)
  </read_first>
  <action>
    Run a Debug-configuration build of the Islet scheme to confirm the fix compiles cleanly. Use `xcodebuild build` (NOT `xcodebuild test` — this project's known headless-hang issue means `test` boots the full app's NSPanel/MediaRemote/IOBluetooth stack and hangs in this environment; `build` is the correct automated gate per project convention, same as 38-08 Task 2).
  </action>
  <acceptance_criteria>
    - Build output ends with `** BUILD SUCCEEDED **`
    - No new compiler warnings/errors reference `NotchWindowController.swift`
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -scheme Islet -configuration Debug 2>&1 | tail -30</automated>
  </verify>
  <done>Debug build succeeds with the Task 1 fix in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Batched on-device UAT — new fix plus 38-08's two never-yet-run scenarios</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <action>No code changes in this task — verification-only, confirming the Task 1 fix holds on real hardware, batched together with 38-08's two outstanding on-device scenarios (neither has ever actually been run) since all three are cheap to verify in one sitting.</action>
  <what-built>
    New fix (this plan): `handleFocusChange`'s off-branch now calls `renderPresentation()`/`updateVisibility()` after `flushTransients(.focus)`, so the Focus pill should disappear promptly when real Focus/DND turns off, instead of staying stuck until an unrelated event happens to trigger a redraw.

    Carried forward from 38-08 (code-complete, never yet on-device confirmed): CR-01 fix (`activityEnabled(_:)` gives `focusKey` its own `false` default — a fresh/toggle-OFF install should not silently auto-start Focus monitoring) and CR-02/WR-02 fix (granting Focus permission via the Continue popover now starts the monitor immediately, no re-toggle/relaunch needed).
  </what-built>
  <how-to-verify>
    Open Xcode, build and run Islet on-device (Cmd-R). Perform all three scenarios below in order. For EACH numbered step, record the VERBATIM observed behavior (not a blanket "approved" or "works") — precise per-step notes are required.

    **Scenario A — Focus-off dismisses the HUD without any unrelated event (NEW, this plan's fix):**
    1. Ensure Focus Mode HUD is enabled and authorized in Settings.
    2. Toggle macOS Focus/Do Not Disturb ON via Control Center. Confirm the Focus pill appears in the notch.
    3. Toggle macOS Focus/Do Not Disturb OFF via Control Center.
    4. Do NOT hover, click, or interact with the island afterward — just watch it, untouched, for at least 5 seconds (two poll cycles).
    5. Record: did the Focus pill disappear on its own within ~2.5-5s of toggling off, with zero interaction? (Expected: YES.)

    **Scenario B — Fresh-install / toggle-still-OFF auto-start check (carried forward from 38-08, CR-01 fix, never yet run):**
    1. Quit Islet if running. Confirm via Settings that "Focus Mode HUD" toggle shows OFF and was never touched this session (or reset via `defaults delete <bundle-id> activity.focus` if needed — check the actual bundle identifier in the Xcode project if unsure).
    2. Confirm `INFocusStatusCenter` authorization is already granted from a prior session (Settings shows no "Permission needed" hint).
    3. Relaunch Islet (quit fully, reopen).
    4. Toggle macOS Focus/Do Not Disturb ON via Control Center.
    5. Record: did the Focus HUD pill appear? (Expected: NO — the toggle is OFF.)

    **Scenario C — First-grant flow without any toggle-off/on workaround (carried forward from 38-08, CR-02/WR-02 fix, never yet run):**
    1. With Focus authorization NOT yet granted (revoke via System Settings if possible; if unable to fully revoke, note this limitation explicitly rather than skipping the scenario), open Islet Settings and flip "Focus Mode HUD" toggle ON.
    2. The permission explanation popover should appear — click "Continue".
    3. Grant the OS permission dialog that appears.
    4. Do NOT re-toggle the switch off/on, and do NOT relaunch the app.
    5. Toggle macOS Focus/Do Not Disturb ON via Control Center.
    6. Record: did the Focus HUD pill appear? (Expected: YES, without any additional manual workaround.)
  </how-to-verify>
  <resume-signal>Report the verbatim observed result for each numbered step in all three scenarios (not just "approved"/"works") — e.g. "Scenario A step 5: pill disappeared after ~3s with no interaction", "Scenario B step 5: HUD did not appear, toggle stayed OFF", "Scenario C step 6: HUD appeared within ~1s of toggling Focus on, no re-toggle needed."</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FocusModeMonitor poll callback → NotchWindowController (@MainActor, owns presentationState) | Unchanged by this plan — the fix adds a render call inside an already-existing, already-trusted internal call path; no new external input crosses a boundary here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-12 | Denial of Service (stale UI) | `handleFocusChange`'s off-branch (pre-fix: stale presentation never cleared) | mitigate | This plan's Task 1 fix — added `renderPresentation()`/`updateVisibility()` tail closes the stale-UI condition directly |
| T-38-13 | Tampering | `TransientQueue`/`presentationState` internal state | accept | Unchanged from prior phase plans — no new attack surface introduced; internal app state only, no external input |
</threat_model>

<verification>
- `handleFocusChange`'s `else` branch calls `flushTransients(.focus)`, then `renderPresentation()` (wrapped in the standard spring animation), then `updateVisibility()`, matching `handleSettingsChanged`'s tail pattern.
- `xcodebuild build -scheme Islet -configuration Debug` succeeds.
- On-device UAT (Task 3) confirms Scenario A (new fix), Scenario B (38-08 CR-01, never run), and Scenario C (38-08 CR-02/WR-02, never run) all behave as expected, with verbatim per-step notes recorded.
</verification>

<success_criteria>
- ROADMAP Success Criterion #2 ("toggling off dismisses it") holds without requiring an unrelated event to trigger the redraw.
- D-06/D-09 satisfied: Focus pill disappears the instant Focus turns off.
- Debug build succeeds with the fix in place.
- Resolver/queue/view layer (already verified correct) remains untouched.
- All three outstanding on-device scenarios (this plan's new fix + 38-08's two carried-forward scenarios) are confirmed in one batched UAT pass.
</success_criteria>

<output>
Create `.planning/phases/38-focus-mode-hud/38-09-SUMMARY.md` when done
</output>
