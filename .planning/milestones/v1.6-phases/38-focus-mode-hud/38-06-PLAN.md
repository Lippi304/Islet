---
phase: 38-focus-mode-hud
plan: 06
type: execute
wave: 3
depends_on: ["38-03"]
files_modified:
  - Islet/SettingsView.swift
autonomous: true
requirements: [HUD-05]

must_haves:
  truths:
    - "The Focus Mode HUD Settings toggle defaults OFF — the one activity toggle in this codebase that does not default true (D-01)"
    - "Permission is requested ONLY at the moment the user flips the toggle from off to on — never at launch, never lazily on first Focus trigger (D-02)"
    - "The status hint below the toggle shows the real permission state using the two locked strings, and shows nothing while the toggle is off (D-05)"
    - "Declining the explanation ('Not Now') leaves the toggle ON with the inert 'Permission needed' hint — no nag, no re-prompt (D-04)"
  artifacts:
    - path: "Islet/SettingsView.swift"
      provides: "focusEnabled @AppStorage toggle + status hint Text + permission explanation popover"
      contains: "Toggle(\"Focus Mode HUD\", isOn: $focusEnabled)"
  key_links:
    - from: "Islet/SettingsView.swift Toggle(\"Focus Mode HUD\")"
      to: "Islet/Notch/FocusModeMonitor.swift"
      via: ".onChange(of: focusEnabled) triggers the permission request/deep-link only when flipping to true and not yet authorized"
      pattern: "onChange\\(of: focusEnabled\\)"
---

<objective>
Build the Settings toggle, permission-status hint, and permission-explanation surface (D-01
through D-05) — the one genuinely new UI pattern this phase introduces (per 38-UI-SPEC.md: no
prior Settings toggle in this codebase needed a manual OS-permission explanation+deep-link flow;
Calendar/Weather/Bluetooth all use real TCC prompts triggered directly by the system).

**Prerequisite check:** if Plan 38-03's SUMMARY.md recorded a D-12 descope, do not execute this
plan.

Purpose: closes ROADMAP Success Criteria #1 (spike-confirmed path drives which explanation copy
ships) and #3 (silent degrade on denial, made legible via the status hint per D-05's own
rationale: "this is what makes D-04's silent-inertness acceptable").
Output: `SettingsView.swift` extended with the toggle, hint, and explanation popover.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-focus-mode-hud/38-CONTEXT.md
@.planning/phases/38-focus-mode-hud/38-UI-SPEC.md
@.planning/phases/38-focus-mode-hud/38-01-SUMMARY.md
</context>

<interfaces>
Existing toggle declaration convention (SettingsView.swift:28-33, already read in full during planning):
```swift
@AppStorage(ActivitySettings.chargingKey)   private var chargingEnabled = true
@AppStorage(ActivitySettings.deviceKey)     private var deviceEnabled = true
```

Existing `Activities` Section (SettingsView.swift:197-202):
```swift
Section("Activities") {
    Toggle("Charging", isOn: $chargingEnabled)
    Toggle("Now Playing", isOn: $nowPlayingEnabled)
    Toggle("Song-Change Toast", isOn: $songChangeToastEnabled)
    Toggle("Devices", isOn: $deviceEnabled)
}
```

The ONE existing "toggle flip triggers a system-settings flow" precedent to mirror structurally
(SettingsView.swift:174-193, Launch-at-login):
```swift
Toggle("Launch Islet at login", isOn: $launchAtLogin)
    .onChange(of: launchAtLogin) { _, on in
        do {
            let result = try LaunchAtLogin.set(on)
            if on && LaunchAtLogin.requiresApproval {
                launchAtLogin = true
                LaunchAtLogin.openLoginItemsSettings()
            } else {
                launchAtLogin = result
            }
        } catch {
            launchAtLogin = LaunchAtLogin.isEnabled
        }
    }
```

`FocusModeMonitor` (Plan 38-03's output) — `static var isAuthorized: Bool`,
`static func openFullDiskAccessSettings()` (or, if 38-01-SUMMARY.md recorded `path-a`, a
`requestAuthorization(completion:)` method instead — confirm the actual final API before wiring).

`ActivitySettings.focusPermissionStatusHint(toggleOn:granted:) -> String?` (Plan 38-03's output).

The ONE existing `.popover` precedent in the codebase (`NotchPillView.swift:2560`, the quick-add
popover) — confirms `.popover(isPresented:)` is this codebase's established sheet/popover
modifier idiom, even though that specific instance lives in a different file; mirror the
modifier usage, not the file.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Toggle row + status hint + onChange permission trigger</name>
  <files>Islet/SettingsView.swift</files>
  <read_first>
    - Islet/SettingsView.swift lines 20-60 (existing @AppStorage declarations) — read before adding the new one, confirms exact insertion point and current property ordering
    - Islet/SettingsView.swift lines 170-225 (generalSection, Activities section, Launch-at-login onChange) — read this exact range before editing
    - .planning/phases/38-focus-mode-hud/38-UI-SPEC.md "Toggle row" table — the exact copy/behavior this task implements
  </read_first>
  <action>
    Add `@AppStorage(ActivitySettings.focusKey) private var focusEnabled = false` alongside the
    other activity `@AppStorage` declarations — the DEFAULT is `false` (D-01), deliberately
    diverging from every sibling toggle's `= true` default; do not copy the sibling default.
    Add `@State private var showFocusPermissionExplanation = false` for the popover's presentation
    binding.
    In the `Activities` Section, add `Toggle("Focus Mode HUD", isOn: $focusEnabled)` after the
    existing `Toggle("Devices", isOn: $deviceEnabled)` line, followed immediately by a status
    hint `Text` bound to `ActivitySettings.focusPermissionStatusHint(toggleOn: focusEnabled,
    granted: FocusModeMonitor.isAuthorized)` — render nothing (`EmptyView` via `if let hint =
    ... { Text(hint) }`) when the function returns `nil`. Style the hint per 38-UI-SPEC.md
    Typography table: 11px regular, `.font(.system(size: 11))`, `.foregroundStyle(.secondary)`.
    Make the hint tappable when it reads "Permission needed — tap to grant": wrap it in
    `.onTapGesture { showFocusPermissionExplanation = true }` (D-04's "retry path entirely
    user-initiated" — this is the ONLY way the explanation re-appears after a "Not Now" dismiss,
    never automatically).
    Add `.onChange(of: focusEnabled) { _, on in ... }` on the Toggle: when `on == true` AND
    `!FocusModeMonitor.isAuthorized`, set `showFocusPermissionExplanation = true` (D-02: the
    permission ask happens at THIS exact moment, never earlier); when `on == false`, do nothing
    further (the controller's `handleSettingsChanged()`, Plan 38-05, independently reacts to the
    UserDefaults change and calls `flushTransients(.focus)` — SettingsView does not need to call
    anything itself on toggle-off).
  </action>
  <acceptance_criteria>
    - `grep -n "@AppStorage(ActivitySettings.focusKey) private var focusEnabled = false" Islet/SettingsView.swift` returns exactly 1 match — confirm the default is literally `false`, not `true`
    - `grep -n "Toggle(\"Focus Mode HUD\", isOn: \$focusEnabled)" Islet/SettingsView.swift` returns exactly 1 match
    - `grep -n "focusPermissionStatusHint" Islet/SettingsView.swift` returns exactly 1 match
    - `grep -n "onChange(of: focusEnabled)" Islet/SettingsView.swift` returns exactly 1 match
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` succeeds
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <done>Toggle exists, defaults off, shows the correct live status hint, and triggers the explanation popover only on an off-to-on flip while unauthorized.</done>
</task>

<task type="auto">
  <name>Task 2: Permission explanation popover</name>
  <files>Islet/SettingsView.swift</files>
  <read_first>
    - .planning/phases/38-focus-mode-hud/38-UI-SPEC.md "Permission explanation surface" section — the two full copy variants (FDA vs. INFocusStatusCenter) and the "build only the variant the spike confirmed" instruction
    - .planning/phases/38-focus-mode-hud/38-01-SUMMARY.md — confirms which variant to build
  </read_first>
  <action>
    Attach `.popover(isPresented: $showFocusPermissionExplanation) { focusPermissionExplanationView
    }` to the `Toggle("Focus Mode HUD", ...)` row (mirrors the codebase's one existing `.popover`
    idiom). Add a new private computed view `focusPermissionExplanationView: some View` building
    the copy from 38-UI-SPEC.md's locked table for whichever path 38-01-SUMMARY.md confirmed:
    if Path B (Full Disk Access) — Heading `"Grant Full Disk Access"` (15px semibold), Body
    `"Islet needs Full Disk Access to detect when Focus or Do Not Disturb is on. Islet only
    reads this one system status — it never reads, modifies, or sends any of your files."` (12px
    regular, line-height 1.4), primary Button `"Open System Settings"` calling
    `FocusModeMonitor.openFullDiskAccessSettings()` then `showFocusPermissionExplanation =
    false`, secondary Button `"Not Now"` calling only `showFocusPermissionExplanation = false`
    (toggle stays on, per D-04). If Path A (`INFocusStatusCenter`) instead — Heading `"Allow
    Focus Status Access"`, Body `"Islet needs permission to detect when Focus or Do Not Disturb
    is on."`, primary Button `"Continue"` calling `FocusModeMonitor.requestAuthorization {
    granted in ... }` then dismissing, secondary Button `"Not Now"` identical to the FDA variant.
    Do NOT build both variants speculatively — only the one the spike confirmed, per
    38-UI-SPEC.md's explicit instruction.
  </action>
  <acceptance_criteria>
    - `grep -n "focusPermissionExplanationView" Islet/SettingsView.swift` returns at least 2 matches (the `.popover` reference + the computed property definition)
    - `grep -n '"Not Now"' Islet/SettingsView.swift` returns exactly 1 match
    - If Path B: `grep -n '"Open System Settings"' Islet/SettingsView.swift` returns exactly 1 match and `grep -n '"Continue"' Islet/SettingsView.swift` returns 0 matches (confirms only one variant was built); if Path A, the inverse
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` succeeds
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <done>The explanation popover renders the spike-confirmed variant's exact locked copy, wires the deep-link/authorization-request button, and "Not Now" dismisses without side effects beyond closing the popover.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| SettingsView.onChange(of: focusEnabled) -> `FocusModeMonitor.openFullDiskAccessSettings()` / `.requestAuthorization` | User-initiated UI action crossing into an OS permission-request/deep-link call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-06-01 | Elevation of Privilege (permission-prompt spoofing) | The explanation popover's copy and button labels | mitigate | Copy is a hardcoded, locked string from 38-UI-SPEC.md — not dynamically constructed from any external/untrusted input, so there is no way for the displayed explanation to misrepresent what permission is being requested or why |
| T-38-06-02 | Elevation of Privilege (over-broad ask) | Full Disk Access is requested for the whole system, not scoped to one file | accept | This is an OS-level API limitation (FDA has no narrower grant), not something Islet's UI can scope down further; the explanation body text explicitly states Islet reads only the one status file and never modifies/sends anything, keeping the ASK legible even though the OS-level grant itself is broad |

</threat_model>

<verification>
Debug build succeeds; toggle defaults off; status hint reflects live permission state; popover
shows only the spike-confirmed copy variant.
</verification>

<success_criteria>
D-01 through D-05 are all satisfied: opt-in default, request-only-on-flip, deep-link (or native
prompt) trigger, silent-inert-on-decline, and a legible persistent status hint.
</success_criteria>

<output>
Create `.planning/phases/38-focus-mode-hud/38-06-SUMMARY.md` when done.
</output>
