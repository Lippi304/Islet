---
phase: 38-focus-mode-hud
plan: 07
type: execute
wave: 4
depends_on: ["38-01", "38-04", "38-05", "38-06"]
files_modified:
  - Islet/FocusDetectionSpike.swift
  - Islet/AppDelegate.swift
autonomous: false
requirements: [HUD-05]

must_haves:
  truths:
    - "No throwaway spike code remains in the shipped codebase — FocusDetectionSpike.swift and its AppDelegate hook are removed once the detection path is locked into FocusModeMonitor.swift"
    - "A fresh install with permission never granted does not crash, spin, or silently poll forever (ROADMAP Success Criterion #3)"
    - "The full end-to-end behavior (persistence, preemption, collapsed-only scope, silent Focus-off) is confirmed on real hardware, not just unit tests"
  artifacts: []
  key_links: []
---

<objective>
Remove the now-unneeded throwaway spike (Plan 38-01) and run the phase's consolidated
on-device UAT checkpoint — the final gate before this phase can be considered complete, covering
every behavior 38-VALIDATION.md marks manual-only (things XCTest structurally cannot assert:
real TCC/FDA state, real Focus/DND toggling, visual/interaction feel).

**Prerequisite check:** if any prior plan's SUMMARY.md recorded a D-12 descope, this plan does
not apply — the phase was already closed out at that step.

Purpose: closes the loop on ROADMAP Success Criteria #1-4 with real hardware verification, and
leaves the codebase clean of the Plan 38-01 diagnostic scaffolding.
Output: `FocusDetectionSpike.swift` deleted, `AppDelegate.swift`'s DEBUG hook line removed, a
signed-off on-device UAT record in this plan's SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-focus-mode-hud/38-VALIDATION.md
@.planning/phases/38-focus-mode-hud/38-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the throwaway spike + final build/test gate</name>
  <files>Islet/FocusDetectionSpike.swift, Islet/AppDelegate.swift</files>
  <read_first>
    - Islet/FocusDetectionSpike.swift (Plan 38-01's output) — confirm its full contents before deleting
    - Islet/AppDelegate.swift — confirm the exact DEBUG-gated call line added by Plan 38-01 before removing it
  </read_first>
  <action>
    Delete `Islet/FocusDetectionSpike.swift` entirely. Remove the `#if DEBUG` /
    `runFocusDetectionSpike()` / `#endif` block Plan 38-01 added to
    `Islet/AppDelegate.swift`'s `applicationDidFinishLaunching(_:)` — leave no trace of the
    spike call. Run `xcodegen generate` to regenerate `Islet.xcodeproj` (removes the deleted
    file's stale project reference — this project's sources are file-glob discovered per
    `project.yml`'s own header comment, so deleting the file alone is not enough to clean the
    generated project).
  </action>
  <acceptance_criteria>
    - `test -f Islet/FocusDetectionSpike.swift` fails (file does not exist)
    - `grep -c "runFocusDetectionSpike" Islet/AppDelegate.swift` returns `0`
    - `grep -rn "FocusDetectionSpike" Islet.xcodeproj/project.pbxproj` returns `0` matches after `xcodegen generate`
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` succeeds
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Release` succeeds
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug && xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Release</automated>
  </verify>
  <done>Spike scaffolding fully removed; both Debug and Release configurations build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Consolidated on-device UAT</name>
  <files>Islet/Notch/FocusModeMonitor.swift, Islet/Notch/IslandResolver.swift, Islet/Notch/NotchWindowController.swift, Islet/Notch/NotchPillView.swift, Islet/SettingsView.swift</files>
  <action>No code changes in this task — verification-only, confirming the whole feature (Plans 38-02 through 38-06) works end-to-end on real hardware.</action>
  <what-built>
    The complete Focus Mode HUD feature: an opt-in Settings toggle (off by default), a
    permission explanation + deep-link flow, a live `FocusModeMonitor` polling the
    spike-confirmed detection path, and a collapsed-pill-only, non-self-dismissing, Charging/
    Device-preemptible "Focus" wing.
  </what-built>
  <how-to-verify>
    All steps use the Xcode GUI (Build and Run, Cmd-R) — do not use terminal commands for any
    verification step below except where explicitly noted.
    1. Build and Run the Islet scheme in Xcode (Cmd-R). Open Settings (menu bar icon -> Settings)
       and confirm the "Focus Mode HUD" toggle under Activities starts OFF with no status hint
       text showing beneath it.
    2. Click the toggle to turn it ON. Confirm the permission explanation popover appears
       immediately, showing the copy Plan 38-06 built (Full Disk Access or Focus Status Access,
       whichever the spike confirmed).
    3. Click "Open System Settings" (or "Continue"). If Full Disk Access: in the System Settings
       window that opens, confirm it landed on Privacy & Security -> Full Disk Access (not the
       Privacy & Security top level) — add Islet via the "+" button and enable it, then return to
       Islet's Settings and confirm the status hint flips from "Permission needed — tap to grant"
       to "Active" (may require re-toggling off/on or relaunching if the app does not live-poll
       permission grants — note which behavior you observe).
    4. Toggle macOS Focus/Do Not Disturb ON via Control Center. Within a few seconds, confirm the
       collapsed pill shows the "Focus" wing (moon icon + "Focus" label + green dot), matching
       the Charging/Bluetooth Droppy-pill wing style from Phase 36.
    5. Wait at least 10 seconds with nothing else happening. Confirm the Focus pill is STILL
       showing (this is the D-06 regression check — a naive implementation would auto-dismiss it
       after ~3 seconds).
    6. While the Focus pill is showing, hover over the notch to expand the island. Confirm Home/
       Tray/Calendar/Weather/Now-Playing all work exactly as normal — the Focus state is NOT
       shown anywhere in the expanded view (D-07).
    7. Collapse the island again (move the pointer away). Plug in your charger (or connect/
       disconnect a Bluetooth device). Confirm the Charging (or Device) pill immediately replaces
       the Focus pill (D-08) — it should NOT wait behind Focus.
    8. Once the Charging/Device pill's own ~3 second window elapses, confirm the Focus pill
       automatically reappears (D-08's "resumes once cleared" behavior) — still active if you
       have not turned Focus off yet.
    9. Toggle macOS Focus/Do Not Disturb OFF via Control Center. Confirm the Focus pill simply
       disappears — no "Focus Off" toast, no separate dismiss animation (D-09).
    10. Quit Islet. In Finder, go to System Settings -> Privacy & Security -> Full Disk Access
        and remove Islet's entry (revoke the grant) if Full Disk Access was the path used.
        Relaunch Islet (Cmd-R in Xcode). Confirm the app launches normally (no crash, no hang)
        and the Focus toggle in Settings still shows "Permission needed — tap to grant" — this is
        the fresh-install-no-permission regression check (ROADMAP Success Criterion #3).
  </how-to-verify>
  <resume-signal>Reply "approved" if all 10 steps behaved as described, or describe exactly which step failed and what you observed instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new — this plan only removes diagnostic code and runs manual verification of already-built
(and already threat-modeled, in Plans 38-03/38-05/38-06) production behavior.

## STRIDE Threat Register

No new threats — this plan's only code change is a deletion. The on-device checkpoint's step 10
(fresh-install, revoked-permission relaunch) is itself the live verification of T-38-05-01's
mitigation claim from Plan 38-05's threat model.
</threat_model>

<verification>
Debug and Release builds succeed with the spike removed; all 10 on-device UAT steps pass.
</verification>

<success_criteria>
ROADMAP Phase 38's four Success Criteria are all confirmed true on real hardware; the codebase
contains zero leftover throwaway diagnostic code.
</success_criteria>

<output>
Create `.planning/phases/38-focus-mode-hud/38-07-SUMMARY.md` when done, recording the human's
verbatim response to the on-device checkpoint (approved, or the specific failure).
</output>
