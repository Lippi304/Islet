---
phase: 40-update-available-hud-sparkle-integration
plan: 03
type: execute
wave: 3
depends_on: ["40-02"]
files_modified:
  - project.yml
autonomous: false
requirements: [HUD-06]

must_haves:
  truths:
    - "On-device: the badge appears/disappears correctly across all 4 (updateAvailable, isExpanded) states, tinted with the theme accent, with no VoiceOver regression"
    - "On-device: tapping the badge or the 'Check for Updates…' menu item opens Sparkle's own dialog without stealing focus on a background/automatic check and without breaking click-through elsewhere on the pill"
    - "On-device: no unprompted Sparkle permission alert appears on the app's second launch (Pitfall 1 regression check)"
    - "On-device: a Release-configuration archive launches without a Gatekeeper/library-validation crash with Sparkle embedded (ROADMAP Success Criterion 4)"
    - "On-device: dismissing Sparkle's own dialog (Remind Me Later / Skip This Version) never clears the badge and no app-level dismiss mechanism exists beyond Sparkle's dialog (D-14)"
  artifacts:
    - path: "project.yml"
      provides: "SUFeedURL temporarily pointed at the local mock appcast for Task 1, then reverted to the D-01 placeholder"
  key_links:
    - from: "docs/appcast-mock.xml"
      to: "SPUUpdaterDelegate.updater(_:didFindValidUpdate:)"
      via: "a temporary file:// SUFeedURL override, reverted at the end of Task 1"
      pattern: "SUFeedURL"
---

<objective>
Confirm on real hardware that the Sparkle + badge pipeline built in Plans 01-02 actually behaves
correctly end-to-end — the two genuinely unverifiable-by-build-alone risks this phase carries
(RESEARCH.md Assumption A3: Sparkle's background-check focus behavior in this LSUIElement app;
RESEARCH.md Pitfall 3: Release-only library-validation crash class already seen once with
MediaRemoteAdapter). This is a dedicated verification-only plan (no implementation code), per this
project's split-checkpoint-from-implementation convention.

Purpose: Close ROADMAP Phase 40 Success Criteria 2, 3, and 4 with real on-device confirmation, not
just a green build.
Output: A confirmed-working Sparkle+badge feature, or a documented list of on-device defects for a
gap-closure plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/40-update-available-hud-sparkle-integration/40-CONTEXT.md
@.planning/phases/40-update-available-hud-sparkle-integration/40-RESEARCH.md
@.planning/phases/40-update-available-hud-sparkle-integration/40-UI-SPEC.md
@project.yml
@docs/appcast-mock.xml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: On-device Debug verification — Sparkle pipeline + badge (D-05..D-13, Pitfall 1/2)</name>
  <files>project.yml</files>
  <action>No code changes in this task — verification-only, plus a temporary/reverted project.yml edit to point the feed at the local mock appcast for testing.</action>
  <what-built>
  The full Debug-time Sparkle + badge pipeline: `SPUStandardUpdaterController` wired in
  `AppDelegate.swift` with a "Check for Updates…" menu item, the `SUEnableAutomaticChecks` Info.plist
  fix (Pitfall 1), the D-11/D-12 Settings toggle, and the D-05/D-06/D-07/D-08 corner badge on
  `NotchPillView`.
  </what-built>
  <how-to-verify>
  1. Temporarily point the feed at the mock appcast: in project.yml, change
     `INFOPLIST_KEY_SUFeedURL` to `file://<absolute path to this repo>/docs/appcast-mock.xml`. Run
     `xcodegen generate`, then build+run Islet from Xcode (Debug), on the notch MacBook.
  2. Confirm launch #1 shows no unexpected Sparkle alert.
  3. Quit and relaunch Islet a SECOND time (Pitfall 1's exact trigger condition). Confirm NO
     unprompted Sparkle permission alert ("May Islet check for updates automatically?") appears — if
     it does, the `SUEnableAutomaticChecks` Info.plist key from Plan 01 Task 1 did not take effect.
  4. From the status-item menu, click "Check for Updates…". Confirm Sparkle's own standard alert
     appears showing the mock appcast's fake newer version, and that the app is allowed to
     activate/come to the foreground for this explicit user-initiated action (expected, not a bug).
  5. Dismiss Sparkle's dialog via "Remind Me Later" — do NOT click Install (the mock enclosure/
     signature is fake and will fail real verification). Confirm the collapsed pill's top-trailing
     corner now shows the small `arrow.up.circle.fill` badge (D-13: the badge reflects the pending
     update even after "Remind Me Later", it is never cleared by tapping).
  6. Confirm the badge is tinted with the current Now Playing accent color (Settings > Appearance/
     Theming — change the Now Playing accent and confirm the badge color follows it live).
  7. Expand the island (any tab: Home, Tray, Calendar, Weather). Confirm the badge disappears
     entirely from every expanded view (D-06).
  8. Collapse the island again. Confirm the badge reappears in the same corner.
  9. Confirm tapping the badge ITSELF (not just the menu item) also opens Sparkle's dialog.
  10. With Accessibility Inspector (or VoiceOver), confirm the badge exposes the accessibility label
      "Update available".
  11. Do a quick hover→expand→move-down click-through trace (this project's established CR-01
      regression check) to confirm the new badge overlay introduced no click-through/hover
      regressions elsewhere on the pill.
  12. In Settings, toggle "Automatically Check for Updates" OFF, quit and relaunch Islet, then run
      `defaults read com.lippi304.islet activity.autoUpdateCheck` — confirm it reads `0`. Toggle it
      back ON afterward (confirm it reads back `1` or is absent, since `@AppStorage` doesn't always
      write the default).
  13. Revert project.yml's `INFOPLIST_KEY_SUFeedURL` back to the D-01 placeholder value set in Plan 01
      Task 1 (the mock `file://` URL was ONLY for this checkpoint). Run `xcodegen generate` again and
      confirm a final Debug build succeeds.
  </how-to-verify>
  <verify>
    <human-check>All 13 numbered steps above confirmed on real hardware, no unresolved issues.</human-check>
  </verify>
  <done>Debug on-device UAT approved; project.yml's SUFeedURL confirmed reverted to the placeholder.</done>
  <resume-signal>Type "approved" or describe issues found at any numbered step above.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device Release verification — embedded Sparkle.framework launch (Pitfall 3, ROADMAP Success Criterion 4)</name>
  <files>build/Islet-Phase40.xcarchive (transient, not committed)</files>
  <action>No code changes in this task — verification-only, archiving and launching a Release build outside Xcode's debugger.</action>
  <what-built>
  The embedded, code-signed `Sparkle.framework` running under Hardened Runtime with the existing
  project-wide `disable-library-validation` entitlement (RESEARCH.md Pitfall 3, same failure class
  previously hit and fixed once already for `MediaRemoteAdapter`).
  </what-built>
  <how-to-verify>
  1. Archive a Release build: `xcodebuild archive -scheme Islet -configuration Release -archivePath
     build/Islet-Phase40.xcarchive` (or Xcode's Product > Archive).
  2. Export/copy the built `Islet.app` out of the archive (Xcode Organizer > Distribute App > Copy
     App, or `xcodebuild -exportArchive`).
  3. Launch the exported Release `Islet.app` directly by double-clicking it in Finder — NOT via
     Xcode's debugger (Pitfall 3 only reproduces on a real launch outside Xcode's debug harness).
  4. Confirm the app launches without crashing and the menu-bar capsule icon appears (no dyld
     "different Team IDs" library-validation crash).
  5. Confirm "Check for Updates…" is present in the status-item menu and clicking it opens Sparkle's
     dialog (a network error or "no update found" response against the still-placeholder `SUFeedURL`
     is an acceptable outcome here — the goal is confirming no crash and no missing-menu-item
     regression in a real Release build, not a live network round-trip).
  </how-to-verify>
  <verify>
    <human-check>All 5 numbered steps above confirmed on a real Release archive launch, no crash.</human-check>
  </verify>
  <done>Release on-device UAT approved; embedded Sparkle.framework launches cleanly under Hardened Runtime.</done>
  <resume-signal>Type "approved" or describe issues found at any numbered step above.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local `file://` mock appcast ↔ Sparkle | Task 1's temporary local-file feed override is a TEST-ONLY trust boundary, reverted at the end of the same task |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-40-07 | Tampering | project.yml's temporary `file://` `SUFeedURL` override left in place after testing | mitigate | Task 1 step 13 explicitly requires reverting `INFOPLIST_KEY_SUFeedURL` back to the D-01 placeholder and re-confirming the build before the checkpoint can be marked approved — a shipped build must never point at a local file path. |
| T-40-08 | Repudiation (process) | Release-configuration crash class going unnoticed until real user download | accept | Directly re-verifies the exact `MediaRemoteAdapter` failure class already documented in project memory (`release-library-validation-crash`) — Task 2 is this phase's dedicated gate for it, matching this project's own established convention of a `-configuration Release`-gated manual check before any phase involving an embedded third-party framework is considered done. |
</threat_model>

<verification>
- Both checkpoint tasks return "approved" with no unresolved issues.
- project.yml's `INFOPLIST_KEY_SUFeedURL` is confirmed reverted to the non-file:// placeholder before sign-off.
- A final `xcodebuild build -scheme Islet -configuration Debug` succeeds after the revert.
</verification>

<success_criteria>
- ROADMAP Phase 40 Success Criteria 2, 3, and 4 all confirmed true on real hardware, not just inferred from a green build.
- No regression in the pill's existing click-through/hover/accent-theming behavior.
- No unprompted Sparkle permission alert or unexpected app activation on background/second-launch checks.
</success_criteria>

<output>
Create `.planning/phases/40-update-available-hud-sparkle-integration/40-03-SUMMARY.md` when done
</output>
