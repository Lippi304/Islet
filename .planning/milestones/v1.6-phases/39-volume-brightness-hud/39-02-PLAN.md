---
phase: 39-volume-brightness-hud
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - Islet/Notch/OSDActivity.swift
  - Islet/Notch/IslandResolver.swift
  - IsletTests/OSDActivityTests.swift
  - IsletTests/IslandResolverTests.swift
autonomous: true
requirements: [HUD-03, HUD-04]

must_haves:
  truths:
    - "A pure OSDActivity value type exists with total mapping functions for volume and brightness, importing only Foundation, unit-tested in milliseconds, with no dependency on CGEventTap/CoreAudio/DisplayServices"
    - "A single ActiveTransient.osd(OSDActivity) case models BOTH Volume and Brightness (D-12's discretion note), so TransientQueue.updateHead's existing same-category-replace semantics give D-12's instant cross-category replace for free"
    - "ActiveTransient.osd resolves collapsed-only (D-11) exactly like Focus, is NOT persistent (self-elapses, unlike Focus), and reuses TransientQueue.preempt() against a standing Focus head without modifying preempt() itself (D-13)"
    - "OSDActivity.isMuted is hardwareMuted OR percent == 0, both trigger paths independently unit-tested (D-03 / RESEARCH Open Question 3)"
  artifacts:
    - path: "Islet/Notch/OSDActivity.swift"
      provides: "Pure OSDActivity enum (.volume/.brightness) + total mapping functions + isMuted"
      exports: ["OSDActivity", "osdVolumeActivity", "osdBrightnessActivity"]
    - path: "Islet/Notch/IslandResolver.swift"
      provides: "ActiveTransient.osd case, IslandPresentation.osd case, resolve() rank-4 collapsed-only branch, TransientQueue.updateHead (.osd, .osd) same-category arm"
      contains: "case .osd(let o) where !isExpanded: return .osd(o)"
  key_links:
    - from: "Islet/Notch/IslandResolver.swift"
      to: "Islet/Notch/OSDActivity.swift"
      via: "ActiveTransient.osd(OSDActivity) / IslandPresentation.osd(OSDActivity) case payload"
      pattern: "case osd\\(OSDActivity\\)"
---

<objective>
Write the PURE, framework-free seams both later plans build against: `OSDActivity.swift` (the
volume/brightness value type + total mapping functions, mirroring `FocusActivity.swift`) and the
`IslandResolver.swift` extension that gives Volume/Brightness a resolver rank (below Focus,
collapsed-only) and the exact TransientQueue mechanics D-09/D-12/D-13 need — all reusing existing
machinery (`preempt()`) rather than duplicating it.

Purpose: per this project's own established convention (`IslandResolver` before controller
wiring, proven in Phase 38 for Focus), the ranking/priority logic must be unit-tested in
milliseconds BEFORE any CGEventTap/CoreAudio/DisplayServices glue exists, so Plans 39-03/39-04/
39-05 build against a locked, tested contract instead of guessing at resolver shape while also
wiring fragile system APIs.
Output: `OSDActivity.swift`, the extended `IslandResolver.swift`, and full test coverage in
`OSDActivityTests.swift` + new cases in `IslandResolverTests.swift`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/39-volume-brightness-hud/39-CONTEXT.md
@.planning/phases/39-volume-brightness-hud/39-RESEARCH.md
@.planning/phases/39-volume-brightness-hud/39-PATTERNS.md
</context>

<interfaces>
<!-- FocusActivity.swift, read in full — the exact shape OSDActivity.swift mirrors -->
From Islet/Notch/FocusActivity.swift:
```swift
enum FocusActivity: Equatable {
    case on
}
func focusActivity(from isFocused: Bool) -> FocusActivity? {
    isFocused ? .on : nil
}
```

<!-- IslandResolver.swift's existing Focus additions (Phase 38), the exact precedent this plan
     reapplies for Volume/Brightness one tier lower -->
From Islet/Notch/IslandResolver.swift:
```swift
enum IslandPresentation: Equatable {
    // ...
    case focus(FocusActivity)                        // Phase 38 / HUD-05: rank 3 transient, collapsed-only (D-07)
    // ...
}
enum ActiveTransient: Equatable {
    case charging(ChargingActivity)
    case device(DeviceActivity)
    case focus(FocusActivity)
}
extension ActiveTransient {
    var isPersistent: Bool {
        if case .focus = self { return true }
        return false
    }
}
// resolve(...)'s transient switch:
switch activeTransient {
case .charging(let a): return .charging(a)
case .device(let d):   return .device(d)
case .focus(let f) where !isExpanded: return .focus(f)
case .focus: break
case nil: break
}
// TransientQueue.updateHead:
mutating func updateHead(_ t: ActiveTransient) {
    guard let h = head else { return }
    switch (h, t) {
    case (.charging, .charging): head = t
    case (.device, .device):     head = t
    default: break
    }
}
```
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: OSDActivity.swift pure value + total mapping (RED-GREEN)</name>
  <files>Islet/Notch/OSDActivity.swift, IsletTests/OSDActivityTests.swift</files>
  <read_first>
    - Islet/Notch/FocusActivity.swift (read in full, 21 lines) — the exact "plain value + total
      function, Foundation-only import" shape to mirror
    - IsletTests/FocusActivityTests.swift (read in full, 21 lines) — the exact test style to mirror
    - Islet/Notch/PowerSourceMonitor.swift lines 42-50 — the defensive optional-cast / clamp
      convention (V5 input validation) OSDActivity's mapping functions must apply at the pure seam
  </read_first>
  <behavior>
    - Test: `osdVolumeActivity(percent: 50, hardwareMuted: false)` returns `.volume(percent: 50, hardwareMuted: false)`
    - Test: `osdVolumeActivity(percent: 150, hardwareMuted: false)` clamps to `.volume(percent: 100, hardwareMuted: false)` (V5 clamp, out-of-range high)
    - Test: `osdVolumeActivity(percent: -10, hardwareMuted: false)` clamps to `.volume(percent: 0, hardwareMuted: false)` (V5 clamp, out-of-range low)
    - Test: `osdBrightnessActivity(percent: 42)` returns `.brightness(percent: 42)`; `osdBrightnessActivity(percent: 200)` clamps to `.brightness(percent: 100)`
    - Test: `OSDActivity.volume(percent: 50, hardwareMuted: true).isMuted == true` (D-03 path 1: hardware mute)
    - Test: `OSDActivity.volume(percent: 0, hardwareMuted: false).isMuted == true` (D-03 path 2: zero level, RESEARCH Open Question 3)
    - Test: `OSDActivity.volume(percent: 50, hardwareMuted: false).isMuted == false` (neither path)
    - Test: `OSDActivity.brightness(percent: 50).isMuted == false` (brightness has no muted state, per 39-UI-SPEC.md)
  </behavior>
  <action>
    Create `Islet/Notch/OSDActivity.swift`, importing ONLY `Foundation`. Define
    `enum OSDActivity: Equatable { case volume(percent: Int, hardwareMuted: Bool); case brightness(percent: Int) }`
    — a SINGLE shared type with an inner case split (per CONTEXT.md's discretion note and
    RESEARCH's Primary Recommendation), not two separate types, so `TransientQueue.updateHead`'s
    same-category match in Task 2 covers both D-09 (same-activity scrub refresh) and D-12
    (Volume<->Brightness instant replace) with one switch arm. Add a computed
    `var isMuted: Bool` that returns `false` for `.brightness` and, for `.volume(percent, hardwareMuted)`,
    returns `hardwareMuted || percent == 0` (D-03, RESEARCH Open Question 3's resolved
    recommendation — write this as the SINGLE source of truth for the muted check; Plan 39-04's
    view layer must read this property, never re-derive the OR itself). Add two TOTAL pure
    mapping functions, `func osdVolumeActivity(percent: Int, hardwareMuted: Bool) -> OSDActivity`
    and `func osdBrightnessActivity(percent: Int) -> OSDActivity`, each clamping `percent` to
    `0...100` via `min(100, max(0, percent))` before constructing the case (V5 input validation —
    mirrors `PowerSourceMonitor.readCurrentPower()`'s defensive-cast discipline, applied here at
    the pure-mapping seam instead of the glue seam, since two different glue files, `VolumeReader`
    and `BrightnessReader`, will call into this ONE shared clamp).
  </action>
  <acceptance_criteria>
    - `Islet/Notch/OSDActivity.swift` imports ONLY `Foundation` — `grep -E "^import" Islet/Notch/OSDActivity.swift` returns exactly one line, `import Foundation`
    - All 8 behaviors above pass as XCTest assertions in `IsletTests/OSDActivityTests.swift`
    - `osdVolumeActivity`/`osdBrightnessActivity` never force-unwrap and clamp is applied unconditionally, not just for out-of-range inputs
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <done>OSDActivity.swift exists with the exact shape above; OSDActivityTests.swift covers all 8 behaviors; build succeeds (Cmd-U run manually per project convention — xcodebuild test hangs on this project, see project memory xcodebuild-test-headless-hang).</done>
</task>

<task type="auto">
  <name>Task 2: IslandResolver.swift extension — .osd case, rank-4 collapsed-only branch, updateHead arm</name>
  <files>Islet/Notch/IslandResolver.swift, IsletTests/IslandResolverTests.swift</files>
  <read_first>
    - Islet/Notch/IslandResolver.swift (already read in full this session — 298 lines) — the exact
      lines to extend: `IslandPresentation` (54-68), `ActiveTransient` (72-76), `isPersistent`
      (83-88), `resolve(...)`'s transient switch (117-123), `TransientQueue.preempt()` (249-263,
      NO changes needed — reused verbatim), `TransientQueue.updateHead()` (273-285)
    - IsletTests/IslandResolverTests.swift lines 619-665 — the exact Focus test block this task's
      new tests mirror (`testFocusWinsWhenCollapsed`, `testFocusFallsThroughWhenExpanded`,
      `testActiveTransientIsPersistentFlags`, `testPreemptPushesFocusToFrontOfPending`)
  </read_first>
  <action>
    Add `case osd(OSDActivity)` to BOTH `IslandPresentation` and `ActiveTransient` (place after
    the existing `case focus(FocusActivity)` line in each, with a comment "Phase 39 / HUD-03/
    HUD-04: rank 4 transient, collapsed-only (D-11), NOT persistent (self-elapses via D-10's own
    1.5s timer, unlike Focus)"). In `resolve(...)`'s transient switch (currently ending
    `case .focus: break` / `case nil: break`), insert a new tier directly below the existing
    Focus tier: `case .osd(let o) where !isExpanded: return .osd(o)` followed by `case .osd: break`
    — same `where !isExpanded` / fall-through shape as Focus's own tier, positioned AFTER
    `.focus(let f) where !isExpanded: return .focus(f)` so Focus still wins if somehow both are
    simultaneously the head (should never happen — TransientQueue only ever has one head — this
    ordering is defensive documentation, not reachable logic). Do NOT add `.osd` to
    `isPersistent` — it must fall through the existing `if case .focus = self { return true };
    return false` unchanged (add a one-line comment above `isPersistent` noting `.osd` is
    deliberately excluded so it self-elapses, unlike Focus). In `TransientQueue.updateHead(_:)`,
    add `case (.osd, .osd): head = t` as a new arm in the existing `switch (h, t)` alongside the
    `.charging`/`.device` arms — this single line is the ENTIRE mechanism for both D-09 (Volume
    scrubbing refreshes the standing head) and D-12 (pressing Brightness while Volume shows
    instantly replaces it), since both are the `.osd` category regardless of which inner case
    each side holds. Do NOT modify `TransientQueue.preempt()` — Plan 39-05's controller wiring
    calls it directly with `.osd(...)` exactly like existing `.charging(...)`/`.device(...)` call
    sites already do.
  </action>
  <acceptance_criteria>
    - `grep -n "case osd(OSDActivity)" Islet/Notch/IslandResolver.swift` finds exactly 2 matches (IslandPresentation + ActiveTransient)
    - `grep -n "case (.osd, .osd): head = t" Islet/Notch/IslandResolver.swift` finds exactly 1 match
    - `IslandResolver.swift`'s only import remains `Foundation` — no AppKit/SwiftUI/CoreAudio introduced
    - New tests added to `IslandResolverTests.swift`: `.osd` wins collapsed / falls through expanded (mirrors testFocusWinsWhenCollapsed/testFocusFallsThroughWhenExpanded), `ActiveTransient.osd(...).isPersistent == false` (extends testActiveTransientIsPersistentFlags), `.osd` preempts a standing Focus head (mirrors testPreemptPushesFocusToFrontOfPending with `.osd(.volume(percent: 50, hardwareMuted: false))` swapped in for `.charging(...)`), and a NEW test asserting `TransientQueue.updateHead` replaces `.osd(.volume(...))` with `.osd(.brightness(...))` instantly (no direct Focus precedent — this is D-12's own mechanism)
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <done>IslandResolver.swift extended per the exact diff above; new IslandResolverTests.swift cases cover collapsed-only win, expanded fall-through, non-persistence, Focus preemption, and the D-12 cross-category instant-replace mechanism; build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan is pure, framework-free value logic — no system API, no untrusted input crosses a boundary here (levels are clamped at this exact seam per V5, closing the boundary before it reaches any consumer) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-02-01 | Tampering | `osdVolumeActivity`/`osdBrightnessActivity` clamp | mitigate | Both mapping functions unconditionally clamp to 0...100 before constructing the case — a malformed/out-of-range read from Plan 39-03's glue can never propagate an invalid percent into the resolver or view layer (Task 1 acceptance criteria) |
</threat_model>

<verification>
`xcodebuild build -scheme Islet -configuration Debug` succeeds. Manual Cmd-U confirms all new
unit tests pass (both `OSDActivityTests.swift` and the new `IslandResolverTests.swift` cases) —
per this project's own recorded constraint, `xcodebuild test` hangs and must not be used as an
automated gate.
</verification>

<success_criteria>
`OSDActivity.swift` and the `IslandResolver.swift` extension exist, are fully unit-tested, and
give every downstream plan (39-03 through 39-05) a locked, tested contract to build against —
no resolver/queue shape guessing required in any later plan.
</success_criteria>

<output>
Create `.planning/phases/39-volume-brightness-hud/39-02-SUMMARY.md` when done.
</output>
