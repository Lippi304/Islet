---
phase: 39-volume-brightness-hud
plan: 07
type: execute
wave: 5
depends_on: ["39-06"]
files_modified:
  - Islet/Notch/OSDInterceptionSpike.swift
  - Islet/AppDelegate.swift
autonomous: false
requirements: [HUD-03, HUD-04]

must_haves:
  truths:
    - "The Wave 1 spike scaffolding (OSDInterceptionSpike.swift + its AppDelegate debug-menu wiring) is fully removed once the production OSDInterceptor has superseded it"
    - "All 4 ROADMAP Phase 39 Success Criteria are confirmed on real hardware in one consolidated checkpoint, mirroring Phase 38's own 38-07 precedent"
    - "The undocumented EnableSystemBanners Control-Center-wide defaults toggle is grepped-for and confirmed absent anywhere in the shipped implementation (Success Criterion 4)"
  artifacts: []
  key_links: []
---

<objective>
Remove Plan 39-01's throwaway spike scaffolding now that the production `OSDInterceptor`/
`VolumeReader`/`BrightnessReader`/`OSDActivity`/resolver/wing/controller/Settings chain fully
supersedes it, then run ONE consolidated on-device UAT checkpoint covering every ROADMAP Success
Criterion plus every locked CONTEXT.md decision (D-01 through D-13) that only becomes observable
with real hardware.

Purpose: mirrors this project's own `38-07-PLAN.md` precedent exactly — the last plan in a
new-transient-pipeline phase is always spike-cleanup + one consolidated real-hardware pass, not a
per-plan smoke test. This is also this phase's OWN version of the "remove the deferred idea"
discipline: a throwaway debug artifact left in the tree past its usefulness is exactly the kind
of drift the project's CLAUDE.md code-quality rule (only change what needs changing) warns about.
Output: a clean tree with zero spike code, and a recorded pass/fail against every ROADMAP Success
Criterion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/39-volume-brightness-hud/39-CONTEXT.md
@.planning/phases/39-volume-brightness-hud/39-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove spike scaffolding</name>
  <files>Islet/Notch/OSDInterceptionSpike.swift, Islet/AppDelegate.swift</files>
  <read_first>
    - Islet/Notch/OSDInterceptionSpike.swift (Plan 39-01's output) — read in full before deleting,
      to confirm nothing inside it is unexpectedly referenced elsewhere
    - Islet/AppDelegate.swift — locate the exact `#if DEBUG` menu items + stored properties Plan
      39-01 added (`osdSpikeDetectOnly`/`osdSpikeDetectAndSuppress` and their two menu-item
      registrations) so removal is surgical, not a blind revert of the whole DEBUG block (which
      also contains unrelated, pre-existing debug menu items — e.g. license stub-flip controls —
      that must NOT be touched)
  </read_first>
  <action>
    Delete `Islet/Notch/OSDInterceptionSpike.swift` entirely. In `Islet/AppDelegate.swift`, remove
    ONLY the two OSD-spike-specific debug menu items and their backing `osdSpikeDetectOnly`/
    `osdSpikeDetectAndSuppress` properties/actions added in Plan 39-01 — leave every other
    pre-existing `#if DEBUG` menu item (license stub-flips, etc.) completely untouched.
  </action>
  <acceptance_criteria>
    - `Islet/Notch/OSDInterceptionSpike.swift` no longer exists (`test -f` returns non-zero)
    - `grep -n "OSDInterceptionSpike\|osdSpikeDetectOnly\|osdSpikeDetectAndSuppress" Islet/AppDelegate.swift` returns NO matches
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Release` succeeds (Release never referenced the spike, so this is a clean confirmation, not just a Debug-only check)
    - `grep -rn "EnableSystemBanners" Islet/` returns NO matches anywhere in the shipped source (Success Criterion 4)
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Release</automated>
  </verify>
  <done>Spike file deleted, AppDelegate.swift's debug menu restored to its pre-spike state (minus the two removed items, plus everything else untouched), Release build confirms zero remaining references, and EnableSystemBanners is confirmed absent from the entire codebase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Consolidated on-device UAT — all 4 ROADMAP Success Criteria</name>
  <files>Islet/Notch/OSDInterceptor.swift, Islet/Notch/VolumeReader.swift, Islet/Notch/BrightnessReader.swift, Islet/Notch/IslandResolver.swift, Islet/Notch/NotchPillView.swift, Islet/Notch/NotchWindowController.swift, Islet/SettingsView.swift</files>
  <action>No code changes in this task — verification-only, running the complete Volume/Brightness HUD end-to-end on real hardware.</action>
  <what-built>
    A complete Volume/Brightness HUD: pressing a volume or brightness key shows a Droppy-style
    icon+bar pill in the collapsed island, optionally suppressing the native macOS OSD once the
    user opts in via Settings and grants Accessibility, with instant cross-category replacement,
    Focus-preemption, and a 1.5s auto-dismiss that resets on every subsequent press.
  </what-built>
  <how-to-verify>
    1. Build and Run the Islet scheme (Cmd-R) in Debug configuration on real hardware.
    2. **Success Criterion 1 (spike already recorded, re-confirm here):** press each of the 4
       media transport keys (play/pause, next, previous, and fast-forward/rewind if present) in a
       real music app — confirm every one still works exactly as before, completely unaffected by
       this phase's changes.
    3. **Success Criterion 2:** press a volume key — confirm the new Volume HUD (icon left, green
       bar right) appears in the collapsed pill and its bar level visually matches the real system
       volume (check against Control Center's own volume slider). Press a brightness key — confirm
       the Brightness HUD (sun icon, orange bar) appears and its level matches System Settings'
       actual brightness. Lower volume to 0 or hit the hardware mute key — confirm the icon swaps
       to the muted variant and the bar fully drains (D-03).
    4. **Success Criterion 3:** rapidly press a volume key several times in succession (scrubbing)
       — confirm the SAME HUD instance updates its bar level smoothly with each press (spring
       animation, D-04), never stacking multiple separate HUD instances, and the auto-dismiss
       timer visibly resets on each press (D-09/D-10 — the HUD should stay up roughly 1.5s after
       the LAST press, not the first). Then press Brightness while the Volume HUD is still
       showing — confirm it INSTANTLY replaces Volume with Brightness (D-12), not queuing behind it.
    5. **D-13 (priority):** toggle Focus/Do Not Disturb ON via Control Center so the Focus HUD
       shows, then press a volume key while it's still up — confirm Volume immediately takes over
       the pill (preempting Focus), and once Volume's ~1.5s elapses, the Focus HUD automatically
       reappears.
    6. **Settings/permission flow (D-05 through D-08):** open Settings, confirm "Replace System
       Volume/Brightness OSD" is OFF by default. Turn it on — confirm the explanation popover
       appears (if Accessibility isn't already granted) with the exact locked copy, and clicking
       "Open System Settings" opens System Settings' Accessibility pane. Grant Accessibility to
       Islet there, return to Islet without relaunching, and press a volume key — confirm the
       native macOS OSD no longer appears (suppression is now active) and the status hint updated
       to "Active" without any further toggle interaction (D-07). Turn the toggle back off and
       press a volume key again — confirm the native OSD reappears alongside Islet's own HUD
       (suppression stopped, HUD still shows, D-06).
    7. **Success Criterion 4:** confirm you never needed to run any `defaults write
       com.apple.controlcenter EnableSystemBanners` command anywhere in this test — the feature
       works entirely through Islet's own toggle.
  </how-to-verify>
  <resume-signal>Reply "approved" if all 4 Success Criteria and the D-01 through D-13 spot-checks above pass, or describe exactly which step failed and what you observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan removes code and verifies existing behavior; it introduces no new trust boundary |
</threat_model>

<verification>
Release build compiles clean with zero spike references and zero `EnableSystemBanners` matches
anywhere in the codebase; the consolidated on-device checkpoint confirms all 4 ROADMAP Success
Criteria and the full D-01 through D-13 decision set.
</verification>

<success_criteria>
Phase 39 (HUD-03, HUD-04) is fully shipped: real hardware confirms transport keys are unaffected,
both HUDs show live levels correctly, scrubbing/cross-category-replace/Focus-preemption all work
per D-09/D-10/D-12/D-13, the Settings/permission flow works end-to-end including the D-07
mid-session auto-upgrade, and the codebase contains zero throwaway spike code and zero
`EnableSystemBanners` usage.
</success_criteria>

<output>
Create `.planning/phases/39-volume-brightness-hud/39-07-SUMMARY.md` when done. MUST record the
per-Success-Criterion pass/fail and note any deviations found (mirrors 38-09-SUMMARY.md's level
of detail for a phase-closing on-device checkpoint).
</output>
