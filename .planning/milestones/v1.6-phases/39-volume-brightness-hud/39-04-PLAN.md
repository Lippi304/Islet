---
phase: 39-volume-brightness-hud
plan: 04
type: execute
wave: 2
depends_on: ["39-02"]
files_modified:
  - Islet/Notch/NotchPillView.swift
autonomous: true
requirements: [HUD-03, HUD-04]

must_haves:
  truths:
    - "The OSD wing renders icon-left / bar-right per D-01, with NO label text and NO numeric percentage anywhere (39-UI-SPEC.md's explicit divergence from Phase 36's icon+label convention)"
    - "Bar and icon colors are FIXED (green for Volume, orange for Brightness, white icon) and never accent-tinted (D-02)"
    - "Volume's icon swaps to speaker.slash.fill and the bar drains to 0 together, driven by the SAME OSDActivity.isMuted computed property — never two independently-triggered UI states (D-03)"
    - "Brightness has no muted-equivalent state — only sun.max.fill, never a swapped icon"
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "osdWings(for:) wing view + presentationSwitch dispatch for .osd"
      contains: "case .osd(let activity): osdWings(for: activity)"
  key_links:
    - from: "Islet/Notch/NotchPillView.swift"
      to: "Islet/Notch/OSDActivity.swift"
      via: "osdWings(for activity: OSDActivity) parameter"
      pattern: "osdWings\\(for"
---

<objective>
Build the collapsed-pill OSD wing view (`osdWings(for:)`) per `39-UI-SPEC.md`'s locked contract —
icon-only left flank, a new minimal two-layer `Capsule` fill bar on the right flank (NOT
`BatteryIndicator`, whose outline/nub/centered-text chrome directly conflicts with D-01's
no-numeric-text rule) — and wire it into the existing `presentationSwitch` dispatch alongside
Charging/Device/Focus.

Purpose: this is a pure view-layer task, mechanically reapplying `wingsShape()` and the
established fixed-color/no-internal-animation conventions Phase 36/38 already proved — no new
shape wrapper, no resolver/controller changes. Runs independently of Plan 39-03's system glue
(the view only needs the pure `OSDActivity` type from Plan 39-02).
Output: `osdWings(for:)`, its dispatch site, and a new minimal fill-bar helper view/subview.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/39-volume-brightness-hud/39-CONTEXT.md
@.planning/phases/39-volume-brightness-hud/39-UI-SPEC.md
@.planning/phases/39-volume-brightness-hud/39-PATTERNS.md
</context>

<interfaces>
<!-- focusWings(for:), read in full (NotchPillView.swift lines 2151-2179) — the exact wing-wrapper
     structure osdWings(for:) reapplies -->
From Islet/Notch/NotchPillView.swift:
```swift
private func focusWings(for activity: FocusActivity) -> some View {
    wingsShape(leftWidth: 118, rightWidth: 160) {
        HStack(spacing: 0) {
            Image(systemName: "moon.fill")
                .font(.system(size: 13, weight: .semibold))
                .symbolRenderingMode(.hierarchical)
                .foregroundStyle(.white)
                .padding(.leading, 14)
            Spacer()
            HStack(spacing: 4) { /* right-wing content */ }
                .padding(.trailing, 20)
        }
    }
}
```

<!-- ProgressBar's GeometryReader/Capsule fill technique (NotchPillView.swift lines 2542-2548) —
     the only existing "filled progress bar" primitive in the codebase, reused for the OSD bar -->
```swift
GeometryReader { geo in
    ZStack(alignment: .leading) {
        Capsule().fill(Color.white.opacity(0.25))
        Capsule().fill(tint).frame(width: geo.size.width * fraction)
    }
}
.frame(height: 3)
```

<!-- presentationSwitch dispatch site, NotchPillView.swift line 735 -->
```swift
case .focus(let activity): focusWings(for: activity)                 // D-02 rank 3 transient (38-04)
```

<!-- OSDActivity.swift (Plan 39-02, already merged) -->
```swift
enum OSDActivity: Equatable {
    case volume(percent: Int, hardwareMuted: Bool)
    case brightness(percent: Int)
}
extension OSDActivity {
    var isMuted: Bool { /* hardwareMuted || percent == 0, false for .brightness */ }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: osdWings(for:) — icon-left wing + minimal fill-bar right wing</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    - Islet/Notch/NotchPillView.swift lines 1929-1952 (`wingsShape<Content: View>`) — the shared
      wrapper EVERY wing routes through; do not build a new wrapper
    - Islet/Notch/NotchPillView.swift lines 2151-2179 (`focusWings(for:)`) — the closest analog:
      single icon-only left flank (no label), `Spacer()` clearing the camera bridge, fixed-white
      D-11-style color discipline
    - Islet/Notch/NotchPillView.swift lines 2515-2554 (`ProgressBar`) — the `GeometryReader` /
      `ZStack(alignment: .leading)` / `Capsule` fill technique to reapply for the OSD bar
    - Islet/Notch/BatteryIndicator.swift (already read in full this session) — READ ONLY to
      confirm what NOT to reuse: its outline/nub/centered-`Text("\(clamped)%")` chrome directly
      conflicts with D-01's "no numeric text" rule, so this task builds a NEW minimal bar, not a
      variant of this component
    - 39-UI-SPEC.md "OSD Wing Contract" section in full — the locked pixel values (118/190 wing
      widths, 150x5pt bar, 2.5 corner radius, 14pt/20pt flank padding, `.speaker.wave.3.fill`/
      `.speaker.slash.fill`/`.sun.max.fill` icon names, `Color.green`/`Color.orange` fill colors)
  </read_first>
  <action>
    Add `private func osdWings(for activity: OSDActivity) -> some View` immediately after
    `focusWings(for:)` in `NotchPillView.swift`. Call `wingsShape(leftWidth: 118, rightWidth:
    190)` (39-UI-SPEC.md's starting values — flag in the SUMMARY that these are subject to
    on-device tuning in Plan 39-07's checkpoint, mirroring Focus's own live-redesign precedent).
    Inside, an `HStack(spacing: 0)`: LEFT — a single centered `Image(systemName:)` (no HStack, no
    label, unlike every other wing) at `.font(.system(size: 13, weight: .semibold))`,
    `.symbolRenderingMode(.hierarchical)`, `.foregroundStyle(.white)` (D-02, never accent-tinted),
    `.padding(.leading, 14)`. Icon name: for `.volume`, `activity.isMuted ? "speaker.slash.fill" :
    "speaker.wave.3.fill"` (D-03 — driven by the SAME `OSDActivity.isMuted` computed property the
    bar's fraction below also reads, never a second independent mute check); for `.brightness`,
    always `"sun.max.fill"` (no swap state). Then `Spacer()` (clears the camera bridge). RIGHT —
    a new private inline helper (either a small `private struct OSDLevelBar: View` or an inline
    `ZStack` — executor's call per 39-UI-SPEC.md's own "small enough it may not warrant its own
    file" note) rendering: `Capsule().fill(Color.white.opacity(0.15))` as the empty track,
    overlaid with `Capsule().fill(tint)` left-anchored via `GeometryReader { geo in ... .frame(width:
    geo.size.width * fraction) }`, sized `150` wide x `5` tall with `cornerRadius: 2.5` (a full
    capsule at that height already reads as fully rounded — do not hardcode a separate corner
    radius constant that fights the Capsule shape). `fraction` is `0` when `activity.isMuted` is
    true (D-03 — bar fully drained), else the activity's clamped `percent` divided by 100 (both
    `.volume` and `.brightness` expose `percent` via their case payload — extract via a local
    `switch activity { case .volume(let p, _): p; case .brightness(let p): p }`). `tint` is
    `Color.green` for `.volume`, `Color.orange` for `.brightness` (D-02, locked). Do NOT apply any
    `.animation()` modifier inside this view — per this file's established "the view drives no
    animation of its own" convention (see `focusWings`/`wings(for:)` header comments), the
    controller (Plan 39-05) wraps every OSD mutation in its own `withAnimation(.spring(...))`
    wrapper (D-04). Pad the bar's trailing edge `20` (matches Focus's shipped right-flank
    padding).
    Add `case .osd(let activity): osdWings(for: activity)` to `presentationSwitch`'s `switch
    presentation` block (line ~735), placed directly after the existing `case .focus(let
    activity): focusWings(for: activity)` line, with a trailing comment `// Phase 39 / HUD-03/
    HUD-04: rank 4 transient (39-02)`.
  </action>
  <acceptance_criteria>
    - `grep -n "case .osd(let activity): osdWings" Islet/Notch/NotchPillView.swift` finds exactly 1 match inside `presentationSwitch`
    - `grep -n "BatteryIndicator" Islet/Notch/NotchPillView.swift` shows no NEW call site added by this task (existing Charging/Device call sites untouched, no third usage added for OSD)
    - `grep -n "speaker.slash.fill\|speaker.wave.3.fill\|sun.max.fill" Islet/Notch/NotchPillView.swift` finds all 3 icon names present
    - `grep -n "Color.green\|Color.orange" Islet/Notch/NotchPillView.swift` finds both fixed colors used within `osdWings`'s body (not `deviceAccent`/`chargingAccent`/`nowPlayingAccent`)
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` succeeds
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <done>osdWings(for:) exists, wired into presentationSwitch, uses wingsShape() verbatim, renders a NEW minimal fill-bar (not BatteryIndicator), applies D-01/D-02/D-03 exactly per 39-UI-SPEC.md; build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure SwiftUI rendering of an already-clamped, already-validated `OSDActivity` value (Plan 39-02's clamp closes this boundary upstream) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-04-01 | Tampering | Bar fraction width computation | accept | `OSDActivity`'s own mapping functions (Plan 39-02) already clamp `percent` to 0...100 before this view ever sees it — no additional clamp needed here, but the view still divides by a fixed `100.0` denominator so even an unexpected value stays bounded to a sane visual range |
</threat_model>

<verification>
`xcodebuild build -scheme Islet -configuration Debug` succeeds. Visual correctness (does the bar
look right on real hardware, does 118/190 need retuning) is verified in Plan 39-07's consolidated
on-device UAT checkpoint, not here.
</verification>

<success_criteria>
`osdWings(for:)` renders exactly per `39-UI-SPEC.md`'s OSD Wing Contract and is reachable from
`presentationSwitch` the moment `IslandPresentation.osd(_)` is resolved (Plan 39-05 wires the
controller side that actually produces that case).
</success_criteria>

<output>
Create `.planning/phases/39-volume-brightness-hud/39-04-SUMMARY.md` when done.
</output>
