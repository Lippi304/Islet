---
phase: 39-volume-brightness-hud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Notch/OSDInterceptionSpike.swift
  - Islet/AppDelegate.swift
autonomous: false
requirements: [HUD-03, HUD-04]

must_haves:
  truths:
    - "An on-device spike confirms .cgSessionEventTap (never the annotated variant) intercepts volume/brightness NX_SYSDEFINED events without breaking any of the 4 media transport keys, with the go/no-go decision and fallback behavior explicitly recorded before full implementation (ROADMAP Success Criterion #1)"
    - "The spike also confirms or denies RESEARCH.md's Open Question 2 (does a .listenOnly tap on .cgSessionEventTap for NX_SYSDEFINED require Accessibility or only Input Monitoring), which decides whether the dual-mode OSDInterceptor architecture Plan 39-03 is written against is viable, or whether the single-mode Accessibility-gates-everything fallback must be used instead"
    - "The NX_KEYTYPE_* bit-decode constants (SOUND_UP=0, SOUND_DOWN=1, MUTE=7, BRIGHTNESS_UP=2, BRIGHTNESS_DOWN=3, subtype=8, key-down mask 0x0A) are verified against real hardware key presses, not assumed from community sources alone (Assumptions Log A1)"
  artifacts:
    - path: "Islet/Notch/OSDInterceptionSpike.swift"
      provides: "DEBUG-only throwaway spike that probes both .listenOnly and .defaultTap CGEventTap modes on NX_SYSDEFINED and prints go/no-go signal to console"
      exports: ["OSDInterceptionSpike"]
  key_links:
    - from: "Islet/AppDelegate.swift"
      to: "Islet/Notch/OSDInterceptionSpike.swift"
      via: "applicationDidFinishLaunching DEBUG-gated instantiation"
      pattern: "OSDInterceptionSpike\\("
---

<objective>
Run the phase's mandatory go/no-go spike (ROADMAP Success Criterion #1) BEFORE any real
implementation: confirm on THIS dev machine (macOS 26/Tahoe) that `.cgSessionEventTap` can
intercept volume/brightness `NX_SYSDEFINED` key events, decide whether a `.listenOnly` tap
(detect-only) is viable WITHOUT Accessibility (RESEARCH.md Open Questions 1/2 — this decides
the dual-mode architecture 39-03 is built against), confirm `.defaultTap` swallowing actually
suppresses the native macOS OSD, and confirm none of the 4 media transport keys are ever
affected by any of this.

Purpose: this project's own `DropInterceptTap.swift` already proved `.cgSessionEventTap` +
`.defaultTap` + Accessibility works for a DIFFERENT event type (`.leftMouseUp`); this spike
proves the NX_SYSDEFINED-specific decode and the `.listenOnly` detect-only path, which have
zero precedent anywhere in this codebase. 38-CONTEXT.md's own lesson ("verify an actual
functional read early... not just authorizationStatus") applies here too — a tap that merely
*compiles* is not evidence it decodes the right keys or suppresses the right thing.
Output: `Islet/Notch/OSDInterceptionSpike.swift` (DEBUG-only, throwaway — deleted in 39-07 once
the architecture is locked into `OSDInterceptor.swift`), a recorded go/no-go decision plus the
confirmed bit-decode constants in this plan's SUMMARY.md that Plan 39-03 reads before writing
the production interceptor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/39-volume-brightness-hud/39-CONTEXT.md
@.planning/phases/39-volume-brightness-hud/39-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the throwaway dual-mode CGEventTap spike</name>
  <files>Islet/Notch/OSDInterceptionSpike.swift, Islet/AppDelegate.swift</files>
  <read_first>
    - Islet/Notch/DropInterceptTap.swift (read in full) — the exact `.cgSessionEventTap` /
      `AXIsProcessTrustedWithOptions` / `CGEvent.tapCreate` / health-check-timer skeleton to
      mirror for tap lifecycle; note it only ever uses `.defaultTap` and `.leftMouseUp` —
      this spike is the first NX_SYSDEFINED decode and first `.listenOnly` use in the codebase
    - Islet/AppDelegate.swift (read in full) — confirm the exact `applicationDidFinishLaunching`
      insertion point and the existing `#if DEBUG` / `setupDebugMenu()` convention (~line 90) to
      extend with two new debug menu actions
    - .planning/phases/39-volume-brightness-hud/39-RESEARCH.md "Architecture Patterns" Pattern 1
      (dedicated-DispatchQueue tap skeleton) and Pattern 2 (NX_SYSDEFINED bit-decode: keyCode =
      (data1 & 0xFFFF0000) >> 16, keyDown = ((data1 & 0xFF00) >> 8) == 0x0A, subtype == 8) — the
      exact constants and shapes this spike must implement and verify on-device
  </read_first>
  <action>
    Create `Islet/Notch/OSDInterceptionSpike.swift`, its entire contents wrapped in a single
    top-level `#if DEBUG` / `#endif` block (never compiles into Release, mirrors the existing
    NotchWindowController DEBUG hand-seed discipline). Define `final class OSDInterceptionSpike`
    with an `enum SpikeMode { case detectOnly, detectAndSuppress }`, an `init(mode: SpikeMode)`,
    and a `start()`/`stop()` pair mirroring `DropInterceptTap`'s `CFMachPort`/`CFRunLoopSource`
    lifecycle exactly, with two required deviations: (1) `eventsOfInterest` is
    `CGEventMask(1 << CGEventType.systemDefined.rawValue)` (raw value 14), not `.leftMouseUp`;
    (2) `options:` is `.defaultTap` when `mode == .detectAndSuppress`, `.listenOnly` when
    `mode == .detectOnly` — for `.detectOnly` mode, do NOT call
    `AXIsProcessTrustedWithOptions` at all before `tapCreate` (this is the exact question being
    tested: does `.listenOnly` on `.cgSessionEventTap` for NX_SYSDEFINED need Accessibility, or
    does it succeed/prompt for Input Monitoring instead, or need nothing at all); for
    `.detectAndSuppress` mode, call `AXIsProcessTrustedWithOptions` with
    `kAXTrustedCheckOptionPrompt: true` exactly like `DropInterceptTap.start()` does, before
    `tapCreate`. Print `"[OSDSpike][\(mode)] tapCreate result: \(tap == nil ? "nil (no tap)" : "success")"`
    immediately after the `tapCreate` call, in BOTH modes.
    Inside the tap callback, on every `NX_SYSDEFINED` event (`type.rawValue == 14`), construct
    `NSEvent(cgEvent: event)` and decode `data1` per Pattern 2's exact formula. Print
    `"[OSDSpike][\(mode)] keyCode=\(keyCode) keyDown=\(keyDown)"` for EVERY decoded key (not just
    volume/brightness — this confirms which raw codes the 4 transport keys and Caps Lock produce
    too, so the allowlist in 39-03 can be double-checked). When `mode == .detectAndSuppress` AND
    `keyCode` is one of `{0, 1, 7, 2, 3}` (SOUND_UP/SOUND_DOWN/MUTE/BRIGHTNESS_UP/BRIGHTNESS_DOWN)
    AND `keyDown` is true, print `"[OSDSpike][detectAndSuppress] SWALLOWING keyCode=\(keyCode)"`
    and return `nil` from the callback (this is the actual suppression test). For every other
    key code in EITHER mode, print `"[OSDSpike][\(mode)] PASSTHROUGH keyCode=\(keyCode)"` and
    return `Unmanaged.passUnretained(event)` unmodified — this is the critical transport-key
    safety check (Pitfall 1): PLAY/FAST/REWIND/PREVIOUS must never enter the swallow branch in
    either mode. Never force-unwrap `NSEvent(cgEvent:)`'s optional result.
    In `Islet/AppDelegate.swift`'s existing `#if DEBUG setupDebugMenu()` block, add two new debug
    menu items — `"OSD Spike: Start Detect-Only"` and `"OSD Spike: Start Detect+Suppress"` — each
    wired to an `@objc` action that lazily constructs and `start()`s the corresponding
    `OSDInterceptionSpike` instance (stored as two new DEBUG-only properties on `AppDelegate`,
    e.g. `osdSpikeDetectOnly` / `osdSpikeDetectAndSuppress`), so the two modes can be tested one
    at a time on-device without relaunching the app. Do not auto-start either spike at launch —
    this must be a deliberate, menu-triggered action per probe (unlike 38-01's single automatic
    probe, this spike needs to isolate detect-only from detect+suppress so their swallow
    decisions never interact).
  </action>
  <acceptance_criteria>
    - `Islet/Notch/OSDInterceptionSpike.swift` exists, its entire contents (including imports)
      are between a single top-level `#if DEBUG` and matching `#endif`
    - `grep -c "^#if DEBUG" Islet/Notch/OSDInterceptionSpike.swift` returns exactly `1`
    - `Islet/AppDelegate.swift` gains exactly two new DEBUG-only menu items whose actions
      construct `OSDInterceptionSpike(mode: .detectOnly)` and `OSDInterceptionSpike(mode: .detectAndSuppress)` respectively
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Release 2>&1 | grep -i "OSDInterceptionSpike"` returns NO matches
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` succeeds
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <done>Debug build compiles clean with both spike modes reachable from the debug menu; Release build excludes the spike symbol entirely.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device dual-mode CGEventTap go/no-go</name>
  <files>Islet/Notch/OSDInterceptionSpike.swift, Islet/AppDelegate.swift</files>
  <action>No code changes — verification-only, running Task 1's spike on real hardware and recording the confirmed architecture + decode constants.</action>
  <what-built>
    Two independently-triggerable DEBUG-only CGEventTap probes: a `.listenOnly` detect-only tap
    and a `.defaultTap` detect+suppress tap, both filtering `NX_SYSDEFINED` on `.cgSessionEventTap`.
  </what-built>
  <how-to-verify>
    1. Open the project in Xcode, Build and Run the Islet scheme (Cmd-R) in Debug configuration.
    2. Open the Debug Console (View -> Debug Area -> Activate Console).
    3. From the menu-bar status item's debug menu, click "OSD Spike: Start Detect-Only". Press a
       volume key and a brightness key. Report: did a system permission dialog appear (which
       one — Accessibility or Input Monitoring — if any), did `tapCreate` succeed (nil or
       non-nil), and did `[OSDSpike][detectOnly] keyCode=... keyDown=...` lines print for both
       key presses. Then press Play/Pause and a Next-track key on any media app and confirm they
       still work normally (this tap must never swallow).
    4. Quit and relaunch, or otherwise reset, then click "OSD Spike: Start Detect+Suppress".
       Press a volume key: does the NATIVE macOS volume OSD still appear on-screen, or is it
       suppressed? Report the `SWALLOWING`/`PASSTHROUGH` console lines seen. Then test all 4
       media transport keys (play/pause, next, previous, and fast-forward/rewind if your
       keyboard has them) in a real music app — confirm every one still works exactly as before,
       with `PASSTHROUGH` printed for each (never `SWALLOWING`).
    5. Report the exact `keyCode` values printed for: volume up, volume down, mute, brightness
       up, brightness down, play/pause, next, previous — so Assumption A1's constants can be
       confirmed or corrected.
    6. Your overall call, exactly one of:
       - `dual-mode-confirmed` — detect-only worked without Accessibility (Input Monitoring only,
         or no dialog at all) AND detect+suppress correctly suppressed the native OSD without
         breaking any transport key.
       - `single-mode-only` — detect-only ALSO required Accessibility (no permission-tier
         separation exists for this event type) — the dual-mode architecture cannot satisfy
         D-06's "HUD shows without Accessibility"; 39-03 must build a single always-`.defaultTap`
         interceptor instead, gated entirely on Accessibility (HUD and suppression share one gate).
       - `suppression-unreliable` — detect+suppress broke a transport key or failed to suppress
         the native OSD — per ROADMAP's own fallback language, the HUD ships showing ALONGSIDE
         the native OSD (never suppressing), regardless of permission state.
  </how-to-verify>
  <resume-signal>Reply with one of: "dual-mode-confirmed", "single-mode-only", or "suppression-unreliable", plus the exact keyCode values and console output observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Islet process <-> WindowServer NX_SYSDEFINED event stream | Untrusted-shape system event data (undocumented private encoding) crossing into this process via CGEventTap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01-01 | Denial of Service | `OSDInterceptionSpike` transport-key passthrough | mitigate | Default branch for every non-volume/brightness key code is unconditional passthrough in BOTH modes (Task 1 acceptance criteria); the on-device checkpoint explicitly re-verifies all 4 transport keys before this plan can pass |
| T-39-01-02 | Elevation of Privilege | `AXIsProcessTrustedWithOptions(prompt:true)` call in `.detectAndSuppress` mode | accept | This spike is DEBUG-only, never ships in Release (verified by Task 1's Release-build grep check), and requests the SAME permission the real feature will eventually request via a deliberate user action — no broader ask than the shipped feature makes |

This plan ships zero production code (everything is `#if DEBUG`-gated and deleted in 39-07) — the
threat surface is limited to "does the throwaway probe ever swallow the wrong key," which the
mitigation above and the checkpoint's explicit transport-key re-test both cover.
</threat_model>

<verification>
Debug build succeeds with both spike modes reachable from the debug menu; Release build excludes
them; the human-verify checkpoint records a definitive `dual-mode-confirmed` / `single-mode-only`
/ `suppression-unreliable` decision plus the confirmed NX_KEYTYPE_* constants that Plan 39-03
depends on before writing `OSDInterceptor.swift`.
</verification>

<success_criteria>
The spike runs on-device and produces an unambiguous go/no-go signal for the dual-mode
architecture and the bit-decode constants, recorded in this plan's SUMMARY.md, satisfying
ROADMAP Phase 39 Success Criterion #1.
</success_criteria>

<output>
Create `.planning/phases/39-volume-brightness-hud/39-01-SUMMARY.md` when done. MUST record the
exact console output observed for both probes, the confirmed keyCode values for all 8 tested
keys, and the final `dual-mode-confirmed` / `single-mode-only` / `suppression-unreliable`
decision — Plan 39-03 depends on reading this file before writing `OSDInterceptor.swift`.
</output>
