---
phase: 39-volume-brightness-hud
plan: 05
type: execute
wave: 3
depends_on: ["39-03", "39-04"]
files_modified:
  - Islet/Notch/NotchWindowController.swift
  - Islet/ActivitySettings.swift
autonomous: true
requirements: [HUD-03, HUD-04]

must_haves:
  truths:
    - "OSDInterceptor starts unconditionally at launch (never gated behind activityEnabled) — the HUD itself requires no toggle and no permission, only suppression is opt-in (D-06)"
    - "Volume/Brightness's dismiss timer resets on every key press within the window (D-09), using a NEW 1.5s constant kept deliberately separate from the shared 3.0s activityDuration (D-10)"
    - "Volume and Brightness instantly replace each other cross-category via TransientQueue.updateHead's (.osd, .osd) arm — no bespoke cross-category conditional exists anywhere in this file (D-12)"
    - "A standing Focus head is preempted (not queued behind) by a Volume/Brightness key press, exactly like Charging/Device already preempt Focus (D-13)"
    - "Turning the OSD suppression Settings toggle off does not stop OSDInterceptor or hide any standing HUD — it only changes the swallow decision on the NEXT key press (D-06)"
    - "Every OSD level mutation (volume/brightness percent change) is wrapped in withAnimation(.spring(...)) at the controller mutation site, never inside the view, so the bar fill animates with a spring rather than snapping instantly (D-04)"
  artifacts:
    - path: "Islet/Notch/NotchWindowController.swift"
      provides: "osdInterceptor property, unconditional start, handleOSDKeyPress(_:), generalized scheduleActivityDismiss() duration, TransientCategory.osd, deinit teardown"
      contains: "private func handleOSDKeyPress(_ kind: OSDKeyKind)"
    - path: "Islet/ActivitySettings.swift"
      provides: "osdSuppressionKey + osdPermissionStatusHint(toggleOn:granted:)"
      exports: ["ActivitySettings.osdSuppressionKey", "ActivitySettings.osdPermissionStatusHint"]
  key_links:
    - from: "Islet/Notch/NotchWindowController.swift"
      to: "Islet/Notch/OSDInterceptor.swift"
      via: "OSDInterceptor(suppressionArmed:onKeyPress:) construction in startOSDInterceptor()"
      pattern: "OSDInterceptor\\(suppressionArmed:"
    - from: "Islet/Notch/NotchWindowController.swift"
      to: "Islet/Notch/IslandResolver.swift"
      via: "transientQueue.preempt(.osd(activity)) / transientQueue.enqueue(.osd(activity)) / transientQueue.updateHead(.osd(activity))"
      pattern: "\\.osd\\(activity\\)"
---

<objective>
Wire `OSDInterceptor`/`VolumeReader`/`BrightnessReader` (Plan 39-03) into the live controller:
construct + start the interceptor unconditionally at launch, translate its `onKeyPress` callback
into the exact D-09/D-12/D-13 `TransientQueue` sequence, and generalize the single shared
`scheduleActivityDismiss()` dismiss-timer to honor D-10's separate 1.5s OSD duration without
touching Charging/Device/Focus's existing 3.0s behavior.

Purpose: this is the plan where the phase's 3 hardest priority/timing decisions (D-09 re-arm-on-
scrub, D-10 separate duration, D-12 cross-category instant-replace, D-13 Focus-preemption) become
real controller behavior, reusing `presentTransientChange()`/`preempt()`/`updateHead()` exactly
as Phase 38 already established rather than inventing new machinery.
Output: `NotchWindowController.swift`'s OSD wiring, `ActivitySettings.swift`'s new key + hint
function (the shared namespace Plan 39-06's Settings UI reads next).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/39-volume-brightness-hud/39-CONTEXT.md
@.planning/phases/39-volume-brightness-hud/39-RESEARCH.md
@.planning/phases/39-volume-brightness-hud/39-PATTERNS.md
</context>

<interfaces>
<!-- OSDInterceptor.swift (Plan 39-03, already merged) -->
```swift
enum OSDKeyKind { case volume, brightness }
final class OSDInterceptor {
    init(suppressionArmed: @escaping () -> Bool, onKeyPress: @escaping (OSDKeyKind) -> Void)
    static var isAccessibilityTrusted: Bool { get }
    func start()
    func stop()
}
```

<!-- VolumeReader.swift / BrightnessReader.swift (Plan 39-03, already merged) -->
```swift
func readSystemVolume() -> (percent: Int, muted: Bool)
final class BrightnessReader { func readBrightness() -> Int? }
```

<!-- OSDActivity.swift (Plan 39-02, already merged) -->
```swift
func osdVolumeActivity(percent: Int, hardwareMuted: Bool) -> OSDActivity
func osdBrightnessActivity(percent: Int) -> OSDActivity
```

<!-- Existing controller shapes this plan directly reuses/extends, all read in full this session:
     NotchWindowController.swift's device-coordinator enqueue closure (lines 392-397, D-08
     Focus-preemption pattern), handlePower's charging enqueue-vs-tick branch (lines 1559-1584),
     handleFocusChange (lines 1593-1608), scheduleActivityDismiss (lines 1618-1640),
     activityEnabled (lines 566-569), handleSettingsChanged (lines 1713-1771), flushTransients +
     TransientCategory (lines 1788-1810), deinit (lines 2099-2159) -->
```swift
// Existing Charging/Device -> Focus preemption pattern (line 396):
if case .focus = self.transientQueue.head { return self.transientQueue.preempt(t) }
return self.transientQueue.enqueue(t)

// Existing scheduleActivityDismiss (line 1618-1640) -- the ONE dismiss scheduler this plan
// generalizes for D-10's separate OSD duration:
private func scheduleActivityDismiss() {
    dismissWorkItem?.cancel()
    guard let head = transientQueue.head, !head.isPersistent else { return }
    let work = DispatchWorkItem { /* advance queue, render, re-arm if head still non-nil */ }
    dismissWorkItem = work
    DispatchQueue.main.asyncAfter(deadline: .now() + activityDuration, execute: work)
}

// Existing TransientCategory + flushTransients (line 1788-1810):
private enum TransientCategory { case charging, device, focus }
private func flushTransients(_ category: TransientCategory) { /* removeAll + model clear + timer re-arm */ }

// Existing activityEnabled default carve-out (line 566-569):
private func activityEnabled(_ key: String) -> Bool {
    let defaultValue = (key == ActivitySettings.focusKey) ? false : true
    return UserDefaults.standard.object(forKey: key) as? Bool ?? defaultValue
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: OSDInterceptor lifecycle + handleOSDKeyPress(_:) — D-09/D-12/D-13</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    - Islet/Notch/NotchWindowController.swift lines 195-230 (monitor property declarations) —
      exact pattern for the new `osdInterceptor` property + its doc comment
    - Islet/Notch/NotchWindowController.swift lines 445-499 (`start()`'s monitor-start block) —
      note `startOutfitRefresh()` (line 482) is the UNCONDITIONAL-start precedent (no
      `activityEnabled` gate) this task's `startOSDInterceptor()` call site mirrors — OSD
      detection is NOT toggle-gated (D-06), unlike every `if activityEnabled(...)` monitor above it
    - Islet/Notch/NotchWindowController.swift lines 613-619 (`startFocusModeMonitor()`) — the
      idempotent `guard x == nil else { return }` construction shape to mirror for
      `startOSDInterceptor()`
    - Islet/Notch/NotchWindowController.swift lines 1559-1608 (`handlePower`'s charging
      enqueue-vs-tick branch AND `handleFocusChange`) — the exact `if case .focus = head {
      preempt } else { enqueue }` (D-13 precedent) and same-category `updateHead` + explicit
      re-arm shapes this task's `handleOSDKeyPress` reuses
    - Islet/Notch/NotchWindowController.swift lines 1610-1640 (`scheduleActivityDismiss`) — the
      ONE dismiss scheduler this task generalizes for D-10 (read the FULL function body, not just
      the excerpt above, to preserve every existing line except the duration computation)
  </read_first>
  <action>
    Add `private var osdInterceptor: OSDInterceptor?` near `focusModeMonitor` (line ~209), with a
    doc comment explaining it starts UNCONDITIONALLY (D-06) unlike every toggle-gated monitor
    above it. Add `private let osdActivityDuration: TimeInterval = 1.5` near `activityDuration`
    (line ~226), commented "D-10 — deliberately separate from the shared activityDuration, never
    consolidated." Add `private let volumeReader = readSystemVolume` is NOT valid Swift for a free
    function reference with this shape — instead call `readSystemVolume()` directly inline where
    needed; add `private let brightnessReader = BrightnessReader()` as a stored property (mirrors
    `licenseService`'s stored-instance pattern) since `BrightnessReader` holds loaded function
    pointers that should be resolved once, not on every brightness key press.
    Add `private func startOSDInterceptor()` mirroring `startFocusModeMonitor()`'s idempotent
    shape exactly, constructing `OSDInterceptor(suppressionArmed: { [weak self] in
    (self?.activityEnabled(ActivitySettings.osdSuppressionKey) ?? false) &&
    OSDInterceptor.isAccessibilityTrusted }, onKeyPress: { [weak self] kind in
    self?.handleOSDKeyPress(kind) })`, then `.start()`. Call `startOSDInterceptor()`
    UNCONDITIONALLY inside `start()` (no `activityEnabled` guard), placed alongside
    `startOutfitRefresh()`'s unconditional-start call (~line 482) with a comment citing D-06.
    Add `private func handleOSDKeyPress(_ kind: OSDKeyKind)` (already on main — the interceptor's
    callback already hopped there per Plan 39-03). Build the `OSDActivity` first: for `.volume`,
    call `readSystemVolume()` and pass its `(percent, muted)` into `osdVolumeActivity(percent:
    hardwareMuted:)`; for `.brightness`, call `brightnessReader.readBrightness()` and `guard let
    percent = ... else { return }` (silent-degrade — a failed brightness read produces NO HUD at
    all for that press, per Plan 39-03's `Int?` contract), then `osdBrightnessActivity(percent:)`.
    Then branch on the CURRENT head: if `case .osd = transientQueue.head` (D-09/D-12's shared same-
    category path — covers BOTH a same-activity scrub AND a cross-activity Volume<->Brightness
    swap, since both are the `.osd` category regardless of inner case), call
    `transientQueue.updateHead(.osd(activity))`, wrap `renderPresentation()` in
    `withAnimation(.spring(response: springResponse, dampingFraction: springDamping))` (D-04 — the
    bar's spring animation lives here, at the controller mutation site, never inside the view),
    then explicitly call `scheduleActivityDismiss()` to RE-ARM the timer (D-09 — this is the one
    deliberate divergence from Charging's % -tick branch, which does NOT re-arm; comment this
    divergence explicitly). Else (nothing `.osd` currently stands): mirror the existing D-13
    Focus-preemption shape exactly — `if case .focus = transientQueue.head { changed =
    transientQueue.preempt(.osd(activity)) } else { changed = transientQueue.enqueue(.osd(activity))
    }`; if `changed`, call `presentTransientChange()` (which already wraps in the spring animation
    and calls `scheduleActivityDismiss()` internally — no separate re-arm needed on this branch).
    Generalize `scheduleActivityDismiss()`'s single hardcoded `+ activityDuration` line to compute
    the duration from the CURRENT head's category: `let duration: TimeInterval = { if case .osd =
    transientQueue.head { return osdActivityDuration }; return activityDuration }()`, then use
    `duration` in the existing `DispatchQueue.main.asyncAfter(deadline: .now() + duration,
    execute: work)` call — this is the ONLY change to this function; every other line (the
    `DispatchWorkItem` body, the `!head.isPersistent` guard, the re-arm-on-advance branch) stays
    byte-for-byte identical, since the same generalized duration computation applies correctly to
    whatever category becomes the NEW head after `advance()` too.
    Add `case osd` to the existing `private enum TransientCategory { case charging, device, focus
    }` and a matching `(.osd, .osd): return true` arm inside `flushTransients(_:)`'s `matches`
    closure, plus a `case .osd: break` arm in the model-clear switch (mirrors Focus's own
    comment: "no separate @Published model to clear -- state lives entirely in the resolver's
    IslandPresentation"). This is defensive completeness — nothing currently calls
    `flushTransients(.osd)` since Plan 39-06's toggle never stops the interceptor (D-06), but the
    exhaustive switch must compile.
    In `deinit` (line ~2099-2159), add `osdInterceptor?.stop()` alongside the existing
    `focusModeMonitor?.stop()` line, mirroring its owner-driven teardown discipline exactly.
  </action>
  <acceptance_criteria>
    - `grep -n "startOSDInterceptor()" Islet/Notch/NotchWindowController.swift` shows it called UNCONDITIONALLY inside `start()` (no `if activityEnabled(...)` wrapping that specific call)
    - `grep -n "func handleOSDKeyPress" Islet/Notch/NotchWindowController.swift` finds exactly 1 match, and its body contains both an `updateHead(.osd(` call path and a `preempt(.osd(` / `enqueue(.osd(` call path
    - `grep -n "osdActivityDuration" Islet/Notch/NotchWindowController.swift` finds the constant declaration AND its use inside the generalized `scheduleActivityDismiss()` duration computation
    - `grep -n "case .osd = transientQueue.head" Islet/Notch/NotchWindowController.swift` confirms the D-09/D-12 same-category branch re-arms via an explicit `scheduleActivityDismiss()` call (not relying on `presentTransientChange()`, which that branch never calls)
    - `grep -n "osdInterceptor?.stop()" Islet/Notch/NotchWindowController.swift` finds exactly 1 match, inside `deinit`
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` succeeds
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <done>OSDInterceptor starts unconditionally at launch, handleOSDKeyPress implements D-09/D-12 (same-category update+rearm) and D-13 (Focus preemption) exactly, scheduleActivityDismiss honors D-10's separate 1.5s duration for an .osd head without changing any other category's timing, deinit tears the interceptor down; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: ActivitySettings.swift — osdSuppressionKey + status hint</name>
  <files>Islet/ActivitySettings.swift</files>
  <read_first>
    - Islet/ActivitySettings.swift (already read in full this session, 148 lines) — lines 19-22
      (`focusKey`) and lines 73-80 (`focusPermissionStatusHint(toggleOn:granted:)`) are the exact
      key + hint-function shape to mirror
  </read_first>
  <action>
    Add `static let osdSuppressionKey = "activity.osdSuppression"` inside `enum ActivitySettings`,
    placed near `focusKey` with a comment: "Phase 39 / HUD-03/HUD-04 (D-05) — gates ONLY native-OSD
    suppression, never the HUD's own visibility (D-06) — see NotchWindowController's unconditional
    `startOSDInterceptor()` call, which does NOT read this key." Add
    `static func osdPermissionStatusHint(toggleOn: Bool, granted: Bool) -> String?` with the
    IDENTICAL body shape as `focusPermissionStatusHint` (`guard toggleOn else { return nil };
    return granted ? "Active" : "Permission needed — tap to grant"`) — 39-UI-SPEC.md's Settings
    Permission Contract locks these exact two strings verbatim, reused rather than reinvented.
  </action>
  <acceptance_criteria>
    - `grep -n "static let osdSuppressionKey" Islet/ActivitySettings.swift` finds exactly 1 match
    - `grep -n "static func osdPermissionStatusHint" Islet/ActivitySettings.swift` finds exactly 1 match, and its body string-matches `focusPermissionStatusHint`'s two hint strings exactly
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` succeeds
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <done>ActivitySettings.swift gains osdSuppressionKey + osdPermissionStatusHint, ready for Plan 39-06's SettingsView toggle; build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `handleOSDKeyPress` <-> live CoreAudio/DisplayServices reads | Untrusted-at-the-margins system state (a value could be `0`/`nil`/mid-transition during a rapid scrub) crossing into the `@Published` resolver-driven UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-05-01 | Denial of Service | `scheduleActivityDismiss()`'s generalized duration computation | mitigate | The `if case .osd = transientQueue.head` check reads the SAME `head` the rest of the function already gates on (`!head.isPersistent`), so no new race window is introduced — the duration lookup and the existing persistence guard observe one consistent snapshot |
| T-39-05-02 | Information Disclosure | Reading live system volume/brightness on every key press | accept | No PII, purely local device hardware state, already the entire purpose of this feature |
</threat_model>

<verification>
`xcodebuild build -scheme Islet -configuration Debug` succeeds. On-device timing/priority
behavior (does the 1.5s dismiss actually feel right, does Volume<->Brightness really swap
instantly, does a Volume press really interrupt a standing Focus pill) is verified in Plan
39-07's consolidated on-device UAT checkpoint, not here.
</verification>

<success_criteria>
The controller drives `OSDInterceptor`/`VolumeReader`/`BrightnessReader` end-to-end into
`IslandPresentation.osd`, with D-09/D-10/D-12/D-13's exact timing and priority rules implemented,
and `ActivitySettings` exposes the shared key/hint namespace Plan 39-06's Settings UI needs next.
</success_criteria>

<output>
Create `.planning/phases/39-volume-brightness-hud/39-05-SUMMARY.md` when done.
</output>
