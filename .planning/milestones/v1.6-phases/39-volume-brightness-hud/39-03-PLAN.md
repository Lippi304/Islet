---
phase: 39-volume-brightness-hud
plan: 03
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - Islet/Notch/OSDInterceptor.swift
  - Islet/Notch/VolumeReader.swift
  - Islet/Notch/BrightnessReader.swift
autonomous: true
requirements: [HUD-03, HUD-04]

must_haves:
  truths:
    - "The production OSDInterceptor implements the architecture Plan 39-01's on-device spike confirmed (dual-mode .listenOnly/.defaultTap, or the single-mode Accessibility-gates-everything fallback), not a guessed architecture"
    - "OSDInterceptor never auto-prompts for Accessibility (D-08) — permission is only ever requested via Plan 39-06's explicit Settings deep-link, never inside this file"
    - "The interceptor's health-check auto-upgrades from detect-only to detect+suppress the moment Accessibility becomes trusted mid-session, with no relaunch and no toggle re-flip required (D-07)"
    - "Every transport key (play/pause/next/previous) passes through completely untouched in every mode — the swallow decision only ever applies to the 5 volume/brightness key codes"
    - "Live volume reads use kAudioHardwareServiceDeviceProperty_VirtualMainVolume, never the renamed VirtualMasterVolume symbol (Pitfall 4)"
    - "A failed brightness read returns nil, never a false 0% (Security Domain: a private-framework symbol failure must suppress the Brightness HUD entirely, not render fabricated data)"
  artifacts:
    - path: "Islet/Notch/OSDInterceptor.swift"
      provides: "Production CGEventTap on NX_SYSDEFINED, mirrors DropInterceptTap's lifecycle skeleton with the spike-confirmed mode architecture"
      exports: ["OSDInterceptor", "OSDKeyKind"]
    - path: "Islet/Notch/VolumeReader.swift"
      provides: "readSystemVolume() -> (percent: Int, muted: Bool) via CoreAudio"
      exports: ["readSystemVolume"]
    - path: "Islet/Notch/BrightnessReader.swift"
      provides: "BrightnessReader.readBrightness() -> Int? via dynamically-loaded DisplayServices.framework"
      exports: ["BrightnessReader"]
  key_links:
    - from: "Islet/Notch/OSDInterceptor.swift"
      to: "Islet/Notch/NotchWindowController.swift"
      via: "onKeyPress: (OSDKeyKind) -> Void closure, called on main"
      pattern: "onKeyPress\\("
---

<objective>
Build the production CGEventTap (`OSDInterceptor.swift`) and the two level-reading glue files
(`VolumeReader.swift`, `BrightnessReader.swift`) — the phase's genuinely new, riskiest system
surface, now unblocked by Plan 39-01's on-device spike confirming exactly which architecture is
viable on this machine.

Purpose: this is where the phase's actual private/undocumented-API risk lives. Every other plan
in this phase is either pure logic (39-02) or a mechanical reapplication of an already-proven
pattern (39-04's wing view, 39-05's controller wiring). This plan is the one genuinely new thing.
Output: `OSDInterceptor.swift`, `VolumeReader.swift`, `BrightnessReader.swift` — three new,
isolated, single-purpose files, each following this project's "one fragile system surface, one
file" convention (`NowPlayingMonitor`, `PowerSourceMonitor`, `FocusModeMonitor`, `DropInterceptTap`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/39-volume-brightness-hud/39-CONTEXT.md
@.planning/phases/39-volume-brightness-hud/39-RESEARCH.md
@.planning/phases/39-volume-brightness-hud/39-PATTERNS.md
@.planning/phases/39-volume-brightness-hud/39-01-SUMMARY.md
</context>

<interfaces>
<!-- DropInterceptTap.swift, read in full (116 lines) — the lifecycle skeleton OSDInterceptor
     mirrors, with the deviations spelled out in the action text below -->
From Islet/Notch/DropInterceptTap.swift:
```swift
final class DropInterceptTap {
    private var machPort: CFMachPort?
    private var runLoopSource: CFRunLoopSource?
    private var healthCheckTimer: Timer?

    func start() {
        guard machPort == nil else { return }
        _ = AXIsProcessTrustedWithOptions([kAXTrustedCheckOptionPrompt.takeUnretainedValue(): true] as CFDictionary)
        guard let tap = CGEvent.tapCreate(
            tap: .cgSessionEventTap, place: .headInsertEventTap, options: .defaultTap,
            eventsOfInterest: CGEventMask(1 << CGEventType.leftMouseUp.rawValue),
            callback: { _, type, event, userInfo in /* ... */ },
            userInfo: Unmanaged.passUnretained(self).toOpaque()
        ) else { return }
        let source = CFMachPortCreateRunLoopSource(kCFAllocatorDefault, tap, 0)
        CFRunLoopAddSource(CFRunLoopGetMain(), source, .commonModes)
        CGEvent.tapEnable(tap: tap, enable: true)
        machPort = tap; runLoopSource = source
        healthCheckTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: true) { [weak self] _ in
            self?.checkHealthAndReinstallIfNeeded()
        }
    }
    nonisolated func stop() { /* removes source, disables tap, invalidates timer */ }
}
```

<!-- OSDActivity.swift (Plan 39-02) — OSDInterceptor does NOT depend on this type; it reports raw
     OSDKeyKind only, keeping the tap decode fully decoupled from the resolver/activity model -->
From Islet/Notch/OSDActivity.swift (Plan 39-02, already merged):
```swift
enum OSDActivity: Equatable {
    case volume(percent: Int, hardwareMuted: Bool)
    case brightness(percent: Int)
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: OSDInterceptor.swift — production dual-mode CGEventTap</name>
  <files>Islet/Notch/OSDInterceptor.swift</files>
  <read_first>
    - Islet/Notch/DropInterceptTap.swift (already read in full this session) — the lifecycle
      skeleton to mirror, with the required deviations below
    - .planning/phases/39-volume-brightness-hud/39-01-SUMMARY.md — READ THIS FIRST: it records
      which of `dual-mode-confirmed` / `single-mode-only` / `suppression-unreliable` the spike
      found, plus the confirmed NX_KEYTYPE_* keyCode values (do not trust this plan's own numbers
      over the spike's on-device-confirmed ones if they differ)
    - .planning/phases/39-volume-brightness-hud/39-RESEARCH.md Pattern 1 (dedicated-DispatchQueue
      tap skeleton) and Pattern 2 (bit-decode) and Pitfall 2 (health-check must also recover from
      `machPort == nil`, not just a disabled-but-existing tap) and Pitfall 3 (NSEvent construction
      must happen on main; decode fast, hop `.async` for the level-read/enqueue side-effect)
  </read_first>
  <action>
    Create `Islet/Notch/OSDInterceptor.swift`. Define `enum OSDKeyKind { case volume, brightness }`.
    Define `final class OSDInterceptor` with `init(suppressionArmed: @escaping () -> Bool,
    onKeyPress: @escaping (OSDKeyKind) -> Void)` — `suppressionArmed` is read FRESH on every
    single key event (never cached), so a Settings toggle flip takes effect on the very next
    press with no tap teardown needed; `onKeyPress` is called on main after the tap's swallow/pass
    decision has already been returned (never blocks the tap's return value).
    IF 39-01-SUMMARY.md recorded `dual-mode-confirmed`: implement `TapMode { case detectOnly,
    detectAndSuppress }`, a `desiredMode()` computed from `AXIsProcessTrusted()` alone (trusted ->
    `.detectAndSuppress` using `.defaultTap`, untrusted -> `.detectOnly` using `.listenOnly`), and
    a `reconcileMode()` called from the 5s health-check timer that tears down and reinstalls the
    tap whenever `desiredMode()` no longer matches the currently-installed mode (this is D-07's
    entire mechanism — no separate "permission granted" callback needed). Run the tap's run-loop
    source on a DEDICATED `DispatchQueue(label: "com.islet.osd-tap", qos: .userInteractive)`, NOT
    `CFRunLoopGetMain()` (Pitfall 1's required deviation from `DropInterceptTap` — rapid key-repeat
    scrubbing risks Droppy's documented main-thread-contention double-HUD bug). Track the
    dedicated queue's own `CFRunLoop` (captured via `CFRunLoopGetCurrent()` inside the `.async`
    block) so `reconcileMode()`'s teardown can call `CFRunLoopStop(...)` before installing a new
    tap — reinstalling without stopping the old run loop would silently stack a second
    `CFRunLoopRun()` behind the first on the same serial queue and never execute it.
    IF 39-01-SUMMARY.md recorded `single-mode-only`: implement a single always-`.defaultTap`
    interceptor (no `TapMode`, no mode-switching) that only ever installs the tap once
    `AXIsProcessTrusted()` is true, re-checking on the same 5s health-check cadence and installing
    the first time it flips true — this collapses D-06's "HUD shows without Accessibility" to "HUD
    requires the same single Accessibility gate as suppression"; note this explicitly in the
    SUMMARY as a locked deviation from CONTEXT.md D-06, superseded by the spike's finding.
    IF 39-01-SUMMARY.md recorded `suppression-unreliable`: implement a permanent `.listenOnly`-only
    interceptor that NEVER swallows any event regardless of `suppressionArmed()`'s value — the
    5th-tap-suppression code path is never built at all, and the Settings toggle (Plan 39-06)
    becomes a documented no-op that Plan 39-06 must gate its own copy around (flag this explicitly
    for Plan 39-06's SUMMARY if this branch is taken).
    In every mode, NEVER call `AXIsProcessTrustedWithOptions` with `kAXTrustedCheckOptionPrompt:
    true` anywhere in this file (D-08) — only the no-prompt `AXIsProcessTrusted()` query. Expose
    `static var isAccessibilityTrusted: Bool { AXIsProcessTrusted() }` for Plan 39-05/39-06 to read.
    Inside the tap callback: guard `type.rawValue == 14` (NX_SYSDEFINED) first, unconditional
    passthrough otherwise. Construct `NSEvent(cgEvent:)` inside a `DispatchQueue.main.sync` block
    (Pitfall 3 — this construction must run on main to avoid the Caps Lock/TSM crash Droppy
    documented; keep this sync block tiny — only the NSEvent construction + bit-decode + keyCode
    classification, no I/O) using the spike-confirmed keyCode allowlist
    `{0: .volume, 1: .volume, 7: .volume, 2: .brightness, 3: .brightness}` and keyDown mask.
    Every other code (including all 4 transport keys) returns from the sync block with `kind ==
    nil`, causing an immediate unconditional `Unmanaged.passUnretained(event)` passthrough BEFORE
    the swallow decision is even evaluated. When `kind != nil`, evaluate the swallow decision
    (`currentMode == .detectAndSuppress && suppressionArmed()` for dual-mode, or the single-mode
    equivalent) synchronously on the tap queue, dispatch `DispatchQueue.main.async { onKeyPress(kind) }`
    for the level-read/enqueue side effect (kept OFF the tap's critical return path per Pitfall
    3), then return `nil` (swallow) or `Unmanaged.passUnretained(event)` (pass) per that decision.
  </action>
  <acceptance_criteria>
    - `Islet/Notch/OSDInterceptor.swift` compiles with the exact mode architecture 39-01-SUMMARY.md recorded (verify the file's own header comment cites which of the 3 outcomes it implements)
    - `grep -n "AXIsProcessTrustedWithOptions" Islet/Notch/OSDInterceptor.swift | grep -v "false"` returns no matches with `true` (D-08 — never auto-prompts)
    - `grep -n "CFRunLoopGetMain" Islet/Notch/OSDInterceptor.swift` returns NO matches (Pitfall 1 — dedicated queue only)
    - `grep -c "case 0, 1, 7\|case 0: kind\|volumeCodes" Islet/Notch/OSDInterceptor.swift` confirms an explicit, bounded key-code allowlist exists (not a catch-all)
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` succeeds
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <done>OSDInterceptor.swift exists implementing the spike-confirmed architecture, never auto-prompts, runs its tap on a dedicated queue, and has a bounded key-code allowlist; build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: VolumeReader.swift (CoreAudio) + BrightnessReader.swift (DisplayServices)</name>
  <files>Islet/Notch/VolumeReader.swift, Islet/Notch/BrightnessReader.swift</files>
  <read_first>
    - Islet/Notch/PowerSourceMonitor.swift lines 26-55 (`readCurrentPower()`) — the exact
      defensive-optional-cast, always-return-a-safe-default discipline to mirror for
      `readSystemVolume()`
    - .planning/phases/39-volume-brightness-hud/39-RESEARCH.md Architecture Patterns "Pattern 3"
      (both code examples, CoreAudio volume read and DisplayServices dynamic-load brightness
      read) — cited, verified-against-SDK code to use directly, not reinvent
    - .planning/phases/39-volume-brightness-hud/39-RESEARCH.md Pitfall 4 —
      `kAudioHardwareServiceDeviceProperty_VirtualMainVolume`, never the renamed
      `VirtualMasterVolume` symbol
  </read_first>
  <action>
    Create `Islet/Notch/VolumeReader.swift` importing `CoreAudio`/`AudioToolbox`. Define
    `func readSystemVolume() -> (percent: Int, muted: Bool)` following RESEARCH Pattern 3's
    CoreAudio example: resolve the default output device via
    `kAudioHardwarePropertyDefaultOutputDevice`, then read
    `kAudioHardwareServiceDeviceProperty_VirtualMainVolume` (NOT `VirtualMasterVolume` — Pitfall
    4) and `kAudioDevicePropertyMute`, each via `AudioObjectGetPropertyData` with a defensive
    `guard ... == noErr else { return (0, false) }` at every step (mirrors
    `readCurrentPower()`'s discipline — a missing/malformed value never force-unwraps or crashes,
    it falls back to a safe `(0, false)` reading, which downstream `osdVolumeActivity`'s clamp
    already handles gracefully). Round the raw `Float32` volume (0.0...1.0) to a percent Int via
    `Int((volume * 100).rounded())`.
    Create `Islet/Notch/BrightnessReader.swift`. Define `final class BrightnessReader` using
    RESEARCH Pattern 3's dynamic-load skeleton: a `@convention(c)` function-pointer typealias for
    `DisplayServicesGetBrightness`, loaded via `CFBundleCreate`/`CFBundleLoadExecutable`/
    `CFBundleGetFunctionPointerForName` against
    `/System/Library/PrivateFrameworks/DisplayServices.framework` in `init()`, with `getBrightness`
    left `nil` if ANY step of that chain fails (guard-chained, never force-unwrapped). Define
    `func readBrightness() -> Int?` that returns `nil` immediately if `getBrightness` is `nil`,
    otherwise calls it against `CGMainDisplayID()` and returns `nil` if the call's own status
    code is non-zero — this file must NEVER return a bare `0` for a failed read (Security Domain:
    a private-framework symbol failure must suppress the Brightness HUD entirely, distinct from
    `PowerReading`'s all-fields-defaulted convention — `Int?`, not a defaulted `Int`, is the
    deliberate divergence here).
  </action>
  <acceptance_criteria>
    - `grep -n "VirtualMainVolume" Islet/Notch/VolumeReader.swift` finds a match; `grep -n "VirtualMasterVolume" Islet/Notch/VolumeReader.swift` finds NO match
    - `readSystemVolume()` never force-unwraps — `grep -c "!" Islet/Notch/VolumeReader.swift | grep -v "!="` shows no bare force-unwrap operators outside of `!=` comparisons
    - `BrightnessReader.readBrightness()`'s return type is `Int?`, not `Int` — confirmed by its declaration
    - `xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug` succeeds
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild build -project Islet.xcodeproj -scheme Islet -configuration Debug</automated>
  </verify>
  <done>VolumeReader.swift and BrightnessReader.swift exist, both defensively cast every system value, use the correct (non-renamed) CoreAudio symbol, and BrightnessReader returns nil (never a false 0%) on any read failure; build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Islet process <-> WindowServer NX_SYSDEFINED stream | Untrusted-shape private-encoding system event data crossing into this process via CGEventTap |
| Islet process <-> `DisplayServices.framework` (private, dynamically loaded) | An unversioned, undocumented system framework whose symbol could vanish/change in a future macOS point release |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-03-01 | Denial of Service | `OSDInterceptor` transport-key passthrough | mitigate | Bounded, explicit key-code allowlist; every other code (including all 4 transport keys) is unconditional passthrough BEFORE the swallow decision is even evaluated (Task 1 acceptance criteria) |
| T-39-03-02 | Denial of Service | Revoked Accessibility mid-session causing a disabled-tap re-enable fight | mitigate | `reconcileMode()`/health-check re-checks `AXIsProcessTrusted()` (no-prompt query) before any reinstall attempt — mirrors this project's own documented Droppy-sourced fix for exactly this bug class (RESEARCH Security Domain) |
| T-39-03-03 | Elevation of Privilege | `AXIsProcessTrustedWithOptions` usage anywhere in this file | mitigate | Never called with `prompt: true` — only the no-prompt `AXIsProcessTrusted()` query is used; the real permission ask lives entirely in Plan 39-06's explicit user-initiated deep link (D-08) |
| T-39-03-04 | Tampering | Private-framework symbol resolution silently misread as valid data | mitigate | `BrightnessReader.readBrightness()` returns `Int?`; a failed load or failed call NEVER produces a fabricated `0%` — the Brightness HUD is suppressed entirely on failure (Task 2 acceptance criteria) |
| T-39-03-SC | Tampering | New Apple-framework usage (CoreAudio, DisplayServices) | accept | No new npm/pip/cargo/SPM packages added this phase (RESEARCH's Package Legitimacy Audit: not applicable) — every API is a public or system-signed Apple framework, no registry-verification gate applies |
</threat_model>

<verification>
`xcodebuild build -scheme Islet -configuration Debug` succeeds for both tasks. On-device
behavior (does the tap actually suppress, does the level read reflect reality) is verified in
Plan 39-07's consolidated UAT checkpoint, not here — this plan's own gate is build-clean +
architectural-discipline greps only.
</verification>

<success_criteria>
`OSDInterceptor.swift`, `VolumeReader.swift`, and `BrightnessReader.swift` exist, implement the
spike-confirmed architecture exactly, never auto-prompt for Accessibility, and give Plans 39-04/
39-05 a locked system-glue contract (`OSDKeyKind`, `readSystemVolume()`, `BrightnessReader`) to
build against.
</success_criteria>

<output>
Create `.planning/phases/39-volume-brightness-hud/39-03-SUMMARY.md` when done. MUST record which
of the 3 spike-confirmed architectures was implemented.
</output>
