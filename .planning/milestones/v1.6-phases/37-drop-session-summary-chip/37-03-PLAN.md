---
phase: 37-drop-session-summary-chip
plan: 03
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - Islet/Notch/NotchWindowController.swift
autonomous: true
requirements: [HUD-07]

must_haves:
  truths:
    - "Closing Tray (island collapses while viewSwitcherState.selectedView == .tray) after >=1 drop this session sets shelfViewState.sessionSummaryChip and schedules its ~2s auto-dismiss (D-01/D-02/D-03/D-06)"
    - "Closing Tray with zero drops this session sets no chip, and the session counter is still reset to 0 (D-02)"
    - "A collapse while a NON-Tray tab is selected neither shows a chip nor resets the session counter (D-01's literal trigger scope)"
    - "Re-expanding the island (click) while the chip is showing cancels its dismiss timer and clears it immediately (D-07)"
    - "A Charging/Device transient becoming active while the chip is showing clears it immediately (D-06/D-07 precedent, mirrors the existing song-change-toast interrupt)"
  artifacts:
    - path: "Islet/Notch/NotchWindowController.swift"
      provides: "chipDismissWorkItem/chipDismissDuration + scheduleChipDismiss() + collapse-trigger + interrupt-clear wiring"
  key_links:
    - from: "handleHoverExit's graceWorkItem / handleClick's toggle-shut branch"
      to: "shelfCoordinator.resetSession() -> dropSessionChipGate(...) -> dropSessionChipContent(...) -> shelfViewState.sessionSummaryChip"
      via: "viewSwitcherState.selectedView == .tray guard"
      pattern: "shelfCoordinator\\.resetSession\\(\\)"
    - from: "presentTransientChange / handleClick's re-expand branch"
      to: "shelfViewState.sessionSummaryChip = nil"
      via: "chipDismissWorkItem?.cancel() + field clear, mirroring the existing songChangeToast interrupt sites"
      pattern: "chipDismissWorkItem\\?\\.cancel\\(\\)"
---

<objective>
Wire the D-01/D-02/D-03/D-06/D-07 controller lifecycle for the drop-session chip: fire it at the
exact moment the island collapses while Tray was the selected tab, auto-dismiss it after ~2s, and
clear it immediately on either a re-expand or an interrupting Charging/Device transient — mirroring
Phase 18's `songChangeToast` lifecycle (`toastDismissWorkItem`/`songToastDuration`,
`presentTransientChange`'s interrupt-clear, `handleClick`'s re-expand interrupt-clear) at every
site, one-for-one.

Purpose: connects Plan 01's pure state (`ShelfCoordinator.resetSession()`, `dropSessionChipGate`,
`dropSessionChipContent`) to the live `@Published` field Plan 02's view renders.
Output: `shelfViewState.sessionSummaryChip` is set/cleared correctly across every collapse, resume,
and interrupt scenario in Success Criteria #2/#3/#4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-drop-session-summary-chip/37-CONTEXT.md
@.planning/phases/37-drop-session-summary-chip/37-PATTERNS.md
</context>

<interfaces>
<!-- From Plan 01 -- already merged before this plan runs -->
```swift
// Islet/Shelf/ShelfCoordinator.swift
private(set) var sessionFilesSaved: Int
@discardableResult func resetSession() -> Int   // captures + zeroes sessionFilesSaved, returns pre-reset count

// Islet/Notch/IslandResolver.swift
func dropSessionChipGate(activeTransient: ActiveTransient?, isExpanded: Bool) -> Bool

// Islet/Shelf/ShelfViewState.swift
struct SessionSummaryChip: Equatable { let count: Int }
func dropSessionChipContent(count: Int) -> SessionSummaryChip?
// ShelfViewState (already owned by NotchWindowController as `private let shelfViewState`):
@Published var sessionSummaryChip: SessionSummaryChip? = nil
```

<!-- Existing controller state this plan reuses as the direct structural template (Phase 18) -->
```swift
private var toastDismissWorkItem: DispatchWorkItem?
private let songToastDuration: TimeInterval = 2.0
private func scheduleToastDismiss() { /* cancel-then-reschedule, clears nowPlayingState.songChangeToast */ }
```
`private let shelfCoordinator = ShelfCoordinator()` and `private let shelfViewState =
ShelfViewState()` are both already-owned stored properties on `NotchWindowController`
(`transientQueue.head: ActiveTransient?`, `interaction.isExpanded: Bool`,
`viewSwitcherState.selectedView: SelectedView` are likewise already-owned and already read
elsewhere in this file — no new properties needed for these).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Collapse-trigger wiring + scheduleChipDismiss() (D-01/D-02/D-03/D-06)</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift lines 205-221 (the `toastDismissWorkItem`/
    `songToastDuration` property declarations — the exact pattern to mirror for the new chip
    timer properties)
    Islet/Notch/NotchWindowController.swift lines 1260-1304 (`handleHoverExit` — the
    `graceWorkItem` closure body: the `!self.isDraggingShelfItem`/`!self.isOnboardingActive`
    guards, `self.interaction.phase = nextState(...)`, `self.renderPresentation()`, the existing
    `if !self.interaction.isExpanded { self.discardPendingDrop() }` collapse-confirmation check —
    the exact insertion point)
    Islet/Notch/NotchWindowController.swift lines 1309-1352 (`handleClick` — `wasExpanded`
    capture, the `withAnimation` block, the existing `if !wasExpanded && interaction.isExpanded
    && nowPlayingState.songChangeToast != nil` toast-interrupt branch, the trailing `if
    !interaction.isExpanded { discardPendingDrop() }` toggle-shut confirmation — the exact
    insertion point for the SYMMETRIC toggle-shut-collapse case)
    Islet/Notch/NotchWindowController.swift lines 1865-1879 (`scheduleToastDismiss()` — the exact
    cancel-then-reschedule `DispatchWorkItem` pattern to mirror)
  </read_first>
  <action>
    Add two new stored properties directly below `songToastDuration` (line 221): `private var
    chipDismissWorkItem: DispatchWorkItem?` and `private let chipDismissDuration: TimeInterval =
    2.0` — mirror the `// Phase 18 / NOW-05 (D-03)` comment style, citing `Phase 37 / HUD-07
    (D-04)` and noting this timer is fully independent of `toastDismissWorkItem` (a song-change
    toast and a drop chip can both be showing at once, per Plan 02's stacked-rows design).

    Add a new private method `scheduleChipDismiss()` directly after `scheduleToastDismiss()`
    (after line 1879), structurally identical: cancel `chipDismissWorkItem`, build a
    `DispatchWorkItem` that (inside the same `withAnimation(.spring(response: springResponse,
    dampingFraction: springDamping))` wrapper) sets `self.shelfViewState.sessionSummaryChip =
    nil`, store it in `chipDismissWorkItem`, and `DispatchQueue.main.asyncAfter(deadline: .now() +
    chipDismissDuration, execute: work)`.

    Add the SAME collapse-trigger logic at BOTH of these two sites (do not extract a shared
    private helper — mirror the existing pattern where `handleHoverExit`'s grace-collapse and
    `handleClick`'s toggle-shut collapse each inline their own toast-interrupt/discard-pendingDrop
    logic separately):

    1. In `handleHoverExit`'s `graceWorkItem` closure, immediately after the existing `if
    !self.interaction.isExpanded { self.discardPendingDrop() }` line (still inside the same
    `withAnimation` block): guard `self.viewSwitcherState.selectedView == .tray`; inside that
    guard, call `let count = self.shelfCoordinator.resetSession()` unconditionally, then check
    `dropSessionChipGate(activeTransient: self.transientQueue.head, isExpanded:
    self.interaction.isExpanded)` together with `dropSessionChipContent(count: count)` returning
    non-nil — when both hold, assign the result to `self.shelfViewState.sessionSummaryChip` and
    call `self.scheduleChipDismiss()`.

    2. In `handleClick`'s `withAnimation` block, immediately after the existing `if
    !interaction.isExpanded { discardPendingDrop() }` line: add the symmetric check guarded by
    `wasExpanded && !interaction.isExpanded` (the toggle-SHUT case specifically, never the
    toggle-open case) with the identical body as site 1 above (using this function's existing
    non-closure style — plain `interaction`/`transientQueue`/`shelfCoordinator`/`shelfViewState`/
    `viewSwitcherState` references, no `self.` prefix, matching the surrounding code in this
    method), calling `scheduleChipDismiss()` on success.

    Both sites must call `shelfCoordinator.resetSession()` UNCONDITIONALLY whenever
    `viewSwitcherState.selectedView == .tray` at the collapse instant — even when the returned
    count is 0 (D-02: the boundary always resets at a Tray-selected collapse, chip-or-not). Do
    NOT call `resetSession()` when the selected view is something other than `.tray` — D-01's
    trigger is scoped to a Tray-selected collapse only; an ordinary collapse from Home/Calendar/
    Weather must leave `sessionFilesSaved` untouched so a later Tray-selected collapse still sees
    the accumulated count.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS' 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func scheduleChipDismiss" Islet/Notch/NotchWindowController.swift` returns 1
    - `grep -c "shelfCoordinator.resetSession()" Islet/Notch/NotchWindowController.swift` returns exactly 2 (handleHoverExit site + handleClick site)
    - `grep -c "dropSessionChipGate" Islet/Notch/NotchWindowController.swift` returns exactly 2
    - `grep -B4 "shelfCoordinator.resetSession()" Islet/Notch/NotchWindowController.swift | grep -c "selectedView == .tray"` returns exactly 2 (both call sites are guarded)
    - Build succeeds
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Interrupt-clear wiring on re-expand and transient takeover (D-07)</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift lines 701-716 (`presentTransientChange` — the exact
    `if nowPlayingState.songChangeToast != nil { toastDismissWorkItem?.cancel();
    nowPlayingState.songChangeToast = nil }` interrupt-clear block, fired the instant
    `transientQueue.head` transitions nil -> non-nil)
    Islet/Notch/NotchWindowController.swift lines 1309-1325 (`handleClick`'s existing `if
    !wasExpanded && interaction.isExpanded && nowPlayingState.songChangeToast != nil {
    toastDismissWorkItem?.cancel(); nowPlayingState.songChangeToast = nil }` re-expand
    interrupt-clear block — CONTEXT.md D-07's own cited precedent)
  </read_first>
  <action>
    In `presentTransientChange` (lines 701-716), add a sibling check alongside the existing
    `songChangeToast` interrupt block, inside the same `withAnimation` wrapper: when
    `shelfViewState.sessionSummaryChip != nil`, cancel `chipDismissWorkItem` and set
    `shelfViewState.sessionSummaryChip = nil`. Place it directly after the existing
    `songChangeToast` block, before `renderPresentation()`.

    In `handleClick`'s re-expand branch (lines 1315-1325), add a sibling check alongside the
    existing `songChangeToast` re-expand interrupt: guarded by the identical `!wasExpanded &&
    interaction.isExpanded` condition, when `shelfViewState.sessionSummaryChip != nil`, cancel
    `chipDismissWorkItem` (not `toastDismissWorkItem`) and set `shelfViewState.sessionSummaryChip
    = nil` (not `nowPlayingState.songChangeToast`). Place it directly after the existing
    `songChangeToast` re-expand block, before the `pruneMissingFiles()` check.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS' 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "chipDismissWorkItem" Islet/Notch/NotchWindowController.swift` returns at least 5 (declaration, cancel+assign in `scheduleChipDismiss`, cancel in `presentTransientChange`, cancel in `handleClick`'s re-expand branch)
    - `grep -c "shelfViewState.sessionSummaryChip = nil" Islet/Notch/NotchWindowController.swift` returns at least 3 (`scheduleChipDismiss`, `presentTransientChange`, `handleClick` re-expand)
    - `grep -A2 "presentTransientChange" Islet/Notch/NotchWindowController.swift | grep -c "sessionSummaryChip"` returns at least 1
    - Build succeeds
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Controller-side timer/state wiring only, over already-vetted inputs (`transientQueue.head`, `interaction.isExpanded`, `viewSwitcherState.selectedView` are all pre-existing, already-trusted local state). No new external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04 | Denial of Service | `chipDismissWorkItem` | mitigate | Cancel-then-reschedule discipline (mirrors `toastDismissWorkItem` exactly) prevents a leaked/duplicated timer from accumulating across rapid collapse/re-expand cycles. |
</threat_model>

<verification>
Build-gate only (`xcodebuild build`) — the full end-to-end lifecycle (collapse-with-drops,
collapse-with-zero-drops, non-Tray collapse, re-expand interrupt, transient interrupt) is
verified on-device in Plan 04's consolidated UAT checkpoint, since it depends on Plan 02's
rendering to be observable at all.
</verification>

<success_criteria>
- `scheduleChipDismiss()` exists, mirrors `scheduleToastDismiss()`'s cancel-then-reschedule shape
- Both collapse sites (`handleHoverExit`, `handleClick`) call `resetSession()` only when Tray was selected, gate via `dropSessionChipGate`/`dropSessionChipContent`, and schedule the dismiss on success
- Both interrupt sites (`presentTransientChange`, `handleClick` re-expand) clear the chip and cancel its timer, mirroring the existing `songChangeToast` interrupt sites exactly
- Build succeeds
</success_criteria>

<output>
Create `.planning/phases/37-drop-session-summary-chip/37-03-SUMMARY.md` when done
</output>
