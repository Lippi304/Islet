---
phase: 37-drop-session-summary-chip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Shelf/ShelfCoordinator.swift
  - IsletTests/ShelfCoordinatorTests.swift
  - Islet/Notch/IslandResolver.swift
  - IsletTests/IslandResolverTests.swift
  - Islet/Shelf/ShelfViewState.swift
  - IsletTests/ShelfViewStateTests.swift
autonomous: true
requirements: [HUD-07]

must_haves:
  truths:
    - "Every successful ShelfCoordinator.append(_:) call during a session increments a gross counter that survives later remove()/clear() calls on the same items (D-03)"
    - "A caller can atomically read-and-zero the session counter (D-02's reset-at-collapse contract)"
    - "The chip's suppression gate returns false whenever a Charging/Device transient is active or the island is expanded, mirroring songChangeToastGate's shape (D-06)"
    - "The chip's content function returns nil for a zero-drop session (no chip on an empty session, Success Criterion #3) and a populated struct otherwise"
  artifacts:
    - path: "Islet/Shelf/ShelfCoordinator.swift"
      provides: "sessionFilesSaved gross counter + resetSession() atomic read-and-zero"
    - path: "Islet/Notch/IslandResolver.swift"
      provides: "dropSessionChipGate(activeTransient:isExpanded:) pure suppression gate"
    - path: "Islet/Shelf/ShelfViewState.swift"
      provides: "SessionSummaryChip struct + dropSessionChipContent(count:) pure seam + sessionSummaryChip @Published field"
  key_links:
    - from: "ShelfCoordinator.append(_:)"
      to: "ShelfCoordinator.sessionFilesSaved"
      via: "increment only when logic.append(item) returns true"
      pattern: "sessionFilesSaved \\+= 1"
---

<objective>
Add the missing shelf-session-boundary concept (HUD-07's core prerequisite): a gross per-session
drop counter on `ShelfCoordinator`, an atomic reset contract for the controller to claim it at
Tray-close, and the two pure seams (suppression gate + content derivation) the chip's rendering
and controller wiring will consume in later waves. Pure logic only — no AppKit, no SwiftUI,
no controller wiring. Mirrors this project's own pure-seams-first convention (Phase 19 shelf
model, Phase 18 song-change-toast gate/content functions).

Purpose: every later plan in this phase (view rendering, controller wiring) depends on these
exact three artifacts existing with these exact signatures.
Output: `ShelfCoordinator.sessionFilesSaved`/`resetSession()`, `IslandResolver.dropSessionChipGate(...)`,
`ShelfViewState.SessionSummaryChip`/`dropSessionChipContent(count:)`/`sessionSummaryChip`, all unit-tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-drop-session-summary-chip/37-CONTEXT.md
@.planning/phases/37-drop-session-summary-chip/37-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ShelfCoordinator gross session counter (D-02/D-03)</name>
  <files>Islet/Shelf/ShelfCoordinator.swift, IsletTests/ShelfCoordinatorTests.swift</files>
  <read_first>
    Islet/Shelf/ShelfCoordinator.swift (full file — append/remove/clear/pruneMissingFiles, the
    existing `@discardableResult` + WR-01 duplicate-cleanup convention around `append(_:)`)
    IsletTests/ShelfCoordinatorTests.swift (full file — the real-disk-I/O fixture convention:
    `fixturesDir` setUp/tearDown, `makeRealItem(named:)` helper, existing append/remove/clear tests)
  </read_first>
  <behavior>
    - Test: appending a genuinely new item (added == true) increments `sessionFilesSaved` by 1.
    - Test: appending a rejected duplicate (added == false, the existing WR-01 path) does NOT
      increment `sessionFilesSaved`.
    - Test: `remove(id:)` on a previously-appended item does NOT decrement `sessionFilesSaved`
      (D-03 gross semantics — a later delete never reduces the reported count).
    - Test: `clear()` after 2 appends does NOT reset `sessionFilesSaved` to 0 (same gross rule —
      only `resetSession()` may zero it).
    - Test: `resetSession()` returns the current count and leaves `sessionFilesSaved` at 0
      afterward; a second immediate `resetSession()` call returns 0.
  </behavior>
  <action>
    Add `private(set) var sessionFilesSaved: Int = 0` as a new stored property on
    `ShelfCoordinator`, declared alongside `logic`. Inside the existing `append(_:)` method, add
    `sessionFilesSaved += 1` in the `added == true` branch (the branch that currently does
    nothing extra — do NOT touch the `!added` branch's existing `ShelfFileStore.deleteSessionCopy`
    call). Do not touch `remove(id:)`, `clear()`, or `pruneMissingFiles()` — D-03 requires the
    counter to survive all three. Add a new method `@discardableResult func resetSession() -> Int`
    that captures `sessionFilesSaved` into a local, sets `sessionFilesSaved = 0`, and returns the
    captured value. Document both additions with a `// Phase 37 / HUD-07 (D-02/D-03)` comment
    explaining the gross-vs-net distinction and that `resetSession()` is the caller's (Plan 03's
    `NotchWindowController`) sole entry point for claiming the count at Tray-selected collapse.
  </action>
  <verify>
    <automated>xcodebuild test -scheme Islet -destination 'platform=macOS' -only-testing:IsletTests/ShelfCoordinatorTests 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sessionFilesSaved" Islet/Shelf/ShelfCoordinator.swift` returns at least 3 (declaration, increment, reset)
    - `grep -c "func resetSession" Islet/Shelf/ShelfCoordinator.swift` returns 1
    - All 5 new tests in `ShelfCoordinatorTests.swift` pass, plus every pre-existing test in that file still passes (no regression)
    - `remove(id:)` and `clear()` method bodies in the diff are byte-identical to before this task (grep confirms no `sessionFilesSaved` reference inside either method)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Suppression gate + content-derivation pure seams (D-05/D-06)</name>
  <files>Islet/Notch/IslandResolver.swift, IsletTests/IslandResolverTests.swift, Islet/Shelf/ShelfViewState.swift, IsletTests/ShelfViewStateTests.swift</files>
  <read_first>
    Islet/Notch/IslandResolver.swift lines 150-200 (songChangeToastGate at line 173-175 — the
    exact shape to mirror; PendingBatteryPoll struct immediately below it, the insertion point)
    IsletTests/IslandResolverTests.swift lines 160-195 (songChangeToastGate's own 5-test block —
    the exact naming/assertion convention to mirror)
    Islet/Shelf/ShelfViewState.swift (full file — `shouldOpenShelfItem`/`shouldBeginShelfItemDrag`
    TOTAL pure gate convention at the bottom of the file, and the `ShelfViewState` class's
    existing `@Published var items` field)
    IsletTests/ShelfViewStateTests.swift (full file — `testShouldOpenShelfItemGate` pattern for
    a standalone pure-function test with no fixture)
  </read_first>
  <behavior>
    - Test (IslandResolverTests): `dropSessionChipGate(activeTransient: .charging(...), isExpanded: false)` returns false
    - Test (IslandResolverTests): `dropSessionChipGate(activeTransient: .device(...), isExpanded: false)` returns false
    - Test (IslandResolverTests): `dropSessionChipGate(activeTransient: nil, isExpanded: true)` returns false
    - Test (IslandResolverTests): `dropSessionChipGate(activeTransient: nil, isExpanded: false)` returns true
    - Test (ShelfViewStateTests): `dropSessionChipContent(count: 0)` returns nil
    - Test (ShelfViewStateTests): `dropSessionChipContent(count: 3)` returns `SessionSummaryChip(count: 3)`
  </behavior>
  <action>
    In `Islet/Notch/IslandResolver.swift`, add a new top-level function immediately after
    `songChangeToastGate` (before the `PendingBatteryPoll` struct): `func dropSessionChipGate(activeTransient: ActiveTransient?, isExpanded: Bool) -> Bool { activeTransient == nil && !isExpanded }`.
    This intentionally has only 2 params, not 3 — there is no Settings toggle for this chip
    (unlike `songChangeToastGate`'s `toastEnabled`), so the "identical shape" from D-06 covers the
    `activeTransient`/`isExpanded` pair only. Add a doc comment citing `Phase 37 / HUD-07 (D-06)`
    and referencing `songChangeToastGate`'s own rationale comment for why this stays a standalone
    function outside `resolve(...)`.

    In `Islet/Shelf/ShelfViewState.swift`, add (in this order, after the existing `ShelfViewState`
    class, alongside `shouldOpenShelfItem`/`shouldBeginShelfItemDrag`): a new
    `struct SessionSummaryChip: Equatable { let count: Int }`, and a new
    `func dropSessionChipContent(count: Int) -> SessionSummaryChip? { count > 0 ? SessionSummaryChip(count: count) : nil }`.
    Then add `@Published var sessionSummaryChip: SessionSummaryChip? = nil` as a new stored
    property on the `ShelfViewState` class itself, declared directly below the existing
    `@Published var items: [ShelfItem] = []` line. Document with a `// Phase 37 / HUD-07
    (D-03/D-05)` comment noting `count` stays a raw `Int` — pluralization ("1 file saved" vs "N
    files saved") is a Plan 02 view-layer concern, not baked in here.
  </action>
  <verify>
    <automated>xcodebuild test -scheme Islet -destination 'platform=macOS' -only-testing:IsletTests/IslandResolverTests -only-testing:IsletTests/ShelfViewStateTests 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func dropSessionChipGate" Islet/Notch/IslandResolver.swift` returns 1
    - `grep -c "struct SessionSummaryChip" Islet/Shelf/ShelfViewState.swift` returns 1
    - `grep -c "func dropSessionChipContent" Islet/Shelf/ShelfViewState.swift` returns 1
    - `grep -c "sessionSummaryChip" Islet/Shelf/ShelfViewState.swift` returns at least 1 (the new `@Published` field)
    - All 6 new tests pass, plus every pre-existing test in both files still passes (no regression)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan adds pure counting/gating logic over already-vetted shelf appends (Phase 19-21's own threat model already covers the underlying file-copy-in/drag paths). No new external input, no new parsing, no new persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Information Disclosure | `sessionFilesSaved` / `SessionSummaryChip.count` | accept | A small in-memory integer, never persisted, never logged, reflects only local same-process drop activity already visible to the user via the Tray strip itself — no new disclosure surface. |
| T-37-02 | Denial of Service | `sessionFilesSaved` counter growth | accept | Bounded by realistic drag-drop volume in a single session (tens of files at most); `Int` overflow is not a realistic threat at this scale. |
</threat_model>

<verification>
Both tasks are pure Foundation logic (no AppKit/SwiftUI) — verified entirely via `xcodebuild test`
targeting the 3 modified test files. No on-device check needed for this plan (rendering/wiring
plans in later waves carry the on-device UAT).
</verification>

<success_criteria>
- `ShelfCoordinator.sessionFilesSaved`/`resetSession()` exist with D-02/D-03 gross semantics, unit-tested
- `dropSessionChipGate` exists in `IslandResolver.swift`, mirrors `songChangeToastGate`'s 2-input shape, unit-tested
- `SessionSummaryChip`/`dropSessionChipContent`/`sessionSummaryChip` exist in `ShelfViewState.swift`, unit-tested
- All existing tests in the 4 modified files (2 production, 2 test) continue to pass
</success_criteria>

<output>
Create `.planning/phases/37-drop-session-summary-chip/37-01-SUMMARY.md` when done
</output>
