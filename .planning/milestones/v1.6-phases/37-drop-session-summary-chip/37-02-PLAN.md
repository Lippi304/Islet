---
phase: 37-drop-session-summary-chip
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - Islet/Notch/NotchPillView.swift
autonomous: true
requirements: [HUD-07]

must_haves:
  truths:
    - "When shelfViewState.sessionSummaryChip is non-nil and the island is at the .idle presentation (nothing playing), the collapsed pill grows and shows the chip text (D-04)"
    - "When shelfViewState.sessionSummaryChip is non-nil and the island is at the .nowPlayingWings presentation (media playing), the media wings grow further and show the chip text below whatever else is already showing (D-04)"
    - "Chip text reads '1 file saved' for count == 1 and 'N files saved' for count >= 2 (D-05)"
    - "When sessionSummaryChip is nil, both collapsedIsland and mediaWingsOrToast render exactly as they did before this plan (no regression)"
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "chipTextRow(_:) + chip-aware growth in collapsedIsland and mediaWingsOrToast"
  key_links:
    - from: "NotchPillView.collapsedIsland / mediaWingsOrToast"
      to: "shelfViewState.sessionSummaryChip"
      via: "@ObservedObject var shelfViewState already present on NotchPillView"
      pattern: "shelfViewState\\.sessionSummaryChip"
---

<objective>
Render the drop-session summary chip as a verbatim reuse of Phase 18's song-change-toast growth
mechanics (D-04), wired into BOTH collapsed-state render paths — `collapsedIsland` (the `.idle`
case, nothing playing) and `mediaWingsOrToast` (the `.nowPlayingWings` case, media playing) —
since Tray can close into either ambient state depending on whether music happens to be playing
at that moment. No new visual language: same `toastExtraHeight` growth, same fading
`.transition(.opacity)` text row, same `wingsSize`-based width.

Purpose: give the pure state from Plan 01 (`shelfViewState.sessionSummaryChip`) a renderer, so
Plan 03's controller wiring has something to observably drive.
Output: chip renders correctly in both ambient collapsed states; existing toast/idle rendering
unchanged when no chip is present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-drop-session-summary-chip/37-CONTEXT.md
@.planning/phases/37-drop-session-summary-chip/37-PATTERNS.md
</context>

<interfaces>
<!-- From Plan 01 (Islet/Shelf/ShelfViewState.swift) — already merged before this plan runs -->
```swift
struct SessionSummaryChip: Equatable {
    let count: Int
}
// ShelfViewState (existing @ObservedObject already wired into NotchPillView as `shelfViewState`):
@Published var sessionSummaryChip: SessionSummaryChip? = nil
```

<!-- Existing NotchPillView.swift constants/functions this plan reuses verbatim (do not redeclare) -->
```swift
static let wingsSize = CGSize(width: 290, height: 32)
static let toastExtraHeight: CGFloat = 32
```
`toastTextRow(_ toast: TrackToast) -> some View` at line 2074 — the exact structural template
for the new `chipTextRow(_:)`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: chipTextRow(_:) + wire into collapsedIsland and mediaWingsOrToast (D-04/D-05)</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift lines 816-850 (`collapsedIsland` — the `.idle` render
    function: `let size = interaction.collapsedNotchSize ?? Self.collapsedSize`, the `shape`
    hoisted-before-fill convention, `.matchedGeometryEffect` MUST precede `.frame` per the
    2026-07-15 diagonal-bounce bugfix comment at lines 831-838, `liquidGlassEffectLayer(...,
    parameters: .collapsed)`, `.onTapGesture { onClick() }`)
    Islet/Notch/NotchPillView.swift lines 2015-2084 (`mediaWingsOrToast(_:)` and `toastTextRow(_:)`
    — the exact growth-overlay-VStack pattern and text-row styling to mirror)
    Islet/Notch/NotchPillView.swift lines 100-122 (the `@ObservedObject var shelfViewState:
    ShelfViewState` declaration — already present, no new property needed)
  </read_first>
  <action>
    Add a new private function `chipTextRow(_ chip: SessionSummaryChip) -> some View` directly
    after `toastTextRow(_:)` (around line 2084), structurally identical to `toastTextRow`
    (same font `.system(size: 12, weight: .medium, design: .rounded)`, `.foregroundStyle(.white)`,
    `.lineLimit(1)`, `.truncationMode(.tail)`, `.padding(.horizontal, 16)`, `.frame(width:
    Self.wingsSize.width, height: Self.toastExtraHeight, alignment: .center)`), but with content
    `Text(chip.count == 1 ? "1 file saved" : "\(chip.count) files saved")` (D-05 pluralization).

    Modify `mediaWingsOrToast(_ p: NowPlayingPresentation)`: add
    `let chip = shelfViewState.sessionSummaryChip` alongside the existing `let toast =
    nowPlaying.songChangeToast`. Change the `height` computation to add a second conditional term:
    `Self.wingsSize.height + (toast != nil ? Self.toastExtraHeight : 0) + (chip != nil ?
    Self.toastExtraHeight : 0)`. Inside the `VStack` overlay, after the existing `if let toast {
    toastTextRow(toast).transition(.opacity) }` block, add a sibling `if let chip {
    chipTextRow(chip).transition(.opacity) }` — both rows can be visible simultaneously (a
    song-change toast and a drop-session chip are independent one-shot fields; stack them, do not
    make one suppress the other).

    Modify `collapsedIsland`: add `let chip = shelfViewState.sessionSummaryChip` at the top of
    the function body. When `chip != nil`, the shape's `.frame` must use `Self.wingsSize.width`
    for width and `Self.wingsSize.height + Self.toastExtraHeight` for height, INSTEAD of the
    current `size.width`/`size.height` (derived from `interaction.collapsedNotchSize ??
    Self.collapsedSize`). When `chip == nil`, keep the existing `size`-based frame completely
    unchanged (no regression to the D-01 hardware-notch-merge sizing). Preserve the existing
    ordering: `.matchedGeometryEffect(id: "island", in: ns)` BEFORE `.frame(...)` in both branches
    (per the diagonal-bounce bugfix). Add `.overlay(alignment: .top) { if let chip {
    chipTextRow(chip) } }` after the existing `.overlay(liquidGlassEffectLayer(...))` line —
    since `collapsedIsland` has no existing top-row content (unlike `mediaWingsOrToast`'s
    `mediaWingsRow`), this overlay is chip-only, no VStack needed. Leave `.scaleEffect`, `.offset`,
    and `.onTapGesture` untouched.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS' 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func chipTextRow" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "shelfViewState.sessionSummaryChip" Islet/Notch/NotchPillView.swift` returns at least 2 (one read site in `collapsedIsland`, one in `mediaWingsOrToast`)
    - `grep -A2 "1 file saved" Islet/Notch/NotchPillView.swift | grep -c "files saved"` returns at least 1 (pluralized branch present)
    - Build succeeds with zero new warnings/errors introduced by this file
    - `grep -c "matchedGeometryEffect" Islet/Notch/NotchPillView.swift` count is unchanged from before this task in `collapsedIsland`'s body (still exactly 1 call in that function, ordering preserved before `.frame`)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure SwiftUI rendering of an already-validated in-memory `Int` (Plan 01). No new user input, no new external data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-03 | Information Disclosure | `chipTextRow` rendered text | accept | Displays only a locally-derived integer count already visible via the Tray strip itself; no filenames, paths, or content are rendered. |
</threat_model>

<verification>
Build-gate only for this plan (`xcodebuild build`) — SwiftUI rendering correctness for both
ambient states is confirmed on-device in Plan 04's consolidated UAT checkpoint, once Plan 03's
controller wiring can actually drive `sessionSummaryChip` live.
</verification>

<success_criteria>
- `chipTextRow(_:)` exists, mirrors `toastTextRow`'s styling exactly, with D-05 pluralization
- `collapsedIsland` grows to show the chip when present, renders pixel-identical to today when absent
- `mediaWingsOrToast` grows to show the chip (stacked below any concurrent song-change toast) when present
- Build succeeds
</success_criteria>

<output>
Create `.planning/phases/37-drop-session-summary-chip/37-02-SUMMARY.md` when done
</output>
