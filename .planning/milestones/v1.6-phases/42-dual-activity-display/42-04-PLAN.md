---
phase: 42-dual-activity-display
plan: 04
type: execute
wave: 3
depends_on: ["42-02", "42-03"]
files_modified:
  - Islet/Notch/NotchWindowController.swift
autonomous: false
requirements: [DUAL-01]

must_haves:
  truths:
    - "Both Countdown and NowPlaying live -> collapsed island shows primary pill + secondary bubble with real artwork"
    - "Tapping the bubble expands to the Now-Playing/Home media view (D-12)"
    - "Any transient (Charging/Device/Focus/OSD) suppresses both the primary pill AND the secondary bubble together, with zero stale bubble left behind (D-10, Pitfall 1)"
    - "When a transient ends, the primary pill reappears first, and the bubble morphs in ~150ms later, not in the same animation frame (D-11)"
    - "Single-activity state (only one of Countdown/NowPlaying live) renders byte-identical to pre-Phase-42 behavior — no bubble ever mounts (D-04)"
  artifacts:
    - path: "Islet/Notch/NotchWindowController.swift"
      provides: "renderPresentation() dual-field wiring + handleSecondaryTap() + (conditional) collapsed-tier hot-zone widening"
      contains: "secondaryRevealWorkItem"
  key_links:
    - from: "NotchWindowController.renderPresentation"
      to: "IslandResolver.resolveSecondary"
      via: "currentPresentation()'s tuple return"
      pattern: "resolveSecondary\\(primary:"
    - from: "NotchPillView.secondaryBubble's onTapGesture"
      to: "NotchWindowController.handleSecondaryTap"
      via: "onSecondaryTap closure wired in makeRootView"
      pattern: "onSecondaryTap:"
---

<objective>
Wire the controller: assign both `presentation` and `secondary` from ONE `resolve(...)`-derived
call every time `renderPresentation()` runs (closing 42-RESEARCH.md's Pitfall 1), stagger the
secondary bubble's reveal after a transient ends (D-11), wire the bubble's tap-to-expand (D-12),
close the click-through hot-zone gap if 42-02's spike found one — then run the phase's on-device
UAT hard-merge gate.

Purpose: This is the plan where the phase's one real risk (42-RESEARCH.md Pitfall 2 — AppKit's
click-through `hotZone` not covering the bubble's real screen position, the same failure class as
the confirmed Phase 40-03 badge-tap bug) either gets closed with a targeted geometry fix or
confirmed unnecessary, based on 42-02's already-completed on-device spike — never guessed at.
Output: Working dual-activity display, tap-to-expand, staggered reveal, on-device UAT sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-dual-activity-display/42-CONTEXT.md
@.planning/phases/42-dual-activity-display/42-RESEARCH.md
@.planning/phases/42-dual-activity-display/42-PATTERNS.md
@.planning/phases/42-dual-activity-display/42-UI-SPEC.md
@.planning/phases/42-dual-activity-display/42-VALIDATION.md
@.planning/phases/42-dual-activity-display/42-02-SUMMARY.md
@.planning/phases/40-update-available-hud-sparkle-integration/40-03-SUMMARY.md

<interfaces>
<!-- Consumed from 42-01 (already merged, wave 1): -->
`resolveSecondary(primary: IslandPresentation, nowPlaying: NowPlayingPresentation) ->
SecondaryActivity?` — Islet/Notch/IslandResolver.swift
`IslandPresentationState.secondary: SecondaryActivity?` — Islet/Notch/IslandPresentationState.swift

<!-- Consumed from 42-03 (already merged, wave 2): -->
`NotchPillView.onSecondaryTap: () -> Void = {}` — the closure property this plan wires from
`makeRootView`, following the exact same `[weak self]` pattern `onClick:` already uses
(NotchWindowController.swift:1878).
`NotchPillView.secondaryBubble(_:)` renders at a FIXED `.offset(x: 220)` (or the HStack fallback,
per 42-03's own on-device Preview outcome — read `42-03-SUMMARY.md` to confirm which mechanism
shipped) from the notch center whenever `presentationState.secondary != nil` — this plan's Task 2
hot-zone widening (if needed) must cover that same real screen position.

<!-- Existing controller conventions this plan must match exactly (already read this session): -->
- `private let springResponse: Double = 0.6` / `private let springDamping: Double = 0.62`
  (NotchWindowController.swift:386-387) — every animated state mutation in this file uses these.
- `scheduleActivityDismiss()`'s `DispatchWorkItem` shape (NotchWindowController.swift:1798-1833):
  cancel-existing → build work item capturing `[weak self]` → wrap the mutation in
  `withAnimation(.spring(response:dampingFraction:))` → store the work item → `DispatchQueue.main.asyncAfter`.
  This plan's D-11 stagger mirrors this shape exactly.
- `currentPresentation()` (NotchWindowController.swift:784-801) has exactly ONE caller —
  `renderPresentation()` (line 807) — confirmed via `grep -n "currentPresentation()"` across the
  file; safe to change its return type without touching any other call site.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: currentPresentation()/renderPresentation() tuple wiring + D-11 staggered reveal</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift lines 777-833 (`currentPresentation()`,
    `renderPresentation()`, `presentTransientChange()` — the exact functions this task rewrites)
    Islet/Notch/NotchWindowController.swift lines 386-387 (spring constants)
    Islet/Notch/NotchWindowController.swift lines 1798-1833 (`scheduleActivityDismiss()` — the
    `DispatchWorkItem`+`asyncAfter` convention to mirror for the stagger)
    .planning/phases/42-dual-activity-display/42-UI-SPEC.md "Reveal/dismiss animation" table
    (locks the 150ms stagger starting value)
  </read_first>
  <action>
    Change `currentPresentation()`'s return type from `IslandPresentation` to `(presentation:
    IslandPresentation, secondary: SecondaryActivity?)` (current line 784). Inside, after the
    existing `resolve(...)` call computes `presentation`, compute `secondary` via
    `resolveSecondary(primary: presentation, nowPlaying: nowPlayingLaunchGate(hasPlayedSinceLaunch:
    nowPlayingState.hasPlayedSinceLaunch, nowPlaying: np))` — applying the SAME launch-gate `np`
    that `resolve(...)`'s own ambient branch already applies internally, so a track that hasn't
    launch-gated through never populates the bubble either. Return `(presentation, secondary)`.
    Rewrite `renderPresentation()` (current lines 806-808): call `currentPresentation()` once,
    assign `presentationState.presentation` immediately (unchanged — inherits whatever
    `withAnimation` context the caller already established, per this function's own existing
    "caller owns the spring wrapper" doc comment). For `secondary`: (a) if the new value is `nil`,
    cancel any pending `secondaryRevealWorkItem` and assign `presentationState.secondary = nil`
    immediately, in the SAME animation context as `presentation` — no stagger on the way out; (b)
    if the new value is non-nil AND `presentationState.secondary` is currently `nil` (a fresh
    nil→non-nil transition — D-11's exact trigger), cancel any pending `secondaryRevealWorkItem`,
    then schedule a NEW `DispatchWorkItem` mirroring `scheduleActivityDismiss`'s exact shape
    (`[weak self]` capture, `withAnimation(.spring(response: self.springResponse,
    dampingFraction: self.springDamping))` wrapping the single `presentationState.secondary =
    <captured value>` assignment), fired via `DispatchQueue.main.asyncAfter(deadline: .now() +
    Self.secondaryStaggerDelay, execute: work)`; (c) if the new value is non-nil AND
    `presentationState.secondary` is ALSO already non-nil (a content update — e.g. a track change
    while both stay live, not a fresh appearance), assign directly in the caller's existing
    animation context, no stagger. Add two new private stored properties near `dismissWorkItem`'s
    own declaration: `private var secondaryRevealWorkItem: DispatchWorkItem?` and `private static
    let secondaryStaggerDelay: TimeInterval = 0.15` (UI-SPEC's locked starting value, tunable in
    Task 3's on-device checkpoint below).
  </action>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "func currentPresentation() -> (presentation: IslandPresentation, secondary: SecondaryActivity?)" Islet/Notch/NotchWindowController.swift` returns exactly 1 match
    - `grep -n "currentPresentation()" Islet/Notch/NotchWindowController.swift` returns exactly 1
      call site (inside `renderPresentation()`) plus the 1 declaration line — confirms no other
      call site was broken by the signature change
    - `grep -n "secondaryRevealWorkItem?.cancel()" Islet/Notch/NotchWindowController.swift` shows a
      cancel call precedes every new `DispatchWorkItem` scheduled for this property (no
      leaked/stacked work items)
    - `xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build` exits 0
  </acceptance_criteria>
  <done>renderPresentation() sets presentationState.presentation and (immediately-or-staggered per the 3 rules above) presentationState.secondary from the SAME resolve()-derived call, every time it runs, from all ~20 existing call sites with zero changes needed at those call sites.</done>
</task>

<task type="auto">
  <name>Task 2: Secondary bubble tap wiring + (conditional) click-through hot-zone extension</name>
  <files>Islet/Notch/NotchWindowController.swift</files>
  <read_first>
    Islet/Notch/NotchWindowController.swift lines 1427-1460 (`handleClick()`, full function — the
    guard-first / withAnimation / trailing-updateVisibility shape `handleSecondaryTap()` mirrors)
    Islet/Notch/NotchWindowController.swift lines 960-1005 (`hotZone`/`expandedZone` computation
    inside `positionAndShow()`)
    Islet/Notch/NotchWindowController.swift lines 1207-1240 (`handlePointer(at:)` — line 1217's
    `activeZone` computation is the exact line this task's conditional branch touches)
    Islet/Notch/NotchWindowController.swift lines 1250-1307 (`visibleContentZone()`'s
    per-presentation `if/else if` branching convention — the pattern to extend if widening is
    needed)
    Islet/Notch/NotchWindowController.swift lines 1866-1901 (`makeRootView` — where
    `onSecondaryTap:` gets wired into the `NotchPillView(...)` constructor call)
    .planning/phases/42-dual-activity-display/42-02-SUMMARY.md (this plan's hard input: "registers"
    or "passes through" — determines whether the widening branch below applies)
  </read_first>
  <action>
    Add `private func handleSecondaryTap()` near `handleClick()`: guard `!isOnboardingActive`
    (mirrors `handleClick`'s own first guard). Inside `withAnimation(.spring(response:
    springResponse, dampingFraction: springDamping))`: set `viewSwitcherState.selectedView =
    .home` (so `resolve()`'s `isExpanded` branch renders `.nowPlayingExpanded`, not whichever tab
    happened to be selected); if `!interaction.isExpanded`, advance `interaction.phase =
    nextState(interaction.phase, .clicked)` (mirrors `handleClick`'s own expand transition, but
    SKIPPED if already expanded — D-12 says "expands to", not "toggles", so a second tap on the
    bubble while the island is already expanded on Home must not re-collapse it); call
    `renderPresentation()`. After the `withAnimation` block, call `updateVisibility()` (mirrors
    `handleClick`'s own trailing call). Wire `onSecondaryTap: { [weak self] in
    self?.handleSecondaryTap() }` into `makeRootView`'s `NotchPillView(...)` constructor call
    (current lines 1869-1896), positioned directly after the existing `onClick:` argument (current
    line 1878), using the identical `[weak self]` closure style. THEN, branch on 42-02's recorded
    spike outcome: IF "passes through" (today's `hotZone` does not cover wing-tier content) —
    extend `handlePointer(at:)`'s collapsed-tier zone check (current line 1217: `let activeZone =
    interaction.isExpanded ? (expandedZone ?? hotZone) : hotZone`) to use a
    presentation-aware collapsed zone whenever `presentationState.secondary != nil`, mirroring
    `visibleContentZone()`'s existing per-case `if/else if` branching convention: add a small new
    helper (e.g. `collapsedInteractiveZone() -> CGRect?`) that returns `hotZone` unchanged for
    every case EXCEPT when a secondary bubble is showing, in which case it widens the rect on the
    trailing side to cover out to `220pt + Self.secondaryBubbleDiameter / 2 + hotZonePadding` past
    the notch center (matching 42-03 Task 2's exact bubble-center positioning constant — read
    `42-03-SUMMARY.md` to confirm the shipped positioning value if it deviated from 220pt), and use
    this helper in place of the bare `hotZone` reference at line 1217 for the collapsed branch. IF
    "registers" (today's `hotZone` already covers wing-tier content) — skip the widening entirely;
    record in this plan's SUMMARY which branch was taken and why (a documented no-op, not a silent
    scope cut).
  </action>
  <verify>
    <automated>xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "onSecondaryTap:" Islet/Notch/NotchWindowController.swift` shows it passed into
      `makeRootView`'s `NotchPillView(...)` call, directly after `onClick:`
    - `grep -n "func handleSecondaryTap" -A 3 Islet/Notch/NotchWindowController.swift` confirms
      `guard !isOnboardingActive` is the first statement (matches `handleClick`'s own guard-first
      structure)
    - If the widening branch was taken: `grep -n "collapsedInteractiveZone" Islet/Notch/NotchWindowController.swift`
      shows the helper defined AND referenced at `handlePointer`'s collapsed-branch line in place
      of the bare `hotZone`
    - `xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build` exits 0
  </acceptance_criteria>
  <done>Tapping the secondary bubble (once physically reachable per whichever branch applied) calls handleSecondaryTap(), which expands to Home/Now-Playing without collapsing an already-expanded island.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 42 on-device UAT — hard merge gate</name>
  <files>Islet/Notch/NotchWindowController.swift, Islet/Notch/NotchPillView.swift, Islet/Notch/IslandResolver.swift, Islet/Notch/IslandPresentationState.swift</files>
  <read_first>
    .planning/phases/42-dual-activity-display/42-VALIDATION.md (the 5 manual-only verifications
    this checkpoint must cover)
    .planning/phases/42-dual-activity-display/42-CONTEXT.md (D-01 through D-13, the full locked
    decision set this checkpoint confirms end-to-end)
  </read_first>
  <action>
    No code changes in this task — verification-only, running the complete dual-activity display
    end-to-end on real hardware with a real live Calendar Countdown and real Now-Playing media.
  </action>
  <what-built>
    The full dual-activity display: the resolver's additive secondary output (42-01), the
    secondary bubble view (42-03), and this plan's controller wiring (Task 1's dual-field
    assignment + D-11 stagger, Task 2's tap wiring + conditional hot-zone widening).
  </what-built>
  <how-to-verify>
    1. Trigger a live Calendar Countdown (event within 1hr) AND live Now-Playing (`.playing`)
       simultaneously; confirm the collapsed island shows the countdown pill as primary PLUS a
       round bubble with real album art to its right, with a visible gap (D-05/D-06/D-07/D-08).
    2. Confirm the bubble's spring-in matches the rest of the app's morph feel — no dropped
       frames, no geometry collisions with the primary pill (ROADMAP criterion 3).
    3. Tap directly on the bubble; confirm it expands to the Now-Playing/Home media view (D-12) —
       this is the single highest-risk step (42-RESEARCH.md Pitfall 2); if the tap does not
       register, this is a REAL gap requiring a follow-up fix to Task 2's hot-zone branch, not a
       silent miss.
    4. Stop the countdown or the media (single-activity state remains); confirm the bubble
       disappears and the remaining activity renders as today's normal single pill/wing,
       byte-for-byte (D-04).
    5. Trigger a Charging or Device transient while both Countdown+NowPlaying are live; confirm
       BOTH the primary pill and the bubble disappear together, replaced only by the transient's
       own splash (D-10) — no stale bubble left behind (the specific regression 42-RESEARCH.md
       Pitfall 1 warns about).
    6. Let the transient end; confirm the primary pill reappears first, then the bubble morphs in
       ~150ms later, NOT in the same animation frame (D-11).
    7. Hover the bubble without clicking; confirm no highlight/hover-reveal appears anywhere on it
       (D-13).
    8. Re-run this project's standard CR-01 hover→expand→move-down click-through trace to confirm
       no phantom click-swallowing was introduced elsewhere on the pill/wings by Task 2's geometry
       changes (only relevant if the widening branch was taken).
  </how-to-verify>
  <resume-signal>"approved" or describe which of the 8 steps failed and how.</resume-signal>
  <acceptance_criteria>
    - Each of the 8 steps above is explicitly confirmed pass or fail, not summarized as a single
      overall "looks good"
    - Any failed step is recorded in this plan's SUMMARY.md as a specific gap, routed for
      gap-closure per this project's established convention (Phase 32/39-07 precedent), not
      silently dropped
  </acceptance_criteria>
  <done>All 8 on-device steps pass, or the SUMMARY records exactly which failed for gap-closure follow-up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| AppKit click-through hot-zone → SwiftUI tap gesture | The one place an untrusted-adjacent surface (arbitrary pointer clicks, including clicks intended for whatever app sits behind the notch) crosses into this app's own interaction logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-07 | Tampering | `NotchWindowController.handlePointer` / `collapsedInteractiveZone` (if the widening branch is taken) | mitigate | Widening is bounded to an exact, code-reviewed constant (`220pt + secondaryBubbleDiameter/2 + hotZonePadding`) tied 1:1 to the bubble's own rendered position from Plan 42-03 — not an open-ended region — closing off accidental over-wide click acceptance; confirmed via Task 3's CR-01 regression trace |
| T-42-08 | Denial of Service | `NotchWindowController.secondaryRevealWorkItem` | mitigate | Every new `DispatchWorkItem` cancels any prior pending one before scheduling (Task 1), preventing unbounded work-item accumulation under rapid state changes — mirrors `scheduleActivityDismiss`'s own established cancel-before-schedule discipline |
| T-42-09 | Repudiation | `handleSecondaryTap()` | accept | Local UI state transition only (selected view + expand phase) — no persisted record, no security-relevant action, matches every other tap handler in this file |
| T-42-10 | Elevation of Privilege | n/a | accept | No new privilege boundary — pure UI/local-data feature, no new entitlements, no new external process |
</threat_model>

<verification>
- `xcodebuild -project Islet.xcodeproj -scheme Islet -configuration Debug build` exits 0 after Tasks 1-2
- Manual Cmd-U: no regression in `IslandResolverTests.swift` or `NotchPillViewTests.swift`
- Task 3's 8-step on-device checkpoint approved (or gaps explicitly recorded)
</verification>

<success_criteria>
- `renderPresentation()` sets both `presentationState.presentation` and `presentationState.secondary` from one resolve()-derived call, every call site, with D-11's stagger applied only on nil→non-nil transitions
- Tapping the secondary bubble reliably expands to Now-Playing (D-12), confirmed on real hardware
- A standing transient suppresses both slots together, with zero stale-bubble regressions (D-10)
- Single-activity behavior is byte-identical to pre-Phase-42 (D-04)
- DUAL-01 is fully shipped and on-device UAT-approved
</success_criteria>

<output>
Create `.planning/phases/42-dual-activity-display/42-04-SUMMARY.md` when done
</output>
