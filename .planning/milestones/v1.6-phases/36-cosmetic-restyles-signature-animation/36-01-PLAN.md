---
phase: 36-cosmetic-restyles-signature-animation
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Notch/NotchPillView.swift
autonomous: false
requirements: [HUD-01, HUD-02]

must_haves:
  truths:
    - "The Charging wing shows a left-side 'Charging' text label next to the bolt icon ONLY while actively charging; the right-side BatteryIndicator is unchanged."
    - "The Bluetooth wing shows a left-side 'Connected' text label next to the device glyph ONLY while connected; when connected with no reported battery level, the right wing shows a fixed-green ring instead of the old checkmark glyph."
    - "Negative states (not charging, disconnected) stay icon-only and dimmed exactly as before — no label appears, no color change."
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "wings(for:) and deviceWings(for:)/deviceTrailing(...) restyled with left-wing labels and the Bluetooth green-ring right-wing case"
  key_links:
    - from: "wings(for:) HStack"
      to: "isCharging conditional Text(\"Charging\")"
      via: "left-wing HStack(spacing: 4)"
      pattern: "Text\\(\"Charging\"\\)"
    - from: "deviceTrailing(isConnected:battery:)"
      to: "Circle().strokeBorder(Color.green"
      via: "isConnected && battery == nil branch"
      pattern: "strokeBorder\\(Color\\.green"
---

<objective>
Restyle the Charging (HUD-02) and Bluetooth/AirPods (HUD-01) collapsed wing HUDs to the Droppy-pill visual language: a left-wing icon+label (shown only in the positive state) and, for Bluetooth's no-battery-known case, a new fixed-green status ring on the right wing. This is a chrome-only edit inside the existing split-wing structure (D-01) — no `DeviceCoordinator`, `BluetoothMonitor`, or IOKit power-monitor changes.

Purpose: Match the user-supplied Droppy reference screenshots (D-02/D-03/D-04) so the two device-state HUDs read at a glance, consistent with the rest of the Liquid Glass visual language shipped in Phase 35.
Output: Updated `wings(for:)`, `deviceWings(for:)`, `deviceTrailing(...)` in `NotchPillView.swift`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-cosmetic-restyles-signature-animation/36-CONTEXT.md
@.planning/phases/36-cosmetic-restyles-signature-animation/36-PATTERNS.md
@.planning/phases/36-cosmetic-restyles-signature-animation/36-UI-SPEC.md
@.planning/phases/36-cosmetic-restyles-signature-animation/reference-droppy-airpods-pill.png
@.planning/phases/36-cosmetic-restyles-signature-animation/reference-droppy-volume-charging-pills.png
</context>

<interfaces>
Current code being restyled (`Islet/Notch/NotchPillView.swift`):

`wings(for activity: ChargingActivity)` (~L1919-1938) — `HStack(spacing: 0) { Image(systemName: "bolt.fill")...padding(.leading, 12); Spacer(); BatteryIndicator(level: percent, accent: chargingAccent).padding(.trailing, 14) }`, wrapped by `wingsShape { ... }` (~L1895-1912, DO NOT MODIFY — reuse verbatim).

`deviceWings(for activity: DeviceActivity)` (~L2036-2059) — `HStack(spacing: 0) { Image(systemName: deviceSymbol(for: glyph))...padding(.leading, 12); Spacer(); deviceTrailing(isConnected:battery:).padding(.trailing, 14) }`, also wrapped by `wingsShape`.

`deviceTrailing(isConnected: Bool, battery: Int?)` (~L2066-2074) — `@ViewBuilder` with `if isConnected, let battery { BatteryIndicator(level: battery) } else { Image(systemName: isConnected ? "checkmark" : "xmark")...foregroundStyle(isConnected ? deviceAccent : Color.white.opacity(0.5)) }`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Charging wing restyle (HUD-02)</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift lines 1895-1938 (wingsShape + wings(for:))
    .planning/phases/36-cosmetic-restyles-signature-animation/36-CONTEXT.md — D-01, D-02, D-03, D-04
    .planning/phases/36-cosmetic-restyles-signature-animation/36-UI-SPEC.md — "HUD Restyle Contract" left/right wing tables
    .planning/phases/36-cosmetic-restyles-signature-animation/reference-droppy-volume-charging-pills.png
  </read_first>
  <action>
    In `wings(for:)` (~L1919-1938), wrap the existing `Image(systemName: "bolt.fill")` in a new `HStack(spacing: 4)` that also contains a conditional `Text("Charging")`, shown only `if isCharging`. Move the current `.padding(.leading, 12)` from the `Image` onto this new wrapping `HStack` (left padding stays 12pt total, per D-02/UI-SPEC "12px left padding, unchanged"). Add `.font(.system(size: 13, weight: .semibold))` to the bolt `Image` (new explicit size, per UI-SPEC "Icon size" row); its existing `.symbolRenderingMode(.hierarchical)` and `.foregroundStyle(isCharging ? Color.green : Color.white.opacity(0.6))` stay unchanged (D-04: negative/dimmed state untouched). The new `Text("Charging")` uses `.font(.system(size: 12, weight: .semibold, design: .rounded))` and `.foregroundStyle(.white)` (UI-SPEC "Label text" row). Leave `Spacer()` and `BatteryIndicator(level: percent, accent: chargingAccent).padding(.trailing, 14)` byte-identical (D-02: right wing needs no change). Add a one-line `// Round N (HUD-02 Droppy restyle, D-02/D-03/D-04) — ...` comment above the edit, following this file's existing "Round N (reason)" comment convention (e.g. the style at line ~1474/1947).
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Text("Charging")' Islet/Notch/NotchPillView.swift` returns 1
    - `grep -A1 'Text("Charging")' Islet/Notch/NotchPillView.swift | head -1` shows the `Text` guarded by `if isCharging` on the same or preceding line
    - `grep -c 'BatteryIndicator(level: percent, accent: chargingAccent)' Islet/Notch/NotchPillView.swift` still returns 1 (right wing untouched)
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors
  </acceptance_criteria>
  <done>Charging wing shows "Charging" label left of the bolt icon only while isCharging is true; BatteryIndicator right wing is byte-identical to before.</done>
</task>

<task type="auto">
  <name>Task 2: Bluetooth wing restyle (HUD-01)</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift lines 2036-2074 (deviceWings(for:) + deviceTrailing(...))
    Islet/Notch/BatteryIndicator.swift (RoundedRectangle stroke-outline idiom to model the new Circle ring after)
    .planning/phases/36-cosmetic-restyles-signature-animation/36-CONTEXT.md — D-02, D-03, D-04
    .planning/phases/36-cosmetic-restyles-signature-animation/36-UI-SPEC.md — "HUD Restyle Contract" right wing table, "Bluetooth, connected, no battery known" row
    .planning/phases/36-cosmetic-restyles-signature-animation/reference-droppy-airpods-pill.png
  </read_first>
  <action>
    In `deviceWings(for:)` (~L2036-2059), wrap the device glyph `Image(systemName: deviceSymbol(for: glyph))` in a new `HStack(spacing: 4)` with a conditional `Text("Connected")`, shown only `if isConnected`. Move `.padding(.leading, 12)` onto this new HStack. Add `.font(.system(size: 13, weight: .semibold))` to the device glyph `Image` (matches Task 1's icon sizing); its existing `.foregroundStyle(deviceAccent.opacity(iconOpacity))` stays unchanged (D-03: left icon keeps accent tinting). New `Text("Connected")` uses the SAME styling as Task 1's "Charging" label: `.font(.system(size: 12, weight: .semibold, design: .rounded))`, `.foregroundStyle(.white)`.
    In `deviceTrailing(isConnected:battery:)` (~L2066-2074), restructure the current `if isConnected, let battery { BatteryIndicator(level: battery) } else { Image checkmark/xmark }` into a 3-way branch: `isConnected && battery != nil` → unchanged `BatteryIndicator(level: battery)`; `isConnected && battery == nil` → NEW `Circle().strokeBorder(Color.green, lineWidth: 1.5).frame(width: 14, height: 14)` (fixed literal `Color.green`, per D-03 — never `deviceAccent`); `!isConnected` → unchanged `Image(systemName: "xmark").font(.system(size: 13, weight: .bold)).foregroundStyle(Color.white.opacity(0.5))` (D-04, untouched). Add a matching "Round N (HUD-01 Droppy restyle, D-02/D-03/D-04)" comment.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Text("Connected")' Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c 'Circle().strokeBorder(Color.green, lineWidth: 1.5)' Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c 'Image(systemName: "checkmark")' Islet/Notch/NotchPillView.swift` returns 0 (replaced by the green ring for the connected/no-battery case)
    - `grep -c 'Image(systemName: isConnected ? "checkmark" : "xmark")' Islet/Notch/NotchPillView.swift` returns 0 (old combined ternary is gone, replaced by the 3-way branch)
    - `grep -c 'BatteryIndicator(level: battery)' Islet/Notch/NotchPillView.swift` still returns 1 (battery-known branch untouched)
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors
  </acceptance_criteria>
  <done>Bluetooth wing shows "Connected" label left of the device glyph only while connected; the right wing shows the fixed-green ring when connected with no battery, BatteryIndicator when a battery is known, and the unchanged dimmed xmark when disconnected.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device UAT — Charging/Bluetooth Droppy-pill restyle</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <action>No code changes in this task — verification-only, confirming Tasks 1-2's restyle matches the Droppy reference screenshots on-device.</action>
  <what-built>Charging and Bluetooth collapsed wings now show a left-wing icon+label (positive state only) and Bluetooth's no-battery-known case shows a green ring instead of a checkmark.</what-built>
  <how-to-verify>
    1. Open the project in Xcode (Islet.xcodeproj), build and run the Debug scheme.
    2. Plug in the charger: confirm the left wing shows a bolt icon + "Charging" text, right wing shows the green battery indicator with percentage — matches `reference-droppy-volume-charging-pills.png`.
    3. Unplug: confirm the "Charging" label disappears and only the dimmed bolt icon remains (no layout jump).
    4. Connect a Bluetooth device with no battery reporting (or simulate via existing dev toggle if present): confirm left wing shows the device glyph + "Connected" text, right wing shows a thin green ring — matches `reference-droppy-airpods-pill.png`.
    5. Disconnect: confirm the "Connected" label disappears, dimmed xmark remains unchanged.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | Pure view-body edit inside an existing, already-shipped chrome wrapper (`wingsShape`). No new data source, no network, no user input — `isCharging`/`percent`/`isConnected`/`battery` are all pre-existing, already-validated fields from `ChargingActivity`/`DeviceActivity` (unchanged by this plan). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-36-01 | Tampering | New "Charging"/"Connected" wing labels | accept | Both strings are static, locked literals (D-02/UI-SPEC Copywriting Contract) — never derived from device names or other untrusted input, so there is no injection/rendering surface to mitigate. |
| T-36-02 | Information Disclosure | New Bluetooth green-ring right-wing case | accept | The ring communicates only a boolean (connected, no battery reported) already visible today via the checkmark it replaces — no new data is exposed. |
| T-36-SC | Tampering | npm/pip/cargo installs | n/a | Zero package-manager installs in this plan. |
</threat_model>

<verification>
- `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors after both tasks.
- On-device checkpoint (Task 3) confirms both restyles visually match the Droppy reference screenshots.
</verification>

<success_criteria>
- Charging wing: left "Charging" label (positive state only) + unchanged right `BatteryIndicator`.
- Bluetooth wing: left "Connected" label (positive state only) + right wing shows green ring (no battery known), `BatteryIndicator` (battery known), or unchanged dimmed xmark (disconnected).
- `DeviceCoordinator`, `BluetoothMonitor`, IOKit power monitor: zero changes (confirmed by `files_modified` scope — only `NotchPillView.swift` touched).
- Full project builds clean.
</success_criteria>

<output>
Create `.planning/phases/36-cosmetic-restyles-signature-animation/36-01-SUMMARY.md` when done
</output>
