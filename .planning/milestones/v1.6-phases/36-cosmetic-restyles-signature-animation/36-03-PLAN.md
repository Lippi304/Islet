---
phase: 36-cosmetic-restyles-signature-animation
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/Fonts/DancingScript-Variable.ttf
  - Islet/Fonts/DancingScript-OFL.txt
  - Islet/Notch/SignatureHeading.swift
  - IsletTests/SignatureHeadingTests.swift
autonomous: true
requirements: [ONBOARD-04]

must_haves:
  truths:
    - "A commercially-safe (OFL-licensed) script font is bundled and registers at runtime without crashing, resolving the D-12 flagged license risk (the original LastoriaBoldRegular.otf is never used)."
    - "SignatureHeading extracts a real vector glyph outline (Path) for every character of a given string via Core Text, positioned left-to-right by the font's own advance widths, oriented right-side-up for SwiftUI's y-down coordinate space."
    - "SignatureHeading renders as a standalone, non-animated view at this stage: every glyph shows solid-filled orange — this is the contract Plan 36-04 animates, not the final on-screen behavior yet."
  artifacts:
    - path: "Islet/Notch/SignatureHeading.swift"
      provides: "SignatureHeading view + glyphPaths(for:font:) + loadSignatureFont(size:) — self-contained, no NotchPillView dependency"
    - path: "Islet/Fonts/DancingScript-Variable.ttf"
      provides: "OFL-licensed variable-weight script font (Google Fonts, Impallari Type)"
    - path: "IsletTests/SignatureHeadingTests.swift"
      provides: "Sanity tests on glyphPaths(for:font:) count + non-empty outlines"
  key_links:
    - from: "SignatureHeading.body"
      to: "SignatureHeading.glyphPaths(for:font:)"
      via: "CTFontCreatePathForGlyph + CTFontGetAdvancesForGlyphs"
      pattern: "CTFontCreatePathForGlyph"
    - from: "SignatureHeading.loadSignatureFont"
      to: "DancingScript-Variable.ttf"
      via: "CTFontManagerRegisterFontsForURL + kCTFontVariationAttribute wght=700"
      pattern: "CTFontManagerRegisterFontsForURL"
---

<objective>
Resolve the D-12 flagged font-license risk by bundling Dancing Script (OFL 1.1, Google Fonts) instead of the demo-licensed LastoriaBoldRegular.otf, and build the Core Text glyph-extraction contract `SignatureHeading` will animate in Plan 36-04. This plan delivers a working, buildable, testable, but NOT YET ANIMATED or wired-in view — no user-visible change yet (not called from `onboardingWelcomeStep` until 36-04).

Purpose: Establish the hardest, least-precedented piece of ONBOARD-04 (per-glyph vector path extraction — flagged in `36-PATTERNS.md` "No Analog Found") as its own verifiable, testable contract before layering the staggered stroke-reveal animation on top, keeping each plan within a healthy context budget.
Output: `Islet/Fonts/DancingScript-Variable.ttf` + `DancingScript-OFL.txt`, `Islet/Notch/SignatureHeading.swift` (new file), `IsletTests/SignatureHeadingTests.swift` (new file).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36-cosmetic-restyles-signature-animation/36-CONTEXT.md
@.planning/phases/36-cosmetic-restyles-signature-animation/36-PATTERNS.md
@.planning/phases/36-cosmetic-restyles-signature-animation/36-UI-SPEC.md
@.planning/phases/36-cosmetic-restyles-signature-animation/reference-signature-component.md
</context>

<interfaces>
No prior analog exists for this file (confirmed in `36-PATTERNS.md` "No Analog Found") — this is genuinely new code. Closest structural precedents to follow (both in `Islet/Notch/`):

`BatteryIndicator.swift` (full file) — the precedent for "one small standalone `View` struct in its own file, plain stored properties, self-contained `Shape`/`Path` drawing, no external dependencies."

`EqualizerBars` (`Islet/Notch/NotchPillView.swift` ~L2330-2391, post-36-02-rewrite) — the precedent for a `View` struct driving its own animation via `TimelineView`, and for keeping a factory function `internal` (not `private`) specifically so a test file can call it under `@testable import`.

Google Fonts `google/fonts` repo, `ofl/dancingscript/` — confirmed (this planning session) to contain exactly one font file, `DancingScript[wght].ttf` (a variable font, `wght` axis range 400-700, default instance 400 — there is no separate static Bold file), plus `OFL.txt`. Raw URLs:
`https://raw.githubusercontent.com/google/fonts/main/ofl/dancingscript/DancingScript%5Bwght%5D.ttf`
`https://raw.githubusercontent.com/google/fonts/main/ofl/dancingscript/OFL.txt`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Bundle the OFL-licensed font + runtime registration helper</name>
  <files>Islet/Fonts/DancingScript-Variable.ttf, Islet/Fonts/DancingScript-OFL.txt, Islet/Notch/SignatureHeading.swift</files>
  <read_first>
    .planning/phases/36-cosmetic-restyles-signature-animation/36-UI-SPEC.md — "Signature Animation Contract" font section (D-12 resolution: Dancing Script Bold, OFL 1.1)
    .planning/phases/36-cosmetic-restyles-signature-animation/36-CONTEXT.md — D-12 (flagged risk)
    project.yml (confirm: sources are auto-discovered from `Islet/` by extension — no project.yml edit needed for a new `.ttf`/`.swift` file, only `xcodegen generate` to pick them up)
  </read_first>
  <action>
    Download the font and its license file: `curl -sL -o Islet/Fonts/DancingScript-Variable.ttf "https://raw.githubusercontent.com/google/fonts/main/ofl/dancingscript/DancingScript%5Bwght%5D.ttf"` and `curl -sL -o Islet/Fonts/DancingScript-OFL.txt "https://raw.githubusercontent.com/google/fonts/main/ofl/dancingscript/OFL.txt"` (the `.txt` is committed for license provenance, not bundled as an app resource). Verify the `.ttf` download landed correctly (expected size ~133,636 bytes per the upstream `METADATA.pb`) before proceeding.
    Run `xcodegen generate` so the new files are picked up by the existing `sources: - path: Islet` glob (no `project.yml` edit needed — confirmed no `ATSApplicationFontsPath`/Info.plist change is required since this uses runtime registration, not the legacy plist-based mechanism).
    Create `Islet/Notch/SignatureHeading.swift` with a private, run-once font loader: `private static func loadSignatureFont(size: CGFloat) -> CTFont` that (a) locates the bundled `.ttf` via `Bundle.main.url(forResource: "DancingScript-Variable", withExtension: "ttf")`, (b) registers it exactly once per process via `CTFontManagerRegisterFontsForURL(url, .process, nil)` guarded by a `private static let registrationOnce: Void = { ... }()` lazy-static (Swift's standard "run exactly once" idiom — mirrors the file-scoped, run-once patterns already used elsewhere in this codebase), (c) builds a `CTFont` at the given `size` with the `wght` variation axis set to `700` (Bold, per D-12's locked "Bold weight") via a variation-attribute font descriptor: construct a variation dictionary `[NSNumber(value: 2003265652): 700.0]` (`2003265652` is the standard OpenType `wght` axis's numeric tag, i.e. the four ASCII bytes `w`,`g`,`h`,`t` read as one big-endian `UInt32` — this is Apple's documented `kCTFontVariationAttribute` dictionary key convention), wrap it as `[kCTFontVariationAttribute as String: variationDict]`, create a `CTFontDescriptor` via `CTFontDescriptorCreateWithAttributes` (include `kCTFontNameAttribute: "Dancing Script" as CFString` so the descriptor resolves the right registered family), then `CTFontCreateWithFontDescriptor(descriptor, size, nil)`. If font creation unexpectedly returns something whose family doesn't match (defensive check), fall back to `CTFontCreateWithName("Dancing Script" as CFString, size, nil)` at its default (400) instance rather than crashing — the fallback still renders the correct family, just not bold.
  </action>
  <verify>
    <automated>ls -la Islet/Fonts/DancingScript-Variable.ttf && xcodegen generate && xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `Islet/Fonts/DancingScript-Variable.ttf` exists and is a valid TrueType/OpenType file (`file Islet/Fonts/DancingScript-Variable.ttf` reports "TrueType Font data" or "OpenType font")
    - `Islet/Fonts/DancingScript-OFL.txt` exists and contains the string "SIL OPEN FONT LICENSE" (`grep -c "SIL OPEN FONT LICENSE" Islet/Fonts/DancingScript-OFL.txt` returns at least 1)
    - `grep -c "CTFontManagerRegisterFontsForURL" Islet/Notch/SignatureHeading.swift` returns 1
    - `grep -c "2003265652" Islet/Notch/SignatureHeading.swift` returns 1
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors
  </acceptance_criteria>
  <done>Dancing Script (OFL) is bundled, registers at runtime exactly once, and a Bold (wght=700) CTFont instance loads successfully with a safe non-crashing fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Glyph path extraction + static (non-animated) render</name>
  <files>Islet/Notch/SignatureHeading.swift</files>
  <read_first>
    Islet/Notch/SignatureHeading.swift (from Task 1 — the font loader this task builds on)
    Islet/Notch/BatteryIndicator.swift (standalone-View-struct precedent)
    .planning/phases/36-cosmetic-restyles-signature-animation/reference-signature-component.md — glyph extraction + positioning logic (`font.charToGlyph`, `path.getPath(x, baseline, fontSize)`, `x += advanceWidth * scale`)
    .planning/phases/36-cosmetic-restyles-signature-animation/36-UI-SPEC.md — "Signature Animation Contract" (font size 28pt, stroke width ~6.16pt derivation)
  </read_first>
  <action>
    Add `static func glyphPaths(for text: String, font: CTFont) -> [(path: Path, advance: CGFloat)]` (keep `internal`, not `private`, for the test file below). For each `Character` in `text` (iterate in order, including the space — every character gets a slot, per D-10): convert to `UniChar` (UTF-16), batch-resolve to `CGGlyph` via `CTFontGetGlyphsForCharacters(font, &uniChars, &glyphs, count)`; for each glyph, read its advance via `CTFontGetAdvancesForGlyphs(font, .horizontal, &glyph, &advance, 1)` and its outline via `CTFontCreatePathForGlyph(font, glyph, &transform)` where `transform` is `CGAffineTransform(translationX: runningX, y: 0)` (`runningX` accumulates each glyph's own advance after use, mirroring the reference's `x += advanceWidth`). Core Text's glyph space is Y-UP (baseline at 0, ascender positive) while SwiftUI draws Y-DOWN — flip each extracted `CGPath` vertically and shift by the font's ascent (`CTFontGetAscent(font)`) before wrapping it as `Path(cgPath:)`, so glyphs render right-side-up; document this flip inline as the load-bearing gotcha for whoever maintains this next (comment discipline, matching `EqualizerBars`'s own extensive inline-reasoning convention).
    Add `static func totalWidth(for paths: [(path: Path, advance: CGFloat)]) -> CGFloat` summing the `advance` values (mirrors the reference's final `width` state) — used to size the view's frame to its actual content instead of a fixed guess.
    Implement `SignatureHeading: View` with `let text: String = "Meet Islet"` (D-09) and a fixed `Color.orange` (D-11 — literal, never `nowPlayingAccent`/any accent). `body` builds the font via `Self.loadSignatureFont(size: 28)` (Task 1), computes `Self.glyphPaths(for: text, font:)` and `Self.totalWidth(for:)`, and renders via a `Canvas { context, size in for (path, _) in glyphPaths { context.fill(path, with: .color(.orange)) } }` sized to `(totalWidth, 28 * 3)` (matches the reference's own `height = fontSize * 3` vertical headroom for ascenders/descenders) — AT THIS STAGE every glyph is drawn solid-filled immediately, with NO animation/trim/stagger yet (Plan 36-04 adds that on top of this same `Canvas` closure). Do not wire `SignatureHeading` into `onboardingWelcomeStep` yet — that happens in Plan 36-04 alongside the animation, so there is no partially-animated user-visible state in between.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "static func glyphPaths(for text: String, font: CTFont)" Islet/Notch/SignatureHeading.swift` returns 1
    - `grep -c "CTFontCreatePathForGlyph" Islet/Notch/SignatureHeading.swift` returns 1
    - `grep -c "CTFontGetAdvancesForGlyphs" Islet/Notch/SignatureHeading.swift` returns 1
    - `grep -c "struct SignatureHeading: View" Islet/Notch/SignatureHeading.swift` returns 1
    - `grep -c "Color.orange" Islet/Notch/SignatureHeading.swift` returns at least 1
    - `grep -c "onboardingWelcomeStep" Islet/Notch/NotchPillView.swift` returns 1 unchanged (confirms this task did NOT touch `NotchPillView.swift` — wiring is Plan 36-04's job)
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors
  </acceptance_criteria>
  <done>SignatureHeading exists as a self-contained, buildable View that extracts and renders real glyph outlines for "Meet Islet" in solid orange, correctly oriented and positioned — not yet animated, not yet wired into onboarding.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Glyph extraction sanity tests</name>
  <files>IsletTests/SignatureHeadingTests.swift</files>
  <read_first>
    Islet/Notch/SignatureHeading.swift (from Tasks 1-2 — the `glyphPaths(for:font:)` signature under test)
    IsletTests/EqualizerBarsTests.swift (file-header comment convention + `@testable import Islet` precedent to mirror)
  </read_first>
  <behavior>
    - `SignatureHeading.glyphPaths(for: "Meet Islet", font:)` returns exactly 10 entries (one per character, including the space — D-10).
    - Every non-space character's `Path` is non-empty (`!path.isEmpty`); the space character's entry may legitimately be empty (that IS the space glyph) and is not asserted either way.
    - `totalWidth(for:)` returns a positive, finite `CGFloat` greater than the sum of any single glyph's advance (sanity check that accumulation, not overwriting, is happening).
  </behavior>
  <action>
    Create `IsletTests/SignatureHeadingTests.swift` following `EqualizerBarsTests.swift`'s exact shape (`import XCTest`, `@testable import Islet`, one `XCTestCase` subclass) with a header comment citing ONBOARD-04/D-09/D-10. Build the font once via `SignatureHeading.loadSignatureFont` equivalent test access (make the loader `internal`, not `private`, in Task 1/2 specifically so this test file can call it) at a fixed test size (e.g. 28), then assert the three behaviors above against `SignatureHeading.glyphPaths(for: "Meet Islet", font:)` and `SignatureHeading.totalWidth(for:)`.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@testable import Islet" IsletTests/SignatureHeadingTests.swift` returns 1
    - `grep -c "glyphPaths" IsletTests/SignatureHeadingTests.swift` returns at least 1
    - `grep -c "XCTAssertEqual" IsletTests/SignatureHeadingTests.swift` returns at least 1 (the count-equals-10 assertion)
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors (do NOT run `xcodebuild test` — hangs headless per this project's own precedent; run manually via Cmd-U in Xcode)
  </acceptance_criteria>
  <done>SignatureHeadingTests.swift exists, builds, and asserts glyphPaths(for:font:) returns 10 entries for "Meet Islet" with non-empty paths for visible characters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Build-time asset supply chain | The new `.ttf` is fetched from an external source (GitHub) during planning/execution rather than authored in-repo — the one genuinely new external-asset surface in this phase. No runtime network access, no user input: once committed, the file is static and bundled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-36-05 | Tampering | `DancingScript-Variable.ttf` supply-chain integrity | mitigate | Source pinned to the specific, previously-verified `google/fonts` repo path (OFL 1.1, confirmed during this planning session) rather than an arbitrary CDN/mirror; acceptance criteria verify the file is a genuine TrueType/OpenType binary and the accompanying `OFL.txt` license text is present before the file is committed — same scrutiny spirit as the Package Legitimacy Gate, applied to a non-package external asset. |
| T-36-06 | Information Disclosure | `glyphPaths(for:font:)` text input | accept | The only caller passes the locked literal `"Meet Islet"` (D-09) — never user-supplied or untrusted runtime text — so there is no injection/rendering surface to mitigate. |
| T-36-SC | Tampering | npm/pip/cargo installs | n/a | Zero package-manager installs; the only new external asset is a directly-fetched OFL font file, covered by T-36-05 above. |
</threat_model>

<verification>
- `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors after all three tasks.
- Manual Cmd-U run of `SignatureHeadingTests` in Xcode.
- No change to `Islet/Notch/NotchPillView.swift` in this plan (confirmed via acceptance criteria) — `onboardingWelcomeStep` is untouched until Plan 36-04.
</verification>

<success_criteria>
- `Islet/Fonts/DancingScript-Variable.ttf` (OFL-licensed) bundled and registers at runtime without crashing.
- `SignatureHeading.glyphPaths(for:font:)` extracts correct, correctly-oriented vector outlines for every character of "Meet Islet".
- `SignatureHeading` renders (unwired, static-fill-only) and builds clean.
- Test file confirms the extraction contract's shape.
</success_criteria>

<output>
Create `.planning/phases/36-cosmetic-restyles-signature-animation/36-03-SUMMARY.md` when done
</output>
