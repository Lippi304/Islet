---
phase: 35-liquid-glass-material
plan: 3
type: execute
wave: 2
depends_on: ["35-01", "35-02"]
files_modified:
  - Islet/Notch/NotchPillView.swift
autonomous: true
requirements: [GLASS-01]

must_haves:
  truths:
    - "The collapsed pill, expanded island, and all 3 activity wings (Charging, Device, Now Playing) render the Liquid Glass warp + chromatic-fringe effect when materialStyle == .liquidGlass, and render exactly as before (Gradient/Solid Black) otherwise (ROADMAP Success Criterion #1)"
    - "The effect is applied as a modifier/overlay on the SAME shape node that already carries matchedGeometryEffect(id: \"island\", in: ns) at all 4 sites — never a new sibling/wrapper view (ROADMAP Success Criterion #2)"
    - "Foreground content (album art, equalizer bars, text, icons) stays crisp/undistorted — the effect layer sits strictly between .frame(...) and each site's own content .overlay(...)/.onTapGesture, never wrapping the content itself (D-03)"
    - "The collapsed pill uses LiquidGlassParameters.collapsed (visibly subtler); the expanded island and all 3 wings use .expanded (full strength) (D-04)"
    - "matchedGeometryEffect still precedes .frame at all 4 sites, byte-for-byte unchanged from before this plan (critical regression guard — island-expand-diagonal-bounce)"
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "liquidGlassEffectLayer(shape:size:parameters:) helper + its application at collapsedIsland, blobShape, wingsShape, mediaWingsOrToast"
  key_links:
    - from: "Islet/Notch/NotchPillView.swift collapsedIsland/blobShape/wingsShape/mediaWingsOrToast"
      to: "Islet/Notch/NotchPillView.swift liquidGlassEffectLayer(...)"
      via: ".overlay(...) chained immediately after .frame(...), gated on materialStyle == .liquidGlass"
      pattern: "liquidGlassEffectLayer"
    - from: "Islet/Notch/NotchPillView.swift liquidGlassEffectLayer(...)"
      to: "Islet/Notch/LiquidGlassShader.swift liquidGlassChannelShaders(...)"
      via: "direct function call"
      pattern: "liquidGlassChannelShaders\\("
---

<objective>
Wire the Plan 35-02 shader/parameter contract into all 4 existing island-shell fill call sites in `NotchPillView.swift` via one shared `liquidGlassEffectLayer(...)` helper, applied as an `.overlay(...)` immediately after each site's existing `.frame(...)` — after the `.matchedGeometryEffect`-before-`.frame` sequence that must stay byte-for-byte unchanged, and before each site's own content overlay/gesture modifiers. This is the plan that makes Liquid Glass visually real.

Purpose: Deliver GLASS-01's actual visual effect (D-01 through D-04) without touching the shape's matchedGeometryEffect/frame sequencing that a prior debug session (island-expand-diagonal-bounce) already fixed at exactly these 4 sites.
Output: `liquidGlassEffectLayer(shape:size:parameters:)` helper + its application at all 4 fill sites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-liquid-glass-material/35-CONTEXT.md
@.planning/phases/35-liquid-glass-material/35-PATTERNS.md
@.planning/phases/35-liquid-glass-material/35-UI-SPEC.md
</context>

<interfaces>
From Islet/Notch/LiquidGlassShader.swift (Plan 35-02's output, already merged):
LiquidGlassParameters — struct with .collapsed / .expanded static instances (borderWidth, blurWidth, distortionScale, redOffset, greenOffset, blueOffset, saturation, backgroundOpacity)
LiquidGlassChannelShaders — struct { base: Shader; red: Shader; green: Shader; blue: Shader }
func liquidGlassChannelShaders(size: CGSize, topCornerRadius: CGFloat, bottomCornerRadius: CGFloat, parameters: LiquidGlassParameters) -> LiquidGlassChannelShaders

From Islet/ActivitySettings.swift (Plan 35-01's output, already merged):
MaterialStyle.liquidGlass case exists; NotchPillView already reads @Environment(\.islandMaterialStyle) private var materialStyle (NotchPillView.swift:137, pre-existing).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: liquidGlassEffectLayer shared helper</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift lines 242-268 (islandFill/gradientMaterial — the single seam this new helper sits alongside, not inside)
    Islet/Notch/NotchPillView.swift lines 574-600 (collapsedIsland — the simplest of the 4 call sites, no content overlay, good reference for the insertion point)
    Islet/Notch/LiquidGlassShader.swift (Plan 35-02's output — LiquidGlassParameters, LiquidGlassChannelShaders, liquidGlassChannelShaders(...) exact signatures)
    .planning/phases/35-liquid-glass-material/35-UI-SPEC.md — "Material / Shader Contract" section, especially the render order ("gradientMaterial fill -> .distortionEffect() -> frost overlay -> foreground content") and the Color section's "Accent (~10%, edge-only)" row (chromatic fringe is edge-only, low-opacity, never a decorative UI color elsewhere)
  </read_first>
  <action>
    Add a new private helper to `NotchPillView`, placed near `islandFill` (after its switch, before `collapsedIsland`): `@ViewBuilder private func liquidGlassEffectLayer(shape: NotchShape, size: CGSize, parameters: LiquidGlassParameters) -> some View`. Body: guard `materialStyle == .liquidGlass` else return `EmptyView()`. Otherwise compute `let shaders = liquidGlassChannelShaders(size: size, topCornerRadius: shape.topCornerRadius, bottomCornerRadius: shape.bottomCornerRadius, parameters: parameters)` and build a `ZStack` with 4 layers, back to front: (1) `shape.fill(Self.gradientMaterial).distortionEffect(shaders.base, maxSampleOffset: CGSize(width: abs(parameters.distortionScale), height: abs(parameters.distortionScale)))` — the primary geometric warp (D-01's edge-warp visible ripple); (2) `shape.fill(Color.red.opacity(0.10)).distortionEffect(shaders.red, maxSampleOffset: CGSize(width: abs(parameters.distortionScale) + parameters.redOffset, height: abs(parameters.distortionScale) + parameters.redOffset)).blendMode(.screen)`; (3) the analogous green layer using `shaders.green`/`parameters.greenOffset`; (4) the analogous blue layer using `shaders.blue`/`parameters.blueOffset` — these 3 tinted, independently-offset, `.screen`-blended passes are the chromatic-aberration edge fringe (D-01's second half); the low `0.10` opacity keeps the fringe reserved to the edge band per the UI-SPEC Color contract's "Accent (~10%, edge-only)" row. Apply `.saturation(parameters.saturation)` to the whole `ZStack`, then `.overlay(Color.white.opacity(parameters.backgroundOpacity))` (the frost layer, UI-SPEC "Secondary" row), then `.clipShape(shape)` (contains any warp bleed to the shape's own footprint — UI-SPEC Spacing Scale: "the distortion must fit inside today's frames"), then `.allowsHitTesting(false)` (decorative only — must never intercept the `.onTapGesture`/drag gestures the 4 call sites' outer shape already carries; this is a click-through safety requirement, not optional, mirroring this project's own CR-01 click-through precedent). Add a doc comment above the function citing D-01/D-02/D-03/D-04 and the UI-SPEC render-order line.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "func liquidGlassEffectLayer" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "\.allowsHitTesting(false)" Islet/Notch/NotchPillView.swift` returns at least 1 (inside liquidGlassEffectLayer)
    - `grep -c "\.clipShape(shape)" Islet/Notch/NotchPillView.swift` returns at least 1
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors (helper compiles but is not yet called anywhere — Task 2 wires it in)
  </acceptance_criteria>
  <done>liquidGlassEffectLayer compiles: renders nothing when materialStyle != .liquidGlass, otherwise a 4-layer warp+fringe+frost composite clipped to the given shape and non-interactive.</done>
</task>

<task type="auto">
  <name>Task 2: Apply liquidGlassEffectLayer at all 4 fill sites</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift lines 574-600 (collapsedIsland — exact current modifier chain: .fill -> .matchedGeometryEffect -> .frame -> .scaleEffect -> .offset -> .onTapGesture)
    Islet/Notch/NotchPillView.swift lines 1515-1550 (blobShape — exact current chain: .fill -> .matchedGeometryEffect -> .frame -> .overlay(alignment: .top) { content } )
    Islet/Notch/NotchPillView.swift lines 1658-1681 (wingsShape — exact current chain: .fill -> .matchedGeometryEffect -> .frame -> .overlay(content) -> .onTapGesture)
    Islet/Notch/NotchPillView.swift lines 1729-1750 (mediaWingsOrToast — exact current chain: .fill -> .matchedGeometryEffect -> .frame -> .overlay(alignment: .top) { ... } )
    .planning/phases/35-liquid-glass-material/35-PATTERNS.md — "matchedGeometryEffect-before-frame ordering" Shared Pattern section (the exact regression this task must not reintroduce)
  </read_first>
  <action>
    At each of the 4 call sites, insert `.overlay(liquidGlassEffectLayer(shape: &lt;the site's NotchShape instance&gt;, size: &lt;the site's frame size&gt;, parameters: &lt;.collapsed or .expanded&gt;))` immediately after the existing `.frame(width:height:)` call and BEFORE whatever modifier already follows it at that site — never between `.matchedGeometryEffect` and `.frame`, never replacing or reordering either of those two calls. Concretely: (1) `collapsedIsland` (`NotchPillView.swift:582-599`) — insert after `.frame(width: size.width, height: size.height)`, before `.scaleEffect(...)`; construct the overlay with `shape: NotchShape()` (matching the site's existing bare `NotchShape()`), `size: size` (the already-computed local `let size = ...` on line 581), `parameters: .collapsed` (D-04: this is the reduced-intensity site). (2) `blobShape` (`NotchPillView.swift:1531-1539`) — insert after `.frame(width: baseWidth, height: totalHeight)`, before `.overlay(alignment: .top) { ... }`; construct with `shape: shape` (the site's existing local `let shape = NotchShape(topCornerRadius: topCornerRadius, bottomCornerRadius: bottomCornerRadius)` on line 1530), `size: CGSize(width: baseWidth, height: totalHeight)`, `parameters: .expanded`. (3) `wingsShape` (`NotchPillView.swift:1666-1673`) — insert after `.frame(width: Self.wingsSize.width, height: Self.wingsSize.height)`, before `.overlay(content()...)`; construct with `shape: shape` (the site's existing local `let shape = NotchShape(topCornerRadius: 12, bottomCornerRadius: 6)`), `size: Self.wingsSize`, `parameters: .expanded`. (4) `mediaWingsOrToast` (`NotchPillView.swift:1733-1740`) — insert after `.frame(width: Self.wingsSize.width, height: height)`, before `.overlay(alignment: .top) { ... }`; construct with `shape: NotchShape(topCornerRadius: 6, bottomCornerRadius: toast != nil ? 16 : 6)` (matching this site's existing inline shape construction — build a second instance with the same literal arguments since this site does not bind the shape to a local `let`), `size: CGSize(width: Self.wingsSize.width, height: height)`, `parameters: .expanded`. Do not touch any other line in any of the 4 modifier chains.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "liquidGlassEffectLayer(shape:" Islet/Notch/NotchPillView.swift` returns 4 (one per call site)
    - `grep -c "parameters: .collapsed" Islet/Notch/NotchPillView.swift` returns 1 (collapsedIsland only)
    - `grep -c "parameters: .expanded" Islet/Notch/NotchPillView.swift` returns 3 (blobShape, wingsShape, mediaWingsOrToast)
    - `awk '/\.fill\(/{fillLine=NR} /\.matchedGeometryEffect\(id: "island", in: ns\)/{if (fillLine && NR-fillLine<=10) c++} END{print c+0}' Islet/Notch/NotchPillView.swift` returns 4 (each `.matchedGeometryEffect` occurrence has a preceding `.fill(` within 10 lines, tolerant of the intervening multi-line D-07 bugfix-comment blocks documented at all 4 real call sites — replaces an earlier immediately-adjacent-line check that broke against those comments)
    - `grep -A1 "\.matchedGeometryEffect(id: \"island\", in: ns)" Islet/Notch/NotchPillView.swift | grep -c "\.frame("` returns 4 (matchedGeometryEffect still immediately precedes .frame at all 4 sites, unchanged — the critical regression guard)
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors
  </acceptance_criteria>
  <done>All 4 fill sites render the Liquid Glass effect when materialStyle == .liquidGlass and render unchanged otherwise; matchedGeometryEffect-before-frame ordering is byte-for-byte preserved at every site; build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | Pure local rendering — no network, no external input, no new data storage. The only "input" driving this code path is the already-local `materialStyle` enum read from `@AppStorage`/EnvironmentKey (Plan 35-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-35-06 | Denial of Service | 4x duplicated shape+fill render passes per frame (3 chromatic layers + 1 base, at up to 4 simultaneous call sites) causing dropped frames during collapse<->expand morph | mitigate | Effect area is small (32-420pt, never full-screen); `.allowsHitTesting(false)` avoids extra hit-test overhead; ROADMAP Success Criterion #3's on-device UAT hard gate (Plan 35-05) explicitly checks for dropped frames before merge — if it regresses, the fallback is reducing chromatic-layer opacity/count, not shipping a janky morph |
| T-35-07 | Spoofing (UI) | `.allowsHitTesting(false)` omitted by mistake, effect layer silently starts swallowing taps/drags | mitigate | Task 1's own acceptance criteria greps for `.allowsHitTesting(false)` inside the helper before Task 2 ever wires it in; Plan 35-05's UAT checklist re-verifies collapsedIsland's tap-to-toggle and wingsShape's tap-to-toggle still work with Liquid Glass selected |
| T-35-SC | Tampering | npm/pip/cargo installs | n/a | Zero third-party packages this task |
</threat_model>

<verification>
- `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors after both tasks
- `grep -A1 "\.matchedGeometryEffect(id: \"island\", in: ns)" Islet/Notch/NotchPillView.swift | grep -c "\.frame("` returns 4 (the ordering regression guard, re-checked at plan level)
</verification>

<success_criteria>
- All 4 island-shell fill sites (collapsedIsland, blobShape, wingsShape, mediaWingsOrToast) render the Liquid Glass warp + chromatic fringe when `.liquidGlass` is selected
- The effect is invisible (EmptyView) for `.gradient`/`.solidBlack`, with zero visual or behavioral change to those two existing styles
- matchedGeometryEffect precedes .frame at all 4 sites, unchanged from before this plan
- collapsedIsland uses `.collapsed` parameters (subtler); the other 3 sites use `.expanded` (full strength), per D-04
</success_criteria>

<output>
Create `.planning/phases/35-liquid-glass-material/35-03-SUMMARY.md` when done
</output>
</output>
