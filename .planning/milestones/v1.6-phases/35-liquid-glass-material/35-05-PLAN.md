---
phase: 35-liquid-glass-material
plan: 5
type: execute
wave: 3
depends_on: ["35-03", "35-04"]
files_modified: []
autonomous: false
requirements: [GLASS-01]

must_haves:
  truths:
    - "The collapsed pill, expanded island, and all 3 activity wings render Liquid Glass correctly through a full collapse<->expand cycle with no artifacts, no dropped frames, and no click-through/tap regression (ROADMAP Success Criteria #1, #3)"
    - "The user has visually approved the result on-device against reference-GlassSurface.md's technique (ROADMAP Success Criterion #4) — this is a subjective, human-only judgment call no automated check can substitute for"
    - "Fresh-install and existing-user default-to-Liquid-Glass behavior (D-06) is confirmed on-device, not just by reading the source"
  artifacts: []
  key_links: []
---

<objective>
Run the Phase-25-style on-device UAT checklist mandated as a hard merge gate by ROADMAP Success Criteria #3/#4 and 35-UI-SPEC.md's "On-Device UAT Gate" section. This phase's shader parameter values (`LiquidGlassParameters.collapsed`/`.expanded` from Plan 35-02) are explicit on-device-tunable starting points, not final — this checkpoint is where they get verified and, if needed, adjusted before the phase can be considered done.

Purpose: GLASS-01 is a visual/material feature with no automated way to verify "looks right" — every comparably visual phase in this project (25, 26, 29, 32, 33) required this same kind of gate, and several needed multiple rounds. Budget for iteration.
Output: On-device UAT confirmation (or a documented list of gaps for a gap-closure round).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-liquid-glass-material/35-CONTEXT.md
@.planning/phases/35-liquid-glass-material/35-UI-SPEC.md
@.planning/phases/35-liquid-glass-material/reference-GlassSurface.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: On-device UAT — Liquid Glass render, collapse/expand, all 3 wings, Settings picker + window background, D-06 default</name>
  <files>Islet/Notch/LiquidGlassShader.metal, Islet/Notch/LiquidGlassShader.swift, Islet/Notch/NotchPillView.swift, Islet/ActivitySettings.swift, Islet/SettingsView.swift</files>
  <action>Build and run Islet with Plans 35-01 through 35-04's changes in place, then execute the 7-step on-device verification below. No code changes in this task — it is verification-only, the hard merge gate ROADMAP Success Criteria #3/#4 require before Phase 35 is done.</action>
  <what-built>
    Plans 35-01 through 35-04 shipped: (1) `MaterialStyle.liquidGlass` as a new default-selected third material option; (2) a Metal `.distortionEffect()` shader (`LiquidGlassShader.metal`) producing an edge-warp ripple plus a 3-layer red/green/blue chromatic-fringe overlay, applied at all 4 island-shell fill sites (collapsed pill, expanded island, Charging/Device/Now-Playing wings) via a shared `liquidGlassEffectLayer(...)` helper; (3) the collapsed pill using visibly subtler parameters than the expanded states (D-04); (4) a third "Liquid Glass" segment in Settings' Theming picker with the default flipped to Liquid Glass in both required locations; (5) a calmer, non-distorted frosted background on the Settings window itself (D-08/D-09).
  </what-built>
  <how-to-verify>
    Open the project in Xcode: `/Users/lippi304/conductor/workspaces/notch/algiers/Islet.xcodeproj`. Select the `Islet` scheme and press Run (Cmd-R) to launch a Debug build.

    1. Hover over the notch to expand the island (Home view). Confirm the collapsed pill and the expanded island both show a visible warped/rippled edge on the black material with a subtle color-fringed edge (not a flat black gradient like before Phase 25/35) — this is the Liquid Glass effect (fresh installs default to it, D-06).
    2. Watch the collapse<->expand transition several times (hover in/out repeatedly). Confirm: no visual artifacts (flickering, torn edges, popping), no dropped-frame stutter, and the pill still morphs smoothly from its notch position with no diagonal jump or bounce (this would indicate the matchedGeometryEffect/frame regression from before this phase has returned).
    3. Play some audio (or let Now Playing show a track) to trigger the Now Playing wings, then plug/unplug the charger to trigger the Charging wings, then connect/disconnect a Bluetooth device (e.g. AirPods) to trigger the Device wings. Confirm all 3 wings show the same Liquid Glass warp + fringe as the pill/expanded island — collapsed pill's effect should read as noticeably SUBTLER than the wings/expanded island's (D-04).
    4. While any of the above is showing content (track title, equalizer bars, charging %, device name), confirm the TEXT AND ICONS stay perfectly crisp and legible — only the black background material should show the warp, never the foreground content (D-03). Report immediately if any text/icon looks smeared or distorted.
    5. Click the menu-bar Islet icon (or however you normally open Settings) to open the Settings window. Go to System (Theming). Confirm a 3rd "Liquid Glass" segment exists alongside "Gradient"/"Solid Black", and it is the one currently SELECTED (confirms the D-06 default flip). Switch between all 3 options one at a time, glancing at the notch island each time — confirm the island updates LIVE with no relaunch needed, and Gradient/Solid Black look exactly as they did before this phase (zero regression to the other 2 styles).
    6. While Settings is open on the System (Theming) tab, look at the Settings WINDOW's own background (not the notch island) — confirm it shows a subtle frosted/blurred dark gradient with a faint rim-light edge highlight, and that it shows NO warp/ripple distortion (D-09) and all text/labels/toggles remain fully readable.
    7. Quit and relaunch Islet (or, if this machine already has an existing Islet install with a prior `materialStyle` choice saved, note whether it changed) — confirm a fresh/default install shows Liquid Glass without the user having picked it (D-06's "new users" half); if you have an existing saved preference already set to Gradient/Solid Black from before this phase, confirm your explicit choice was RESPECTED (D-05/D-06 only changes the default for users who never chose — an existing user's `.gradient`/`.solidBlack` preference should survive this upgrade if `.gradient`/`.solidBlack` was already what was actually persisted to `UserDefaults`, not merely "the compiled-in default").
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 checks pass, or describe exactly which check(s) failed and what you saw (e.g. "check 3: Device wings show no fringe at all" or "check 2: still bounces diagonally on expand") so a gap-closure plan can target the right file.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | This plan performs no code changes — it is a human-verification gate over already-shipped local-rendering code from Plans 35-01 through 35-04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-35-10 | n/a | This plan (checkpoint only) | n/a | No code artifacts produced by this plan; the threat surface is entirely inherited from Plans 35-01 through 35-04, whose own threat models already cover it |
</threat_model>

<verification>
- Human operator completes all 7 on-device checks and types "approved", OR reports specific gaps for a gap-closure plan
</verification>

<success_criteria>
- ROADMAP Success Criteria #1-4 for Phase 35 are all confirmed true on-device
- The 35-UI-SPEC.md "On-Device UAT Gate" checklist's 7 items all pass
- If any check fails, the specific failure is captured precisely enough to scope a `/gsd:plan-phase 35 --gaps` gap-closure plan without re-investigation
</success_criteria>

<output>
Create `.planning/phases/35-liquid-glass-material/35-05-SUMMARY.md` when done
</output>
