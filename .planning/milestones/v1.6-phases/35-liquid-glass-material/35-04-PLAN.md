---
phase: 35-liquid-glass-material
plan: 4
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - Islet/SettingsView.swift
autonomous: true
requirements: [GLASS-01]

must_haves:
  truths:
    - "Settings' System (Theming) picker offers a third 'Liquid Glass' segment alongside 'Gradient'/'Solid Black', and selecting it live-updates the island shell with no relaunch (D-05)"
    - "Fresh installs AND existing users default to Liquid Glass — SettingsView.swift's @AppStorage default is .liquidGlass, matching Plan 35-01's EnvironmentKey default (D-06, the second of the two required locations per 35-PATTERNS.md)"
    - "The Settings window background shows the calmer frosted variant (blur/frost + calmer gradient + rim-light stroke) with NO distortion shader, and remains fully readable (D-08/D-09)"
  artifacts:
    - path: "Islet/SettingsView.swift"
      provides: "Third Picker segment (Liquid Glass), @AppStorage default flipped to .liquidGlass, window-background modifier (calmer gradient + .ultraThinMaterial + rim-light stroke, no distortionEffect)"
  key_links:
    - from: "Islet/SettingsView.swift systemSection Picker"
      to: "Islet/ActivitySettings.swift materialStyleKey"
      via: "$materialStyle @AppStorage binding"
      pattern: "Text\\(\"Liquid Glass\"\\)\\.tag\\(MaterialStyle\\.liquidGlass\\)"
---

<objective>
Add the third "Liquid Glass" segment to Settings' existing Theming picker, flip the `@AppStorage` default to `.liquidGlass` (D-06's second location — Plan 35-01 already flipped the EnvironmentKey copy), and give the Settings window its own calmer, non-distorted frosted background (D-08/D-09) — a separate integration point from the island shell, not routed through `islandFill`.

Purpose: Complete D-06's "default for both new and existing users" requirement (both hardcoded-default locations must agree) and deliver the explicitly-approved Settings-window scope extension (D-08) without touching Plan 35-03's island-shell code at all — zero file overlap, safe to run in parallel with it.
Output: Updated `systemSection` Picker, updated `@AppStorage` default, new Settings window background modifier.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-liquid-glass-material/35-CONTEXT.md
@.planning/phases/35-liquid-glass-material/35-PATTERNS.md
@.planning/phases/35-liquid-glass-material/35-UI-SPEC.md
</context>

<interfaces>
From Islet/ActivitySettings.swift (Plan 35-01's output, already merged): MaterialStyle.liquidGlass case exists.
From Islet/Notch/NotchPillView.swift (pre-existing, unchanged by this plan): `private static let gradientMaterial = LinearGradient(stops: [.init(color: .black, location: 0.0), .init(color: .black, location: 0.65), .init(color: .black.opacity(0.5), location: 1.0)], startPoint: .top, endPoint: .bottom)` — the direction this plan's calmer Settings-window gradient must mirror (top->bottom, black-to-transparent), at lower/lighter alpha per D-09. NotchPillView.swift is NOT modified by this plan — read-only reference for the gradient direction.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Theming picker third segment + @AppStorage default flip</name>
  <files>Islet/SettingsView.swift</files>
  <read_first>
    Islet/SettingsView.swift lines 43-51 (the `@AppStorage(ActivitySettings.materialStyleKey)` declaration and its "no manual Binding needed" doc comment convention to preserve)
    Islet/SettingsView.swift lines 250-269 (systemSection — the exact Picker block to extend, and its "plain two-word label, no icon, no subtitle" convention already followed by the existing 2 segments)
    .planning/phases/35-liquid-glass-material/35-CONTEXT.md — Decision D-06 ("both new AND existing users")
    .planning/phases/35-liquid-glass-material/35-PATTERNS.md — "Duplicate default locations" note (SettingsView.swift:48 and ActivitySettings.swift:109 are BOTH independently hardcoded; Plan 35-01 already flipped the second one)
  </read_first>
  <action>
    In `SettingsView.swift:48`, change `@AppStorage(ActivitySettings.materialStyleKey) private var materialStyle: ActivitySettings.MaterialStyle = .gradient` to end in `= .liquidGlass` (D-06, second of the two required default-flip locations — `ActivitySettings.swift`'s `IslandMaterialStyleKey.defaultValue` was already flipped in Plan 35-01). In the `systemSection` Picker (`SettingsView.swift:255-258`), add a third line `Text("Liquid Glass").tag(MaterialStyle.liquidGlass)` inside the existing `Picker("Style", selection: $materialStyle) { ... }` block, immediately after the existing `Text("Solid Black").tag(MaterialStyle.solidBlack)` line — matches the exact plain-label convention of the two existing segments, no icon, no subtitle, per the UI-SPEC Copywriting Contract's exact string "Liquid Glass".
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "private var materialStyle: ActivitySettings.MaterialStyle = .liquidGlass" Islet/SettingsView.swift` returns 1
    - `grep -c "Text(\"Liquid Glass\").tag(MaterialStyle.liquidGlass)" Islet/SettingsView.swift` returns 1
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors
  </acceptance_criteria>
  <done>Settings' Theming picker offers 3 segments; the @AppStorage default is .liquidGlass, matching ActivitySettings.swift's EnvironmentKey default from Plan 35-01.</done>
</task>

<task type="auto">
  <name>Task 2: Settings window calmer background (D-08/D-09)</name>
  <files>Islet/SettingsView.swift</files>
  <read_first>
    Islet/SettingsView.swift lines 80-138 (body's outer NavigationSplitView { ... } detail: { ... } chain and its trailing `.frame(width: 520, height: 380)` — the exact attachment point for a new `.background(...)` modifier)
    .planning/phases/35-liquid-glass-material/35-UI-SPEC.md — "Settings window background" table (Blur/frost, Gradient direction, Rim-light highlight, explicit "Distortion: None" row)
    .planning/phases/35-liquid-glass-material/35-CONTEXT.md — Decisions D-08 (scope extension, Onboarding explicitly excluded) and D-09 (calmer variant, no distortion shader — readability risk on text-heavy forms)
  </read_first>
  <action>
    In `SettingsView.swift`, add a `.background(...)` modifier chained onto the `body`'s outer `NavigationSplitView { ... } detail: { ... }` block, placed BEFORE the existing trailing `.frame(width: 520, height: 380)` (so the background fills the full window before the frame constrains it — matches SwiftUI's standard background-then-frame ordering, unrelated to the island-shell's matchedGeometryEffect-before-frame constraint, which does not apply to this file). The background is a `ZStack` with 3 layers: (1) a `LinearGradient` with the SAME `startPoint: .top, endPoint: .bottom` direction as `NotchPillView.gradientMaterial`, but a calmer/lighter stop sequence appropriate for a text-heavy form — e.g. `.black.opacity(0.25)` at `0.0`, `.black.opacity(0.15)` at `0.65`, `.black.opacity(0.05)` at `1.0` (roughly half the alpha of the island shell's stops at every point, per D-09's "must stay readable" requirement); (2) `Color.clear.background(.ultraThinMaterial)` (the frost/blur layer, D-09 — explicitly NO `.distortionEffect()` anywhere in this file); (3) a `RoundedRectangle(cornerRadius: 12)` (a reasonable window-chrome corner radius, not tied to `NotchShape`'s notch-specific radii) with `.strokeBorder(Color.white.opacity(0.15), lineWidth: 1)` for the rim-light highlight per D-09. Add a doc comment above the modifier citing D-08 (scope extension, approved) and D-09 (calmer/no-distortion rationale), and explicitly noting this is a SEPARATE integration point from `islandFill` — this file has no shader/distortion code at all.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ultraThinMaterial" Islet/SettingsView.swift` returns 1
    - `grep -c "strokeBorder(Color.white.opacity" Islet/SettingsView.swift` returns 1
    - `grep -c "distortionEffect" Islet/SettingsView.swift` returns 0 (D-09 hard constraint — no distortion shader on this surface)
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors
  </acceptance_criteria>
  <done>Settings window shows a calmer gradient + ultraThinMaterial frost + rim-light stroke background, with zero distortionEffect usage anywhere in SettingsView.swift; build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | Pure local Settings UI — a closed-set Picker (3 tags) and a static decorative background; no network, no external input, no new data storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-35-08 | Tampering | `@AppStorage(materialStyleKey)` corrupted/unparseable value | accept | Already-established project pattern (see `SettingsView.swift:43-47`): SwiftUI's `RawRepresentable` `@AppStorage` overload falls back to the declared default automatically — no crash path |
| T-35-09 | Information Disclosure / Readability | Frosted/gradient background reduces text contrast on a form with license-key entry and other sensitive-ish fields | mitigate | D-09's explicit calmer alpha curve (roughly half the island shell's opacity at every gradient stop) keeps text/forms readable; Plan 35-05's on-device UAT checklist explicitly re-checks Settings-window readability before merge |
| T-35-SC | Tampering | npm/pip/cargo installs | n/a | Zero third-party packages this task |
</threat_model>

<verification>
- `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors after both tasks
</verification>

<success_criteria>
- Settings' Theming picker has 3 segments; selecting "Liquid Glass" persists via the existing `materialStyleKey` AppStorage
- Both hardcoded default locations (`SettingsView.swift:48`, `ActivitySettings.swift:109` from Plan 35-01) agree on `.liquidGlass`
- Settings window renders the calmer frosted background with zero `.distortionEffect()` usage in this file
</success_criteria>

<output>
Create `.planning/phases/35-liquid-glass-material/35-04-SUMMARY.md` when done
</output>
