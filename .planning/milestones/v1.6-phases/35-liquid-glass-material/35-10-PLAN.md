---
phase: 35-liquid-glass-material
plan: 10
type: execute
wave: 2
depends_on: ["35-09"]
files_modified: []
autonomous: false
requirements: [GLASS-01]

must_haves:
  truths:
    - "The collapsed pill, expanded island, and all 3 activity wings render as DARK glass — a mostly solid/dark surface (D-15: allowed to read as dark as .solidBlack) with the desktop only bleeding through as a faint, narrow, colored rim-light right at the rounded edge (D-12/D-13/D-14, matching reference-transparency-target.png) — not uniformly bright/light across the whole surface (the exact round-2 rejection) and not a flat opaque grey with zero transparency (the round-1 rejection)"
    - "The material composites cleanly through a full collapse<->expand cycle with no artifacts, no dropped frames, and no click-through/tap regression (ROADMAP Success Criteria #1, #3)"
    - "The warp + chromatic-fringe effect (D-01/D-03, unaffected by this round) is still visible and is confirmed as the user-perceived differentiator between Liquid Glass and Solid Black, not center darkness (D-15)"
    - "The user has visually approved the result on-device against reference-GlassSurface.md's technique AND reference-transparency-target.png's narrow-rim/dark-center pattern (ROADMAP Success Criterion #4) — this is a subjective, human-only judgment call no automated check can substitute for"
    - "Fresh-install and existing-user default-to-Liquid-Glass behavior (D-06) and the Settings window's calmer background (D-08/D-09, unaffected by any Phase 35 remediation round) are re-confirmed on-device"
  artifacts: []
  key_links: []
---

<objective>
Re-run the full Phase-25-style on-device UAT checklist (`35-UAT.md`'s 7 tests, blocked on Test 1 twice now — round 1's opaque-grey rejection and round 2's uniformly-bright rejection) now that the D-12/D-13/D-14/D-15 frost-over-material remediation (Plan 35-09) has shipped. This plan SUPERSEDES the rejected `35-05-PLAN.md` (round 1) and `35-08-PLAN.md` (round 2) checkpoints — neither is re-run or reused; `35-UAT.md` remains the historical record of both prior rejections. This is the hard merge gate ROADMAP Success Criteria #3/#4 require before Phase 35 is considered done.

Purpose: GLASS-01 is a visual/material feature with no automated way to verify "looks right". Round 1 failed because the base was opaque; round 2 fixed translucency but the base had no dark tint and read as uniformly bright. Plan 35-09 restructured the compositing so a genuinely dark frost layer is the default state, with the backdrop only revealed at a narrow rim. This checkpoint verifies that fix actually reads as dark glass on real hardware, not just that it compiles.
Output: On-device UAT confirmation (or a documented list of gaps for a further gap-closure round).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-liquid-glass-material/35-CONTEXT.md
@.planning/phases/35-liquid-glass-material/35-UAT.md
@.planning/phases/35-liquid-glass-material/35-UI-SPEC.md
@.planning/phases/35-liquid-glass-material/reference-GlassSurface.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: On-device UAT (round 3) — dark frost base, narrow rim reveal, collapse/expand, all 3 wings, Settings picker + window background, D-06 default</name>
  <files>Islet/Notch/LiquidGlassShader.swift, Islet/Notch/NotchPillView.swift</files>
  <read_first>
    .planning/phases/35-liquid-glass-material/35-UAT.md (the original 7-test checklist plus both prior round rejections this round re-verifies from scratch)
    .planning/phases/35-liquid-glass-material/reference-transparency-target.png (the user-supplied target screenshot — dark/readable center, desktop bleeding through only as a thin colored rim-light at the very edge)
  </read_first>
  <action>Build and run Islet with Plans 35-01 through 35-04, 35-06, 35-07, and the new 35-09 remediation in place, then execute the 7-step on-device verification below. No code changes in this task — it is verification-only, the hard merge gate ROADMAP Success Criteria #3/#4 require before Phase 35 is done.</action>
  <what-built>
    Plans 35-01 through 35-04 shipped the original Liquid Glass material feature. Round 1's checkpoint (35-05) rejected it as a flat opaque grey with no transparency. Round 2 (35-06/35-07) fixed translucency by swapping in a raw `.ultraThinMaterial` base, but its checkpoint (35-08) was rejected too: the material has no inherent dark tint, so the whole surface read as uniformly bright, tracking whatever was behind the notch (a light Xcode toolbar). Round 3 (this plan's prerequisite, 35-09) restructured the compositing per `35-CONTEXT.md` D-12/D-13/D-14/D-15: `islandFill`'s `.liquidGlass` branch and `liquidGlassEffectLayer` now put a solid dark frost layer (`Self.gradientMaterial`, the same near-opaque black used by the `.gradient` style) IN FRONT of the warped `.ultraThinMaterial` backdrop, with the frost's own alpha ramped by the existing `liquidGlassEdgeOpacity` shader — opaque toward the center (allowed to read as dark as `.solidBlack`, D-15), narrow and transparent right at the rounded edge (D-13/D-14), so the backdrop only bleeds through a thin rim instead of washing the whole surface.
  </what-built>
  <how-to-verify>
    Open the project in Xcode: `/Users/lippi304/conductor/workspaces/notch/algiers/Islet.xcodeproj`. Select the `Islet` scheme and press Run (Cmd-R) to launch a Debug build.

    1. Hover over the notch to expand the island (Home view). Confirm the collapsed pill and the expanded island now read as DARK glass — mostly dark/solid in the center (it is fine and expected if this looks nearly as dark as `.solidBlack` there, per D-15), with a thin, colored rim-light hint of your desktop/wallpaper bleeding through ONLY right at the rounded edge (compare against `reference-transparency-target.png`). This should NOT look like a uniformly bright/light panel (that was the round-2 rejection) and should NOT look like a flat opaque panel with zero visible warp/fringe (that was the round-1 rejection). Also confirm the warped/rippled edge and subtle chromatic fringe (D-01) are still visible at that same rim. Report precisely what you see, especially if the center still looks noticeably lighter than Solid Black or if the rim looks too wide/soft rather than a thin sliver.
    2. Watch the collapse<->expand transition several times (hover in/out repeatedly). Confirm: no visual artifacts (flickering, torn edges, popping), no dropped-frame stutter, and the pill still morphs smoothly from its notch position with no diagonal jump or bounce.
    3. Play some audio (or let Now Playing show a track) to trigger the Now Playing wings, then plug/unplug the charger to trigger the Charging wings, then connect/disconnect a Bluetooth device (e.g. AirPods) to trigger the Device wings. Confirm all 3 wings show the same dark-glass-with-narrow-rim look as the pill/expanded island — collapsed pill's effect should read as noticeably SUBTLER than the wings/expanded island's (D-04).
    4. While any of the above is showing content (track title, equalizer bars, charging %, device name), confirm the TEXT AND ICONS stay perfectly crisp and legible against the now-mostly-dark background — only the background material shows warp/rim-transparency, never the foreground content (D-03).
    5. Click the menu-bar Islet icon to open Settings. Go to System (Theming). Confirm the "Liquid Glass" segment is still present and selected by default (D-06). Switch between all 3 options one at a time, glancing at the notch island each time — confirm the island updates LIVE with no relaunch needed, and Gradient/Solid Black look exactly as they did before this phase (zero regression to the other 2 styles). Also confirm Liquid Glass's center darkness is comparable to (not dramatically different from) Solid Black's — per D-15 this is expected and correct; the warp+fringe+narrow rim is what should visually distinguish the two styles, not center brightness.
    6. While Settings is open on System (Theming), look at the Settings WINDOW's own background (not the notch island) — confirm it still shows the calmer frosted/blurred dark gradient with a faint rim-light edge highlight, NO warp/ripple distortion (D-09, untouched by any Phase 35 remediation round), and all text/labels/toggles remain fully readable.
    7. Quit and relaunch Islet — confirm a fresh/default install shows Liquid Glass without the user having picked it (D-06's "new users" half); if this machine already has an existing saved `materialStyle` preference from before this phase, confirm your explicit choice was RESPECTED.
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 checks pass, or describe exactly which check(s) failed and what you saw (e.g. "check 1: center still looks lighter than Solid Black" or "check 1: rim looks too wide, not a thin sliver") so a further gap-closure plan can target the right parameter/file precisely.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | This plan performs no code changes — it is a human-verification gate over already-shipped local-rendering code from Plans 35-01 through 35-04, 35-06, 35-07, and 35-09 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-35-18 | n/a | This plan (checkpoint only) | n/a | No code artifacts produced by this plan; the threat surface is entirely inherited from Plans 35-01 through 35-04, 35-06, 35-07, and 35-09, whose own threat models already cover it |
</threat_model>

<verification>
- Human operator completes all 7 on-device checks and types "approved", OR reports specific gaps for a further gap-closure plan
</verification>

<success_criteria>
- ROADMAP Success Criteria #1-4 for Phase 35 are all confirmed true on-device, including the dark-glass-with-narrow-rim read that both prior rounds failed to achieve
- The `35-UI-SPEC.md` "On-Device UAT Gate" checklist's 7 items all pass
- If any check fails, the specific failure is captured precisely enough to scope a further `/gsd:plan-phase 35 --gaps` gap-closure plan without re-investigation
</success_criteria>

<output>
Create `.planning/phases/35-liquid-glass-material/35-10-SUMMARY.md` when done
</output>
</content>
