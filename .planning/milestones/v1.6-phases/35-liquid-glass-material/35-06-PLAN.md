---
phase: 35-liquid-glass-material
plan: 6
type: execute
wave: 1
depends_on: ["35-02"]
files_modified:
  - Islet/Notch/LiquidGlassShader.metal
  - Islet/Notch/LiquidGlassShader.swift
autonomous: true
requirements: [GLASS-01]

must_haves:
  truths:
    - "liquidGlassDistortion (D-01's warp, unchanged behavior) and the new liquidGlassEdgeOpacity colorEffect function share the SAME extracted edge-falloff computation (D-11) — never two independently-tuned falloff curves"
    - "LiquidGlassParameters exposes edgeOpacity/centerOpacity (D-11's opacity ramp) and a fringeOpacity field (replaces the hardcoded 0.10 literal Plan 35-07 will remove from NotchPillView.swift), with .collapsed values visibly subtler than .expanded at every new field (D-04)"
    - "Neither new function nor new field is consumed anywhere yet (interface-first scaffolding step) — Plan 35-07 is the consumer"
  artifacts:
    - path: "Islet/Notch/LiquidGlassShader.metal"
      provides: "liquidGlassEdgeFalloff shared helper (extracted from liquidGlassDistortion) + liquidGlassEdgeOpacity [[stitchable]] colorEffect function"
    - path: "Islet/Notch/LiquidGlassShader.swift"
      provides: "LiquidGlassParameters.edgeOpacity/centerOpacity/fringeOpacity fields + retuned .collapsed/.expanded static instances"
  key_links:
    - from: "Islet/Notch/LiquidGlassShader.metal liquidGlassDistortion"
      to: "Islet/Notch/LiquidGlassShader.metal liquidGlassEdgeFalloff"
      via: "direct function call (single source of truth for the edge/border falloff, D-11)"
      pattern: "liquidGlassEdgeFalloff\\("
    - from: "Islet/Notch/LiquidGlassShader.metal liquidGlassEdgeOpacity"
      to: "Islet/Notch/LiquidGlassShader.metal liquidGlassEdgeFalloff"
      via: "direct function call"
      pattern: "liquidGlassEdgeFalloff\\("
---

<objective>
Fix the root cause the on-device UAT rejection diagnosed (35-UAT.md Test 1, D-02 superseded by D-10/D-11): extract the existing warp shader's edge-falloff computation into a shared helper, add a second Metal colorEffect function that uses that SAME falloff to ramp per-pixel opacity from transparent-at-the-edge to opaque-at-the-center, and retune `LiquidGlassParameters` with the new fields plus a `fringeOpacity` field replacing NotchPillView.swift's hardcoded `0.10` literal. This is the interface-first contract step — nothing calls the new function/fields yet (Plan 35-07 wires them into NotchPillView.swift's material base).

Purpose: Give Plan 35-07 a ready-to-call, already-compiling edge-opacity shader + retuned parameter contract, mirroring how Plan 35-02 originally staged the warp shader ahead of Plan 35-03's wiring.
Output: `liquidGlassEdgeFalloff` shared helper, `liquidGlassEdgeOpacity` colorEffect function, `LiquidGlassParameters.edgeOpacity/centerOpacity/fringeOpacity` fields with retuned `.collapsed`/`.expanded` values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-liquid-glass-material/35-CONTEXT.md
@.planning/phases/35-liquid-glass-material/35-UAT.md
@.planning/phases/35-liquid-glass-material/reference-GlassSurface.md
</context>

<interfaces>
From Islet/Notch/LiquidGlassShader.metal (Plan 35-02's output, current — read before refactoring):
[[ stitchable ]] float2 liquidGlassDistortion(float2 position, float2 size, float topCornerRadius, float bottomCornerRadius, float borderWidth, float blurWidth, float distortionScale)
Steps 1-5 of this function (topCornerRadius/bottomCornerRadius blend, rounded-box SDF, edgeDist, edgeSize, smoothstep transition `t`) are the exact block to extract.

From Islet/Notch/LiquidGlassShader.swift (Plan 35-02's output, current):
struct LiquidGlassParameters { var borderWidth, blurWidth, distortionScale, redOffset, greenOffset, blueOffset, saturation, backgroundOpacity: CGFloat; static let collapsed/.expanded }
struct LiquidGlassChannelShaders { let base, red, green, blue: Shader }
func liquidGlassChannelShaders(size:topCornerRadius:bottomCornerRadius:parameters:) -> LiquidGlassChannelShaders
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared edge-falloff + add liquidGlassEdgeOpacity colorEffect function</name>
  <files>Islet/Notch/LiquidGlassShader.metal</files>
  <read_first>
    Islet/Notch/LiquidGlassShader.metal (full file, current — Steps 1-5 of liquidGlassDistortion are the block to extract; Steps 6-7 stay inside liquidGlassDistortion unchanged)
    .planning/phases/35-liquid-glass-material/35-CONTEXT.md — Decisions D-10 (translucent Material base) and D-11 (edge-weighted opacity reusing the SAME edgeDist/border-band falloff as the warp shader — "one source of truth for how close to the edge is this pixel")
    .planning/phases/35-liquid-glass-material/35-UAT.md — Test 1's failure description (flat opaque grey, no see-through) — the concrete symptom this shader-level fix targets
  </read_first>
  <action>
    Refactor `Islet/Notch/LiquidGlassShader.metal`: extract Steps 1-5 of the existing `liquidGlassDistortion` function (the topCornerRadius/bottomCornerRadius blend by `position.y/size.y`, the rounded-box SDF distance, `edgeDist`, `edgeSize`, and the `smoothstep(0.0, edgeSize + blurWidth, edgeDist)` transition) into a new shared helper `static float liquidGlassEdgeFalloff(float2 position, float2 size, float topCornerRadius, float bottomCornerRadius, float borderWidth, float blurWidth)` placed above `liquidGlassDistortion`, returning the same `t` value (0.0 = right at the shape boundary / full effect, 1.0 = interior / no effect) Steps 1-5 currently compute inline. Update `liquidGlassDistortion`'s body to call `float t = liquidGlassEdgeFalloff(position, size, topCornerRadius, bottomCornerRadius, borderWidth, blurWidth);` in place of its own Steps 1-5, keeping Steps 6-7 (the epsilon-guarded outward direction vector and `position + dir * (1.0 - t) * distortionScale`) unchanged — this refactor MUST NOT alter `liquidGlassDistortion`'s numeric output at all (D-01 is unaffected, this is a pure extraction). Then add a new function below `liquidGlassDistortion`: `[[ stitchable ]] half4 liquidGlassEdgeOpacity(float2 position, half4 color, float2 size, float topCornerRadius, float bottomCornerRadius, float borderWidth, float blurWidth, float edgeOpacity, float centerOpacity)` — SwiftUI's `.colorEffect(_:)` per-pixel color-transform signature (`float2 position, half4 color, ...` — NEVER `[[ stitchable ]] float2` like the distortion function; colorEffect shaders return `half4`, never a position). Body: `float t = liquidGlassEdgeFalloff(position, size, topCornerRadius, bottomCornerRadius, borderWidth, blurWidth); float alpha = mix(edgeOpacity, centerOpacity, t); return half4(color.rgb, color.a * half(alpha));`. Add a header comment above `liquidGlassEdgeOpacity` citing D-10/D-11: this multiplies the translucent Material base's per-pixel alpha down to `edgeOpacity` right at the rounded edge (desktop visibly bleeds through, matching `reference-transparency-target.png`) and up to `centerOpacity` toward the interior (keeps foreground content legible per D-03), driven by the IDENTICAL `liquidGlassEdgeFalloff` curve the warp shader uses — one source of truth for "how close to the edge is this pixel", per D-11's explicit instruction not to invent a second independent falloff curve.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "static float liquidGlassEdgeFalloff" Islet/Notch/LiquidGlassShader.metal` returns 1
    - `grep -c "\[\[ *stitchable *\]\]" Islet/Notch/LiquidGlassShader.metal` returns 2 (liquidGlassDistortion + liquidGlassEdgeOpacity)
    - `grep -c "half4 liquidGlassEdgeOpacity" Islet/Notch/LiquidGlassShader.metal` returns 1
    - `grep -c "liquidGlassEdgeFalloff(position, size, topCornerRadius, bottomCornerRadius, borderWidth, blurWidth)" Islet/Notch/LiquidGlassShader.metal` returns 2 (called from both liquidGlassDistortion and liquidGlassEdgeOpacity)
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors
  </acceptance_criteria>
  <done>liquidGlassDistortion's numeric behavior is unchanged after the refactor (D-01 unaffected); liquidGlassEdgeOpacity compiles as a second stitchable colorEffect function sharing the identical falloff curve; project builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: LiquidGlassParameters edgeOpacity/centerOpacity/fringeOpacity fields + retuned constants</name>
  <files>Islet/Notch/LiquidGlassShader.swift</files>
  <read_first>
    Islet/Notch/LiquidGlassShader.swift (full file, current — LiquidGlassParameters struct and its .collapsed/.expanded static instances to extend)
    Islet/Notch/LiquidGlassShader.metal (Task 1's output — liquidGlassEdgeOpacity's exact parameter order/types this file's Shader-construction code must match)
    Islet/Notch/NotchPillView.swift lines 283-333 (current liquidGlassEffectLayer — read-only reference showing WHERE the hardcoded `Color.red/green/blue.opacity(0.10)` literals live today; Plan 35-07, not this plan, edits this file)
    .planning/phases/35-liquid-glass-material/35-CONTEXT.md — Decision D-11 and the "Retuning note" paragraph directly below it (backgroundOpacity 0.04/0.07 and the R/G/B fringe opacity 0.10 are stale, calibrated against an opaque base)
  </read_first>
  <action>
    In `Islet/Notch/LiquidGlassShader.swift`, add three new fields to `LiquidGlassParameters`: `var edgeOpacity: CGFloat` and `var centerOpacity: CGFloat` (D-11's opacity ramp — the alpha `liquidGlassEdgeOpacity` mixes between at the edge vs. the interior), and `var fringeOpacity: CGFloat` (replaces the hardcoded `Color.red/green/blue.opacity(0.10)` literals Plan 35-07 will update in NotchPillView.swift — single source of truth for retuning, grouping with backgroundOpacity per the CONTEXT.md retuning note's explicit callout of both as stale). Update `.collapsed` to `LiquidGlassParameters(borderWidth: 0.15, blurWidth: 2.5, distortionScale: -5, redOffset: 0, greenOffset: 0.5, blueOffset: 1, saturation: 1.0, backgroundOpacity: 0.05, edgeOpacity: 0.15, centerOpacity: 0.55, fringeOpacity: 0.15)` and `.expanded` to `LiquidGlassParameters(borderWidth: 0.11, blurWidth: 7, distortionScale: -13, redOffset: 0, greenOffset: 1.25, blueOffset: 2.5, saturation: 1.08, backgroundOpacity: 0.08, edgeOpacity: 0.20, centerOpacity: 0.70, fringeOpacity: 0.20)` — `.collapsed` stays visibly subtler than `.expanded` at every new field too (D-04); `backgroundOpacity`/the new `fringeOpacity` are bumped up from the pre-revision 0.04/0.07/0.10 values per the CONTEXT.md retuning note (those were calibrated against a fully opaque base and read as barely-visible now that the base itself is translucent) — these are on-device-tunable starting points, not final, the same Claude's-Discretion grant Plan 35-02's original values used, to be verified/adjusted during Plan 35-08's UAT. Add a doc comment above the 3 new fields citing D-10/D-11, explaining `edgeOpacity`/`centerOpacity` drive `liquidGlassEdgeOpacity`'s alpha ramp while `fringeOpacity` is the chromatic-fringe passes' own opacity (previously hardcoded inline in NotchPillView.swift, now centralized here). Update the file's existing top-of-file doc comment (the one explaining the `brightness` row's fold-in) to also note a SECOND colorEffect shader now exists (`liquidGlassEdgeOpacity`, added by this plan's Task 1) alongside the original distortion shader, so a future reader finds both from this file's header.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "var edgeOpacity: CGFloat" Islet/Notch/LiquidGlassShader.swift` returns 1
    - `grep -c "var centerOpacity: CGFloat" Islet/Notch/LiquidGlassShader.swift` returns 1
    - `grep -c "var fringeOpacity: CGFloat" Islet/Notch/LiquidGlassShader.swift` returns 1
    - `grep -c "edgeOpacity: 0.15, centerOpacity: 0.55, fringeOpacity: 0.15" Islet/Notch/LiquidGlassShader.swift` returns 1 (collapsed)
    - `grep -c "edgeOpacity: 0.20, centerOpacity: 0.70, fringeOpacity: 0.20" Islet/Notch/LiquidGlassShader.swift` returns 1 (expanded)
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors
  </acceptance_criteria>
  <done>LiquidGlassParameters.collapsed/.expanded both carry edgeOpacity/centerOpacity/fringeOpacity, collapsed values subtler than expanded at every field; project builds clean; neither field is consumed anywhere yet (Plan 35-07 is the consumer).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | Pure local GPU rendering — same trust boundary as Plan 35-02. No user-supplied numeric input, no network, no file I/O; only compile-time-fixed Swift static constants feed the shader. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-35-11 | Denial of Service | `liquidGlassEdgeFalloff` given a degenerate `size` (zero width/height) | mitigate | Inherits the original T-35-04 guard verbatim — the `size.y > 0.0 ? ... : 0.0` ternary in the extracted falloff function is unchanged by the refactor, so the zero-size case still resolves to a defined `t` value with no division-by-zero |
| T-35-12 | Tampering | `LiquidGlassParameters` hardcoded constants | accept | Same disposition as T-35-05 (Plan 35-02): `.collapsed`/`.expanded` remain hardcoded Swift static constants, never user-editable at runtime — no Settings control exposes raw shader numbers |
| T-35-SC | Tampering | npm/pip/cargo installs | n/a | Zero third-party packages this task — Metal/SwiftUI are first-party frameworks already linked |
</threat_model>

<verification>
- `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors after both tasks
- `liquidGlassDistortion`'s Steps 6-7 (direction + warp application) are textually unchanged by the refactor — only Steps 1-5 moved into the new shared helper
</verification>

<success_criteria>
- `Islet/Notch/LiquidGlassShader.metal` compiles with `liquidGlassEdgeFalloff` (shared) + `liquidGlassEdgeOpacity` (new colorEffect) alongside the unchanged-behavior `liquidGlassDistortion`
- `Islet/Notch/LiquidGlassShader.swift` compiles with `LiquidGlassParameters.collapsed`/`.expanded` carrying edgeOpacity/centerOpacity/fringeOpacity, retuned per the CONTEXT.md revision note
- Neither new function nor new field is called from anywhere yet (interface-first contract step) — Plan 35-07 is the consumer
</success_criteria>

<output>
Create `.planning/phases/35-liquid-glass-material/35-06-SUMMARY.md` when done
</output>
</output>
