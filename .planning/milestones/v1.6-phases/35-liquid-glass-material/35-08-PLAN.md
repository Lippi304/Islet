---
phase: 35-liquid-glass-material
plan: 8
type: execute
wave: 3
depends_on: ["35-06", "35-07"]
files_modified: []
autonomous: false
requirements: [GLASS-01]

must_haves:
  truths:
    - "The collapsed pill, expanded island, and all 3 activity wings render a genuinely translucent Liquid Glass look — visible edge warp, chromatic fringe, AND desktop-visible transparency at the rounded edge (D-10/D-11) — not the flat opaque grey the original 35-05 checkpoint rejected"
    - "The material composites cleanly through a full collapse<->expand cycle with no artifacts, no dropped frames, and no click-through/tap regression (ROADMAP Success Criteria #1, #3)"
    - "The user has visually approved the result on-device against reference-GlassSurface.md's technique AND reference-transparency-target.png's edge-transparency pattern (ROADMAP Success Criterion #4) — this is a subjective, human-only judgment call no automated check can substitute for"
    - "Fresh-install and existing-user default-to-Liquid-Glass behavior (D-06) and the Settings window's calmer background (D-08/D-09, unaffected by this remediation) are re-confirmed on-device"
  artifacts: []
  key_links: []
---

<objective>
Re-run the full Phase-25-style on-device UAT checklist (35-UAT.md's 7 tests, all blocked on Test 1 since the original 35-05 checkpoint rejection) now that the D-10/D-11 material-base remediation (Plans 35-06/35-07) has shipped. This plan SUPERSEDES the failed `35-05-PLAN.md` checkpoint — 35-05 is not re-run or reused; `35-UAT.md` remains as the historical record of the first rejection. This is the hard merge gate ROADMAP Success Criteria #3/#4 require before Phase 35 is considered done.

Purpose: GLASS-01 is a visual/material feature with no automated way to verify "looks right". The first attempt (35-05) failed because the material was opaque; Plans 35-06/35-07 fixed the root cause. This checkpoint verifies the fix actually reads as translucent glass on real hardware, not just that it compiles.
Output: On-device UAT confirmation (or a documented list of gaps for a further gap-closure round).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-liquid-glass-material/35-CONTEXT.md
@.planning/phases/35-liquid-glass-material/35-UAT.md
@.planning/phases/35-liquid-glass-material/35-UI-SPEC.md
@.planning/phases/35-liquid-glass-material/reference-GlassSurface.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: On-device UAT (round 2) — translucent Liquid Glass render, collapse/expand, all 3 wings, Settings picker + window background, D-06 default</name>
  <files>Islet/Notch/LiquidGlassShader.metal, Islet/Notch/LiquidGlassShader.swift, Islet/Notch/NotchPillView.swift, Islet/ActivitySettings.swift, Islet/SettingsView.swift</files>
  <read_first>
    .planning/phases/35-liquid-glass-material/35-UAT.md (the original 7-test checklist and the exact Test 1 failure this round re-verifies)
    .planning/phases/35-liquid-glass-material/reference-transparency-target.png (the user-supplied target screenshot — dark/readable center, desktop bleeding through at the rounded edge)
  </read_first>
  <action>Build and run Islet with Plans 35-01 through 35-04 and the new 35-06/35-07 remediation in place, then execute the 7-step on-device verification below. No code changes in this task — it is verification-only, the hard merge gate ROADMAP Success Criteria #3/#4 require before Phase 35 is done.</action>
  <what-built>
    Plans 35-01 through 35-04 shipped the original Liquid Glass material feature (Settings option, warp+fringe shader, Settings window background). The original on-device checkpoint (35-05) rejected the result as a flat opaque grey with no visible transparency. Plans 35-06/35-07 (this remediation round) fixed the root cause: (1) `Islet/Notch/LiquidGlassShader.metal` gained a shared `liquidGlassEdgeFalloff` helper and a new `liquidGlassEdgeOpacity` colorEffect shader; (2) `Islet/Notch/LiquidGlassShader.swift`'s `LiquidGlassParameters` gained `edgeOpacity`/`centerOpacity`/`fringeOpacity` fields with retuned starting values; (3) `Islet/Notch/NotchPillView.swift`'s `islandFill` `.liquidGlass` branch and `liquidGlassEffectLayer`'s own base layer now fill with a real translucent `.ultraThinMaterial` instead of the opaque `gradientMaterial`, with the new edge-weighted opacity shader ramping transparency from more-see-through at the rounded edge to more-opaque toward the center.
  </what-built>
  <how-to-verify>
    Open the project in Xcode: `/Users/lippi304/conductor/workspaces/notch/algiers/Islet.xcodeproj`. Select the `Islet` scheme and press Run (Cmd-R) to launch a Debug build.

    1. Hover over the notch to expand the island (Home view). Confirm the collapsed pill and the expanded island now show a VISIBLY TRANSLUCENT surface — you should be able to see a hint of your desktop wallpaper/whatever is behind the notch bleeding through, especially right at the rounded edge, getting darker/more opaque toward the center (compare against `reference-transparency-target.png`). Also confirm the warped/rippled edge and subtle chromatic fringe are still visible (D-01, unchanged from before). This is the exact check that failed in the first round — report precisely what you see if it still looks flat/opaque.
    2. Watch the collapse<->expand transition several times (hover in/out repeatedly). Confirm: no visual artifacts (flickering, torn edges, popping), no dropped-frame stutter, and the pill still morphs smoothly from its notch position with no diagonal jump or bounce.
    3. Play some audio (or let Now Playing show a track) to trigger the Now Playing wings, then plug/unplug the charger to trigger the Charging wings, then connect/disconnect a Bluetooth device (e.g. AirPods) to trigger the Device wings. Confirm all 3 wings show the same translucent warp + fringe as the pill/expanded island — collapsed pill's effect should read as noticeably SUBTLER than the wings/expanded island's (D-04).
    4. While any of the above is showing content (track title, equalizer bars, charging %, device name), confirm the TEXT AND ICONS stay perfectly crisp and legible against the now-translucent background — only the background material shows warp/transparency, never the foreground content (D-03). Report immediately if any text/icon looks smeared, distorted, or hard to read against the busier (non-flat-black) background.
    5. Click the menu-bar Islet icon to open Settings. Go to System (Theming). Confirm a 3rd "Liquid Glass" segment exists alongside "Gradient"/"Solid Black", and it is the one currently SELECTED (D-06 default). Switch between all 3 options one at a time, glancing at the notch island each time — confirm the island updates LIVE with no relaunch needed, and Gradient/Solid Black look exactly as they did before this phase (zero regression to the other 2 styles).
    6. While Settings is open on System (Theming), look at the Settings WINDOW's own background (not the notch island) — confirm it still shows the calmer frosted/blurred dark gradient with a faint rim-light edge highlight, NO warp/ripple distortion (D-09, this surface is untouched by this remediation), and all text/labels/toggles remain fully readable.
    7. Quit and relaunch Islet — confirm a fresh/default install shows Liquid Glass without the user having picked it (D-06's "new users" half); if this machine already has an existing saved `materialStyle` preference from before this phase, confirm your explicit choice was RESPECTED.
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 checks pass, or describe exactly which check(s) failed and what you saw (e.g. "check 1: still looks opaque, no wallpaper visible" or "check 3: Device wings show no fringe at all") so a further gap-closure plan can target the right file precisely.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | This plan performs no code changes — it is a human-verification gate over already-shipped local-rendering code from Plans 35-01 through 35-04 and 35-06/35-07 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-35-15 | n/a | This plan (checkpoint only) | n/a | No code artifacts produced by this plan; the threat surface is entirely inherited from Plans 35-01 through 35-04 and 35-06/35-07, whose own threat models already cover it |
</threat_model>

<verification>
- Human operator completes all 7 on-device checks and types "approved", OR reports specific gaps for a further gap-closure plan
</verification>

<success_criteria>
- ROADMAP Success Criteria #1-4 for Phase 35 are all confirmed true on-device, including the translucency the original 35-05 round rejected
- The 35-UI-SPEC.md "On-Device UAT Gate" checklist's 7 items all pass
- If any check fails, the specific failure is captured precisely enough to scope a further `/gsd:plan-phase 35 --gaps` gap-closure plan without re-investigation
</success_criteria>

<output>
Create `.planning/phases/35-liquid-glass-material/35-08-SUMMARY.md` when done
</output>
