---
phase: 35-liquid-glass-material
plan: 7
type: execute
wave: 2
depends_on: ["35-06", "35-03"]
files_modified:
  - Islet/Notch/NotchPillView.swift
autonomous: true
requirements: [GLASS-01]

must_haves:
  truths:
    - "islandFill's .liquidGlass branch (D-10) returns a real translucent Material-based ShapeStyle (.ultraThinMaterial) instead of the opaque gradientMaterial, while .gradient/.solidBlack remain byte-for-byte unchanged"
    - "liquidGlassEffectLayer's own base warp layer (D-10) fills with the SAME translucent Material instead of gradientMaterial, so the existing distortion shader visibly warps an actual translucent surface — a refraction of what's behind — not a flat opaque color"
    - "The translucent base's opacity is edge-weighted via the new liquidGlassEdgeOpacity colorEffect shader (D-11): more transparent right at the rounded edge, more opaque toward the interior, using LiquidGlassParameters.edgeOpacity/centerOpacity"
    - "The 3 chromatic-fringe passes read parameters.fringeOpacity instead of the hardcoded 0.10 literal, so Plan 35-06's retuned values take effect"
    - "matchedGeometryEffect still precedes .frame at all 4 sites, byte-for-byte unchanged (island-expand-diagonal-bounce regression guard) — this plan touches only liquidGlassEffectLayer's body and the islandFill switch, never the 4 call sites' own modifier chains"
  artifacts:
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "islandFill .liquidGlass branch returning translucent Material; liquidGlassEffectLayer using translucent base + edge-weighted colorEffect opacity ramp + parameters.fringeOpacity"
  key_links:
    - from: "Islet/Notch/NotchPillView.swift liquidGlassEffectLayer"
      to: "Islet/Notch/LiquidGlassShader.metal liquidGlassEdgeOpacity"
      via: ".colorEffect(...) shader call chained after .distortionEffect(...)"
      pattern: "liquidGlassEdgeOpacity"
    - from: "Islet/Notch/NotchPillView.swift liquidGlassEffectLayer"
      to: "Islet/Notch/LiquidGlassShader.swift LiquidGlassParameters.edgeOpacity/centerOpacity"
      via: "direct field access as Shader arguments"
      pattern: "parameters\\.edgeOpacity"
---

<objective>
Wire Plan 35-06's translucent-material contract into `NotchPillView.swift` per D-10/D-11: swap the opaque `gradientMaterial` base for a real translucent `.ultraThinMaterial` at BOTH locations the on-device UAT rejection traced the bug to (`islandFill`'s `.liquidGlass` branch AND `liquidGlassEffectLayer`'s own base warp layer), and layer the new edge-weighted opacity ramp on top so the effect reads as glass — transparent at the rounded edge, more opaque toward the center — matching the user-supplied `reference-transparency-target.png`. This is the plan that makes the material actually translucent on-device.

Purpose: Close the 35-UAT.md Test 1 gap ("flat opaque grey... nicht dass man durchgucken kann") by fixing the root cause D-10/D-11 diagnosed — distorting a uniform opaque color is indistinguishable from not distorting it — without touching the matchedGeometryEffect/frame sequencing Plan 35-03 already wired correctly.
Output: `islandFill`'s `.liquidGlass` branch translucent; `liquidGlassEffectLayer`'s base layer translucent + edge-weighted opacity ramp; fringe passes reading `parameters.fringeOpacity`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-liquid-glass-material/35-CONTEXT.md
@.planning/phases/35-liquid-glass-material/35-UAT.md
@.planning/phases/35-liquid-glass-material/35-PATTERNS.md
@.planning/phases/35-liquid-glass-material/35-UI-SPEC.md
@.planning/phases/35-liquid-glass-material/reference-GlassSurface.md
</context>

<interfaces>
From Islet/Notch/LiquidGlassShader.swift (Plan 35-06's output, already merged):
LiquidGlassParameters — gains edgeOpacity: CGFloat, centerOpacity: CGFloat, fringeOpacity: CGFloat fields (D-11), retuned .collapsed/.expanded static instances
LiquidGlassChannelShaders — unchanged (base/red/green/blue Shader)
func liquidGlassChannelShaders(size:topCornerRadius:bottomCornerRadius:parameters:) -> LiquidGlassChannelShaders — unchanged signature

From Islet/Notch/LiquidGlassShader.metal (Plan 35-06's output, already merged):
[[ stitchable ]] half4 liquidGlassEdgeOpacity(float2 position, half4 color, float2 size, float topCornerRadius, float bottomCornerRadius, float borderWidth, float blurWidth, float edgeOpacity, float centerOpacity) — new colorEffect function, looked up via ShaderLibrary.default (name: "liquidGlassEdgeOpacity")

From Islet/Notch/NotchPillView.swift (Plan 35-01/35-03's output, current — being modified by this plan):
private var islandFill: AnyShapeStyle — exhaustive switch, .liquidGlass branch currently `AnyShapeStyle(Self.gradientMaterial)`
private func liquidGlassEffectLayer(shape: NotchShape, size: CGSize, parameters: LiquidGlassParameters) -> some View — already applied at all 4 call sites by Plan 35-03; this plan edits ONLY this function's body and the islandFill switch, never the 4 call sites themselves
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: islandFill .liquidGlass branch — translucent Material (D-10, location 1 of 2)</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift lines 242-273 (islandFill's current switch — gradientMaterial/solidBlackMaterial constants and the exact `.liquidGlass` case this task changes)
    .planning/phases/35-liquid-glass-material/35-CONTEXT.md — Decision D-10 (translucent base for `.liquidGlass` only; `.gradient`/`.solidBlack` untouched; D-02 explicitly superseded)
  </read_first>
  <action>
    In `islandFill`'s switch (`Islet/Notch/NotchPillView.swift`), change the `.liquidGlass` case from `return AnyShapeStyle(Self.gradientMaterial)` to `return AnyShapeStyle(.ultraThinMaterial)` — a real, live-blurring SwiftUI `Material` (NSVisualEffectView-backed) per D-10, replacing the opaque black gradient this branch previously duplicated from `.gradient`. Do not touch the `.gradient`/`.solidBlack` cases — they keep returning `Self.gradientMaterial`/`Self.solidBlackMaterial` exactly as before, zero regression to those two styles. Update the `.liquidGlass` case's doc comment (previously citing D-02 and explaining why this branch matched `.gradient`) to cite D-10 instead, noting D-02 is superseded by the 2026-07-16 revision and this is now a genuinely translucent base, not a duplicate of `.gradient`'s opaque fill.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A1 "case .liquidGlass:" Islet/Notch/NotchPillView.swift | grep -c "AnyShapeStyle(.ultraThinMaterial)"` returns 1
    - `grep -c "case .gradient: return AnyShapeStyle(Self.gradientMaterial)" Islet/Notch/NotchPillView.swift` returns 1 (unchanged)
    - `grep -c "case .solidBlack: return AnyShapeStyle(Self.solidBlackMaterial)" Islet/Notch/NotchPillView.swift` returns 1 (unchanged)
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors
  </acceptance_criteria>
  <done>islandFill's .liquidGlass branch returns a translucent .ultraThinMaterial ShapeStyle; .gradient/.solidBlack are byte-for-byte unchanged; project builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: liquidGlassEffectLayer — translucent base + edge-weighted opacity + fringeOpacity wiring (D-10 location 2, D-11)</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift lines 275-333 (liquidGlassEffectLayer's current full body — the 4-layer ZStack this task rewrites: base warp layer, 3 chromatic-fringe layers, saturation/frost/clip/hit-testing modifiers)
    Islet/Notch/LiquidGlassShader.swift (Plan 35-06's output — LiquidGlassParameters.edgeOpacity/centerOpacity/fringeOpacity, liquidGlassChannelShaders(...))
    Islet/Notch/LiquidGlassShader.metal (Plan 35-06's output — liquidGlassEdgeOpacity's exact parameter order/types)
    .planning/phases/35-liquid-glass-material/35-CONTEXT.md — Decisions D-10, D-11, and the "Existing (first-attempt) implementation" canonical refs note identifying NotchPillView.swift's overlay base fill as the second of the two opaque-base locations
    .planning/phases/35-liquid-glass-material/35-UAT.md — Test 1 (the exact rendering failure this task fixes)
  </read_first>
  <action>
    Rewrite `liquidGlassEffectLayer`'s body in `Islet/Notch/NotchPillView.swift` to fix the two compounding opacity bugs D-10/D-11 diagnosed: (1) the base warp layer's own fill, and (2) the fringe passes' hardcoded opacity. Change the ZStack's first layer from `shape.fill(Self.gradientMaterial).distortionEffect(shaders.base, maxSampleOffset: CGSize(width: abs(parameters.distortionScale), height: abs(parameters.distortionScale)))` to the same `.distortionEffect(...)` call but starting from `shape.fill(.ultraThinMaterial)` instead of `shape.fill(Self.gradientMaterial)`, then chain `.colorEffect(Shader(function: .init(library: .default, name: "liquidGlassEdgeOpacity"), arguments: [.float2(size), .float(shape.topCornerRadius), .float(shape.bottomCornerRadius), .float(parameters.borderWidth), .float(parameters.blurWidth), .float(parameters.edgeOpacity), .float(parameters.centerOpacity)]))` immediately after `.distortionEffect(...)` (warp first, then the edge-weighted opacity ramp applied to the already-warped result) — this applies D-11's `liquidGlassEdgeOpacity` shader with the SAME `size`/`topCornerRadius`/`bottomCornerRadius`/`borderWidth`/`blurWidth` values already used to build `shaders.base`, plus the new `parameters.edgeOpacity`/`parameters.centerOpacity` fields from Plan 35-06. Change all 3 chromatic-fringe layers' hardcoded `Color.red.opacity(0.10)` / `Color.green.opacity(0.10)` / `Color.blue.opacity(0.10)` to `Color.red.opacity(parameters.fringeOpacity)` / `Color.green.opacity(parameters.fringeOpacity)` / `Color.blue.opacity(parameters.fringeOpacity)` respectively (Plan 35-06's new field, single source of truth replacing the literal). Leave the ZStack's trailing `.saturation(parameters.saturation)`, `.overlay(Color.white.opacity(parameters.backgroundOpacity))`, `.clipShape(shape)`, and `.allowsHitTesting(false)` modifiers exactly as they are today — those four stay unchanged. Update the function's header doc comment to cite D-10/D-11 (translucent base + edge-weighted opacity ramp) alongside the existing D-01/D-02/D-03/D-04 references (note D-02 superseded), and explicitly state that `shape.fill(.ultraThinMaterial)` is now the actual visible surface of the effect — `islandFill`'s own `.liquidGlass` branch (Task 1 of this plan) sits underneath this overlay and is visually redundant there, but is kept translucent too for consistency and defensiveness, per D-10's instruction to fix both locations. Also add a one-line note acknowledging D-11's "opacity/blur" phrasing: this task implements the spatially-varying OPACITY ramp (verifiable, drives the observable "desktop bleeds through at the edge" result); SwiftUI has no primitive for a spatially-varying blur RADIUS within a single Material fill, so blur intensity itself stays uniform (the `.ultraThinMaterial` choice already supplies live blur at all, unlike the fully opaque prior state) — flag any on-device gap here for Plan 35-08's UAT rather than preempting it.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "shape.fill(.ultraThinMaterial)" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "name: \"liquidGlassEdgeOpacity\"" Islet/Notch/NotchPillView.swift` returns 1
    - `grep -c "parameters.edgeOpacity" Islet/Notch/NotchPillView.swift` returns at least 1
    - `grep -c "parameters.centerOpacity" Islet/Notch/NotchPillView.swift` returns at least 1
    - `grep -c "opacity(parameters.fringeOpacity)" Islet/Notch/NotchPillView.swift` returns 3 (red, green, blue)
    - `grep -c "opacity(0.10)" Islet/Notch/NotchPillView.swift` returns 0 (old hardcoded fringe literal fully replaced)
    - `grep -c "\.allowsHitTesting(false)" Islet/Notch/NotchPillView.swift` returns at least 1 (unchanged, still present)
    - `grep -A1 "\.matchedGeometryEffect(id: \"island\", in: ns)" Islet/Notch/NotchPillView.swift | grep -c "\.frame("` returns 4 (regression guard — this plan must not touch the 4 call sites' own modifier chains)
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors
  </acceptance_criteria>
  <done>liquidGlassEffectLayer's base layer fills with translucent .ultraThinMaterial, warped by the existing distortion shader then alpha-ramped by the new liquidGlassEdgeOpacity colorEffect (edge more transparent, center more opaque, D-11); all 3 fringe passes read parameters.fringeOpacity; matchedGeometryEffect-before-frame ordering untouched at all 4 sites; project builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | Pure local rendering, same trust boundary as Plan 35-03 — no network, no external input, no new data storage. The only "input" is the already-local `materialStyle` enum and Plan 35-06's compile-time-fixed parameter constants. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-35-13 | Denial of Service | Adding a `.colorEffect(...)` pass on top of the existing 4x `.distortionEffect(...)` fill layers increases per-frame GPU cost during collapse<->expand morph | mitigate | Effect area stays small (32-420pt, never full-screen); Plan 35-08's on-device UAT hard gate explicitly re-checks for dropped frames before merge, same as Plan 35-03's T-35-06; fallback if it regresses is reducing layer count/opacity, not shipping a janky morph |
| T-35-14 | Spoofing (UI) | `.allowsHitTesting(false)` accidentally dropped while rewriting liquidGlassEffectLayer's body | mitigate | Task 2's own acceptance criteria re-greps `.allowsHitTesting(false)` after the rewrite, mirroring Plan 35-03's T-35-07 guard; Plan 35-08's UAT re-verifies tap-to-toggle still works with Liquid Glass selected |
| T-35-SC | Tampering | npm/pip/cargo installs | n/a | Zero third-party packages this task |
</threat_model>

<verification>
- `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors after both tasks
- `grep -A1 "\.matchedGeometryEffect(id: \"island\", in: ns)" Islet/Notch/NotchPillView.swift | grep -c "\.frame("` returns 4 (the ordering regression guard, re-checked at plan level)
- `grep -c "opacity(0.10)" Islet/Notch/NotchPillView.swift` returns 0 (fringe opacity fully centralized to LiquidGlassParameters)
</verification>

<success_criteria>
- `.liquidGlass` renders a genuinely translucent, edge-weighted-transparency surface at all 4 island-shell fill sites — not a flat opaque grey/black panel
- `.gradient`/`.solidBlack` remain pixel-identical to before this plan (zero regression)
- matchedGeometryEffect precedes .frame at all 4 sites, unchanged from before this plan
- All chromatic-fringe opacity and edge/center opacity values are driven by `LiquidGlassParameters`, no hardcoded literals remain in `liquidGlassEffectLayer`
</success_criteria>

<output>
Create `.planning/phases/35-liquid-glass-material/35-07-SUMMARY.md` when done
</output>
</output>
