---
phase: 35-liquid-glass-material
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - Islet/ActivitySettings.swift
  - Islet/Notch/NotchPillView.swift
autonomous: true
requirements: [GLASS-01]

must_haves:
  truths:
    - "ActivitySettings.MaterialStyle has a third case, liquidGlass, alongside the existing gradient/solidBlack (D-05) — additive, neither existing case removed. Scaffolding step: compiles cleanly, no visible effect until Plan 35-03 wires the shader in."
    - "The EnvironmentKey fallback default (IslandMaterialStyleKey.defaultValue) is .liquidGlass, so NotchPillView renders Liquid Glass correctly even before the controller wires the real @AppStorage value (D-06, half 1 of 2 — SettingsView.swift's own default is Plan 35-04's job). Scaffolding step: ready for Plan 35-03 to consume, no visible effect on its own."
    - "NotchPillView's islandFill switch is exhaustive and compiles with the new case, returning the SAME gradientMaterial base as .gradient (D-02: the base gradient direction is never replaced, only later composited with a shader in Plan 35-03). Scaffolding step: identical rendering to .gradient until Plan 35-03 layers the shader overlay on top."
  artifacts:
    - path: "Islet/ActivitySettings.swift"
      provides: "MaterialStyle.liquidGlass case + IslandMaterialStyleKey.defaultValue = .liquidGlass"
    - path: "Islet/Notch/NotchPillView.swift"
      provides: "islandFill switch's .liquidGlass branch (returns Self.gradientMaterial, same as .gradient)"
  key_links:
    - from: "Islet/Notch/NotchPillView.swift islandFill"
      to: "Islet/ActivitySettings.swift MaterialStyle"
      via: "exhaustive switch over materialStyle"
      pattern: "case \\.liquidGlass:"
---

<objective>
Add `.liquidGlass` as a third case on `ActivitySettings.MaterialStyle` (D-05) and flip the `IslandMaterialStyleKey` environment default to it (D-06, the EnvironmentKey half — `SettingsView.swift`'s own `@AppStorage` default is a separate location, flipped in Plan 35-04). Extend `NotchPillView.islandFill`'s exhaustive switch with the new case, keeping the SAME `gradientMaterial` base `.gradient` already uses (D-02) — this is the real, final value for this switch, not a placeholder; the distortion/chromatic-fringe effect that makes Liquid Glass visually distinct is layered on top via a separate overlay in Plan 35-03, not through this switch branch.

Purpose: Establish the `MaterialStyle` contract every other Phase 35 plan builds against, without leaving the codebase in a non-compiling state at any point in this plan.
Output: `MaterialStyle.liquidGlass` case, `IslandMaterialStyleKey.defaultValue = .liquidGlass`, `islandFill`'s new exhaustive branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-liquid-glass-material/35-CONTEXT.md
@.planning/phases/35-liquid-glass-material/35-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MaterialStyle.liquidGlass case + EnvironmentKey default flip</name>
  <files>Islet/ActivitySettings.swift</files>
  <read_first>
    Islet/ActivitySettings.swift (full file — the `MaterialStyle` enum at lines 39-46, its doc comment convention at lines 39-42 mirroring the D-XX-referencing style used at lines 19-21/31-33, and the `IslandMaterialStyleKey`/`islandMaterialStyle` EnvironmentKey plumbing at lines 108-129)
    .planning/phases/35-liquid-glass-material/35-CONTEXT.md — Decisions D-05, D-06
  </read_first>
  <action>
    In the `MaterialStyle` enum (`Islet/ActivitySettings.swift:43-45`), change `case gradient, solidBlack` to `case gradient, solidBlack, liquidGlass` — additive, both existing cases untouched. Update the doc comment immediately above (lines 39-42) to mention the new case and cite D-05, following this file's established `// Phase NN / ID (D-XX):` comment convention (e.g. matching the style at lines 39-42 verbatim, extended to name `liquidGlass` as the third option and reference `35-CONTEXT.md`). In `IslandMaterialStyleKey` (`Islet/ActivitySettings.swift:108-110`), change `static let defaultValue: MaterialStyle = .gradient` to `static let defaultValue: MaterialStyle = .liquidGlass`, citing D-06 in an adjacent comment (this is the EnvironmentKey fallback used before the controller wires the real `@AppStorage` value — the `@AppStorage` declaration itself lives in `SettingsView.swift:48`, a SEPARATE default flipped in Plan 35-04; both must end up `.liquidGlass` per D-06's "both new AND existing users" requirement, and this task covers only this file's copy).
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "case gradient, solidBlack, liquidGlass" Islet/ActivitySettings.swift` returns 1
    - `grep -c "defaultValue: MaterialStyle = .liquidGlass" Islet/ActivitySettings.swift` returns 1
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` fails at this point ONLY on `Islet/Notch/NotchPillView.swift`'s `islandFill` switch statement (a "switch must be exhaustive" compiler error naming the missing `.liquidGlass` case) — Task 2 fixes this; confirm no OTHER file reports an error
  </acceptance_criteria>
  <done>MaterialStyle has 3 cases; IslandMaterialStyleKey.defaultValue is .liquidGlass; the only build error is NotchPillView.swift's expected non-exhaustive switch.</done>
</task>

<task type="auto">
  <name>Task 2: islandFill exhaustive-switch branch for .liquidGlass</name>
  <files>Islet/Notch/NotchPillView.swift</files>
  <read_first>
    Islet/Notch/NotchPillView.swift lines 242-268 (gradientMaterial/solidBlackMaterial static constants and the islandFill switch they feed — the single seam ROADMAP Success Criterion #2 requires every fill site to keep reading from)
    .planning/phases/35-liquid-glass-material/35-CONTEXT.md — Decision D-02 (the gradient base is preserved, never replaced, by the new material)
    .planning/phases/35-liquid-glass-material/35-PATTERNS.md — "Material base + branch pattern" section (both structurally-valid options and why the switch itself should NOT gain a distinct AnyShapeStyle value for liquidGlass)
  </read_first>
  <action>
    In `islandFill`'s switch (`Islet/Notch/NotchPillView.swift:263-268`), add `case .liquidGlass: return AnyShapeStyle(Self.gradientMaterial)` — literally the SAME return expression as the existing `.gradient` case, per D-02 (the shader/fringe composite that visually distinguishes Liquid Glass from plain Gradient is added as a separate overlay layer in Plan 35-03, applied at the same 4 call sites AFTER `.frame(...)`, not as a different value returned from this switch). Add a one-line comment above the new case citing D-02 and Plan 35-03 so a future reader understands why this branch looks identical to `.gradient` instead of introducing a new `AnyShapeStyle`.
  </action>
  <verify>
    <automated>xcodebuild build -scheme Islet -destination 'platform=macOS'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A1 "case .liquidGlass:" Islet/Notch/NotchPillView.swift | grep -c "gradientMaterial"` returns 1
    - `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors
  </acceptance_criteria>
  <done>islandFill is exhaustive over all 3 MaterialStyle cases; .liquidGlass returns the same gradientMaterial as .gradient; full project builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | Pure local enum/config change — no network, no external input, no new data storage. `MaterialStyle` is only ever set via the Settings Picker (Plan 35-04), a closed set of 3 tags, never free text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-35-01 | Tampering | `@AppStorage(materialStyleKey)` / `UserDefaults` corruption | accept | Already-established project pattern: SwiftUI's native `RawRepresentable where RawValue == String` `@AppStorage` overload falls back to the declared default on any unparseable value (see `SettingsView.swift:43-47` comment) — no crash path, no new risk introduced by adding a 3rd case |
| T-35-SC | Tampering | npm/pip/cargo installs | n/a | Zero third-party packages this task |
</threat_model>

<verification>
- `xcodebuild build -scheme Islet -destination 'platform=macOS'` succeeds with zero errors after both tasks
</verification>

<success_criteria>
- `ActivitySettings.MaterialStyle` has exactly 3 cases: `gradient`, `solidBlack`, `liquidGlass`
- `IslandMaterialStyleKey.defaultValue` is `.liquidGlass`
- `islandFill` is exhaustive and its `.liquidGlass` branch matches `.gradient`'s return value exactly (D-02)
- Full project builds clean with zero errors
</success_criteria>

<output>
Create `.planning/phases/35-liquid-glass-material/35-01-SUMMARY.md` when done
</output>
