---
milestone: 1.0.1
audited: 2026-07-04T16:20:00Z
status: tech_debt
scores:
  requirements: 2/2
  phases: 3/3
  integration: 3/3
  flows: 2/2
gaps: {}
tech_debt:
  - phase: 09-fullscreen-flash-window-space-retry
    items:
      - "CR-01: `AppDelegate.quit()` calls `NSApp.terminate(nil)` directly without nil-ing `notchController`, so `NotchWindowController.deinit` (and the `CGSSpace` teardown — `CGSHideSpaces`/`CGSSpaceDestroy`) never runs on a normal 'Quit Islet'. The dedicated max-level CGS Space leaks in WindowServer on every ordinary app quit, accumulating across quit/relaunch cycles. Does not affect the flash-elimination goal itself (confirmed by both 09-VERIFICATION.md and the milestone integration check) — a resource-management follow-up."
      - "WR-01/WR-02 (info): `CGSSpace.swift` has no validation of CGS private-API return values, and assumes `Int`/`Int32` width fits `CGSSpaceSetAbsoluteLevel`'s single passed value. Low severity — current on-device behavior unaffected."
  - phase: 07-now-playing-progress-bar
    items:
      - "IN-01/IN-02 (info, from 07-REVIEW.md): magic-number extraction and untested View-layer math in `ProgressBar`. Explicitly deferred as info-level, non-blocking."
  - phase: 08-fullscreen-enter-flash-elimination
    items:
      - "Pre-existing (not caused by any v1.0.1 phase): `xcodebuild test` hangs in non-interactive/sandboxed worktree-agent sessions due to a Bluetooth TCC-authorization wait in `BluetoothMonitor`/`IOBluetoothCoreBluetoothCoordinator` (from v1.0 Phase 6). Logged in `.planning/phases/09-fullscreen-flash-window-space-retry/deferred-items.md`. Makes `xcodebuild test` unusable from any non-interactive environment; does not affect on-device or interactive Xcode runs."
---

# Milestone v1.0.1 Audit — Pre-Release Polish

**Audited:** 2026-07-04T16:20:00Z
**Phases:** 7, 8, 9 (all complete, all disk-status complete)
**Status: tech_debt** — both requirements satisfied, integration sound, no blockers; one real (already-flagged, non-blocking) resource leak and a few info-level items should be reviewed before shipping.

## Requirements Coverage (3-source cross-reference)

| Requirement | Phase | REQUIREMENTS.md | VERIFICATION.md | SUMMARY frontmatter | Final Status |
|-------------|-------|------------------|------------------|----------------------|---------------|
| PBAR-01 | 7 | `[x]` Complete | passed / SATISFIED | `requirements-completed: [PBAR-01]` (07-01) | **satisfied** |
| FS-01 | 8 (escalated) → 9 (resolved) | `[ ]` Pending (stale — needs update at close) | Phase 8: ESCALATED/OPEN (documented override, pre-authorized terminal state) → Phase 9: passed / SATISFIED | `requirements-completed: [FS-01]` (09-01) | **satisfied** (resolved by Phase 9) |

No orphaned requirements. Both v1.0.1 requirements are satisfied by code, verified against the live codebase (not just SUMMARY claims), with on-device human verification recorded for both.

**Note:** REQUIREMENTS.md's traceability table and checkbox for FS-01 still read "Pending" — this is expected staleness that the milestone-complete step (archiving REQUIREMENTS.md with outcomes) resolves; not a gap in the underlying work.

## Phase Verification Summary

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 7 — Now Playing Progress Bar | passed | 7/7 truths | Clean; only doc-staleness noted |
| 8 — Fullscreen-Enter Flash Elimination | passed (1 override) | 9/9 (1 via override) | Terminates in a documented escalation per ROADMAP's own pre-authorized terminal state — correct, not a failure |
| 9 — Fullscreen Flash Window/Space Retry | passed | 8/8 | Resolves FS-01; 4 downstream plans correctly no-op via cascading Task-0 guards |

## Cross-Phase Integration & E2E Flows

Independent integration check (gsd-integration-checker, live source read of `NotchWindowController.swift`, `NotchPanel.swift`, `CGSSpace.swift`, `AppDelegate.swift`, `NotchPillView.swift`, `NowPlayingState.swift`, `NowPlayingPresentation.swift`):

- **Now Playing expanded view (144pt + ProgressBar) vs. Phase 9's CGS Space join:** WIRED, no coupling. `expandedSize` drives both panel-frame geometry and SwiftUI layout independently of Space membership; the one-time `notchSpace.windows.insert(panel)` never touches frame/content.
- **Fullscreen hide/restore with Now Playing active:** WIRED. `updateVisibility()` is content-agnostic and unmodified by Phase 9's Space-join logic; behavior is identical to pre-Phase-9 for hide/show timing.
- **Phase 8 residue:** None — zero exploratory code remains in any shared file; `NotchPanel.swift` predates Phases 8/9 entirely.
- **CR-01 leak vs. Phase 7 flow:** Confirmed no interaction — Phase 7 has no window/Space lifecycle touchpoints.

**Verdict: integration sound.** No new blockers found beyond the already-tracked CR-01.

## Nyquist Compliance

| Phase | VALIDATION.md | Compliant | Notes |
|-------|----------------|------------|-------|
| 7 | exists | true | wave_0_complete: true |
| 8 | exists | false (PARTIAL) | Expected — phase terminated in escalation, no fix shipped to validate |
| 9 | exists | true | wave_0_complete: true |

No action required — Phase 8's partial Nyquist status reflects its correct escalation outcome, not a gap.

## Recommendation

Both v1.0.1 requirements (PBAR-01, FS-01) are satisfied and independently verified against the live codebase. Integration across phases 7-9 is sound. The one concrete follow-up (CR-01 CGS Space leak on quit) is non-blocking and already flagged — proceeding to `/gsd:complete-milestone 1.0.1` is reasonable, with CR-01 tracked as backlog/tech debt for a `/gsd:quick` fix or early v1.1 housekeeping item.
