import XCTest

// Placeholder so the IsletTests target has at least one source file for the
// initial `xcodegen generate`. Real suites (NotchGeometryTests / DisplayResolverTests)
// are added in Tasks 2 and 3; this file is removed once a real test file exists.
final class PlaceholderTests: XCTestCase {}
